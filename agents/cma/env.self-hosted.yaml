# Self-hosted environment: agent loop runs on Anthropic, tools execute on YOUR machine
# (worker.py with workdir = local fx-ai-trader checkout). Reaches local sqlite-fx,
# MASSIVE parquet, BT scripts, app.py, watchdog via the worker's bash tool.
name: fxai-selfhosted
config:
  type: self_hosted
