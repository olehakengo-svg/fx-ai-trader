---
id: 20260505-1602-w4-meta-C-15m-eur_usd
title: "[W4-Meta C-mini] EUR_USD_15m parquet 生成 (single pair)"
owner: codex
status: queued
priority: P0
created_at: 2026-05-05T16:02:00+0900
roadmap_gate: "W4-Redesign 15m cache 生成 — split into per-pair to dodge container restart cycle"
rule: R3
prereq_artifacts:
  - tools/bt_data_cache.py
  - modules/data.py
related:
  - knowledge-base/wiki/lessons/feedback_bt_must_use_massive.md
---

# 0. なぜこのタスクか

`EUR_USD_15m.parquet` が worker 側 cache に欠落。MASSIVE_API_KEY は user が設定済み (2026-05-05)。

W4-Meta C を per-pair に分割した理由: 元の C は 6 pair × 4500 日 fetch = 60-120 分の長時間タスクで Render container restart cycle (~1-3h) と衝突して orphan 連発。1 pair なら ~5-10 分で完結予定。

# 1. 仕様

## Step 1: API 経路試行

```python
import os
from modules.data import fetch_ohlcv_massive
import pandas as pd

if not os.environ.get("MASSIVE_API_KEY"):
    print("MASSIVE_API_KEY missing — fallback to resample")
    # Step 2 へ
else:
    df = fetch_ohlcv_massive("EUR_USD", "15m", days=4500)  # ~12 年
    df.to_parquet("data/cache/massive/EUR_USD_15m.parquet")
```

## Step 2: 5m → resample fallback (API 不可時)

```python
df_5m = pd.read_parquet("data/cache/massive/EUR_USD_5m.parquet")
df_15m = (df_5m.resample("15min", label="right", closed="right")
                .agg({"Open":"first","High":"max","Low":"min","Close":"last","Volume":"sum"})
                .dropna())
df_15m.to_parquet("data/cache/massive/EUR_USD_15m.parquet")
```

## Step 3: 検証

- N (bar count) >= 1000
- start_date < 2024-01-01
- end_date > 2026-04-01
- OHLC 値 NaN なし

# 2. Acceptance

- `data/cache/massive/EUR_USD_15m.parquet` 存在
- 検証 pass
- API/resample のいずれを使ったかコミットメッセージに明記

# 3. Out of Scope

- 他 pair の生成 (別 task)
- BT 検証
- Shadow promote

# 4. Notes

- 1 pair 専用なので軽量、container restart 前に完結する想定
- MASSIVE_API_KEY 設定済みなので API 経路優先 (resample は backup)


## Result (2026-05-05T05:54:53Z)

exit=0 · 218s

```
完了しました。

`data/cache/massive/EUR_USD_15m.parquet` を生成し、commit 済みです。

- Commit: `e3763b5 Add EUR_USD 15m Massive API cache`
- 経路: Massive API paginated chunk fetch
- Resample fallback: 未使用
- 検証:
  - `N=305414`
  - `start=2014-01-05T22:00:00+00:00`
  - `end=2026-05-05T05:45:00+00:00`
  - `OHLC_NaN=0`
  - 全 acceptance checks pass

補足: 既存の `fetch_ohlcv_massive("EUR_USD", "15m", days=4500)` は API 自体は成功しましたが、30 page 上限で `2018-01-22` までしか取得できず `end_date > 2026-04-01` に fail したため、同じ Massive API を date chunk + pagination 完走で取得し直しました。
```
