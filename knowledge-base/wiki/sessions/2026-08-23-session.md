# Session Log: 2026-08-23

## セッションで行ったこと（時系列）

### Phase 1: autopilot — 残存デプロイ churn の実測分解と恒久ゼロ化 (rule:R3)

**起点**: roadmap v2.3 は R1 待ち/監視のみ、pre-reg registry も新規 TRIGGERED ゼロ
(t5 は外部一次情報 + user R1 待ちで着手不可)。実行可能で M1 (摩擦削減) に直結する
項目として、PR #199 (phase-1) の残存 churn を選択。

1. **phase-1 効果を実測で確定** — Render deploy 一覧 (MCP) と main の commit 列を突合。
   マージ後 31 commit / 実デプロイ 4 = **1.7/日** (baseline 18.8 から −91%)。
   §4.2 の推定 7.9/日 は保守側に外れていた。
2. **残存源の全量分解** — ローカル再生が Render 実績と **4/4 完全一致**したので網羅性が
   担保できた。(a) analyst-memory.md 3/平日、(b) bt-results/phase1b + data/sentiment 1/日、
   (c) raw/cell_deepdive/ 不定。
3. **(a) の再分類** — 全数 grep で `analyst-memory.md` の read 元は `/api/analyst-opinion`
   (人手起動) のみ、取引パス参照ゼロと確定。「ランタイム read」の 2 値分類を
   **取引パス / 助言専用 / write-only** の 3 階層へ改訂し、助言専用を ignore 可に。
   代償 (陳腐化) は `memory_stale_days` テレメトリで観測可能にした。
4. **🔴 guard 自体の欠陥を発見** — `_ignored_paths()` の regex がリスト内コメント行で
   打ち切られ、以降の entry が検査から消えていた。`data/**` 誤 ignore の counterfactual を
   注入しても 6 テスト全て pass = 検査の無力化。修正 + 専用 pin テスト追加。
   修正後は同 counterfactual で 4 テストが落ちることを確認 (非 vacuous)。
5. **drift guard を非 KB ルートへ拡張** — `data` / `bt-results` 起点リテラルも走査。
6. **効果**: 同一 31 commit 窓で would-deploy **4 → 0**。過剰抑制の対照として直近
   276 commit では 32 commit が依然デプロイを起こすことを確認 (コード/設定/ランタイムデータ)。

成果物: `render.yaml` / `app.py` / `tests/test_render_build_filter.py` (6 tests) /
[[deploy-churn-trading-gap-2026-08-21]] §8 / changelog

## コミット一覧（自動記録）
1. data(bt): phase1b daily re-run 2026-08-23 (197d window, verdict=NULL) (335a2c3b, 2 files)

## 未解決事項
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 (別セッション task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 (別セッション task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立。成立日に runbook §3 を直接執行
- [ ] **MoF 月次介入額の公表チェック (~08-29)** — `mof-monthly-total-2026-08-29-check` (deadline 08-28)。着地当日に user 報告
- [ ] **E23 (台帳 #25)** TDW ライセンスは user 決裁待ち
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は **user のみ実行可** — 09-18 スキャンで再確認
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 — **別セッション実行中** (spawn_task task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 — **別セッション実行中** (task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (BB_RSI×2 + hull/sweep×2、純粋 cosmetic)
- [ ] **E22 (通貨 VRP) の S2 診断起動** — `tools/rapid_edge_probe.py` 経路、explore 枠 1/3。§2.1 事前コミット節を pre-reg に内蔵させること
- [ ] **E21 (human_signal_stream 帰属分解) の S2 起動** — OANDA transaction API で手動トレードの標本量実測から。§2.2 スコープ制限を複写
- [ ] E23 (中銀声明テキスト差分) は **2026-08-28 (E7 verdict) まで着手禁止**
- [x] ~~v2.3 draft の user レビュー~~ → **正式化完了 (Phase 5, PR #57)**。摩擦訂正も反映済み
- [x] ~~USDJPY_CARRY_DIP_LIVE_ENABLE の dashboard 実値~~ → **本番確認済み =1 (PR #55 の `/api/demo/live-enable-flags`)**
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 — **別セッション実行中** (task_d932525c)。完了で exit-repair 感度分析の実測フロア更新可
- [x] ~~exit-repair grid BT の実行~~ → **verdict FAIL / H0 採択で完了 (2026-07-08、13日前倒し)**。WS3 シグナル張り替えが v2.3 主戦線に。次アクション = WS3 の MFE 分布 R3 診断の起案
- [ ] Render dashboard の無参照キー物理削除 (BB_RSI×2 + hull/sweep×2、純粋 cosmetic — dashboard UI のみ、API/CLI に delete 無し)
