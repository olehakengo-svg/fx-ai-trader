# Session Log: 2026-09-11

## セッションで行ったこと（時系列）

### Phase C: ps 席供給 30d 再計測の正式 verdict (autopilot、rule:R3)

**選定理由**: registry で唯一の**期日超過**エントリ (`ps-seat-supply-remeasure-30d`、期日 09-10)。
09-09 に窓完成待ちで意図的に保留され、MEMORY `project_ps_capture_estimand_disjoint_2026_09_09` が
「実行主体 = fx-roadmap-autopilot 日次実行」と明記していた到達経路の実行日にあたる。

1. **verdict 確定 = 🔴 REJECT** — 窓完成 (`window.complete=true`、as-of 01:32Z) を確認のうえ
   `tools/ps_seat_supply_remeasure.py --json` を pre-reg どおり **1 回だけ**実行。
   design 34 / 観測 unique 7 / **capture 20.6%** [10.3-36.8%]。是正前 baseline 31% を下回り、
   PR #172 §7(a) の供給回復予測は不成立。09-09 diagnostic から差分ゼロ (design 側も動かず)
2. **§8 補足検証 完遂** — 帰属対象を 3 席 (NZD_JPY/EUR_AUD/USD_CAD、design 17 本ゼロ) に固定:
   - hedge_block = **0 本**。`MODE_CONFIG` 実測で 3 ペアは搬送 mode が席のみ = 反対建玉経路が無く
     **構造的に bind 不能**。頻度推定ではなく mode 構成から導いたので推定誤差を持たない
   - blackout 床 = 実測 3.4 deploy/日 × 上界 5 分 → 17 本中 **~0.2 本**。10x 過大でも ~2.0 本、
     説明には wall-clock **84% 被覆 = 実測の 70 倍**が必要 → 結論は再構成誤差に非依存
   - 残余 ~100% は**帰属不能**
3. 🔴 **残余の性質 = 「測っていない」** — `_block_counts` は再起動で消え / `gate_block_daily` は
   09-11 稼働開始 / **`evaluated_candidates` は HourlyEngine に call site が無い**。
   本番 31d summary の 48 戦略に `price_shock_rev_*` が 1 件も無いことで確定
   (観測 7 行を出した EUR_GBP / AUD_JPY の席すら不在)。hull funnel の「書けるが読めない」より 1 段階手前
4. **執行**: `compute_hourly_signal` に `log_candidates` を追加 (best-effort、live 挙動不変)。
   `bar_time` は PR #168 fallback 踏襲 (call-site 欠落 4 例目の再発防止)。
   pin 10 本 (`tests/test_hourly_candidate_logging.py`、counterfactual 込み)
5. **registry**: remeasure を resolved、後続 `ps-seat-supply-hourly-c1-coverage` (09-25) を新設。
   queue ticket を done へ移送 (`## Claude Review` 付き、独立検証 3 点) + 後続 ticket 起票

**盛らなかった点**: 3 席の残余は「計装が無いので分からない」であって「上流で候補が出ていない」ではない。
(A)/(B) の判別は 09-25 の後続エントリに委ねる。EV/WR/PnL は未計算 —
`ps-carveout-regate-post-172` (09-30) の look は未消費 (P-10 型 ban 遵守)。

### Phase D: マージゲートの空振り発見と修理 (rule:R3)

PR #249 のマージ手続きで `tools/pr_review_gate.py 249` が **PR open の 18 秒後**に
「マージ可」を返した。額面で受け取らず GitHub 側を突合したところ、Codex connector の
進捗サマリは `🔄 Running` — レビューは走ってすらいなかった。

- **機構**: `collect_findings` が「codex/chatgpt 名義のコメントが 1 件あれば到着」と判定。
  connector が PR open 直後に投げる**状態表**がこれを満たす。findings ゼロ → exit 0。
  `--wait` も状態 2 を名乗らないため無効
- **影響**: R2 ゲート (レビュー着弾前マージの防止) が**全 PR で空**だった。
  「検知器も write-only になりうる」「防御が名乗る範囲をカバーしない」の 3 例目
- **修理**: サマリを findings 母集団から除外 / Status=Running は exit 2 /
  Running と Completed 混在は Running 優先 (fail-closed) /
  サマリの Commit SHA と HEAD を照合し不一致なら `@codex review` を指示
- 🔴 **第 2 形状は本修理の PR (#250) 自身で発覚** — connector が
  「create an environment for this repo」という**セットアップ通知**を返し、
  レビューが 1 度も走っていないのにサマリ対策後もなお到着扱いだった。
  到着の positive evidence を (i) 正式 review オブジェクト / (ii) P1/P2 本文 /
  (iii) サマリ Completed の 3 つに限定 (素の通知は到着ではない、fail-closed)。
  **1 回の修理で終わらせず、修理 PR 自身をゲートに通して実挙動を確認したことで見つかった**
- pin 10 本追加 (counterfactual 2 形状込み、計 24)。#249 自体は
  Status=Completed / SHA 一致 (7e4fc41) / findings ゼロを実確認してからマージ


## コミット一覧（自動記録）
1. fix(process): マージゲートが進捗サマリを「レビュー到着」と数えていた — R2 ゲートが全 PR で空だった (rule:R3) (c3dd1635, 4 files)

## 未解決事項
- [ ] 🆕 **ps 席 3 席の残余帰属 (A) 上流 vs (B) 下流** — registry `ps-seat-supply-hourly-c1-coverage`
      (2026-09-25、計装 deploy +14d)。**帰属が出るまで §7(a) 型の供給是正を重ねない**
- [ ] 🆕 **capture estimand の 2 軸分離 (P-1)** / **部分バー発火の closed-bar 整合 (P-2、R1)** —
      [[ps-seat-supply-remeasure-2026-09-10]] §6 起案のまま未着手
- [ ] **`ps-carveout-regate-post-172`** (09-30) — capture 20.6% のままでは N≥10 到達困難、
      「供給側の別問題として stale レビュー」分岐に入る見込み
- [ ] **`conf<30` 等の他 block ラベルの estimand 分離** — 全モードの block family 構成が変わる別件
- [ ] **`git stash list` = 178 件** + ローカル main の乖離 (origin 比 ahead 32 / behind 97)
- [ ] **M1 定義への統計資格条件付与** — user 決裁 (analyses §8)
- [ ] **kalman / weekend_gap の LIVE 約定通算ゼロ** — registry `t9-kalman-d7-fire-info`
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立
- [ ] `e1-positioning-ingest-freshness` — MYFXBOOK 資格情報の Render env 投入は user のみ実行可
- [ ] **fx-data ディスク 1GB → 5GB 拡張** — 課金増で user 決裁事項 (C1 書込み増で 09-25 に再評価)

---

## 並行セッション記録 (敵対的反証レビュー Wave 1/2) — ローカル checkout から救済 (2026-09-17)

> 📌 本節は同日にローカル main checkout 上で走った**別セッション** (PR #240-#248 の敵対的反証レビュー wave) の記録。ローカル座礁分の救済 PR で union 統合した。

### Phase A: 敵対的反証レビュー Wave 1 の完遂 (user「止めているから遅い」検証、前日から継続)
- **PR #240-#244 全マージ**: #240 時限証拠保全 + carry-dip drop-cause RESOLVED / #241 zn-cache 修理 (dispatch 検証: 右端 2026-08-18→09-10 前進を origin/main で実測) / #242 トリガ評価空白監査 (クラッシュは #236 で既修復と確定、空白中の見逃し = E1 と T5 のみ) / #243 E1 ingest インシデント (backoff + 検知 17h→15 分、breach 会計: 24h 欠測閾値 09-11T09:00Z・budget breach 09-13 深夜) / #244 **scan#4 前倒し** (family A 採用 = 台帳 #27・凍結期日 09-24 / family B park / E23 🔒 LOCK・explore 期日 09-20 / E26-28 全棄却 / rate-anchor 二重根因修理 → dispatch 検証中)
- **能動測定ライン 0 → 2 本** (family A + E23)、23 日ぶりの回復。queue の e23 waiver 削除・ticket done 移送済み
- 未着手の実行計画残 = blocker-refutation P8-P22 (hull localize / kalman 監査 / keeper×kill gap / 摩擦較正 09-17 / t8 draft rebase / sr×EUR_JPY 判定即応化 ~09-18 / ledger anchor 再基準化ほか)
- 🔴 **user 待ち: Myfxbook 資格情報復旧** (E1 予算の救済はこれのみ、最初の不可逆点 09-11T09:00Z)

### Phase B: Wave 2 完遂 (blocker-refutation P8/P10/P12/P13)
- **PR #245-#248 全マージ** (計 CI green + connector ゲート、通算 2 日で #230-#248 の 15 PR):
  - **#245 keeper×kill-switch ギャップ封鎖** — kill 状態でも 10,000u 実弾往復が継続する構造を fail-closed 修理 (counterfactual 実証)。盲点 2 (実弾ガバナンス) 監査パケット起票
  - **#246 t8 forensic** — draft 再解決 (99e3560d、即マージ可能状態) + **構造的デッドロック確定**: 第4ブロッカー (静的 spread 1.5p の rowless kill) がトリガ計数を殺し、その修正は draft 側のみ = 4 イベント (08-20/09-01/09-07/09-09) 計数漏れ。解消 3 択は user 決裁 (推奨 = (b) shadow-row 記録の先行切り出し)
  - **#247 rnb 二重欠陥修理** — conf 0-1 vs 30 の単位不一致 + ✅ marker 欠落。**修理なしでは本日登録の shadow レーンが永遠に無発火だった**。他 22 モードは全数調査で無被害。cadence checkpoint (09-24/10-08) 併設
  - **#248 hull 残余の帰属確定** — 上流無傷 (勝者 7.9/週 = 期待一致)、残余 100% = `_tick_entry` gate 合算 (spread_guard×TP 近接の構造衝突が筆頭)。**09-30 retire は誤帰属と確定** (registry 注記済み)。gate_block_daily 永続計装を新設 (live 挙動不変)。gate 変更 (spread_guard 免除等) は R1/R2 別決裁として起票
- ローカル main の index.md 競合マーカー残骸を origin 版で修復
