# Session Log: 2026-09-03 (autopilot)

## Phase 0: 起点

`git pull` で **local main が origin より 16 commit 遅れ**ていたのを解消してから開始
(MEMORY「push ≠ 完了、origin 到達までが保存」の読み手側)。

pre-reg trigger watch: 🔴 TRIGGERED は `t5-jpy-cap-restore-price` のみ =
**R1 / user 決裁事項につき autopilot は執行しない** (registry の message に明記済)。
その他 24 件は watching。ロードマップ v2.3 の実行可能 R2/R3 項目は在庫ゼロ。

→ セッション開始プロトコルの「直近 12h トレード活動 (0 件なら原因調査)」で着手点を得た。

## Phase 1: 発見 — LIVE 約定が 7.5 日ゼロ、しかし全検知器が無音

`/api/demo/trades` の `oanda_trade_id != ''` で数えると、**最後の LIVE 約定は
2026-08-26 14:59 UTC** = 実時間 7.5 日 / **133 市場オープン時間**前。

同時刻の検知器の出力 (本番 status を実際に食わせて確認):

| 検知器 / 画面 | 出力 |
|---|---|
| `live_n_stagnation` | `[]` |
| `candidate_stagnation` | `[]` |
| `engine_tick_stall` | `[]` |
| 画面 `trade_row` (ラベル「約定行」) | **ok / 15 分** |

## Phase 2: 原因 — estimand の取り違え (**記述が実装について偽だった**)

`demo_trades` は shadow と LIVE を同居させ、**本番実測で 501 行中 LIVE は 1 行
= 99.8% が shadow**。shadow は毎日 80-111 行流れる。

`check_live_n_stagnation` は `/api/demo/trades?limit=500` の**全行**から最新時刻を
取るため、「最終行」は LIVE が完全に止まっても常に数分以内 → **約定停止を原理的に
検知できない**。にもかかわらず `check_engine_tick_stall` の canonical な estimand 表は
`live_n_stagnation` を「約定が出たか (ゲート通過後)」と記述していた。

同じ畳み込みが画面側にも: `trade_row` はラベル「約定行」で SQL はフィルタ無し。

**2026-08-26 の「修復」で直らなかった理由**: あれは 126 日 no-op だった field 契約の
バグを直したが、**母集団を間違えた**。「一度も鳴らない」→「名乗っている理由では
一度も鳴らない」に変わっただけ。→ 修復の検証は「動くか」でなく
「**名乗っている estimand を測っているか**」で行う。

詳細: [[lesson-live-fill-estimand-shadow-conflation-2026-09-03]]

## Phase 3: 実装 (rule:R3)

1. **`live_fill_stagnation` 新設** — `oanda_trade_id` を持つ行だけを見る
2. **サーバ側集計** (`get_row_freshness` に第3系列 `live_fill`) — watcher の 500 行窓は
   shadow に埋め尽くされ**実測 7.67 日しか遡れず**、ドリフトがそれを超えると最後の
   LIVE 行が窓から落ちて計算不能になる (09-03 時点で窓の先頭は最終約定の 3.5 時間前 =
   崖っぷち)。**読み手はあるが窓が狭すぎる**型 = PR #213/#214 と同型
3. **estimand 表の訂正** + **画面ラベルの訂正** (「約定行」→「トレード行 (shadow込)」/
   「LIVE 約定」新設)
4. 閾値は SSOT (`modules/freshness_policy.LIVE_FILL_STAGNATION_HOURS`) に集約

### 閾値較正 = 120 市場オープン時間

本番 6,000 行 (06-24〜09-03) / LIVE 約定 63 件 / 到着間隔 62 本:

| 母集団 | n | median | p90 | max |
|---|---|---|---|---|
| 全期間 | 62 | 5.0h | 43.2h | **224.7h** |
| carve-out 後 (07-29〜) | 15 | 30.0h | 65.8h | **75.3h** |

**全期間 max 224.7h は較正から除外** — `_is_xau_inst` バグが preserve 型 LIVE 送信を
3.5 ヶ月殺していた時期 (PR #119→#124 で修復) の尾で、**現行構成に存在しない母集団**。
消滅した構成のギャップを混ぜると閾値が過去のバグの尾で膨らみ本物のドリフトを見逃す
= 09-01 候補行較正の「max の 3 倍」の**鏡像**。

replay: 72h→誤発火1 / 96h→0 / **120h→0** / 144h→実ドリフト見逃し。
120h は carve-out 後 max の 1.59 倍で、**全期間で唯一の発火が preserve バグの尾
= 真陽性** (当時この検知器があれば捕捉できた)。⚠️ n=15 は薄い、誤発火時は**上げる**。

### counterfactual 7/7 — うち **2 件が初回素通り = 実欠陥**

| # | 変異 | 結果 |
|---|---|---|
| 1 | `main()` 配線削除 | ✅ 2 本落ちる |
| 2 | SQL の `oanda_trade_id` フィルタ削除 | ✅ 2 本落ちる |
| 3 | 閾値 72h | ✅ |
| 4 | 閾値 200h | ✅ |
| 5 | 週末除外を実時間に | ✅ |
| 6 | `unparseable` を総括 return に畳む | ⚠️ **初回素通り** |
| 7 | 閾値を watcher にベタ書き | ⚠️ **初回素通り** |

- **6**: `no_rows` 早期 return は死にコードで、下の `if st != "ok": return` が
  **`unparseable` (= 行はあるが時刻が壊れている = 本物の異常) まで黙って捨てていた**。
  本 PR が直している「静かに skip」型の穴を新検知器に作り直すところだった
- **7**: `aw.X is fp.X` は **120 が CPython 小整数キャッシュ域**のため
  ベタ書きでも True。値の一致では書き写しを検出できない → 束縛が `_fp` 由来という
  **構造**を pin し直した

> counterfactual が初回に通ったら「安全」でなく「pin が無い」の証拠 — MEMORY に 2 例
> あったところへ 3・4 例目。今回は 2 件とも**実装側の実欠陥を伴っていた**。

## Phase 4: ドリフトそのものの disposition (過剰主張を避ける)

**発火 = バグとは限らない。** 直近 8 日の LIVE 資格ファミリ発生数:

| entry_type | shadow | live |
|---|---|---|
| `price_shock_rev_aud_jpy_h1_long` | 1 | **1** (08-26 の約定) |
| `usdjpy_carry_dip_accumulator` | 1 | 0 |
| `price_shock_rev_eur_gbp_h1_long` / `weekend_gap_fade` / `kalman_d7` | 0 | 0 |

同期間 shadow 総数 **536 行** = engine は稼働し、**LIVE 資格の無いセルばかりが鳴っている**。
08-26 の約定が転送経路の健全性を実証。→ **実体はシグナル枯渇であって転送障害ではない。**

⚠️ **未解決の副次観察**: 08-27T16:02Z の `usdjpy_carry_dip_accumulator`
(roadmap T8 で live 稼働中のはず) が `is_shadow=1` で記録され **+15.5p の WIN**。
shadow 落ちの理由は未特定 → registry で 09-30 判定。

**M1 への含意**: LIVE 資格セル群の発生率が **~1 件/週**。M1 (月次符号転換) は
符号以前に**母数が足りない** — M3 (clean live N≥30 セル 3 個 = 現在 0 個) が
スループット側から見えている姿。

## 検証

- 新規 21 本 + 既存更新を含め **全 2,928 passed / 15 skipped**
- `scripts/check.py` **全 9 チェック通過** (警告 2 件は既存: 破損 wikilink 199 / e23 queue は user 決裁待ち)
- 本番 status を模擬した end-to-end: 既存 3 検知器すべて `[]`、新検知器が
  `market_open_hours_since 133.8` で発火、画面 `live_fill_row` = **stale** / worst = stale
- ⚠️ semgrep が `modules/demo_db.py` の SQL 連結 6 件を報告 — **全て origin/main に既存**
  (718/784/798/887/948/998 行) で本 PR の変更 (静的リテラル、連結・外部入力なし) とは無関係。
  スコープ外につき本 PR では触っていない

## 成果物

| 種別 | パス |
|---|---|
| 検知器 | `scripts/anomaly_watcher.py` (`check_live_fill_stagnation`) |
| 鮮度系列 | `modules/demo_db.py` (`_FRESHNESS_TABLES` に `live_fill`) |
| status 露出 | `modules/demo_trader.py` (`_row_freshness_payload`) |
| 閾値 SSOT + 画面 | `modules/freshness_policy.py` |
| テスト | `tests/test_live_fill_stagnation.py` (21 本) / `tests/test_freshness_policy.py` (更新) |
| 教訓 | `wiki/lessons/lesson-live-fill-estimand-shadow-conflation-2026-09-03.md` |
| 監視 | registry `live-fill-threshold-recalibration` (12-03) / `live-fill-drought-2026-08-26-disposition` (09-30) |


## コミット一覧（自動記録）
1. auto: KB session-end save (2026-09-03) (8a1fc7b6, 4 files)
2. auto: KB session-end save (2026-09-03) (de21f8ca, 2 files)

## 未解決事項 (繰越)

- [ ] **LIVE 約定ドリフトの disposition** — registry 09-30 判定。副次に carry_dip の shadow 落ち理由
- [ ] **LIVE 資格セルの発生率 ~1件/週** — M3 スループット問題として roadmap 起票候補
- [ ] **kalman 初 fill 待ち** — 直近 8 日 shadow ゼロ。registry t9 で管理済
- [ ] **SVK 継続監視** — 09-03 時点 rt=6 / $120k / 520k、`behind_pace=false`
- [ ] **B7/B8 の live 可能セル層** — N=17/26。2026-11-30 に N≥30 確認 → 新 pre-reg
- [ ] queue 滞留: `20260818-e23-cb-text-adjudication-s2` — TDW ライセンスが **user 決裁待ち**
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は **user のみ実行可**
- [ ] **fx-data ディスク 1GB → 5GB 拡張** — 課金増で **user 決裁事項**
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立
