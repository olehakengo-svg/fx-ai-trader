# Session Log: 2026-08-26

## セッションで行ったこと（時系列）

### Phase 1: ロードマップ現在地の確認
- `git log` / MEMORY / 08-25 session log で未解決事項を確認。実行可能な最優先候補として
  「`evaluated_candidates` の retention/rotation 不在 (Render Disk 圧迫リスク)」を選択

### Phase 2: 事故が「継続中」であることの発見 (本セッションの主眼)
- 対策設計のため本番の実データを確認したところ、**書込みが今も止まっていた**
  - `/api/demo/trades` の最終 `entry_time` = **2026-08-21T18:46:24Z** (約 5 日前)
  - `/api/demo/evaluated-candidates?days=1|2|3` = いずれも **0 件**
  - Render app log (08-26T03:16-03:18Z) = `database or disk is full` **consecutive=85**、
    positioning / health / `log_candidates` / `_tick_entry` の全経路が失敗中
- MEMORY `project_render_disk_full_write_outage_2026_08_25` の「08-21→08-25」は**閉じていなかった**
- `/api/demo/status` は `main_loop_alive=true` + tick/block カウンタ増加 = **全てメモリ内カウンタ**。
  ダッシュボードは正常に見え続ける

### Phase 3: 恒久対策の実装 (rule:R3)
- `modules/disk_guard.py` 新設 — 測定 (`disk_status` / `db_footprint` / `table_rows`)、
  pre-flight (`has_room_for_backup`)、回復 (`emergency_reclaim`)
- `DemoDB.backup_database` を **ローテーション先行 → pre-flight → コピー** 順に修正
  (旧順序は満杯時に自己増悪ループになる)。counterfactual で旧実装が落ちることを確認
- `candidate_logger.prune_candidates` (90 日) を起動時 + 日次レビューに配置
- `check_live_n_stagnation` の **126 日間 no-op** を修復 (存在しない `last_trade_time` 依存)
  + 契約が壊れた場合を `stagnation_check_broken` として報告するよう変更
- `GET /api/admin/disk_status` / `POST /api/admin/disk_reclaim` / `POST /api/admin/prune_candidates`
- anomaly_watcher に `check_disk_capacity` (warn 75% / critical 90%、閾値は API 応答から取得)
- テスト 23 件追加、全 2,730 件 green / `check.py` 全 9 チェック通過

## 未解決事項
- [ ] **本番復旧の実測確認** — deploy 後に `/api/admin/disk_status` で `used_pct` と
      `evaluated_candidates` 行数を実測し、書込み再開 (trades / candidates の新規行) を確認する
- [ ] **disk 1 GB の増設要否 (user 決裁)** — 回収後も高止まりするなら `sizeGB` 引き上げ。課金発生のため要決裁
- [ ] **hull 残余 4.7x の localize** — 蓄積待ち (書込み停止中は前進不能)
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 (別セッション task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 (別セッション task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立。成立日に runbook §3 を直接執行
- [ ] **MoF 月次介入額の公表チェック (~08-29)** — `mof-monthly-total-2026-08-29-check` (deadline 08-28)
- [ ] **E23 (台帳 #25)** TDW ライセンスは user 決裁待ち
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は **user のみ実行可**
- [ ] queue 滞留: `20260818-e23-cb-text-adjudication-s2` (8日) / `20260819-family-a-statement-ladder-prereg-draft` (7日) — SLA 3日超
