# Session Log: 2026-09-22

## セッションで行ったこと（時系列）

### Phase 1: 前セッションの座礁作業の棚卸し
- autopilot 起動時の実測: **作業が 3 箇所で座礁**していた
  - ローカル `main` が origin/main に対し **8 ahead / 30 behind** (auto KB save が直接コミット、未 push)
  - PR #272 ブランチに未 push commit 1 本 (`fix(review): #272 レビュー 6 巡目`)
  - PR #273 worktree に**未コミットの実装 + test 変更** (Codex 第20波 P2/P3 対応)
- どちらの PR も `mergeStateStatus=DIRTY` (hot file conflict) でマージ待ち

### Phase 2: PR #273 — 第20波の未コミット作業を検証したら自分の pin が赤だった
- `pytest tests/test_cell_deepdive_lock_redaction.py` → **1 failed / 58 passed**。
  落ちていたのは**この作業自身が追加した** `test_pagination_dedups_drift_and_uses_a_served_row_cursor`
- 🔴 **機構の再診断**: Codex P2 は page drift を「重複する」としか述べておらず、
  実装 docstring もそれを受けて「drift は重複するだけで skip しない / de-dup すれば
  union は exact」と書いていた。**これは偽**。前方挿入された行はカーソルが既に通過した
  offset に着地するので**一度も配信されない (SKIP)**。de-dup は膨張のみを直し、
  欠落は直さない ⇒ **残差の向きは UNDER-count (偽陰性)**
- ✅ **修正**: `_paginate_pass()` (1 pass) と `paginate_trades()` (drift-free を要求する
  retry) に分離。重複を 1 件でも落とした pass は証拠つきで破棄・再実行し、
  全試行 drift なら `SystemExit`。meta に `attempts` / `drift_observed` を残す
- ✅ P3: `--min-rows` の default を `None` 化し、広告されていた `--min-rows 0` の脱出口を
  実際に到達可能に (50 行署名検査が無条件に先行していた)
- pin 更新: `status=all` 形状は毎試行 drift 証拠を出すため fail-loud へ変わった
  (第16波の「危険だが complete=true を返す」pin を新挙動へ更新)
- 実測: **59 passed** (当該ファイル) / full suite **3467 passed, 15 skipped, 1 xfailed**


### Phase 3: PR #272 / #273 の conflict 解消 + 座礁 commit の push
- #272: 未 push だった 6 巡目 commit (`86613d66`) を push。full suite **3485 → 3498 passed**
- 両 PR で `origin/main` を merge し hot file (changelog / session log) を **union 解決**
  (09-21 session log は**並行 2 セッション分**だったので A/B の 2 節に分けて両方保存)
- 再レビュー依頼 (`@codex review`) → #273 に **P2×3 が新着**

### Phase 4: 第21波 P2×3 消化 — うち 1 件は第20波の穴の「隣」だった
- (ll) **open 行を closed ページングの前に取得** — 旧順序では closed pass 終了後・
  open 要求前に CLOSE した約定が**両集合から欠落**する。🔴 **第20波の retry は
  この窓を塞がない** (retry が見るのは closed pass 内部の drift だけ)。
  open を先に取ると穴が**重複**に変わり identity で解消できる
- (mm) **redact セルの primary を実際に一致した LOCK に** — `covering[0]` は 1 行も
  持たないことがあり、compact 出力が無関係な LOCK の registry_id/閾値/母集団 0 を
  redaction 原因として公表していた。matched 数の argmax + `n_rows_matched`/`primary`
- (nn) **as-run レポートの手順も bare curl のままだった** (第18波で help を直した同型の 2 例目)
- pin **62 本**。3 件とも counterfactual 確認済 (修正を戻すと当該 pin が落ちる)。
  順序 pin は機構より先に実体を assert (`[8] == [8, 9]` = 約定 9 の消失)

### Phase 5: PR #272 第7巡 — 自分が公表した感度表が旧 estimator の出力だった
- gate が挙げた 3 件を個別に検証: sr_audit.py:486 P1 と hunt_event_dataset.py:198 P2 は
  **既に消化済み** (前者はコード上に `if not bench_prepared["ok"]: return 5` が存在、
  後者は 6 巡目の `DEDUP_WINDOW_SEC`) ⇒ **gate は両方向に誤る**の実例をもう 1 つ追加
- 新着 P2 は妥当。独立再計算 (provenance 後 69,576 行) で正値を確認:
  **15m 27,335 / 1h 20,642 / 4h 14,953 / 24h 11,614 / 無制限 9,946**
- 🔵 旧値は **chaining する旧実装**の出力。5 巡目で anchored に変えた際に再計算せず、
  **無制限列だけ一致**していたため表全体が生きているように見えていた
- 🔴 PR #230 で自分が書いた教訓 (「撤回した主張を運ぶ全 readout 面を同じコミットで洗う」)
  の再発。**ツールのコメントは readout 面**
- pin 63 本 — ツール表と readout の**相互整合**を assert (両側静的テキストなので
  データ増加で壊れない)。counterfactual 確認済

### Phase 6: 第22波 — 自分の修正が鏡像の穴を開けていた
- c71aee29 (第21波) に対する再レビューで **P2×2 新着**。どちらも妥当
- (oo) 🔴 **第21波で closed→open の穴を塞いだら open→closed の鏡像が開いていた**:
  パス中に**新規 OPEN** した約定はどちらの集合にも入らないまま `complete=true` だった。
  **順序の入れ替えは窓を消さず移動させるだけ** ⇒ closed パスを 2 回の open 読み取りで
  挟む (bracket) 形に。`open_before ∪ open_after` が mid-pass open を捕まえ、
  「open_before に居て open_after にも closed にも居ない行」= 穴として**検出可能**に。
  穴は証拠なので破棄して再走、全試行で穴なら SystemExit
- (nn2) **全行が dedup repeat の LOCK が棚卸しから消滅**していた。キー集合を dedup 後の
  行から作っていたため。`ws3-*` は unique 述語を宣言しないので**正本 watcher は数える**
  ⇒ LOCK が n_decide 到達でも監査が無報告になりうる。キー集合=raw / 計数=dedup 後に分離
- 🔴 **危険の向きはこの PR を通じて一貫して UNDER-count** (skip / hole / mirror hole /
  LOCK 消滅) = 全て「無かったことになる」方向
- pin 63 → **66 本**、4 件すべて counterfactual 確認済
- 実測: full suite **3487 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 7: #272 第8巡 — 推定量が到着順に依存していた (公表数字が 3 巡連続で動いた)
- (a) **anchored 窓は到着順依存** — 古い timestamp が後から来ると `(ts-anchor)` が負になり
  必ず現在の窓に入る。実測 **9,946 identity 中 205 件が逆順ペア** (429 箇所) =
  現に N を過小計数していた。⇒ identity ごとに時刻昇順へ並べてから窓を張る
- **訂正値**: 15m **27,565 (2.52x)** / 1h **20,692 (3.36x)** / 4h 14,953 / 24h 11,614 /
  無制限 9,946。4h 以降不変 (逆順ペアの間隔が 4h 未満)。必要 WR **51.025% は不変**
  — あれは訂正後 N での値で、旧 N=20,642 では 51.027% だった (偶然の一致)
- 波及訂正: tool コメント表 / readout (表・膨張係数・corrigendum) / registry
  `current_state` / strategy card。**readout の `[sr_audit] dataset:` ブロックは
  as-run 記録なので不改変** (旧推定量の出力である旨を明記)
- 🔴 **同じ数字が 3 巡連続で動いた** (6 巡目 窓無し→1h / 7 巡目 chaining→anchored の
  洗い漏れ / 8 巡目 到着順依存)。**教訓は「数字を直す」ではなく「推定量を凍結してから
  点推定を出す」** — 3 回とも定義が動いている間に publish していた
- (b) 🔴 **P1: GDELT が検証前に保存系列を上書きしていた** — 宛先 CSV を `"w"` で開いた
  後に鮮度検査。stale 応答が既に系列を壊しており raise は元に戻さない。さらに daily
  workflow は `if: !cancelled()` で data ディレクトリ全体を stage するため
  **hard 失敗のまま退行がコミットされていた**。⇒ 候補を `.{slug}.tmp.csv` へ書き、
  候補を検査し、通ったものだけ `os.replace` で atomic 昇格。落ちたら候補を削除
- pin 63 → **66 本**。3 件 counterfactual 確認済 ((b) は当時の実装を忠実に再現して
  「保存系列が上書きされた」で落ちることを確認)
- 実測: full suite **3502 passed, 15 skipped, 1 xfailed**

### Phase 8: 第23波 — 塞いだはずの穴を同じ関数の 3 行上が開けていた
- (pp) 第18波で「`trades` を欠く応答を拒否」を入れたのに、直前に
  **`isinstance(payload, list)` を無条件に通す**分岐が残っていた。endpoint は常に
  オブジェクトを返すので bare list は malformed 応答しか通さず、HTTP-200 `[]` が
  **短ページ=終端**と読まれて部分スナップショットに `complete=true` が付いていた
- 🔑 **保存済み snapshot の reader は bare array を受け続ける (意図的)** — ローカル
  ファイルは別母集団。pin は両方を主張 (endpoint は SystemExit / local は list 受理)
- pin 66 → **67 本**、counterfactual 確認済

### Phase 9: #272 第9巡 — 8 巡目の 3 修正がそれぞれ隣に穴を残していた
- (c) 🔴 **P1: 鮮度は被覆を保証しない** — 末尾日しか見ないので「末尾だけ新しい部分系列」が
  8 巡目の検証を通り完全な履歴を置き換える。`gdelt_coverage()` を新設し昇格前に
  **行数と開始日**を比較、縮んだら raise
- (d) **後続 slug の fetch 失敗で候補が staged tree に残る** — workflow は失敗時も
  `git add` するので候補が corpus としてコミットされる。候補を **system temp dir** へ
  移し finally で rmtree。**finally だけでなく「git add の見えない場所」が本質**
- (e) **tz-aware と naive の混在で sort が TypeError** → 監査全体が異常終了。
  正規化を **`_parse_entry_time()` 自身**に置いた (sort と窓の減算が同じ値を使うため)。
  🔵 公表値は不変 (現コーパスは全 naive)
- pin 66 → **69 本**、3 件 counterfactual 確認済
- 実測: full suite **3505 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 10: 第24波 — 完全性の主張を「発見的」から「証明」へ
- (qq) **v3 の帰属を自分の session の行だけで計算** — 親セル全体の行で matched を
  出していたため、セル内の全 v3 record が同じ primary を選んでいた (shadow=Tokyo /
  live=London なら一方が他方の registry_id・n_decide を報告)
- (rr) 🔵 **bracket 内で生まれて死んだ約定を検出可能にした** — 3 読み取りすべてに
  現れないので open 行起点の検査では原理的に見えない。⇒ **確認用 2 回目 closed pass**。
  **CLOSED 行は append-only** なので `confirm ⊇ closed` が常に成り立ち、差集合は
  ちょうど bracket 中に CLOSE した集合 ⇒ **一致は「窓内で何も CLOSE しなかった」証明**
- 🔑 穴の 2 信号は合算せず別々に報告 (同一約定が両方を立てるので足すと過大)。
  pin が最初に捕まえたのはこの二重計上
- pin 67 → **69 本**、2 件とも counterfactual 確認済
- 実測: full suite **3490 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 11: #272 第10巡 — 被覆は集計量では守れない / 同じ数字が 4 巡連続で動いた
- (f) 🔴 **P1: 行数一致 + 開始日一致は被覆を保証しない** — 歴史の途中の日付を落として
  同数を末尾に足すと両方の集計チェックを通り、**内部に穴のある系列**で完全な履歴を
  置き換える。⇒ `gdelt_coverage()` が**日付集合**を返し、ゲートを**集合包含**へ。
  候補内の日付重複も拒否。**「集計量が悪化していない」≠「情報が失われていない」**
- (g) **D3 契約節の数字が 4 巡分の訂正から取り残されていた** — 観測単位を定義している
  docstring 冒頭が `57,656 / 82.9% / 5.8 倍` のままで、**現行のどのモードにも一致しない**
  (既定 1h = 48,884 / 70.3% / 3.36 倍)
- 🔑 **再発を止めたのは文言修正でなく pin の拡張** — 7 巡目の相互整合 pin は感度表しか
  見ておらず、3 行上の契約節が独立にドリフトできた。pin を契約節にも広げ算術で assert。
  実際この pin は**作業中に自分の誤った backup 復元で契約節が巻き戻ったのを即検出した**
- pin 69 → **72 本**、3 件 counterfactual 確認済
- 実測: full suite **3507 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 12: 第25波 — 「重複の片方を選ぶ」は無害な選択ではなかった
- (ss) 2 回の open 読み取りで同じ identity が両方に出たとき、旧実装は
  `open_before` を残していた。open 行は**通常の live 経路で変化する**:
  `DemoDB.set_oanda_trade_id()` が**行が OPEN のまま** `oanda_trade_id` を埋め
  `is_shadow` を反転させる。bracket は identity を比較するので**この変化はどの穴
  検査にも掛からず**、古い copy を残すと **live 約定を shadow として報告する**
- 🔑 **dedup/union は「どちらの copy を残すか」を意味で決める** — 第21波の
  `merge_open_into_closed` では「CLOSED 側が exit_time を持つから勝つ」と意味で
  決めていたのに、第22波で足した open 側の union は**先に入れた方が残る**という
  実装の副作用に任せていた。同じ PR の中で規律が片側にしか適用されていなかった
- 変化数は `open_mutated_midfetch` として meta に残す (identity ベースの検査には
  映らないので、記録しなければどこにも残らない)
- pin 69 → **70 本**、counterfactual 確認済
- 実測: full suite **3491 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 13: #272 第11巡 — 「観測を捨てない」を promotion ゲートに持ち込んでいた
- (h) 🔴 `entry_time` が読めない行を**独立観測として数えていた**。collapser の内部では
  「残す」が正しい (黙って観測を消さない) が、`prepare()` が隔離しないため
  **`entry_time: "bad"` のラベル付き重複 30 行が「独立 30 イベント」として N の床を通り
  偽の有意を製造できた**。⇒ 窓使用時は `prepare()` で **fail closed**
  (`quarantined_undatable`)。**同じ入力に対し層ごとに安全な向きが逆**
- 🔵 現コーパスは該当 0 行なので公表値は不動。⚠️ **この欠陥は本 PR の test fixture 内に
  生きていた** — `2026-09-01T24:00:00`〜`T39:00:00` を生成しており **16 行の不正時刻が
  16 独立観測として通っていた** (fixture を翌日 roll over で修正)
- (i) **logger docstring が 3 つ目の読み手面**だった (`20,642` のまま「これを使え」と指示)
- 🔑 **pin を「読み手面の集合」に張り直した** — tool 契約節 + tool 感度表 + readout +
  logger docstring の 4 面。**changelog / session log は訂正の歴史記録なので意図的に
  pin しない**。指示面と記録面を分けた
- pin 72 → **74 本**、2 件 counterfactual 確認済
- 実測: full suite **3508 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 14: 第26波 — 第25波の修正の鏡像を自分で確認していなかった (本 PR 3 例目)
- (tt) 第25波で open 行は「新しい読み取りが勝つ」と決めたのに、**closed 2 パスは
  identity しか比較せず古い payload を返していた**。`DemoDB.set_oanda_trade_id()` は
  **status を条件にしない**ので、速く CLOSE した約定が closed 集合に入った**後**に
  `oanda_trade_id` / `is_shadow` を書き換えられる ⇒ キー集合一致で完全性チェックは
  通り、**古い shadow 表現が complete=true の下で書き出される**
- ⇒ 値でも confirm を採り `closed_mutated_midfetch` を計上。confirm pass 自身の
  drift 証拠も `confirm_pass` として保存 (完全性の論拠の一部なので捨てられない)
- 🔴 **「対称側を確認する」は修正後の状態にも回す — 本 PR 3 例目**
  (21→22 の鏡像 / 25→26 の鏡像)。**自分が直した直後のコードが次の非対称の発生源**
  になるのが本 PR で最も再現性の高いパターン。⇒ dedup/union/2-pass を書いたら
  「どちらの copy が勝つか」を**両方の集合について**明示したかを完了条件に加える
- pin 70 → **71 本**、counterfactual 確認済
- 実測: full suite **3492 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 15: #272 第12巡 — 前巡で足したガードが 2 つとも「広すぎる」方向に倒れていた
- (j) **時刻不正のブロック理由をセルに絞る** — 11 巡目の fail-closed は `select_cell()` の
  **前**に全体を走査しており、**別ペアの不正行 1 件で clean な USD_JPY/support 監査 30 行が
  reject** された。⇒ **除外は全体 / ブロック理由はセル内のみ**に分離
- 🔴 **5 巡目 (b) と完全に同型** — 「collapse が select_cell の前に走るので別ペアの衝突 1 件で
  無関係な clean セルが DATA-BLOCKED」。**同じ間違いを、その修正を参照しながら書いた
  新しいガードで再発させた**
- (k) **昇格を全 slug で all-or-nothing に** — `os.replace` はファイル単位では atomic だが
  ファイル間では違う。slug 1 置換 → slug 2 失敗で**世代混在**になり、workflow が
  hard 失敗後も全部 stage するので混在がコミットされる。⇒ backup してから置換し、
  どれかが raise したら全部ロールバック
- 🔑 **「fail-closed は安全側」だけでは設計が決まらない** — 倒しすぎると「正常入力を
  止める」別の故障。⇒ ガードの完了条件に「**この検査はどの母集団に対して真であるべきか**」
  (全体 / セル / ファイル単位) の明記を加える
- pin 74 → **77 本**、2 件 counterfactual 確認済
- 実測: full suite **3510 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過
### Phase 16: 第27波 — 読み取りを足すのをやめて「as-of を定義」した
- (uu) 第26波で `open_after` の**後ろ**に confirm pass を足したため、**confirm 実行中に
  OPEN した約定**がどの読み取りにも入らなくなっていた。🔑 **読み取りを 1 本足すと窓が
  1 つ後ろへ移るだけ**で無限後退する。⇒ 順序を `open → closed → closed(confirm) → open`
  に変え、**末尾 open の瞬間を snapshot の as-of と定義**。「as-of 後に OPEN して CLOSE
  しない約定は含まれない」は**欠陥ではなく境界**で、欠陥は「as-of より前に存在したのに
  欠けている行」。**完全性の主張を as-of 相対で述べ直した**
- (vv) `since` 前に OPEN した live 約定について、正本 watcher は `date_from` を渡すが
  endpoint の **OPEN 分岐が無視する**ため `count_live_matching()` は数える。本ツールは
  除外していた。**どちらが正しいとも言えない**ので §9 と同じ規律で**両方出す**
  (決着は `sr-anti-hunt-eurjpy-count-basis-declaration`)
- 🔵 **vacuous pin を counterfactual が捕まえた** — (uu) の pin は当初 open 呼び出し回数で
  分岐する stub だったため**修正を戻しても通った**。「closed pass が 2 本終わったか」で
  分岐する形に直して初めて `[] == [6]` で落ちた。**「pin を書いた」≠「pin が効く」**
- pin 71 → **73 本**、2 件 counterfactual 確認済

### Phase 17: 🔴 環境欠陥 — 編集した .py が import されず旧 bytecode が走っていた
- 第27波 (uu) の pin が**ソースは正しいのに落ちる**という食い違いから発覚。
  `dis.get_instructions()` の実行順は**旧版**、`inspect.getsource()` は**新版**を返していた
- 原因: この環境の python3 は `sys.pycache_prefix = ~/Library/Caches/com.apple.python`
  で **bytecode キャッシュがリポジトリ外**に置かれ (`find . -name '*.pyc'` では見えない)、
  キャッシュの記録 source-mtime が現ソースの mtime と一致して**検証を通って**しまう。
  プロジェクト配下に **119,373 個**滞留していた
- ⇒ purge して両 worktree の full suite を再実行: #273 **3494 passed** / #272 **3510 passed**
- ⇒ 直近 2 件の counterfactual を **purge を挟む形で再検証**し、結論が変わらないことを確認
  (CF(uu) = `[] == [6]` / CF(j) = 自己矛盾したブロック理由)
- 🔑 **「CF を当てたのにテストが通った」は pin が恒真である証拠にならない** — CF が
  読み込まれていない可能性がある。**counterfactual は「CF → purge → 実行 → 復元 →
  purge → 実行」**で回す。full suite の "N passed" も測ったツリー状態を保証しない
  (commit exit 0 が嘘をつくのと同型の bytecode 版)
- MEMORY に恒久記録: `feedback_pycache_prefix_stale_bytecode_verification`

### Phase 18: レビュー消化の打ち切り判断 + registry 起票
- 本セッションで消化したレビューは **合計 13 巡** (#272 = 7〜12 巡目 / #273 = 第20〜27波)。
  **指摘は全て実在の欠陥で誤検知ゼロ**、P1 は全件修正、pin は #272 **77 本** /
  #273 **73 本** へ増加し**全件 counterfactual 済み**
- ⚠️ **レビューは「クリーン」で終わっていない** — 最終巡でも毎回 1〜2 件の新規 P2 が
  出続けている。**打ち切りはマージ判断であって完了宣言ではない**
- 欠陥族は一貫して 3 つ: (a) fetch/完全性の窓 (b) 数値の陳腐化 (c) ガードの広すぎ/狭すぎ
- ⇒ registry `review-backlog-272-273-continuation` を起票 (**期日 2026-10-06**):
  両 PR の最終 HEAD に対しレビューを 1 回走らせ残 P1/P2 を triage する
## コミット一覧（自動記録）
1. fix(tools): Codex 第20波 P2/P3 — page drift は重複だけでなく SKIP もする (rule:R3) (6944eea8, 5 files)
2. Merge remote-tracking branch 'origin/main' into fix/deepdive-lock-redaction (50587553, 0 files)
3. fix(tools): Codex 第21波 P2x3 — open 行を先に取る / redaction を実際の LOCK に帰属 (rule:R3) (c71aee29, 5 files)
4. fix(tools): Codex 第22波 P2x2 — 第21波の修正が鏡像の穴を開けていた (rule:R3) (fbc55cb4, 4 files)
5. fix(tools): Codex 第23波 P2 — 契約を強めた当の関数に fail-open が残っていた (rule:R3) (0e46f159, 4 files)
6. fix(tools): Codex 第24波 P2x2 — 完全性の主張を発見的から証明に上げた (rule:R3) (79442832, 4 files)
7. fix(tools): Codex 第25波 P2 — 重複の片方を選ぶのは無害な選択ではなかった (rule:R3) (b0c0a2c9, 4 files)
8. fix(tools): Codex 第26波 P2 — 確認用 closed pass は値でも勝つ (rule:R3) (ac5d2932, 4 files)
9. fix(tools): Codex 第27波 P2x2 — 読み取りを足すのをやめて as-of を定義した (rule:R3) (fe622ba3, 4 files)
10. docs(registry): レビュー消化の継続枠を起票 — 打ち切りは完了宣言ではない (rule:R3) (87a1d069, 2 files)
11. fix(hooks): main 乖離の発生源を塞いだ + 座礁 KB 救済 + committed conflict marker 修復 (rule:R3) (c76cdb25, 3568 files)
12. fix(hooks): main 乖離の発生源を塞いだ + 座礁 KB 救済 + committed conflict marker 修復 (rule:R3) (978b1ae4, 9 files)
13. fix(hooks): レビュー P1+P2 — 退避を HEAD==main に限定 + 退避 ref を一意に (rule:R3) (5aa44d10, 4 files)
14. fix(hooks): レビュー 2 巡目 P1 — main 上でも「hook 製 KB コミットだけ」を証明する (rule:R3) (f2da2ff2, 4 files)
15. fix(hooks): レビュー 3 巡目 P2 — 退避 ref は完全 sha にする (rule:R3) (a4db57d3, 4 files)
16. fix(hooks): レビュー 4 巡目 P1 — SIGPIPE で混在検査が反転していた (rule:R3) (063efcdb, 4 files)
17. fix(hooks): レビュー 5 巡目 P1 — 検査した sha を押す + 残り P2 を registry へ繰延 (rule:R3) (7bd6fed9, 5 files)
18. fix(fork-safety): 本番 HTTP 全盲 3h18m の根本原因 — gunicorn master の pre-fork 窓で DailyReview が SQLite を回していた (rule:R3) (97194e38, 20 files)
19. fix(review): #277 Codex P2×2 — api_down を connection/5xx のみに限定 + 捏造ガードを strategy_report にも同じ出口で適用 (rule:R3) (1f6a441b, 7 files)
18. auto: KB session-end save (2026-09-22) (b53fd5cd, 1 files)
19. auto: KB session-end save (2026-09-22) (319af098, 1 files)
20. auto: KB session-end save (2026-09-22) (d29d143b, 2 files)
21. Merge origin/main into claude/agitated-borg-8763a6 — sessions/2026-09-22-session.md は union 解決 (#278 の Phase と本 PR の Phase 26-28 を両方保存) (ced0730a, 0 files)
22. fix(review): #277 Codex P2×2 (4 巡目) — api_down を connection のみに限定 (5xx は応答あり) + 否定判定を節単位へ (rule:R3) (0101e3a9, 5 files)
23. fix(review): #277 Codex P2×2 (5 巡目) — 応答あり失敗を api_http_error に分離 + 否定判定を原因語束縛へ (rule:R3) (b7927f02, 7 files)
24. auto: KB session-end save (2026-09-22) (251de2b0, 3 files)
25. auto: KB session-end save (2026-09-22) (ab0987a3, 1 files)
26. docs(KB): スプリント 0922 終結 — registry 統合 (繰延 9 / 更新 9 / 新規 5) + index/changelog/session 同期 (rule:R3) (ad490129, 9 files)
27. auto: KB session-end save (2026-09-22) (0c66a789, 1 files)

## 未解決事項
- [ ] ローカル `main` の 0/0 復帰 (座礁 8 commit の救済後に reset)
- [ ] PR #272 の未 push commit + conflict 解消


### Phase 19: 座礁 KB の救済 + main 乖離の**発生源**を塞いだ
- 🔴 **発生源 = `scripts/hooks/session-end-save.sh`**: step 2 が KB 変更を **HEAD (主 checkout
  では `main`) へ直接コミット**し、step 3 の `git push origin main` は **behind だと必ず失敗**。
  失敗を stderr に出すだけだったので「**コミットは main に積まれ push は落ちる**」が毎日
  繰り返され乖離が再生産されていた。09-17 の PR #264 は *backlog* を掃除したが
  *ジェネレータ*を残しており、**5 日で ahead 8 まで再蓄積**した
- ✅ main への push 失敗時に **`kb-rescue/<branch>-<date>` へ退避**する形に修復。
  **成功時の挙動は不変**、`--force` なし、origin/main 不変
- **pin 3 本** (`tests/test_session_end_save_rescue.py`) — **grep でなく実 git repo +
  bare remote に実スクリプトを走らせる**。counterfactual 確認済
- ✅ **座礁 KB の救済**: 8 commit のうち **main が既に上位互換だった 6 ファイルは main を採用**、
  実質失われていた **4 ファイルのみ**救済 (`raw/trade-logs/2026-09-21.md` /
  `wiki/index.md` の 09-21 System State / `log.md` / `strategies/tokyo-nakane-momentum.md`)
- 🔵 救済した 09-21 review は **`tokyo_nakane_momentum` の 8 発火 = 59 分内・3 ペア・全 BUY
  = 独立 8 標本ではなく「JPY 売り」1 ベットの 8 重複**、勝ち 3 本の `close_reason` が全て
  `SL_HIT` = ラベル破綻 3 例目、を記録していた。**昇格審査でクラスタ性を N から割り引く
  前処理が必須**という論点が保存された
- 🔴 `wiki/sessions/2026-08-30-session.md` に **conflict marker が 3 週間コミットされたまま**
  main に載っていた (未解決の `git stash pop`)。union 解決し **KB 全体で marker ゼロ**を確認
- 🔴🔴 **その欠陥は診断した直後に実害を出していた** — hermetic 化する前のテスト実行が
  **pre-commit 経由で走った際に、リークした `GIT_INDEX_FILE` 越しに実リポジトリの index へ
  `git add -A` を適用**しており、直後の私の commit がその index を丸ごと拾って
  **3,568 files / −1,156,926 行 (tools/ tests/ reports/ data/ ほぼ全部) を削除する commit**
  を作っていた。`git show --stat` の行数が異常値だったので即検知 ⇒ **push 前に**
  `reset --mixed` で撤回し、**ファイル名を列挙して再 stage** (working tree は無傷 =
  index のみの汚染だった)。以後この PR では `git add -A` を使わない
- 🔑 **教訓 2 つ**: (1) **テストが「実リポジトリを触りうる」形なら、それは CI/pre-commit
  経由で実際に触る** — hermetic 化は品質の問題ではなく安全性の問題だった。
  (2) **commit 後に `--stat` の規模を必ず見る** — 「exit 0」でも「意図した範囲」の保証は
  ない ([[feedback_commit_exit0_lies_autosaver_bypasses_precommit]] の index 版)
- 🔑 **「backlog を掃除した」と「ジェネレータを止めた」は別** — 再発する欠陥は事象ではなく
  **発生機構**に pin を置く
- 🔵 **pre-commit が私のテストの欠陥を捕まえた**: 初版は `GIT_DIR` / `GIT_INDEX_FILE` の
  **環境変数リーク**を考慮しておらず、**pre-commit 経由 (= `git commit` の中から pytest が
  走る) では temp repo でなく実リポジトリを操作**して実 pre-commit を temp ディレクトリに
  対して発火させていた (**単体実行では通るので気づけない**)。`GIT_*` 除去 +
  `core.hooksPath=/dev/null` で hermetic 化し、**リーク環境を再現した状態でも通ることを
  実測**。🔑 **テストを足したら「CI/pre-commit が呼ぶ経路でも同じ結論か」を確認する**

### Phase 21: PR #276 レビュー 2 巡目 — main 上でも「KB だけ」を証明していなかった
- 1 巡目の `BRANCH == main` ガードは feature ケースを塞いだが、**local main 自体に
  未公開の非 KB コミット**があれば `HEAD:refs/heads/...` は**それも publish** する。
  しかも**本ランが KB を 1 件もコミットしていなくても発火**していた
- ⇒ (a) **本ランが実際にコミットしたか**を HEAD 前後比較で判定 (`exit 0` は commit 成功を
  意味しない) / (b) **`origin/main..HEAD` の全コミットが hook 製 KB コミットであること**を
  subject **と** `git diff-tree` のパス両方で検査。混在/検査不能なら **publish せず停止**
- 🔑 **公開は不可逆なので、ここだけ fail-closed の向きが「退避しない」** — 座礁 (回復可能) と publish (回復不可能) では安全な向きが逆。**「安全側」は一意でなく、
  何が不可逆かで決まる**
- pin 5 → **7 本**、2 件とも counterfactual 確認済
- 実測: full suite **3590 passed, 15 skipped, 1 xfailed** / check.py 全10チェック通過

### Phase 22: PR #276 レビュー 3 巡目 — 一意性が目的なら省略形を使ってはいけない
- 退避 ref の sha を `--short` で作っていたが **`core.abbrev` に従う**ので 4 桁まで縮みうり、
  **2 巡目で直したはずの non-fast-forward 座礁が再発**する。完全 sha (40 桁) へ変更
- pin も「**ref が 40 桁 sha を含む**」ことを assert する形へ (CF: `--short=4` で
  `got 'f269'` と落ちる)

### Phase 23: PR #276 レビュー 4 巡目 — ガードが「存在意義そのものの場面」で反転していた
- 混在検査を `git diff-tree ... | grep -qv '^knowledge-base/'` と**パイプで**書いていたため、
  **非 KB パスが早く、後続 KB パスがパイプバッファ (64KiB) を埋めるほど多い**とき
  `grep -q` が先に exit し `git` が **SIGPIPE (141)** を受ける。`pipefail` により
  パイプライン status が非ゼロ ⇒ **`if` は偽** ⇒ **混在履歴を「KB だけ」と判定して publish**
- ⚠️ **仮想的な話ではない** — 本セッションで実際に作った **3,568 files の混在コミットが
  まさにこの形**。⇒ パイプを廃し変数に取ってシェルで走査
- pin 7 → **8 本**: 600 個の長名 KB ファイル + 早くソートされる `app.py` で条件を再現し、
  **counterfactual でパイプ版が実際に publish してしまうことを実測**
- 🔑 **パイプ + `pipefail` + 早期 exit するフィルタ の組み合わせは、判定を静かに反転させる**

### Phase 24: PR #276 レビュー 5 巡目 — 検査した sha を押す / 残り 1 件は明示的に繰延
- (a) **P1: refspec の source を固定 sha に** — `HEAD` は symbolic なので、収集〜検査の間に
  **別エージェントが共有 checkout へコミット**すると、検査は旧履歴なのに**新しい HEAD が
  publish** される。⇒ `VALIDATED` を検査**前**に固定し、push もそれを明示的に押す
- 🔵 **この pin は「機構 pin」だと明示した** — 当該 race には**決定的な注入点が無く**、
  `HEAD:` に戻しても behavioural pin は緑のままだった (= 恒真)。⇒ **恒真 pin を
  「検証済み」と偽らず**、機構 assertion (refspec source が `HEAD` でない ∧ sha が検査前に
  固定) へ書き換え、**限界を docstring に明記**
- (b) **P2 は意図的に繰延** — post-commit が**コミット作成後**に session log へ追記するため
  **末尾差分が必ず 1 本残る** (本セッションで merge を 2 回ブロックした実害)。ただし
  **PR #276 が作った欠陥ではなく既存設計**なので、registry
  `kb-session-log-postcommit-trailing-edit` (**期日 2026-10-13**) へ三択で繰延
- 🔑 **振る舞いで確かめられないものを振る舞い pin のふりで置かない** — 恒真 pin は
  **検知していないことを検知したと錯覚させる**ので、空振りゲートと同じ害になる
- pin 8 → **10 本**

### Phase 25: PR #276 のマージ判断 — 「5 巡消化した」は「クリーンになった」ではない
- CI 3 ジョブ green。ただし**最終 commit `7bd6fed9` へのレビューは到着を待たずにマージ**
  した (connector が同一 PR への連続依頼で応答しなくなった)。**これを伏せずに registry
  `review-backlog-272-273-continuation` へ追補**し、期日までに #276 最終 HEAD も
  triage する枠にした
- #276 の欠陥族 3 系統: (a) **頼まれていない publish** (b) **ガードの範囲不足**
  (c) **shell の落とし穴** (SIGPIPE×pipefail / `--short` の `core.abbrev` 依存 /
  symbolic HEAD の TOCTOU)。いずれも**修復のたびに一段狭い同型**が出た
- 🔑 **残余リスクの向きは「退避しない」(回復可能) であり「publish してしまう」
  (回復不可能) ではない** — 全ガードを fail-closed 方向に倒してあるので、
  マージ判断はこの非対称性に基づく

### Phase 26: 本番 HTTP 全盲インシデント (00:15〜03:33Z) の根本原因確定と修復 (rule:R3)
- **形**: deploy 00:12:50 (`data(mof-statements): daily collect`) 直後から **3h18m** 全 API が 499 (client timeout)。`[MainLoop]` tick は連続、`[API-SLOW]` 0 行、`WORKER TIMEOUT` 0 行、memory 562MB/4GB。03:33:27 に Render が SIGTERM → 再起動 → 03:35:55 復旧
- 🔴 **同型が過去に 2 件** (本調査で発見): 09-12 (3h31m) / 09-15 (3h22m)。3 件とも **UTC 0 時台デプロイ直後**。0 時台起動 7 件中 3 件 (43%) が盲目、0 時台以外は 0 件
- 🔑 **根本原因**: Render の gunicorn は app.py を **master (PID 39) で import** し 20〜300ms 後に worker を fork。`DemoTrader.__init__` の DailyReviewEngine スレッドが 0 時台起動で**即座に**全件スキャン + 204MB backup を master で回し、fork がその最中に落ちる → worker は SQLite のプロセス共有 mutex を locked のまま継承 → **DB を触る全ルートが永久ハング** (`HEAD /` だけ 200)。gthread main loop は notify を続けるので `--timeout` 不発、Render は TCP 疎通のみ → 数時間放置。engine は **master 側で**生存 (= 「engine 生存」と「HTTP 生存」は別プロセスの別 estimand)
- 🔵 **局所再現**: 親スレッドが SQLite backup/読み書き中に 60 回 fork → 子の fresh 接続が **6/60 壊れる** (対照 0/60)。macOS では失敗モードが例外 (`disk I/O error`)、Linux のハングは未再現 — 比率は引用禁止
- 🔵 **副産物**: **エンジンが master と worker で二重に走っている**ことを確定 (全盲中は master のみ前進 / 03:35:56 StatusHeal 後は `iter=240` が 2 回)。in-memory dedup はプロセス内のみ、DB フラグは dup を防がない。本 PR では触らず registry `dual-engine-master-worker-disposition` (10-06) へ
- ✅ **修復** (取引パラメータ不変): (F1) `DailyReviewEngine.start(defer=True)` + heartbeat 経路の `ensure_daily_review_running` (§11 と同型、StatusHeal からは呼ばない — master に戻るため) (F2) `/healthz/http` (fresh sqlite connect + SELECT 1、StatusHeal 非接触) + render.yaml `healthCheckPath` (F3) 夜間 ingest 3 パスを ignoredPaths へ (毎晩 2 回のデプロイ churn) (F4) `freshness_policy.classify_outage` SSOT + watcher `http_blind` (read-timeout 全滅 = listen 中・無応答 ↔ 接続拒否/5xx = api_down) (F5) daily_report: `FetchResult` で失敗と空を分離、DATA FETCH テーブル + 規則 5 + 原因捏造の出力後検査 → 生成器注記
- **pin 5 ファイル / counterfactual 5 本実測** (start() 復元・heartbeat 呼び出し削除・http_blind 分岐削除・healthCheckPath 削除・捏造ガード削除で各 1 本が赤)
- 🔑 **教訓**: §11 (2026-07-16) は同じ fork 問題を **network thread だけ**塞いでいた。「対称に処置せよ」の 3 例目 — resource の種類 (network / DB) を替えて同型が再発。lesson: [[lesson-prefork-master-must-not-touch-db-2026-09-22]] / 分析: [[http-blind-fork-poisoning-2026-09-22]]
- 🔴 **KB 記録の訂正**: `raw/trade-logs/2026-09-22-pre_tokyo.md` の「Render 無料 tier 特有のスリープ」は**捏造** (Pro plan、原因は fork 中 SQLite 継承)。`data/monitoring/nav_floor_projection.csv` の 09-22 行欠落も同事故の直接結果

### Phase 27: PR #277 レビュー P2×2 消化 — 同 PR 内で「片側だけ塞ぐ」を 2 回
- (a) `classify_outage`: 「timeout ゼロなら api_down」が **other / 4xx 混在まで停止と断定**していた (全 401 = serving 中なのに「停止」)。⇒ api_down は connection/5xx **のみ**、全 4xx は `http_error`、他は mixed。状態コードは " for url" 以降 (`:443`) を見ない
- (b) 捏造ガードが analyst のみ → **analyst 本文を入力に受け取る strategy planner** が同じ原因語を再生産できた。⇒ `finalize_llm_report` を両レポート共通の出口に + planner prompt に禁止規則
- 🔑 同じ PR で「対称の反対側」を 2 回取りこぼした — 検知器を書いたら「同じ入力を受ける**他の消費者**」を列挙する

### Phase 28: PR #277 レビュー 3 巡目 P2×3 — 「部分集合を母集団として分類」「観測から状態を断定」「ガードの適用範囲」
- (c) 失敗分だけを `classify_outage` に渡していた → 1 本 timeout + 4 本成功が「HTTP 全盲」。`n_ok` を渡して `partial` へ
- (d) `*.onrender.com` への read-timeout は edge までの接続しか証明しない。「プロセスは listen 中」は Render ログ (`HEAD /` 200 / `[MainLoop]`) で裏取りした事実であって、検知器の観測から出る結論ではない → `http_blind` は観測クラス、文言から断定を除去
- (e) 捏造ガードが fetch 成功時にも掛かり、否定文も脚注していた → `n_failed>0` のみ + 文単位で否定マーカー除外 (`find_invented_causes`)
- 🔑 3 巡で計 5 件、全て「検知器が何を測っているか」の estimand 境界の取りこぼし。**分類関数には母集団 (成功数) を渡せ / 観測の名前に状態の主張を混ぜるな / ガードの適用条件を明示せよ**
- 並行セッションから連絡: main に #278 が入り sessions/2026-09-22-session.md のみ conflict → union で解決してマージへ
## 2026-09-22 (JST 15:00〜) — 「今どこで止まってて何をすれば勝てるか」再評価 (read-only、rule:R3)

### 実施
- user 問いに対し、一次確認 (本番 API / Render ログ / registry / KB 原本) → 14 エージェント workflow (地上 4 / 提案 3 / 反証 6 / 統合 1) → 統合レポートを [[path-to-win-reassessment-2026-09-22]] に保存。反証で refuted 20 件、採用施策 8 件、user 決裁 7 件 (UD1–UD7)。
- **結論**: 09-20 メモの不動点は成立 (3 脚で過剰決定)。$0 で time-to-M2 を有限化するレバーは無し。30 日の Claude の仕事は (a) 統合決裁パケットを**訂正済み入力**で 10-08 に届ける (b) kill 検知器 (F3/F4/M3b) の estimand 是正 (c) floor と観測系の防御。

### 新事実 (KB 未記録だったもの)
- 🔴 **本番 HTTP 全断 00:15→03:35Z (3h20m)**: 00:12 の `data(mof-statements)` deploy 直後から全リクエスト 499 (15s client timeout) / 03:32 502。**engine は生存** (MainLoop tick 02:40・02:58–03:33 連続、Watchdog HB 03:29)。Render が 03:33:38 に instance 再起動 → 復旧 03:35:55。その後 deploy 再起動 ×3 (03:56 #272 / 04:09 #273 / 06:01 #276)。09-15 03:39–03:49 にも同型 (instance restarted 03:49:54)。**root cause UNKNOWN** (仮説: deploy 直後の gthread 枯渇 / SQLite lock)。daily report 03:11 は 0 データで「Render 無料 tier スリープ」と誤帰属 (サービスは Pro) — 無効。nav_floor CSV 09-22 行も欠落。**別セッション (background task) で根因調査・churn 遮断・検知器を実施中**。
- wg 09-20 21:01Z: USD_JPY gap −19.0p < 21.4p / AUD_USD −20.5p < 25.0p → NO-QUALIFY、G0' event #2 は 09-27 へ繰越 (分母外)。09-13 ABANDONED_DRIFT は改定後 1 件目の不成立として既カウント → **次の qualifying 不成立 1 件で執行モダリティ R1 再審**。card 未転記。
- LIVE 約定は 09-11 が最後 (10.8 日、freshness stale)。30d clean live N=9 / −27.2p、M1 NOT_MET、M1_STRONG 0 セル。carry_dip の drought は agg_kelly ではなく `_tick_entry` 下流ガード (velocity_down 4 / same_price 1 / AGG_KELLY 0、11d 台帳)。11 日ゼロは Poisson P 0.045–0.16 で異常ではない。
- rnb_support_bounce closed shadow N=2 (09-11 以降 0) → 09-24 checkpoint-1 (n_floor 3) TRIGGERED 見込み。
- 研究: 着手可能ライン 0 (#27 FAIL)。E1 評価器 96 tests green、凍結 export tool 不在 (手続き要件、verdict 無効ではない)。

### 09-20 メモの訂正 (引用前に必ず参照)
1. 「carry_dip は demo +103.0p / broker −41.1p で符号逆」→ **refuted**。突合 7 本は demo −16.4p / broker −41.1p で両方負、正符号は未突合 3 本 (+130.9p)。reconcile 対象は 14 本。
2. 「無回答なら 12-03 に F4 強制起票」→ F4 の読み手は fit (CSV 09-16 以降全 fit)。発火は keeper のみ 2027-01-08 / drift 込み 12-04〜07。keeper のみなら F2 (12-31) の後。
3. D2(i)「0.2x 解除 = eq_peak ラチェット」→ stale。SSOT は JPY 台帳 (demo_trader.py:1002–1004)。**D2(i) は 07-03 P0-1 (edge-cell lot × dd_mult) と 08-05 template L73 (乗算しない) の user 決裁 2 件の衝突**で、L1 開通と同時に binding。
4. 「D3 (4.2 多 leg) は template 既決」→ 未裁定 (L167 は単 leg 例)。L1 開通 NAV ¥300k vs ¥900k。
5. USD-quote L1 開通 NAV ¥450k → 為替 157.6 で ¥472,800。keeper 単価はプロコース USD/JPY 0.8 銭 原則固定 = 構造下限 (設計値変更の効果 ≤¥260/月)。

### 決裁依頼 (user)
- UD1 (即日): OANDA status 画面で 9 月 keeper $520k が「今月の取引額」に算入され GOLD 表示か。status 頁は毎営業日判定なので今日読める。
- UD3 (10-31): 会員単位の出来高合算 (user 手動 USD_JPY 口座で keeper 代替 = 09-01 案 B の再決裁) の一次確認可否。
- UD4 (10-15 推奨) U2 資本上限 / UD5 (11-30) D1–D12 / UD6 D5 carry_dip / UD7 D8 4 原則解釈。

### メモリ更新
- feedback_respond_in_japanese (新規) / project_path_to_win_memo_2026_09_20 (訂正追記) / project_http_blackout_engine_alive_2026_09_22 (新規)
20. fix(review): #277 Codex P2×3 — 部分失敗を partial に限定 / http_blind を観測クラスに格下げ / 捏造ガードを失敗時のみ+否定文除外 (rule:R3) (304875e8, 8 files)

### Phase 29: PR #277 レビュー 4 巡目 P2×2 — 5xx は「応答あり」/ 否定は節単位
- (f) 5xx を transport-down に数えていた → app 由来 500 でも「origin 応答なし」。`api_down` は connection のみ、全 5xx は `http_5xx` (edge か app かは状態コードで判別不能)
- (g) 否定判定が文全体 → 「X ではなく、スリープが原因」が素通り。原因語と同じ節の否定だけを認める
- 🔑 4 巡 7 件の共通形: **含意を観測の名前に混ぜる**。分類器の各クラスに「観測」と「含意しないこと」を並記する

### Phase 30: PR #277 レビュー 5 巡目 P2×2 — 後段が前段の分類を畳み戻していた
- (h) 分類器は http_error / http_5xx を返すのに watcher が `api_unreachable` に畳み、通知文が復旧手順へ誤誘導 → 専用 event `api_http_error`
- (i) 節単位の否定でも「API が応答しないのは…スリープが原因」の「しない」が原因語を隠す → 否定は原因語束縛のみ (直後窓 / 英語前置)
- 🔑 分類の後段 (通知文・ガード) は分類器と同じ粒度を保つ。前段で分けた情報を後段で再び畳むと、分類器のテストは緑のまま通知だけが誤る

### Phase 31: PR #277 レビュー 6 巡目 P2×1 — `SELECT 1` は DB を触らない
- health check プローブが定数評価で読取りロックを取らず、ファイルロック / 詰まった txn で DB ルートが全滞留しても 200 を返す → `sqlite_master` の実 read へ
- 🔑 「DB が生きているか」のプローブは DB ルートと**同じ資源で待つ文**でなければ estimand が違う

### Phase 32: スプリント 0922 執行 — 11 PR マージ + registry / KB 統合 follow-up (rule:R3)
- **マージ済み PR (UTC)**: #278 再評価 08:37 / #280 triage + PR 規律 09:10 / #277 HTTP 全盲根因修復 10:06 / #282 carry_dip 突合 14/14 10:11 / #279 統合決裁パケット v0 14:15 / #281 wg card + R1 骨子 14:15 / #283 rnb lane-health 14:16 / #286 E1 凍結 export tool 14:16 / #287 storm guard 4 点 14:17 / #284 臨時スキャン #29 step 0 14:17 / #285 F4 decomposed 14:29 — code 4 (#277 / #285 / #286 / #287) は市場時間中のマージ (今後は market-safe 窓、[[sprint-triage-pr-discipline-2026-09-22]] §3.2)
- **巡目実測** (`@codex review` 依頼回数): 依頼あり 9 PR で 5〜18 (中央値 6)、#287 = 18 (P1 18 件全修正) / #286 = 8 / #285 = 7。docs-only #278 / #280 は依頼なし
- **P2 繰延 9 件** (6 PR、上限 ≤6 到達、全て実欠陥・未修正) → registry `review-backlog-sprint0922-p2-deferrals` (索引 10-17) + 期日別 sub-entry `-0926/-1003/-1006/-1008` (09-26 = event #3 前日、cron 判定は翌 00:20Z) (Codex P2 2 巡目で分割)。`review-ack` は繰延の明示のみで解決としての使用 0 件
- **registry 統合 (本 follow-up PR)**: 新規 6 (`review-backlog-sprint0922-p2-deferrals` / `ud1-gold-screen-check` 09-23 / `e1-first-look-freeze-due` 10-08 / `storm-guard-enforce-decision` 10-20 / `wg-dst-cutoff-basis-r3` 10-25 / `sprint-0922-triage-close` 10-22) + 既存 9 へ 09-22 追記 (g0prime 期日 09-28 → 10-05、condition / since 不変)。#285 が先に registry を編集したため、本 PR は #285 マージ後の origin/main (dd446899) から分岐して衝突を回避
- **render.yaml**: `knowledge-base/raw/alpha_budget/**` と `knowledge-base/wiki/research/**` を ignoredPaths へ — 06:24Z の `auto: KB session-end save` (b53fd5cd) が alpha_budget json 1 ファイルで本番を再デプロイした残余 churn。app.py / modules/ の参照ゼロを grep で確認 (読み手は cron service と手動 ingest のみ)、pin 追加
- 🔴 **未解決**: 取引エンジンの master / worker 二重起動 (#277 副産物、`dual-engine-master-worker-disposition` 10-06、shadow N の regime break を伴うため未着手) / UD1 Gold 画面確認は user 未回答 (`ud1-gold-screen-check` 09-23) / status['storm_guard'] の読み手 (daily report / anomaly_watcher) 未作成 = write-only
- hot file 同期: index.md System State 先頭 / wiki/changelog.md + CHANGELOG.md / research/index.md (adhoc-scan link) / audit-index.md 3 行 / 本 Phase
