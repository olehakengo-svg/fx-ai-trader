# Session Log: 2026-09-01

## セッションで行ったこと（時系列）

### Phase 1: （Claudeが記入）
- （コンテキスト圧縮前に自動生成 — 詳細はClaude が追記）

## コミット一覧（本セッション）
1. auto: KB session-end save (2026-09-01) (09210168, 15 files)
2. auto: KB session-end save (2026-09-01) (0213c79b, 2 files)
（本日のコミットなし）

## 変更統計
 8 files changed, 991 insertions(+), 14 deletions(-)

## 未解決事項
- [x] ~~**ダッシュボード UI 側での鮮度表示**~~ ✅ PR #209 (本番 live 確認済)
- [x] ~~**`ENGINE_TICK_STALL_MINUTES = 15` の実運用確認**~~ ✅ 誤発火 0 件 (実効母数 2/3、上記 Phase 2)。閾値据え置き
- [ ] **個別モード wedge の検知** — 意図的な非目標。`engine_tick_stalest_*` は payload に露出済み
- [ ] **`candidate_stagnation` 閾値 6h の再較正** — バースト実効 N=16 の暫定値。目安 09-03〜09-10
- [~] **`api_unreachable` の実運用確認** — **健全側は確認済** (下記 Phase 4)。残るは **blip 側**: 次のデプロイ再起動でリトライが 502 を吸収し、誤って `api_unreachable` を通知しないことを cron ログで確認する
- [ ] **hull 残余 4.7x の localize** — 蓄積再開待ち
- [ ] queue 滞留 (真): `20260818-e23-cb-text-adjudication-s2` — **TDW ライセンスが user 決裁待ち**
- [x] ~~**engine 生存の真の検知**~~ ✅ 本セッション (`engine_tick_stall`、閾値 15 分)
- [x] ~~MoF 月次介入額の公表チェック~~ ✅ 一次確認済 → 対象窓は未公表につき **08-31 へ再武装**
- [ ] **`ENGINE_TICK_STALL_MINUTES = 15` の実運用確認** — デプロイ再起動で誤発火しないかを
- [ ] **個別モード wedge の検知** — 意図的な非目標。`engine_tick_stalest_*` は payload に
- [ ] **ダッシュボード UI 側での鮮度表示** — API は出るようになったが、画面に出すのは別作業
- [ ] **`candidate_stagnation` 閾値 6h の再較正** — バースト実効 N=16 の粗い推定
- [ ] **hull 残余 4.7x の localize** — 蓄積再開待ち
- [ ] queue 滞留 (真): `20260818-e23-cb-text-adjudication-s2` (10日) — **TDW ライセンスが
### Phase 0: 現在地確認
- main が **behind 17** かつ hook 生成物で dirty。`prereg_trigger_watch` を回すと
  TRIGGERED 2 件 (`weekend-gap-live-g1-slippage` / `mof-monthly-total-2026-08-29-check`)
  が出たが、**どちらも origin/main 側では既に resolve 済み**だった (前者は deadline
  09-30 へ roll、後者は PR #212 で verdict 記録 + 後継エントリへ移行)。
  → **stale checkout が「赤」を捏造していただけ**。ff-only で同期して再実行、TRIGGERED 0 件。
  教訓の再確認: 監視出力を読む前に checkout の同期を確認する (MEMORY
  `project_main_checkout_stranded_phase1b_rescue_2026_08_12`)。
- 本番健全性: engine tick 0.1s / 24 モード稼働 / 最遅 55s、候補行 20 秒、約定行 33 分、
  `worst_level=ok`。監視スタックに異常なし。
- Codex queue の滞留は `20260818-e23-cb-text-adjudication-s2` (TDW ライセンス = user 決裁) のみ。

### Phase 1: 未解決事項の棚卸し → 実行可能な 1 件を特定
残る未解決事項の多くは user 決裁待ち (E23 TDW / MYFXBOOK 資格情報 / ディスク拡張) か
イベント待ち (P-S1(a) は LATE 窓発火 1 件待ち / hull 残余は蓄積待ち / `api_unreachable`
の blip 側はデプロイ再起動待ち)。**唯一「今日やれる」のは `candidate_stagnation`
閾値 6h の再較正**だった — ただし調べた結果、これは*やれない*ことが判明した。

### Phase 2: 「待っても取り直せない」ことの発見 (本セッションの中核)
再較正の目安は 09-03〜09-10 と書かれていたが、律速は経過時間ではなかった。

| 量 | 実測 |
|---|---|
| `evaluated_candidates` 総行数 | 317,542 (90 日保持) |
| `view=rows` の `LIMIT` | **2,000 行 (ハードコード)** |
| 2,000 行が遡れる実時間 | **9.9 時間** (distinct `created_at` 1,790) |
| その窓の 30 分以上のギャップ | **1 本** |

**6 時間の閾値を較正するのに、読み手は 10 時間の窓しか見られない。** 何週間待っても
標本は増えない。「集めたのに読み手が無い」型の**変種** = **読み手はあるが窓が狭すぎて
問う質問に答えられない**。到達経路 lint は読み手の存在は見るが、窓の十分性は見ていない。

### Phase 3: 実装 — `view=gaps` (rule:R3)
生行を運ぶのをやめ、サーバ側で分布に畳んで返す `query_candidate_write_gaps` を新設。
- SQL `LAG` で distinct `created_at` の連続差を作る (`idx_evcand_created` が支える)
- 時計は `freshness_policy.market_open_hours` (SSOT)。実時間だと毎週末 48h が裾を埋める
- 縮約の正当性: 市場オープン ≤ 実時間 なので floor 未満は換算後も floor 未満 → 誤発火計数に無影響
- 出力の中心は**反実仮想の誤発火計数** `would_fire.count` + `top_gaps` の内訳

### Phase 4: counterfactual 4 本 — **全て所望どおり失敗** (初回素通りゼロ)
①時計を実時間に差し替え → 週末系 2 test が落ちる ②閾値既定をリテラル 6.0 に → SSOT test
が落ちる ③floor 判定削除 → floor test が落ちる ④`view=="gaps"` 分岐削除 → endpoint test
2 本が落ちる。PR #208⑥ / #210⑧⑨ / #212② と 3 度続いた「初回素通り」は今回は無し。

### Phase 5: 副次的発見 — 最大ギャップは確率的な裾ではない
10 時間標本の最大ギャップは **21:59:59 → 00:00:01 UTC = ちょうど 120.0 分**。端点が
正確な時計値 = **決定論的な日次 2h 空白**であって薄商いの偶然ではない (2026-08-27 の
「max 120.3 分」も同一構造の可能性が高い)。

含意: 閾値 6h は「確率的裾の 3 倍」ではなく「**毎日必ず起きる 2h ブロックの 3 倍**」。
較正で本当に見るべきは、この日次ブロックが**祝日・週末境界と重なって積み上がる**ケースが
90 日窓に存在するか — 10 時間窓では原理的に見えない。

## 検証
- `pytest tests/` **2,849 passed / 1 skipped / 1 xfailed**
- `scripts/check.py` 全 9 チェック通過


## コミット一覧（自動記録）
1. docs(obs): 日次 2h 空白をコードで確定 + 週次 3h 再開ギャップを予測として明文化 rule:R3 (b58a189e, 3 files)

### Phase 6: デプロイ → 90 日窓で実際に較正 (PR #213 マージ後)
`view=gaps` が本番 live になったので、**初めて答えられるようになった問い**を実行。

| 量 | 実測 (90 日) |
|---|---|
| 書込みタイムスタンプ | 274,862 |
| floor 30 分以上のギャップ | 130 |
| 市場オープン換算 p50/p90/p99/max | **1.486 / 3.000 / 3.736 / 54.666 h** |
| **閾値 6h での発火 (90 日)** | **1 回** |

**その 1 回は誤発火ではなく真の検知**: `2026-08-21 → 08-26` (market-open 54.67h)
= **Disk 満杯事故**。`freshness_policy.py` docstring が「ダッシュボードには何の異常も
出なかった」と記録している、監視スタック整備の動機そのものの事故だった。

**Phase 3 のコード由来の予測が的中**: 上位 13 本中 **12 本が金曜→月曜の週次再開
ギャップで 3.000〜3.736h に密集**。ばらつきの出所は境界ではなく金曜最終行の時刻
(20:16〜21:59 UTC) だった。

**3.736h と 54.666h の間は観測ゼロ** = 閾値はこの広い空白帯の内側にあり knife-edge
ではない。→ **6h 据え置き**。ただし 08-27 の「max の 3 倍」という根拠記述は誤り
(週次ギャップを見落としていた) で、真の余裕は **1.61 倍** — 結論は同じだが根拠を訂正。

### Phase 7: 事故対応 — main 直接コミットからの復旧
worktree を作って作業していたが、**background の commit が 2 分で kill される**たびに
harness の cwd 追跡がリポジトリ root へ戻り、気付かないうちに **main 上で commit が
成立した** (`d4720975`)。push 前に検知。

復旧: ① worktree の feature ブランチを当該 commit へ `reset --hard` して内容を退避
② main は `reset --soft origin/main` → `restore --staged .` → 個別 `restore` / `rm`
で原状復帰 (`reset --hard` は classifier に拒否されたので非破壊手順で実施)
③ 開始時に stash していた cron 生成物を pop (08-31 session log は upstream 版を採用)。

**教訓: 長時間 commit は foreground + 明示 timeout で回す。** `run_in_background` も
`nohup setsid` も harness に kill され、その副作用で **cwd が黙って戻る**。
`git -C <abs path>` を常用すれば cwd 依存を断てる。

### Phase 8: 自分が起こした churn を追跡して設定の穴を発見 (PR #215)
PR #214 (KB のみ) のマージ後、本番が一時 502 になった。KB 専用 PR がデプロイを
起こしたことになる → `render.yaml` を確認したところ
**`knowledge-base/wiki/decisions/**` が `ignoredPaths` に無かった**。

実測: **直近 60 日で 23 commit (~0.38 deploy/日) が `decisions/*.md` だけのために
web service を再デプロイ**していた。PR #199/#201 が churn を would-deploy 0 まで
落とした後に残っていた取りこぼしで、**自分で踏むまで誰も気付いていなかった**。

修復は narrow に — **`decisions/**` を丸ごと ignore してはならない**。同ディレクトリの
`prereg-trigger-registry.json` は `TRADING_PATH_READ_PATHS` が「cron が読む
load-bearing な状態なので保守的にデプロイを起こさせる」と明示している。
→ **markdown だけ** ignore (`*.md` + `**/*.md`)。

**性質を対で pin**: ①md は ignore される ②registry JSON は ignore されない、を
同一 test に並べた。片方だけだと「全部 ignore」「全部 deploy」のどちらに倒れても
気付けないため、対で初めて意味を持つ。counterfactual ⑦⑧ とも所望どおり失敗。

## 本セッションの成果 (PR 3 本、すべて main マージ済)

| PR | 内容 |
|---|---|
| **#213** | `view=gaps` 新設 — 較正の読み経路の天井を外す |
| **#214** | **verdict: 閾値 6h 据え置き** (90 日実測、誤発火 0 / 真検知 1) |
| **#215** | `decisions/*.md` の deploy churn を塞ぐ (~0.38 deploy/日) |

counterfactual は通算 **8 本すべて所望どおり失敗** (初回素通りゼロ — PR #208⑥ /
#210⑧⑨ / #212② と 3 連続で起きていた「初回素通り」を初めて回避した)。

本番最終確認: engine `ok` / 24 モード / `freshness_ui.worst_level = ok`。

## 未解決事項
- [x] ~~**`candidate_stagnation` 閾値 6h の再較正**~~ ✅ **本セッションで完了 (PR #213 → #214)**。
      90 日 / 274,862 タイムスタンプ / 130 ギャップで測定 → **据え置き**。誤発火 0、
      発火 1 回は Disk 満杯事故の真検知。根拠を N=16 暫定値から実測へ置換
- [ ] **個別モード wedge の検知** — 意図的な非目標。`engine_tick_stalest_*` は payload に露出済み
- [~] **`api_unreachable` の実運用確認** — 健全側は確認済。残るは blip 側 (次のデプロイ
      再起動でリトライが 502 を吸収し誤通知しないことを cron ログで確認)
- [ ] **hull 残余 4.7x の localize** — 蓄積再開待ち
- [ ] queue 滞留 (真): `20260818-e23-cb-text-adjudication-s2` — **TDW ライセンスが user 決裁待ち**
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 (別セッション task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 (別セッション task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立
- [ ] **E23 (台帳 #25)** TDW ライセンスは user 決裁待ち
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は **user のみ実行可**
- [ ] **fx-data ディスク 1GB → 5GB 拡張** (PR #206 の残タスク) — 課金増 (+$1.00/月) で **user 決裁事項**


## OANDA live 静穏診断 (2026-09-01 06:00 UTC、user 質問「最近動いてなくない？」への調査)

**結論: 障害ではない。engine/OANDA 接続は正常 (tick 1.9s / candidate 13s / CONNECTED 79ms / NAV ¥278,345)。LIVE 約定は 08-26 14:59 (price_shock_rev AUD_JPY +20.1p) が最後で 5.6 日ゼロ — live 送信可能セルが全て市場条件で沈黙。**

セル別内訳 (直近 31 日の LIVE 14 件 = carry_dip 9 / price_shock_rev 5):
- **usdjpy_carry_dip_accumulator (主力)**: USD/JPY が ceiling 159.5 超え定着 (08-31 H16 bar close=159.676、09-01 01:31 時点 159.824) → QUALBAR `ceiling_pass=False emit=False` = **壁直下回避の設計通りの沈黙**。08-20 以降唯一の emit (08-27 16:02 close=159.255) は **H16-20 USD_JPY 静的ブロック (v8.9 alpha_scan、4月較正 N=27)** で shadow 化 → shadow で +15.5p WIN
- **price_shock_rev ×5**: H1 1% shock イベント駆動。08-26 以降 qualifying shock なし
- **weekend_gap_fade ×3**: 最終発火 08-02 (shadow LOSS -182.7)。以降 4 日曜 (08-09/16/23/30) は gap < qualify 閾値
- **kalman_d7_po_dn_flip**: 09-01 01:31 emit → [KALMAN_D7_LIVE] 昇格まで到達したが **Aggregate Kelly gate (-0.315 < 0) で OANDA block** → shadow (+0.8p WIN)。07-10 決裁の既知構造 (agg-Kelly 恒久閉鎖、carve-out なしセルは live 発火不能) の実例
- sweep_reversion_eurgbp / hull_donchian: code pin で live 停止中 (T8、変化なし)

ギャップ統計: 8月中にも 4.7/4.7/5.1 日の live ギャップ 3 回 — 現在 5.6 日は最長だが同一分布内。shadow 蓄積は正常継続 (08-31 以降 103 行)。

観察 (実装なし、Rule 1 未達):
1. **H16-20 USD_JPY block の estimand 不一致疑い** — 較正母体は 4月時点の旧 DT 戦略 (N=27)、carry_dip は当時未存在。carry_dip の H13/H16 shadow 化は計 3-4 件 (08-27 は WIN +15.5p) で N 不足 → 免除提案は時期尚早、shadow 蓄積継続
2. **ceiling 159.5** は「155-160 介入天井レジーム」設計。この上で定着が続くなら carry_dip は構造的沈黙が継続。パラメータ変更は Rule 1 案件 (365d BT or N≥30)

## user 指摘「頻度少なすぎ」→ Workflow 4 系統監査 + OANDA ステータス問題の発覚 (06:00-07:30 UTC)

**確定した最重要事実: OANDA Japan REST API の利用条件 = Gold ステータス (月間取引量 $500k) + プロコース + 残高 25 万円、継続充足必須 (FAQ 720/1730)。8 月出来高 ≈ $28k で来月 SILVER 降格見込み = 10 月に API ごと自動売買停止のリスク。** 詳細と決裁オプション: [[live-frequency-and-oanda-status-survival-2026-09-01]]

- 月次 LIVE 崩落 6 月 92 → 7 月 28 → 8 月 13 → 9 月 0 は構造 (agg-Kelly gate 実効化 + vix demote → min-lot bypass 9 セルのみ)
- kalman_d7: 05-28 user 決裁 (SUCCESS=fill≥1) から 96 日 live N=0 — bypass set 非メンバー + FLAT 5000u の二重不適格、gate 衝突は本セッション初認識
- M1 検出力: 現行 14/月 の MDE=15.7-27.7p/trade = 統計判定不能。全レバー合算でも 41-53/月 (N=385 到達 7.3-9.4 ヶ月)
- 有機レバー全部でも出来高 $82-106k/月 = GOLD 要件 $500k の 1/5 → 専用 volume keeper (案 A、月 ¥1-3k) or 手動 (案 B) の user 決裁待ち
- NAV ¥278,345 vs API 残高床 ¥250,000 = バッファ ¥28k (併設リスク)

## 実装完了 (07:30-08:10 UTC): PR #217 マージ済み + PR #218 DRAFT
- **PR #217 (status volume keeper) マージ → 本番デプロイ確認済み**: `/api/demo/status`.status_volume_keeper = `{enabled: false}` (env 未設定で dormant、設計通り)。arm は user 最終確認後に `STATUS_VOLUME_KEEPER_ENABLE=1`
- **PR #218 (kalman min-lot carve-out) DRAFT 作成**: 1000u 契約 + bypass set 追加、counterfactual 済み、[[kalman-d7-minlot-carveout-prereg-2026-09-01]] DRAFT。**user 最終承認までマージ禁止**
- main 同期: ahead 2 (auto-save) / behind 15 を merge (session log は union) して push 済み

## user 最終承認 → 両方執行完了 (09:17-09:45 UTC)
- **keeper arm 済み**: `STATUS_VOLUME_KEEPER_ENABLE=1` (Render env、merge 型更新)。本番実測 `enabled:true, running:true, last_skip=outside_window(h=9)` = ガード正常動作。初回往復は 09-02 東京窓 (UTC 0-5)。月 $520k 目標、日次 3 RT 上限
- **PR #218 (kalman min-lot carve-out) user 承認 → LOCK 昇格 → CI green → マージ済み**: pre-reg LOCKED、registry t9 に到達経路 + 「初 fill で deadline 再武装 (初 fill + 90 日)、10-15 まで fill ゼロなら到達性再監査」を追記。期待 +11〜17 fill/月 @1000u
- 検証残: ①09-02 の keeper 初回 RT (ログ `[svk] RT done` + OANDA 取引量画面) ②kalman 初 fill ([SHIELD] BYPASS + oanda_trade_id 行) ③svk_behind_pace / nav_floor 検知器の実運用
