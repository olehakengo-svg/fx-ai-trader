# Session Log: 2026-08-24

## セッションで行ったこと（時系列）

### Phase 0: sync guard
- main が origin より **1 commit behind** (`7c2ae4c8`)。`git rev-list --left-right` の
  左右を読み違えて一度 push を試み rejected → stash → `pull --ff-only` → pop で復旧。
  作業は worktree `.worktrees/cand-readout` に分離。

### Phase 1: 現在地確認
- roadmap v2.3 の T 項目は全てクローズ済 (T-MTF は 08-23 に是正済)。
- `prereg_trigger_watch`: TRIGGERED は `t5-jpy-cap-restore-price` のみ (第2要件は
  外部一次情報限定 + 復帰は R1 = user 決裁のため autopilot 執行不可)。
- `ps1a_execution_check`: unique N=9/10 で WAITING (最終発火 08-12、11.1 日前)。
- → 期日系は全て未到達。**実行可能な最優先 = 発火率アノマリの R3 診断**と判断。

### Phase 2: hull_donchian_fade 発火率ギャップの funnel 分解 (rule:R3)
`t8-hull-shadow-freq` が 49 日間「実測 1.62/週 vs 期待 13.3/週」を表示し続けていた件。
詳細: [[hull-fire-rate-funnel-2026-08-24]]

- **階段関数の発見**: hull trade は全 17 行 (全 shadow)、**最終発火 08-06 で以後 18 日ゼロ**。
  週平均が停止を平滑化して隠していた。
- **シグナル生成器は健全**: 凍結スペックを MASSIVE EUR_USD 15m にオフライン再生
  (16.3 週) → **205 signals = 12.59/週** で期待値を再現。無発火窓にも **33 signals**。
- **funnel**: 12.59 → HTF Hard Block −25.4% → 9.39 → 実測 hold 直列化 → 7.61/週
  vs 実測 1.62/週 = **残余 ~4.7x 未説明**。
- **誤結論を 1 度回避**: `max_hold_bars=96` (24h) を直列化に使うと残余 1.90x で
  「ほぼ説明できた」に見えたが、実測 hold は median **0.57h** → 残余は 4.7x が正。
- **「MR ⇒ counter-HTF kill ~100%」は hull に当てはまらない**: 実測 25.4%
  (生存 153 中 119 が `htf=mixed` 窓)。sweep の観測を family 一般則に外挿しない。
- **block counter はゼロ**: `/api/demo/block-counts?strategy=hull_donchian_fade` は空。
  同時刻の `daytrade_eur:order_bar_dedup=52` は wick 27 + doji 25 で完全に説明され
  hull の寄与ゼロ。ただし counter は restart でリセットされ観測窓 ~3h のため決定的でない。

### Phase 3: 真因 = C1 テーブルが write-only → 読み出し経路を新設
- `evaluated_candidates` は 2026-04-28 から毎バー書かれているが **route 無し**、
  `query_candidate_summary()` の呼び出し元は **自身の unit test のみ**。
  silent drop 可視化のための基盤が観測不能のまま 4 ヶ月データを蓄積していた。
- fix: `GET /api/demo/evaluated-candidates` (`view=summary|rows|meta`) +
  `query_candidate_meta` / `query_candidate_rows`。テスト 10 件追加。
- estimand 警告を docstring に内蔵 (記録は HTF Hard Block の**後**なので
  count=0 は「シグナル無し」ではなく「select_best 段まで生存した候補が無し」)。
- 付随観測: retention job が無く単調増加 → `view=meta` で行数を可視化 (挙動不変)。

## 検証
- `pytest tests/` → **2681 passed, 17 skipped, 1 xfailed**
- `scripts/check.py` → **全9チェック通過** (警告は既存の broken wikilink と queue SLA)
- `prereg_trigger_watch` は registry 変更後も正常動作
- live パラメータ・発注挙動は不変更 (観測系のみ)

## 次アクション
1. デプロイ後 1 週間以上蓄積 →
   `/api/demo/evaluated-candidates?strategy=hull_donchian_fade&days=14&view=summary`
   で残余 4.7x を **(a) select_best 段未到達 / (b) 到達後の下流 block** に二分。
2. (a) なら `HTF_BLOCK_SHADOW_RESCUE` への hull 登録可否を 4原則#3 の観点で検討 (R3)。
3. `evaluated_candidates` の retention は別 R3。

## 未解決事項
- [ ] **hull 残余 4.7x の localize** — 上記 next action 1 (蓄積待ち)
- [ ] `evaluated_candidates` の retention/rotation 不在 (Render Disk 圧迫リスク)
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 (別セッション task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 (別セッション task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立。成立日に runbook §3 を直接執行
- [ ] **MoF 月次介入額の公表チェック (~08-29)** — `mof-monthly-total-2026-08-29-check` (deadline 08-28)
- [ ] **E23 (台帳 #25)** TDW ライセンスは user 決裁待ち
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は **user のみ実行可**
- [ ] queue 滞留: `20260818-e23-cb-text-adjudication-s2` (6日) / `20260819-family-a-statement-ladder-prereg-draft` (5日) — SLA 3日超
