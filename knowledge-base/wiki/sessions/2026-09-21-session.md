# Session Log: 2026-09-21

## セッションで行ったこと（時系列）

### Phase 1: 現在地確認 (autopilot 起動)
- `git log` / registry / scheduled-tasks を照合。**直近の active registry は 2 件**:
  `rnb-shadow-lane-health-checkpoint-1` (期日 09-24) と
  `ps-seat-supply-hourly-c1-coverage` (期日 09-25)
- `tools/prereg_trigger_watch.py` 実行 — TRIGGERED は `t5-jpy-cap-restore-price` のみ
  (**Rule 1 = user 決裁事項なので autopilot は執行しない**、第2要件は外部一次情報待ちで保留のまま)
- 09-20/09-21 に自動化コミットが無いことを確認したが、scheduled-tasks の `lastRunAt` は
  いずれも当日 (fx-daily-quant-loop 09-20T22:06Z / phase1b 09-20T21:34Z / oanda-sentiment
  09-21T00:06Z) で**タスク自体は発火している** = 週末で出力が空だった可能性が高く、
  異常と断定できない。本セッションでは扱わず観測のみ記録

### Phase 2: ps 席 C1 readout の 4 日前倒し執行 (rule:R3)
- **手順 1 (計装到達) を期日を待たずに先に叩いた** — この手順は「届いていなければ以降が
  無意味」な gating check で、前倒しコストがゼロかつ配線落ちなら 14 日のロスを防げる
- 結果 PASS → そのまま (ii) 書込み量 / (iii) 帰属まで完遂。**3 点すべて判定、roll 不要**
- 帰属 = **(B) 下流 100%**。対象 3 席は distinct bar 機会 **6/6 が `spread_wide`**
- 算術一致 (Σblocks == C1 行数) を**コード照合で裏取り** (予約 5491 行 → spread_wide 6244 行)
- 🔴 **配線バグではなく設計衝突**と判定: 3 席は `vol_q=Q5` × 1%-tile ショックでのみ発火し、
  `spread_wide` は静的 per-pair 定数 (1.5–3.0p) なので **entry 条件と block 条件が正相関**

### Phase 3: magnitude 計装 (同一コミット、live 挙動不変)
- readout 自身の結論が「閾値超過量が永続面に無く disposition を選べない」だったため、
  `gate_block_daily` に `metric_n/sum/min/max` + `parse_reason_metric()` を追加
- **読み手 (`per_cell_metrics`) を同一コミットで併設**、**counterfactual の落下を実測確認**、
  **「NG を返す既知の入力」を pin**
- 後続 registry `ps-seat-spread-magnitude-readout` (2026-10-19) を新設
- ⚠️ **pre-commit が初版を止めた (有用な失敗)** — estimand 宣言の reader 配線検査が
  「magnitude の読み手が `persisted` 経由の dict 透過だけで、コード上に名前で現れない」を
  ERROR にした。**宣言を緩めず** app.py に `per_cell_metrics` を明示露出して解消。
  併せて magnitude の**単位が reason ごとに違う** (pip / 秒 / UTC 時) ことに気づき pin を追加
- ⚠️ **pre-commit は 2 回止めた** — 2 回目は `/api/demo/block-counts` の**厳密 payload 契約**
  テスト (`test_api_demo_block_counts_returns_mode_and_strategy_counts`) が
  top-level キー追加を検出。契約を意図的に拡張したので**契約テストを同一コミットで更新**
  (露出が `persisted` の忠実な鏡であることまで pin = 単なる存在確認に緩めない)。
  🔵 **教訓: 「full suite green」は実装を変える前の測定値では意味がない** —
  app.py を触った後に再測すべきだった (最初の green 宣言は yml 変更前の状態)

## コミット一覧（本セッション）
1. `readout(ps-seat)`: C1 coverage 帰属完了 = 下流 100%、3 席は spread_wide で 6/6 全滅
   + gate block magnitude 計装 (rule:R3) — branch `research/ps-seat-c1-readout-2026-09-21`

⚠️ **auto-saver との競合を 1 件記録** — 本セッション中に
`auto: KB session-end save (2026-09-21)` フックが**staged の作業を勝手にコミットした**
(c226391a、11 files、汎用メッセージ + **pre-commit をバイパス**)。未 push だったので
正式メッセージで amend 済み (現 HEAD)。c226391a は amend で消滅したので上の一覧には残さない。
🔴 **これが危険なのはメッセージではなく pre-commit バイパス** — 同じ pre-commit が
本 PR で実欠陥を 2 件捕まえていたので、フックのコミットをそのまま残していたら
契約テスト破壊が未検証で main に載っていた。**セッション中に stage を置いたままにすると
auto-saver が未検証でコミットする**ので、置き場は commit 直前に限る

## 変更統計
- `modules/block_event_logger.py` / `modules/demo_trader.py` (計装、live 挙動不変)
- `tests/test_gate_block_metric.py` (新規 10 tests)
- KB: analyses §11 / registry (1 resolve + 1 新設) / changelog / estimand 宣言 / queue→done

## 検証
- `python3 -m pytest tests/ -q` → **3,416 passed / 17 skipped / 1 xfailed**
- `python3 scripts/check.py` → **全10チェック通過** (prereg registry lint OK)
- counterfactual: metric passthrough を外すと
  `test_spread_wide_magnitude_survives_entry_block_path` が落ちることを実行確認

## 触っていないもの (peek 会計)
- **EV / WR / PnL は一切計算していない** — `ps-carveout-regate-post-172` の凍結 look は未消費
- gate / tier / lot / 閾値は**一切変更していない** (3 席の disposition は Rule 1)
- `t5-jpy-cap-restore-price` の lot 1.0x 復帰は未執行 (user 決裁事項)

## 未解決事項
- [ ] `rnb-shadow-lane-health-checkpoint-1` (期日 **2026-09-24**): closed shadow N=2/3。
      期日到達で N<3 なら lane-health 調査 (conf gate / QUALIFIED_TYPES / shadow_only 配線 /
      signal 頻度) を R3 で実施。count のみで outcome は見ない (P-10 非抵触)
      - 🔵 **本セッションで read-only の先行観測を取った (判定はしていない)**:
        `rnb_usdjpy` の persisted block は `no_signal` **29,457** が支配的で、signal が
        出た時は `session_hours` 211 / `order_bar_dedup` 106 / `mtf_strong_bias` 9 /
        `1h_rr_low` 3 / `velocity_down` 3 / `same_price_3pip` 1。
        **`order_bar_dedup` が出ている = lane は order 層まで到達しており死んでいない**
      - 🔴 **ただし `evaluated_candidates` の `rnb_support_bounce` 行は 0 = C1 計装が
        rnb 経路を覆っていない** — 09-11 に HourlyEngine を塞いだのと**同型の観測面欠損の
        3 例目**。上流/下流の割り付けは ps 席と同じく C1 が無いと原理的にできないので、
        09-24 の調査は**まず rnb 経路への C1 計装 (R3)** から入るのが筋。
        ⚠️ `no_signal` 29,457 は「条件が satisfied でない **tick** 数」で「機会ゼロ」ではない
        (bar ではなく tick 単位) — ps 席で 14.1 倍の膨張を見た直後なので単位に注意
- [ ] `ps-seat-spread-magnitude-readout` (期日 **2026-10-19**): magnitude 分布を読んで
      3 席の disposition を起案 (Rule 1 なので起案まで)
