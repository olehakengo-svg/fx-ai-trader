# Session Log: 2026-08-07

## セッションで行ったこと（時系列）

### Phase 1: 現在地確認
- `/fx-roadmap-status` + `git log` + `prereg_trigger_watch.py` を実行
- **pre-reg トリガ**: 自走執行可能な発火はゼロ。`t5-jpy-cap-restore-price` のみ TRIGGERED (D1 close 158.419 < 159.50) だが**user 判断事項**。監視中 = t8-sweep N=8/10 (未達) / vix-sell N=14/20 / ws3-stage2 N=5/100 / E7 verdict 08-28 / E1 first look 10-15
- **wave 系の探索線**: #21 explore FAIL クローズ済 / #22・E1・E7・E12 は LOCK 走行中 (first look 前は計算禁止) → **新規に着手できる探索スロットは無い**
- → 実行対象を **08-06 daily が残した未解決の構造課題**から選択

### Phase 2: `SL_HIT` ラベル衝突の調査・修正 (rule:R3)
選定理由: 08-05 に提起され **2 日間未着手**、v2.3 T3 (payoff 非対称診断) の**ブロッカー**と記録されていた項目。

1. **機構特定** — `_check_sltp_realtime` の `close_reason` 判定が「**現在の** SL」との比較のみ。BE-lock/トレーリング/Profit Extender が SL を利益側へ動かすため、利確 exit も `SL_HIT` になる
2. **本番実測 (N=3308)** — SL の entry 相対位置で分割: 利益側 1894 本 → 97.6% 正 PnL / リスク側 1414 本 → 99.6% 負 PnL。**誤分類 1.5%** で判別子として確定。`outcome=WIN` は **54.2%** (08-05 の 46.2% は N=106 の過小評価)
3. **実害の特定** — `_sl_hit_history` の消費者 2 本 (cascade cooldown = 同ペア全戦略 45–600s ブロック / Fast-SL 適応防御 = 次 SL を ATR×0.3 拡大) が**勝ちで発火**。誤発火率 54.2%、Fast-SL 側 57.1%
4. **Rule 3 認定** — 直前の隣接ブロックが同一目的 (`SL 後の再エントリー防止`) で既に `outcome != "WIN"` を持っており、**同一意図の 2 ブロックが非対称**。統計的新規主張ではないので 365日BT 不要
5. **修正 + 回帰 pin 4 tests** (AST レベル、修正前ソースで落ちる**負のコントロール検証済**)
6. KB: `analyses/sl-hit-label-collision-2026-08-07.md` 新規 + changelog + log.md

**判断の記録 (動機 = データ駆動)**: 感情/直感ではなく、08-06 daily が明示した未解決項目のうち「live 挙動に影響 ∧ 自走可能 ∧ M1 (月次符号転換) 寄与」で選定。エントリー機会の回復方向の修正であり、tier/lot/live 送信可否は不変更。

**意図的に見送った選択肢**:
- `close_reason` の改名 (`TRAIL_EXIT` 等) — 既存 3308 行と全 BT/分析ハーネスの estimand を非可換に破壊する
- `_sl_hit_history` への shadow 行混入の是非 — 誤発火 1792 件中 1786 が shadow だが、cascade 防御は価格レベルの microstructure 事象を見る設計なので混入は一概に誤りでない。scope 外として継続課題に記録
- `usdjpy_carry_dip_accumulator` の `_1H_PRESERVE_SLTP` 登録 — **live の SL 幾何を 18.8p → 150p へ 8 倍にする変更**であり、08-05 の判断 (user 決裁事項) を維持

## コミット一覧（自動記録）
1. fix(live): SL_HIT ラベル衝突 — 勝ち決済が SL 狩り防御を発火させていた (rule:R3) (730b9dfa, 8 files)

## 未解決事項
- [ ] **cascade_cd / Fast-SL の block 実数が audit に出ない** — 本修正の効果 (誤発火 −54%) を実測できない。block カウンタ輸出が必要。08-06 daily の「シグナル供給 5 session 連続半減 (−70%)」の候補要因でもあり、**次の最優先作業候補**
- [ ] `shadow_demote_registry.py:40` の demote 根拠「SL_HIT 56.2%」は今回判明した衝突値そのもの → 再検討 (保守側ゆえ緊急性なし、R2)
- [ ] `_sl_hit_history` への shadow 行混入の是非
- [ ] `usdjpy_carry_dip_accumulator` の `_1H_PRESERVE_SLTP` 未登録 (SL 契約破棄、user 決裁待ち・3日経過)
- [ ] `price_shock_rev_aud_jpy_h1_long` の Rule 2 決裁 (集中度 90.3%、user 決裁材料として整理済み・3日経過)
- [ ] 破損 wikilink 131件 (うち 73件は機械修正可) の一括修正決裁
- [ ] risk dash `dd_status`/`jpy_ledger` が win を登録しない件
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
