# Session Log: 2026-08-09

## セッションで行ったこと（時系列）

### Phase 1: autopilot 現在地確認
- `prereg_trigger_watch` 実行 → TRIGGERED 1 件 (`t5-jpy-cap-restore-price`、user 判断事項で継続)、watching 多数
- watching の中で **2 件の供給ライン異常**を検出:
  - `t9-kalman-d7-fire-info`: 実測 **0.00/週** vs BT 期待 3.9/週 (2026-07-06 以降ずっと 0)
  - `t8-hull-shadow-freq`: 実測 2.27/週 vs 期待 13.3/週 (5.9× 過少)
- ロードマップのボトルネックは「クリーン N 蓄積」→ 供給ライン故障の調査を最優先項目に選択

### Phase 2: kalman_d7 0-fire の分母調査 (T9 追補の判定表に従う)
- 本番 trades API: 直近 50 日で kalman_d7 = **0 行** (期待 ~28)
- Render app ログ `[kalman_d7] QUALBAR` を取得 → **全 12 行が `hour=12` / `session_pass=False`**
- 実バー時刻は 03:00〜21:15 UTC に散っているのに報告 hour が一定 = **定数フォールバックの直接証拠**

### Phase 3: 根本原因の特定 (R3 構造バグ)
- `app.py:2552` `_dt_hour_utc = bar_time.hour if bar_time ... else 12`
- `bar_time` を渡すのは **BT 経路のみ**。live (`demo_trader._tick` → `compute_fn(df, tf, sr, symbol)`) は渡さない
- → **live の DT 全戦略が「常に UTC 12:00・常に金曜でない」で時間帯ゲートを評価**していた
- 潜伏 123 日 (`9c849cef` 2026-04-08 で再混入。2026-04-04 に同型バグを一度修正済み = **回帰**)

### Phase 4: 影響の実証 (自然実験)
- 対照群: `turtle_soup` / `london_session_breakout` の redesign_v2 は `ctx.hour_utc` を信用せず
  `df.index` から自前導出 (= 回避策) → 同一エンジン・同一 tick で走る対照になる
- BT 窓外発火率: `ctx.hour_utc` 直読み群 **83/237 = 35.0%** vs 回避策群 **0/28 = 0.0%**
- **Fisher exact one-sided p = 1.32e-05**
- 発火ゼロ群 (窓が h=12 を含まない): kalman_d7×3 / pd_eurjpy_h20 / tokyo_range_breakout /
  london_ny_swing / tokyo_nakane — **エッジが無いのではなく評価されていなかった**

### Phase 5: 修正 + 監視配線バグの併修
- `app.py`: DT ctx の時刻導出を `bar_time → df.index[-1] → now(UTC)` に統一 (BT 契約は不変)
- **副次発見**: 退避条件を registry に載せようとしたところ、`prereg_trigger_watch` の
  `live_count_decision` が `match: prefix` を `fetch_live_count` へ渡していなかった。
  kalman は 1 セル = 3 entry_type のため live 件数が**恒久的に 0 = 監視が沈黙**する状態
  (T5 の 18 日執行ギャップと同型) → `count_live_matching(prefix=)` を shadow 側と同契約に
- registry に `t9-kalman-d7-live-n10-ev-check` 新設 (2026-05-28 決裁の退避条件を機械監視化)
- 回帰 pin: `tests/test_dt_ctx_hour_utc_live.py` (9 tests、**修正前ソースで 7 件落ちることを検証**)
  + `test_prereg_trigger_watch.py` に prefix/配線テスト 2 件
- 全 suite **2561 passed** / `check.py` 全9チェック通過 / watcher 本番実行で新エントリの評価を確認

## 判断記録

| 項目 | 判定 | 根拠 |
|---|---|---|
| Rule 分類 | **R3** | 同一関数内の内部矛盾 (`is_trade_prohibited` は 4 行上で正しく now() に降りている)。統計的新規主張ではない |
| kalman_d7 の live 化 | **触らない** | 2026-05-28 に user が option B で明示決裁済み。バグ修正は「承認された設計を初めて動かす」ものであり新規昇格 (R1) ではない。Claude 側の勝手な demote は user 決裁の上書きになる |
| kalman_d7 の安全弁 | **監視を新設** | 決裁時の退避条件 (Live N≥10 で EV<0 → 0.1× 降格) を registry に載せる (T5 教訓: pre-reg には監視主体を必ず併設) |
| tier/lot 変更 | **なし** | 本 PR は時刻導出の修正のみ |

## 未解決事項
- [ ] **`t8-hull-shadow-freq` の 2.27/週 vs 期待 13.3/週 (5.9× 過少)** — 本バグとは**別原因**
      (hull は `ctx.hour_utc` を使わない)。次サイクルの起点となる所見を 2 つ記録:
      1. **hull の per-bar dedup は live で死んでいる既知パターン** — `hull_donchian_fade.py:69`
         の `self._last_emit_bar_ts` は `__init__` 内の**インスタンス属性**。engine は毎 tick
         再構築されるため live では毎回空辞書 (MEMORY `project_engine_reconstruction_live_dedup_dead`)。
         kalman は同じ罠を避けて `_qualbar_logged` を**クラス属性**にしている (該当教訓をコメントで
         明示) — hull だけ対策されていない。ただしこれは**過剰発火**方向なので観測されている
         過少発火の説明にはならない (下流 `recent_emit` が潰していると推測)
      2. → 従って律速は**上流** = qualifying bar 自体が BT 期待より少ない可能性が高い。
         `closed_idx = -1 if backtest_mode else -2` (live は確定足評価) の非対称、または
         期待値 13.3/週 の出典が陳腐化しているかを疑う
      **手順**: 本セッションと同じ「分子 → 分母 → 分母の各入力値が動いているか」を適用する
      ([[lesson-frozen-telemetry-value-2026-08-09]] の波及チェックリスト)。
      hull には QUALBAR 相当の計装が無いため、まず分母の計装追加から
- [ ] **修正デプロイ後の clean live 再計測** — 群 A の 35.0% 窓外発火と金曜ブロック不作動が
      負エッジ (−242.6p / payoff 0.274) にどれだけ寄与していたかの分離。N 蓄積待ち
- [ ] `shadow_demote_registry` の「SL_HIT 56.2%」再検討 (2026-08-07 起票、R2 で別途)
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 — 別セッション (task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 — 別セッション (task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (BB_RSI×2 + hull/sweep×2、純粋 cosmetic)

## コミット一覧（自動記録）
1. data(bt): phase1b daily re-run 2026-08-09 (183d window, verdict=NULL) (9faa9912, 2 files)
2. auto: KB session-end save (2026-08-09) (6b0f5ea4, 6 files)
3. fix(live): DT ctx hour_utc/is_friday の live 凍結を修正 (rule:R3)
4. fix(live): DT ctx.hour_utc/is_friday の live 凍結を修正 [rule:R3] (63b0307c, 10 files)
5. docs(KB): sentinel 値の設計教訓を追記 — もっともらしい既定値は回避策を無効化する (c8bb7d36, 1 files)
