# Session Log: 2026-08-28

## セッションで行ったこと（時系列）

### Phase 1: 現在地確認

- `git log` / `check.py` / registry 走査。main と origin/main は同期済み (0/0)。
- `check.py` 全9チェック通過。真の queue 滞留は 1 件 (`20260818-e23-cb-text-adjudication-s2`, 10 日) だが
  E23 は **TDW ライセンスが user 決裁待ち**でブロック中 = Claude 側では前進不能。
- active な registry トリガ 26 件を走査 → **本日期限は 1 件**:
  `mof-monthly-total-2026-08-29-check` (deadline 2026-08-28)。

### Phase 2: MoF 月次介入額の一次確認 (期限当日, rule:R3)

一次ソース (財務省 外国為替平衡操作の実施状況・月次) を直接読んだ。結果は
[[mof-monthly-total-check-2026-08-28]] に集約。要点:

| 窓 | ページ | 平衡操作額 |
|---|---|---|
| 2026-06-29〜07-29 | `20260731.html` | **0円** |
| 2026-07-30〜08-27 (**対象窓**) | `20260831.html` | **未公表 (HTTP 404)** |

- **user の「7月の負けは介入をくらった」説は 07-29 までについて反証**。
  総額 0 は「窓内の全日に介入なし」= 総額 >0 の「窓内のどこかに」より**遥かに強い情報**
  (日次帰属は Q3 開示 ~11 月まで不明、という非対称性がゼロの側では効かない)。
- **ワースト 2 日 07-30 / 07-31 は未決着** — 次窓に落ちる。
- **registry 初稿の「公表 ~08-29」は誤り**と判明: 過去 12 窓のページ名が
  全て**月末営業日** (20260731 / 20260630 / 20260529 …)。→ resolve せず
  **deadline 2026-08-31 (月) へ再武装**。テストの日付 pin も理由付きで更新。
- T5 復帰第2要件には**否定側の材料**。`t5-jpy-cap-restore-price` は第1要件のみ点灯、
  **lot 0.5x 維持で変更なし**。cross-LOCK 遵守 (公式開示の読み取りのみ、価格推定なし)。

### Phase 3: エンジン生存の真の検知を実装 (未解決事項の最上位, rule:R3)

08-27 に「cron は状態を持てないので設計が要る」として見送った項目。
**解法 = 差分をサーバ側で取る** (`write_probe.last_ok_at` と同じ分業)。設計判断の
詳細は [[changelog]] 2026-08-28 の項に集約。要点のみ:

- `DemoTrader._record_tick(mode)` = tick カウンタと実時刻を**不可分**に更新する唯一の経路。
  increment を各経路にコピーする設計は **call-site 欠落を 4 回踏んだ型** (PR #168 / #204 等)
  なので、最初から集約し **「外に生の increment が生えたら落ちる」構造 pin** を置いた。
- `_engine_tick_payload()` → `/api/demo/status` に `engine_tick_*`。
  watcher に `check_engine_tick_stall` (閾値 **15 分**)。
- **estimand が素直**: tick 前進はゲートにもシグナル有無にも**市場の開閉にも**依存しない
  → `candidate_stagnation` と違い **発火 = エンジン異常と読んでよい唯一の系列**。
  逆に**週末を市場オープン時間へ換算してはいけない** (本物の週末停止を毎回見逃す)。
- 閾値の実測根拠 (本番 24 モードを 20 秒 x 8 標本 = 142 秒窓、前進 n=89):
  **24/24 モードが漏れなく前進**、モード別 median 40.4s / p90 60.9s / max 81.2s。
  デプロイ ramp 3.6 分 (PR #199 実測) の約 4 倍として 15 分。
  ⚠️ 標本化 20 秒の aliasing で測定値は真値より**上振れ** = 閾値側に安全。

**counterfactual 10 本**で全テストが実際に落ちることを実行確認。
**うち ⑥ (main() 配線削除) は初回に素通りした** — 検知器を実装して通知文言まで
書いても `main()` から呼ばれなければ全テスト green のまま無音になる状態が実在した。
C1 が 4 ヶ月 write-only だったのと**同型の失敗が検知器側にもある**ことの実証で、
配線 pin を追加してから再確認した。

本番 (旧コード) に対する dry-run で `engine_tick_missing` 1 件のみ・Discord 通知なし
= graceful degradation を実測確認。

## コミット一覧（自動記録）
1. data(bt): phase1b daily re-run 2026-08-28 (202d window, verdict=NULL) (f5a64c09, 2 files)
2. fix(obs): エンジン生存を tick 前進の実時刻で検知 + MoF 月次介入額の一次確認 (rule:R3) (9 files)

## 未解決事項 (引き継ぎ)

- [x] ~~**engine 生存の真の検知**~~ ✅ 本セッション (`engine_tick_stall`、閾値 15 分)
- [x] ~~MoF 月次介入額の公表チェック~~ ✅ 一次確認済 → 対象窓は未公表につき **08-31 へ再武装**
- [ ] **`ENGINE_TICK_STALL_MINUTES = 15` の実運用確認** — デプロイ再起動で誤発火しないかを
      1〜2 週観察。誤発火が出たら**上げる** (下げるのは実測を取り直してから)
- [ ] **個別モード wedge の検知** — 意図的な非目標。`engine_tick_stalest_*` は payload に
      露出済みなので、必要になった時点でモード別 interval (10-60s) を較正して足す
- [ ] **ダッシュボード UI 側での鮮度表示** — API は出るようになったが、画面に出すのは別作業
      (`engine_tick_*` も同様)
- [ ] **`candidate_stagnation` 閾値 6h の再較正** — バースト実効 N=16 の粗い推定
- [ ] **hull 残余 4.7x の localize** — 蓄積再開待ち
- [ ] queue 滞留 (真): `20260818-e23-cb-text-adjudication-s2` (10日) — **TDW ライセンスが
      user 決裁待ちでブロック中**、Claude 側では前進不能
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 (別セッション task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 (別セッション task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立
- [ ] **E23 (台帳 #25)** TDW ライセンスは user 決裁待ち
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は **user のみ実行可**
- [ ] **fx-data ディスク 1GB → 5GB 拡張** (PR #206 の残タスク) — 課金増 (+$1.00/月) で **user 決裁事項**
