# Session Log: 2026-05-12

## セッションで行ったこと（時系列）

### Phase 1: （Claudeが記入）
- （コンテキスト圧縮前に自動生成 — 詳細はClaude が追記）

### Phase 2: cfd-trader MT5 経路実装（OANDA CFD 発注ルート構築）

**契機**: ユーザー指示「MT5であればOANDA接続できるのでそこ経由から発注まで進めたい、すでにOANDAのCFD口座で開設してある」

**前提確認** (`scripts/cfd_check_oanda_rest_cfd_support.py`):
- OANDA Japan 店頭 CFD アカウント `900190542` は MT4/MT5 専用プラットフォーム
- v20 REST API は SPX500 等の CFD instrument を露出しない (400 を返す)
- **結論**: 発注経路は **MT5 経由のみ**。Windows VPS + MT5 EA shim + cfd-trader HMAC client で構成

**実装** (#11–#17):

| # | 内容 | 主要ファイル |
|---|---|---|
| 11 | 監査スキーマ拡張: `broker_trade_id TEXT` 列 + 3-bucket (Shadow/Live/Unrouted) 静的SQL | `cfd_trader/audit/{oanda_audit,queries}.py` |
| 12 | `BrokerClient` Protocol + `BrokerOrderResult` `__post_init__` で `filled ⇔ broker_trade_id!=''+fill_price` を不変条件として強制 | `cfd_trader/broker/{protocol,null}.py` |
| 13 | `MT5RemoteBroker` (HMAC-SHA256 署名, 防御的デモート: shim が `filled` でも `broker_trade_id` 欠落なら `rejected` 降格), `factory.build_broker_from_env` (fail-closed=NullBroker) | `cfd_trader/broker/{mt5_remote,factory}.py` + `SHIM_SPEC.md` |
| 14 | `shadow.runner.execute_one_day` に `broker` + `live_gate` 追加。SHADOW は常時、`live_gate=True` の bar のみ追加で LIVE/UNROUTED 行を書く | `cfd_trader/shadow/runner.py` |
| 15 | UI 4カード分割 (Shadow/Live/**Unrouted**/**MT5 broker**), broker_status_from_env を System panel に表示 | `cfd_trader/web/{app.py,templates/overview.html}` |
| 16 | mt5_shim Flask service (HMAC verify + idempotency LRU + `MetaTrader5` lazy import で macOS テスト可能) | `services/mt5_shim/{app,auth,mt5_adapter}.py` |
| 17 | round-trip 統合テスト (Flask test_client を `requests.Session` 風にブリッジし wire-contract を pin) | `services/mt5_shim/tests/test_roundtrip.py` |

**構造的保証**:
- **LIVE 判定式**: `is_shadow=0 AND broker_trade_id IS NOT NULL AND broker_trade_id != ''` (memory feedback_live_vs_shadow_strict_separation 遵守)
- **LIVE bucket 汚染を型で阻止**: `BrokerOrderResult.__post_init__` が `status=='filled' AND not broker_trade_id` を `ValueError` で拒否
- **静的SQL discipline**: runtime composition なし (semgrep rule 準拠)
- **Fail-closed broker**: env (CFD_MT5_SHIM_URL/SECRET) 欠落 → NullBroker → UNROUTED 行のみ

**検証**:
- `cfd_tests/` + `services/mt5_shim/tests/` = 156 passed + 2 skipped
- `tests/` = 1446 passed + 1 xfailed (regression なし)
- `scripts/check.py` = ✅

**ユーザー側に残る作業** (コードは完備):
1. 漏洩 OANDA トークン `85100bdd...84ae` のローテーション
2. Windows VPS プロビジョン (Vultr / Contabo)
3. VPS 上で `services/mt5_shim/` 起動、cfd-trader 側に `CFD_MT5_SHIM_URL` / `CFD_MT5_SHIM_SECRET` を設定

3つ揃った時点で runner が自動的に `MT5RemoteBroker` を拾い、`live_gate=True` の瞬間から LIVE bucket が埋まる。

## コミット一覧（本セッション）
1. data(bt): phase1b daily re-run 2026-05-12 (94d window, verdict=NULL) (97c698c, 3 files)
2. auto: KB session-end save (2026-05-12) (afb7bff, 1 files)
3. auto: KB session-end save (2026-05-12) (f0904e2, 2 files)
4. auto: KB session-end save (2026-05-12) (2e4286c, 2 files)
5. auto: KB session-end save (2026-05-12) (46bdf42, 2 files)
6. auto: KB session-end save (2026-05-12) (a54d893, 2 files)
7. auto: KB session-end save (2026-05-12) (461b4de, 2 files)
8. auto: KB session-end save (2026-05-12) (2b8a8b7, 2 files)
9. auto: KB session-end save (2026-05-12) (b65635f, 2 files)
（本日のコミットなし）

## 変更統計
 6 files changed, 2993 insertions(+)

## 未解決事項
- [ ] （前回セッションから引き継ぎなし）

