# 2026-09-18 セッションログ — family A statement_ladder 凍結 (roadmap autopilot)

**モード**: scheduled task `fx-roadmap-autopilot` (自走)
**選定項目**: family A statement_ladder (台帳 #27) の敵対的検証 + 凍結コミット
**rule**: R1 手続き (explore pre-reg 凍結)。live / tier / lot 変更ゼロ、価格データ不使用。

## 1. なぜこの項目を選んだか

roadmap v2.3 のボトルネックは 2 本 — (a) 正の摩擦調整 EV セルの不在、(b) LIVE スループット不足。
(a) の唯一の解消経路は**新しい供給ライン**であり、registry 棚卸しの結果、2026-09-18 時点で
期日内かつブロッカー無しで着手できる能動ラインは **family A 1 本のみ**だった:

- `ps-seat-supply-hourly-c1-coverage` — **日付ゲート 2026-09-25** (計装 deploy +14d)。09-16 に手順 1
  (計装到達確認) は前倒し実施済み = PASS。14 日窓の蓄積待ちで今日は前進不能
- E23 (#25) — 09-15 UNDERPOWERED park、救済禁止
- E1 / ECG / E12 — 時限ロック (first look 10-15 / 11-06 / 2027-02-05)
- family A (#27) — **期日 2026-09-24、材料は全て in-repo、ブロッカー無し**

scan 第5次 §2.3 の会計 (「唯一の着手可能ライン」) と一致。

## 2. やったこと

1. **corpus 連続性の凍結前チェック** (scan 第5次 §1.2 の要求) → PASS。57 月ファイル欠落ゼロ /
   512 レコード parse 失敗ゼロ / `lexicon_scores.csv` 512 行 1:1 / 右端 2026-09-15 =
   09-17 に回収された negative sample が着地済み
2. **敵対的検証 1 本** → [[family-a-adversarial-verification-2026-09-18]]。blocking 1 件 (A-1)、
   要修正 2 件 (A-2, A-6)、確定 3 件 (A-3, A-4, A-5)、確認 1 件 (A-7)
3. **凍結検出器の実装 + 回帰 pin** — `tools/family_a_ladder_detector.py` /
   `tests/test_family_a_ladder_detector.py` (16 tests)
4. **凍結コミット** — pre-reg §10 に凍結内容を追記し status を DRAFT → 🔒 LOCKED
5. registry / 台帳 #27 / changelog を同一コミットで更新

## 3. 一番大きい発見 (A-1)

**「梯子の一番上の段」が、実は梯子ではなく事後報告だった。**

pin 版 lexicon の level 5 は `レートチェック` (act に先行する talk) と
`介入を実施 / 行った / 行い / いたし / 行う` `平衡操作を実施` (act の事後ナレーション) を
同じ段に置いていた。corpus 全期間 512 会見を実査すると、**L5 判定 9 件はすべて後者**で、
`レートチェック` 由来は 0 件。

これを L≥4 の primary 検出器に入れると、検出器は「介入が起きた直後の会見で介入の話をしている」
ことを読んで介入を"予測"する。**look-ahead が検出器の定義の内部に入る**。family A の estimand は
hit/FA 較正そのものなので、これは推定量を壊す blocking 欠陥だった。

面白いのは、これが scan 第5次 §1.2 の欠陥 (negative sample 欠落 → precision 水増し) と
**同じ向きの誤り**で、場所だけがデータ側から検出器定義側に移っていた点。
「FP 率較正が本 family の価値」という一文を両方の場面で使っている。

修正は **lexicon を触らずに検出器側の水準写像で行った** — pin (`569dbe3f`) は比較可能性の
凍結なので、そこを動かすと凍結の意味が消える。

## 4. A-2 (自己批判的に拾った 2 件目)

DRAFT の rearm 規約「直近 R 営業日に L≥4 が無いこと」を素直に**水準系列**へ適用すると、
carry-forward T=5 の分だけ実効不応期が R+T=25 営業日 (≈35 暦日) に膨らむ。
§3 の episode 個別化規約は gap ≥ 30 暦日 (≈21 営業日) なので、**規約上は別エピソードである
2 件を検出器が 1 件に潰す**。event 単位で測り直すと最小間隔 R+1=21bd で規約に整合し、
同時に H=20 の hit 窓が重ならなくなる (trial の独立性近似が成立)。

テストで先に落ちたのはこの挙動で、**テストの修正ではなく設計の修正**として処理した。

## 4.5 A-8 — レビューが 3 件目を拾った (Codex P2、凍結前に修正)

PR #265 の connector レビューが「土日開催の会見が検出器から落ちる」と指摘。
独立に corpus を全数走査したところ **13/512 件が非営業日開催で、うち 2026-05-04 (みどりの日) は
L4 `断固`** — 指摘された L3/L0 の例より重く、**初版の検出器では event がひとつも出ない**実害だった。
非営業日会見が集中するのは G7/IMF 総会週末と GW = 為替が最も緊張する局面で、miss が最悪の場所に入る。

修正 = 翌営業日へ roll forward (衝突は max)。前方に倒す論拠は「非営業日の発言が作用しうる最初の日は
翌営業日」であること (後方 roll は look-ahead)。**凍結後に検出器を変えると pre-reg の破棄に等しいので、
マージ前に処理した。**

⚠️ **`tools/pr_review_gate.py` はこの P2 を「指摘なし」と報告した** — inline review comment を
見ていない。ゲート出力を額面で受け取らず生のレビュー本文 (`gh api .../pulls/265/comments`) を
読んだので捕捉できた。MEMORY `project_review_gate_vacuous_2026_09_11` の**再発 2 例目**で、
ゲート自体の修理は別 PR。

## 5. 次にやること

- **pass-1 (label-free)**: 凍結検出器でイベント列挙 → Gate A (armed 率 ≤ 0.25) /
  Gate B (event ≥ 5 ∧ 暦年 ≥ 2) 判定 → コミット。**label 非接触なので explore の look を消費しない**
- Gate 通過時のみ **pass-2 (測定)**: day-level Youden J + episode-block circular-shift
- registry `family-a-explore-verdict-deadline` = **2026-09-28**
- ⚠️ 事前の見立て: L4 `断固` は 2022:3 / 2023-25:0 / 2026:13 と年次が偏っており、
  rearm 後の event 数と暦年分散次第で **Gate B (UNDERPOWERED) の公算は小さくない**。
  E23 と同じく「park でも窓は焼かない」構造にしてあるのが本凍結の要点

## 5.6 派生タスク 2: マージゲートの findings 軸が恒真だった (PR #267、rule:R3)

§4.5 で「ゲートが P2 を見落とした」と書いた件を追跡したら、**見落としではなく構造的失明**だった。
connector は P1/P2 を inline review comment で投げるが、ゲートは
`gh pr view --json reviews,comments` しか読まない — `reviews` に入るのは review の本文で、
thread 本文は含まれない。

直近 10 PR を数えると **inline に 29 件、top-level review 本文に 0 件**。
つまり「P1/P2 指摘なし — マージ可」は導入 (09-10) 以来ずっと**恒真**だった。
09-11 に 2 度直したのは**到着軸**で、findings 軸には触れていなかった。

同じ PR で旧/新を突き合わせると #264/#257/#253 が「マージ可」→「未消化 4/7/6 件」に変わり、
**#265 は「対応 commit で消化済み — マージ可」** = 消化検出も効いている
(何でもブロックする実装ではない)。

さらに: **同等の実装は PR #227 に書かれ、18 巡レビューされ、CLOSED されていた。**
main に残ったのは簡易版で、既存テストの冒頭に「#227 版テストは盲目移植していない」と
明記されている。正しい設計が一度書かれて捨てられていた、という形のプロセス欠陥。

## 6. 触っていないもの

live / tier / lot / OANDA / 本番 DB / 価格データ / 介入ラベルとのジョイント計算。
`data/external/mof_statements/` は読み取りのみ。

## コミット一覧（自動記録）
1. research(family-a): 🔒 explore pre-reg 凍結 — ladder L5 は事後ナレーションだった (rule:R1) (946f3813, 8 files)
2. fix(process): マージゲートの findings 軸が導入以来ずっと恒真だった (rule:R3) (3c4a2e10, 6 files)

