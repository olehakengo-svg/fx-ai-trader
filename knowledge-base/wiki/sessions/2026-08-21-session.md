# Session Log: 2026-08-21

## セッションで行ったこと（時系列）

### Phase 0: main 同期復旧 (autopilot 起動時)
- ローカル main が origin に対し **ahead 1 / behind 4** で乖離 (座礁事故の 3 例目相当、
  ただし今回は早期検知)。stash → rebase → push で解消、`fcd4acc7` を origin へ。
- MEMORY `project_main_checkout_stranded_phase1b_rescue_2026_08_12` の
  「commit した ≠ 永続化、origin 到達までが保存」を再確認。

### Phase 1: pre-reg トリガ健全性確認
- `tools/prereg_trigger_watch.py` 初回実行で **11 トリガが "trade API unavailable"**。
- 本番 `/api/demo/status` を直接プローブ → **502**。→ Phase 2 へ。
- 原因判明後に再実行 → **全トリガ正常取得**。TRIGGERED は
  `t5-jpy-cap-restore-price` のみ (D1 close=158.925 < 159.50)。ただし復帰は AND 条件で、
  第2要件 (介入観測) は **MoF 公式一次情報のみ**で認定 = MoF #4 cross-LOCK により
  価格シグネチャ認定は禁止 → **執行せず据置**。
- `tools/ps1a_execution_check.py`: P-S1(a) は **unique N=9/10 で WAITING** (最終発火
  2026-08-12、8.1 日前)。トリガ未成立につき runbook 執行なし。
- 結論: **本日執行可能な pre-reg 項目はゼロ**。

### Phase 2: 502 の追跡 → 構造欠陥の発見 (本日の主成果)
- Render 確認: web service は `not_suspended`、deploy `dep-da3qk8bbc2fs738udf50` は
  `live`。app ログでも MainLoop 稼働中 → **障害ではなく、Phase 0 の自分の push が
  誘発した再デプロイの swap 窓**だった (再プローブ 200)。
- ここで「1 commit で本番取引エンジンが止まる」ことに着目し定量化 →
  **[[deploy-churn-trading-gap-2026-08-21]]**。

### Phase 3: 是正 (rule:R3、PR #199)
- `render.yaml` web service に `buildFilter.ignoredPaths` を追加。
  ランタイム KB 参照を `app.py`+`modules/` 全数 grep で確定し、**read パスは除外**。
- `tests/test_render_build_filter.py` 3 件 (縮小検知 / read 巻き込み検知 / drift guard)。
  故意の違反注入で赤くなることを確認 = 非 vacuous。

## 主要な数値

| 指標 | 値 |
|---|---|
| web service デプロイ (14d 実測) | 18.8/日 |
| うち `knowledge-base/` のみの commit | 78% |
| 1 デプロイの完全無 tick 窓 | ≈59.5 秒 (instance ログ実測) |
| 全 24 モード ramp 完了 | +2m39s |
| 最悪ケース (連続 KB commit) | 新 instance 寿命 85 秒・warm-up 未完了で kill |
| 是正後デプロイ | **18.8/日 → 7.9/日 (−58%)** |

## 判断ログ
- **rule:R3 適用** — 構造欠陥 (取引プロセスの不要再起動) であり、シグナル
  ロジック・QUALIFIED_TYPES・lot・フィルタを一切変更しない。365日BT 不要。
- **保守的設計** — ランタイムが読む KB パスは 1 つも ignore しない。
  `analyst-memory.md` (残存デプロイ最大要因 41/14d) はシグナル経路外と確認したが、
  ランタイム read である以上は据置。ignore 化は user 決裁事項として残す。
- **主張の限定** — 逸失 pip は主張しない (断続窓と発火の同時性は未測定)。
- **自己修正** — 初回の削減率 −65% は `git show --name-only` が merge commit で空を
  返す性質を見落とし 35 merge を母数から落としていた。first-parent 差分で再計算し
  **−58% (18.8→7.9/日)** が正。KB/changelog/commit すべて訂正済み。


## コミット一覧（自動記録）
1. fix(deploy): KB ドキュメント commit による取引エンジン再起動を抑制 (rule:R3) (0d8b2c14, 5 files)
   ※ 76a83d76 は同一 commit の amend 前 (削減率の merge commit 訂正)

## 未解決事項
- [ ] `analyst-memory.md` を ignoredPaths に入れるか (user 決裁、−17% 相当)
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 (別セッション task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 (別セッション task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立
- [ ] **MoF 月次介入額の公表チェック (~08-29)** — deadline 08-28、着地当日に user 報告
- [ ] **E23 (台帳 #25)** TDW ライセンスは user 決裁待ち
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は user のみ実行可
- [ ] 09-18 月次スキャン第4次 — family A/B/C 統合裁定 (C は FAIL 確定済み)
