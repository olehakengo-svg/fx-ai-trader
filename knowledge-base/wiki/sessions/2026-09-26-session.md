# Session Log: 2026-09-26

## セッションで行ったこと（時系列）

### Phase 1: autopilot 現在地確認 (fx-roadmap-autopilot 06:20Z 起動)
- origin/main は 09-26 の cloud auto commit (R2 alert 03:33Z / intervention watch) のみ。ローカル `main` は ahead 1 (auto-save、本 log の骨格) / behind 2 — 本 PR に本 log を載せ、マージ後に reset で 0/0 へ
- 本番: engine tick 0.8s / `running` / `main_loop_alive` / `watchdog_alive` / `emergency_killed=False`、`engine_process_role=forked` / `engine_start_origin=statusheal` (二重エンジン現況どおり)。直近 12h の live fill 0 は金曜 21:00Z 以降の週末クローズなので正常。**最終 LIVE fill = #893201 (09-25 06:03Z)**
- R2 alert (`shadow-promote-r2-alert-2026-09-26-0333`) は is_shadow=1 セルの WARN 16 / CRITICAL 8 — 原則 3 (Shadow は削らない) により処置対象外、記録のみ
- registry 期日 1 週以内: `weekend-gap-execution-amendment-g0prime` (event #2 = 09-27 21:00Z、判定は月曜)、`ps-carveout-regate-post-172` (09-30、clean live N since 08-11 = **0** → 期日に stale レビュー)、`oanda-gold-monthly-status-record-2026-10` (user 専権、10-01 依頼)、`dual-engine-emit-proc-attribution-readout` (10-01、7 日窓未充足)。`e21-personal-account-decision` / `live-fill-drought-2026-08-26-disposition` は message で消化済みだが `resolved` キー無し・`active:false` — cron 判定外なので今回は触らない
- 09-25 の `usdjpy_carry_dip_accumulator` LIVE 2 敗 (#893195 −¥161 / #893201 −¥171) は wiki-daily [[2026-09-25]] 発見 1 に記録済み、戦略カード / index / registry は未反映 → Phase 2 で反映

### Phase 2: `[SLTP_CONSTRUCT]` / `[BROKER_TP]` 計装 (rule:R3 record-only、branch `feat/sltp-construct-marker-r3-2026-09-26`)
- **選定理由**: 09-25 session の未解決事項「follow-up 計装 (R3、別 PR): kalman fill 行の `reasons` に SL 選択枝・C0c 発動・MTF TP bonus を marker で永続化」= 10-08 user 決裁 packet の前提。加えて 09-25 の carry_dip 2 敗が **同じ共有 SL/TP 経路 (C0a/C0c/C0b)** の出力で、カードの「機構未特定 (order construction 上流)」が code reading で閉じる: mode `daytrade_1h` は `_get_base_mode` で接尾辞が剥がれず ATR 倍率 `.get("daytrade_1h", 0.8)`=0.8 / MIN 3p / MAX 50p、SR ベース (nearest_support − 0.3×ATR、RR≥1.0) が優先 → 観測帯 9.8–28.5p と整合。ただし fill 毎の分岐は 16/16 とも復元不能 = 計装が要る
- **実装** (`modules/demo_trader.py` +109): 定数 `_SLTP_CONSTRUCT_REASON_TAG` / `_BROKER_TP_REASON_TAG`、`_new_sltp_trace` / `_format_sltp_construct_marker` / `_format_broker_tp_marker` / `parse_sltp_construct_marker` (読み手用)。`_tick_entry` の SL/TP 構築 12 分岐 (preserve / sr / atr_rrlow / atr_nosr / clamp min・max / lowliq / fastsl / ct / rn ×2 / range_tp / mtf_tp) で `_sltp_trace` を更新、row insert 直前 (`[EMIT_PROC]` の前) に marker を `_reasons_with_inv` へ。promoted 送信経路では `_tp_oanda` 決定直後に `[BROKER_TP] basis=<qh|exempt|range_mr|wg_none|passthrough> mult tp_p` を `append_trade_reason` (送信可否に無関係、失敗はログのみ)。**値・gate・is_shadow・lot・送信判定に触れない**
- **読み手同梱** (write-only 回避): `tools/sltp_construct_readout.py` — 本番 `/api/demo/trades` を entry_type × (live/shadow) で集計 (SL 選択枝比率 / clamp / C0c 発動率 / MTF ×1.3 率 / 宣言 vs 実発注 距離中央値 / broker basis)。**outcome・EV・WR は読まない**。保存済み 2,000 行 (デプロイ前) で smoke: `rows_with_marker=0` を正しく報告
- **pin**: `tests/test_sltp_construct_marker_r3.py` 15 本 — 各分岐は「通る入力 → 1 / 通らない入力 → 0」の両側 (lowliq 21Z vs 12Z、fast-SL 同ペア 200s 前 vs 別ペア、rn .000 vs .300、SR 採用 17.4p vs SR+clamp 20p)。ハーネス構築で判明した経路制約: EUR_USD promoted は 19Z で `session_pair` block → USD_JPY 21Z で駆動 / fast-SL は `cascade_cd` (daytrade 90s) の外・5 分窓の内 (200s 前) に seed / ATR 0.0005 は浮動小数で MIN 0.0005 を下回り `clamp=min` (marker は正直に報告、基準 ATR を 0.0008 へ)
- **counterfactual**: rn trace 代入と `basis="qh"` 代入を外す → 3 本落ち (rn / broker TP 送信 / broker TP 拒否)。復元後 `git diff --stat` 109 行で一致。`scripts/check.py` 10/10 通過 (wikilink 492 は既存)
- **KB 同コミット**: wiki changelog / CHANGELOG.md / carry_dip カード「09-25/26 更新」節 (fill #15・#16、N=16、R2 不成立、機構、計装) / kalman カード follow-up 行 / index.md System State bullet / registry `kalman-d7-live-exit-spec-mismatch-disposition` + `carry-dip-v3-revival-watch` message 追記 / Codex queue task 追記 (計装済み、差し替えは marker 付き live N 蓄積後)

### Phase 3: PR #300 レビュー消化
- Codex P2 4110716471 (1 巡目): 宣言距離 `decl_sl_p` / `decl_tp_p` の基準が current_price (実約定 bid/ask) だった → rebase / slippage で `sig["entry"]` とずれると宣言側が偽の拡大/縮小に見える (60p 宣言を +20p 後に執行 → decl 40 / tp 60)。**実欠陥** (readout の主目的「宣言 vs 実発注」を汚す)。`_new_sltp_trace(decl_entry, …)` で signal entry を trace に持ち、宣言側は sig.entry 基準・実発注側は current_price 基準に分離、`entry_drift_p` を新設。pin 2 本 (formatter / bid/ask +3p 実経路)、17 passed。readout の中央値列にも drift を追加

- Codex P2 4110734587 (2 巡目): `[BROKER_TP] tp_p` の基準が `_signal_price` で `[SLTP_CONSTRUCT] tp_p` (current_price) と不整合 → 同一基準 (current_price) に統一、pin 1 本 (bid/ask +3p → 57 vs 48)。1 巡目と同族 (基準の混在) — 1 巡目で直したとき broker 側を対称に見ていなかった ([[feedback_check_the_symmetric_side_2026_09_19]] の再発、軽微)

- Codex P2 4110787960 (3 巡目、readout): 既定表が ct / range_tp / entry_drift_p を省いていた (JSON には有り) → 列追加 + broker_basis 列。**ツール側の自己検査**: synthetic 行 1 本を通して全列が出ることを smoke で確認 (下記)

- Codex P2 4110824129 / 4110824136 (4 巡目、readout): (i) `--since` をローカルで filter していたため LIMIT/OFFSET の後ろで古い行が黙って落ちる → `date_from` をサーバへ渡し offset 500 でページング、MAX_PAGES 超過は `truncated=True` を表示 / (ii) lane を live/shadow の 2 値にしていた → **3 分割** (live = id 非空 / shadow = is_shadow=1 ∧ id 空 / flag_drift = is_shadow=0 ∧ id 空) — [[feedback_live_vs_shadow_strict_separation]] を自分のツールで破っていた。smoke: 偽ページ 2 枚 (500+120) で 620 行・truncated=False、flag_drift 行が shadow に混ざらないこと

- Codex P2 4110860717 (5 巡目、readout): `status=all` は open 行を毎ページ先頭に全件 prepend し offset は closed にしか効かない → ページ数だけ open が重複。open は `status=open` で 1 回、ページングは `status=closed`、summarize 側でも trade_id dedup (`dedup_dropped` を表示)。smoke: open 3 + closed 500+120 → 623 行 / 重複 0、重複を故意に混ぜた入力で dedup_dropped=2

## コミット一覧（本セッション）
1. feat(engine): entry 時 SL/TP 構築の分岐を fill 行に永続 — [SLTP_CONSTRUCT] / [BROKER_TP] marker + 読み手 (rule:R3 record-only) (10787e30, 11 files)
2. fix(review): #300 P2 4110716471 — 宣言 SL/TP 距離の基準を signal entry に分離 + entry_drift_p (rule:R3) (28b5b4b3, 5 files)
3. fix(review): #300 P2 4110734587 — [BROKER_TP] tp_p の基準を current_price に統一 (rule:R3) (642dd76d, 4 files)
4. fix(review): #300 P2 4110787960 — readout 既定表に ct / range_tp / entry_drift_p / broker_basis 列を追加 (rule:R3) (b7874101, 2 files)
5. fix(review): #300 P2 4110824129 / 4110824136 — readout の窓取得をサーバ date_from + offset ページングに、lane を live/shadow/flag_drift の 3 分割に (rule:R3) (7a503aed, 2 files)
6. fix(review): #300 P2 4110860717 — readout の open 行を 1 回だけ取得し closed をページング + trade_id dedup (rule:R3) (58ce3cd9, 2 files)
(マージ後に sha を追記)

## 未解決事項
- [ ] ローカル `main` の 0/0 復帰 (本 PR マージ後に reset、旧 tip は refs/archive/)
- [x] **follow-up 計装 (R3、別 PR)**: kalman fill 行の `reasons` に SL 選択枝 (SR / ATR / clamp) ・C0c 発動 (低流動性 / fast-SL / round-number) ・MTF TP bonus を marker で永続化 → 本 PR `[SLTP_CONSTRUCT]` / `[BROKER_TP]`。次: デプロイ後の最初の fill で marker 付与を実測 (`tools/sltp_construct_readout.py --since 2026-09-26`)
- [ ] `kalman-d7-live-exit-spec-mismatch-disposition` (10-08): Codex 制約付き BT (診断のみ) の結果をレビューし **user 決裁 packet** を組む (肢 a: exit を宣言仕様へ = Rule 1 / b: 維持 / c: shadow 降格)。BT の EV は keep/demote の根拠にしない。R2 は通常の live 損失停止規律 (realized N ベース) にのみ留保
- [ ] `ps-carveout-regate-post-172` (09-30): clean live N since 08-11 = 0 (09-26 実測) → 期日に「席が是正されても発火しない = 供給側の別問題」の stale レビューを resolution に書く
- [ ] `weekend-gap-execution-amendment-g0prime`: event #2 = 09-27 21:00Z、月曜 daily report と同時に qualify / NO-QUALIFY を strategies/weekend-gap-fade.md へ転記
- [ ] `oanda-gold-monthly-status-record-2026-10` (09-30): 10-01 JST に user へ status 画面 (10 月ランク) の確認依頼 (user 専権)
- [ ] `e21-personal-account-decision` / `live-fill-drought-2026-08-26-disposition`: message 上は消化済みだが `resolved` フィールド未設定 — 次の registry 触る PR で resolved 化 (user 既読は不要な事務項目か確認)
- [ ] PR #272 の未 push commit + conflict 解消 (PR 自体は 09-22 MERGED — worktree `hunt-dataset-readout` の残骸確認)
