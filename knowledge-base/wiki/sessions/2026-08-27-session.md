# 2026-08-27 セッションログ (fx-roadmap-autopilot)

## 起点

前セッション (08-26) の PR #206 が **open のまま未マージ**、かつローカル checkout が
マージ済みブランチ上で main から 13 commit 遅れ (座礁ハザードの 3 例目)。

## Phase 1 — PR #206 の完遂 + 本番実測

- CI 3/3 green (test / hip1-holdout-guard / semgrep) を確認して `gh pr merge 206 --merge --admin`。
- ローカル未 push の KB auto-save commit (hunt_events / session log = 超ホット file) は
  **PR に混ぜず**分離。main を ff で同期。
- **本番実測で PR #205/#206 の live 検証を完了**:
  - `write_probe: {"ok": true}` = PR #206 の書込み直接検知が**本番で稼働**
  - disk `used_pct 40.08%` / backups **1 本** (keep_last=2 が効いている)
  - `demo_trades 16,548` / `evaluated_candidates 315,173`
- ⚠️ 途中 `/api/admin/disk_status` が **502** を返したが、これは**自分のマージが起動した
  デプロイの再起動窓** (deploy `dep-da7qgi6k1f9s73d9cbgg` 03:00:24→03:02:46Z) であり
  障害ではなかった。デプロイ直後の 502 を事故と誤診しないこと。

## Phase 2 — 未解決事項の最上位を実装 (rule:R3)

**`last_trade_row_age` / `last_candidate_row_age` を `/api/demo/status` に出す** を実装。
設計判断と実測根拠は [[changelog]] 2026-08-27 の項に集約。要点のみ:

- 判定列は `created_at` (DB DEFAULT で必ず埋まる)。`bar_time` は PR #204 の全 NULL 前例により不採用。
- status を **5 値** (`ok`/`no_rows`/`no_table`/`unparseable`/`error`) に分離 — 「行が無い」と
  「クエリが落ちた」の混同を構造的に禁止。
- `error`/`now` は `row_freshness_*` へ名前空間化 — 汎用 `error` キーとの衝突で
  「鮮度クエリ失敗」が「status 全体の失敗」に化けるのを防ぐ。
- **field を足すだけで終えない**: 読み手として `candidate_stagnation` 検知器を同一コミットで併設。
  C1 テーブルが 4 ヶ月 write-only だった同型を繰り返さないため。
- 検知器の閾値 6h は**バースト間ギャップ**の実測 max 120.3 分 (水 22-00 UTC の薄商い) の 3 倍。
  ⚠️ **初稿の統計を自己訂正**: 素の行間隔 (median 0.10 分) は*バースト内*密度であって停止判定の
  ケイデンスではない。候補行は 1 バーで複数戦略ぶんが一斉に書かれる (2,000 行 = 16 バースト)。
  正しい指標は**バースト間** median 3.2 分 / p90 59.7 分 / max 120.3 分。閾値の値 6h は不変だが
  **実効 N=16** の粗い推定 = 暫定。1〜2 週後に取り直す。誤発火時は**上げる**方向のみ。
- **自己検証の経緯**: デプロイ後に候補行が 31 分増えないのを見て「定数固着はコードを疑え」を
  自分の計測に適用 → バースト構造に気付いた。当時の 70 分ギャップは p90 と max の間 =
  **異常ではなかった**。初稿の median 0.10 分を信じていれば異常と誤断していた。

**counterfactual 4 本**で全テストが実際に落ちることを実行確認 (green のまま無力化していない証拠)。

## Phase 3 — 偽陽性の除去 (付随)

`check.py` の queue SLA 検査が `status:` を読まずファイル名日付のみで判定していたため、
**完了済み**の family-a タスクを 8 日間「滞留」と警告し続けていた。

- done 残置を SLA 滞留と**別種の警告**に分離 (偽陽性の常時点灯は本物の停滞を埋もれさせる)。
- family-a タスクは成果物を検査のうえ Claude Review を付して `done/` へ移送
  (DRAFT 明記・全要求セクション実在・ジョイント統計ゼロ = 測定禁止遵守を確認)。
- 結果、SLA 警告は**真の滞留 1 件のみ** (`20260818-e23-cb-text-adjudication-s2`, in_progress 9 日) に。


## コミット一覧（自動記録）
1. fix(obs): 行鮮度を status に出し「凍結 vs 静かな相場」を分離 + 評価停止の新検知 (rule:R3) (150d6dfd, 11 files)
2. test(obs): 行鮮度の error 経路を実 sqlite 破損で pin (rule:R3) (cba4f5fb, 1 files)

## 未解決事項 (引き継ぎ)

- [x] ~~`last_trade_row_age` / `last_candidate_row_age` を `/api/demo/status` に出す~~ ✅ 本セッション
- [x] ~~queue 滞留 `20260819-family-a-statement-ladder-prereg-draft`~~ ✅ 完了済みの偽陽性と判明、done へ移送
- [ ] **ダッシュボード UI 側での鮮度表示** — API は出るようになったが、画面に出すのは別作業
- [ ] **`candidate_stagnation` 閾値 6h の再較正** — バースト実効 **N=16** の粗い推定。1〜2 週の
      実運用後に**バースト間**ギャップ分布を取り直す (誤発火が出たら**上げる**、下げるのは実測なしに行わない)
- [ ] **hull 残余 4.7x の localize** — 蓄積再開待ち (5 日分の N が失われた)
- [ ] queue 滞留 (真): `20260818-e23-cb-text-adjudication-s2` (9日、in_progress のまま)
- [ ] trendline_sweep MTF ゲート LIVE 転送 block 可否の engine 側調査 (別セッション task_566c4c4d)
- [ ] slippage_pips/spread_at_exit 列の trades API 輸出 (別セッション task_d932525c)
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立
- [ ] **MoF 月次介入額の公表チェック (~08-29)** — `mof-monthly-total-2026-08-29-check` (deadline 08-28)
- [ ] **E23 (台帳 #25)** TDW ライセンスは user 決裁待ち
- [ ] `e1-positioning-ingest-freshness`: MYFXBOOK 資格情報の Render env 投入は **user のみ実行可**
- [ ] **fx-data ディスク 1GB → 5GB 拡張** (PR #206 の残タスク) — keep_last=2 でも main の正味成長で
      2〜5 ヶ月後に warn 帯へ再接近。**課金増 (+$1.00/月) を伴うため user 決裁事項**
