# 2026-09-05 セッション — rnb_usdjpy: 測っていない量を名乗る block カウンタと 153 日 dead mode

**モード**: fx-roadmap-autopilot (自走) / **Rule**: R3 / **PR**: `fix/rnb-direction-filter-estimand-2026-09-05`

## 選定理由

`/fx-roadmap-status` + 直近 session log + 監視ログ (index.md 09-04 エントリ) を突き合わせ、
未解決項目のうち **「本ログで最も古い un-actioned 🔴」** = `rnb_usdjpy:direction_filter` の
**8 回連続 escalation (2026-08-26 → 09-04)、コード読み未実施** を選択。
R3 (構造バグ / 診断) なので autopilot 実行可、かつ 09-04 に roadmap へ起票されたばかりの
**M3 スループット (発火機会不足)** と同じ軸に載る。

## 結論

監視が 8 日追っていた仮説 (「`compute_rnb_signal` の WAIT-path バグ」) は**外れ**。
実体は独立した 2 つの構造事実で、どちらもコードを読めば確定 (統計不要)。

### (A) カウンタが測っていない量を名乗っていた → 修正済み

`direction_filter` gate は「方向が逆」と「シグナルが無い (WAIT)」を同名で計上していた。
`direction_filter` を持つ唯一のモード `rnb_usdjpy` の signal_fn は
**SELL への return path を構造上持たない** ⇒ このカウンタは**恒久的に 100% が WAIT**。

| 窓 (USD_JPY 15m, MASSIVE) | バー | BUY | SELL | WAIT |
|---|---:|---:|---:|---:|
| full 12.8y | 315,623 | 2,225 (0.705%) | **0** | 99.295% |
| 365d | 24,695 | 157 (0.636%) | **0** | 99.364% |
| 60d | 4,199 | 22 (0.524%) | **0** | 99.476% |

本番 09-04 は **535/535 = 1.0000**、全 block 集計の **15.6% (第 4 位 family)**。
`direction_filter` が block chain の**最初**にあるため WAIT が `conf<30` に到達せず、
「`conf<30` = ZERO」という**それ自体が異常に見える観測**を生んで仮説を補強していた。

**修正 = 同一分岐内のラベル分離のみ。挙動・発注・shadow 判定は不変。**

### (B) 153 日間「動いているが 1 行も出せない」モード → 修正しない (Rule 1)

`rnb_support_bounce` ∉ `QUALIFIED_TYPES` (104 型) ∪ `CONDITIONAL_TYPES` (空)。
`unknown_type` gate は shadow bypass を持たないので BUY が出ても **shadow 1 行すら出ない**。
導入コミット `db5e3e4c` (2026-04-05) は 4 箇所を配線して `QUALIFIED_TYPES` だけ忘れた。
⇒ **2026-04-05 → 2026-09-05 = 153 日**。

既存テスト `tests/test_preserve_types_tick_entry.py` は事実を正確に記録していたが
**意図された設計として pin** しており、異常として上申する読み手がいなかった
(= [[m1-kpi-readout-and-mechanical-flip-2026-09-04]] と同じ読み手不在型)。

## 実装

| 種別 | パス |
|---|---|
| 修正 | `modules/demo_trader.py` (block 理由ラベルの estimand 分離、挙動不変) |
| テスト | `tests/test_rnb_block_reason_estimand.py` (7 本) |
| 分析 | `wiki/analyses/rnb-dead-mode-and-block-estimand-2026-09-05.md` |
| 教訓 | `wiki/lessons/lesson-block-counter-unmeasured-estimand-2026-09-05.md` |
| registry | `rnb-support-bounce-registration-decision` (2026-10-06) |
| roadmap | KPI 表 M3 行に追補 |

恒久ガード = 全 `auto_start` モード (22) の signal_fn が返す literal `entry_type` が
`QUALIFIED ∪ CONDITIONAL ∪ BLOCKED` に入ることを AST で検査。違反は 1 件のみで、
**既知集合との完全一致 (==)** で assert ⇒ 新規ドリフトも既知ドリフトの解消も落ちる。

## counterfactual 検証 (8/8 が所望のテストだけ落ちる)

| # | 変異 | 落ちたテスト |
|---|---|---|
| 1 | ラベル巻き戻し (常時 `direction_filter`) | `test_wait_is_counted_as_no_signal…` ✅ |
| 2 | 常時 `no_signal` | `test_opposite_direction_is_still_counted…` ✅ |
| 3 | `compute_rnb_signal` に SELL literal 追加 | `test_compute_rnb_signal_cannot_emit_sell` ✅ |
| 4 | `rnb_support_bounce` を QUALIFIED へ登録 | `test_buy_passes…` + `test_auto_start_modes_have_no_unknown_registration_drift` ✅ |
| 5 | 新規未登録 entry_type を auto_start モードへ追加 | drift ガード ✅ |
| 6 | 2 つ目のモードに direction_filter 付与 | `test_direction_filter_is_only_used_by_rnb_usdjpy` ✅ |
| 7 | drift 走査が何も見ない | drift ガード ✅ |
| 8 | literal 抽出器を常時空集合化 | drift ガード ✅ |

**初回素通りゼロ。** ⚠️ 手続き上のミス 1 件 — 初回の counterfactual 実行で `restore()` に
`git checkout --` を使ったため**未コミットの修正本体ごと巻き戻り**、CF2-CF6 が
「修正なし」の木に対して走っていた (結果は無効)。ファイル snapshot 方式に変えて全 8 本を再実行。
**教訓: 未コミットの変更がある木で counterfactual を回すなら restore は git ではなくファイル退避で行う。**

## 全体検証

- 全 suite **2,960 passed / 17 skipped / 1 xfailed**、`scripts/check.py` **9/9 通過**

## デプロイ後に答え合わせする予測

1. `rnb_usdjpy:direction_filter` → **恒久 0** (非ゼロなら構造前提が壊れた合図 = 分析ページを更新)
2. `rnb_usdjpy:no_signal` ≈ tick 数 (~2,880/日)
3. `rnb_usdjpy:unknown_type:rnb_support_bounce` が**初めて可視化**され tick の ~0.5-0.7% に出る
   = RNB live セットアップ頻度の初の直接観測 (registry 期日 2026-10-06 の判定材料)

⚠️ **estimand 注意**: 上表の頻度は**確定足**評価。live は 30 秒ごとに**形成中バー**を評価するので
消える一時的 BUY を拾い、実測はより高く・ノイジーになりうる。

## M3 スループットへの含意 (user 決裁事項として提示、執行しない)

| セル | 発火頻度 |
|---|---:|
| `usdjpy_carry_dip_accumulator×USD_JPY×BUY` (現行最速) | 2.10 /週 |
| `price_shock_rev_eur_gbp_h1_long` | 0.93 /週 |
| `price_shock_rev_aud_jpy_h1_long` | 0.47 /週 |
| **`rnb_support_bounce×USD_JPY×BUY` (未登録、確定足推定)** | **3.0 /週** (365d) / 3.3 /週 (12.8y) |

登録すれば現行最速セルを上回る shadow 蓄積レーンになりうるが、**Rule 1 (user 決裁)**。
`QUALIFIED_TYPES` 追加は `_UNIVERSAL_SENTINEL` 経由の minlot live 経路にも触れるため
無条件 shadow ではなく、**R2 自動 demote gate 併設が前提**。
config コメントの `BUY EV=+7.7` は **BE/Trail ablation 前**の 2026-04-05 BT 由来で**引用不可**。

## 未解決事項 (繰越)

- [ ] 🆕 **`rnb_support_bounce` 登録の可否** — registry `rnb-support-bounce-registration-decision` (2026-10-06、live 頻度実測後に R1 手続きへ乗せるか判断)
- [ ] 🆕 **`conf<30` 等の他 block ラベルの estimand 分離** — 同型の折り畳み (「シグナル無し」と「低信頼」) だが**全モードの block family 構成が変わる**ため影響評価を伴う別件
- [ ] 🆕 **`git stash list` = 178 件** (09-04 監視ログ起票、本セッション未着手)。ローカル main の working tree も他ジョブ由来の未コミット変更を抱えたまま
- [ ] **M1 定義への統計資格条件付与** — user 決裁 (analyses §8)
- [ ] **M3 スループット ~14 ヶ月** — 本セッションで構造欠損 1 件を特定。残る対策 (資格ロースタ拡大 / 検証設計の N 要件見直し) は継続
- [ ] **carry_dip の shadow 落ち理由** — registry `carry-dip-live-to-shadow-drop-cause`
- [ ] **kalman / weekend_gap の LIVE 約定通算ゼロ** — registry `t9-kalman-d7-fire-info`
- [ ] **SVK 継続監視** — `behind_pace=false`
- [ ] **ps-seat-supply-remeasure-30d** — 期日 09-10、N<15 濃厚 = §8 補足検証へ
- [ ] **P-S1(a)**: unique N=9/10 — 次の LATE 窓発火 1 件でトリガ成立
- [ ] **B7/B8 の live 可能セル層** — 2026-11-30 に N≥30 確認
- [ ] queue 滞留 `20260818-e23-cb-text-adjudication-s2` (19 日) — TDW ライセンスが user 決裁待ち
- [ ] `e1-positioning-ingest-freshness` — MYFXBOOK 資格情報の Render env 投入は user のみ実行可
- [ ] **fx-data ディスク 1GB → 5GB 拡張** — 課金増で user 決裁事項
- [ ] Render dashboard の無参照キー物理削除 (純粋 cosmetic)
