# Session Log: 2026-09-16 (roadmap autopilot)

## 概要

P-S1(a) 執行トリガが 2026-09-14 に成立 (unique N=10) していたが、その verdict
`OPTION_B_EXECUTE` は **estimand 不一致の産物**と判定。自動 live 昇格を停止し、
user 決裁を 2 件 → 1 件 (二択) に整理した。**live 変更ゼロ**。

## 時系列

1. **現在地確認** — ローカル main が diverged (44 ahead / 178 behind) だったため
   `origin/main` から worktree `autopilot/0916-watch` を切って作業。
   ⚠️ ローカル main の registry には `roster-e2-silent-promoted-cells` を
   `artifact_presence` 型のまま (requirements 無し) にした未コミット改変があり、
   `prereg_trigger_watch.py` が全 51 entry で KeyError 落ちする状態だった。
   **origin/main では同 entry は `conditional_info` で健全** = ローカル artifact。
   本番監視への影響なし (Render cron は main を読む)。
2. **pre-reg trigger watch (origin/main)** — TRIGGERED 2 件:
   `t5-jpy-cap-restore-price` (第2要件未認定、user 決裁) と
   **`t8-sweep-defer-decision` (shadow N=10 ≥ 10 到達)**。
3. **P-S1(a) dry-run** → `OPTION_B_EXECUTE` (unique N=10 / gross spaced EV +2.92p)。
4. **estimand 監査 (rule:R3)** → 条件成立は無効と確定 (下記)。
5. 判定器修正 + pin 6 本追加 + KB 更新 → PR。

## 中核の発見

| 層 | 量 | 出所 |
|---|---|---|
| 凍結閾値 +6.22 p/t | **net**-of-spread (`− SPREAD_PIP`=1.5p) | `research_sweep_reversion_grid_12y.py` L156-158 |
| 判定器が読む shadow EV | **gross** (摩擦控除なし) | `demo_db.py:1223` |
| shadow の約定価格 | **mid** (`entry_price == signal_price`、実測 10/10 で差 0.00p) | 本番 API |

同一 10 本を閾値と同じ estimand に揃えると **spaced net EV = −3.33 p/t (符号反転)**。

- **convention 非依存**: 研究 parity −3.33 / entry 全幅 −5.14 / house RT −9.59。
  唯一正になるのは「spread=1.5p」仮定 (+1.43) のみ = 実測で **0/10 充足**
  (entry spread min/med/max = 5.4/6.6/16.6p)。
- **cap 救済集合は空**: cap≤5.0p で N=0、cap 6-10p では net 負 ⇒ forensic #2 §8-1 の
  「cap を breakeven 未満に締める」選択肢は消滅。
- **自動執行が armed だった**: scheduled task `ps1a-sweep-trigger-executor` は
  `OPTION_B_EXECUTE` で runbook §3 (PR → `--admin` merge → deploy) まで自走する委任。
  新 verdict `USER_REDECISION_ESTIMAND` により既定分岐 (live 不触) で停止。
- 参考 (判定には未使用): gross の勝ちは `close_reason="SL_HIT"` かつ pnl 正が 5/10 =
  BE/トレール利確のラベル衝突 ⇒ gross 自体も上振れ側。
- 独立な乖離: 研究 gross +7.72p に対し shadow 実測 gross +2.92p = **エッジ再現 38%**。
  不成立の原因は (i) 再現不足 (ii) 摩擦前提違反の 2 つが同方向に効いている。

## 成果物

- `tools/ps1a_execution_check.py` — gross/net 併記 + `USER_REDECISION_ESTIMAND` 新設
  (gross>0 ∧ net≤0、または spread 被覆 <1.0 で執行停止)
- `tests/test_ps1a_execution_check.py` — 12 → **16 pin**
- [[ps1a-trigger-estimand-audit-2026-09-16]] (新規)
- packet Status → 🔴 執行停止 / runbook §2 新分岐 / roadmap T6 行 / registry message

## やらなかったこと (と理由)

- **Option B 執行せず** — 負 EV 帯への live 送信になる。
- **Option C (retire) も自動執行せず** — どの量で読むかの変更自体が凍結文言の修正で、
  packet §6-2 は符号割れ時を user 再決裁と規定。live 露出ゼロ (全行 shadow) なので
  Rule 2 の緊急性も無い。
- **score_gate の shadow rescue 実装せず** — forensic #2 §7 の判断を維持
  (分母回復が負 EV 帯の行で spaced EV を動かす)。

## 未解決事項

- [ ] 🆕 **P-S1(a) の二択 user 決裁** — (a) Option C retire [Claude 推奨] /
      (b) 執行形態の再設計 (新規 pre-reg、Rule 1)。⚠️ unique N 単調増加のため
      registry の `10-28 に N<5 → retire R2` は**到達不能** = 放置しても自動終結しない
- [ ] `t5-jpy-cap-restore-price` TRIGGERED 継続 — 第2要件 (介入の方向/文脈) は
      月次開示では認定不能、lot 1.0x 復帰は R1 = user 決裁
- [ ] 🆕 **ローカル main の working tree 汚染** — diverged (44 ahead/178 behind) +
      registry の未コミット改変。研究作業は worktree 固定で回避中だが要掃除
- [ ] `edge-supply-scan-monthly` 期日 **2026-09-18** (2 日後) — 能動測定ライン 0 本の
      臨時スキャン。A/B/C 統合裁定の前提材料は揃済み
- [ ] 🆕 `rnb_support_bounce` 登録の可否 — registry `rnb-support-bounce-registration-decision` (10-06)
- [ ] **M1 定義への統計資格条件付与** — user 決裁
- [ ] **M3 スループット ~14 ヶ月** — 資格ロースタ拡大 / 検証設計の N 要件見直し
- [ ] 🆕 **E1 ingest は復旧を実測確認** (09-10 認証失敗停止から) — 2026-09-16T01:08Z 時点で
      `/api/positioning/status`: `myfxbook.logged_in=true` / `last_login_at 2026-09-16T00:06:47Z` /
      `auth_failure_streak=0` / `last_error=''` / verified 13 keys 鮮度 ~0.3h / source=myfxbook。
      ⚠️ ただし (a) カウンタは 00:06:47Z の restart でリセットされるため**復旧時刻は本
      endpoint 単独では確定不能** (b) poll_cycles=4 のみ = **持続性は次の ~24h の cycle で確認要**
      (c) **09-13T23:57Z の breach 消費は取り消されない** — first look 2026-10-15 の
      postpone 判断は復旧とは別件として残る
- [ ] queue 滞留 `20260818-e23-cb-text-adjudication-s2` — TDW ライセンスが user 決裁待ち
- [ ] **fx-data ディスク 1GB → 5GB 拡張** — 課金増で user 決裁事項
