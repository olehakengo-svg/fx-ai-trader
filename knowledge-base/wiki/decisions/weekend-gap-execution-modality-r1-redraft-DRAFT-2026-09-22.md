# 📝 DRAFT: weekend_gap 執行モダリティ R1 再審 — 骨子 (2026-09-22)

> **Status: 📝 DRAFT — LOCK ではない。R1 起案でもない。** 本文書は「packet §6 の事前コミット『改定後 qualifying イベント 2 連続で fill 不成立 → 執行モダリティ自体を再審 (R1 再起案)』が**発動した場合**に、その日から起案を始められるよう分岐と変更候補を事前固定した骨子」であり、凍結値 (§2/§3/§5 of [[weekend-gap-stage2-execution-prereg-2026-07-24]]、[[weekend-gap-execution-contract-r1-packet-2026-09-10]] §4) は**一切変更しない**。発動しなければ本文書は未発動として archive する。
> rule:R3 (文書のみ、code / registry / live 経路 不変更)。R1 起案への昇格と候補の選択は **user 承認事項**。
> 起点: [[path-to-win-reassessment-2026-09-22]] §3 Rank 6 (「次の qualifying 不成立 1 件で R1 再審発動 → 骨子 DRAFT を 09-27 前に用意」)。
> **本文書で計算していないもの**: shadow row の outcome (pnl / MFE / 埋め率)、G1 slippage 集計、G2 累積、OOS 窓 (2022-01-01〜2026-06-30) の再集計、explore 窓の outcome 条件付き統計。数字は全て価格系 (gap / drift / 時刻) と既存凍結値の引用のみ。
> **2026-09-23 改訂**: PR #281 review P2 2 件 (4070529776 / 4070529787) を消化 — §2 未到達行 / §3 2d 送信適格化 (fill ではない) / §6 転記項目 / §7 追加。同日 PR #289 review 1 巡目 P1 (4075088689) で亜種 (c) bridge-refused を追加。**2 巡目 P1 (4075480956) で方針転換: 未到達 (a)〜(d) は現行契約下では「不成立 (v)」として G0' の event を消費する** — 非消費・繰越案は §4 row 4 の AMENDMENT 提案 A-1 へ移管 (user 承認まで無効)。2 巡目 P2 (4075480961) 亜種 (d) 手動停止 (mode_off) 追加 / P2 (4075480969) order-bar dedup の帰属規則。**3 巡目** P2 (4075642842) (b) を上流 shadow bypass へ一般化 / P2 (4075642847) promotion cause は event 時点の永続記録のみ + 親セッション敵対的レビュー所見 6 件 (ABANDONED 後 pre-row block = 不成立 (i) / `drawdown` は dead code / SEND→guard→ABANDONED の帰属優先 / SENTINEL_BLOCK_DIAG を (a) 一次ソース / reason 併記 / 旧行番号を実測に統一)。行番号は main `7145a93c` 実測 (demo_trader.py / oanda_bridge.py / weekend_gap_fade.py)。凍結値・OOS 数値は不変更 (rule:R3)。

---

## 0. 3 行サマリ

1. **どこにいるか**: 執行契約 (B) 発効 (2026-09-10) 後の qualifying イベントは 1 件 (2026-09-13 USD_JPY、gap −50.0p) で、live は **`ABANDONED_DRIFT` (drift +41.0p > +8.0p)** = 改定後不成立 **1 件目**。09-20 は 3 ペアとも NO-QUALIFY (分母外)。live fill は live 化 (07-25) 以降 **0/4 qualifying イベント**、契約 B 下では 0/1。次の検証点 = **2026-09-27 (日) 21:00 UTC**。
2. **何が起きたら発動するか**: 次の qualifying **pair-event** (時系列順) が `ABANDONED_DRIFT` / `ABANDONED_HALT` / 新種 cancel / 送信失敗 (iv) / 未到達 (v) なら、直前の pair-event (= 09-13 ABANDONED_DRIFT) と合わせて **不成立 2 連続 = packet §6 発動** → 本 DRAFT を R1 起案 packet へ昇格 (user 承認)。fill なら F2 resolve (watcher は TRIGGERED を返すだけ — **registry 明示編集で resolve**、§2) + **G0' 完了** (packet §6 は「改定後の最初の 2 qualifying イベント」に限る — 以後の放棄は記録のみ、rolling gate は未承認)。同一週末に複数 pair-event が qualify し結果が混在した場合、**順序付けは永続 first-qualification ts (未実装、R3) が存在するまで運用に使わず、全時刻を転記して W5 で user 上程** (§2)。全て同一クラスなら順序不問で event #2 の結果は一意。NO-QUALIFY・cap skip なら繰越 (§2 で事前固定)。**pre-send guard 未到達 (`PRE_SEND_GUARD`、§2 亜種 (a)〜(d)) は現行契約下では不成立 (v) として消費** — 非消費化は AMENDMENT 提案 A-1 (§4 row 4、user 承認まで無効)。
3. **候補の要点 (§3)**: 09-13 型 (gap が halt 窓 ~4 分で ~47p 消費) を救える (= **送信判定まで到達させる**。fill は下流の cap / pre-send guard / cancel / 送信失敗が残るため forward 計測、§3 2d) 契約変更は **drift 境界 +8.0p の変更/撤廃**か**シグナル基準の executable 化**しかなく、いずれも estimand 変更 = **fresh forward OOS のみが confirmatory** (OOS 窓再接触は禁止)。打ち切り +15 分・送信タイミング・指値化は 09-13 型に効かない。契約不変更で今すぐできるのは **放棄イベントの価格系 forward 観測 (§3-6)** のみ。

---

## 1. 現状の事実 (出所付き)

| 項目 | 値 | 出所 |
|---|---|---|
| 契約 B 発効 | 2026-09-10 (user「進めて」、rule:R1)。送信 = OANDA tradeable 確認後の最初の評価 tick / 打ち切り 初バー ts +15 分 / drift 放棄 +8.0p / halt-race 再送 1 回 | packet §4、`strategies/daytrade/weekend_gap_fade.py:89–93` |
| event #1 (2026-09-13) | USD_JPY gap **−50.0p** (Fri close 153.620 → Sun open 153.120) ≥ 21.4p → BUY fade 発火 21:05:03Z、shadow row id 17602。tradeable 確認時 (quote_age 7.5s) の adverse drift **+41.0p > +8.0p** → `ABANDONED_DRIFT`。oanda_audit `weekend_gap_exec_abandon(ABANDONED_DRIFT,drift=+41.00p)` 21:05:05Z。EXEC_B 遷移 (Render ログ実読 09-22): `HOLD` 14 行 21:01:27–21:04:33Z (tradeable=False、halt 窓) → `ABANDONED_DRIFT` 21:05:02.5Z (tradeable=True、quote_age 7.493s、send_mid 153.53 / sunday_open 153.12) | [[daily-observations-2026-09]] O-2026-09-14-1 / [[2026-09-16]] L241 / Render ログ API 実読 2026-09-22 (service `srv-d6va1of5r7bs73en10vg`、2026-09-13T20:55–21:30Z、text=`WEEKEND_GAP` 25 行・hasMore=false) |
| event #1 の他 2 ペア | **EUR_USD gap −2.2p < 20.0p → no-qualify** (21:01:08Z) / **AUD_USD gap −14.7p < 25.0p → no-qualify** (21:01:29Z) — gap 診断ログで**確定** (分母外、pair-event 列に現れない)。⚠️ 09-22 初版は oanda_audit 09-13 行数 1 ([[2026-09-16]] L242) から「no-qualify (推定)」と書いていたが、**audit 不在が証明するのは「監査される執行経路に到達しなかった」ことだけ** — `_weekend_gap_tick` (demo_trader.py:4117–) は weekend_key None / latch 済 / `df is None or len(df) < 10` / `det is None` で**非監査 return** し、`_tick_entry` にも `_add_oanda_audit` 前の early block があるため、qualify して上流で失敗した pair-event を audit 行数では検出できない (PR #281 review P2、2 巡目)。以後は診断ログ (または shadow row) で確定するまで **UNKNOWN と書く** (§6) | Render ログ API 実読 2026-09-22 (service `srv-d6va1of5r7bs73en10vg`、2026-09-13T20:55–21:30Z、text=`WEEKEND_GAP` 25 行・hasMore=false) / code: `modules/demo_trader.py:4136–4172` |
| 2026-09-20 | USD_JPY **−19.0p** < 21.4p / AUD_USD **−20.5p** < 25.0p / EUR_USD **−1.9p** < 20.0p → 3 ペア NO-QUALIFY、row/latch なし | Render ログ `[WEEKEND_GAP]` gap 診断行 2026-09-20T21:01:13–21:01:29Z (2026-09-22 実読、[[2026-09-22-session]]) |
| 不成立カウント | 改定後 qualifying イベント **1/2 消費** (drift 放棄は不成立に含む — registry `weekend-gap-execution-amendment-g0prime` message「halt >15m / drift 放棄 / 新種 cancel」)。NO-QUALIFY は分母外 | packet §6 / registry g0prime |
| live fill 通算 | **0/4** qualifying イベント (07-26 インフラ障害 / 08-02・09-06 MARKET_HALTED / 09-13 ABANDONED_DRIFT)。G1/G2/G3 の live N = 0 | [[weekend-gap-fade]] イベントログ |
| 境界導出時の実測 | OANDA 初 M1 遅延 = **+4.0 分 (48/48、min=max)**。qualifying・cap 通過 N=8 の +5m adverse drift **mean +3.15p**、全 48 pair-weekend p90 **6.7p**、+8.0p 抵触 3/48 (qualifying 0/8) | `bt-results/wg_gap_drift-2026-09-10.json` aggregates / packet §5.2–5.3 |
| 09-13 の drift vs 導出値 | +41.0p は mean 比 ~13 倍、p90 比 ~6 倍 (O-2026-09-14-1 は「~5 倍 / ~6 倍」と記載 — 分母を mean 3.15 とすると 13 倍、p90 6.7 とすると 6.1 倍。本稿は両方併記) | 上記から単純除算 |
| 期日 | registry `weekend-gap-execution-amendment-g0prime` 期日 **2026-09-28** (繰越必要) / F2 `project-falsification-f2-wg-live-conversion` **2026-12-31** (live N=0 なら「PASS→live 変換未実証」認定) / 冬時間初週末 **2026-11-01** | registry |
| 頻度 | 2.07 qualifying 週末/月 (凍結 OOS report §6) → 週末あたり p≈0.48 → 3 週末連続 non-qualifying ≈ 14% ([[path-to-win-reassessment-2026-09-22]] §3 Rank 6 は 14〜17% 幅) | stage-2 prereg §1 |

---

## 2. 分岐の事前固定 — G0' event #2 (改定後 2 番目の qualifying pair-event = 2026-09-27 以降で最初に qualify する pair-event、cap skip 除く; 未到達 `PRE_SEND_GUARD` は不成立 (v) として消費)

> packet §6 の境界は「**改定後の最初の 2 qualifying イベント**」に限定された一回限りの配管確認で、rolling gate ではない (`weekend-gap-execution-contract-r1-packet-2026-09-10.md` §6 row 1–2 / registry g0prime)。09-13 USD_JPY `ABANDONED_DRIFT` = event #1 (不成立)。**event #2 が起きた時点で G0' は終了する** — fill なら完了・通常運用、不成立なら 2 連続 = 発動。event #3 以降は存在しない。

| 結果 (pair-event 単位) | 分類 | 即時アクション (Claude、record-only / R3) | 本 DRAFT の扱い |
|---|---|---|---|
| **fill** (demo row `oanda_trade_id` 非空 ∧ oanda_audit sent→filled、1 回目 `MARKET_HALTED` cancel → 再送 fill を含む) | 成立 = **G0' 完了** (fill 1/2、packet §6 row 1「配管確認完了、通常運用へ」) | **F2 は自動では resolve されない**: `tools/prereg_trigger_watch.py::evaluate_live_count_decision` (L80–99) は clean live N≥n_decide=1 で `TRIGGERED`「再評価を実施せよ」を**返すだけ**で、`active=false` / `resolved` / `resolution` を書かず registry を一切変更しない (watcher に registry 書込み経路なし、2026-09-22 code 読み)。**resolution は明示手順** — §6 の 24h 規則内に registry 編集 PR (別担当) で `active: false` + `resolved: <YYYY-MM-DD>` + `resolution: 「PASS→live 変換の初実証 (F2 U7)、改定後 fill 1/2、demo row id / oanda_trade_id / oanda_audit id」` を記録する (既存 inactive エントリの規約: `resolved` 24/27・`resolution` 25/27、例 `vix-sell-pilot-recheck`)。**編集までは F2 は active のまま TRIGGERED 表示が続く = 未 resolve として扱う** (watcher 出力を resolve 済みの証拠にしない)。G0' 手順 (1)–(5) (registry g0prime) を実施 — (4) の slippage 突合は**当該 1 event の persisted 値の読み取りのみ、rolling 集計はしない** (G1 は code gate、N≥6 まで人手で計算しない)。card へ 24h 以内転記 (§6) | **発動せず → 本 DRAFT は未発動として即 archive** (G0' は 2 event で終了、event #3 は存在しない)。以後の放棄は §3-6 の観測記録のみで、連続不成立 trigger は終了 — 継続 (rolling gate) は packet §6 の改定 = user 承認事項 (PR #281 review P2、4 巡目) |
| **不成立** = qualifying pair-event (cap skip 除く) が demo row `oanda_trade_id` **空**のまま終端したもの: (i) **`ABANDONED_DRIFT` / `ABANDONED_HALT`** (送信前放棄、packet §6「drift 放棄 / halt >15m」。**EXEC_B `decision=ABANDONED_HALT|ABANDONED_DRIFT` の後に pre-row `_block` で row・latch が作られなかった pair-event も (i) として消費する** — `_weekend_gap_tick` は HOLD 以外の decision を全て `_tick_entry` に流し (4254–4267)、`_is_shadow=True` (5171–5174) の後も `max_per_mode_pair` 5234–5257 (per-cell shadow 枠超過) / `hedge_block` 5272–5281 (shadow 免除なし) / `max_open` 5287–5299 (≥ max_open+8) や予約後の `_block` で row も latch も作らず return しうる。modality の verdict (放棄) は既に出ているので転記名 `不成立(i) ABANDONED_*/PRE_ROW_BLOCK(<reason>)`、識別 = EXEC_B `decision=ABANDONED_*` ∧ 当該週末の row なし ∧ latch なし ∧ `gate_block_daily` / DB logs `[SENTINEL_BLOCK_DIAG] weekend_gap_fade blocked at: <reason>` (1361–1366) の増分。予約前 blocker なら latch が無いので後続 tick で再評価され、窓内に row・latch が書かれればその終端で分類する (窓終了時点ルール、下行 (iv)); 予約後 blocker なら order-bar dedup (5532–5537) で窓内固定)、(ii) **SEND 後 FOK cancel 2 回とも `MARKET_HALTED`** (`modules/oanda_bridge.py:751–800` の halt-race 再送は 1 回目 `MARKET_HALTED` で始まるが 2 回目の応答を制限しない — trade id も filled audit 行も残らない = **halt 型の不成立**、packet §6「halt」系に読む)、(iii) **SEND 後の `MARKET_HALTED` 以外の reason の cancel** (新種 cancel)。(ii) を「新種 cancel でない」として外さない (PR #281 review P2、4 巡目)。(iv) **SEND 後、fill も cancel tx も無く終端 = 送信失敗**: bridge `OPEN … FAILED` (`ok=False` — retry 上限 (429/503/timeout/network) 後または非 retry エラー、`modules/oanda_bridge.py:893–909`) または `OPEN … ok but no tradeID` (`ok` 応答に fill/cancel tx が無い、`:880–892`) — いずれも `pending_op_mark_failed`、oanda_audit は `sent` のみで `filled` 無し (**`sent` 行が無い bridge 拒否は (iv) ではなく (v) の亜種 (c)**)、demo row `oanda_trade_id` 空。**処置**: 当該 pair-event は不成立として**消費**する (G0' は配管確認であり送信失敗は配管の失敗そのもの — 先例: card は 07-26 インフラ障害を qualifying 非 fill event として数えている)。**人手・自動の再送はしない** (契約 B §4.4 の再送は 1 回目 `MARKET_HALTED` cancel 限定)。event #2 なら 2 連続 = 発動、G0' 終了。⚠️ packet §6 の列挙 (halt / drift 放棄 / 新種 cancel) は送信失敗を名指ししていない — 見出し語「fill 不成立」で読む。昇格時に文言を確定 (§3 row 7、PR #281 review P2、6 巡目)。(v) **未到達 `PRE_SEND_GUARD(<reason>)`** (下行、亜種 (a)〜(d)): 送信判定 SEND 通過後に `_tick_entry` の pre-send guard / slot 由来 shadow 化 / bridge 拒否 / 手動停止で OANDA 送信試行 (7876) に至らず終端 — **現行契約下では不成立として消費**する (fill 経路の (i)〜(iv) と区別して記録、定義・識別は下行)。非消費化は AMENDMENT 提案 A-1 (§4 row 4) の user 承認後のみ | **event #2 が不成立 = 09-13 (event #1) と合わせて 2 連続 = packet §6 発動** | 発動時: (i) 事象を card へ 24h 以内転記 (価格系のみ)、(ii) 本 DRAFT §3 の表を一次データで確定し **R1 起案 packet v1 をイベント +7 日以内** (09-27 発動なら 10-04) に起票、(iii) user へ「R1 再審起案の承認」を 1 行で依頼。**発動中の live 経路は契約 B のまま継続** (放棄は fail-closed で shadow 分母を保存する — 止める理由がない、4原則#3)。packet §6 の「family live-execution 保留」を選ぶのも user 事項 | **R1 起案へ昇格** (user 承認) |
| **`SKIPPED_SPREAD`** (cap 10.0p 超) | 正当な未執行 | 分母記録のみ (`block_cause=weekend_gap_spread_cap(spread=X.XXp)`)。packet §6 row 1 の「cap skip を除く」により **G0' の 2 イベントに数えない** → 繰越 | 発動せず |
| **不成立 (v) 未到達 `PRE_SEND_GUARD(<reason>)`** = qualifying ログ (`[WEEKEND_GAP] {pair} qualifying event:`、demo_trader.py:4263–4266) が出た pair-event のうち、契約 B の送信判定 `[WEEKEND_GAP][EXEC_B] … decision=SEND` (4236–4238、marker `_wg_exec_send_ok` 4262) を**通過した後**、OANDA 送信試行 (`self._oanda.open_trade`、7876) に到達せず終端したもの。4 亜種: **(a) 早期 `_block(...)` return** — row も latch も生成されない (`_block` 5009–5010 → `_record_entry_block` 1319 = in-memory カウンタ + `gate_block_daily` 永続集計 + **DB logs `[SENTINEL_BLOCK_DIAG] weekend_gap_fade blocked at: <reason>` (1361–1366; wg ∈ `_UNIVERSAL_SENTINEL` 9878–/9927 ∧ `_SILENT_DROP_DIAG_TYPES` 10022–/10031 なので必ず出る — tick 時刻と完全 reason を持つ一次ソース、8000 行刈り込みのため 24h 内読取**)。wg は `_is_shadow_eligible_full` (5089–5096) が真なので、`recent_emit` / `spike` / `velocity_*` / `regime_guardrail` / `session_pair`・alpha_scan 系は `_block` ではなく shadow bypass 分岐に落ちる = (a) ではなく **(b)**。**order-bar 予約 (`_maybe_reserve_order_bar_emit` 呼び出し 5523–5531、実体 1369–、key = (entry_type, instrument, signal, bar_ts)) の前後で挙動が異なる**: **(a-前) 予約前 blocker** = `direction_filter`/`no_signal` 5025 / `score_gate` 5068 / `r2_shadow_demoted_cell` 5207 / `max_per_mode_pair` 5252 (per-cell shadow 枠も超過) / `hedge_block` 5280 / `max_open` 5299 (shadow 上限 max_open+8 も超過) / (`drawdown` 5302–5303 は **dead code** — `_check_drawdown` 3906–3950 は全 return 経路で False (3948「デモは常に継続」)。CB 到達時は `_oanda_kill` 3952–3965 が `_allowed_modes = set()` (3956) にするだけで sig は通過し、下流で **(c2) `bridge:mode_<mode>_not_allowed`** として現れる — DB log `🔴 OANDA CB発動` 3959 / Render `[CB] OANDA killed` 3961 を併記して識別) / `session_hours` 5321・`market_close_30min` 5331 (wg 3 モードは `active_hours_utc` を持たず非該当) / `exposure:*` 5351 (wg は 1000u で見積) / `regime_squeeze_mr` 5390 / `regime_*_dt_tf` 5450・5472 / `gbp_asia_flash_crash` 5496 / `conf<閾値` 5505 — 予約前に return するため latch も予約も残らず、**次 tick で再評価される**; **(a-後) 予約後 terminal blocker** (wg で `_block` に落ちるもの) = `same_price` 5590 (edge cell 非該当時) / `cooldown` 5851 / `consec_loss` 5861 / `circuit_breaker` 5873 / `cascade_cd` 5889 / `mtf_*` 6448・6456 / `sl_cluster` 6749 / `rr_floor` 6759 / `friction_guard_cd` 6796 / `spread_gate` 6902 / `spread_sl_gate` 6928 ほか 5559–6928 で shadow bypass を持たない `_block` (`recent_emit` 5559 / `spike` 6364 / `velocity_*` 6397・6406 / `regime_guardrail` 6183 / `session_pair`・alpha_scan 5940–6083 は wg では bypass 分岐 = (b)) — wg の `_closed_bar_ts` は entry 窓全体で `first_bar_ts` 固定 (`weekend_gap_fade.py:351`) のため、一度ここで block されると以後の tick は全て 5532–5537 で `[ORDER_BAR_DEDUP] blocked …` (DB log) を書いて return し、`gate_block_daily` には reason_key `order_bar_dedup` が積まれる (1406–1408) — 元の条件が解けても**再評価されない** (初回 block で窓内確定)。(既 latch pair の再発火は `_weekend_gap_tick` 4158–4159 の latch 判定で data fetch 前に遮断され `_tick_entry` に届かない — `weekend_gap_latched` 5146–5147 は backstop — ので新規事象ではなく、`weekend_gap_tradeable_unconfirmed` 5183–5184 は decision=SEND を経ない冗長経路の backstop で qualifying ログも出ない = いずれも本行の対象外)。**(b) row あり shadow 化 (slot 由来ほか上流 shadow bypass)** — wg は `_UNIVERSAL_SENTINEL` (9878–、9927) に属し `_is_shadow_eligible_full` (5089–5096) が真のため、slot 制約に加えて**上流 guard も `_block` ではなく shadow bypass 分岐**に落ちる: `[SHADOW] recent_emit bypass:` 5552–5558 / `[SHADOW] spike bypass:` 6357–6362 / `[SHADOW] velocity_up bypass:` 6390–6395 / `[SHADOW] velocity_down bypass:` 6399–6404 / `[SHADOW] Regime guardrail:` 6176–6181 / `[SHADOW] session_pair bypass:` 5933–5970 / alpha_scan 系 `[SHADOW] … block/gate: … → shadow` 5977–6083 (`_is_live_tier_exempt` 5114 でない場合) / `[SHADOW] Phase0 tier gate:` 7504–7510 (post-row、PAIR_PROMOTED 外のため wg 非該当) — いずれも row + latch `EXECUTED` + audit `skipped`/`shadow_tracking` + `[SENTINEL_BLOCK_DIAG] weekend_gap_fade candidate built but shadow-downgraded before OANDA promotion` (7369–7375) / `… OANDA skipped after candidate build: shadow_tracking (… shadow=True)` (7998–8003、wg 3 ペアは `_PAIR_PROMOTED` 9436–9438 で `_diag_live_intent` 7351–7358 が真) を残す (PR #289 review 3 巡目 P2 4075642842)。slot 由来は `max_per_mode_pair` (5244–5250、`[SHADOW] Slot bypass: weekend_gap_fade …` を `_add_log` = DB logs に残す — ただし DB logs は直近 8000 行のみ保持 (`demo_db.py:1931–` の自動刈り込み) のため 24h 内の読取が実質必須) または `max_open` (5296–5297、**専用ログ行なし**) で `_is_shadow=True` に落ち、shadow row は残るが OANDA 送信されない: row `oanda_trade_id` 空、oanda_audit `bridge_status=skipped` / `block_reason=shadow_tracking` (7957–7975 — `_wg_shadow_cause` が空なので汎用値)、latch state は **`EXECUTED`** (7025–7033 — latch は OANDA 送信前に書かれるので fill の証拠ではない)。**(c) bridge-refused** — `_tick_entry` は demo row (`_db.open_trade` 6984) と latch **`EXECUTED`** (7025–7033) を作った**後**に `self._oanda.open_trade` (7876) を呼ぶが、bridge が送信せず `False` を返す 3 経路: **(c1)** bridge daily-loss gate (`oanda_bridge.py:668–690`; `_check_daily_loss_gate` 550– = 当日 UTC の Live total_pnl ≤ −`_daily_loss_limit_pips` (env、既定 20.0p) で当日残りの全 transmit 停止、当日中 sticky・エラー時は fail-open) → bridge 自身が oanda_audit `bridge_status=blocked` / `block_reason=daily_loss_limit(<pnl>pip<=-<limit>pip)` を書き `🔗 OANDA: [HALT] … not transmitted` をログ; **(c2)** demo_trader 側 pre-check (7941–7955) で `_bridge_active` (7678) 偽 or mode 不許可 → oanda_audit `blocked` / `block_reason=bridge_inactive` または `mode_<mode>_not_allowed` — **row の is_shadow は書き換えられない** (`oanda_trade_id` 空の非 shadow row が残る); **(c3)** race — pre-check 通過後に bridge 内 `if not self.active` (649) / `is_mode_allowed` (658) が偽 → **audit も log も書かずに** `False`。(c1)(c3) は呼び出し側 7925–7940 で `_is_shadow=True` + `update_shadow_status(trade_id, True)` (`demo_db.py:1603` = `UPDATE demo_trades SET is_shadow=1`) + DB log `[SHADOW_FIX] Bridge refused transmission: weekend_gap_fade <pair> trade=<id>`。3 経路とも **`sent` audit 行は書かれず** (7911–7918 は `_send_accepted` 7902 真のみ)、OANDA に order は届かない。latch は送信前に `EXECUTED` で永続化済みなので `_weekend_gap_tick` 4158–4159 の latch 判定が再発火を遮断 (5146–5147 は backstop) = bridge 拒否の tick で終端確定 (PR #289 review 1 巡目 P1 4075088689)。**(d) 手動停止 (mode_off) ほか promotion gate 由来の非送信** — strategy control が `off` (`OandaBridge.get_strategy_mode` 281–289、`_strategy_overrides` 113 / `set_strategy_mode` 307) だと `_is_promoted_ex` (10416–) が `(False, "mode_off")` を返す (10429; `_tick_entry` 7347 で評価 = row 6984 と latch `EXECUTED` 7025–7033 の**後**)。`_is_promoted` 偽 → post-gate escalation 7790–7798 で `_is_shadow=True` + `update_shadow_status(trade_id, True)` + DB log `[SHADOW_FIX] Post-gate escalation: weekend_gap_fade <pair> → shadow (OANDA gate blocked, excluded from resend)` → OANDA 未連携分岐 7956–7996 で oanda_audit `bridge_status=skipped` / `block_reason=shadow_tracking` (`_is_shadow` が真のため; `手動停止` 7976–7977 は `_is_shadow` 偽 = PRIME live lock (6945–6955、tier A/B) 下でのみ到達)。同じ経路は `pair_demoted` / `force_demoted` / `session_filter` / SHIELD・Kelly・MC ruin gate (7737–7784 の fall-through) でも起きる。OANDA は試みられず `sent` 無し、latch `EXECUTED` 済みで再評価なし (PR #289 review 2 巡目 P2 4075480961) | **不成立 (v) = 現行契約下では G0' の event を消費する**。根拠: 承認済み packet §6 表 (`weekend-gap-execution-contract-r1-packet-2026-09-10.md` L114–115) は G0' を「改定後の最初の 2 qualifying イベント」で定義し、row 1「fill 成立 (cap skip / 正当放棄を除く)」/ row 2「2 イベント連続で fill 不成立 (halt >15m / drift 放棄 / 新種 cancel)」— 非消費は cap skip・正当放棄 (registry g0prime message: 「正当放棄 (cap skip / ABANDONED_HALT / ABANDONED_DRIFT latch)」「NO-QUALIFY・cap skip は分母外」) に限られ、pre-send guard / slot / bridge 拒否 / 手動停止は名指しされていない。これらを分母から外すのは **user 承認済みの決定境界 (分母) の変更 = R1 + user、autopilot の R3 では不可** (CLAUDE.md Rule 1、PR #289 review 2 巡目 P1 4075480956)。よって現行契約に忠実な保守側の読み = 消費。**fill 経路の不成立 (i)〜(iv) とは区別して記録** (転記名 `不成立(v) PRE_SEND_GUARD(<reason>)`) — 帰属のためであり消費判定には影響しない。非消費・繰越案とその理由 (OANDA に order が届いておらず modality 未検証 / 容量・設定 outcome の誤帰属) は **AMENDMENT 提案 A-1 (§4 row 4)** に移し、user 承認まで効かない | (i) card イベントログへ `不成立(v) PRE_SEND_GUARD(<reason>)` として **24h 以内に必ず転記** (§6)。(ii) **識別手順 (帰属用)** — (a): Render ログ `qualifying event` ∧ 同 pair の EXEC_B `decision=SEND` ∧ **latch が `EXECUTED` でない** (row なし・latch なし、または後続 tick で `ABANDONED_*` に転じた場合を含む — (iv) の優先規則) ∧ DB logs `[SENTINEL_BLOCK_DIAG] weekend_gap_fade blocked at: <reason>` (1366、tick 時刻・完全 reason = **一次ソース**) / `gate_block_daily` (entry_type=weekend_gap_fade、当該日曜、日次集計の裏取り) の増分。**`<reason>` = 窓内で最初に増分した `order_bar_dedup` / `recent_emit` 以外の reason_key** (SENTINEL_BLOCK_DIAG の時刻順で初出)、**全 reason_key を併記**する。`order_bar_dedup` は分類理由に使わない — 予約後 blocker は初回の 1 件だけが真の blocker で以後は dedup の反復 (PR #289 review 2 巡目 P2 4075480969)。`recent_emit` (窓 900s、`_tf_to_window_sec` 15m) も帰属に使わない — wg では `_block` でなく `[SHADOW] recent_emit bypass` (= (b)) に落ち、かつ bar dedup (5523–5537) が先に return するため、複数 reason が並んだときの帰属を汚すだけである。(b): shadow row (`is_shadow=1`、reasons に `[WG_EXEC_B] … decision=SEND` あり ∧ `[WEEKEND_GAP_SPREAD_SKIP]` なし) ∧ oanda_audit `skipped` / `shadow_tracking` ∧ `sent` 行なし ∧ latch `EXECUTED` ∧ `oanda_trade_id` 空 ∧ DB logs に `[SHADOW_FIX] Post-gate escalation` **なし** ((d) との区別)。**gate の帰属は DB logs の `[SHADOW] <gate> …: weekend_gap_fade` 行 (event 時刻、8000 行刈り込みのため 24h 内読取) のみ**: `[SHADOW] Slot bypass` → `slot:max_per_mode_pair`; `[SHADOW] recent_emit bypass` / `spike bypass` / `velocity_up bypass` / `velocity_down bypass` / `Regime guardrail` / `session_pair bypass` / alpha_scan 系 `… → shadow` → `bypass:<gate>`; `[SHADOW]` 行が無く `[SENTINEL_BLOCK_DIAG] … shadow-downgraded` (7372) のみ → `max_open` 亜種 (5296–5297、専用ログ無し) を消去法 (`[WEEKEND_GAP] live stopped` / abandon / cap の log・cause が無い ∧ 当該時刻の open_trades ≥ max_open) で `slot:max_open`、確定できなければ `bypass:UNKNOWN`。(c): latch `EXECUTED` ∧ row `oanda_trade_id` 空 ∧ `sent` 行なし ∧ (oanda_audit `bridge_status=blocked` で `block_reason` ∈ {`daily_loss_limit(…)`, `bridge_inactive`, `mode_<mode>_not_allowed`} — または audit 行が一切無く DB logs に `[SHADOW_FIX] Bridge refused transmission: weekend_gap_fade <pair> trade=<id>` がある race)。転記名 `bridge:<block_reason>`、race は `bridge:refused_no_audit`。不成立 (iv) との区別 = (iv) は `sent` 行あり + `OPEN … FAILED` / `ok but no tradeID` log、(c) は `sent` 無し。(d): DB logs `[SHADOW_FIX] Post-gate escalation: weekend_gap_fade <pair>` ∧ oanda_audit `skipped` (`shadow_tracking`; PRIME lock 下のみ `手動停止`) ∧ `sent` 行なし ∧ latch `EXECUTED` ∧ `oanda_trade_id` 空 ∧ **帰属は event 時点の永続記録のみ**: `[SHADOW_FIX] Post-gate escalation` DB log の timestamp (= event 時刻) と oanda_audit `block_reason` (あれば — `_is_shadow` 偽の PRIME lock 下のみ `手動停止` が残る) → `手動停止` audit があれば `promo:mode_off`、無ければ **`promo:UNKNOWN` に固定** (`_promo_block_cause` = `mode_off` / `pair_demoted` / `force_demoted` / `session_filter` … は row にも audit にも永続化されない)。**転記時に `get_strategy_mode()` の現在値を読んで推定することは禁止** — event 後に off へ切替 → 偽の `promo:mode_off`、off から復帰 → 真の手動停止が UNKNOWN 化する (PR #289 review 3 巡目 P2 4075642847)。`_promo_block_cause` を row/audit に永続する R3 code 変更が必要 (本 PR は docs のみ、親セッションが別タスク chip として起票済み)。**oanda_audit の不在から推定しない** (§6)。`_shadow_reason_label` (7359–7367) は edge-cell force-live override 時の log 限定で row に残らず、wg の識別には使えない。(iii) **event #2 が (v) なら契約どおり不成立 2 連続 = packet §6 発動** → R1 再審 packet v1 をイベント +7 日以内に起案し、packet §1 (故障の機構) で **modality 型 ((i)〜(iv)) と容量・設定型 ((v)、亜種と reason)** を分けて user に提示、A-1 の採否も同じ packet で上程。旧「同一 guard 2 回で R3 レビュー」は契約の R1 再審に吸収 (削除)。(iv) **終端分類は entry 窓終了時点の最終状態**: (a-前) は再評価されるので窓内に guard が解けて送信試行に至れば fill / 不成立 (ii)〜(iv) の行が適用される。**優先規則**: 窓内に一度でも `decision=SEND` を通過し、その tick で `_tick_entry` guard により終端 (`gate_block_daily` / SENTINEL_BLOCK_DIAG 増分) した pair-event は、後続 tick で drift > +8.0p 等により `ABANDONED_*` に転じ shadow row + latch `ABANDONED_*` が残っても (放棄 sig は `_is_shadow=True` 5171–5174 で exposure guard 5340 を skip するため row を作りうる)、転記は `不成立(v) PRE_SEND_GUARD(<最初の理由>)` を**主**、後続 `ABANDONED_*` を**従**として併記する (消費は 1 event — 消費判定は (i) でも (v) でも同じ、帰属のみが変わる); (a-後) は初回 block で確定 (以後 dedup); (b)(c)(d) は latch `EXECUTED` が送信前に書かれるため当該 tick で確定 (再評価なし)。+15 分打ち切りは halt (tradeable=False) 継続時の `ABANDONED_HALT` 条件であり、guard 到達の終端条件ではない。(v) §4 row 8 の永続 first-qualification ts は guard early-return の再発火で上書きしない設計 — 再発火は同一 pair-event として扱う。(vi) 本行は「観測して待つ」の 3 度目 (§7) ではなく分類規則の適用 | **event #2 が (v) なら発動** (契約どおり不成立 2 連続 — R1 起案へ昇格、user 承認)。非消費・繰越への変更は A-1 (§4 row 4) の user 承認後のみ (PR #281 review P2 4070529776 / PR #289 review 1 巡目 P1 4075088689、2 巡目 P1 4075480956・P2 4075480961・4075480969、2026-09-23 改訂) |
| **NO-QUALIFY** (3 ペアとも閾値未達) | 分母外 | gap 診断ログの値を card へ転記 (near-miss を理由に閾値は触らない — stage-2 §8)。次週へ繰越 (10-04 → 10-11 → …) | 発動せず |
| **同一週末に複数 pair-event が qualify** (例: fill + abandon) | pair-event 単位、**G0' の 2 event で終了**。順序 = **qualification 順** (先に qualify した pair-event が event #2) が原則 | **順序付けは、永続 first-qualification ts が存在するまで運用に使わない** (PR #281 review P2、5 巡目)。理由: 専用の永続 ts は未実装 (§4 row 8) で、代理はいずれも qualification 順を保証しない — (i) 終端結果 (filled / cancel tx) の時刻は halt-race 再送 +30s (`oanda_bridge.py:751–800`) で反転する (反例 (d)); (ii) Render ログ `[WEEKEND_GAP] … qualifying event` 行 (demo_trader.py:4263–4266) は `_tick_entry` 前に print され、guard (max_per_mode_pair / hedge_block / max_open、5234–5299; `drawdown` 5302–5303 は dead code) が return すると同一ペアが次 tick で再発火し複数時刻を持つ上、非永続 (retention ~30 日); (iii) system_kv latch `ts` (4001–4034 / 7025–7033) と shadow row `entry_time` (6984) は **pricing HOLD・`_tick_entry` guard・`open_trade` の後**に立つため、先に qualify したペアが後に latch する (guard return なら latch しない) — event #2 を取り違えて発動を抑止/誤発火させうる。**事前固定の扱い**: (α) 同一週末の qualifying pair-event の結果が**全て同一クラス** (全 fill / 全 不成立) → 順序不問で event #2 の結果は一意 → 上の行をそのまま適用; (β) **混在** → event #2 を機械的に決めない。全 pair-event の全時刻 (Render ログ初回 `qualifying event` 発火 [再発火があれば全件] / latch `ts` / `entry_time` / 終端) を 24h 以内に card へ転記 (§6) し、**W5 として user に上程** (「先に qualify したのはどれか」の両読みと帰結 = 発動 / G0' 完了 を併記)。裁定まで発動判定は保留、live 経路は契約 B のまま継続 (放棄は fail-closed、4原則#3); (γ) 永続 first-qualification ts 実装後 (R3 単独 PR、§4 row 8、**09-27 21:00Z 前が望ましい**) は同 ts 順で機械判定に戻す。**判定は G0' = 改定後最初の 2 qualifying pair-event のみ**: 09-13 USD_JPY = event #1 (不成立)。event #2 で G0' は終了 — fill → 完了・通常運用 (後続 pair-event は記録のみ)、不成立 → 2 連続 = 発動。worked example (順序が確定している前提、先頭 = 09-13 DRIFT): (a) `fill → ABANDONED_DRIFT → ABANDONED_HALT` → event #2 = fill → **G0' 完了、発動せず** (後続 2 放棄は §3-6 の観測データ。rolling 規則は packet §6 の外 = 未承認); (b) `ABANDONED_DRIFT → fill → …` → event #2 = DRIFT → **発動** (後続 fill は F2 resolve のみ); (c) `fill → fill → ABANDONED_DRIFT` → (a) と同じ; (d) 終端時刻の反例: pair A が先に qualify → SEND → halt-race 再送で +33s 後に filled、pair B が後に qualify → 即時 `ABANDONED_DRIFT`。qualification 順では event #2 = A-fill → 完了・発動せず、終端時刻順なら B-DRIFT = **偽の発動** — (β) の上程ではこの両読みを併記する。F2 resolve (registry 明示編集) は fill の事実で独立に起きる。⚠️ packet §6 / registry g0prime は「イベント」の単位 (pair-event / 週末) を明示していない — **本稿の読みは pair-event。週末単位の別規則や G0' 終了後の rolling 連続不成立規則を運用に使うには packet §6 文言の user 承認が先** (未承認、本稿は使わない)。昇格時に文言を確定する | (α) は上の行で決まる / (β) は W5 裁定まで保留 / (γ) 実装後は機械判定 |

**文言上の未整備 (昇格時に解消、凍結値には触れない)**: packet §6 row 1 は「fill 成立 (cap skip / **正当放棄**を除く)」、row 2 は「fill 不成立 (halt >15m / **drift 放棄** / 新種 cancel)」— drift 放棄が「正当放棄」に含まれるなら両行が矛盾する。運用上の読み (O-2026-09-14-1、registry g0prime message、[[2026-09-22-session]]) は **drift/halt 放棄 = 不成立、cap skip のみ = 正当な未執行**。本稿はこの読みで 09-13 を 1 件目とカウントしている。

---

## 3. 変更候補表 (発動時に一次データで確定する — 本稿は骨子)

前提: 09-13 型の不成立は「gap の大半 (~47p/50p) が OANDA halt 窓 (~4 分、48/48 で +4.0 分) の内側で消費される」型。**約定不能な時間帯に起きた値動きは、どの送信タイミング・注文種別でも捕まえられない**。したがって候補は (a) 消費後の残余で入る (drift 境界の変更) か、(b) 消費後の価格を基準に signal を再定義する (estimand の executable 化) か、(c) 何も変えず観測を積む、の 3 系統に分かれる。

| # | 候補 | 触る凍結値 (出所) | estimand への影響 | 09-13 型への効果 | 必要な検証 | user 承認 | 起案者暫定評価 |
|---|---|---|---|---|---|---|---|
| 1 | **打ち切り +15 分 の変更** (+30 分 / entry 窓末端 60 分まで) | `WEEKEND_GAP_HALT_ABANDON_MIN = 15` (`strategies/daytrade/weekend_gap_fade.py:89`)、packet §4-2、test pin `tests/test_weekend_gap_execution_contract_b.py:120` | entry が Sunday open からさらに離れ、繰下げ drift コストが増える方向 (全 48 の +15m adverse drift mean 1.27p / p90 5.6p / max 30.4p、json)。qualifying サブセットの +15m 値は未計測 | **なし** — 打ち切りは一度も binding していない (実開場 +4.0 分 48/48、09-13 も tradeable 確認済みで放棄理由は drift) | 価格のみ: `tools/wg_gap_drift_measure.py` の offset を 30/60 分へ拡張し、09-06 以降の新週末で再計測 (outcome 非結合、OOS 窓不接触) | 凍結値変更 = **R1** | **動機なし、据え置き**。11-01 DST の R3 再導出 (§5) のみ実施 |
| 2a | **drift 境界 +8.0p 据え置き** | — | 不変 | なし (09-13 型は今後も放棄され続ける = 選択バイアス仮説が真なら「最大級 event を live から系統的に除外」) | §3-6 の forward 観測で仮説を検証 | 不要 | 現状。fill 成立率の見積り (点 ~94% / 保守 ~75%、packet §5.2) は **+8.0p 抵触率 3/48 を全週末で置いた値** — qualifying 大 gap 週末での抵触率が高いなら見積りは過大。昇格時に修正値を出す (価格のみで可) |
| 2b | **drift 境界の引き上げ** (例: 固定 12p / 15p / 全週末 p95) | `WEEKEND_GAP_DRIFT_ABANDON_PIPS = 8.0` (`:90`)、packet §4-3、test pin `:132`、demo_trader.py:4192 コメント | 境界は「凍結 stressed-net +7.90p を全消しする水準」として導出 (packet §4-3)。引き上げ = **mean 基準で残余 EV ≤ 0 の event を live 母集団に入れる**。「大 gap ほど 4h で多く戻る」が真なら救われるが、それは **outcome 条件付き主張 = OOS 窓では計算禁止**。shadow 分母は不変 (LIVE 側 winning-location フィルタの緩和、4原則#3 の非対称は保たれる)。G2 (−60p) が per-N の唯一の防波堤になる | 部分的 (+41p は 12p/15p でも放棄。09-13 型を救うには実質 2d) | (i) 価格のみ: \|gap\| vs tradeable 時 drift の相関 (O-2026-09-14-1 の反証可能予測) を forward 週末 + json 48 件で計測 — 可。(ii) outcome 条件付き (drift 帯別の 4h 回帰): **OOS 窓 = 禁止** / **explore 窓 (2014–2021) = 仮説生成のみ可、confirmatory 価値なし**、しかも explore 窓に 1m データが無ければ tradeable 時点 drift を再現できない (実施可能性未確認)。(iii) **fresh forward OOS** (境界値を pre-reg し live/shadow 両方で N 蓄積) = 唯一の confirmatory 経路、N は 2.07 週末/月で遅い | **R1 全段 + user** (凍結値変更 + 4原則 解釈は UD7 と同型) | 効果が限定的で estimand コスト大。**単独では推奨しない** |
| 2c | **gap 比例境界** (0.4×\|gap\| 等) | 同上 | パラメータ面の拡大 (packet §4-3 で**不採用済み**) | 09-13: 0.4×50 = 20p でも +41p は放棄 | 同 2b | R1 | packet 決裁を覆す理由が無い。**不採用維持** |
| 2d | **drift 境界の撤廃** (tradeable 確認のみで送信、G1/G2 のみで防御) | 同上 + packet §4-3 削除 | **per-event の負 EV 上限が消える**: 09-13 なら残余 (−50+41) = 9p の gap を fade する entry になり、凍結 stressed-net +7.90p は mean で全消し済み。live 母集団 = 「qualify した全 event の tradeable 価格 entry」= **OOS が測った estimand (Sunday open 価格 entry) とは別物**。G2 −60p は N=12 まで作動しない | **送信適格化のみ (fill ではない)**: drift 境界を外しても下流で `SKIPPED_SPREAD` (cap 10.0p、demo_trader.py:6229–6249 / quote 取得不能の fail-closed 6313–6326)、pre-send guard 未到達 (§2)、bridge の daily-loss gate `blocked` (oanda_bridge.py:668–690 = §2 不成立 (v) 亜種 (c))、FOK cancel 2 回目 `MARKET_HALTED` / `MARKET_HALTED` 以外の reason の cancel (halt-race 再送 751–800、fill 判定 802–807)、transport・order 失敗 (`ok but no tradeID` 880–892 / `OPEN … FAILED` 893–909) が残る。**fill 率は未知 = forward 計測**、F2 が resolve するとは書かない (PR #281 review P2 4070529787)。送信判定に到達した 09-13 型の entry は残余 (−50+41) = 9p の fade = 負 EV の可能性が高い event を取ることと同義 | 同 2b(iii): fresh forward OOS のみ。事前に「fill 成立率↑ と per-event EV↓ のトレードオフ」を凍結値からの単純減算で開示 (packet §5.3 の様式) | R1 全段 + user | F2 (12-31、N≥1) を resolve **し得る**最短経路 (送信適格化まで — fill は forward 計測で確実ではない) だが、**「PASS→live 変換の実証」ではなく「別 estimand の live 開始」**。F2 の趣旨 (PASS の live 変換) を満たすかは user の解釈事項として上程する |
| 3a | **送信タイミング: 現行** (tradeable 後の最初の評価 tick、poll ≤60s) | — | 不変 | なし | — | 不要 | 現状 |
| 3b | **22:01 固定 (spread 崩落後)** | packet §4-1 / stage-2 §1「22:01 遅延は estimand 変更 = OOS 再検証不能 → 前向き検証のみ」(既裁定) | entry が open+61 分 → 繰下げ drift がさらに増加。spread は通常圏 (cap 10.0p の意味が消える) | **なし** (drift は増える方向) | fresh forward のみ (既裁定) | R1 | 既裁定どおり **不採用** |
| 3c | **tradeable poll の短縮** (≤60s → ≤10s) | `WEEKEND_GAP_TRADEABLE_POLL_MAX_SEC = 60.0` (`:91`)、packet §4-1 | drift 窓が最大 50s 短縮 = favorable 方向。estimand 上は同一 (tradeable 後初 tick) | **なし** — 09-13 は tradeable 確認時点 (quote_age 7.5s) で既に +41p。消費は halt 窓内 | 価格のみ: EXEC_B ログの tradeable 確認〜送信の実遅延を forward で記録 | 凍結値 = R1 (ただし方向は保守側のみ) | 効かないので**単独起案しない**。他候補と同時なら同梱可 |
| 3d | **開場前 pending 注文** (halt 中に limit/stop を置き、開場と同時に成立させる) | 契約 B §4-1 (送信前置条件) + stage-2 §2.2 | 指値化 (#4) と同じ estimand 問題 + **OANDA が halt 中の pending order を受理するかが未確認** (`MARKET_HALTED` は FOK cancel の reason として実測済みだが pending 受理可否は一次確認ゼロ) | 不明 (仕様次第。stop 注文なら 09-13 型は約定するが adverse 方向の約定になる) | 一次確認 (OANDA API 仕様 read-only) → 成立しても #4 と同じ検証が必要 | R1 | 一次確認だけは昇格前に可 (仕様読み、発注なし)。採用は #4 と同じ扱い |
| 4 | **指値化** (Sunday open 価格 / open±x に limit、GTD = 打ち切り) | stage-2 §2.2「指値変種は不採用」、packet §3 (C) 棄却 | fill が「価格が基準まで戻る」条件付きになる = **adverse selection** (gap が fade しない/再拡大するときだけ fill) | **なし** — 09-13 は BUY fade で価格が open より +41p 上。open (153.120) の BUY limit は gap 再拡大時しか fill しない | fill 率は forward のみ (shadow 側で「指値なら fill したか」を価格のみで並行記録 = 分母外の観測、stage-2 §2.2 が既に許容) | R1 | 09-13 型の救済にならない。**不採用維持**、並行記録のみ検討 |
| 5 | **シグナル基準の executable 化** (sunday_open = OANDA 初 tradeable mid、gap も同基準で再計算) | シグナル定義 (card §シグナル定義、stage-2 §2.1、OOS prereg §2 凍結定義「Sun 21:00 UTC 以降の最初の 15m バー Open」) — **OOS estimand そのもの** | **最大**。09-13 は gap −50 → 残余 ≈ −9p で **qualify 不成立** (不成立ではなく分母外へ移る)。OOS verdict (arm B PASS) は Sunday open 基準で測ったもので、この定義には**適用できない**。OOS 窓で再計算 = 再接触 = 禁止 → 事実上 **新 family** | 「不成立」を消す (イベント自体が消える) — 選択バイアス問題の定義上の解消であり、EV の証明ではない | fresh forward OOS のみ (N floor は OOS prereg §4(e) arm B N≥60 相当 → 数年)。explore 窓は 1m データが無ければ再現不能 | R1 全段 + user (family 新設に相当) | 論理的に最も正直だが、**時間軸が M 系列と噛み合わない**。昇格時に「選択肢として上程、推奨はしない」 |
| 6 | **契約不変更の forward 観測パケット** (放棄イベントの価格系蓄積) | なし | なし (観測のみ)。EXEC_B ログと `[WG_EXEC_B]` reasons が既に drift / send_mid / tradeable / quote_age を永続化 (§4.6) | 救済しない。**2b/2d/5 の検証入力になる** | 価格のみ: 全 qualifying event の (\|gap\|, tradeable 時 drift, decision) を card に転記 → O-2026-09-14-1 の予測 (\|gap\|≥40p で drift>8p が常態) を N 蓄積で検証。outcome 非結合 | **不要** (記録のみ、R3) | **今すぐ実施 (本 PR の card 転記がその第 1 行)**。no-regret |
| 7 | **G0' 判定規則の文言整備** (packet §6 の「正当放棄」/ 単位) | なし (文言のみ、閾値不変) | なし | なし | — | 解釈確認 1 行 | §2 末尾の読みで運用、昇格時に packet §6 へ追記 |

**表の読み方**: 09-13 型を**送信判定まで到達させられる**のは 2d と 5 だけで (fill は別問題 — 下流の cap / pre-send guard / cancel / 送信失敗が残り forward 計測、§3 2d)、どちらも「OOS が PASS した estimand とは別の estimand を live で始める」ことになる。これは **「PASS→live 変換の実証」(F2 の趣旨) とは別の問い**であり、昇格時の packet はこの点を user 向け 3 行の 1 行目に置く。

---

## 4. 発動時の R1 起案 packet の構成 (packet 2026-09-10 の様式を踏襲)

| § | 内容 | 本 DRAFT からの引き継ぎ | 発動時に追加する一次データ |
|---|---|---|---|
| 0 | 決裁サマリ 3 行 | §0 | 不成立 2 件目の事実 (時刻・decision・drift・tx) |
| 1 | 故障の機構 (2 不成立の root cause 分解: drift 型 / halt 型 / 新種 cancel) | §1 | 2 件目の EXEC_B ログ全遷移 (HOLD→SEND/ABANDONED)、oanda_audit 行、cancel tx (該当時) |
| 2 | 一次データと測定 (価格のみ、look-burn 回避の宣言) | §3 の検証列 | `tools/wg_gap_drift_measure.py` を 09-06 以降の週末で再走 (offset 拡張含む)、qualifying event の (\|gap\|, drift) 表 |
| 3 | 候補比較 | §3 表を確定 | 各候補の fill 成立率見積り (価格のみ) と per-event EV の凍結値からの単純減算 |
| 4 | AMENDMENT 条項案 (採用候補のみ、承認時にこの文言で LOCK) | **提案 A-1 (未承認、user 決裁事項、PR #289 review 2 巡目 P1 4075480956 で本欄へ移管)**: 「pre-send guard 未到達 (§2 不成立 (v) 亜種 (a)〜(d)) は G0' / G0'' の event を消費しない (分母外・繰越)」。理由: OANDA に order が届いておらず契約 B の modality (FOK / halt-race 再送 / drift 境界) は一切試されていない; 容量 (slot / DD / exposure)・口座リスク容量 (daily-loss halt)・設定状態 (inactive / mode 不許可 / 手動停止) の outcome を不成立に数えると modality に誤帰属し R1 判断を歪める。付帯 (α) 同一 reason で 2 回未到達 → 容量・設定側の R3 レビュー起案 (閾値・設定の変更は R1 = user)、(β) 持続型 (DD/日次 CB は `_oanda_kill` 3952–3965 が `_allowed_modes` を空にするため (c2) `bridge:mode_<mode>_not_allowed` として現れ、user が OA モードを再 ON するまで毎週末持続 — `🔴 OANDA CB発動` 3959 / `[CB] OANDA killed` 3961 のログを併記して識別; `bridge_inactive` / `mode_off` も同型) は毎週末同じ理由で繰越が続き G0' が永久に閉じない型になるため (α) が 2 回目で必ず拾う。**承認までは §2 のとおり (v) 消費で運用** — packet §6 row 1「正当放棄」の範囲拡張 = 分母 (決定境界) の変更なので R1 + user | 触る凍結値の新値と根拠、不変更リストの明示。A-1 を採る場合は packet §6 row 1/2 の文言改定として同梱 |
| 5 | fill 成立率の事前見積り + estimand コスト (F2 テンプレ、meta-audit §6) | §3 の estimand 列 | 2 件の不成立を含めた見積り修正 (qualifying 大 gap 週末の抵触率) |
| 6 | 採用/棄却境界 (forward、承認時に凍結) | §2 の様式 | 新契約下の G0'' (最初の 2 qualifying event) と「3 度目はしない」の帰結 (family live-execution 保留の条件) |
| 7 | registry 変更提案 (registry 本体は別担当) | §6 の 24h 規則 | g0prime 期日繰越 / F2 message 追記 / G1・G2 message 追記 |
| 8 | 実装チェックリスト + pre-reg 突合 (stage-2 §8 様式) | — | 凍結値 pin テストの更新範囲、counterfactual pin を同一 commit。**専用 first-qualification ts の system_kv 永続化** (qualifying event 判定時に 1 回、guard early-return の再発火で上書きしない — R3、凍結値不変更、PR #281 review 4 巡目) は発動と無関係に単独 R3 PR で実施可 — **09-27 21:00Z 前を推奨**。無ければ混在週末の event #2 は W5 上程 (§2 (β))。pre-send guard 未到達 (`PRE_SEND_GUARD`、§2) による同一 pair の再発火は同一 pair-event であり、この ts の消費・上書き対象ではない (未到達 (v) の消費判定は §2 — A-1 承認前は消費) |

**禁止事項 (packet §4 末尾の継続)**: OOS 再接触・再集計 / qualify 閾値・cap・G1/G2/G3 凍結値の変更 (候補が触るのは §4 AMENDMENT 条項の値のみ) / BE/Trail/TP 追加 / GBP_USD 拡張 / 「observe して待つ」の 3 度目。

---

## 5. 冬時間 2026-11-01 — 打ち切り時刻の R3 再導出 (2026-10-25 までに完了)

- **暦**: EU 夏時間終了 = 2026-10-25 (10 月最終日曜) だが OANDA の週明け開場は NY 17:00 基準で **10-25 は 21:00 UTC のまま** (US は EDT 継続)。US 夏時間終了 = **2026-11-01** (11 月第 1 日曜) → 以後 Sunday open **22:00 UTC**、実開場は stage-2 §2.1 の記載どおり **22:04 見込み** (未実測 — 凍結 12 週末・16 週末とも全て夏時間、packet §5.3 (i))。
- **再導出の対象は「基準時刻の解決」であって「+15 分」ではない** (+15 分は凍結値、変更は R1)。10-25 までに以下を price-only / code-read で確認し、結果を [[weekend-gap-fade]] へ追記する:
  1. `first_bar_ts` (`det["first_bar_ts"]`、demo_trader.py:4205) が冬に **MASSIVE 22:00 stamped バー**へ解決するか — 前冬 (2025-11-02〜2026-03-08) の Sunday 初バー ts を parquet で読む (価格系のみ、outcome 非接触)。`weekend_key_for` は `hour >= 21` で 22:xx を含む (`weekend_gap_fade.py:120` docstring)。qualify guard の冬 22:00 は `tests/test_weekend_gap_fade.py:166` で pin 済みだが、**打ち切り基準 (first_bar_ts + 15 分) の冬解決は pin されていない** → test 追加は R3 (凍結値に触れない)。
  2. もし MASSIVE が冬に 21:00 stamped バーを出す構造なら、打ち切り 21:15 < 実開場 22:04 で **毎冬週末 `ABANDONED_HALT` が決定論的に発火 = 冬季 live fill 0 の構造バグ** → Rule 3 (構造バグ) で 11-01 前に修理。これは「+15 分」の変更ではなく基準時刻の是正。
  3. G0 発火時刻仕様 (冬 22:05±5m、card §前向きゲート) と EXEC_B の実送信時刻の整合を 11-01 当日に確認 (packet §6: 実開場が 22:04–22:05 から >±10 分乖離 → R3 再導出、凍結値変更は R1)。
  4. 11-01 が qualify し、**かつその時点で G0' event #2 が未発生の場合のみ**、それを G0' の event #2 として数える (DST を分母外にしない)。9〜10 月の qualifying pair-event で G0' が既に終了 (event #2 発生) していれば **数えない** — §2 のとおり event #3 は存在せず、rolling gate は未承認 (PR #281 review P2、6 巡目)。数えない場合も本節の R3 再導出と月次 re-run は実施する。同日は `tools/sunday_open_spread_measure.py` / `wg_gap_drift_measure.py` の月次 re-run を必ず走らせる (常設 gate、冬時間初週末は注視 — card §前向きゲート「常設」)。
- **期限**: 1–2 を 2026-10-25 まで、3–4 は 11-01 当日〜11-02 (24h 規則)。

---

## 6. 記録規則 — 「イベント後 24h 以内に card 転記」 (g0prime 手順への追記案)

- **事実**: 09-13 の event #1 は O-2026-09-14-1 (09-14) と trade-log 09-16 に記録されたが、戦略カード [[weekend-gap-fade]] のイベントログには **2026-09-22 (9 日後) まで転記されなかった**。G0' 手順は「週次監査で確認」としか書いておらず、card 転記の期限が無かった。
- **規則 (提案、registry g0prime message への追記 = 別担当)**: 日曜 21:00 UTC (冬 22:00) のイベント後 **24h 以内 (月曜 daily report と同時)** に、qualify / NO-QUALIFY を問わず card イベントログへ 1 節を追記する。
- **転記するもの (価格系・執行系のみ)**: ペア別 gap (p) と閾値、**全時刻の併記 — Render ログ `qualifying event` 初回発火 (再発火があれば全件)、system_kv latch `ts`、shadow row `entry_time`、終端 (filled / cancel)** (いずれも単独で順序キーにしない — 永続 first-qualification ts 実装まで混在週末は W5 上程、§2)、decision (SEND / HOLD 回数 / ABANDONED_* / SKIPPED_SPREAD / **`不成立(i) ABANDONED_*/PRE_ROW_BLOCK(<reason>)`** (ABANDONED 後に row・latch 無し、§2 不成立行 (i)) / **`不成立(v) PRE_SEND_GUARD(<reason>)`** — §2 (v) 行、(a) は窓内初出の `order_bar_dedup` / `recent_emit` 以外の reason_key を主に**全 reason_key を併記** (SENTINEL_BLOCK_DIAG 時刻順)、(b) は `slot:<種別>` (`max_per_mode_pair` / `max_open`) または `bypass:<gate>` (`[SHADOW] <gate> …` ログのみから、確定不能は `bypass:UNKNOWN`)、(c) は `bridge:<block_reason>` (`daily_loss_limit` / `bridge_inactive` / `mode_<mode>_not_allowed` / `refused_no_audit`)、(d) は event 時点記録 (`[SHADOW_FIX] Post-gate escalation` timestamp / audit `block_reason`) からのみ — `手動停止` audit があれば `promo:mode_off`、無ければ `promo:UNKNOWN` に固定、現在の strategy mode からの推定禁止 — を書く)、tradeable・quote_age、drift (p)、send_mid、latch 値、oanda_audit id / tx id と終端結果の時刻 (fill / cancel — 順序キーには使わない)、fill 時は `oanda_trade_id` と当該 1 event の persisted slippage 値 (配管確認、集計しない)。
- **ペア別分類の確定源は Render ログ `[WEEKEND_GAP]` gap 診断行 (`gap=… no-qualify` / `qualifying event`) または shadow row のみ**。oanda_audit の有無・行数から no-qualify を推定しない — audit 不在は「監査経路未到達」しか意味せず、`_weekend_gap_tick` の非監査 return (weekend_key None / latch 済 / データ欠損・不足 / det None) や `_tick_entry` の早期 block で qualify 後に消えた pair-event と区別できない。診断行が retention (~30 日) 内に読めなければ当該ペアは **UNKNOWN** と転記し、pair-event 列 (§2) と G0' カウントは「UNKNOWN が qualify していた場合の条件付き」で併記する (PR #281 review P2、2 巡目)。**診断行自体が無い qualify 済みペア** (データ欠損で `det is None` かつ `gap_pips` None) は packet §6 の列挙 (halt / drift / 新種 cancel) の外 = 分類未定義 → 昇格時の文言確定事項 (§3 row 7) に含める。`_tick_entry` 早期 block は qualifying 診断行 (4263–4266) の**後**に起きるので「診断行が無い」型ではなく、2026-09-23 改訂で **`PRE_SEND_GUARD` = 不成立 (v)** (現行契約下では G0' event を消費、非消費化は §4 row 4 A-1 の user 承認後のみ) として §2 に事前定義済み (PR #281 review P2 4070529776 / PR #289 review 2 巡目 P1 4075480956)。
- **転記しないもの**: shadow row の pnl / MFE / 埋め率、G1 rolling mean、G2 累積、drift 帯別や CB 週別などの条件付き outcome (O-2026-09-14-2 の教訓 — 未登録 split の事後シードは禁止)。
- **NO-QUALIFY 週末も転記する**: 近接値 (09-20 の −19.0p / −20.5p) は「閾値を触る根拠」ではなく「分母外イベントの記録」。閾値は凍結値 (stage-2 §8)。

---

## 7. 禁止事項 (本 DRAFT と昇格後の packet に共通)

- OOS 窓 (2022-01-01〜2026-06-30) の再接触・再集計、shadow outcome の転記、G1/G2 の中間計算、drift 帯別・gap 帯別の 4h 回帰の計算 (どの窓でも confirmatory には使えない、explore 窓は仮説生成のみ)。
- 凍結値 (+15 分 / +8.0p / poll 60s / quote age 10s / 再送 30s×1 / cap 10.0p / 1000u / 4h / disaster SL 150p / qualify 閾値 / G1 +2.0p / G2 −60p / G3) の autopilot 変更。全て R1 + user。
- near-miss (09-20 −19.0p / −20.5p、07-26 +19.9p) を理由にした qualify 閾値の再計算 (定義ドリフト、`weekend_gap_fade.py:50–51` コメント)。
- 「live fill 0/7 累計」型の改定前後混在カウント ([[path-to-win-reassessment-2026-09-22]] §7-7)。改定後の連続不成立は **契約 B 下の qualifying イベントのみ**で数える。
- 「観測して待つ」の 3 度目 (packet §6)。発動したら 7 日以内に packet v1 を出す。
- 本 DRAFT を LOCK / 決裁済みとして引用すること。
- watcher (`evaluate_live_count_decision`) の `TRIGGERED` 表示を F2 **resolved** と読むこと — resolve は registry の `active:false` + `resolved` + `resolution` 明示記録のみ (§2 row 1、PR #281 review P2)。
- pair-event 列を週末単位で一括分類すること (fill の有無で週末をまとめて成立/不成立と読む) — 判定は pair-event 単位、対象は G0' の 2 event のみ (§2、PR #281 review P2 1・4 巡目)。
- G0' event #2 が fill で完了した後も連続不成立 trigger を生かし続けること (rolling gate) — packet §6 は「改定後の最初の 2 qualifying イベント」のみ、継続は packet 改定 = user 承認 (PR #281 review P2 4 巡目)。
- SEND 後 2 回とも `MARKET_HALTED` cancel を「新種 cancel でない」として不成立から外すこと — trade id も filled audit 行も無い = 不成立 (halt 型) (§2 row 2、PR #281 review P2 4 巡目)。
- SEND 後に fill も cancel tx も無く終端した qualifying pair-event (`OPEN … FAILED` / `ok but no tradeID`) を未分類のまま置くこと、または人手・自動で再送すること — 不成立 (iv) として event を消費、再送は契約 B §4.4 の halt-race 限定 (§2 row 2、PR #281 review P2 6 巡目)。
- G0' が event #2 で終了した後の 11-01 qualifying event を G0' の event として数えること (§5-4、PR #281 review P2 6 巡目)。
- oanda_audit の不在・行数から未観測ペアを no-qualify と分類すること — 確定源は gap 診断ログ / shadow row のみ、読めなければ UNKNOWN (§6、PR #281 review P2 2 巡目)。
- 混在週末の pair-event 列を終端結果 (filled / cancel tx) の時刻、Render ログの qualifying event 発火時刻、system_kv latch `ts`、shadow row `entry_time` のいずれかで**機械的に**並べて event #2 を決めること — 終端時刻は halt-race 再送 +30s で反転、ログ行は非永続で guard early-return により再発火、latch / entry_time は HOLD・guard・open_trade の後に立つ (遅れる、または立たない) ため、どれも qualification 順を表さない。永続 first-qualification ts (§4 row 8、R3) が存在するまで混在週末は W5 で user 上程 (§2 (β)、PR #281 review P2 3〜5 巡目)。
- pre-send guard 到達の qualifying pair-event (`PRE_SEND_GUARD`、§2 不成立 (v) 行、亜種 (a)〜(d)) を **user 承認前に分母外 (非消費・繰越) として扱うこと、または未分類で置くこと** — 現行契約 (packet §6 表 L114–115 / registry g0prime message) の非消費は cap skip・正当放棄・NO-QUALIFY に限られ、未到達は不成立 (v) として消費する。非消費化は AMENDMENT 提案 A-1 (§4 row 4) の R1 + user 承認のみ。slot 由来 shadow row / bridge-refused row / 手動停止 row (いずれも `oanda_trade_id` 空・latch `EXECUTED`・`sent` 無し) を fill 経路の不成立 (i)〜(iv) と**混ぜて記録すること**も禁止 (帰属のため (v) と明記)。分類は entry 窓終了時点の最終状態、(a) の帰属は初出の `order_bar_dedup` / `recent_emit` 以外の block 理由 (SENTINEL_BLOCK_DIAG を一次ソース、全 reason 併記)、(b) の gate 帰属は `[SHADOW] <gate>` ログのみ、(d) の帰属を転記時の現在 strategy mode から推定しない (event 時点記録のみ)、`drawdown` を到達可能 guard として引用しない (dead code、CB は (c2) として現れる) (PR #281 review P2 4070529776 / PR #289 review 1 巡目 P1 4075088689・2 巡目 P1 4075480956・P2 4075480961・4075480969、2026-09-23 改訂)。
- 候補 2d の drift 境界撤廃を「09-13 型が fill する / F2 が resolve する」と読むこと — 撤廃は送信適格化に過ぎず、下流の `SKIPPED_SPREAD` / pre-send guard (bridge-refused `blocked` = 不成立 (v) 亜種 (c) を含む) / FOK cancel (2 回目 `MARKET_HALTED`・別 reason) / 送信失敗が残る。fill 率は forward 計測 (§3 2d・表の読み方、PR #281 review P2 4070529787、2026-09-23 改訂)。

---

## 8. user 承認が必要になる点 (発動時、または混在週末 (β) のみ — 現時点では 0 件)

| id | 問い | 選択肢 | いつ |
|---|---|---|---|
| W1 | R1 再審起案の承認 (packet v1 提示) | 起案する / family live-execution 保留 / 契約 B 継続で観測延長 (packet §6 は「3 度目はしない」と凍結しているため、これは §6 の撤回 = 明示決裁が必要) | 不成立 2 件目 +7 日以内 |
| W2 | 候補の選択 (§3: 2a / 2b / 2d / 5 / 6 のみ、他は不採用維持) | packet v1 §3 の比較表 | packet v1 提示後 |
| W3 | F2 の解釈 — 「PASS→live 変換の実証」は Sunday open 基準 estimand の live fill のみを指すか、executable 基準 (2d/5) の fill も含むか | 前者 / 後者 | W2 と同時 (2d/5 を選ぶ場合のみ) |
| W4 | 4原則#3 の解釈 — drift 境界は「勝てる場所で勝つ条件だけ転送する LIVE 側フィルタ」として意図的か (UD7 velocity_down と同型) | 解釈の裁定 | W2 と同時 (2b/2d の場合) |
| W5 | 同一週末に複数 pair-event が qualify し結果が混在した場合の event #2 の裁定 (永続 first-qualification ts 実装前のみ、§2 (β)) | 「先に qualify したのはどれか」の両読みと帰結 (発動 / G0' 完了) を併記して上程 — 全時刻 (ログ初回発火 / latch / entry_time / 終端) を添付 | 該当週末 +24h (card 転記と同時)。裁定まで発動判定保留、live 経路は契約 B 継続 |

---

## 参照

- [[weekend-gap-fade]] (戦略カード、イベントログ 2026-09-13 / 2026-09-20 節は本 PR で転記) / [[weekend-gap-execution-contract-r1-packet-2026-09-10]] (§4 AMENDMENT、§5 見積り、§6 境界) / [[weekend-gap-stage2-execution-prereg-2026-07-24]] (§2.2 原文、§8 禁止事項) / [[weekend-gap-oos-prereg-2026-07-24]] (§3 OOS 窓と接触規律)
- [[daily-observations-2026-09]] O-2026-09-14-1 (event #1 の価格系記述と選択バイアス仮説) / O-2026-09-14-2 (未登録 split 事後シード禁止の教訓)
- [[2026-09-16]] L241–242 (oanda_audit の放棄行と日別行数) / [[2026-09-22-session]] (09-20 NO-QUALIFY の Render ログ実読)
- [[path-to-win-reassessment-2026-09-22]] §3 Rank 6 / §7-7 / §8 (wg 頻度 N=4 の caveat)
- [[process-meta-audit-2026-09-07]] §6 F2 (2026-12-31)
- code: `strategies/daytrade/weekend_gap_fade.py:50–58, 68–70, 89–97` / `modules/demo_trader.py:97–100, 4180–4245` / `tests/test_weekend_gap_execution_contract_b.py:54–66, 120, 132` / `tests/test_weekend_gap_fade.py:166`。2026-09-23 改訂で追加 (main `7145a93c` 実測): `modules/demo_trader.py:1319, 3906, 4236–4267, 5009–5010, 5234–5351, 6229–6249, 6313–6326, 7025–7033, 7359–7367, 1361–1366, 1369–1408, 3906–3965, 4158–4159, 4254–4267, 5089–5096, 5114, 5171–5174, 5523–5537, 5552–5558, 5933–6083, 6176–6181, 6357–6404, 6945–6955, 7347–7375, 7504–7510, 7790–7798, 7876, 7902–7955, 7956–8003, 9436–9438, 9878–9927, 10022–10031, 10416–10429` / `modules/demo_db.py:1603` / `modules/oanda_bridge.py:113, 281–289, 307, 550, 649, 658, 668–690, 751–909` / `strategies/daytrade/weekend_gap_fade.py:79, 261–269, 351` / 契約: `weekend-gap-execution-contract-r1-packet-2026-09-10.md:110–115`
- data: `bt-results/wg_gap_drift-2026-09-10.json` (16 週末 × 3 ペア、2026-05-24〜09-06、価格のみ)
- 規律: [[lesson-asymmetric-agility-2026-04-25]] (停止 = R2 軽 / 増額・契約変更 = R1 重)、MEMORY `feedback_audit_past_verdicts_2026_08_05` (falsification 引用前の estimand 監査)
