# Trade Monitor Alerts: 2026-07-30

## Alert: 01:12 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=226, daytrade_eur:hedge_block=221

### 診断
## 原因分析

### 推測（ログ・数値から高確度で推定）

**① rnb_usdjpy:direction_filter=226（異常高値）**
- RNBストラテジーがUSD/JPYに対して継続的にシグナルを生成しているが、`direction_filter`（方向フィルター）に226回連続でブロックされている
- 現在のトレンド方向とRNBシグナル方向が長時間乖離している状態が続いていると推定
- 相場が一方向に強くトレンドしており、RNBの逆張り系シグナルが全て弾かれ続けている可能性が高い

**② daytrade_eur:hedge_block=221（異常高値）**
- EUR関連ペアで既存ポジションのヘッジ判定が221回発動し続けている
- 既存ポジション（おそらくEUR系の含み損または含み益保護中のポジション）が長時間決済されずに残存しており、新規シグナルを全てブロックし続けていると推定
- `daytrade_gbpusd:hedge_block=82`も同様の傾向であり、ヘッジブロック機構全体が過敏または既存ポジションが滞留していると推定

**③ ORDER_BAR_DEDUP頻発**
- `dual_sr_bounce USD_JPY SELL`が同一`bar_ts=2026-07-30T01:00:00`で繰り返しブロックされている
- 正常動作（重複発注防止）ではあるが、件数が多い場合はシグナル生成ループが過剰に回っている可能性

---

### 確認事項（実機で要検証）

**rnb_usdpjy:direction_filter について**
- [ ] 現在のUSD/JPY相場のトレンド方向とRNBの想定売買方向を確認
- [ ] direction_filterの判定ロジック（閾値・参照足）がデグレードしていないか確認
- [ ] フィルターがリセットされる条件を満たしているか（スタック状態でないか）

**daytrade_eur:hedge_block について**
- [ ] EUR系ペアに現在オープン中のポジションが存在するか確認
- [ ] そのポジションの損益・保有時間を確認（異常に長期保有になっていないか）
- [ ] `[HOLD] DT含み益保護`ログ（01:11:43）がEUR系にも発生していないか確認
- [ ] hedge_block判定に使われる参照ポジションの取得ロジックに不具合がないか確認

**全般**
- [ ] `main_loop_restarts=1` — 再起動の発生時

## Alert: 08:02 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=387, daytrade_eur:hedge_block=294

### 診断
## 原因分析

### 推測（ログ・カウントから高確度で推定）

**① `rnb_usdjpy:direction_filter=387`（最大異常）**
- RnB（レンジ&ブレイク）戦略のUSDJPYで、方向フィルターが継続的に発動し続けている
- MTFログ（`trend_up_weak`, D1=1, H4=1）と照合すると、上位足トレンドと逆方向のシグナルが大量発生していると推測
- フィルター自体は正常動作だが、**シグナル生成ロジックがトレンド方向と噛み合っていない**可能性が高い

**② `daytrade_eur:hedge_block=294`**
- EURペア（EUR/USD, EUR/JPY）でヘッジブロックが異常累積
- `daytrade_eurjpy:hedge_block=133`, `daytrade_gbpusd:hedge_block=124` と比較して顕著に高く、**EURポジションが長時間ヘッジ状態でスタックしている**と推測
- `[HOLD] DT含み益保護`ログが連続しており、既存BUYポジション(35a9b7f7)の保持によりSELL系シグナルが全域ブロックされている可能性

**③ `ORDER_BAR_DEDUP` の連続発火（GBP/USD, EUR/USD）**
- `bar_ts=07:45:00` に固定されたまま複数モードがブロックされており、**バー更新が止まっている、またはバータイムスタンプの参照が古いキャッシュを向いている**と推測
- `main_loop_restarts=1` があり、再起動後にバー状態の引き継ぎが不完全だった可能性

---

### 確認が必要な事項

- **rnb_usdjpy の direction_filter 条件のログ詳細**：何方向のシグナルが弾かれているか（BUY/SELL比率）
- **EURポジション(35a9b7f7以外)の保有状況**：ヘッジ判定に使われている反対側ポジションの有無・ロット
- **バータイムスタンプの更新状況**：07:45:00から08:00以降に正しく更新されているか、データフィード側の遅延・断絶の有無
- **main_loop_restarts=1 の発生時刻と原因**：再起動時にblock_countsがリセットされたか、状態が引き継がれたか
- **direction_filterの閾値設定**：直近のパラメータ変更履歴

---

## 対処方針

**即時確認・対応（優先度：高）

## Alert: 11:43 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=332

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=332 の異常値について

- **rnb_usdjpy（レンジブレイク戦略、USD/JPY）のdirection_filterが332回**と、他のblock_countsと比較して突出して高い
- 他の戦略の同種カウントが26〜191の範囲に収まる中、332は**約1.7〜12倍の乖離**であり統計的に異常
- direction_filterはシグナルの方向性（BUY/SELL）が内部フィルター条件と合致しない場合にブロックするロジックと推測される
- **原因仮説①**: USD/JPYが現在強いトレンド（または急激な値動き）にあり、rnb戦略が逆張りシグナルを連続生成している
- **原因仮説②**: rnb_usdjpyのdirection_filter判定に使用している上位時間軸（D1/H4等）のトレンド判定が固着・スタックしており、全シグナルを一方向にブロックし続けている
- **原因仮説③**: main_loop_restarts=1が発生しており、再起動時にrnb_usdjpyの内部状態（方向バイアス変数等）がリセットされずに引き継がれた可能性

---

### 【推測】全体的な傾向について

- `ORDER_BAR_DEDUP`の多発はbar_ts固着（同一バーへの重複シグナル）であり、**主ループの処理頻度がバー確定周期より速すぎる**正常動作の範囲内の可能性が高い
- `HOLD/含み益保護`ログが高頻度で出ており、**既存ポジションが長時間保有されている**状態

---

## 確認事項（実機で要検証）

1. **USD/JPYの現在の相場状況**
   - 直近数時間の価格推移・ボラティリティを確認し、rnb戦略のエントリー条件（レンジブレイク方向）と相場の実方向が乖離していないか確認

2. **direction_filterの判定ソース確認**
   - rnb_usdjpyがdirection_filterに使用しているトレンド指標（D1・H4のMA/ADX等）の現在値を取得し、値が正常に更新されているか確認

3. **main_loop_restart後の状態引き継ぎ確認**
   - 再起動タイミングとdirection_filterカウント急増のタイムラインを照合
   - rnb_usdjpyの内部状態変数（direction_bias等）が再起動前後で同一値を保持していないか確認

## Alert: 15:44 UTC

### 検出された問題
- 🔴 **含み損-32.2pip** (2ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 【推測】含み損 -32.2pip の原因候補

- **含み損ポジションはログに現れていない別ペア**の可能性が高い
  - ログに見えるのは `addfabc9`（USDJPY BUY、含み益中）のみ
  - -32.2pip を抱えている2ポジションは別モード・別通貨ペアと推測
  - `daytrade_1h_audjpy`（hedge_block:64）、`daytrade_eur`（hedge_block:58）、`daytrade_gbpusd`（hedge_block:93）あたりが候補
- **hedge_blockが大量発生**しているペアで、ヘッジ注文がブロックされ損失が拡大継続している可能性

### 【推測】ORDER_BAR_DEDUPの大量発生について

- `bar_ts=15:30:00` に固定されたシグナルが **30秒インターバルのループ内で繰り返し生成**されており、次の足（16:00:00）に更新されるまで無限にブロックされ続けている
- 本来は正常動作だが、**ループ周期とバー確定タイミングのズレ**が過剰なブロックログを生んでいる

### 【推測】main_loop_restarts:1 について

- 一度再起動が発生している。含み損ポジションが**再起動前後で状態引き継ぎが不完全**になっている可能性

---

## 確認事項（要実機確認）

1. **含み損2ポジションの通貨ペア・エントリー方向・SL設定の有無**を証券会社ダッシュボードで直接確認
2. `main_loop_restarts:1` の**再起動タイムスタンプとその直前ログ**を確認（ポジション情報のロスト有無）
3. 含み損ペアに対応するモードで **hedge_blockが連続発生していないか**を確認（ヘッジが機能していない可能性）
4. SL（ストップロス）が**ブローカー側に実際に発注されているか**を確認（未発注ならリスク無限大）

---

## 対処方針

- **即時対応（含み損について）**
  - ブローカーダッシュボードで2ポジションを直接確認し、SLが設定されていなければ手動で設定する
  - -32.2pip がシステム設計上のSL範囲内であれば現状維持、超過または設定なしなら手動クローズを検討

- **hedge_block多発ペアの精査**
  - `daytrade_gbpusd:hedge_block:93` は異

## Alert: 17:12 UTC

### 検出された問題
- 🔴 **含み損-74.9pip** (8ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 【推測】含み損-74.9pipの主因候補

- **USD/JPY・AUD/JPY方向の読み誤り**が複数ポジションに波及している可能性
- `bb_rsi_reversion`と`ema_trend_scalp`がR2シャドウデモートで連続ブロックされており、**逆張り系セルが実環境と乖離したシャドウ状態に陥っている**ことが新規ヘッジ・利確機会を逃している可能性
- `daytrade_eur:hedge_block=118`・`daytrade_gbpusd:hedge_block=111`の件数が突出しており、**ヘッジブロックが過剰発動してドローダウン拡大を放置している**可能性
- `rnb_usdjpy:direction_filter=125`が大量発火しており、**USD/JPYで方向フィルターが長期間片側をブロックし続けている**（トレンド方向と既存ポジション方向が逆転した状態で固着している可能性）

---

### 【確認事項】（実施前に要確認）

- **ポジション詳細の即時確認**
  - 8ポジションの通貨ペア・方向・建値・現在価格を取得し、損失の集中先を特定する
  - `daytrade_1h_audjpy`・`daytrade_audjpy`・`daytrade`(USDJPY)の各ポジション有無を確認

- **R2シャドウデモートの状態確認**
  - `bb_rsi_reversion × USD_JPY`および`ema_trend_scalp × USD_JPY/EUR_USD`がデモートされた時刻と、その直前のシグナル履歴を確認
  - シャドウセルが本番セルと乖離した原因（サーキットブレーカー発動か、スコア連続不良か）を確認

- **ヘッジブロック多発の原因確認**
  - `daytrade_eur`・`daytrade_gbpusd`のhedge_blockが100件超である理由（ヘッジ条件判定ロジックのバグか、意図的抑制か）を確認

- **direction_filterの閾値状態確認**
  - `rnb_usdjpy`のdirection_filterが125件ブロックした方向（BUY/SELL）と、現在のUSD/JPYトレンド方向を照合する

- **main_loop_restarts=1の確認**
  - 再起動タイミングとポジション取得・状態復元が正常に行われたかを確認

---

## 対処方針（優先順）

- **① 即時：8ポジションの手動確認と
