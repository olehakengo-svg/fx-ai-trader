# Trade Log: 2026-08-28 (evening — wiki-daily run #2)

**Source**: Render API (primary) — https://fx-ai-trader.onrender.com
**Generated**: wiki-daily-update scheduled task
**Runner clock (UTC)**: 2026-08-28T11:52Z
**Baseline**: this is the **second wiki-daily run of 2026-08-28**. `raw/trade-logs/2026-08-28.md` was written at 05:00:51Z and is preserved unmodified; every delta below is vs **that 05:00Z snapshot**, elapsed **~6h51m**. OANDA audit pulled at `limit=600` = **2026-08-17 15:47 → 2026-08-28 11:46 UTC**, `total` = **15,966 (+16)**.

---

## Headline: the realized book is bit-flat over 7 hours — but the block counters rolled again, and that settles what they measure

Nothing in the realized book moved: N, PnL, WR, EV, Wilson, DD, edge, Kelly and DSR are **byte-identical** to 05:00Z. That is unremarkable for a 7-hour intraday window and is **not** a repeat of the 08-21→08-26 freeze — the pipeline is demonstrably live (16 new audit rows, `engine_tick_age_sec` 2.8s, `last_candidate_row_age_sec` 6.1s, 24 modes running).

The one substantive gain is a **resolved ambiguity**, not a new number:

> The 08-26 and 05:00Z logs both had to write "block counters are NOT comparable across pulls — the window rolled/reset" and could not say which. **This pull settles it.** Total blocks fell **17,718 → 3,871 (−78%)** in 6h51m while `main_loop_restarts` stayed at **1** and the engine never stopped. A process restart cannot explain it, and neither can a real decline in blocking — because the *proportions are preserved almost exactly*:

| Blocker | 05:00Z | 11:52Z | share 05:00Z → 11:52Z |
|---|---:|---:|---|
| `hedge_block` | 6,265 | **1,310** | 35.4% → **33.8%** |
| `r2_shadow_demoted_cell` | 4,135 | **988** | 23.3% → **25.5%** |
| `order_bar_dedup` | 3,637 | **868** | 20.5% → **22.4%** |
| `direction_filter` | 2,624 | **630** | 14.8% → **16.3%** |
| **total** | **17,718** | **3,871** | — |

⇒ **`/api/demo/block-counts` is a rolling/windowed counter, not a cumulative-since-boot counter.** Absolute block totals must never be diffed across snapshots; only shares within a single pull are meaningful. This should be treated as a standing rule for this log — it retroactively explains the 08-26 "62,472 → 57,029 ticks" oddity without needing a reset event.

### `rnb_usdjpy:direction_filter` — the ratio is now exactly 1.0000
| Pull | blocks | ticks | ratio |
|---|---:|---:|---|
| 08-26 | 2,624 | 2,622 | 1.0008 |
| 08-28 05:00Z | 2,624 | 2,622 | 1.0008 |
| **08-28 11:52Z** | **630** | **630** | **1.0000** |

**Third consecutive confirmation, and the first exact one.** On an independently re-windowed sample, *every single `rnb_usdjpy` tick was blocked by `direction_filter`* — 630 of 630. This is no longer suggestive: a selective filter cannot reject 100% of a fresh window. Consistent with the known `compute_rnb_signal` WAIT-path `entry: 0` bug. 🔴 **The code read remains not done — top open follow-up, unchanged for 3 runs.**

### `hedge_block` shadow→live suppression — independently re-confirmed
The 05:00Z finding reproduces on a fresh sample without re-reading code: `hedge_block` is still the **#1 blocker (1,310 = 33.8%)**, and `/api/demo/status` `open_trades` is **5 rows, every one `is_shadow: 1`** (2× `sr_break_retest` AUD_JPY, `session_time_bias` EUR_USD, `session_time_bias` GBP_USD, `streak_reversal` USD_JPY) while the OANDA heartbeat reports `open_trade_count: 0` / `margin_used: ¥0`. The book turned over (6 shadow positions → 5, different strategies) and the property held. Filed for user decision as of 05:00Z; **no code touched.**

---

## Portfolio Summary (post-cutoff 2026-04-08〜, `is_shadow=false`, `exclude_xau=true`)
| Metric | Value | vs 2026-08-28 05:00Z |
|--------|-------|---------------|
| Live N | **579** | flat |
| W / L / BE | 252W / 292L / 35BE | flat |
| Win Rate | 43.5% | flat |
| Decided WR | 46.3% | flat |
| EV | −1.06 pip/trade | flat |
| Total PnL | **−611.1 pip** | flat |
| Wilson Lower / BF Lower | 42.2% / 39.4% | flat |
| Avg R | 0.12 | flat |
| Shadow Count | **12,557** | ⚠️ **−1** |
| `oanda_audit` total | **15,966** | **+16** |

⚠️ **`shadow_count` decremented 12,558 → 12,557.** A monotonic append-only counter should not fall. The magnitude is trivial and it does not touch the live book, but the *direction* is wrong and this is the first time it has been observed in this log. Recorded, **not diagnosed** — one observation is not enough to call it a defect rather than a boundary/rounding effect in the aggregation query. Re-check on the next run: a second decrement makes it real.

## Risk State — unchanged in every field
| Metric | Value | vs 05:00Z |
|--------|-------|---------------|
| Drawdown (pip basis) | 🔴 **1198.8 pip** (eq −$1181.9 vs peak +$16.9) | flat (119.9% legacy $1000 basis) |
| Drawdown (`dd_pct` field) | 0.097 = 9.7% (JPY NAV ledger) | flat |
| JPY NAV ledger | base ¥359,109 / peak ¥359,288.47 / current ¥324,450.50 / DD ¥34,837.97 | flat |
| Defensive Mode | Active (lot 0.2×) | flat |
| Ruin Probability (MC) | 0.0% ✅ | flat |
| Overall Edge (Kelly) | **−20.06%** | flat |
| Kelly Fraction | 0.0 | flat |
| Risk-window WR / odds | 43.75% / 0.8273 | flat |
| DSR (overall) | 0.0, haircut 100%, Sharpe −0.0927, n_trials 4 | flat |
| 30d attribution | gross = net **−58.4** (n=16), friction 71.3 (4.46/trade) | flat |
| 30d by instrument | AUD_JPY −102.5 (n=3) / EUR_GBP −9.8 (n=3) / USD_JPY +53.9 (n=10) | flat |
| VaR95 / VaR99 / CVaR95 | 53.38 / 109.23 / 123.2 | flat |

The 30d `effective_date_from` did **not** roll (still 2026-07-29T11:50:31, n=16), so unlike 08-23 and 05:00Z there is no window arithmetic to net out this time. The read given at 05:00Z stands: **edge −20.06% is still deeply negative, Kelly is 0.0, DSR is 0.0 at a 100% haircut.**

⚠️ `attribution` still reports **`gross_pnl` = `net_pnl` = −58.4 while `friction` = 71.3** — friction computed but not subtracted. Pre-existing across ≥3 snapshots, unresolved, not diagnosed here.

---

## OANDA Audit
Pulled at **`limit=600`**, window **2026-08-17 15:47 → 2026-08-28 11:46 UTC**, `total` **15,966**.

| Bucket (full 600) | Count |
|---|---:|
| `skipped` (all `shadow_tracking`) | 552 |
| `blocked` (all `agg_kelly<0`) | 34 |
| `sent` | 7 |
| `filled` | 7 |

**New-since-05:00Z slice: 16 rows** (05:01:49Z → 11:46:50Z) — **16 `skipped` / 0 `blocked` / 0 `sent` / 0 `filled`**.
Instruments: GBP_USD 6 / AUD_JPY 3 / EUR_JPY 2 / USD_JPY 2 / EUR_USD 2 / EUR_GBP 1.
Strategies: `session_time_bias` 5 / `sr_break_retest` 3 / `sr_anti_hunt_bounce` 2 / `dt_sr_channel_reversal` 2 / `streak_reversal` 2 / `trendline_sweep` 1 / `gbp_deep_pullback` 1.

🟢 **Zero `agg_kelly` blocks in this slice** — the first slice in this log with none. Not a gate change: with 16 candidates and no live-eligible mode reaching the sizing stage, the gate simply had nothing to reject. Do not read as the aggregate-Kelly gate standing down; it blocked 7 in the immediately preceding window at an unchanged −0.2006 edge.

### No new LIVE fills
**Last live fill remains #9 — `price_shock_rev_aud_jpy_h1_long` AUD_JPY, oanda #709537, 08-26 14:59:26Z.** Cumulative **9 fills / 9 real trade ids / 0 false-sent** — the 07-02 accept/reject contract still holds. `entry_type` twin-meaning applies as always (`sent` = strategy, `filled` = MODE — `reference_oanda_audit_twin_meaning`).

⚠️ `limit=30` would have covered only ~the last 3 audit rows and reported **0 live fills** — **6th consecutive confirmation** of `feedback_audit_limit30_hides_live_fills`. The 600-row pull was required to see fill #9 at all.

### Zero-unit emissions — persisting, still SR-concentrated
**98 of 600 rows (16.3%)** carry `units: 0`; in the fresh 16-row slice, **4 of 16 (25%)** — `sr_anti_hunt_bounce` ×2, `sr_break_retest` ×2. All are `bridge_status=skipped` ⇒ **zero live exposure**, but shadow is the estimator under shadow-first, and the SR-family concentration continues to overlap the known `sr_anti_hunt_bounce` metadata corruption. Unchanged in character since 08-23; **still undiagnosed.**

---

## Engine health (`/api/demo/status`)
`running: true` / `main_loop_alive: true` / `watchdog_alive: true` / `sltp_checker_active: true` / `emergency_killed: false`
`main_loop_restarts`: **1** (unchanged since 08-26 — no restart in the counter-roll window)
`engine_tick_age_sec` 2.8 / 24 modes running / stalest `daytrade_1h_euraud` at 52.6s / `engine_tick_status: ok`
`last_candidate_row_at` 2026-08-28 11:52:40 (age 6.1s) / `last_trade_row_at` 11:46:50 (age 356s) — both `ok`
OANDA heartbeat: CONNECTED, 187ms, NAV **¥278,345.48**, `open_trade_count` 0, `margin_used` ¥0, no recent errors.

⚠️ **JPY NAV ledger drift persists: ¥46,105.02** (ledger ¥324,450.50 vs broker NAV ¥278,345.48) — unchanged in substance from 05:00Z. Sizing is unaffected (0.20× tier already saturated); reporting continues to understate broker-measured drawdown.

## Learning API
- **No new adjustment** — latest is still **id=93 (2026-08-19 14:25)**, now the **5th consecutive snapshot** at that value.
- `current_params.entry_type_blacklist` is still **`[]`** ⇒ the **blacklist write remains a no-op**; the same `sr_channel_reversal` adjustment (WR 25.0% / EV −0.98 / n=190) will keep re-firing indefinitely. Unresolved since 06-30. Of 30 journaled adjustments, **12 are byte-identical re-affirms of this one exclusion**.
- `daytrade` EV **−2.60** (n=94, ready) / `scalp` EV **−0.15** (n=388, ready) / `swing` `ready=false` (n=0, min 10) — **bit-identical to 05:00Z and 08-26.**
- `current_params`: confidence_threshold 30 / max_open_trades 8 / max_consecutive_losses 3 / learn_every_n 10 / `daily_loss_limit_pips` −99999 / `max_drawdown_pips` −99999 (**both limits effectively disabled**).

## Strategy pages
**No strategy page updated.** No cell gained or lost a closed trade in this window, so there is no new data for any page to absorb. `usdjpy_carry_dip_accumulator` (N=9 / WR 55.6% / **+84.0**, the only materially positive cell) and `price_shock_rev_aud_jpy_h1_long` (N=3 / WR 66.7% / **−102.5**) are unchanged from 05:00Z.

**No tier change executed.** Nothing in this window bears on any promote/demote threshold.

## Lint
- `tier_integrity_check --check` → **PASS** (exit 0). 2 warnings, both pre-existing: `hull_donchian_fade` QUICK_HARVEST_EXEMPT not in ELITE/PAIR_PROMOTED; no strategy file for `ob_retest`.
  - ⚠️ Its 14 "legacy dead inline, no production firing in 30+ days" INFO lines still include **`ihs_neckbreak`**, which has a live block counter in **this** pull (`daytrade_eurjpy:unknown_type:ihs_neckbreak` = 1). The checker reads local state, not the Render API. **5th consecutive run reporting this contradiction.**
- `sync_kb_index --check` → **PASS, "index.md is in sync"** (exit 0). First clean pass without needing `--write` — the 05:00Z run's date-stamp write is holding.
- **WR/PnL cross-page consistency**: ✅ consistent. `index.md`, `2026-08-28.md` and this file all carry N=579 / WR 43.5% / PnL −611.1 / DD 1198.8.
- **Broken wikilinks** (path-aware resolver, suffix-matching): `wiki/index.md` **7** — the identical set to 08-23/08-26/08-28-05:00Z (5 auto-generated portfolio-block refs `mqe-gbpusd-fix` / `vsg-jpy-reversal` / `ma-regime-switch` / `ma-trend-perfect` / `ob-retest`, plus trade-log refs `2026-04-29` / `2026-04-27`); `wiki/strategies/` **1** non-MEMORY (`xs-momentum-rsi-tv-phase2-2026-05-13`). KB-wide 125, of which 55 are MEMORY-style `project_*`/`feedback_*`/`reference_*` refs resolving outside the KB ⇒ **70 in-KB**. ⚠️ The KB-wide figure is **not comparable to the 76/29/47 reported at 05:00Z** — a different (stricter) resolver was used; the two stable headline figures (index.md 7, strategies 1) match exactly. **0 new broken links from this run.** `[[2026-08-28]]` and `[[2026-08-28-evening]]` both resolve ✅.
- **Staleness**: 🟢 audit current to 11:46:50Z (6 min at pull), candidate rows 6.1s old, engine ticking. 🔴 **`/api/oanda/equity` now 18 days stale** — last point still **2026-08-10 07:20:34Z**, `count` still **928**, while 9 live fills have closed. Unchanged from 05:00Z, worsening by the day. 🔴 Learner 9 days stale (id=93, 08-19).

## Still-open (carried, unchanged from 05:00Z unless noted)
- 🔴 **08-21→08-26 outage cause never established** — bounded to 105.5h but unexplained; recurrence would blind the estimator again. Needs a Render-side log read for that window.
- 🔴 **`rnb_usdjpy:direction_filter` blocks = 100% of ticks (630/630, exact)** — code read outstanding, now 3 confirmations deep.
- 🔴 **`hedge_block` shadow→live suppression** — a decision (R2/H2 rationale), not a bug; backwards under shadow-first; largest single source of suppression. Awaiting user call.
- 🔴 **carry-dip declared 150p SL vs observed 18.8p substitution** — still the highest-value open item; 9 live fills have closed on the only positive cell in the book.
- 🔴 **Price-Shock family demote decision** — AUD_JPY N=3 / −102.5 (BT EV +32.25), EUR_GBP N=3 / −9.8 (BT EV +55.81); both invert BT sign in live.
- 🔴 `/api/oanda/equity` 18d stale.
- ⚠️ 🆕 **`shadow_count` decremented by 1** — first observation, re-check next run.
- ⚠️ 🆕 **`/api/demo/block-counts` is windowed, not cumulative** — never diff its absolute totals across pulls (resolved this run; recorded as a standing rule).
- JPY NAV ledger drift ¥46,105.02 / learner blacklist no-op / zero-unit emissions (16.3% of pull) / `attribution` friction not subtracted / `API_AUTH_TOKEN` watchdog gap / `sr_anti_hunt_bounce` metadata corruption.
