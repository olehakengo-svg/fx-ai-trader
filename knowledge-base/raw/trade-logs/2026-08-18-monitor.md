# Trade Monitor Alerts: 2026-08-18

## Alert: 04:22 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=326

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=326`** は他のblock_countsと比較して**桁違いに大きい**（次点の`daytrade_audjpy:hedge_block=186`の約1.75倍、多くは一桁〜三桁前半）
- `direction_filter`は方向性フィルター（トレンド方向と逆シグナルを弾くロジック）と推定されるため、**USDJPYで一方向のシグナルが大量発生しているが、すべてフィルターに弾かれている**状態と考えられる
- つまり「シグナルエンジンは頻繁に発火しているが、方向判定で全滅している」というロジック上のデッドロック的状態
- main_loop_restarts=1が発生しており、**過去に一度異常終了→再起動**している点も関連している可能性あり

### 推測（中確度）

- USDJPYが現在レンジ相場またはトレンド転換点にあり、短期シグナルと方向フィルターの**参照足・判定ロジックが乖離**している
- direction_filterの参照するトレンド判定値（MA・ADX等）が**固着またはキャッシュ更新されていない**可能性
- rnb（レンジブレイク系と推定）モードが再起動後に**状態リセットされず、フィルター条件が不整合**のまま継続している可能性

---

## 確認事項

- [ ] `rnb_usdjpy`のdirection_filter判定に使用しているトレンド指標（MA、ADX等）の**現在値と更新タイムスタンプ**を確認
- [ ] main_loop_restarts=1の**再起動発生時刻とその直前のエラーログ**を確認（再起動後に状態が正しく復元されたか）
- [ ] direction_filterのカウントが**いつ頃から急増したか**（再起動前後か、それ以前から継続しているか）
- [ ] `rnb_usdjpy`モードの`last_signal`内容（WAITのみ連続しているか、シグナル自体は出ているか）
- [ ] USDJPYの現在の価格帯がrnbモードの**ブレイク判定閾値付近で振動**していないか（レンジ境界付近でのチャタリング）

---

## 対処方針

- **緊急度：中**（emergency_killed=false、他モードは正常稼働中のため即時停止は不要）

1. direction_filterの参照指標キャッシュを**強制リフレッシュ**し、値が正常に

## Alert: 08:19 UTC

### 検出された問題
- 🔴 **含み損-76.1pip** (6ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 【推測】含み損 -76.1pip / 6ポジションの原因

- **daytrade_gbpjpy の hedge_block が61件**と突出しており、GBP/JPY でヘッジが過剰にブロックされた結果、損失ポジションが逃げ場を失って塩漬けになっている可能性が高い
- **rnb_usdjpy の direction_filter が59件**と多く、USD/JPY でのエントリーが方向フィルターに阻まれ、逆張りポジションが残存したまま損失拡大している可能性
- `scalp_5m_eur:unknown_type:engulfing_bb_lvn_london_ny` が1件検出されており、**未定義シグナルタイプによる想定外エントリー**が混入している可能性

### 【確認事項】

- **6ポジションの内訳（通貨ペア・方向・保有時間・個別損益）を確認する**
  - GBP/JPY と USD/JPY が主因か、他通貨ペアが含まれるかを切り分ける
- **main_loop_restarts が1回**発生しているため、再起動前後でポジション管理ステートの引き継ぎが正常に行われたか確認する
- `unknown_type:engulfing_bb_lvn_london_ny` のポジションが6件の中に含まれていないか確認する
- `daytrade_gbpjpy:hedge_block` 61件が正常なリスク管理によるブロックか、バグによる誤ブロックかをロジック側で確認する

---

## 対処方針

### 即時対応（手動介入判断）

- 6ポジションの損益・保有時間を確認し、**損切りラインを超過しているものは手動クローズを検討**する
- `unknown_type` で建てられたポジションが存在する場合は**優先的に手動クローズ**し、不正エントリー扱いとする
- GBP/JPY の含み損ポジションについて、hedge_block の解除可否を判断する

### 短期対応（設定・ロジック見直し）

- `daytrade_gbpjpy:hedge_block` の閾値・条件が過剰に厳しくなっていないかレビューする
- `rnb_usdjpy:direction_filter` が長時間ブロックし続けている場合、フィルター条件のタイムアウト設定を見直す
- `unknown_type:engulfing_bb_lvn_london_ny` を**既知タイプとして登録するか、未定義シグナルはエントリー拒否**するよう修正する

### 中期対応（再発防止）

- main

## Alert: 12:18 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=378, daytrade_eurjpy:hedge_block=356, daytrade_gbpusd:hedge_block=304, scalp:r2_shadow_demoted_cell=286, daytrade_gbpjpy:hedge_block=220

### 診断
## 原因分析

### 【推測】高カウントの主因

**1. `rnb_usdjpy:direction_filter=378`（最大値）**
- RNBストラテジーがUSDJPYの方向フィルターに大量に弾かれている
- トレンド方向とシグナル方向が継続的に逆転している状態が長時間続いている可能性
- あるいはdirection_filterの判定ロジックが過敏になっているか、参照する上位足トレンド判定がスタックしている可能性

**2. `daytrade_eurjpy/gbpusd/gbpjpy:hedge_block` （220〜356）**
- 複数通貨ペアで同時多発しており、単一ペアの問題ではなく共通原因を示唆
- ヘッジ判定の基準となるポジション状態または残高状態が閾値付近で振動（チャタリング）している可能性
- またはヘッジ解除条件が満たされないまま長時間ブロックが継続している可能性

**3. `scalp:r2_shadow_demoted_cell=286` / `scalp_5m系`も高値**
- shadow trackingセルが大量にdemote状態に落ちており、スキャルp系全体でエントリー不能に近い状態
- R2評価が全般的に低迷しているか、shadow demoteのリセット条件が機能していない可能性

---

### 【確認済み事実】ログから読み取れること

- `ORDER_BAR_DEDUP`：同一バー内で同一シグナルが数秒ごとに連続発火 → **シグナル生成ループの重複実行**が発生している
- `SCORE_GATE`：`eurgbp_daily_mr`がscoreミスアライン（0.45〜0.46）で繰り返し到達 → スコアが閾値直下で毎回再試行されている
- `HOLD DT含み益保護`：複数オーダーが利益保護HOLDに入っており、SIGNAL_REVERSEが無効化され続けている → **既存ポジションがクローズされずブロックが積み上がる構造**になっている
- `main_loop_restarts=1`：1回再起動があり、カウンターがリセットされていない可能性

---

## 対処方針

### 緊急確認事項（今すぐ）

- **hedge_blockの原因となっているポジションの実際の状態確認**
  - 当該通貨ペア（EURJPY/GBPUSD/GBPJPY）でヘッジ条件をトリガーしているポジションが実際にブローカー側で存在するか照合
  - ゾン
