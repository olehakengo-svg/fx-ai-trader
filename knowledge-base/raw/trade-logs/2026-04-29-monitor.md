# Trade Monitor Alerts: 2026-04-29

## Alert: 01:46 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=274

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter` のカウント274は他フィルタの5〜10倍超**であり、明らかに異常値。これは `direction_filter` が**継続的かつ高頻度でシグナルを弾き続けている**状態を示す
- ログを見ると `fib_reversal BUY` と `bb_rsi_reversion BUY` が**数秒間隔で大量に繰り返し生成**されており、シグナルループが発生している可能性が高い（通常のインターバル制御が機能していない）
- `WIDE_RANGE` レジームが検出されているにもかかわらず、`direction_filter` が **BUYシグナルを全拒否**し続けている → レジーム判定と方向フィルタの**矛盾・競合**が継続中
- `daytrade` の `last_signal` が `WAIT` / confidence 23 と低く、上位足（D1=0）が下向きのため、direction_filterがロング方向を一律ブロックしていると推測

### 推測（中確度）

- `main_loop_restarts: 1` があり、**再起動後にフィルタカウンタがリセットされていない**か、再起動のトリガー自体がこのループ由来の可能性
- シグナル生成ロジックが `interval` の待機をスキップして**MTFモニターや別スレッドから多重呼び出し**されている恐れがある

---

## 確認事項（要調査）

- [ ] `direction_filter` の判定ロジックで参照している上位足（D1 / H4）のトレンド方向と現在値を確認
- [ ] `rnb_usdjpy` モードのシグナル生成頻度制限（`recent_emit` 相当のクールダウン）が実装されているか確認
- [ ] `fib_reversal` / `bb_rsi_reversion` が数秒間隔で再評価されるトリガーが何か（タイマー or イベント駆動か）を特定
- [ ] `block_counts` がプロセス起動からの**累積カウントか、直近ウィンドウのカウントか**を確認（累積なら相対比較が必要）
- [ ] `main_loop_restarts` の発生時刻とブロック急増のタイミングが一致するか確認

---

## 対処方針

- **即時対応**
  - `rnb_usdjpy` モードを一時的に `running: false` に設定し、シグナルループを停止して安定確認
  - D1トレンドが下向きである間は `direction_filter` の動作が**正常である可能性**

## Alert: 07:55 UTC

### 検出された問題
- 💰 **含み益+80.6pip** (12ポジション, LIVE=1 Shadow=11) | Top: vsg_jpy_reversal×EUR_JPY +18.0pip | vsg_jpy_reversal×EUR_JPY +18.0pip | vsg_jpy_reversal×EUR_JPY +18.0pip
- ⚠️ **Shadow含み益+89.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】主因

**Shadow含み益+89.6pipがLIVE昇格していない根本原因は「PAIR_PROMOTEDリストにShadow稼働中の通貨ペアが含まれていない」こと**

---

### 【推測】副次要因

- **vsg_jpy_reversalがEUR_JPYで同一ポジション(+18.0pip)を3件重複計上している可能性**
  - Top3が完全に同一エントリー名・同一pipsのため、重複カウントバグ or 同一シグナルの多重発火が疑われる
- **dt_fib_reversalがGBP_USDで連続してShadow強制されている**
  - `score=-1.03 misaligned with BUY` + `TREND_BEAR block` が繰り返し発火しており、スコアとトレンド判定が乖離した状態で同じシグナルを連打している
- **EUR_USDでSQUEEZE検知によるMRブロックが多発**
  - `bb_width_pct=2.0` が閾値ギリギリで断続的にブロック→非ブロックを繰り返している可能性
- **main_loop_restarts=1** が記録されており、昇格判定ロジックがリスタート前後で状態を引き継げていない可能性

---

### 【確認事項】（推測と分離）

| # | 確認項目 | 確認方法 |
|---|----------|----------|
| 1 | PAIR_PROMOTEDに対象ペア（EUR_JPY等）が登録されているか | 設定ファイル・環境変数を直接参照 |
| 2 | vsg_jpy_reversal×EUR_JPY 3件が本当に別ポジションか、同一UUIDか | ポジションIDを照合 |
| 3 | Shadow昇格の閾値条件（pip数・継続時間・勝率等）を現在の89.6pipが満たしているか | 昇格ロジックの条件定義を確認 |
| 4 | main_loop_restart時にShadow昇格候補の状態が永続化されているか | 状態保存ファイル・DBの内容を確認 |
| 5 | dt_fib_reversalのスコア算出元データ（トレンド判定・MTF）に古いキャッシュが残っていないか | キャッシュTTLとタイムスタンプを確認 |
| 6 | bb_width_pct=2.0がSQUEEZE閾値と一致しており誤検知ではないか | 閾値設定値と現在の市場ボラティリティを確認 |

---

##

## Alert: 09:56 UTC

### 検出された問題
- 💰 **含み益+32.8pip** (10ポジション, LIVE=1 Shadow=9) | Top: dt_fib_reversal×EUR_JPY +12.4pip | wick_imbalance_reversion×GBP_JPY +11.1pip | ema200_trend_reversal×EUR_JPY +6.2pip
- ⚠️ **Shadow含み益+39.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】根本原因

**Shadow含み益+39.9pip（9ポジション）がOANDAに送信されていない主因**は、複数のブロック機構が連鎖的に発動し、プロモーション対象シグナルが実枠に昇格できていないこと。

---

### 推測（ログ・ステータスから読み取れるもの）

- **max_openブロックが全通貨ペアで飽和状態**
  - daytrade系・scalp系ともに`max_open`カウントが20超で上限に張り付いており、新規LIVE枠が物理的に空いていない
  - LIVE=1に対しShadow=9という非対称が、枠不足による昇格停止を示している

- **`same_price_0pip / 5pip`ブロックが大量発生**
  - EUR_USD・GBP_USD・GBP_JPYで同値・近接価格の重複シグナルが弾かれ続けており、同一レンジ内での往復シグナルが大量生成されているとみられる

- **`recent_emit`ブロックも多数**
  - 短時間に同一モードから連続シグナルが出ており、クールダウン制限が連続的にかかっている

- **TREND_BULLブロック（conf<65）によるShadow降格**
  - `bb_squeeze_breakout` / `ema_trend_scalp`がconf=33〜59で閾値未満のまま繰り返し発火。EV=-0.776と負値のままShadow蓄積している

- **SQUEEZE/WIDE_RANGEレジーム干渉**
  - GBP_USDはSQUEEZE検出でMRブロック、EUR_USDはWIDE_RANGEでスコア補正が入っており、レジーム判定が不安定な状態

- **`daytrade_eurgbp:unknown_type:ihs_neckbreak`が31件**
  - シグナルタイプが未定義のまま大量発生しており、処理分岐でロストしている可能性が高い

---

### 確認事項（要実機確認）

- **OANDA側の現在オープン枠残数**：max_open上限がシステム設定値と一致しているか、OANDA APIの実残数と乖離していないか
- **PAIR_PROMOTEDへの追加可否判断**：Shadow上位3ペア（EUR_JPY / GBP_JPY）の勝率・EV実績をバックテストログで確認
- **`ihs_neckbreak`のunknown_type**：シグナルタイプ定義ファイルへの登録漏れか、タイポによる不一致か

## Alert: 11:24 UTC

### 検出された問題
- 💰 **含み益+80.6pip** (10ポジション, LIVE=0 Shadow=10) | Top: wick_imbalance_reversion×GBP_JPY +22.9pip | wick_imbalance_reversion×GBP_JPY +15.4pip | ema200_trend_reversal×EUR_JPY +14.7pip
- ⚠️ **Shadow含み益+81.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【確定事実】

- **全10ポジションがShadowモード**で動作しており、LIVE=0のためOANDAへの実注文が一切送信されていない
- `[SKIP] wick_imbalance_reversion — Reason: shadow_tracking` のログが明示的に送信拒否を記録している
- GBP_JPY・EUR_JPY等の対象ペアが `PAIR_PROMOTED`（Shadowから昇格済み）リストに含まれていないことが根本原因
- `DT RANGE bypass: *** (TF in RANGE → shadow)` が連続発生しており、レンジ判定でShadow強制バイパスが多発している
- `[SL_CAP]` によりSL距離が 0.377→0.200 に強制圧縮されている（リスク管理ルールが発動済み）

### 【推測・要確認事項】

**推測：**
- GBP_JPYは `daytrade_gbpjpy:same_price_5pip` でブロック43件・`recent_emit` 42件と多く、過去の同値・連続発火抑制が積み重なりLIVE昇格の評価機会を逃している可能性がある
- `main_loop_restarts: 1` により再起動時にShadow→LIVE昇格状態がリセットされた可能性がある
- `rnb_usdjpy:direction_filter` が152件と突出しており、USDJPYのRnBモードが方向フィルターで完全停止している可能性がある

**要確認事項：**
- `PAIR_PROMOTED` リストの現在の設定値（GBP_JPY・EUR_JPYが含まれているか）
- Shadow昇格条件（勝率・pip閾値）の充足状況
- `main_loop_restarts: 1` の再起動タイミングとその前後でのShadow評価カウンタのリセット有無
- SL_CAP適用後のRR比がLIVE送信条件を下回っていないか（RR1:2.6は基準値内に見えるが設定確認）

---

## 対処方針

### 即時対応

- **GBP_JPY・EUR_JPY を `PAIR_PROMOTED` に手動追加**することを検討
  - 含み益+80pip超・複数シグナル一致・MTF確認済み（d1=1, h4=1）の状況は昇格根拠として十分
  - ただしSL_CAPが発動した直後のエントリー（cd3057c2）は実勢SL距離が圧縮されているため、**このポジション単体はLIVE化対象から除外**することを推奨

### 短期対応

- `PAIR_PROMOTED` への

## Alert: 13:16 UTC

### 検出された問題
- 💰 **含み益+146.3pip** (12ポジション, LIVE=0 Shadow=12) | Top: wick_imbalance_reversion×GBP_JPY +33.9pip | wick_imbalance_reversion×GBP_JPY +26.4pip | wick_imbalance_reversion×GBP_JPY +21.2pip
- ⚠️ **Shadow含み益+149.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】根本原因

**GBP_JPY が `PAIR_PROMOTED`（LIVE昇格リスト）に含まれていないため、`wick_imbalance_reversion×GBP_JPY` のシグナルがShadowモードに留まり、OANDAへ発注されていない。**

---

### 【推測】副次的な構造問題

- `daytrade_gbpjpy:recent_emit` (42件) および `same_price_5pip` (41件) のブロックが大量発生しており、GBP_JPY向けフィルターが過剰に機能している可能性がある
- `rnb_usdjpy:direction_filter` が137件と突出して多く、USD_JPY系の方向性フィルターが非常に厳しい状態にある
- USD_JPY の xs_momentum シグナルが `EV=-2.486` でShadowブロックされており、H13時間帯フィルター（`H13 USD_JPY block`）が継続発動中
- 12ポジション全てがShadowのみ（LIVE=0）であり、**システム全体としてLIVE発注がゼロ**という異常な状態

---

### 【確認事項】（手を動かす前に必ず確認）

1. **`PAIR_PROMOTED` の現在の設定値** — GBP_JPYが明示的に除外・未登録になっていないか
2. **Shadowモード昇格ロジックの条件** — 勝率・EV・連続成績など、GBP_JPYが昇格条件を満たしているかどうか
3. **LIVE=0の原因がPAIR_PROMOTEDのみか、別のkill/block条件が重なっていないか** — `emergency_killed=false` は確認済だが、ペアレベルの個別抑制がないか
4. **`daytrade_gbpjpy:same_price_5pip` の閾値設定** — GBP_JPYの値動き特性に対して5pip閾値が広すぎてブロック過多になっていないか
5. **`main_loop_restarts=1` の再起動タイミング** — 再起動前後でShadow/LIVE状態のリセットや設定読み込みに問題がないか

---

### 【対処方針】（優先順）

- **即時対応**
  - GBP_JPYを`PAIR_PROMOTED`に追加することの妥当性をShadow成績（+149.6pip, 勝率・EV）で判断する
  - 追加する場合は、既存の12ポジションの扱い（Shadowのまま保持 or 強制クローズ後に再エントリー）を事前に決定する

- **短期対応

## Alert: 15:50 UTC

### 検出された問題
- 🔴 **含み損-79428652.3pip** (56ポジション) → ポジション確認推奨
- ⚠️ **block_counts異常** — scalp:max_open=262, rnb_usdjpy:direction_filter=245, scalp_eur:max_open=243

### 診断
## 原因分析

### 🔴 含み損 -79,428,652.3pip（56ポジション）

#### 【推測】
- pip単位の数値が異常に大きい（通常のFXでは考えられない桁）ため、**pip計算ロジックのバグ**が最有力
  - 通貨ペアの小数点桁数（decimal precision）を誤認識している可能性（例：JPY建てを誤って5桁で割るなど）
  - ポジション集計時に**複数ポジションを掛け算・二重集計**している可能性
- 56ポジションという数自体は異常ではないが、ログに「含み益」が正常に表示されているポジションも存在しており、**表示計算のみのバグで実際の損益は正常**である可能性が高い

#### 【確認事項】
- ブローカーAPI（OANDA等）上の実際のポジション含み損益を直接確認し、システム表示値と乖離があるか照合する
- pip換算ロジック内で使用している乗数・除数の定義を確認する
- 集計対象ポジション数（56件）の内訳が正しいか確認する

---

### ⚠️ block_counts異常（scalp:262, rnb_usdjpy:245, scalp_eur:243）

#### 【推測】
- **`scalp` / `scalp_eur` の `max_open` ブロック多発**
  - スキャルピング系モードで既存ポジション数が上限に張り付いており、新規エントリーがほぼ全量ブロックされている状態
  - 上記の含み損ポジション56件との関連が疑われる：**損切りされずにポジションが滞留し、枠を圧迫**している可能性
- **`rnb_usdjpy:direction_filter` のブロック多発（245回）**
  - USD/JPYが一方向（おそらく下落）に傾いており、direction_filterが逆張りエントリーを継続的に拒否している状態
  - フィルター自体は正常動作している可能性が高いが、相場環境との乖離によりシステムが膠着している

#### 【確認事項】
- 現在の未決済ポジション数が各モードの `max_open` 設定値に達しているか確認する
- `rnb_usdjpy` の方向判定に使われているフィルターの基準値と、現在の相場トレンドの整合性を確認する
- ポジションが長期間クローズされずに滞留しているものがないか確認する（特にスキャルピング系）

---

## 対処方針

### 即時対応
-
