# Trade Monitor Alerts: 2026-06-23

## Alert: 01:56 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=264

### 診断
## 原因分析

### 【推測】

- `rnb_usdjpy:direction_filter` が264カウントに達しているのは、**USDJPYの方向性フィルター（トレンド判定・バイアス条件）が長時間・高頻度でシグナルを棄却し続けている**状態を示す
- rnbモードのロジックが「現在の相場方向」と「エントリー方向」を一致させられない状況が継続している可能性が高い
- 時刻が01:53〜01:56（東京早朝・アジア時間初動）であり、**方向性が定まらない横ばい・レンジ相場**に入っている可能性
- `daytrade` の last_signal が `confidence: 21 / WAIT` であることとも整合しており、**USDJPYペア全体でシグナル生成が困難な相場環境**にある可能性が高い

### 【確認事項と推測の区別】

| 区分 | 内容 |
|------|------|
| **推測** | direction_filterが方向一致を取れない相場環境（レンジ・フラット）にある |
| **推測** | rnbモードの方向判定に使用するインジケーター（MA・ADX・モメンタム等）が閾値未達のまま推移している |
| **推測** | 264カウントは異常な蓄積速度であり、**フィルター条件が過剰に厳しいか、閾値設定が現在の相場ボラティリティに合っていない**可能性 |
| **要確認** | rnb_usdjpyモードの `direction_filter` の具体的な判定ロジックと閾値設定値 |
| **要確認** | block_countsのリセットタイミング（起動後からの累積か、直近N分の集計か） |
| **要確認** | 同時間帯の過去データと比較してこのカウントが統計的に外れ値かどうか |
| **要確認** | USDJPYの現在のATR・ボラティリティ指標の実測値 |

---

## ログ側の補足所見（別問題）

- `dt_sr_channel_reversal` が `same_price_0pip` で繰り返しブロックされており、**GBPペアの価格がほぼ動いていない（0pip差）**
- `recent_emit` のbypass秒数が130〜264秒と900秒閾値に未達のまま連続発火しており、**シグナル生成ロジックが過剰に高頻度で走っている**
- こちらはrnb_usdjpyとは独立した別問題

---

## 対処方針

### 即時確認
