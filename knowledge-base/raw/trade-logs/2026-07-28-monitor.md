# Trade Monitor Alerts: 2026-07-28

## Alert: 01:17 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=221

### 診断
## 原因分析

### 推測（高確率）

- **`rnb_usdjpy:direction_filter=221` の異常値**は、`rnb_usdjpy` モジュールのシグナルが **方向フィルターに大量連続ブロック**されていることを示す
- 他のブロックカウント（最大でも `daytrade_eurgbp:hedge_block=102`）と比較して **倍以上の突出値**であり、単なる通常ブロックではなくロジックの連続誤発火が疑われる
- USDJPYが一方向に強くトレンド継続中（163円台）のため、`rnb`（レンジブレイク系と推定）の **エントリー方向判定が相場の実態と乖離**し、逆方向シグナルを出し続けている可能性が高い
- ログに `rnb_usdjpy` の直接エントリーログが現れていないことから、**フィルター前段階で大量生成→全ブロック**のループに近い状態になっている可能性

### 推測（中程度）

- `main_loop_restarts=1` があることから、再起動後にカウンターがリセットされずに**累積値が膨らんでいる**可能性
- シグナル生成ロジック側のタイムアウトや価格取得異常により、**同一バーで繰り返し再評価**が発生している可能性

---

### 確認事項（要調査）

1. `rnb_usdjpy` のシグナル生成ログを直近1時間分抽出し、**何方向のシグナルが何回生成されているか**を確認
2. `direction_filter` の判定基準（トレンド方向の参照TFと現在値）を確認し、**フィルター条件が実相場と整合しているか**を検証
3. `block_counts` のカウンターが **再起動前後でリセットされているか**を確認（累積バグの切り分け）
4. `rnb_usdjpy` モジュールの **実行間隔とシグナル生成頻度**が設計値と一致しているかを確認

---

## 対処方針

- **即時対応**
  - `rnb_usdjpy` モジュールを一時的に **停止または監視専用モードに切り替え**、他モジュールへの影響を遮断する
  - `block_counts` の値を手動リセットし、**再起動後の純増分**で再計測して問題の継続有無を判断する

- **調査対応**
  - `direction_filter` が参照するトレンド判定ロジック（移動平均・バイアス方向）を確認し、**USDJ

## Alert: 11:48 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=270

### 診断
## 原因分析

### 【推測】probable causes

- `rnb_usdjpy:direction_filter` のカウント **270** は、他のブロック項目と比較して突出して高い（次点の `daytrade_1h_audjpy:hedge_block=142` の約2倍）
- `rnb_usdjpy` ストラテジーが短時間に大量のシグナルを生成しているにもかかわらず、**direction_filter が継続的にそれを全拒否している**状態と推測される
- 原因として以下の2パターンが考えられる：
  - **相場の方向性がストラテジーの許容レンジから長時間外れている**（例：USD/JPYが一方向トレンドを継続しており、rnbの逆張り条件に合わない）
  - **direction_filterの参照する上位TF・指標（D1/H4トレンド判定など）の値が固着またはバグにより更新されていない**

---

### 【確認事項】要検証

1. **direction_filterの判定ロジック確認**
   - 参照している上位TF（D1・H4等）のトレンド方向値が正常に更新されているか
   - フィルタ閾値やフラグがハードコードまたは設定ミスにより固定化されていないか

2. **rnb_usdjpyのシグナル発生頻度確認**
   - 他のモードと比較してシグナル生成レートが異常に高くないか（ロジック暴走の可能性）
   - main_loop_restarts=1 が発生した前後のタイミングとカウント急増が一致するか確認

3. **USD/JPY相場の現状確認**
   - ログ上の entry=163.9xx 付近での相場レジーム（強トレンド中か否か）を確認
   - rnbが逆張り系であれば、トレンド相場での大量ブロックは正常動作の可能性もある

4. **カウントのリセットタイミング確認**
   - block_countsが累積カウントか起動後リセット型か確認し、270が「異常な短時間集中」か「累積の結果」かを切り分ける

---

## 対処方針

- **即時対応（監視強化）**
  - rnb_usdjpyのシグナル生成ログを時系列で抽出し、direction_filterブロックが1分あたり何件発生しているか定量確認
  - main_loop_restarts=1 前後のログを確認し、再起動によりカウンタが飛んでいないか確認

- **短期対応（原因次第）**
  - 上位TF更新が止まっている場

## Alert: 13:24 UTC

### 検出された問題
- 🔴 **含み損-35.0pip** (7ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 【推測】含み損-35pip / 7ポジションの主因

- **複数モード（daytrade/scalp/rnb等）が同一方向（BUY優勢）でUSDJPY・EURJPYに集中エントリー**しており、相場が逆行した可能性が高い
- `streak_reversal BUY @ 163.842`、`dual_sr_bounce EUR_JPY BUY`が連続ブロックされている＝**エントリー根拠となったバーがすでに古く（13:15足）、その後価格が逆走**していると推測
- `vol_momentum_scalp USD_JPY SELL`シグナルが13:20足で複数回ブロックされており、**逆方向（SELL）シグナルが出ているにも関わらずhedge_blockで抑制**されている可能性
- `SHADOW BUY TREND_BEAR block` `EV=-2.486`が出現→**弱気トレンド環境でBUYポジションが積まれていた**ことを示唆

---

### 【推測】ORDER_BAR_DEDUPの異常な高頻度発生

- 同一バー（13:15足）に対するシグナルが約2分間で10回以上発火→**シグナル生成ループが過剰に回っている**か、キャッシュ更新タイミングがずれている
- `main_loop_restarts: 1`が記録されており、**再起動直後にバー判定状態がリセットされ重複発火**した可能性

---

### 【確認事項】（実機で要確認）

1. **7ポジションの内訳**（通貨ペア・方向・エントリー価格・建玉時刻）を取得し、BUY/SELL比率と損益分布を確認
2. `vol_momentum_scalp USD_JPY SELL`がhedge_blockされた理由→既存BUYポジションとのヘッジ抑制が意図通りか確認
3. `main_loop_restarts: 1`の発生時刻とポジション建玉時刻の突合→再起動起因の誤エントリーでないか確認
4. `SHADOW EV=-2.486`の判定時刻とポジション保有状況の照合
5. SLが正常に設置されているか全7ポジション分を確認

---

## 対処方針

- **即時対応**
  - 7ポジションのSL設置状況を最優先で確認、未設置があれば手動で設置
  - 含み損がさらに拡大する場合（目安-50pip超）は手動クローズを検討
  - `vol_momentum_scalp SELL`が複数回出ているため、hedge_

## Alert: 17:19 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=339

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=339 の異常原因

**最も可能性が高い原因**

- `rnb_usdjpy` モードのシグナル生成ロジックが、方向フィルター（`direction_filter`）の判定を毎ループ通過できない状態で **高頻度にシグナル試行を繰り返している**
- USDJPYが現在レンジ相場（ログ内 `RANGE bypass`、`vol=squeeze`、`WAIT`シグナル、conf=20）であり、**rnbモードが期待する方向性（トレンド）と実勢の相場状態が恒常的に不一致**になっている
- `main_loop_restarts=1` の再起動タイミングで、カウンターがリセットされず過去分が累積している可能性

**補足根拠**
- 他のblock_countsは最大339未満（最大163）であり、339は統計的に突出している
- ログ上にrnbモードの直接的なログ出力がなく、サイレントに弾かれ続けている状態とみられる

---

### 【確認事項】（実施前に要検証）

- [ ] `rnb_usdjpy` のシグナル生成インターバル設定値 — 他モードより短くないか
- [ ] `direction_filter` の判定ロジック — 現在のUSDJPY相場（レンジ・スクイーズ）に対して条件が過剰に厳しくなっていないか
- [ ] block_countsのカウンターリセットタイミング — 再起動後にリセットされているか、累積集計になっているか
- [ ] 直近の`rnb_usdjpy`シグナル出力ログ — 最後に通過（エントリー）できた時刻を確認

---

## 対処方針

**即時対応**

- `rnb_usdjpy` モードのシグナル試行頻度を確認し、**不必要に高頻度ループしていないかチェック**する
- block_countsの監視閾値を「絶対値」ではなく「単位時間あたりの増分」で評価するよう改善を検討する

**短期対応**

- USDJPYがレンジ・スクイーズ状態の間は `rnb_usdjpy` モードを**一時サスペンド or インターバル延長**するレンジ判定ガードを追加する
- `direction_filter` の条件が現行相場環境に対して適切かレビューし、必要であればパラメータを調整する

**運用管理**

- `main_loop_restarts` 発生時にblock_countsをリセットする、または
