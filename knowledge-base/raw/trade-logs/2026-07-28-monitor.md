# Trade Monitor Alerts: 2026-07-28

## Alert: 01:17 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=221

### 診断
## 原因分析

### 推測（高確率）

- **`rnb_usdjpy:direction_filter=221` の異常値**は、`rnb_usdjpy` モジュールのシグナルが **方向フィルターに大量連続ブロック**されていることを示す
- 他のブロックカウント（最大でも `daytrade_eurgbp:hedge_block=102`）と比較して **倍以上の突出値**であり、単なる通常ブロックではなくロジックの連続誤発火が疑われる
- USDJPYが一方向に強くトレンド継続中（163円台）のため、`rnb`（レンジブレイク系と推定）の **エントリー方向判定が相場の実態と乖離**し、逆方向シグナルを出し続けている可能性が高い
- ログに `rnb_usdjpy` の直接エントリーログが現れていないことから、**フィルター前段階で大量生成→全ブロック**のループに近い状態になっている可能性

### 推測（中程度）

- `main_loop_restarts=1` があることから、再起動後にカウンターがリセットされずに**累積値が膨らんでいる**可能性
- シグナル生成ロジック側のタイムアウトや価格取得異常により、**同一バーで繰り返し再評価**が発生している可能性

---

### 確認事項（要調査）

1. `rnb_usdjpy` のシグナル生成ログを直近1時間分抽出し、**何方向のシグナルが何回生成されているか**を確認
2. `direction_filter` の判定基準（トレンド方向の参照TFと現在値）を確認し、**フィルター条件が実相場と整合しているか**を検証
3. `block_counts` のカウンターが **再起動前後でリセットされているか**を確認（累積バグの切り分け）
4. `rnb_usdjpy` モジュールの **実行間隔とシグナル生成頻度**が設計値と一致しているかを確認

---

## 対処方針

- **即時対応**
  - `rnb_usdjpy` モジュールを一時的に **停止または監視専用モードに切り替え**、他モジュールへの影響を遮断する
  - `block_counts` の値を手動リセットし、**再起動後の純増分**で再計測して問題の継続有無を判断する

- **調査対応**
  - `direction_filter` が参照するトレンド判定ロジック（移動平均・バイアス方向）を確認し、**USDJ
