# Trade Monitor Alerts: 2026-06-17

## Alert: 02:15 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=303

### 診断
## 原因分析

### 【推測】問題の本質

**`rnb_usdjpy:direction_filter` が 303 という突出した値になっている主因は、USDJPY が方向性の定まらないレンジ相場に長時間滞留しており、rnbモードのdirection_filterが連続発火し続けているため。**

他のblock_countが数十〜百台前半なのに対し303は明らかな外れ値であり、単なる「よくブロックされるフィルタ」ではなくループ的な連続発火が起きている可能性が高い。

---

### 【推測】副次的に観察される異常

- `daytrade:same_price_5pip` が48件 → USDJPYが狭いレンジ内（5pip以内）で往復しており、price stagnation状態
- `SCORE_GATE` で `score=-0.09` が固定値のまま繰り返し出現 → スコア計算が更新されていないか、インプット特徴量が変化していない
- `recent_emit bypass` が毎サイクル発火（446s→480s→512s→544s と30〜32秒刻みで増加）→ シグナル自体は毎インターバルごとに生成され続けている
- `[HOLD] DT含み益保護` が並走 → 既存BUYポジションが保持されたまま、反転シグナルが抑制されている

---

### 【確認事項】（実機で確認すべきこと）

1. **rnbモードのシグナル生成頻度**
   - direction_filterが何秒おきに評価されているか
   - インターバル設定が意図より短くなっていないか（設定ミス・再起動後の設定ズレ）
2. **USDJPYの現在の値動き**
   - 直近1〜2時間のレンジ幅を確認（5pip以内の膠着であれば相場環境起因と判断可）
3. **score=-0.09 の固定化**
   - スコア計算のインプット（特徴量キャッシュ、価格データ）が正常に更新されているか
   - キャッシュ or データフィード側でフリーズしていないか
4. **main_loop_restarts=1 との関係**
   - 再起動のタイミングとblock_count急増のタイミングが一致するか
   - 再起動後にカウンタがリセットされずに累積されていないか
5. **rnbモードのdirection_filterの判定ロジック**
   - 現在の相場状態でほぼ必ずブロックされる条件になっていないか（フィルタ閾値が過剰に厳しいか）

## Alert: 08:51 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=521, daytrade_eur:hedge_block=396, daytrade_gbpusd:hedge_block=306, scalp:r2_shadow_demoted_cell=208

### 診断
## 原因分析

### 推測（ログ・カウントから高確率で判断できるもの）

**① `rnb_usdjpy:direction_filter=521`（最多・最重大）**
- RNBロジックの方向フィルターが極端に頻発している
- USD/JPYが160円台で一方向への強いバイアスが続いており、フィルターが全シグナルを遮断し続けている可能性が高い
- カウント521は他のブロックの2〜6倍であり、正常範囲を明確に逸脱している

**② `daytrade_eur:hedge_block=396` / `daytrade_gbpusd:hedge_block=306`**
- EUR/USD・GBP/USDでヘッジポジションが長時間解消されず、反対方向シグナルを大量にブロックし続けている
- ログ上でも `session_time_bias → hedge_block` の連鎖が繰り返されており、ヘッジ状態が固着していることを裏付けている
- `含み益保護`ログが同時多発しており、既存ポジションが決済されないままシグナルだけが積み上がっている構図

**③ `scalp:r2_shadow_demoted_cell=208`**
- スキャルプのR2セルが継続的にデモート（降格）されており、ボラティリティ環境またはスプレッド条件がスキャルプ判定閾値を下回り続けている
- ADX=18.5のレンジ相場であることと整合する

---

### 確認が必要な事項（推測を含む・要実機検証）

| # | 確認項目 | 懸念理由 |
|---|----------|----------|
| 1 | `rnb_usdjpy`のdirection_filterロジックに閾値バグ・無限ループ的な条件が存在しないか | 521という突出した数値は設計上の上限を超えている可能性がある |
| 2 | EUR/USD・GBP/USDのヘッジポジション実残高と意図した状態の一致確認 | ヘッジが意図せず張られたまま（または誤ってダブルサイドで保有）になっている可能性 |
| 3 | `main_loop_restarts=1` の再起動タイミングとブロックカウント増加の相関 | 再起動後にカウンターがリセットされずに加算継続している可能性 |
| 4 | `london_breakout → shadow_tracking` の連続スキップが意図通りか | EV=-2.714でシャドウに落としているが、ロンドン初動を全損している場合はEV計算のデータ品質を確認 |
| 5
