# Trade Monitor Alerts: 2026-06-02

## Alert: 02:13 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=271

### 診断
## 原因分析

### 推測（ログ・カウントから強く示唆される）

- **`rnb_usdjpy:direction_filter=271`** は他のblock_countsと比較して突出して高い値であり、RnB（レンジ＆ブレイクアウト）ストラテジーのUSDJPYが、シグナルは継続発生しているものの**方向性フィルターに271回連続で弾かれている**状態
- ログの `ADX: 16.7`、`L1: neutral`、`signal: WAIT` から、現在のUSDJPYは**明確なトレンドが存在しないレンジ相場**であり、RnBのdirection_filterが「方向感なし＝エントリー不可」と判定し続けている可能性が高い
- `main_loop_restarts: 1` があるため、**再起動後にカウンターがリセットされていない**か、再起動以降ずっとフィルターに引っかかり続けているかのどちらか

### 確認事項（断定前に要検証）

- [ ] `direction_filter` の判定ロジックが参照しているインジケーター（ADX閾値・トレンド判定ソース）が正常な値を返しているか、またはフリーズ・スタック状態になっていないか
- [ ] 271というカウントが**累積カウント**なのか**セッション内カウント**なのかを確認（累積なら正常範囲の可能性もある）
- [ ] RnBストラテジー自体の `last_signal` が存在するか、またはNone/staleになっていないか（statusのmodesセクションにrnb_usdjpyのエントリーが見当たらない点が気になる）
- [ ] `direction_filter` の判定に使用しているデータフィードのタイムスタンプが最新か（データ停滞による誤判定の可能性）
- [ ] 他通貨ペアのRnBインスタンスに同様の高カウントが出ていないか（USDJPYのみなら相場環境起因、複数なら設定・ロジック起因）

---

## 対処方針

- **即時対応（監視継続）**
  - 現時点では `emergency_killed: false`、`main_loop_alive: true` であり緊急停止は不要
  - ただしrnb_usdjpyが**実質エントリー機能停止状態**であることを認識した上で監視継続

- **短期対応（原因切り分け）**
  - direction_filterの判定ソース（ADX・トレンド系インジケーター）の現在値をダンプして正常動作を確認
  - rnb_usdjpyの `last_

## Alert: 08:17 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=468, daytrade_eurjpy:hedge_block=382, daytrade_gbpusd:hedge_block=311, daytrade_eur:hedge_block=240, scalp:r2_shadow_demoted_cell=209

### 診断
## 原因分析

### 【推測】各ブロック異常の原因

**① `rnb_usdjpy:direction_filter=468`（最大値・最重要）**
- RNB戦略のdirection_filterが長時間・大量にブロックし続けている
- USDJPYのトレンド方向が戦略の許容方向と合致しない相場が継続中と推測
- フィルター閾値が現在の相場レンジ・ボラティリティに対して過剰に厳格な可能性

**② `daytrade_eurjpy:hedge_block=382` / `daytrade_gbpusd:hedge_block=311` / `daytrade_eur:hedge_block=240`**
- ログに`BUY e78db2a3`のポジション含み益保護が頻繁に発火しており、逆シグナルを大量に無効化している
- EURJPYで`live=1/1`スロットが埋まったままshadowに流れ続けており、hedge_blockの連続計上が発生していると推測
- 既存ポジション（特にe78db2a3）が長時間決済されず、ヘッジブロックを累積させている構造的な詰まり

**③ `scalp:r2_shadow_demoted_cell=209`**
- R2セルへのデモーション（格下げ）が多発＝スキャルpシグナルがセルランク不足で量産ブロックされている
- 市場がスキャル戦略のエントリー条件を満たす場面でもセル評価が低い相場環境が継続中と推測

**④ `daytrade_gbpjpy:gbp_asia_flash_crash=156`**
- アジア時間のGBP急変動フィルターが継続発動中
- GBP系全般でフラッシュクラッシュガードが高止まり（gbpusd側にも43件）

---

### 【確認事項】（推測ではなく要検証）

| 確認項目 | 確認方法 |
|---|---|
| `e78db2a3`ポジションの建値・現在損益・建玉時刻 | ブローカーポジション一覧またはOANDA API |
| `rnb_usdjpy:direction_filter`の判定ロジックとUSDJPY現在レジーム | direction_filter内部変数のログダンプ |
| EURJPYのlive slotが解放されていない理由（TP未到達 or ロジックバグ） | スロット管理テーブルの状態確認 |
| GBPフラッシュクラッシュ判定の閾値と現在のATR/Spread値 | gbp_asia_flash_crash条件変数の

## Alert: 13:45 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=316

### 診断
## 原因分析

### 【推測】`rnb_usdjpy:direction_filter=316` の異常カウントについて

**主因の推測：**

- `rnb_usdjpy` モードにおいて、方向性フィルター（`direction_filter`）がUSDJPYのシグナルを**長時間・高頻度でブロックし続けている**状態
- 他のblock_countsが10〜180台に収まる中、316は**突出した値**であり、フィルタが「詰まった」か「ロジック的に解除されない条件に入り込んでいる」ことを示唆
- USDJPYは同ステータス内で `daytrade` / `daytrade_1h` 双方が稼働中であり、**ヘッジポジションや既存ポジションの方向が `rnb` モードの期待方向と恒常的に競合**している可能性
- `main_loop_restarts=1` があることから、再起動後に何らかの内部状態（directional bias / position context）が**正しくリセットされずに引き継がれた**可能性

---

### 【推測と確認事項の分離】

#### 推測（根拠あり）
| # | 推測内容 | 根拠 |
|---|----------|------|
| 1 | direction_filterの解除条件を満たさない相場状態（レンジ・反転継続）が長期化している | 他モードのMTFログで `range_wide`, `range_tight`, `d1=0, h4=0` が頻出 |
| 2 | main_loop再起動後にdirectional stateが意図せず保持されている | `main_loop_restarts=1` |
| 3 | daytradeモードのBUYポジション保護（profit=0.02800）がUSDJPY全体の方向判定に影響している | HOLDログ `7650f1d7` が複数回出現 |

#### 要確認事項（確認なしでは断定不可）
- `rnb_usdjpy` の `direction_filter` が参照している具体的な判定ソース（EMA方向・MTFバイアス・ポジション方向のいずれか）
- 316カウントがいつから急増したか（再起動前後か、特定の時刻からか）
- `rnb_usdjpy` モードが現在シグナル自体は生成しているか、それとも上流で止まっているか
- direction_filterの自動解除ロジック（TTLやカウンタリセット条件）が存在するかどうか

---

## 対処方針

- **即時確認**
  - `rnb_usdjpy` のdirection_filter判定ソースのログを抽出し、カウント急増の開始時

## Alert: 17:58 UTC

### 検出された問題
- 💰 **含み益+21.3pip** (14ポジション, LIVE=0 Shadow=14) | Top: session_time_bias×EUR_USD +8.1pip | sr_fib_confluence×EUR_USD +7.8pip | xs_momentum×EUR_USD +5.7pip
- ⚠️ **Shadow含み益+28.0pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】Shadow→LIVEに昇格できていない主因

- **`same_price_0pip`ブロックが最終ゲート**になっている
  - `session_time_bias`シグナルが`recent_emit`をbypassしてShadowには到達しているが、`same_price_0pip`チェックで止まっている
  - `daytrade_eur:same_price_0pip`のblock_countが**67**と突出しており、EUR_USDの複数戦略が同一価格帯で重複発火している状態
- **`recent_emit`クールダウン(900秒)** が複数戦略に広く効いており、新規LIVE送信の機会を抑制している（block_count: 58）
- BUY側ポジション(+7.5pip級)で**含み益保護HOLDが連続発動**しており、SIGNAL_REVERSEが全て無効化されている → ポジション整理が進まずmax_openブロックが累積

### 【確認事項】（推測と区別）

- `same_price_0pip`の基準価格が何に対して判定されているか（直前エントリー価格 vs 現在Bid/Ask）
- Shadow側14ポジションの各エントリー価格と現在レートの差分 → 本当に0pip圏内か
- `PAIR_PROMOTED`リストにEUR_USDが含まれていない、または昇格条件の閾値（pip数・継続時間）を未達か
- `main_loop_restarts: 1`の再起動タイミングとblock_count累積の相関（再起動後にカウンターがリセットされず積み上がっている可能性）

---

## 対処方針

### 即時対応

- **`same_price_0pip`判定の基準価格を確認**し、EUR_USDの現在レートとの乖離が実際に0pipか検証する
- Shadow含み益+28.0pipが一定水準以上継続しているなら、**EUR_USDを`PAIR_PROMOTED`に追加**してLIVE昇格を許可する
- `daytrade_eur:same_price_0pip`ブロックが異常に高い場合、**同一シグナルの重複発火抑制ロジック**（エントリー価格の丸め精度など）を見直す

### 短期対応

- `recent_emit`クールダウン900秒がShadowバイパス後も`same_price_0pip`で二重ブロックになっている構造を整理し、**ブロック優先順位と通過条件を明示化**する
- BUY側の含み益保護HOLD（+7.5pip）が長時間継続しているポジションについて、**利確トレイリングまたはTP
