# Trade Monitor Alerts: 2026-06-24

## Alert: 01:56 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=289, daytrade:hedge_block=237

### 診断
## 原因分析

### 【推測】根本原因

**1. `rnb_usdjpy:direction_filter=289`（最多ブロック）**
- RNBロジックが上位足または複合フィルターで「方向確定不能」または「逆張り禁止」状態に長時間ロックされている
- USDJPY が明確なトレンド or レンジ境界に居座っており、方向フィルターが常に否決し続けている可能性
- フィルター条件の閾値が現在の市場ボラティリティ帯に対して過剰に厳格になっている可能性

**2. `daytrade:hedge_block=237`（第2位）**
- 既存ポジション（ログに確認できる SELL 5bc74c09 / 72fc0b01）が建ち続けており、ヘッジブロックが累積カウントされている
- 含み益保護（HOLDログが約10秒間隔で繰り返し発火）が有効なため、ポジションがクローズされず、結果としてヘッジブロックが解除されない状態が継続

**3. `fib_reversal` R2_SHADOW_DEMOTEの連続発火**
- `fib_reversal`セルが短時間に非常に高頻度（約2〜3秒間隔）でshadow追跡→demoteを繰り返している
- セルの復帰条件が甘い、またはシグナル生成ロジックが市場条件に対してノイジーな状態

---

### 【確認が必要な事項】

| 確認項目 | 目的 |
|---|---|
| RNBのdirection_filterが参照している上位足（1H/4H）の現在値と判定ロジック | フィルターが永続ロック状態になっているか確認 |
| SELL 5bc74c09 / 72fc0b01 のオープン時刻・TP/SL設定 | ヘッジブロックの解除が見込めるか判断 |
| HOLDの含み益保護threshold（0.00030）が現在のUSDJPYスプレッド・ボラに対して適切か | 過剰なHOLD継続を引き起こしていないか |
| `fib_reversal`セルのdemote→復帰サイクルのタイムアウト設定 | 異常なチャーニングが設定バグか市場起因かを切り分け |
| direction_filterのカウンターにリセット条件が存在するか | ブロックカウントが単純加算なのか時間減衰があるのか |

---

## 対処方針

### 即時対応
- **SELL 2ポジション（5bc74c09 /

## Alert: 05:59 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=273

### 診断
## 原因分析

### 【推測】`rnb_usdjpy:direction_filter=273` の原因

- `direction_filter` は、rnb_usdjpy モードにおいて **トレンド方向と逆行するシグナルをブロックするフィルタ**と推定される
- カウント273は他のブロック項目と比較して**突出して高い**（次点の `daytrade_eurjpy:hedge_block=198` の約1.4倍）
- USD/JPYが**一方向に強いトレンド**を形成しており、rnbモードが逆張りシグナルを連続生成 → 全件フィルタで棄却されている状態と推測
- ログの `R2_SHADOW_DEMOTE` が USD/JPY で多発していることと整合しており、**相場の方向性が強く固定されている**局面である可能性が高い
- `daytrade_1h` の confidence=0、`daytrade` の confidence=22 と低迷しており、**方向感の欠如または強トレンド中の逆張り頻発**が背景にある可能性

---

### 【確認事項】（確認してから判断すべき点）

- [ ] rnb_usdjpy の `direction_filter` のしきい値ロジック・参照するトレンド判定ソースを確認（何をもって"逆方向"と判定しているか）
- [ ] 直近のUSD/JPY価格推移を確認（161円台で一方向トレンドが続いていないか）
- [ ] `direction_filter` のカウントが**今セッション開始からの累積か、直近N分のウィンドウ集計か**を確認（累積なら増加自体は正常範囲の可能性）
- [ ] rnb_usdjpy が**実際にエントリーできている取引があるか**を確認（完全に機能停止していないか）
- [ ] `main_loop_restarts=1` の再起動タイミングとカウント急増のタイミングが一致しないか確認

---

## 対処方針

### 緊急度：低〜中（システム異常ではなくフィルタ正常動作の可能性が高い）

- **まず静観**：direction_filterは設計通りに機能している可能性が高く、強トレンド局面での過剰ブロックは正常動作の範囲内
- **rnb_usdjpy のシグナル生成頻度を確認**：そもそものシグナル生成数が多すぎる場合、上位ロジック（エントリー条件）の絞り込みを検討
- **direction_filter の参照ソース見直し**：トレンド判定に使用しているインジケータ（EMA乖離・AD
