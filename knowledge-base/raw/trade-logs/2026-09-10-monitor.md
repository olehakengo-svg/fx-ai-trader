# Trade Monitor Alerts: 2026-09-10

## Alert: 01:43 UTC

### 検出された問題
- 💰 **含み益+30.8pip** (1ポジション, LIVE=1 Shadow=0) | Top: usdjpy_carry_dip_accumulator×USD_JPY +30.8pip
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=229

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:no_signal=229` の異常値**
  - `rnb_usdjpy` モード（おそらくRange-and-Breakout系）がシグナル生成条件を満たせない状態が229回連続して継続している
  - USD/JPYが現在トレンド相場（carry_dip_accumulatorで+30.8pip含み益中）にあり、レンジ系ストラテジーの発火条件（レンジ幅・ボラティリティ帯等）を満たさないことが根本原因と推測
  - 相場環境とモードの設計意図が一致していない（レンジ系モードがトレンド相場に対して沈黙している正常動作の可能性もあり）

- **`R2_SHADOW_DEMOTE` の連続ブロック（scalp系全般、特にUSD_JPY・EUR_USD）**
  - `ema_trend_scalp` セルがshadow-trackingに降格され続けており、R2スコア（精度・信頼度指標）が閾値を下回っている状態が持続中
  - LIVEスロット満杯（live=1/1）かつshadow待機（shadow=0/2）の状態でスロットバイパスが繰り返し発生 → shadowエントリーが積み上がらない

- **`ORDER_BAR_DEDUP` の繰り返し（dt_sr_channel_reversal USD_JPY SELL）**
  - 同一バー（01:30:00）のSELLシグナルが何度も生成・ブロックされている
  - SELL方向シグナルは`[HOLD] DT含み益保護`によっても二重にブロックされており、現行BUYポジション保護が正常機能している

- **`daytrade_1h_eur` の最遅ティック（56.5秒）**
  - EUR系の1hモードでティック受信が他モードより遅延。現時点では閾値（15分）内だが要注視

---

### 確認事項（断定不可）

| 確認項目 | 理由 |
|---|---|
| `rnb_usdjpy` の設計上のシグナル発火条件（ATR帯域・レンジ幅閾値等） | no_signalが正常沈黙か異常停止かの判断に必須 |
| `rnb_usdjpy` モードのシグナル生成ログ（229回以前の最後の発火時刻） | 突然停止したのか、徐々に減衰したのかで原因が変わる |
| `R2スコア` の現在値と降格閾値 | ema_trend_scalpが継続dem

## Alert: 06:43 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=753, scalp:r2_shadow_demoted_cell=413, daytrade_eurjpy:hedge_block=308, scalp_eur:r2_shadow_demoted_cell=280, daytrade_gbpjpy:gbp_asia_flash_crash=211

### 診断
## 原因分析

### 【推測】各ブロック異常の原因

**① rnb_usdjpy:no_signal=753（最大異常）**
- RNBモデルがUSD/JPYに対してシグナルを全く出力できていない
- モデルへの入力フィーチャ（価格・指標）が欠損または閾値外になっている可能性
- またはRNBモデル自体がクラッシュ/フリーズしてデフォルト値を返し続けている可能性

**② scalp/scalp_eur:r2_shadow_demoted_cell=413/280（複数モード横断）**
- ログを見るとUSD/JPYの`fib_reversal`と`ema_trend_scalp`セルが繰り返しデモートされている
- R2（決定係数）が閾値を割り込む相場環境が継続している、またはセル自体の内部状態が劣化・固着している
- scalp/scalp_eurの両モードで同時多発しているため、共通の上流フィーチャ（USD/JPY価格データやR2計算ロジック）に問題がある可能性が高い

**③ daytrade_eurjpy:hedge_block=308**
- ログに`hedge_block(daytrade/EUR_JPY:SELL)`が頻出
- EUR/JPY方向でSELLポジションが既に存在、または建値判定がSELL方向に固着しており、BUY系シグナルが全てブロックされ続けている
- ヘッジ解除トリガーが正常に発火していない可能性

**④ daytrade_gbpjpy:gbp_asia_flash_crash=211**
- GBP系の`gbp_asia_flash_crash`フィルターが継続発動中
- アジア時間帯にGBP/JPYの急騰・急落を検出し、フラッシュクラッシュ判定が張り付いている
- フィルターのクールダウン時間が長すぎるか、解除条件が未達のまま

**⑤ ORDER_BAR_DEDUP多発（EUR/JPY, GBP/JPY）**
- `bar_ts=2026-09-10T06:30:00`に固定されたまま、同一バーのシグナルが繰り返し投入されている
- バー更新ロジックが06:30足を正しくクローズできておらず、07:00足へ進んでいない可能性（バーローリングの遅延・失敗）

**⑥ daytrade_1h_nzdjpy:stalest_age=57.8秒**
- NZD/JPYのティックフィードが滞留しており、1hモードの更新が遅延

---

### 【確認事
