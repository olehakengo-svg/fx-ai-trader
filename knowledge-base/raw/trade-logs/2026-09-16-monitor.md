# Trade Monitor Alerts: 2026-09-16

## Alert: 07:00 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=404, daytrade_gbpjpy:order_bar_dedup=204

### 診断
## 原因分析

### 推測（ログ・数値から高確率で推定できる事項）

**① `rnb_usdjpy:no_signal=404`（異常値）**
- RnBストラテジーがUSD/JPYに対してシグナルを一切生成できていない
- 直近ログに `[SHADOW] DT RANGE bypass: sr_break_retest … SELL USD_JPY` および `[ORDER_BAR_DEDUP] blocked sr_break_retest USD_JPY SELL` が繰り返し出現しており、USD/JPYがレンジ判定に張り付いてシャドウ降格→dedup遮断のループに入っている
- RnBはトレンド方向性を前提とする設計のため、レンジ相場継続によりシグナル条件を恒常的に満たせていない可能性が高い

**② `daytrade_gbpjpy:order_bar_dedup=204`（異常値）**
- 同一バー（`bar_ts`固定）から繰り返しシグナルが発火し、dedupフィルタが大量に遮断している
- 他通貨ペアの同フィルタ（eurjpy:192, gbpusd:97等）と比較してもGBP/JPYが突出しており、シグナル生成ロジックが同バー内で異常に多重発火している
- `gbp_asia_flash_crash:12` `mtf_strong_bias:3` `r2_shadow_demoted_cell:24` も同時多発しており、GBP/JPYで複数フィルタが連鎖的に作動している

---

### 確認事項（断定前に要検証）

- **USD/JPYのレンジ判定パラメータ**：現在のATRまたはボラティリティ閾値が実勢相場に対して過大設定になっていないか
- **RnBのシグナル生成条件**：no_signalカウントがいつから増加し始めたか（今セッションのみか、複数セッションか）
- **GBP/JPYの`bar_ts`固定問題**：同一バーの `bar_ts=2026-09-16T06:45:00` が繰り返されているのはUSD/JPYの例だが、GBP/JPYでも同様の固定化が起きているか、バーロールオーバーが正常に行われているか
- **`daytrade_1h_audusd` tick遅延（stalest=58.9秒）**：エンジン全体への影響はないが、1hモードのデータ供給元（外部フィード）に問題がないか

---

## 対処方針

**`rnb_usdjpy:no_signal` 対応**
- USD/JPYのレンジ/トレンド判定

## Alert: 12:53 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=1018, scalp:r2_shadow_demoted_cell=535, daytrade_eur:order_bar_dedup=446, scalp_eur:r2_shadow_demoted_cell=396, daytrade_gbpusd:order_bar_dedup=392

### 診断
## 原因分析

### 推測（ログ・数値から高確度で推定）

**① rnb_usdjpy:no_signal=1018（最大異常）**
- レンジ相場判定が長時間継続し、RnBロジックのエントリー条件を満たせない状態が続いている
- USD/JPYがレンジブレイク閾値に到達せず、シグナル生成自体が停止している可能性が高い

**② r2_shadow_demoted_cell 急増（scalp:535, scalp_eur:396, scalp_5m系:200台）**
- R2セルがshadow trackingに降格されたまま復帰していない
- 複数通貨ペア・複数戦略で同時多発しているため、特定セルの問題ではなくR2昇格ロジック全体またはその入力データ（価格・指標）に問題がある可能性が高い
- USD/JPY・EUR/USD・EUR/JPYに集中しており、共通の上位指標（MTF bias、ATR、ボラティリティ判定）が起因している可能性あり

**③ order_bar_dedup 急増（daytrade_eur:446, daytrade_eurjpy:348, daytrade_gbpusd:392）**
- 同一bar_ts（12:45:00）からの重複発注ブロックがGBP_JPYで確認されており、同一バーで複数回シグナルが生成・再評価されるループ的挙動が発生している
- tick受信頻度の増加、またはシグナル評価タイミングの重複が原因と推測

**④ SCORE_GATE: EUR/JPY sr_break_retest score=-0.31（繰り返し）**
- スコアがマイナス固定で止まっており、特定セルのスコア計算が更新されていないか、入力特徴量が固着している
- `ob_retest score=4.06`も同一タイムスタンプで繰り返されており、キャッシュ・評価結果の固着が疑われる

---

### 確認事項（実際に調査すべきこと）

| 優先 | 確認項目 | 確認対象 |
|---|---|---|
| 🔴高 | USD/JPYのATR・ボラティリティ指標が閾値以下で固着していないか | 指標計算モジュール・市場データフィード |
| 🔴高 | R2昇格ロジックの昇格条件（スコア閾値・評価タイミング）が正常に動作しているか | r2_shadow_demote判定ロジック |
| 🔴高 | daytrade_1h_audusdのtick stalest=49.8秒がデータ
