# Trade Monitor Alerts: 2026-05-05

## Alert: 04:51 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=225

### 診断
## 原因分析

### 🔍 推測（高確度）

- **`rnb_usdjpy:direction_filter=225`は他の全ブロックカウントと比較して突出した値**（次点の`daytrade_eurjpy:hedge_block=138`の約1.6倍）
- `rnb_usdjpy`モードが高頻度でシグナルを生成しているが、**direction_filterによって全量ブロックされ続けている**状態
- ログ上でMTF（マルチタイムフレーム）モニターが`USD_JPY`に対し`d1=1 h4=-1`と**上位足が相反する方向**を示しており、これがdirection_filterの判定根拠になっている可能性が高い
- 2026-05-05 04:50台のログで`mtf=uncertain`が繰り返し記録されており、**方向性が定まらないレンジ相場に`rnb`モードのエントリーロジックが高頻度反応している**と推測

---

### 🔍 推測（中確度）

- `main_loop_restarts=1`が発生しており、再起動後にカウンターがリセットされていない場合、**実際の発生密度はさらに高い**可能性
- `rnb`（レンジブレイク系）ロジックがレンジ相場でシグナルを乱発するロジック上の相性問題が根本にある可能性

---

## ✅ 確認事項

- [ ] `direction_filter`の判定ロジックで使用している上位足TF（D1/H4）の現在の方向値と閾値設定を確認する
- [ ] `rnb_usdjpy`のシグナル生成インターバル・クールダウン設定が意図通りか確認する
- [ ] `main_loop_restarts=1`の再起動タイミングと理由をログから特定し、カウントが累積継続しているか確認する
- [ ] 同時間帯の他通貨ペア（EUR/USD等）で`direction_filter`ブロックが発生していないか比較し、USD/JPY固有の相場環境起因かを切り分ける
- [ ] `log_count=9348`に対して`rnb_usdjpy:direction_filter`の占有率（約2.4%）が許容範囲の設計仕様内かを確認する

---

## 🛠 対処方針

- **即時対応（監視）**
  - 現時点では`emergency_killed=false`かつトレードは継続中のため、緊急停止は不要
  - ただし225という値が今後も増加し続ける場合は、rnbモードの一時停止を検討する

- **短期対応**
  -

## Alert: 13:04 UTC

### 検出された問題
- 💰 **含み益+80.6pip** (11ポジション, LIVE=0 Shadow=11) | Top: sr_anti_hunt_bounce×GBP_JPY +22.8pip | post_news_vol×USD_JPY +19.8pip | sr_fib_confluence×GBP_USD +13.5pip
- ⚠️ **Shadow含み益+80.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 推測（ログ・ステータスから高確率で推定）

**主因：全11ポジションがShadowモードに閉じ込められている**

- `LIVE=0 / Shadow=11` という状態は、シグナルは発生しているがOANDAへの実注文が一切送信されていないことを示す
- ログに `DT RANGE bypass: sr_fib_confluence (TF in RANGE → shadow)` が繰り返し出ており、**レンジ相場判定によってShadow強制ルーティングが連続発動**している
- `PAIR_PROMOTED`リストにGBP_JPY・USD_JPY・GBP_USDが含まれていない、またはPROMOTEDフラグが剥がれた可能性がある
- `main_loop_restarts: 1` が発生しており、**再起動時にPAIR_PROMOTEDの状態がリセット**された可能性がある

**副因：多重ブロックが発注経路を完全に塞いでいる**

- `hedge_block`・`recent_emit`・`same_price`が主要通貨ペア（GBP_JPY×6、GBP_USD×2など）に集中しており、仮にShadowを解除しても即座に発注される保証がない
- `rnb_usdjpy:direction_filter×6` はUSD_JPYの方向フィルターが強く機能しており、post_news_volシグナルが実注文に到達できていない可能性がある

---

### 確認事項（実際に検証が必要なもの）

- [ ] `PAIR_PROMOTED`リストの現在の中身を確認（GBP_JPY・USD_JPY・GBP_USDが含まれているか）
- [ ] main_loop再起動のタイミングとPAIR_PROMOTED初期化処理の有無を確認
- [ ] Shadowルーティング条件（RANGE判定閾値）が意図した設定値のままか確認
- [ ] `hedge_block`が集中しているGBP_JPY・GBP_USDについて、対向ポジションが実際に存在するか、または誤検知かを確認
- [ ] `daytrade_1h` / `daytrade_1h_eur` が `running: false` になっている理由を確認（意図的な停止か障害か）

---

## 対処方針

- **即時対応**
  - PAIR_PROMOTEDにGBP_JPY・USD_JPY・GBP_USDを手動追加してShadow→Live昇格を試みる
  - ただし昇格前に`hedge_block`・`recent_emit`の残存状況を確認し、即時多重発注が起きないことを確認する

- **短

## Alert: 15:26 UTC

### 検出された問題
- 💰 **含み益+115.2pip** (15ポジション, LIVE=0 Shadow=15) | Top: sr_anti_hunt_bounce×GBP_JPY +39.5pip | dual_sr_bounce×GBP_USD +18.1pip | dual_sr_bounce×EUR_USD +13.8pip
- ⚠️ **Shadow含み益+127.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】主要原因

- **Shadow専有状態（LIVE=0, Shadow=15）**
  - 全15ポジションがShadowモードに留まり、OANDAへ実注文が一切送信されていない
  - `PAIR_PROMOTED`設定に対象通貨ペアが登録されていないため、Shadowから実運用へのプロモーションが発生していない

- **hedge_blockの多発による新規エントリー抑制**
  - `daytrade_gbpjpy:hedge_block=19`、`daytrade_eurjpy:hedge_block=19`、`scalp:hedge_block=21`など、ほぼ全モードでhedge_blockが上限近くまで蓄積
  - 既存BUYポジションへのSELL逆シグナルが含み益保護（`SIGNAL_REVERSE無効化`）で継続ブロックされており、ヘッジ解消の契機が失われている

- **daytrade:recent_emit=13 の過剰抑制**
  - recent_emitカウンタが高止まりしており、新規シグナルの実発火が抑制されている可能性

- **EUR_USDのSentinel bypass連発**
  - `session_time_bias`スコアが低く（0.03）、SELLシグナルが毎回Shadowに強制格下げされている
  - `stoch_trend_pullback × Overlap suppress`でconfidenceが52→26に半減しており、スコアゲート通過不能

---

### 【確認事項】（要調査）

- `PAIR_PROMOTED`の現在の設定値に GBP_JPY・GBP_USD・EUR_USD が含まれているか
- Shadowモードへの格下げ判定ロジックで、今回の通貨ペアが意図的に除外されているか、それとも設定漏れか
- hedge_blockのカウンタリセット条件（時間ベース or ポジションクローズ連動）が正常に機能しているか
- `main_loop_restarts=1` の再起動が発生した時刻と、Shadowポジション生成タイミングの前後関係
- `daytrade_1h` および `daytrade_1h_eur` が `running=false` になっている意図的な停止か否か

---

## 対処方針

- **即時対応**
  - `PAIR_PROMOTED`に`GBP_JPY`・`GBP_USD`・`EUR_USD`を追加し、上位pip貢献ペアから順にLIVEプロモーションを有効化する
  - プロモーション前に含み益の現在値とスプレッド状況を確認し、スリッページリスクを評価する

- **hedge_block解消**
  - 含み
