# Trade Monitor Alerts: 2026-06-09

## Alert: 01:51 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=265

### 診断
## 原因分析

### 推測（ほぼ確実）

- **`rnb_usdjpy:direction_filter=265`** は他の全ブロックカウントと比較して**桁違いに大きい**（次点の`daytrade_eur:hedge_block=107`の約2.5倍）
- rnb_usdjpyの**方向フィルタ（direction_filter）が現在の相場環境と継続的に噛み合っていない**状態が長時間続いている
- 時刻が01:39〜01:50（東京時間深夜〜早朝）であり、**アジア時間初動の方向感が定まらない**レンジ相場が原因で、フィルタ判定が振れ続けている可能性が高い
- ログに`R2_SHADOW_DEMOTE`がUSD_JPYで頻発しており、**スカルプ系でも同通貨ペアのシグナル品質が全体的に低下**している状況と一致する

### 推測（可能性あり）

- rnb_usdjpyのdirection_filterが参照するトレンド判定ロジック（EMA/ADX/RSI等）のパラメータが、現在の相場ボラティリティ帯に**チューニングされていない**
- フィルタのリセット条件が機能していない、または**カウンターが累積リセットされていない**バグの可能性

---

## 確認事項

- [ ] `rnb_usdjpy`のdirection_filterが参照している**方向判定ソース**（例：上位足トレンド、EMA傾き、ADX閾値）の現在値を確認
- [ ] direction_filterブロックが**いつから急増し始めたか**をログで時系列確認（本日セッション開始時からか、それ以前からか）
- [ ] block_countsの**リセットサイクル**（日次/セッション毎など）が正常に機能しているかを確認
- [ ] `R2_SHADOW_DEMOTE`のUSD_JPY集中が、rnb_usdjpyのシグナル抑制と**時系列的に連動しているか**を突き合わせ

---

## 対処方針

- **即時対応**
  - rnb_usdpyのdirection_filterが参照するトレンド判定の現在値をダッシュボードで目視確認し、相場実態（160円近辺の推移）と整合しているか判断する
  - 異常な累積であればblock_countsの手動リセットを検討（ただしバグ起因の場合は再発するため根本対処が必要）

- **短期対応**
  - direction_filterの**閾値をアジア時間帯向けに

## Alert: 05:57 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=321

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=321 の根本原因

**主因：USD/JPY の方向性が上位足と短期足で乖離・ロック状態に陥っている**

- `direction_filter` が321回という突出した数値は、他のブロック要因（最大162）の約2倍。rnb戦略がシグナルを出し続けているが、方向フィルターが全て弾いている状態が長時間継続していることを示す
- ログの `SCORE_GATE` が `streak_reversal / BUY` を連続ブロック（score=-1.12〜-0.62）しており、スコア評価が一貫してBUY方向を否定している
- `MTF_MONITOR` が `vol_momentum_scalp / SELL / range_tight` を繰り返し出力しており、上位足（H4=1, D1=0）がレンジ・売り方向に傾いている
- rnb戦略のシグナル方向（推定BUY方向）が、上位足フィルターの判定方向と反転し続けている

---

### 【確認事項】（実害判断に必要）

- [ ] `direction_filter` のカウントが今セッション内で急増したのか、それとも長期累積なのか（セッション開始時との差分確認）
- [ ] rnb_usdjpy の内部シグナル方向が現在何を出力しているか（BUY固定になっていないか）
- [ ] `direction_filter` の判定ロジックが参照している上位足データの更新タイムスタンプ（データフィードが止まっている可能性）
- [ ] `main_loop_restarts=1` の再起動タイミングとカウント急増の相関
- [ ] スキャルプ5mのWR 46.2%（13件）が許容範囲内かどうかの学習閾値確認

---

## 対処方針

**即時確認（運用影響度：中）**

- rnb_usdjpy の `direction_filter` ロジックが参照するフィルター変数（上位足トレンド判定値）を直接ダンプし、値が固着・フリーズしていないか確認する
- `main_loop_restarts=1` の再起動前後でフィルター状態がリセットされずに引き継がれていないか確認する

**状況判断**

- 上位足データが正常更新されている場合 → **現状維持で問題なし**（相場環境がレンジで方向感なし、フィルターが正常機能している正常系と判断できる）
- 上位足データが固着・古い場合 → **rnb_usdjpy を一時停止**し、フィル

## Alert: 09:32 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=374, daytrade:hedge_block=289

### 診断
## 原因分析

### 推測（ログ・数値から推定）

**① `rnb_usdjpy:direction_filter=374`（異常高値）**
- RNBストラテジーがUSDJPYに対して方向フィルターで継続的にブロックされている
- 相場のレンジ・方向感の欠如、またはフィルター閾値に対してシグナルが逆張り方向に偏っている可能性が高い
- カウントが374と突出しており、フィルター自体がほぼ常時作動している状態＝事実上RNBが機能停止に近い

**② `daytrade:hedge_block=289`（高値）**
- daytradeモードで既存ポジションと逆方向エントリーが継続的にブロックされている
- USDJPYでBUY/SELL両方向のシグナルが交互に発生しており、ヘッジ抑制が過剰作動している可能性
- または既存のSELL/BUYポジションが長期滞留しており、逆方向シグナルが出続けている

**③ 付随問題（ログより）**
- `same_price_0pip`によるブロックが連続発生（session_time_bias が30秒以内に繰り返しシグナル）→ シグナル生成ループの異常頻度
- `SHADOW_FIX`によるOANDAゲートブロック後のシャドウ昇格が常態化
- `FR=200%⚠️`：ファンディングレートまたはポジション偏りが警戒域

---

### 確認事項（推測でなく要確認）

- **RNBポジション・シグナル方向の実態**：現在のUSDJPY相場トレンドとRNBの`direction_filter`設定閾値のギャップがどの程度か
- **daytradeの未決済ポジション有無と方向**：hedge_blockの起点となっているポジションが残存しているか、その保有時間
- **session_time_biasのシグナル生成インターバル設定**：30〜100秒おきに同一シグナルが出続けている根本原因（ループ周期 vs クールダウン設定の不整合）
- **main_loop_restarts=1の発生タイミング**：これらblockカウント増加と再起動が相関しているか
- **`FR=200%⚠️`の内容**：ファンディングレートなのか、ポジションサイズ比率なのかを確認

---

## 対処方針

**即時対応**
- `rnb_usdjpy:direction_filter`の作動条件を確認し、USDJPYの現在レジームがフィルター

## Alert: 12:28 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=257

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter` が 257 と突出して高い**のは、RnBストラテジーのシグナル生成ループが短周期で連続実行され、direction_filterによるブロックが累積し続けていることを示す
- ログに `spike bypass: fib_reversal (16018.8pip/60s)` および `velocity_up bypass: fib_reversal (16017pip)` が複数回出現しており、**異常な価格変動値（≒16000pip）がUSDJPYで検出されている**。これはティックデータの異常値（NaN→数値変換ミス、前回終値との差分計算バグ等）の可能性が高い
- direction_filterが「売りシグナルのみ連打」を弾いている構図と一致する（SELL USD_JPY が繰り返しブロックされている）
- `recent_emit bypass` が秒単位（12s < 60s）で発生しており、**シグナル生成そのものがスロットリングを超えて高頻度で呼ばれている**

### 推測（中確度）

- `main_loop_restarts: 1` があり、再起動タイミングで内部カウンタがリセットされず、direction_filterのカウントが二重計上されている可能性
- `entry_type: "unknown"` がdaytradeのlast_signalに出ており、シグナル分類ロジックに未処理パスがある

---

## 確認事項

- [ ] USDJPYの生ティックデータに異常値（極端なスパイク）が混入していないか → データソース・ブローカーAPIのraw値を直接確認
- [ ] direction_filterカウンタのリセット条件（セッション切替・再起動時）が正常に動作しているか
- [ ] spike bypassの `16018pip` の出所：前回価格がゼロまたはNaNになっていないか（価格差分計算の初期化不備）
- [ ] シグナル生成ループのインターバル設定がrnb_usdjpyで意図通りか（他ストラテジーと比較）
- [ ] `main_loop_restarts: 1` の再起動原因ログを確認（例外・OOM等）

---

## 対処方針

- **即時対応**
  - `rnb_usdjpy` のシグナル生成を一時停止し、異常spike bypassが継続していないか確認
  - USDJPYティックデータの異常値フィルタ（例：前回比5%超の変動を弾くガード）が機能しているか検証・強制適用

- **短期対応**
  - direction_filterカウンタに**上限キャップ**または

## Alert: 16:23 UTC

### 検出された問題
- 💰 **含み益+30.5pip** (6ポジション, LIVE=0 Shadow=6) | Top: sr_break_retest×GBP_USD +11.6pip | xs_momentum×EUR_USD +11.2pip | xs_momentum×EUR_USD +4.4pip
- ⚠️ **Shadow含み益+31.0pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】Shadow→LIVE昇格が阻害されている原因

- **`r2_shadow_demoted_cell`** が `scalp`・`scalp_eur`・`scalp_5m_gbp` 系で大量発生（計85件）しており、該当ストラテジーのセルがデモート状態に固定されている可能性が高い
- **`sr_break_retest×GBP_USD`** と **`xs_momentum×EUR_USD`** はShadowで+10pip超の含み益を出しているにもかかわらず、対応するペア（GBP_USD / EUR_USD）が `PAIR_PROMOTED` リストに未登録であるため、LIVE昇格の条件を満たせていない
- `hedge_block` が daytrade 系全般で大量（daytrade_eur:64件、daytrade_eurgbp:52件など）発生しており、新規LIVE発注の経路が塞がれている
- `same_price_*pip` ブロックが複数ストラテジーで繰り返し発生 → 現在の価格帯が直近エントリー価格に近すぎてフィルタに引っかかり続けている

---

### 【確認が必要な事項】

| 確認項目 | 確認先 |
|---|---|
| `PAIR_PROMOTED` の現在の登録ペア一覧 | 設定ファイル / 環境変数 |
| `r2_shadow_demoted_cell` のデモート判定ロジック・解除条件（手動リセットが必要か） | システム設計仕様 |
| `xs_momentum×EUR_USD` と `sr_break_retest×GBP_USD` がどの昇格条件を満たしていないか（pip閾値 / 時間 / 勝率） | 昇格ロジック定義 |
| `hedge_block` の発動根拠となっているポジション（反対方向のLIVEポジションが残存していないか） | OANDA現在ポジション |
| `main_loop_restarts=1` の再起動タイミングと原因 | システムログ |

---

## 対処方針

### 即時対応
- **GBP_USD / EUR_USD を `PAIR_PROMOTED` に追加**する。Shadow含み益が+31pipに達しており、昇格遅延の機会損失が明確
- `r2_shadow_demoted_cell` の累積件数が異常に多いため、**デモート状態のセルを手動でリセット**し、再評価サイクルに乗せる

### 短期対応
- `hedge_block` の発動源となっている**残存ポジションをOANDAで確認**し、意図しないヘッジ状態であれば手動クローズ
