# Trade Monitor Alerts: 2026-07-03

## Alert: 01:29 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=218

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=218`** は他のブロックカウント（最大166）と比較して突出して高く、**方向フィルタが長時間・高頻度でシグナルを棄却し続けている**状態
- `rnb_usdjpy`（レンジブレイク系）が生成するシグナル方向と、上位時間足または外部フィルタが判定するトレンド方向が**持続的に不一致**になっている可能性が高い
- ログの `[MTF_MONITOR] USD_JPY ... mtf=trend_up_strong d1=2 h4=1` から、**D1・H4ともに強い上昇トレンド**が確認されており、レンジブレイク戦略が逆張り（SELL方向）シグナルを多発→フィルタで全棄却、という構図が推測される
- `daytrade`側では BUY ポジション `a194a0c9` が含み益を維持しており、相場は上方向に動いている＝rnbがSELLを出し続けている構図と整合

### 推測（中確度）

- `direction_filter` のリセット条件（トレンド転換 or 時間経過）が満たされておらず、カウンタが**セッションを通じて積算**されている可能性
- rnb_usdjpyのモデルまたはパラメータが現在の強トレンド相場に**未適応**（レンジ想定で設計されているため）

---

### 確認事項（要調査）

- `rnb_usdjpy` の直近シグナル方向（BUY/SELL）の内訳ログ確認 → 大半がSELL棄却か否か
- `direction_filter` の判定ソース（MTFフィルタ or 独自フィルタ）と閾値設定の確認
- `block_counts` のリセットタイミング（セッション単位 or 起動単位）の確認 → 218が1セッション分の積算か複数セッションまたいでいるか
- `rnb_usdjpy` モードの `last_signal` 内容（ステータスがトランケートされており未確認）

---

## 対処方針

- **緊急度：低〜中**（システムは正常動作中、フィルタが意図通り機能している可能性が高い）

1. `rnb_usdjpy` の直近シグナルログを抽出し、棄却方向の偏りを定量確認する
2. 現在の相場コンテキスト（強上昇トレンド）がrnbの動作想定レンジ相場と乖離していることを記

## Alert: 08:43 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=210

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=210 の主因候補

- **MTFバイアスとの方向不一致が継続している**
  ログに `mtf=trend_up_strong d1=2 h4=1` が確認できる。上位足が強いBUYバイアスを示している状態で、rnbモードがSELL方向のシグナルを繰り返し発生させ、direction_filterに弾かれ続けている可能性が高い
- **rnb_usdjpy固有の方向判定ロジックが上位足トレンドと逆行するシグナルを大量生成している**
  他モードのdirection_filterカウントが0または存在しない中、rnb_usdjpyだけが210と突出しており、シグナル生成ロジック自体が現在の相場環境（強い上昇トレンド）に適合していない状態と推測される
- **カウンターがリセットされていない（累積値の異常）**
  main_loop_restartsが1であり、再起動後もカウンターが引き継がれている場合、実際のブロック頻度より数値が膨らんでいる可能性もある

---

### 【確認事項】（推測と分離）

1. **rnb_usdjpyのdirection_filterの判定基準**を確認する
   - フィルタ参照元が何か（D1/H4のどちらか、またはその組み合わせか）
   - 判定閾値が現在のMTF状態と整合しているか
2. **カウンターのリセット仕様を確認する**
   - 起動時にゼロリセットされているか、それとも永続カウントか
   - 直近N分あたりのブロック頻度に換算するとどの程度か（異常判定閾値の妥当性）
3. **210という数値が「今セッション内」で発生したものか確認する**
   - log_count=9724に対して210はおよそ2%。他モードと比較して相対的に異常か絶対値として異常かを切り分ける
4. **rnbモードの最終エントリー成立時刻・PnLを確認する**
   - direction_filterで全ブロックされているならポジション無しのはずだが、意図通りか確認

---

## 対処方針

- **即時対応（低リスク）**
  - rnb_usdjpyモードのシグナル生成頻度および方向分布をログから集計し、何割がdirection_filterで弾かれているか定量化する
  - カウンターがリセット対象かを確認し、されていなければ手動リセット後に再計測して実

## Alert: 13:52 UTC

### 検出された問題
- 🔴 **含み損-36.3pip** (10ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 【推測】含み損-36.3pip（10ポジション）の主因

- **ヘッジブロックの多発**（daytrade_eur:28件、daytrade_gbpusd:22件など）により、本来ヘッジで損失を抑えるべきポジションが新規エントリーできず、既存ポジションが一方向にさらされている可能性
- **same_price_0pip/5pipブロック**（eurgbp:18件、gbpusd:6件）が連続発動しており、価格が特定レンジに滞留＝損切りラインに向けてじりじり動いているレンジ相場の可能性
- `ma_regime_switch` のSELLシグナルが `EV=-2.486` かつスパイク判定（16125pip/60s）でシャドウ追放されており、**急激な価格変動が直前に発生した**可能性
- `mtf=trend_up_strong` なのにSELLエントリーしているポジションが含み損の一因である可能性（MTFトレンドと逆張り）
- `rnb_usdjpy:direction_filter` が27件と突出しており、**USDJPYが一方向トレンドに入っている**にもかかわらず逆方向ポジションを保有している可能性

### 【推測】システム挙動面

- `R2_SHADOW_DEMOTE`（EUR_USD, ema_trend_scalp）が短時間で連続発生しており、EURUSDで**シャドウセルが正常セルに昇格できない状態が固着**している可能性
- `squeeze_release_momentum` が `same_price_0pip` で繰り返しブロックされており、**スクイーズ後のブレイクを検出しているが同一価格判定で弾かれ続けている**（価格が動いていない or tick更新が停滞）

---

## 確認事項（推測ではなく要確認）

- [ ] **10ポジションの内訳**：通貨ペア・方向・エントリー価格・現在損益を個別確認
- [ ] **MTF方向とポジション方向の整合性**：`trend_up_strong` と逆方向ポジションが何件あるか
- [ ] **OANDAのポジション実態**：システム認識と実際のブローカー保有数に乖離がないか
- [ ] **スパイク発生の実際の価格履歴**：16125pip/60sは異常値。ティックデータまたはfeed障害でないか確認
- [ ] **same_price_0pipブロックの価格更新状況**：tick/feedが止まっていないか（価格フィード障害の疑い）
-
