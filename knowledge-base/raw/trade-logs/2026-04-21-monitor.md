# Trade Monitor Alerts: 2026-04-21

## Alert: 04:38 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=491, daytrade_gbpusd:score_gate=390, daytrade_eur:score_gate=385, daytrade_eurgbp:session_pair=292, scalp_eur:session_pair=264

### 診断
## 原因分析

### 【推測】異常カウントの根本原因

**1. rnb_usdjpy:direction_filter=491（最多）**
- RNBストラテジーの方向フィルターが現在のUSDJPY相場のトレンド方向と恒常的に不一致
- フィルター閾値が現在のレンジ・ボラティリティ環境に対して過剰に厳格化されている可能性
- または方向判定ロジックのシグナルソース（例：上位足トレンド）が誤った値を返し続けている可能性

**2. daytrade_gbpusd:score_gate=390 / daytrade_eur:score_gate=385**
- `sr_fib_confluence score=-4.80`、`unknown score=-1.25` がログに頻出しており、スコアが構造的にマイナス圏に張り付いている
- "unknown" エントリータイプが score=-1.25 を返しているのは、エントリータイプ未定義時のデフォルト値の可能性
- スコア計算に使われる指標（ファンダメンタルスコア、ボラティリティ補正値等）がアジア時間帯の薄商いで異常値を掴んでいる疑い

**3. daytrade_eurgbp:session_pair=292 / scalp_eur:session_pair=264**
- 現在時刻04:38がアジア時間帯であり、EURGBPおよびEUR系スキャルプの `active_hours` 定義（欧州・ロンドン時間）と完全に不一致
- シャドウ処理は正常動作しているが、**そもそもアジア時間帯にシグナル生成ループ自体が回り続けている**ことが無駄なカウント積み上げの原因

---

### 【確認事項】（推測ではなく要検証）

- `rnb_usdjpy:direction_filter` のフィルター判定に使っている上位足データ（D1/H4）の取得値が正常かどうか
- `daytrade_gbpusd` / `daytrade_eur` のスコア計算で "unknown" エントリータイプが発生している箇所の特定（エントリータイプ分岐の漏れか、データ未取得か）
- `sr_fib_confluence score=-4.80` の固定値が、特定の相場条件下でハードコードされた値を返していないか
- `main_loop_restarts=2` の再起動タイミングと block_count 急増のタイミングが一致しているか（再起動後に内部状態リセットが不完全な可能性）
- `active_hours` の設定がサーバータイム

## Alert: 15:03 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=399, daytrade_eur:score_gate=300, daytrade_eurgbp:score_gate=231

### 診断
## 原因分析

### 推測（高確度）

- **rnb_usdjpy:direction_filter=399**
  - RnBストラテジーの方向フィルターが現在の相場環境（トレンド方向）と一致するシグナルをほぼ全件弾いている
  - 直近の価格アクションがフィルター条件（例：EMA傾きや高値安値構造）と強く逆行している可能性が高い
  - シグナル生成自体は正常に動作しており、フィルターが意図通り機能している可能性あり（バグではなく仕様通りの動作）

- **daytrade_eur:score_gate=300 / daytrade_eurgbp:score_gate=231**
  - ログより `trendline_sweep score=-5.97` および `session_time_bias score=-5.63` が繰り返しブロック
  - スコアが大幅なマイナスで固定されており、単発ノイズではなく相場環境がスコアリングモデルと長時間乖離していると推測
  - daytrade_eurgbp は `Sentinel bypass (is_shadow will be enforced)` が連発 → shadowモード強制により実取引はされていないが、ブロックカウントが積み上がっている

- **共通要因**
  - 15:00〜15:02の約2分間で大量カウント増加 → シグナル生成ループのインターバルが極端に短いか、同一シグナルが重複発火している可能性

---

### 推測（中確度）

- `main_loop_restarts=1` が記録されており、再起動後にカウンターがリセットされずに引き継がれている可能性
- rnb_usdjpy の direction_filter=399 は異常に高く、フィルタロジック内で状態がリセットされないまま蓄積しているバグの可能性もある

---

### 確認事項

1. **rnb_usdjpy:direction_filter**
   - direction_filterの判定条件（EMA/ADX/高値安値など）の現在値を確認し、意図通りの相場認識をしているか検証する
   - カウンターが `main_loop_restart` をまたいでリセットされているか確認する
   - シグナル発火頻度のログを確認し、異常な重複発火がないか確認する

2. **daytrade_eur / daytrade_eurgbp:score_gate**
   - `trendline_sweep` と `session_time_bias` のスコアが固定値（-5.97, -5.63, -1.15）のまま変化していないか確認 → モデル入力データがスタールしていないか
   - is_shadowフラグの発動条件と、Sentin

## Alert: 16:46 UTC

### 検出された問題
- 💰 **含み益+46.2pip** (6ポジション, LIVE=0 Shadow=6) | Top: sr_break_retest×USD_JPY +16.4pip | vwap_mean_reversion×EUR_USD +14.2pip | stoch_trend_pullback×EUR_USD +9.0pip
- ⚠️ **Shadow含み益+48.2pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】根本原因

**Shadow 6ポジション / LIVE 0ポジション** という状態の主因は以下の複合ブロックと判断される：

1. **Exposure上限による全方位ブロック**
   - JPY net exposure が 30,000u・40,000u でそれぞれ上限(20,000u)を大幅超過（計59件）
   - USD net exposureも同様にブロック（6件）
   - これが最大の発注抑制要因と推測

2. **`rnb_usdjpy:direction_filter` の異常多発（174件）**
   - 全ブロック中で突出して最多
   - USD_JPY方向フィルターが長時間にわたり一方向を拒否し続けている可能性
   - `sr_break_retest×USD_JPY`が最高含み益を出しているにもかかわらずLIVE昇格できていない主因の一つと推測

3. **`daytrade_eur:score_gate`（110件）＋`session_time_bias score=-5.34`**
   - EUR_USD系が時間帯バイアスにより全シグナルを継続ブロック中
   - 16:45時点（欧州序盤）でスコアが恒常的にマイナスになっている

4. **`daytrade_eurgbp:same_price_0pip`（44件）＋`spread_guard`（36件）**
   - EUR_GBPは価格変動の乏しさとスプレッド拡大で二重ブロック
   - Sentinel bypassでshadow扱いにはなっているが、LIVE昇格条件を満たせていない

---

## 確認事項（推測でなく要実確認）

| # | 確認項目 | 確認場所 |
|---|----------|----------|
| 1 | JPY/USD net exposureがなぜ上限を超えているか（既存LIVE建玉の残存？手動ポジションの混入？） | OANDA口座の現在ポジション一覧 |
| 2 | `rnb_usdjpy:direction_filter`の判定条件・現在のレジーム判定値 | rnb_usdjpy設定ファイル／レジーム判定ログ |
| 3 | `session_time_bias`のスコア計算基準と-5.34が正常値かどうか | score計算ロジック、時間帯テーブル |
| 4 | `main_loop_restarts: 1` の原因（何時に再起動したか、エラー内容） | システムログ（起動前後の前後30秒） |
| 5 | Shadow→LIVE昇格の`PAIR_PROMOTED`追加判断基準（閾値・

## Alert: 18:54 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=212

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filterが212回と突出している理由

- **方向性フィルターが一方向（BUYまたはSELL）を継続的に拒否し続けている**
  - rnb_usdjpy戦略が出力するシグナル方向と、direction_filterが参照するトレンド・レジーム判定が長時間乖離している状態と思われる
  - 他のblock_countが概ね10〜170台の中、212は約1.2〜17倍の水準であり、通常の一時的ブロックではなくフィルター条件がほぼ恒常的にヒットしている

- **USDJPYが強いトレンド相場またはレンジ転換点にあり、rnb戦略のシグナル方向が市場構造と逆を向き続けている可能性**
  - daytrade側でも `regime_trend_bull_dt_tf`（15件）や `velocity_down`（12件）が出ており、レジーム判定が強く働いていることと一致する

- **direction_filterが参照する上位足レジームまたは外部フィルター変数が固着（更新されていない）している可能性**
  - main_loop_restartsが1回あり、再起動後にフィルター状態が初期化されなかった、または再計算前の古い値を保持し続けている疑いがある

---

### 【確認事項】（推測と分けて列挙）

1. **direction_filterの判定ロジック確認**
   - 何の指標・条件を参照しているか（上位足MA、ADX、レジームフラグ等）
   - フィルター値が最後に更新された時刻（main_loop再起動前後で変化があるか）

2. **再起動（main_loop_restarts=1）との因果関係確認**
   - 再起動前後でblock_countが急増していないかタイムスタンプで確認
   - 再起動後にdirection_filterの状態変数がリセット・再計算されているかコード確認

3. **USDJPYの市場状況確認**
   - 現在のUSDJPY実勢レート・ボラティリティが通常域か確認（159.469付近）
   - rnb戦略が出力しているシグナル方向（BUY/SELL）の内訳確認

4. **rnb_usdjpy戦略のシグナル出力頻度確認**
   - そもそもシグナル自体の発生回数が多いのか、それともブロック率が高いのかを区別する

---

## 対処方針

- **緊急度：中（システム停止はしていないが、戦略が実
