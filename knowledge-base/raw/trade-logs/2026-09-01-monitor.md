# Trade Monitor Alerts: 2026-09-01

## Alert: 14:21 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=497, daytrade_eur:hedge_block=278, daytrade:hedge_block=261, scalp:r2_shadow_demoted_cell=259, scalp_eur:r2_shadow_demoted_cell=233

### 診断
## 原因分析

### 推測（ログ・カウントから高確度で推定）

**1. `rnb_usdjpy:direction_filter=497`（最多・最重症）**
- RnBストラテジーがUSDJPYで方向フィルターに大量衝突している
- 市場のトレンド方向とストラテジーの想定方向が長期間乖離しており、フィルターが一方向にほぼ全拒否している状態
- 単純なレンジ逆張りロジックが、強いトレンド相場に対応できていない可能性が高い

**2. `daytrade_eur:hedge_block=278` / `daytrade:hedge_block=261`**
- ヘッジポジションが長時間解消されず、新規エントリーをブロックし続けている
- ログに`hull_donchian_fade`が`score=3.47, misalign:SELL`で繰り返しブロックされており、ヘッジ状態が固着している
- EUR_USD方向のスコアと実シグナル方向が逆転したまま放置されている

**3. `scalp:r2_shadow_demoted_cell=259` / `scalp_eur:r2_shadow_demoted_cell=233`**
- `vol_momentum_scalp × USD_JPY`セルが繰り返しR2シャドウに降格されており、そのセルのモデルが現在の相場ボラティリティに適合していない
- ログで同一セルが数秒おきに連続ブロックされており、降格→復帰→再降格のサイクルが高速で発生している疑い

**4. `ORDER_BAR_DEDUP`の連続発生**
- `trendline_sweep GBP_USD`と`wick_imbalance_reversion AUD_JPY`が同一バー(`bar_ts=14:15:00`)を起点にシグナルを出し続けている
- バーが更新されていないのにシグナルが再生成されているか、dedup判定のキーが機能していない

---

### 確認事項（要実機確認）

- `rnb_usdjpy:direction_filter`の閾値パラメータが現在のトレンド強度に対して適切か（ADXや傾き指標との乖離確認）
- ヘッジポジションの現在状態：`daytrade_eur` / `daytrade`でオープン中のヘッジが実際に存在するか、それとも解消済みなのにフラグが残っているか（フラグ固着バグの可能性）
- `vol_momentum_scalp × USD_JPY`セルの直近R2スコア推移ログ（正常降格か、境界値付近でのチャタリングか）
- `bar_

## Alert: 18:55 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=947, daytrade_eur:hedge_block=516, scalp_eur:r2_shadow_demoted_cell=500, scalp:r2_shadow_demoted_cell=495, daytrade:hedge_block=419

### 診断
## 原因分析

### 【推測】主因

1. **rnb_usdjpy:direction_filter=947（最大値）**
   - RnBストラテジーがUSD/JPYで一方向シグナルを大量発生させており、方向フィルターが連続発動している
   - 相場が強トレンド中か、シグナルロジックが特定方向に偏った出力をし続けている可能性

2. **r2_shadow_demoted_cell多発（scalp系・daytrade系横断）**
   - shadow-tracking cellが複数通貨ペア（USD/JPY・EUR/USD・USD/CHF）・複数ストラテジー（bb_rsi_reversion・fib_reversal・engulfing_bb・vol_momentum_scalp・sr_channel_reversal）で同時にdemote状態に陥っている
   - R2評価が全体的に閾値を下回っており、**相場環境そのものがモデルの想定外のレジームに入っている**可能性が高い

3. **daytrade系:hedge_block多発（eur=516、main=419、gbpusd=333等）**
   - 既存ポジションとヘッジ方向に合わないシグナルが大量生成されており、hedge_blockが常態化している
   - `[HOLD] DT含み益保護`ログと合わせると、**含み益ポジションを抱えたまま逆シグナルが頻発**している状態

4. **daytrade_1h_eurgbp:stalest_age=54.6秒**
   - 1hタイムフレームのEUR/GBPモードでティック処理が遅延している

---

### 【確認事項】（未確認のため要検証）

| 確認項目 | 確認方法 |
|---|---|
| USD/JPYが強トレンド・急騰急落中か | チャート・ボラティリティ指標を確認 |
| R2モデルの直近再学習日時・性能劣化の有無 | モデルメタデータ・バックテストログ |
| shadow_demote解除条件（復帰閾値）が正常に機能しているか | R2_SHADOW_DEMOTEのpromoteログが存在するか確認 |
| hedge_block頻発の起点ポジション（48ca660f）のエントリー時刻と現在損益 | ポジション管理テーブル |
| daytrade_1h_eurgbpのスタール原因（データフィード遅延 vs 処理負荷） | 当該モードのスレッド・キュー状態 |
| direction_filterの判定パラメータ（閾値・ルックバック期間）が変更されていないか | 設定ファイルのdiff |

## Alert: 22:08 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=1292, scalp:r2_shadow_demoted_cell=688, scalp_eur:r2_shadow_demoted_cell=669, daytrade_audjpy:hedge_block=576, daytrade_eur:hedge_block=516

### 診断
## 原因分析

### 【推測】各ブロックの発生原因

**1. `rnb_usdjpy:direction_filter=1292`（最多・最重要）**
- RNBストラテジーがUSD/JPYに対して一方向（BUYまたはSELL）のシグナルを出し続けているが、direction_filterが反対方向を禁止している状態が長期間継続中
- トレンド相場またはレンジブレイク後の強い一方向性が原因で、フィルター閾値と市場の方向性が恒常的に不一致を起こしている可能性が高い

**2. `scalp:r2_shadow_demoted_cell=688` / `scalp_eur:r2_shadow_demoted_cell=669`**
- スキャルピング系でR2シャドーセルの降格が多発。ボラティリティや価格挙動がモデルの想定レンジから外れており、シャドートラッキングセルが量産→即降格のループに入っている可能性
- ログに `vol_momentum_scalp x USD_JPY`、`sr_channel_reversal x USD_CHF` が頻出しており、特定ペア＋特定ロジックで集中発生している

**3. `daytrade_audjpy:hedge_block=576` / `daytrade_eur:hedge_block=516`**
- ヘッジポジションが解消されないままシグナルが発火し続けている
- または、既存ポジションとの方向矛盾が継続しており、hedge_blockが正常に機能して遮断しているが件数が異常に多い
- SCORE_GATEログの `ny_close_reversal score≒-1.81〜-1.82 vs BUY` が連続しており、daytrade_eurはスコアとシグナル方向の乖離が固着している

---

### 【確認事項】（推測と分離）

- [ ] `rnb_usdjpy` のdirection_filterの閾値設定値と、現在のUSD/JPYの実勢トレンド方向を照合する
- [ ] hedge_blockが高いモード（audjpy, eur）で、実際にヘッジポジションが残存しているかブローカー側のポジション一覧を確認する
- [ ] `ny_close_reversal` スコアが-1.8台に張り付いている原因を確認（スコア計算のインプットデータが古い・固着していないか）
- [ ] `engine_tick_stalest_age_sec=61.5 / daytrade_1h` について、1hモードのティック更新が遅延している原因を確認（データフィード断続かバッチ遅延か）
- [ ] `candidate_row` の最終更新が21分前（21:
