# Trade Monitor Alerts: 2026-04-20

## Alert: 07:27 UTC

### 検出された問題
- 💰 **含み益+34.1pip** (6ポジション, LIVE=1 Shadow=5) | Top: vwap_mean_reversion×GBP_JPY +20.0pip | vwap_mean_reversion×EUR_JPY +7.7pip | dual_sr_bounce×GBP_USD +6.8pip
- ⚠️ **Shadow含み益+15.0pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】主要原因

**① GBP_JPY がShadow止まりの主因：`daytrade_gbpjpy` スロット枯渇**
- `Slot bypass: unknown daytrade_gbpjpy/GBP_JPY (1/1 → shadow)` が繰り返し発生
- LIVEスロットが1/1で満杯のため、後続シグナルが全てShadow送りになっている
- `daytrade_gbpjpy:score_gate` ブロック数118件・`exposure:same-direction` 12件も重なり、新規LIVEエントリーの経路が事実上閉塞

**② EUR_USD シグナルのShadow化：MTFコンフリクト + EVマイナス**
- `mtf=trend_up_strong` に逆らうSELLシグナル（`sr_channel_reversal`）でEV=-2.714
- H7ブロックも加わり、正当なフィルタリング結果と見られる

**③ USD_JPY `vix_carry_unwind` SELL の連続ブロック**
- score=-4.00〜-4.15でSCORE_GATE閾値（0）を大幅に下回る
- `rnb_usdjpy:direction_filter` が163件と突出しており、USD_JPY全体に方向バイアスフィルターが強く掛かっている

**④ `SIGNAL_REVERSE` が含み益保護により無効化**
- 1d2a3653（+0.20pip相当）・186ab5fe（+0.077pip）が複数回HOLD保護発動
- 既存ポジションが益を抱えたまま滞留し、スロットとエクスポージャーを占有し続けている

---

### 【確認事項】（実機確認が必要）

- `daytrade_gbpjpy` のLIVEスロット上限値の設定値（現在1が上限か、それとも満杯なだけか）
- `PAIR_PROMOTED` リストに GBP_JPY・GBP_USD が現在含まれているか、および追加の承認フロー
- `rnb_usdjpy:direction_filter` が163件に達している根本設定（フィルター方向・期間・閾値）
- Shadow益+15.0pipの内訳ペア・戦略の詳細（上位5件のうちLIVE昇格候補を絞り込むため）
- 1d2a3653・186ab5feポジションのTP/SL設定と想定クローズ時刻（スロット解放タイミングの予測）
- `main_loop_restarts: 1` の再起動理由（ログ上の例外内容）

---

## 対処方針

**即時対応（運

## Alert: 15:59 UTC

### 検出された問題
- 💰 **含み益+24.5pip** (9ポジション, LIVE=0 Shadow=9) | Top: sr_fib_confluence×GBP_USD +20.2pip | dt_fib_reversal×EUR_JPY +12.5pip | bb_rsi_reversion×EUR_USD +1.0pip
- ⚠️ **Shadow含み益+35.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】根本原因

**1. Shadow専用動作による実注文ゼロ**
- LIVE=0 / Shadow=9 という状態は、対象ペア・ストラテジーが `PAIR_PROMOTED` に未登録のため、シグナルが出ても実注文に昇格できていない
- `shadow_tracking` スキップログ（`ema_trend_scalp`）がこれを裏付けている

**2. SCORE_GATEによる大量ブロック**
- `daytrade` / `daytrade_eurjpy` / `daytrade_eurgbp` でスコアが一貫して大幅マイナス（-1.3〜-2.9）
- 相場環境がスコアモデルの想定外である可能性（MTFログに `range_tight` / `vol=squeeze` / `d1=0, h4=-1` が見られる）
- `rnb_usdjpy:direction_filter` 84件も方向性不一致を示しており、スコア低下の一因と推測

**3. USD/GBPエクスポージャー上限への抵触**
- `daytrade_eur` でUSD net 40k〜60k超（制限20k）が合計38件
- `daytrade_gbpusd` でGBP net 30k〜40k超（制限20k）が42件
- 既存ポジションが含み益保護でホールド中であるため、新規エントリーがブロックされ続けている

**4. DT含み益保護の連続発火**
- `cd070f56`（profit=0.125）・`c0efb17b`（profit=0.002）がSIGNAL_REVERSEを繰り返し無効化
- 保護自体は正常動作だが、ポジションが長期間残ることでエクスポージャーを圧迫している

---

### 【確認事項】（推測と分離）

- [ ] Shadow=9のポジションが含む具体的なペア・ストラテジーの一覧（`PAIR_PROMOTED` への追加候補を確定するために必要）
- [ ] スコアモデルの特徴量定義において `range_tight` / `squeeze` 環境が想定内かどうか（スコアが構造的に出ない相場か、一時的か）
- [ ] `main_loop_restarts=1` の再起動原因（例外ログ・OOM・外部APIタイムアウトなど）
- [ ] `cd070f56` ポジションのエントリー時刻・TP設定（+12.5pipで利確されないのはTP設定値の問題か、意図的なトレーリングか）
- [ ] エクスポージャー上限（20k）の設定が現在の市場ボラ
