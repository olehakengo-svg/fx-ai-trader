# Trade Monitor Alerts: 2026-06-22

## Alert: 02:29 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=495

### 診断
## 原因分析

### 推測（ほぼ確実）

- **`rnb_usdjpy:direction_filter` が 495 カウント**は、他のブロック項目（最大でも 119）と比較して**1桁以上の差**があり、明らかな異常値
- `rnb_usdjpy` の direction_filter が**長時間・継続的にシグナルを全拒否**している状態と推測される
- 原因候補：
  - USDJPY が方向性フィルターの閾値条件（例：トレンド強度・バイアス判定・ADX等）を**長期間満たせない相場環境**に入り込んでいる
  - direction_filter の判定ロジックが**何らかのデータ異常・計算不整合**により常にNGを返している（ロジックバグ or 入力データ固着）
  - フィルター条件が**過去の相場環境向けにチューニングされており、現レンジ相場（161円台膠着）に対して過剰反応**している

### 推測（補足・やや不確か）

- ログに `rnb_usdjpy` 関連のエントリが**一切出ていない**ことから、フィルターが手前で全遮断しており、下流のロジックに到達していない可能性が高い
- `daytrade` の last_signal が `WAIT / confidence=20` であり、USDJPY全体として**方向感の乏しい状態**が続いていることとも整合する
- main_loop_restarts=1 があるが、direction_filter カウントの異常はそれ以前から蓄積していた可能性あり

---

## 確認事項

1. **direction_filter の判定根拠となる指標値**（ADX、EMA傾き、バイアス値など）を直接確認し、正常値範囲内か検証する
2. **カウントがいつから急増し始めたか**をログ・タイムスタンプで確認する（再起動前後か、特定時刻からか）
3. direction_filter に渡される**入力データ（OHLC・インジケーター）が固着・欠損していないか**確認する
4. `rnb_usdjpy` の**フィルター閾値設定値**が意図通りのパラメータになっているか確認する（デプロイ時の設定ミスの可能性）
5. 他通貨ペアの `direction_filter` カウントが**正常範囲内かどうか**比較し、USDJPY固有の問題か共通ロジックの問題かを切り分ける

---

## 対処方針

- **即時対応**
  - direction_filter の現在の判定値・入力パラメータをログ出
