# Trade Monitor Alerts: 2026-07-21

## Alert: 01:19 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=223, daytrade_gbpusd:hedge_block=221

### 診断
## 原因分析

### 推測（ログ・カウントから高確度で推定）

**rnb_usdjpy:direction_filter=223**
- USDJPYが長時間、方向性フィルター（トレンド方向チェック）に弾かれ続けている
- ADX=13.4〜13.5という極めて低い値から、**レンジ・膠着相場**が続いており、direction_filterが機能しているのは正常動作の可能性が高い
- ただしカウントが200超は「同一条件でシグナルが大量生成されている」ことを示す。シグナル生成側の問題（無限ループ的な再評価）を疑う

**daytrade_gbpusd:hedge_block=221**
- GBP/USDのBUYシグナルが、既存ポジションとの方向衝突（ヘッジブロック）により弾かれ続けている
- ログ上で`squeeze_release_momentum`が約30秒おきに**同一スコア・同一ADXで繰り返し生成**されている（Score:+2.89、ADX:13.4固定）
- これは**シグナルの冪等性チェック漏れ**、または`recent_emit`抑制が当該モードに効いていないことを強く示唆
- ヘッジブロック自体は正常に機能しているが、シグナル生成が止まらないためカウントが積み上がっている

---

### 確認事項（要調査）

- [ ] `daytrade_gbpusd`モードに`recent_emit`フィルターが設定・有効化されているか（他モードには存在するが当該モードのカウントがゼロ）
- [ ] `rnb_usdjpy`のdirection_filterが発火するループ周期と、シグナル評価周期が一致していないか（過剰評価になっていないか）
- [ ] GBP/USDの既存ヘッジポジションが意図的に保持されているものか、クローズ漏れではないか
- [ ] `main_loop_restarts=1`の再起動タイミングと、カウント急増のタイミングが一致するか
- [ ] block_countのリセットタイミング（起動からの累積か、直近N分かを確認し閾値の妥当性を再評価）

---

## 対処方針

**即時対応**
- `daytrade_gbpusd`に`recent_emit`または同一シグナル重複抑制フィルターが適用されているか確認し、未適用なら他モードと同等の設定を追加する
- GBP/USDのヘッジポジション状態を手動確認し、クローズ漏れであれば決済処理を行う

## Alert: 04:55 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=227

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=227 の主因

**direction_filterが異常に高カウント（227）になっている根本原因として以下を推測：**

- `rnb_usdjpy` モードが現在の相場環境（レンジ・方向感なし）に対して、シグナル自体は大量生成しているが、フィルター条件（トレンド方向一致チェック）に片っ端で引っかかっている
- ADX=16.9（ログより）というレンジ相場環境で、`rnb`（Range & Breakout系と推定）のシグナルが方向フィルターを通過できない状態が長時間続いている
- direction_filterの閾値または参照するトレンド判定ソース（L1=neutral）が、現在の低ADX環境では常時ブロックするロジックになっている可能性

---

### 【推測】副次的な異常

- `daytrade_gbpusd:hedge_block=69` / `order_bar_dedup=75` が突出しており、GBPUSDで同一方向ポジションが長時間保持されたまま新規シグナルが連続ブロックされている疑い
- `scalp:r2_shadow_demoted_cell=60` / `scalp_eur:r2_shadow_demoted_cell=54` はsr_channel_reversalセルが連続デモートされており、SRライン評価が不安定になっている可能性
- `main_loop_restarts=1` は軽微だが、再起動タイミングでカウンターがリセットされていない場合、累積カウントが混在している懸念あり

---

## 確認事項

### 優先度：高
- [ ] `rnb_usdjpy` の direction_filter 判定ロジックで参照しているトレンド方向ソース（L1 / ADX閾値）の設定値を確認 — 特に「neutralをどう扱うか」のルール
- [ ] direction_filterカウントが**いつから急増したか**をログで確認（main_loop_restart前後か、相場急変タイミングか）
- [ ] `rnb_usdjpy` モードのシグナル生成頻度が正常範囲内かを確認（シグナル自体が異常多発していないか）

### 優先度：中
- [ ] `daytrade_gbpusd` のheadge_block長期化の原因となっているポジション状態を確認（ポジション方向・保有時間・ロットの正常性）
- [ ] `sr_channel_reversal` のR2スコアがGBPUSD・USDJPYで連続デモートされている閾値設定を確認
- [ ] カウンターリセットタイミングが

## Alert: 08:02 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=240

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=240`** は、他のblock_countsと比較して**桁違いに大きい**（次点の`scalp:r2_shadow_demoted_cell=98`の約2.5倍）
- `rnb_usdjpy`の方向性フィルターが**長時間・高頻度でシグナルを棄却し続けている**状態
- USDJPYが現在**レンジ相場または方向フィルターの閾値付近に張り付いている**可能性が高い
- `daytrade`モードのlast_signalがconfidence=20・WAIT・entry_type=unknownであり、**USD/JPYの方向性が取れていないことと整合**する

### 推測（中確度）

- `direction_filter`のパラメータ（閾値・参照する足・判定ロジック）が**現在の相場特性（低ボラ・方向感欠如）に対してタイトすぎる**
- `main_loop_restarts=1`があり、**再起動後にフィルター状態がリセットされずカウントが加速している**可能性
- フィルター判定が毎ループ呼ばれており、**ブロック自体は正常動作だがカウント上限チェックが機能していない**可能性

---

## 確認事項

- [ ] `rnb_usdjpy`の`direction_filter`の判定ロジック・閾値設定値を確認（何pips/何%の方向性を要求しているか）
- [ ] 直近のUSDJPY価格推移を確認（レンジ幅・トレンド有無）
- [ ] `direction_filter`のカウント計上タイミングを確認（シグナル生成のたびか、足確定のたびか）
- [ ] `main_loop_restarts=1`の発生時刻とカウント急増タイミングが一致するか
- [ ] 他の`rnb_*`系戦略が存在する場合、同様に高カウントになっていないか（rnb固有かUSDJPY固有かの切り分け）
- [ ] `direction_filter`に異常時の自動停止・上限アラート機構が実装されているか

---

## 対処方針

- **即時対応**
  - `rnb_usdjpy`の`direction_filter`判定ログを抽出し、何の条件で毎回棄却されているか特定する
  - カウントが増え続けているだけで実害（誤発注・資金流出）がないことを確認し、**緊急停止の必要性を判断**する

- **短期対応**
  - USDJPYが方向感のない相場局面

## Alert: 17:04 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=310, daytrade_eur:hedge_block=225

### 診断
## 原因分析

### 推測（ログ・数値から高確度で推定）

**① `rnb_usdjpy:direction_filter=310`（最大の異常値）**
- RNBストラテジーがUSD/JPYに対して方向フィルターを連続発動している
- 他の`hedge_block`系（daytrade: 87、scalp: 95など）と比較して310は突出しており、**フィルター条件が恒常的に満たされない状態**に陥っている可能性が高い
- USD/JPYが現在レンジ相場または強トレンド中で、RNBの方向判定ロジックとの乖離が継続している可能性
- `daytrade`の`last_signal.confidence=20`、`signal=WAIT`が傍証

**② `daytrade_eur:hedge_block=225`**
- EUR系通貨ペアで既存ヘッジポジションが長期間オープンのまま残存し、新規エントリーを連続ブロックしている
- 他EUR系（`daytrade_eurgbp:hedge_block=27`、`daytrade_eurjpy:hedge_block=114`）も高水準であり、**EUR全般でヘッジ解除が進んでいない**状況を示す
- ログの`[HOLD] DT含み益保護: SELL 2ff33b5f`が繰り返し発生しており、既存SELLポジションが利益保護ロジックでリバースを阻止し続けているのと整合する

**③ `R2_SHADOW_DEMOTE`の頻発**
- `ema_trend_scalp × USD_JPY / EUR_USD`が短時間に大量demote
- USD/JPY・EUR/USDでトレンドが頻繁に反転または不安定な状態にあり、シャドーセルの信頼性が低下している
- scalp系全般の`r2_shadow_demoted_cell`が高水準（scalp: 84、scalp_5m: 84）と一致

---

### 確認事項（要調査）

| 確認項目 | 目的 |
|---|---|
| `rnb_usdjpy`の方向フィルター判定ロジックのパラメータ（閾値・対象インジケーター） | フィルター固着の条件を特定 |
| `daytrade_eur`でヘッジポジション（ID: 2ff33b5f）のオープン時刻・ロット | ヘッジが意図通りに機能しているか確認 |
| `main_loop_restarts=1`の発生タイミング | 再起動前後でblock_countsがリセットされているか、310・225が再起動後に急増したか |
| USD/JPY・EURUSDの直近チ
