# Trade Monitor Alerts: 2026-07-21

## Alert: 01:19 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=223, daytrade_gbpusd:hedge_block=221

### 診断
## 原因分析

### 推測（ログ・カウントから高確度で推定）

**rnb_usdjpy:direction_filter=223**
- USDJPYが長時間、方向性フィルター（トレンド方向チェック）に弾かれ続けている
- ADX=13.4〜13.5という極めて低い値から、**レンジ・膠着相場**が続いており、direction_filterが機能しているのは正常動作の可能性が高い
- ただしカウントが200超は「同一条件でシグナルが大量生成されている」ことを示す。シグナル生成側の問題（無限ループ的な再評価）を疑う

**daytrade_gbpusd:hedge_block=221**
- GBP/USDのBUYシグナルが、既存ポジションとの方向衝突（ヘッジブロック）により弾かれ続けている
- ログ上で`squeeze_release_momentum`が約30秒おきに**同一スコア・同一ADXで繰り返し生成**されている（Score:+2.89、ADX:13.4固定）
- これは**シグナルの冪等性チェック漏れ**、または`recent_emit`抑制が当該モードに効いていないことを強く示唆
- ヘッジブロック自体は正常に機能しているが、シグナル生成が止まらないためカウントが積み上がっている

---

### 確認事項（要調査）

- [ ] `daytrade_gbpusd`モードに`recent_emit`フィルターが設定・有効化されているか（他モードには存在するが当該モードのカウントがゼロ）
- [ ] `rnb_usdjpy`のdirection_filterが発火するループ周期と、シグナル評価周期が一致していないか（過剰評価になっていないか）
- [ ] GBP/USDの既存ヘッジポジションが意図的に保持されているものか、クローズ漏れではないか
- [ ] `main_loop_restarts=1`の再起動タイミングと、カウント急増のタイミングが一致するか
- [ ] block_countのリセットタイミング（起動からの累積か、直近N分かを確認し閾値の妥当性を再評価）

---

## 対処方針

**即時対応**
- `daytrade_gbpusd`に`recent_emit`または同一シグナル重複抑制フィルターが適用されているか確認し、未適用なら他モードと同等の設定を追加する
- GBP/USDのヘッジポジション状態を手動確認し、クローズ漏れであれば決済処理を行う

## Alert: 04:55 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=227

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=227 の主因

**direction_filterが異常に高カウント（227）になっている根本原因として以下を推測：**

- `rnb_usdjpy` モードが現在の相場環境（レンジ・方向感なし）に対して、シグナル自体は大量生成しているが、フィルター条件（トレンド方向一致チェック）に片っ端で引っかかっている
- ADX=16.9（ログより）というレンジ相場環境で、`rnb`（Range & Breakout系と推定）のシグナルが方向フィルターを通過できない状態が長時間続いている
- direction_filterの閾値または参照するトレンド判定ソース（L1=neutral）が、現在の低ADX環境では常時ブロックするロジックになっている可能性

---

### 【推測】副次的な異常

- `daytrade_gbpusd:hedge_block=69` / `order_bar_dedup=75` が突出しており、GBPUSDで同一方向ポジションが長時間保持されたまま新規シグナルが連続ブロックされている疑い
- `scalp:r2_shadow_demoted_cell=60` / `scalp_eur:r2_shadow_demoted_cell=54` はsr_channel_reversalセルが連続デモートされており、SRライン評価が不安定になっている可能性
- `main_loop_restarts=1` は軽微だが、再起動タイミングでカウンターがリセットされていない場合、累積カウントが混在している懸念あり

---

## 確認事項

### 優先度：高
- [ ] `rnb_usdjpy` の direction_filter 判定ロジックで参照しているトレンド方向ソース（L1 / ADX閾値）の設定値を確認 — 特に「neutralをどう扱うか」のルール
- [ ] direction_filterカウントが**いつから急増したか**をログで確認（main_loop_restart前後か、相場急変タイミングか）
- [ ] `rnb_usdjpy` モードのシグナル生成頻度が正常範囲内かを確認（シグナル自体が異常多発していないか）

### 優先度：中
- [ ] `daytrade_gbpusd` のheadge_block長期化の原因となっているポジション状態を確認（ポジション方向・保有時間・ロットの正常性）
- [ ] `sr_channel_reversal` のR2スコアがGBPUSD・USDJPYで連続デモートされている閾値設定を確認
- [ ] カウンターリセットタイミングが
