# Trade Monitor Alerts: 2026-09-21

## Alert: 01:57 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=429

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:no_signal=429` が突出して異常値**
  - 他のブロック理由（hedge_block, order_bar_dedup等）は概ね数十〜百台であり、429という値は明らかに外れている
  - `no_signal` はシグナル生成ロジック自体が条件を満たせず発火できない状態を意味すると推定
  - rnb_usdjpy戦略固有の問題であり、エンジン全体や他通貨ペアには波及していない

- **シグナル枯渇の主因として考えられるもの（いずれかまたは複合）**
  - フィルタ条件が現在の相場レジームと乖離しており、恒常的にシグナル不成立になっている
  - rnb（レンジブレイク系と推定）戦略に必要なボラティリティ・レンジ幅が閾値を超えられていない
  - 参照する指標データ（ATR, レンジ算出元バー等）に欠損または異常値が混入している
  - 戦略パラメータ（閾値・ルックバック期間等）が直近の相場環境に合わず死に条件化している

### 推測（中確度）

- `daytrade_1h_nzdusd` のtick age が59.2秒と最遅であることは別問題の可能性が高いが、データフィード全体の遅延がrnb_usdjpyのシグナル判定タイミングにも影響している可能性は否定できない
- `ORDER_BAR_DEDUP` で `bar_ts=01:45:00` が繰り返しブロックされており、同一バーからのシグナル再送が頻発→ rnb_usdjpy以外の戦略でもシグナル生成ロジックが過剰にトリガーされる状態がシステム全体に存在する

---

## 確認事項（要調査）

1. **rnb_usdjpy の no_signal カウントの推移**
   - 今回が突発的な急増か、それとも以前から漸増しているか→ 時系列でカウント変化を確認
2. **rnb_usdjpy 戦略の入力データ確認**
   - シグナル判定に使うレンジ幅・ATR・ローソク足データが正常に供給されているか
   - NaNや欠損が混入していないか
3. **シグナル条件ロジックの最終変更履歴**
   - 直近のデプロイ・パラメータ変更がrnb_usdjpyに影響していないか
4. **現在のUSDJPYの相場

## Alert: 07:26 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=455, daytrade:hedge_block=297, scalp:r2_shadow_demoted_cell=278

### 診断
## 原因分析

### 推測（ログ・カウントから高確度で推定）

**① rnb_usdjpy:no_signal=455（最多）**
- USDJPYのRnBモジュールがシグナル生成条件を満たせない状態が長時間継続している
- streak_reversalのSCORE_GATEブロックが毎20〜30秒ペースで連発しており、これと同根の可能性が高い
- モデルがSELLシグナルを出し続けているが、スコア（2.10〜2.13）がゲート閾値を下回り続けている → シグナル自体は発生しているが「有効シグナルなし」扱いが積み上がっている構造と推測

**② daytrade:hedge_block=297**
- 既存ポジションに対してヘッジ抑止が大量発動している
- GBPUSDのSL_HIT（-5.9pips）が発生しており、損失後にポジション方向が混在し、ヘッジブロックが連鎖している可能性
- GBPのgbp_asia_flash_crash（84+18件）が同時多発しており、GBP系のポジション偏りがヘッジ判定を引っ張っている疑い

**③ scalp/scalp_5m:r2_shadow_demoted_cell=278+（合計500件超）**
- EUR_USDのfib_reversalセルがR2シャドウ降格を連続的に受けている（ログで連発を確認）
- R2モデルの評価値が閾値を継続的に下回っている → 相場がフィボ逆張りに不適な一方向トレンド中の可能性
- scalp系全通貨（EUR・GBP含む）に広く波及しており、R2評価ロジック自体またはその入力データに系統的な問題がある可能性

---

### 確認が必要な事項（推測との切り分け）

| 確認項目 | 目的 |
|---|---|
| RnBモジュールのno_signal判定ロジック：「シグナル未生成」と「シグナル生成済みだがブロック」のどちらで455がカウントされているか | SCORE_GATEとの二重計上か、別問題かの切り分け |
| streak_reversalのSELLスコアが2.10〜2.13に張り付いている原因 | 入力フィーチャの固着（feed停止・キャッシュ固定）か、モデル出力の正常な低スコアか |
| hedge_blockのトリガー条件：現在の全オープンポジション方向とロット一覧 | GBP系の偏りが原因か

## Alert: 20:09 UTC

### 検出された問題
- 💰 **含み益+21.7pip** (7ポジション, LIVE=0 Shadow=7) | Top: dual_sr_bounce×AUD_JPY +6.8pip | sr_weighted_bounce×EUR_USD +3.4pip | sr_weighted_bounce×EUR_USD +3.4pip
- ⚠️ **Shadow含み益+21.7pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=697, daytrade_eurgbp:hedge_block=648, daytrade_eur:hedge_block=600, scalp_eur:max_open=431, scalp:max_open=409

### 診断
## 原因分析

### 推測（ログ・データから高確度で推定）

**① Shadow専有・LIVE未昇格問題**
- 7ポジション全てがShadowモードに留まっており、OANDAへ発注されていない
- `PAIR_PROMOTED`設定が不足しているか、昇格条件（スコア閾値・リスク上限等）を満たしていない可能性が高い
- +21.7pipの含み益が実損益に反映されず機会損失が継続中

**② `rnb_usdjpy:no_signal=697` — 異常に高いブロック数**
- RnBロジックがUSD/JPYでシグナルを全く生成できていない
- 相場レンジ離脱、ボラティリティ逸脱、またはパラメータ（SR帯・乖離幅）が現在の値幅に対して乖離している可能性

**③ `daytrade_eurgbp:hedge_block=648` / `daytrade_eur:hedge_block=600`**
- EUR/GBP・EURペアで逆方向ポジションが長時間保有され、ヘッジブロックが連続発火している
- 含み益保護（HOLDログ多数）でSIGNAL_REVERSEが連続無効化されており、これがヘッジブロック積み上がりの主因と推測

**④ `scalp_eur:max_open=431` / `scalp:max_open=409`**
- スキャルピング系がmax_open上限に継続的に到達 → 新規エントリー機会を大量に逃している
- 既存ポジションが長時間クローズされないことで枠が解放されない状態の可能性

**⑤ ORDER_BAR_DEDUP連続発火（GBP_USD / EUR_USD SELL）**
- 20:00バー起点のny_close_reversalシグナルが同一バーで重複ブロックされ続けている
- バーが更新されないか、シグナル生成ループが同一bar_tsを繰り返し参照している

**⑥ `daytrade_1h_eur`のティク遅延（36.7秒）**
- 最遅モードとして検出されているが現状閾値(15分)内でありクリティカルではない
- ただし他モードとの処理競合の兆候として注視が必要

---

### 確認事項（要検証）

- [ ] `PAIR_PROMOTED`の設定リストにAUD/JPY・EUR/USDが含まれているか
- [ ] Shadow→LIVE昇格の条件（勝率・バックテスト要件等）がハードコードで遮断していないか
- [ ] `rnb_usdj
