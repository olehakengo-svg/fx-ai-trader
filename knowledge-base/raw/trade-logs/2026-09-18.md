# 2026-09-18 Trade Log (wiki-daily-update)

**Pull 時刻**: 2026-09-18T21:54:54–21:54:57Z (5 endpoint を 3 秒以内 = **coherent snapshot**) / **前 log**: [[2026-09-17]] (09-17T23:10Z) ⇒ **0.95 日デルタ**
**orphan check**: `pgrep -f app.py` → **なし** ([[feedback_check_orphan_local_app]])
⚠️ **task 定義の `limit=30` ではなく `limit=500` を使用** ([[feedback_audit_limit30_hides_live_fills]])。
⚪ 21:13Z に 4 endpoint を先行取得済。21:54Z 版との差は **`shadow_count` +1 のみ** (13,794→13,795)、`learning`/`audit` は byte 単位で同一。以下はすべて **21:54Z 版**。

## データソース
| endpoint | 状態 |
|---|---|
| `/api/demo/stats?date_from=2026-04-08` | 200 |
| `/api/demo/learning` | 200 |
| `/api/risk/dashboard` | 200 |
| `/api/oanda/audit?limit=500` | 200 (500 行 = 09-11T14:32:02〜09-18T19:31:56) |
| `/api/demo/status` | 200 (engine / SVK / block_counts / `strategy_status` 100 件 / `freshness_ui`) |
| `/api/demo/trades?limit=200` | 200 (09-16〜09-18、**200 行すべて `is_shadow=1`**) |

---

## 🔴🔑 発見 1: **09-17 が立てた予測が的中 — LIVE 約定 freshness が閾値を突破し、`freshness_ui` は初めて `stale` を出した**

[[2026-09-17]] は「残り約 11.85 市場オープン時間、突破は **2026-09-18T11:00Z 前後**の見込み (予測であって観測ではない)」と書いた。**本 run で観測になった**:

| 項目 | 09-17 23:10Z | **本 run (09-18 21:54Z)** |
|---|---|---|
| `last_live_fill_row_at` | 2026-09-11 11:02:36 | **不変** (#893161) |
| `last_live_fill_row_age_sec` | 562,090.6 (6.51 日) | **643,943.3 (7.45 日)** |
| 市場オープン換算 | 108.15h / 120h = **90.1%** | **130.0h / 120h = 108.3%** |
| `freshness_ui` `live_fill_row.level` | `ok` | 🔴 **`stale`** |
| `freshness_ui` `worst_level` | ok | 🔴 **`stale`** |

130.0h − 120h = **10 市場オープン時間の超過** ⇒ 突破時刻は **09-18T11:55Z 前後**と逆算でき、09-17 の予測 (11:00Z 前後) と **1 時間以内で一致**。KB の予測が実測で検証された初の事例として記録する。

🔴 **ただし同一 payload 内で 2 つのフィールドが矛盾している**:
- `freshness_ui.families[live_fill_row].level` = **`stale`**
- トップレベル `last_live_fill_row_status` = **`ok`**

同じ `last_live_fill_row_age_sec` (643,943.3) を源とする 2 指標が**逆の判定**を返している。バナー系 UI がトップレベルを読んでいる場合、**閾値を 8.3% 超過してもなお「ok」と表示され続ける**。⇒ どちらが正かはコード未確認のため断定しない。**次 run の最優先フォローアップ**。

---

## 🔴🆕 発見 2: **learner は「動いていない」のではなく「動いた結果が反映されていない」— blacklist 書き込みが no-op と確定**

これまでの run は learner を「stale / blacklist no-op」と記述してきたが、本 run は**より強い形で特定できた**。

| ソース | 主張 |
|---|---|
| `/api/demo/learning` `adjustments[0]` | **id 94 / 2026-09-15 13:18:14 / mode `scalp` / `parameter: entry_type_blacklist_add` / `old_value 0.0 → new_value 1.0` / reason `sr_channel_reversal: WR 25.0%, EV -0.98 → 除外` / `sample_size 190`** |
| `/api/demo/learning` `current_params` | **`entry_type_blacklist: []`** (空) |
| `/api/demo/status` `strategy_status` | `sr_channel_reversal` (cat `scalp`) = **`blacklisted: false` / `enabled: true`** / n=4 / WR 50.0 / EV **+1.73** |

⇒ **learner は 3.36 日前に「除外」を決定し記録したが、その決定は `current_params` にも `strategy_status` にも一切到達していない**。`strategy_status` 100 件のうち `blacklisted: true` は **0 件**。
⇒ 「学習ループが止まっている」ではなく「**学習ループは回っているが出力が捨てられている**」。これは監視の意味が変わる — adjustment の**件数**を見ても異常は検出できない。

⚪ 同 payload の breaker 2 本は **8 スナップショット連続で無効**: `daily_loss_limit_pips` **−99999** / `max_drawdown_pips` **−99999**。`confidence_threshold` 30 / `max_open_trades` 8 / `max_consecutive_losses` 3 / `learn_every_n` 10 も不変。

---

## 🔴🆕 発見 3: **audit 500 行の窓から LIVE 行が完全に消えた — `limit=500` はもう live 検証に足りない**

| run | 500 行の窓 | bridge 内訳 |
|---|---|---|
| 09-17 | 09-10T13:51〜09-17T21:00 | skipped 477 / blocked 19 / **sent 2 / filled 2** |
| **本 run** | **09-11T14:32〜09-18T19:31** | skipped **484** / blocked **16** / **sent 0 / filled 0** |

最後の LIVE 約定 **#893161 (09-11T11:02:36Z)** は**窓の開始より 3.5 時間前**に落ちた。⇒ 本 run 以降、`limit=500` では **「LIVE が 0 本」と「LIVE 行が窓外に出た」を区別できない**。[[feedback_audit_limit30_hides_live_fills]] の教訓は **limit の絶対値ではなく「検証したい事象を窓が含むか」**であり、次 run 以降は **`limit≥800` か明示的な date filter** を使うこと。本 run はこの限界を `/api/demo/status` の `last_live_fill_row_at` で補って判定した。

⚪ `agg_kelly` blocked は **16 行**、**全件が `-0.327<0` でビット同一**、範囲 **09-14T03:31〜09-18T12:08**。戦略別は `xs_momentum_rsi` **7** / `vsg_jpy_reversal` **5** / `dt_bb_rsi_mr` **4**。⇒ **agg_kelly は 09-11T13:04Z 以降 8 日間 −0.327 で凍結**、符号は不変。

---

## ⚪ 実現 book: **4 run 連続で bit-for-bit 不変** (09-14 = 09-16 = 09-17 = **09-18**)

| 指標 | 値 | 09-17 比 |
|---|---|---|
| N (decided) | **587** | flat |
| W / L / BE | **257 / 294 / 36** | flat |
| WR (as-reported) | **43.8%** | flat |
| decided WR | **46.6%** | flat |
| EV | **−1.12** | flat |
| PnL | **−658.4 pip** | flat |
| Wilson lower / BF lower | **42.5 / 39.8** | flat |
| avg R | **0.12** | flat |
| `shadow_count` | **13,795** | 13,723 → **+72** |
| `oanda_audit.total` | **17,429** | 17,344 → **+85** |

⇒ **45 戦略 / 0.95 日 / decided fill 0 本**。動いたのは shadow 側のみ。broker heartbeat も **NAV ¥275,516.8319 が 09-16・09-17 と完全一致**、`open_trade_count` 0 / `unrealized_pl` 0 / `margin_used` 0 / latency 101.5ms / `recent_errors` []。

⚠️ **worst cell は 4 run 連続で `price_shock_rev_aud_jpy_h1_long`**: N=4 / WR 50.0% / **EV −45.00/本 = −180.0 pip = book 全損 (−658.4) の 27.3%**。`strategy_status` は **`enabled: true` / `promotion: pending` / `blacklisted: false`** のまま。de-risk 提案は 09-14 から **4 run 連続で未決 (user 決裁待ち)**。

---

## 🟡 発見 4: **09-18 の shadow book は −283.9 pip / WR 42.7% — 損失の 49% が 1 戦略に集中**

`/api/demo/trades` の 09-18 分 **82 行 (全 CLOSED / 全 shadow)**:

| 区分 | 値 |
|---|---|
| N | **82** (W 35 / L 47) |
| WR | **42.7%** |
| PnL | **−283.9 pip** |
| close_reason | **SL_HIT 67 (81.7%)** / SIGNAL_REVERSE 7 / TIME_DECAY_EXIT 6 / **TP_HIT 2 (2.4%)** |
| spread_at_entry | median **1.3** / max **3.0** (異常スプレッドなし) |

**戦略別 (下位/上位)**:

| 戦略 | n | PnL |
|---|---|---|
| `dt_sr_channel_reversal` | 15 | **−139.8** ← 全損の **49.2%** |
| `htf_false_breakout` | 8 | −47.4 |
| `london_fix_reversal` | 4 | −39.3 |
| `wick_imbalance_reversion` | 5 | −27.7 |
| `session_time_bias` | 13 | −22.1 |
| … | | |
| `dual_sr_bounce` | 5 | +8.2 |
| `ob_retest` | 1 | +12.0 |
| **`donchian_momentum_breakout`** | **6** | **+49.6** ← 当日唯一の実質プラス寄与 |

⚠️ `dt_sr_channel_reversal` は `strategy_status` でも **n=15 / WR 40.0% / EV −2.95** で、当日の 15 本がそのまま promo 母数になっている = **当日 book と promo 統計が同一標本** ⇒ この cell の EV は**単一営業日由来**であり、独立 N として扱ってはならない ([[feedback_cohort_time_check]])。

🔴 **`dedup_violation` が 82 行中 11 行 (13.4%)**: `wick_imbalance_reversion` 2 / `vix_carry_unwind` 2 / `donchian_momentum_breakout` 2 / `dt_sr_channel_reversal` 2 / `vol_spike_mr` 2 / `vdr_jpy` 1。除外後は **n=71 / W29-L42 / WR 40.8% / −252.1 pip** (汚染分は WR 54.5% / −31.8 pip で**汚染側のほうが WR が高い**) ⇒ dedup 行を混ぜると WR が **+1.9pp 水増し**される。[[project_r2_audit_dedup_contamination_2026_06_08]] の再現。**cell 統計を引くときは `dedup_violation=0` を必須条件にすること**。

🔴 **`SL_HIT` ラベル破綻の再現**: SL_HIT **67 本のうち 32 本 (47.8%) が正の PnL**。既知の欠陥 ([[project_carrydip_sl_contract_and_slhit_label_2026_08_05]] の「SL_HIT 106 本中 49 本 = 46.2% が正 PnL」) が、**独立した 09-18 標本で 47.8% と再現**した。⇒ 「SL_HIT = 損切り」と読む集計はすべて誤る。

⚪ `outcome: BREAKEVEN` の 3 本はいずれも **PnL が厳密には負** (−0.1 / −0.1 / −0.2) ⇒ BREAKEVEN ラベルは「摩擦込みでゼロ近傍」であって「PnL=0」ではない。36 BE を「損益ゼロ」として扱う集計は**わずかに楽観側**。

---

## ⚪ 執行 QA (毎日必須)

Protocol [[daily-market-review-protocol]] §執行QA。tier-master (`generated_at` 2026-09-17T08:45Z、`elite_live` **0** / `pair_promoted` **21 cell** / `strategy_lot_boost` 10) を live-eligible の基準として照合。

**09-18 の audit 84 行のうち live-eligible cell に該当したのは 7 行 (4 イベント)**:

| 時刻 (UTC) | cell | bridge | block_reason |
|---|---|---|---|
| 01:01:21 | `dt_bb_rsi_mr` × USD_JPY SELL | **blocked** | `agg_kelly=-0.327<0` |
| 03:07:18 | `vsg_jpy_reversal` × EUR_JPY SELL | **blocked** | `agg_kelly=-0.327<0` |
| 12:08:42 | `xs_momentum_rsi` × USD_JPY BUY | **blocked** | `agg_kelly=-0.327<0` |
| 09:34:33 | `bb_squeeze_breakout` × EUR_USD BUY | skipped | `shadow_tracking` のみ |

(各 blocked イベントは `shadow_tracking` 行と対になるため 3×2+1 = 7 行)

✅ **live-eligible 発火の 3/4 は単一ゲート `agg_kelly` で説明が付く** — 設計通りの抑制であり、outage ではない。
⚠️🆕 **4 件目 `bb_squeeze_breakout × EUR_USD` だけが `agg_kelly` 行を持たない** — shadow 行のみで、**live 送信経路に到達した形跡がない**。他 3 cell と同じ live-eligible 資格を持ちながらゲート判定に現れない ⇒ **抑制の理由が他 3 件と異なる**。コード未確認のため原因は断定しない。観測として [[daily-observations-2026-09]] に **O-2026-09-18-2** で記録。

⚪ `prereg-trigger-registry.json` を `bb_squeeze_breakout` / `agg_kelly` で検索 — **該当 family の事前コミット済み再審条件はヒットなし** ⇒ 独自 R1 マークでの前倒しはせず、観測記録に留める。

---

## ⚪ plumbing / SVK

| 指標 | 値 |
|---|---|
| `engine_tick_age_sec` | **4.4s** (`ok`)、最遅 `daytrade_1h_usdcad` **62.3s** |
| 稼働モード | **24** / `main_loop_alive` true / `main_loop_restarts` **1** (不変) / `watchdog_alive` true / `sltp_checker_active` true / `emergency_killed` false |
| `last_candidate_row_at` | 2026-09-18 20:54:12 (61 分前 / 市場換算 0.1h、`ok`) |
| `last_trade_row_at` | 2026-09-18 19:31:56 (2.4h / 市場換算 1.5h、`ok`) |
| `log_count` | 9,126 / `open_trades` **[]** |
| SVK | `rt_count` **26** / `volume_usd` **520,000 / 520,000 = 100.0%** / `behind_pace` false / `last_rt_at` **2026-09-14T01:01:07Z** / `last_skip_reason` `outside_window(h=21)` |

⇒ **SVK は 09-14 以降 1 往復も追加しておらず、2026-09 の friction 支出は確定して増えない**。

⚪ `block_counts` (⚠️ top-30 truncated、`modules/demo_trader.py:2061-2063`): `eurgbp:hedge_block` **723** / `gbpusd:hedge_block` 516 / `eur:hedge_block` 522 / `scalp:r2_shadow_demoted_cell` 515 / `scalp_eur:r2_shadow_demoted_cell` 471 / `rnb_usdjpy:no_signal` **1,506**。⚠️ **`rnb_usdjpy` は 09-02 まで `direction_filter` 100% ブロックだったが、本 run では `no_signal` 1,506 に変わっている** — 遮断段が移動したのか表示上位 30 に `direction_filter` が入らなくなっただけなのかは、`/api/demo/block-counts` (全件版) を引かないと区別できない。**未取得**、次 run のフォローアップ。

---

## ⚪ risk dashboard — 窓は回ったが中身は 14 trade で静止

`effective_date_from` **2026-08-19T21:54:58** (rolling 30d)、`n_trades_post_filter` **14**。

| 指標 | 値 |
|---|---|
| Kelly edge | **−0.0666** / odds 0.452 / WR 0.6429 / `recommended_fraction` **0.0** |
| DSR | **0.1292** (`is_significant` **false**、観測 Sharpe 0.005 vs 閾値 0.3184、n_trials 4、haircut 0.0) |
| attribution | gross **+1.9** / friction **63.6** / **avg friction 4.54/trade** / alpha +1.9 / sizing −0.0 |
| MC | ruin **0.0%** / median max DD 94.66 / worst DD99 255.09 / `small_sample_lowconf` **true** |
| by_instrument | USD_JPY **+66.1** (n=8) / EUR_GBP −6.8 (n=4) / **AUD_JPY −57.4 (n=2)** |
| correlation | 4 戦略、相関行列は `usdjpy_carry_dip_accumulator` の自己相関 1.0 以外**全て null** (n 不足) / `flagged_pairs` [] |

🔑 **friction 63.6 vs gross +1.9** ⇒ **摩擦が gross の 33.5 倍**。30 日窓で 14 本しか無い状態では、**エッジの有無を議論する以前に摩擦が支配的**。[[project_cell_edge_deep_audit_2026_06_08]] の「真因は friction」と整合。

🔴 **DD 4 指標は 8 スナップショット連続 flat**: `dd_pips` **1209.0** / `dd_pct` **0.0972** / eq_current **−1192.1** / eq_peak **+16.9** / `lot_multiplier` **0.2** / `jpy_ledger.eq_current_jpy` **¥324,381.63**。
⚠️ **`dd_pct` 0.0972 を現況として引用しない** (09-14 に提起、以降維持) — 本 run も broker tx 0 本の窓であり、「ledger 凍結」と「正しい静止」を分離できない。broker 基準の実 DD は base ¥359,109 に対し **−23.3%**、表示値の **2.4 倍**。demo ledger ¥324,381.63 vs broker NAV ¥275,516.83 の乖離は **¥48,864.80** (09-17 から不変)。

---

## Lint (Phase 4)

| # | チェック | 結果 |
|---|---|---|
| 1 | WR/PnL 数値の整合 | ✅ stats は 09-17 とビット同一 (N=587 / WR 43.8% / −658.4) ⇒ 戦略ページの書き換え不要。`index.md` の Portfolio ブロック・System State の実現 book 数値とも一致 |
| 2 | 壊れた wikilink | KB 全体 **2,274 md ファイル (basename 2,253 種) / 3,779 link**。MEMORY 常駐 (105 ファイル) を正当な宛先とし、`../x/y` 形式で basename が実在するものを「解決可能」に分離すると: **解決可能 96 種 / 真に不在 55 種 (51 ファイル)**。🔴 **`wiki/index.md` は 5 件** — `ma-regime-switch` / `ma-trend-perfect` / `mqe-gbpusd-fix` / `ob-retest` / `vsg-jpy-reversal`、**いずれも auto-synced Portfolio ブロックが参照する戦略ページの不在**。`wiki/log.md` の 4 件 (`lesson-名前` / `link` / `wikilink` / `shadow-promote-r2-alert 系列`) は散文中のプレースホルダで実リンクではない。`wiki/strategies/` は **1 件** (`macd-rsi-pullback.md` → `xs-momentum-rsi-tv-phase2-2026-05-13`)。**本 run で新規に壊したリンクは 0** |
| 2b | ⚠️ **本 run の計測バグと訂正** | 初回のリンク計測は結果を **basename でキー付けした dict** に集約していたため、同名ファイル (`index.md` が複数ディレクトリに存在) が**衝突して上書きされ**、`wiki/index.md` を「0 件」と誤報した。全**パス**を走査する方式に修正して再計測した値が上の行。**「index.md 壊れリンク 0」は誤り** |
| 2c | 🔴 ギャップ | 壊れた 5 件のうち **`vsg-jpy-reversal` は tier-master の `pair_promoted` (live-eligible) であり、本日 03:07:18Z に発火して `agg_kelly` ゲートで blocked になった実稼働 cell** — それでいて**戦略ページが存在しない**。`mqe-gbpusd-fix` も同じく `pair_promoted`。⇒ **live-eligible なのに KB 上に頁が無い戦略が最低 2 本** |
| 3 | データ鮮度 (>3 日) | 🔴 **LIVE 約定 7.45 日** (閾値突破、発見 1) / 🔴 **learner adjustment 3.36 日**かつ**反映 0** (発見 2) / ⚪ engine 4.4s・candidate 61 分・trade row 2.4h はすべて `ok` / ⚪ tier-master `generated_at` 09-17T08:45Z (1.5 日、許容) |
| 4 | Tier 整合 | ⚠️ `strategy_status` の `blacklisted: true` は **0/100** だが learner id 94 は blacklist 追加を記録 (発見 2) ⇒ **tier/blacklist 表示は learner を反映していない**。tier 変更は本 run で**なし** |
| 5 | repo | `main...origin/main` **ahead/behind なし** (HEAD `043fa9d7 Merge pull request #270`)。working tree の変更は `data/sentiment/oanda_labs_h4_history.parquet` のみ (sentiment cron 由来、KB 外) ⇒ 09-17 が報告した「ahead 1 / behind 8」は解消済 |

---

## 未決事項 (user 決裁待ち / 繰り越し)

1. 🔴 **`price_shock_rev_aud_jpy_h1_long` の de-risk** — 4 run 連続未決。EV −45.0/本、book 全損の 27.3%、`enabled: true` のまま
2. 🔴 **learner blacklist 書き込みの no-op 修復** (発見 2、本 run で新規特定)
3. 🟡 **`last_live_fill_row_status` と `freshness_ui.level` の矛盾**の正否判定 (発見 1)
4. 🟡 **`index.md` 正本問題** — 09-17 の決裁項目「(a) run 内 commit / (b) reset 前退避の規則化 / (c) 正本を trade-log + log.md へ移す」は未決。⚪ 本 run は `main` が origin と一致した状態で開始できており、退行は再発していない
5. ⚪ **audit の pull limit を ≥800 へ引き上げ** (発見 3)
6. ⚪ **`/api/demo/block-counts` (全件版) の取得** — `rnb_usdjpy` の遮断段移動の確認
