# Trade Monitor Alerts: 2026-09-17

## Alert: 08:58 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=570, scalp:r2_shadow_demoted_cell=341, scalp_eur:r2_shadow_demoted_cell=231

### 診断
## 原因分析

### 推測（ログ・カウントから高確率で言える事項）

**① `rnb_usdjpy:no_signal=570` — RNBモードがシグナルを全く出せていない**
- USD/JPYにおいてstreak_reversalのスコアが固定で `-3.00` になっており、SCORE_GATEにも連動して大量ブロックされている
- RNBロジックが要求する方向性条件（トレンド・スコア閾値）を市場が満たしていない状態が長時間継続している
- 単純な「無シグナル」ではなく、スコア計算またはトレンド判定のインプットが偏っている可能性が高い

**② `scalp:r2_shadow_demoted_cell=341` / `scalp_eur:r2_shadow_demoted_cell=231` — スキャルプ系のshadow追跡セルが大量降格**
- `ema_trend_scalp x EUR_USD` と `engulfing_bb x USD_JPY` の2セルがほぼ独占的に繰り返し降格されている
- ローソク足の更新頻度に対してセルのR2回復が追いついていない（セルが降格→復帰→降格のサイクルを高速で繰り返している）
- EUR/USDおよびUSD/JPYで値動きの方向が頻繁に反転しているか、フィード品質（tick精度・タイムスタンプずれ）の問題が疑われる

**③ `ORDER_BAR_DEDUP` の集中 — 同一バー (`08:45:00`) のシグナルが数分にわたり繰り返し生成**
- `dual_sr_bounce EUR_JPY SELL` が同一bar_tsで大量重複しており、シグナル生成ロジックがバーの確定を正しく検知できていないか、バーのタイムスタンプが更新されていない
- 上流のOHLCフィード更新遅延、またはシグナル生成のデバウンス失敗が疑われる

---

### 確認が必要な事項（断定できないもの）

- USD/JPYのstreak_reversal スコアが `-3.00` に張り付いている期間と、その算出元データ（連敗カウント・履歴）の内容
- `ema_trend_scalp` / `engulfing_bb` の各セルにおけるR2値の時系列推移（降格閾値を恒常的に下回っているのか、瞬間的な振動なのか）
- EUR/JPY `08:45:00` バーのOHLCフィードが正しく次のバーに切り替わっているかどうか
- `daytrade_1h_euraud
