# Trade Monitor Alerts: 2026-05-20

## Alert: 01:58 UTC

### 検出された問題
- 💰 **含み益+1855.7pip** (9ポジション, LIVE=0 Shadow=9) | Top: price_shock_rev_aud_jpy_h1_long×AUD_JPY +1810.0pip | sr_break_retest×EUR_JPY +14.0pip | sr_anti_hunt_bounce×EUR_USD +8.9pip
- ⚠️ **Shadow含み益+1859.1pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=296

### 診断
## 原因分析

### 推測（確認前）

**問題1: Shadow含み益がOANDAに送信されていない（LIVE=0, Shadow=9）**
- `price_shock_rev_aud_jpy_h1_long×AUD_JPY` (+1810pip) を筆頭に9ポジションがShadowモードで稼働中
- AUD_JPY がおそらく `PAIR_PROMOTED` リストに未登録のため、Shadowから実トレードに昇格していない
- Shadow側で含み益が膨らみ続けているが、実口座への反映機会を逃し続けている状態

**問題2: `rnb_usdjpy:direction_filter=296` 異常**
- 他のblock_countsと比較して突出して高い（次点は `daytrade_eurjpy:hedge_block=200`）
- `rnb_usdjpy` の方向フィルターが**ほぼ全シグナルを遮断**している状態
- 原因候補：①トレンド判定ロジックの基準値が現在の相場（USD/JPY強上昇局面と推測）に対して逆張り方向を向いている、②フィルターのパラメータが固定値でレンジ外の相場に対応できていない、③参照しているEMA/MA等のベースラインが古いキャッシュ値を使っている

**問題3: `R2_SHADOW_DEMOTE` の連続発火**
- `ema_trend_scalp x USD_JPY` が数十秒間隔で繰り返しブロックされている
- Shadow降格セルが解除されず、scalp系シグナルが実質機能停止している可能性

**問題4: `DT含み益保護` の多発**
- 複数ポジション（cbc3a800=+0.229, 85c9bdf3=+0.151等）で逆張りシグナルを頻繁に無効化
- 含み益保護ロジック自体は正常動作だが、**利確トリガーが設定されていないか機能していない**可能性

---

### 確認事項（実施すべきこと）

**即時確認**
- [ ] `PAIR_PROMOTED` の現在の設定値にAUD_JPYが含まれているか確認
- [ ] `price_shock_rev_aud_jpy_h1_long` のポジション開始時刻と累積経過時間を確認（+1810pipは異常値である可能性）
- [ ] `rnb_usdjpy:direction_filter` のフィルター判定に使用しているシグナルソース・基準値を確認

**調査確認**
- [ ] `R2_SHADOW_DEMOTE` がリセットされる条件（時間経過 or 明示的なリセット

## Alert: 06:03 UTC

### 検出された問題
- 💰 **含み益+47.8pip** (11ポジション, LIVE=0 Shadow=11) | Top: sr_break_retest×EUR_JPY +17.8pip | session_time_bias×GBP_USD +11.8pip | sr_break_retest×EUR_JPY +11.8pip
- ⚠️ **Shadow含み益+57.0pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】根本原因

**Shadow益がLIVEに昇格していない主因：ブロックフィルタの過剰作動**

- `daytrade:hedge_block` (20回)、`daytrade_eurjpy:hedge_block` (16回)、`daytrade_gbpusd:hedge_block` (14回) が最大カウントに達しており、ヘッジブロックが新規エントリーを全面的に抑制している可能性が高い
- `rnb_usdjpy:direction_filter` (20回) がUSD/JPY方向フィルタで上限到達、USDJPYモード系が事実上停止状態
- `scalp:r2_shadow_demoted_cell` (14回) + `scalp_5m` 系の連続デモートにより、scalp系セルがshadow昇格対象から外れ続けている
- `DT含み益保護`ログが毎秒複数発火 → 既存ポジションのSELLシグナルが全て無効化され、新規シグナル処理リソースが圧迫されている構造的疲弊の可能性

---

### 【推測】副次的原因

- Shadow=11 / LIVE=0 という乖離は、**PAIR_PROMOTEDリストにEUR/JPY・GBP/USDが含まれていない**、またはプロモーション条件（信頼度閾値・MTF合意）を満たせていないことが原因と考えられる
- `[SHADOW] BUY TREND_BEAR block: sr_break_retest conf=48 → shadow` → トレンドベアブロックにより信頼度48でも昇格不可。TREND判定とshadowシグナルの方向不一致が継続している
- `MTF_MONITOR GBP_JPY: mtf=uncertain d1=2 h4=-1` → MTF合意未達でLIVE昇格が阻止されている
- `main_loop_restarts=1` → 直近に一度メインループが再起動しており、その際に昇格キューや状態変数がリセットされた可能性

---

## 【確認事項】（実機で要確認）

- [ ] `PAIR_PROMOTED` 設定にEUR/JPY・GBP/USDが含まれているか
- [ ] hedge_blockのカウントリセット条件・TTLが正常に機能しているか（上限20で張り付いている異常性）
- [ ] `rnb_usdjpy:direction_filter` 上限20到達がループリセット未反映によるカウント継続ではないか
- [ ] main_loop_restart時にblock_countsがクリアされず積算されていないか（再起動後もカウントが引き継がれている疑い）
- [
