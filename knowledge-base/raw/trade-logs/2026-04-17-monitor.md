# Trade Monitor Alerts: 2026-04-17

## Alert: 11:15 UTC

### 検出された問題
- 💰 **含み益+35.3pip** (10ポジション, LIVE=0 Shadow=10) | Top: dt_sr_channel_reversal×USD_JPY +11.0pip | dt_fib_reversal×EUR_JPY +9.1pip | sr_fib_confluence×EUR_USD +4.4pip
- ⚠️ **Shadow含み益+38.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】根本原因

**Shadow含み益+38.9pip がOANDAに反映されていない主因は、対象通貨ペアが `PAIR_PROMOTED` リストに未登録であること**

具体的には以下の多層ブロックが重なっている：

- `dt_sr_channel_reversal×USD_JPY`、`dt_fib_reversal×EUR_JPY`、`sr_fib_confluence×EUR_USD` の3シグナルがいずれもShadowに滞留
- `score_gate` ブロックが最多（daytrade:42, daytrade_eurjpy:46）→ スコア閾値を通過できていない
- `Regime guardrail` が `up_trend_BUY` に対して否定的（WR=31.7%, EV=-0.776）→ レジーム判定がシグナルと矛盾
- `DT RANGE bypass` が `sr_fib_confluence×GBP_USD` を継続ブロック → TFがRANGE判定のまま
- `ema_trend_scalp` は conf=56〜57 で閾値65未満 → 複数モードで連続ブロック

---

## 対処方針

### ✅ 確認事項（まず事実確認が必要）

- [ ] `PAIR_PROMOTED` の現在の登録内容（USD_JPY, EUR_JPY, EUR_USD が含まれているか）
- [ ] Shadowポジション10件の各シグナル種別と通貨ペアの内訳
- [ ] `score_gate` の現行閾値設定値（daytrade/daytrade_eurjpy系）
- [ ] `Regime guardrail` の `up_trend_BUY` 判定の根拠データ（N=60の期間・更新タイミング）
- [ ] `DT RANGE bypass` の RANGE判定がいつから継続しているか（TFのタイムフレーム設定）
- [ ] `daytrade_1h` および `daytrade_1h_eur` が `running: false` になっている理由

---

### 🔧 対処方針（優先順）

**① PAIR_PROMOTED への追加検討（最優先）**
- USD_JPY, EUR_JPY, EUR_USD をPAIR_PROMOTEDに追加し、Shadowシグナルをライブ昇格可能な状態にする
- ただし一括追加はリスクがあるため、含み益上位の1ペア（USD_JPY）から段階的に追加

**② score_gate 閾値の見直し**
- daytrade/daytrade_eurjpy系でブロック数が最多クラス（42〜46件）
- 現行閾値が市況に対して過剰にタイトになっている可能性があるため、直近

## Alert: 15:40 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=375, daytrade:score_gate=344, daytrade_gbpjpy:score_gate=287

### 診断
## 原因分析

### 【推測】高確度

- **rnb_usdjpy:direction_filter=375** — RNBモードがUSDJPYの方向性フィルタで大量にブロックされている。ログの`xs_momentum score=-6.71`が連続出力されており、モメンタム系スコアが極端にマイナスに振れている状態が続いている。市場の方向性とモデルの想定方向が強く乖離していると見られる
- **daytrade:score_gate=344 / daytrade_gbpjpy:score_gate=287** — スコアゲートが組織的に発動している。単発ノイズではなく、特定の相場環境（トレンドレス or 急変動）でスコア算出の前提が崩れている可能性が高い
- **SCORE_GATE `unknown` ストラテジー名** — `daytrade_gbpjpy`で戦略名が`unknown`として記録されている。スコア計算前にストラテジー識別が失敗しており、スコアがデフォルト値（負）で処理されている疑いがある
- **GBP_USD / EUR_USD の SHADOW大量発生** — TREND_BULL状態でWR=0%のままTFバイパスがかかり続けている。勝率集計がリセットされたか、TF判定ロジックが正常に更新されていない可能性がある

### 【推測】中確度

- `main_loop_restarts=1` が発生しており、再起動前後でblock_countがリセットされずに累積している可能性がある
- スコアゲートのブロックが多い通貨ペア（USDJPY・GBPJPY）に集中しており、JPY絡みペアで共通の入力特徴量（金利・ボラティリティ指標等）が異常値を示している可能性がある

---

## 確認事項（推測と分離）

1. **`rnb_usdjpy:direction_filter`のフィルタ条件定義** — 現在の閾値設定値と、直近のUSDJPY価格・モメンタム指標の実測値を突き合わせる
2. **`unknown`ストラテジー名の発生箇所** — `daytrade_gbpjpy`のストラテジー名解決ロジックを確認。シグナル生成時にname属性が未設定のケースがないか
3. **WR=0%のTF判定ロジック** — TREND_BULL判定後の勝率(WR)が更新されているか。直近の約定履歴とWR計算タイミングを確認
4. **main_loop_restart時のblock_countリセット有無** — 再起動時にカウンタが引き継がれているか

## Alert: 18:37 UTC

### 検出された問題
- ⚠️ **block_counts異常** — daytrade_eurjpy:score_gate=254, daytrade:score_gate=251, rnb_usdjpy:direction_filter=244, daytrade_gbpjpy:score_gate=221

### 診断
## 原因分析

### 【推測】根本原因

**全モードでスコアが継続的にマイナス域に張り付いている**
- `score_gate` ブロックのシグナル名が大多数 `unknown` であり、戦略ロジックが正常なシグナル名を生成できていない可能性が高い
- スコア値が `-0.92`〜`-3.00` と一定値に固定されている傾向があり、スコア計算の入力フィーチャが変化していない（フリーズ・古いデータ参照の疑い）
- `rnb_usdjpy:direction_filter=244` が `score_gate` 系と同水準でカウントされており、方向フィルタも同じ相場環境を長時間拒否し続けている

**`main_loop_restarts=1` の再起動後から状態がリセットされていない可能性**
- 再起動後に内部状態（スコア計算用バッファ・特徴量キャッシュ）が不完全な形で引き継がれ、`unknown` シグナルが量産されている疑い

---

### 【確認事項】（推測と区別）

| # | 確認内容 | 確認方法 |
|---|---------|---------|
| 1 | シグナル名 `unknown` が発生し始めた時刻と `main_loop_restarts=1` のタイムスタンプの一致 | アプリログの再起動ログを検索 |
| 2 | スコア計算に使うOHLCデータ・指標値が最新時刻に更新されているか（データフィード疎通確認） | 各モードの `last_signal.time` と現在時刻の乖離チェック |
| 3 | `daytrade` の `last_signal.confidence=20` が異常に低い値でないか（正常範囲の確認） | 過去正常時のconfidence分布と比較 |
| 4 | `rnb_usdjpy:direction_filter` の連続拒否が相場トレンド不在によるものか、フィルタ計算バグか | direction_filterの判定根拠ログを確認 |
| 5 | `daytrade_eur:exposure` ブロックが18〜17件残っており、EUR建てポジションが上限を超えたままか | 現在のオープンポジション一覧確認 |

---

## 対処方針

### 即時対応

- **全モードの状態バッファを安全にリセット**し、再起動を実施（ポジション保持中のモードは `[HOLD]` ログ確認後に実施）
  - `e675425f`（profit=0.188〜0.200）と `94735a98`（profit=0

## Alert: 22:05 UTC

### 検出された問題
- ⚠️ **block_counts異常** — daytrade_eurjpy:score_gate=360, rnb_usdjpy:direction_filter=360, daytrade_gbpjpy:score_gate=356, daytrade_eurgbp:score_gate=353, daytrade_eur:score_gate=342

### 診断
## 原因分析

### 【推測】高確度

- **現在時刻が22:00直前（NYクローズ前後）であり、市場環境が構造的にエントリー不適切な状態**
  - `ny_close_reversal`・`session_time_bias`・`unknown`スコアがいずれも大幅なマイナス（-0.55〜-3.88）
  - スコアリングモデルがNYクローズ帯を「エントリー禁止」と正しく評価している可能性が高い
- **block_countが350〜360に達しているモードは、ほぼ6時間分（interval=30秒×720回の半分程度）の連続ブロック**を意味しており、**セッション開始直後からずっとスコアがネガティブな状態が継続している**
- `rnb_usdjpy:direction_filter=360`は、方向性フィルターが終日一貫してブロックしており、**USDJPYのトレンド方向が戦略の想定と逆向き、または無方向**であることを示唆

### 【推測】中確度

- `entry_type: unknown`でブロックされているケース（EUR_JPY, GBP_JPY, EUR_GBP）は、**シグナルのentry_typeが未登録またはスコアテーブルに存在しないタイプ**を発していると考えられる
  - スコアが`-1.52`〜`-2.04`と一定値で固定されているのが傍証（デフォルト値の返却）
- `main_loop_restarts: 1`は軽微だが、再起動タイミングでblock_countがリセットされていない可能性がある

---

## 【確認事項】（実施前に必ず確認）

1. **スコアテーブル／スコアリング関数に`unknown`エントリーが定義されているか**
   - `entry_type: unknown`が実際のシグナル名なのか、フォールバック扱いなのかを区別する
2. **block_countのカウンター起点**を確認
   - システム起動時からの累積か、日次リセットか、rolling windowか
   - 360という値がinternal上限（例：6時間×1回/分）に相当するなら**上限到達によるサチュレーション**の可能性
3. **rnb_usdjpy の direction_filter 判定ロジック**を確認
   - 何のインジケーター・条件で方向を決定しているか、パラメータが現相場（USDJPY 158円台）に適合しているか
4. **daytrade_eurjpy / daytrade_gbpjpy のシグナル生成元**を確認
   - `entry_type`が`unknown`として記録される
