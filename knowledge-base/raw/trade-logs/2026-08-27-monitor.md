# Trade Monitor Alerts: 2026-08-27

## Alert: 16:56 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=1332, scalp:r2_shadow_demoted_cell=726, daytrade_eurgbp:hedge_block=529, daytrade:hedge_block=512, daytrade_gbpusd:order_bar_dedup=498

### 診断
## 原因分析

### 【推測】主因

**現在の15分足バー（16:45:00 UTC）がまだ確定していない（または直近に確定したばかり）にもかかわらず、複数モードが同一bar_tsに対して繰り返しシグナルを生成し続けている。**

- `ORDER_BAR_DEDUP` が同一 `bar_ts=2026-08-27T16:45:00+00:00` に対して多数ブロックしており、**シグナル生成ロジックのループ or ポーリング間隔が短すぎる**ことが根本
- `direction_filter=1332`（rnb_usdjpy）は突出して高く、**方向フィルタが永続的に発動し続ける相場環境**（強トレンドまたはレンジ判定の誤検知）が続いていることを示唆
- `r2_shadow_demoted_cell=726`（scalp）は、**EMA追従セルが繰り返しデモートされている**＝スキャルpモードでトレンド方向の一貫性が取れていない
- `hedge_block`の高値（daytrade_eurgbp:529, daytrade:512）は、**既存ポジションと逆方向シグナルが大量発生**していることを示す
- ログの `WIDE_RANGE detected` が同一シグナル（EUR_GBP BUY）に対して数秒おきに重複発火しており、**REGIMEイベントの重複トリガー**が疑われる

---

### 【推測と確認事項の分離】

#### 推測（現時点での仮説）

| # | 推測内容 |
|---|---------|
| 1 | シグナル評価ループの呼び出し頻度が高く、同一バー内で何度も評価が走っている（ポーリング間隔 < バー確定間隔） |
| 2 | `rnb_usdjpy:direction_filter` の異常値はUSDJPYが強トレンド中またはフィルタ閾値が現相場に対して厳しすぎる |
| 3 | `scalp:r2_shadow_demoted_cell` の高値はスキャルプ系でレンジ/トレンドの切り替わりが激しく、セル評価が収束しない |
| 4 | `hedge_block` 高値は、含み益保護HOLDと逆張りシグナルが同時多発する相場環境（ボラティリティ上昇）に起因 |
| 5 | REGIMEイベントの重複発火は、WIDE_RANGE判定のデバウンス処理が機能していないか存在しない |

#### 確認が必要な事項

- [
