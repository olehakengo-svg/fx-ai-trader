# Trade Monitor Alerts: 2026-04-30

## Alert: 01:44 UTC

### 検出された問題
- 💰 **含み益+82.5pip** (10ポジション, LIVE=0 Shadow=10) | Top: vsg_jpy_reversal×EUR_JPY +12.8pip | vsg_jpy_reversal×EUR_JPY +12.8pip | vsg_jpy_reversal×EUR_JPY +12.8pip
- ⚠️ **Shadow含み益+88.4pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因特定

### 【推測】

**1. Shadow専有問題（最重要）**
- `vsg_jpy_reversal × EUR_JPY` が10ポジション全てShadow止まり
- `PAIR_PROMOTED` に `EUR_JPY` が未登録、またはvsg_jpy_reversalモードがLIVE昇格条件を満たしていない
- Shadow含み益+88.4pipがOANDAに届いておらず、実利益ゼロの状態が継続中

**2. GBP系シグナルの全面遮断**
- `gbp_asia_flash_crash` ブロックが `daytrade_gbpjpy:28件` `daytrade_gbpusd:76件` と大量発生
- アジア時間帯のフラッシュクラッシュ警戒ルールが過剰に機能している可能性

**3. EUR系の過剰ブロック**
- `daytrade_eur:session_pair:79件` `daytrade_eurgbp:session_pair:77件` がほぼ全シグナルを遮断
- セッションフィルタの時間帯設定がずれている可能性（01:44 JST = UTC16:44付近）

**4. DT含み益保護の高頻度発火**
- 約30秒間に20回以上 `SIGNAL_REVERSE無効化` が発生
- 保護ロジック自体は正常動作だが、逆張りシグナルが頻繁に発生している背景に注意

---

### 【確認事項】

**Shadow→LIVE昇格まわり**
- [ ] `PAIR_PROMOTED` リストに `EUR_JPY` が含まれているか
- [ ] `vsg_jpy_reversal` モードのLIVE昇格条件（勝率・サンプル数・信頼度閾値）の充足状況
- [ ] Shadow10ポジションの積み上がりに上限設定があるか（ポジション過多リスク）

**GBP遮断まわり**
- [ ] `gbp_asia_flash_crash` の発動条件（価格閾値・時間帯）が現在の相場水準に合っているか
- [ ] 直近にGBP系でフラッシュクラッシュ相当の事象が実際に発生したか、または誤検知か

**EURセッションフィルタまわり**
- [ ] `session_pair` フィルタの許可時間帯定義（UTC/JSTの混在ミスがないか）
- [ ] `daytrade_1h_eur` が `running: false` のままである理由

**main_loop_restarts: 1**
- [ ] 再起動の原因ログ（直近のERROR/CRITICALログ）を確認
-

## Alert: 07:58 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=249

### 診断
## 原因分析

### 推測（高確率）

- **`rnb_usdjpy:direction_filter` が 249 回** という異常値は、他のブロックカウンター（最大でも scalp:recent_emit=115）と比べて **2倍以上の突出**
- RNB（レンジブレイクアウト系と推測）の direction_filter は「シグナル方向がフィルター条件を満たさない場合にブロック」するロジックと思われる
- USDJPYが現在 **TREND_BULL / RANGE_TIGHT** の混在状態（MTF_MONITORログ: `d1=0 h4=1 vol=squeeze`）にあり、**上位足と下位足のトレンド方向が一致しない状態が長時間継続**している可能性が高い
- その結果、RNBロジックが「方向不一致」を連続検出し、カウンターが膨張し続けている

### 推測（中確率）

- direction_filter の判定ロジックに **バグまたはパラメータ劣化**があり、正常な相場でも弾き続けている（フィルターが過剰に厳格化している）
- カウンター自体が **リセットされずに累積**している実装上の問題（再起動後もカウンターが引き継がれている等）

---

## 確認事項

1. **`rnb_usdjpy` の直近シグナル発生履歴**を確認 → そもそもシグナルが出ているか、それとも完全に枯渇しているか
2. **direction_filter の判定条件**（使用している上位足・指標・閾値）を確認 → パラメータ変更履歴と照合
3. **カウンターのリセットタイミング**を確認 → 日次リセットや再起動時リセットが正常に機能しているか（`main_loop_restarts=1` があることに注意）
4. **USDJPYの上位足（D1・H4）のトレンド状態**を確認 → MTFログで `d1=0 h4=1` となっており、D1が方向不定のままであるかを確認
5. **他の通貨ペアに同様の direction_filter 膨張がないか**確認 → USDJPY固有かロジック共通の問題かを切り分ける

---

## 対処方針

- **即時対応**
  - `rnb_usdjpy` を一時的に **停止またはdryrun化**し、意図しないエントリーが出ないことを確保する
  - direction_filterカウンターを **手動リセット**し、リセット後も即座に再膨張するか観察する

- **調査対応**
  - direction_

## Alert: 10:03 UTC

### 検出された問題
- 💰 **含み益+33.1pip** (15ポジション, LIVE=0 Shadow=15) | Top: wick_imbalance_reversion×EUR_JPY +20.0pip | sr_fib_confluence×EUR_USD +13.8pip | wick_imbalance_reversion×EUR_JPY +8.6pip
- ⚠️ **Shadow含み益+60.0pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】主因

**Shadow専有・LIVE昇格ゼロ問題**

- 全15ポジションがShadowのまま滞留し、OANDAへ1件も発注されていない
- Shadow含み益+60.0pipが実損益に変換されていない＝機会損失が拡大中

**ブロックカウントの異常集積**

- `scalp:max_open=38`, `scalp_eur:max_open=53`, `daytrade:hedge_block=36` など、複数モードで高カウントが並行発生
- max_openブロックが積み重なり、新規シグナルが実発注ルートに到達できていないと推測
- hedge_blockが全モードで大量発生 → BUY/SELL両方向ポジションが混在し、相互抑制が起きている可能性

**SIGNAL_REVERSE連続無効化**

- 同一ポジションIDに対し数秒間隔で繰り返しSIGNAL_REVERSEが発火 → 含み益保護ロジックが過剰動作
- 逆張りシグナルが出るたびに抑制されており、エグジットタイミングを逃し続けているリスクがある

---

### 【確認事項】（推測でない・要実確認）

| 確認項目 | 確認方法 |
|---|---|
| PAIR_PROMOTEDリストにEUR/USD・EUR/JPYが含まれているか | 設定ファイル／環境変数を直接参照 |
| Shadow→LIVE昇格条件（confidence閾値・pair制限）を満たしているか | 昇格ロジックのフィルタ条件ログを抽出 |
| max_openの上限値設定が意図通りか（運用中に変更されていないか） | 設定値と現在の実オープン数を照合 |
| hedge_blockの解除条件が正常に機能しているか | ヘッジ判定ロジックのステート確認 |
| main_loop_restarts=1 のリスタート発生時刻・原因 | ログの`[RESTART]`または`[ERROR]`エントリを検索 |
| log_count=9995 でログローテーション上限に近づいていないか | ログ切り捨てによる情報欠損の有無を確認 |

---

## 対処方針（優先順）

- **①即時** PAIR_PROMOTEDにEUR_JPY・EUR_USDを追加し、Shadow→LIVE昇格を解放する
- **②即時** scalp/scalp_eurのmax_open上限値を現状の実オープン数と照合し、不整合があれば設定をリセットまたは

## Alert: 13:15 UTC

### 検出された問題
- 💰 **含み益+23.9pip** (4ポジション, LIVE=0 Shadow=4) | Top: vix_carry_unwind×USD_JPY +31.2pip | ema_trend_scalp×USD_JPY -1.4pip | xs_momentum×GBP_USD -2.9pip
- ⚠️ **Shadow含み益+31.2pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — scalp_eur:hedge_block=357, rnb_usdjpy:direction_filter=339, daytrade_gbpusd:hedge_block=209

### 診断
## 原因分析

### 推測（ログ・数値から高確率で判断できるもの）

**① vix_carry_unwind +31.2pip がLIVE送信されていない**
- PAIR_PROMOTEDリストにUSD_JPYが含まれていないか、vix_carry_unwindストラテジーが昇格条件を未達のままshadow_trackingに留まっている
- ログに `[SKIP] shadow_tracking` が明示されており、OANDAへのルーティングが意図的にブロックされている

**② scalp_eur:hedge_block=357 の異常高値**
- EUR_USD方向でSELLポジション（a44ca5fb）が長時間保持されており、それに対するヘッジブロックが蓄積し続けている
- `[HOLD] DT含み益保護` が繰り返し発動しており、逆シグナルを全て無効化 → ヘッジブロックカウンタが解消されない正のフィードバックループに入っている

**③ rnb_usdjpy:direction_filter=339 の異常高値**
- USD_JPYのMTFが `range_tight`、d1=0、h4=1 で方向が定まらず、direction_filterが延々とブロックし続けている
- vol=squeeze 状態のため、レジーム判定が頻繁に切り替わりカウントが累積している

**④ daytrade_gbpusd:hedge_block=209**
- GBP_USDでも同様のヘッジブロック蓄積。`scalp_5m_gbp:hedge_block=173` と連動しており、GBP系全体でヘッジポジション保護が長期化している可能性

---

### 確認事項（断定前に要検証）

- `a44ca5fb` のSELL EUR_USDポジションのエントリー時刻・保持時間 → ヘッジブロック累積期間の特定
- PAIR_PROMOTEDの現在のリスト → vix_carry_unwindが昇格対象に入っているか
- vix_carry_unwindの昇格判定ロジック（pip閾値・保持時間条件）が現在の+31.2pipに対して適用されているか
- block_countsのリセットタイミング（セッション跨ぎでリセットされるか、手動リセット必要か）
- EUR_USDのWR=0%が継続している原因（TREND_BULLレジーム判定の根拠データ）
- main_loop_restarts=1 の再起動タイミングとblock_counts引き継ぎの有無

---

## 対処方針

**即時対応**

- `vix_carry_unwind×USD_JPY` をPAIR_

## Alert: 15:42 UTC

### 検出された問題
- 💰 **含み益+288.6pip** (16ポジション, LIVE=0 Shadow=16) | Top: dt_sr_channel_reversal×EUR_JPY +63.7pip | dt_sr_channel_reversal×EUR_JPY +56.8pip | dt_sr_channel_reversal×EUR_JPY +50.2pip
- ⚠️ **Shadow含み益+292.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=214

### 診断
## 原因分析

### 【推測】問題の原因

**① Shadow=16 / LIVE=0（OANDAへ未送信）**
- `dt_sr_channel_reversal` 戦略がShadowモードのまま運用されており、`PAIR_PROMOTED` リストに `EUR_JPY` が登録されていない
- Shadowモードはシグナル生成・含み益計算は行うが、実際の発注をスキップする設計になっている
- +292.9pipの含み益がすべて仮想であり、実利益ゼロの状態が継続中

**② `rnb_usdjpy:direction_filter=214` の異常値**
- 他のブロックカウントが20〜190台に分散しているなか、`rnb_usdjpy` の `direction_filter` だけが214と突出
- `direction_filter` はトレンド方向の不一致でシグナルを棄却するフィルタであるため、USDJPYが特定方向に強くトレンドしているか、フィルタ条件が過剰に厳しく設定されている可能性が高い
- あるいはフィルタロジックにバグがあり、条件判定が永続的にブロック側に固着している可能性もある

**③ DT含み益保護の連続発火**
- ログ全体がほぼ `[HOLD] DT含み益保護` と `Scalp MFEガード` で占められており、新規シグナルがほぼ全滅している状態
- EUR_JPYの含み益（+50〜+63pip）が大きく育っており、`threshold` を大幅に超えているため `SIGNAL_REVERSE` が全件無効化されている
- これ自体は設計通りの動作だが、ポジションが固着して新規エントリーの機会損失が発生し続けているリスクがある

---

### 【確認事項】実際に調査すべき項目

| # | 確認項目 | 確認方法 |
|---|---------|---------|
| 1 | `dt_sr_channel_reversal` の `PAIR_PROMOTED` リスト内容 | 設定ファイル・DB確認 |
| 2 | EUR_JPYがPAIR_PROMOTEDに追加された場合のリスク許容量（16ポジション同時発注になるか） | ポジション上限設定を確認 |
| 3 | `rnb_usdjpy:direction_filter` が急増したタイムスタンプ | ログのブロックカウント時系列を確認 |
| 4 | `direction_filter` の判定条件・パラメータ（閾値・参照足・方向定義） | rnb_usdjpyのロジック定義を確認 |
|

## Alert: 17:07 UTC

### 検出された問題
- 💰 **含み益+262.9pip** (15ポジション, LIVE=0 Shadow=15) | Top: vix_carry_unwind×USD_JPY +46.9pip | dt_sr_channel_reversal×EUR_JPY +46.7pip | dt_sr_channel_reversal×EUR_JPY +39.8pip
- ⚠️ **Shadow含み益+266.5pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【確定している事実】

- **17:06:33に日次損失CB（サーキットブレーカー）が発動**
  - 日次損失が -32.3pip に達し、制限値 -30pip を超過
  - OANDA向け全モード（daytrade / scalp）が自動停止
  - Shadow（デモ）モードは継続稼働中

- **Shadow側に+266.5pipの含み益があるがOANDAには未送信**
  - LIVE=0 / Shadow=15 の状態 → 全15ポジションがShadowのみ
  - CB発動前後のタイミングで新規エントリーがすべてShadow側で積み上がった可能性が高い

- **CB発動直前にSL_HIT（-6.0pip）が発生**（0d59c6ee）
  - これが日次損失を-30pipの閾値超えに押し込んだトリガーと思われる

- **含み益保護（SIGNAL_REVERSE無効化）が大量発火**
  - EUR_JPY・USD_JPY系BUY/SELL複数ポジションが保護対象として正常動作

---

### 【推測（要確認）】

| 推測 | 確認方法 |
|---|---|
| CB発動以前に日次損失がすでに-24〜-28pip程度蓄積していた可能性（1回のSL_HITで超過） | 当日の損益推移ログを遡って確認 |
| Shadow側の+266pipはCB発動後に積み上がったのか、CB発動前から存在したのか不明 | Shadowポジションのエントリー時刻を確認 |
| `PAIR_PROMOTED`未設定のため、Shadow益がLIVEに反映されない設計になっている | PAIR_PROMOTED設定リストを確認 |
| `rnb_usdjpy:direction_filter`が114回ブロックしており、USD_JPY系のシグナルが過剰に抑制されている可能性 | direction_filterの判定根拠（トレンド方向設定）を確認 |

---

## 対処方針

### 緊急対応（即時）

- **CB発動状態の確認と解除判断**
  - 本日の日次損失が確定しているか、それとも含み損が含まれているかを区別する
  - 翌営業日リセット待ちか、手動解除が必要かを確認する

- **Shadow側15ポジションの精査**
  - エントリー時刻・戦略・方向を確認し、LIVE昇格させるべきポジションを選別する
  - 既に含み益が大きいポジション（vix_carry_unwind×USD_JPY +46.9pip

## Alert: 19:03 UTC

### 検出された問題
- 💰 **含み益+321.3pip** (11ポジション, LIVE=0 Shadow=11) | Top: vix_carry_unwind×USD_JPY +61.5pip | dt_sr_channel_reversal×EUR_JPY +46.5pip | dt_sr_channel_reversal×EUR_JPY +39.6pip
- ⚠️ **Shadow含み益+322.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — scalp:max_open=256, scalp_eur:max_open=250

### 診断
## 原因分析

### 【確定している事実】

- **全11ポジションがShadowモード**（LIVE=0）→ OANDAへの実注文が一切送信されていない
- `scalp:max_open=256`、`scalp_eur:max_open=250` が突出して高い → scalpモード系が大量のシグナルを生成しているにもかかわらず、`max_open`ブロックで全て抑制されている
- `daytrade_eurjpy:hedge_block=176`、`rnb_usdjpy:direction_filter=179` なども高水準 → 多方向でブロックが積み重なっており、新規エントリーが実質機能していない
- `DT含み益保護`ログが数秒間隔で大量出力 → ループごとに全ポジションを走査しており、**ログ処理負荷が高い**（log_count=8110）
- `main_loop_restarts=1` → 過去に一度ループが落ちている

---

### 【推測】（要確認）

- **Shadow→Live昇格条件（PAIR_PROMOTED）が満たされていない可能性**
  - 推測：通貨ペアがPAIR_PROMOTEDリストに未登録、または昇格閾値（pip数・勝率・連続益など）に未到達
  - 確認事項：PAIR_PROMOTED設定ファイルまたはDB上の昇格条件と現在の評価値

- **`scalp`系のmax_openが異常に高い原因**
  - 推測①：max_open上限値の設定値自体が低すぎる（例：上限=5なのに256回ブロック）→ 設定値と実オープン数の乖離
  - 推測②：Shadowポジションがクローズされずに蓄積し、カウントが膨らんでいる
  - 確認事項：scalp/scalp_eurの現在オープンポジション数と`max_open`設定値

- **ループ再起動（restarts=1）の影響**
  - 推測：再起動後にShadowポジションの状態が正しく復元されなかった可能性
  - 確認事項：再起動タイミングとポジション生成タイミングの時系列照合

---

## 対処方針

### 即時対応

- **PAIR_PROMOTED登録を確認・追加**
  - USD_JPY（vix_carry_unwind）、EUR_JPY（dt_sr_channel_reversal）を昇格対象として明示的に追加を検討
  - 昇格基準（最低pip閾値・期間）を現状の含み益+321pipと照合し、条件を満たしているか確認

- **scalp/scalp_eur

## Alert: 20:47 UTC

### 検出された問題
- 💰 **含み益+178.3pip** (20ポジション, LIVE=0 Shadow=20) | Top: dt_sr_channel_reversal×EUR_JPY +57.9pip | dt_sr_channel_reversal×EUR_JPY +51.0pip | dt_sr_channel_reversal×EUR_JPY +44.4pip
- ⚠️ **Shadow含み益+248.1pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【確定している事実】

- **LIVE=0 / Shadow=20** → 20ポジション全てがShadowモードで動作しており、OANDAへの実注文が一切送信されていない
- **Shadow含み益+248.1pip** → システムは正常にシグナルを生成・保有しているが、実損益はゼロ
- `block_counts`の`max_open`・`hedge_block`が各ストラテジーで数十〜150件超と非常に多い → 新規エントリーが大量にブロックされている
- `daytrade_eurjpy:hedge_block=126` が突出して高い → EUR/JPYのヘッジブロックが特に深刻
- `[HOLD] DT含み益保護` ログが連続 → 既存ポジションの含み益保護ロジックが逆張りシグナルを正常に抑制中（これ自体は正常動作）
- `[SCORE_GATE] misaligned` ログ → スコアとシグナル方向の不整合によるブロック（これも正常動作）
- `main_loop_restarts=1` → 直近で1回再起動が発生している

---

### 【推測】（要確認）

- **PAIR_PROMOTEDにEUR/JPYが登録されていない可能性** → Shadowのまま昇格していない
- `hedge_block`多発の原因が「実ポジション不在によるShadow内部の擬似ヘッジ判定」なのか、「設定値が過剰に保守的」なのか不明
- `main_loop_restarts=1` の再起動原因（例外・OOM・設定リロード等）が不明
- Shadowモードに留まっている理由が「意図的な設定」なのか「PAIR_PROMOTED未設定のまま放置」なのか不明

---

## 対処方針

### 🔴 最優先

- **`PAIR_PROMOTED`にEUR/JPYを追加する前に、以下を必ず確認する**
  - Shadow運用期間・勝率・ドローダウンが昇格基準を満たしているか
  - 現在のShadow含み益が「本当に取れた利益」か、スリッページ・流動性リスクを考慮した上で評価する
  - 20ポジション同時保有のまま実注文に切り替えると証拠金・リスク管理上問題がないか確認

### 🟠 重要

- **`main_loop_restarts=1` の原因調査**
  - 再起動前後のログを確認し、例外スタックトレースを特定する
  - 再起動タイミングとポジション状態の整合性を確認（
