# Trade Monitor Alerts: 2026-06-15

## Alert: 02:28 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=487, daytrade_eur:hedge_block=250

### 診断
## 原因分析

### 【推測】各異常の主因

**`rnb_usdjpy:direction_filter=487`**
- RNBストラテジーがUSDJPYに対して方向フィルター（トレンド方向と逆のシグナル排除）を大量発動している
- 現在のUSDJPY相場が強いトレンド状態にあり、逆張りシグナルが連続生成され続けている可能性が高い
- シグナル生成ロジック自体が現相場環境に対してノイズを多発している可能性

**`daytrade_eur:hedge_block=250`**
- EUR系通貨ペアでヘッジポジションが長時間・大量に維持されており、新規エントリーがブロックされ続けている
- ヘッジ解除条件（価格戻り・決済トリガー）を満たせないまま膠着している可能性
- `daytrade_gbpjpy:hedge_block=150`、`daytrade_1h_audjpy:hedge_block=102` も同様のパターンで、複数通貨ペアにまたがっているため、相場全体のレンジ膠着またはヘッジ解除ロジックの閾値問題が疑われる

---

### 【確認事項】

**即時確認すべき項目**

- [ ] RNBストラテジーの現在のトレンド判定値・direction_filterの閾値設定を確認（フィルターが厳しすぎないか）
- [ ] `rnb_usdjpy:direction_filter` のカウント増加速度を確認（今日に入って急増したか、以前から高水準か）
- [ ] EUR系ペアで現在保有中のヘッジポジションの建値・含み損益・ヘッジ解除条件を確認
- [ ] `daytrade_eur` のヘッジブロック発動が今日いつ頃から急増したか（ログで時刻を特定）
- [ ] `main_loop_restarts=1` の再起動タイミングとhede_block急増の前後関係を確認

**追加で確認すべき項目**

- [ ] `fib_reversal × USD_JPY` の R2_SHADOW_DEMOTE が高頻度（ログ内で10件以上集中）→ USD_JPYのfibリバーサルセルが継続的に降格され続けている原因（価格レンジ、パラメータ設定）
- [ ] HOLDログで `dc25f1eb / 121b58af` の profit=0.021〜0.025 で長時間保護されているSELLポジションが正常にBE_LOCK後も決済されていないか確認
- [ ] ヘッジブロック

## Alert: 09:49 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=560, daytrade_1h_audjpy:hedge_block=286, scalp:r2_shadow_demoted_cell=212, scalp_eur:r2_shadow_demoted_cell=202

### 診断
## 原因分析

### 推測（根拠あり）

**① `rnb_usdjpy:direction_filter=560`（最重大）**
- 他の`direction_filter`カウンタが存在しない中でこの値だけ突出しており、USD/JPY方向フィルタが恒常的にシグナルを棄却し続けている
- 160円台という水準でトレンド判定ロジックが「常に逆方向」と判断するパラメータ設定になっている可能性が高い
- またはフィルタ条件（MA乖離・ATR・バイアス閾値等）の基準値が現在のボラティリティレンジに合っていない可能性

**② `daytrade_1h_audjpy:hedge_block=286`**
- AUD/JPYで長時間にわたりヘッジポジションが解消されず、新規シグナルが継続ブロックされている
- 既存のヘッジポジションが含み損・ロック状態のまま残留しているか、ヘッジ解除条件の閾値に価格が到達していない可能性

**③ `scalp:r2_shadow_demoted_cell=212` / `scalp_eur:r2_shadow_demoted_cell=202`**
- ログと完全一致：`fib_reversal x EUR_USD`、`ema_trend_scalp x EUR_USD`、`sr_channel_reversal x USD_JPY`が数秒おきに大量demoteされている
- R2セルがシャドウ昇格後すぐにdemoteされるループに入っている＝シャドウ追跡セルの昇格条件と降格条件が現在の価格動作に対してバウンスしている状態
- EUR/USDが小幅レンジでチョッピングしており、セルの有効期限または確度条件が揺れ続けている可能性

---

### 確認が必要な事項

| 項目 | 確認内容 |
|---|---|
| rnb_usdjpy | `direction_filter`の判定ロジック（MA方向・バイアス閾値）と現在のUSD/JPY価格帯との整合性 |
| rnb_usdjpy | 直近のフィルタ通過率ログ or フィルタ値の時系列変化 |
| daytrade_1h_audjpy | AUD/JPYの現在オープンポジション・ヘッジペアの状態（ブローカーAPI確認） |
| daytrade_1h_audjpy | `hedge_block`の解除条件（価格乖離幅・時間条件）が満たされているかのログ |
| R2 shadow demote | `fib_reversal` / `ema_trend_scalp` / `sr_channel_rever

## Alert: 15:34 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=261

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=261 の異常値について

- **`direction_filter` が261回という値は他のブロック項目と比較して突出して高い**（次点の `scalp:r2_shadow_demoted_cell=196` より65件多い）
- `rnb_usdjpy` モジュールが生成するシグナルの方向（BUY/SELL）が、現在の相場レジームまたはフィルタ条件と**継続的に不一致**を起こしている可能性が高い
- USDJPY が現在 **トレンド方向が不明瞭または逆方向**にあり、rnbストラテジーのシグナル方向判定ロジックが相場実態とずれている可能性
- daytrade側でも `score_gate` ブロックが発生しており（`streak_reversal score=1.68 misaligned with signal=SELL`）、**USDJPY全体でシグナルの方向性混乱**が生じている傍証となる

---

### 【推測】その他の副次的状況

- `r2_shadow_demoted_cell` がscalp系で多発 → ema_trend_scalp / sr_channel_reversal がUSDJPY・GBPUSDで**シャドウトラッキングに降格され続けている**（精度低下中）
- `daytrade_gbpusd` の `SHADOW Slot bypass` が頻発かつ直近でSL_HIT（-8.1pips）→ GBPUSDも不安定な状態
- `main_loop_restarts=1` はあるが `emergency_killed=false` → システム自体は継続稼働中

---

## 確認事項

| 優先度 | 確認内容 |
|--------|----------|
| 🔴 高 | `rnb_usdjpy` の `direction_filter` がどの条件で発火しているか（フィルタ定義：トレンド方向判定の基準指標を確認） |
| 🔴 高 | rnb_usdjpyのシグナル方向（BUY/SELL比率）と直近のUSDJPY価格トレンドの乖離の有無 |
| 🟡 中 | `direction_filter` の閾値カウントがリセットされる条件・タイミング（累積なのか時間窓なのか） |
| 🟡 中 | `main_loop_restarts=1` の発生タイミングと、rnb_usdjpy初期化との関連 |
| 🟡 中 | `scalp:r2_shadow_demoted_cell=196` の多発開始時刻とUSDJPY相場の変動点の一致確認 |
| 🟢 低 | `dayt

## Alert: 19:35 UTC

### 検出された問題
- 🔴 **含み損-30.1pip** (6ポジション) → ポジション確認推奨
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=315, daytrade_eurgbp:hedge_block=296

### 診断
## 原因分析

### 推測（ログ・数値から読み取れる仮説）

**① rnb_usdjpy:direction_filter=315（異常高値）**
- USDJPYが一方向に強いトレンドを継続しており、rnbロジックの方向フィルターが長時間同一方向をブロックし続けている可能性
- フィルター条件がリセットされずにカウントが積算されている（バグ or 設計上の上限なし）可能性
- 含み損-30.1pipと連動している可能性が高い（逆方向ポジションが建ってしまっている）

**② daytrade_eurgbp:hedge_block=296（異常高値）**
- EURGBPでヘッジポジションが既に存在し、新規エントリーが長時間ブロックされ続けている
- ヘッジロックが解除されないまま積算 → ヘッジポジション自体が含み損の一部である可能性

**③ R2_SHADOW_DEMOTE の連続発火（bb_rsi_reversion / fib_reversal × USDJPY）**
- USDJPY上で shadow-tracking セルが繰り返しdemoteされており、相場のボラティリティまたはトレンド継続により逆張りシグナルが全滅状態
- 約3分間で30件超 → 通常の発火頻度を大幅に超えており、シグナル生成ループ自体が異常な頻度で回っている疑い

---

### 確認事項（要実機確認）

| # | 確認項目 | 確認方法 |
|---|----------|----------|
| 1 | 含み損6ポジションの内訳（通貨ペア・方向・建値） | ブローカーポジション画面 or APIで即時確認 |
| 2 | rnb_usdjpy:direction_filterのリセット条件が満たされているか | rnbロジックのフィルタースコープ・TTL設定を確認 |
| 3 | daytrade_eurgbpのヘッジポジションが実際に存在するか、それとも幽霊ロックか | EURGBPポジション有無とblock_countsの照合 |
| 4 | main_loop_restarts=1 の再起動タイミングとblock_counts積算開始の相関 | ログの起動時刻とカウント急増タイミングを突合 |
| 5 | R2_SHADOW_DEMOTEの発火間隔（約10〜15秒）がループ間隔と一致しているか | scalp/daytrade各modeのinterval設定と照合 |

---

## 対処方針

### 即時対応（優先度
