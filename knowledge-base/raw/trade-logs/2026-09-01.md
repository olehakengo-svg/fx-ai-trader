# 2026-09-01 Daily Trade Log (wiki-daily-update)

**Pull times**: demo/stats・learning・risk・audit = 2026-09-01 **11:47–11:52 UTC** / status・block-counts・equity = **11:52:37 UTC**
**Clock check**: ✅ local UTC 11:52:37 vs server `row_freshness_now` 11:52:37.787 — **skew 0s**. The 08-26 ~5h40m skew does not reproduce (**3rd consecutive non-reproduction**).
**Audit window**: `limit=800` = 2026-08-17T12:46:26Z → **2026-09-01T11:35:20Z** (14.95 days, `total` 16,153; `execution_audit_count` 16,153 at 11:52Z).

---

## 1. Realized book — bit-flat for the **4th** consecutive snapshot

| Field | Value | vs 08-31 |
|---|---:|---|
| N (live_count / total) | **579** | flat |
| W / L / BE | 252 / 292 / 35 | flat |
| WR | **43.5%** | flat |
| decided WR | **46.3%** | flat |
| EV | **−1.06** | flat |
| total_pnl | **−611.1 pip** | flat |
| Wilson lower | 42.2 | flat |
| Wilson BF lower | 39.4 | flat |
| avg R | 0.12 | flat |

Every realized field is bit-for-bit identical to 08-31 **and** to both 08-28 runs. **Zero closed
fills across ~4 days** (08-28 11:52Z → 09-01 11:52Z).

⚪ **Still suppression, not outage — the engine is provably alive and the shadow book advances:**

- `shadow_count` 12,655 → **12,719 (+64)**
- `oanda_audit.total` 16,080 → **16,153 (+73)**; `execution_audit_count` **16,153** (the two agree
  exactly this pull, unlike 08-31 where they differed by 15)
- `engine_tick_age_sec` **0.3s**, **24** running modes, stalest mode `daytrade_1h_eur` 62.8s
- `main_loop_alive` / `watchdog_alive` / `sltp_checker_active` = true, `main_loop_restarts` still **1**
- OANDA heartbeat `ok`, latency **103ms**, `last_check` 11:51:15Z

🟢 **Freshness recovered from the 08-31 slowdown**: `last_candidate_row_age_sec` **7.8s** (08-31:
775.3s) — the late-Sunday lull is gone. `last_trade_row_age_sec` **1037.8s (17.3 min)**, `status: ok`.

---

## 2. Risk dashboard — the positive window is now **frozen in place**, and it is still arithmetic

The window rolled again (`effective_date_from` 08-01T13:41:34 → **08-02T11:51:00**, +22.2h) but
**n stayed at 13 and every headline number is bit-identical to 08-31**:

| Field | 08-28 | 08-31 | **09-01** |
|---|---:|---:|---:|
| Kelly edge | 0.0 | +0.6709 | **+0.6709** |
| recommended | — | half_kelly 0.128 | **half_kelly 0.128** |
| Sharpe (observed) | −0.0927 | +0.3067 | **+0.3067** |
| DSR / haircut | 0.0 / 100% | 0.5624 / 15.1% | **0.5624 / 15.1%** |
| 30d net | −58.4 | +94.3 | **+94.3** |
| n_trades | 16 | 13 | **13** |

⇒ No trade entered the window and none aged out of it. **This is the same 13 trades re-reported a
day later, not a persisting edge.** The 08-31 verdict stands verbatim: **do not propagate as edge
recovery** — this is the **5th consecutive window-arithmetic reading**.

🔴 **The production sizing gate still disagrees, at the same value.** All **3** blocked rows in this
window read `agg_kelly=-0.315<0` — the identical figure to 08-31's 6 blocked rows. The dashboard
(n=13 rolling-30d live slice) and the gate (aggregate book) remain openly divergent, and **the
gate's number is the one that deploys capital**.

Nothing in the window is statistically significant: `dsr_overall.is_significant` **false**,
`small_sample_lowconf` **true**, all three per-strategy Kellys **`insufficient`** (n=9/3/1).
Per-strategy DSR is computed only for carry-dip (0.5193 / haircut 5.2% / **not significant**, z=0.048).

Window composition (n=13, unchanged): USD_JPY 9 **+84.0** / AUD_JPY 1 +20.1 / EUR_GBP 3 −9.8.
All cross-pair correlations **null** (insufficient overlap), `flagged_pairs` empty.

**Defensive posture unchanged**: `defensive_mode` **true**, `lot_multiplier` **0.2**,
DD **1198.8 pip** (`dd_pct` 0.097 on the JPY-NAV basis = **119.9%** on the legacy $1000 pip basis),
`eq_current` −$1181.9 vs peak +$16.9 — **all flat**.

⚠️ **`attribution` friction still not subtracted** — `gross_pnl = net_pnl = alpha = 94.3` with
`friction = 42.5` (`avg_friction_per_trade` 3.27). Correctly applied the 30d net is **+51.8**.
Existing, unresolved, **not diagnosed this run**.

---

## 3. Live path — 6th day with no fill

**Last live fill is still `price_shock_rev_aud_jpy_h1_long` AUD_JPY oanda #709537 (2026-08-26
14:59:26Z) — 6.0 days ago.**

New slice **08-31T13:32:50Z → 09-01T11:35:20Z (74 rows, 21.96h)**:

| bridge_status | count |
|---|---:|
| `skipped` (all `shadow_tracking`) | **71** |
| `blocked` (all `agg_kelly=-0.315<0`) | **3** |
| `sent` | **0** |
| `filled` | **0** |

**Live rate 0.0% — 2nd consecutive zero-fill slice.**

- Instruments: USD_JPY 19 / GBP_USD 17 / EUR_USD 11 / AUD_JPY 10 / GBP_JPY 9 / EUR_JPY 5 / EUR_GBP 3
- Top entry types: `session_time_bias` 10 / `dt_sr_channel_reversal` 5 / `streak_reversal` 5 /
  `sr_break_retest` 5 / `dt_bb_rsi_mr` 4 / `wick_imbalance_reversion` 4 / `sr_weighted_bounce` 4 /
  `dual_sr_bounce` 4
- **zero-unit emission**: **10/74 = 13.5%** in the new slice, **135/800 = 16.9%** across the full
  window. All `bridge_status=skipped` ⇒ **zero live exposure**, but under shadow-first the shadow
  book *is* the estimator. Property unchanged since 08-23, **still undiagnosed**.

Within the 15-day pull the accept/reject contract holds: **7 sent / 7 filled / 7 real OANDA ids /
false-sent 0** (window truncated at 08-17; the full cumulative is 10/10 per the 08-31 log). The
07-02 contract is intact.

---

## 4. 🔴 `hedge_block` shadow→live suppression — **4th independent confirmation**

`/api/demo/status` `open_trades` = **4 rows, every one `is_shadow: 1`**, against broker
`open_trade_count: 0` / `margin_used: ¥0`:

| entry_type | mode | instrument | dir | entry_time (UTC) |
|---|---|---|---|---|
| `session_time_bias` | daytrade_gbpusd | GBP_USD | SELL | 09-01 11:35:20 |
| `vol_spike_mr` | daytrade | USD_JPY | BUY | 09-01 11:31:38 |
| `session_time_bias` | daytrade_eur | EUR_USD | SELL | 09-01 10:04:55 |
| `session_time_bias` | daytrade_eur | EUR_USD | SELL | 09-01 09:49:38 |

**The open book has now turned over completely three times (6 → 5 → 3 → 4 rows, no strategy
carried over) with the property preserved.** This is independent corroboration of the 08-28 code
read (`modules/demo_trader.py:4915-4928`, `_mode_inst_live + _mode_inst_shadow`).
**Not a bug — a decision (rule:R2/H2). Still awaiting user adjudication. No code touched.**

🟢 **New observation — the open shadow rows carry populated metadata.** All 4 have a **non-empty
`alpha_snapshot`** (KSFT/KSFT2/RSV10/ROC10/QTLD5, `stale_bars: 0`) and 2 of 4 carry
`edge_cell_id = "E8"`. This is the opposite of the 100%-empty condition recorded in
`project_sr_anti_hunt_demo_trades_meta_loss_2026-06-03`. ⚠️ **Scope caveat**: that memo is about
**closed `sr_anti_hunt_bounce` shadow rows**, and none of these 4 are that strategy — this is
**not** evidence the corruption is fixed, only that the currently-open book is clean. A targeted
re-query of `sr_anti_hunt_bounce` rows is still required.

### Block family shares (single-pull only)

⚠️ **Counter window rolled again** — total ticks **5,228** this pull vs 33,473 on 08-31, with
`main_loop_restarts` still 1. Per the 08-28 rolling-counter rule, **no cross-pull deltas are
computed**; within-window shares only.

| family | count | share | (08-31 share) |
|---|---:|---:|---:|
| `hedge_block` | 555 | **34.2%** | 28.4% |
| `r2_shadow_demoted_cell` | 534 | **32.9%** | 28.8% |
| `order_bar_dedup` | 257 | 15.8% | 23.1% |
| `direction_filter` | 235 | 14.5% | 16.7% |
| `same_price_0pip` | 17 | 1.0% | — |
| `conf<30` | 16 | 1.0% | 0.4% |

Total **1,625** blocks over **5,228** ticks across 24 modes. `hedge_block` retakes the #1 slot it
lost by 0.4pp on 08-31 — **shares are approximately conserved and this is not evidence of a gate
change.**

---

## 5. 🔴 `rnb_usdjpy:direction_filter` — **5th consecutive confirmation**

**235 blocks / 235 ticks = ratio 1.0000** — every single tick in a freshly re-windowed counter was
rejected.

| pull | blocks | ticks | ratio |
|---|---:|---:|---|
| 08-26 | 2,624 | 2,622 | 1.0008 |
| 08-28 05:00Z | 2,624 | 2,622 | 1.0008 |
| 08-28 11:52Z | 630 | 630 | 1.0000 |
| 08-31 17:14Z | 1,477 | 1,509 | 0.9788 |
| **09-01 11:52Z** | **235** | **235** | **1.0000** |

A selective filter cannot produce this. Consistent with the known `compute_rnb_signal` WAIT-path
`entry: 0` bug. **The code read is still not done — 5 runs running as the top open follow-up.**

---

## 6. 🔴 `/api/oanda/equity` — now **22 days stale**

Last curve point still **2026-08-10T07:20:34Z**, `count` still **928**, `total_pl_jpy` −80,686,
`total_pl_pips` −942.2. Progression **15d → 18d → 21d → 22d**, monotonically worsening across four
snapshots while 10 live fills have closed.

**JPY ledger drift unchanged**: `jpy_ledger.eq_current_jpy` **¥324,450.50** vs broker NAV
**¥278,345.48** = **¥46,105.02** — identical to 08-31 to the yen. Sizing impact still none (the
`DD_LOT_TIERS` chain is saturated at 0.20×); reporting impact still material (headline DD
understates broker-measured DD).

---

## 7. Learning API — frozen for a **7th** snapshot

- Latest adjustment **id=93** (2026-08-19 14:25:37) — **13.1 days stale**, unchanged for 7
  consecutive snapshots
- `entry_type_blacklist` still **`[]`** ⇒ the exclusion is a **no-op, unresolved since 06-30**.
  Of the 30 journaled adjustments, ids 90–93 are byte-identical re-affirmations of the same
  `sr_channel_reversal` exclusion
- `current_params`: conf threshold 30, `learn_every_n` 10, `max_consecutive_losses` 3,
  `max_open_trades` 8, daily-loss and max-DD limits both **−99999** (disabled)
- daytrade EV **−2.60** (n=94, WR 41.5% — high −1.12/n32, mid −4.07/n39, low −2.18/n23; **every**
  confidence band negative)
- scalp EV **−0.15** (n=388, WR 40.7% — high −0.71/n54, mid −0.39/n207, **low +0.48/n127** is the
  only non-negative band)
- swing `ready: false` (sample 0, min_required 10)

**Bit-identical to 08-31, 08-28 (both runs) and 08-26.**

---

## 8. Strategy pages / tier — **no update**

Zero closed fills ⇒ no cell gained or lost a trade, and no promote/demote threshold is touched.
The two live-path strategies are unchanged from 08-28 and 08-31:

| strategy | N | WR | PnL | Wilson / BF |
|---|---:|---:|---:|---|
| `usdjpy_carry_dip_accumulator` | 9 | 55.6% | **+84.0** | 26.7 / 15.7 |
| `price_shock_rev_aud_jpy_h1_long` | 3 | 66.7% | **−102.5** | 20.8 / 9.9 |
| `price_shock_rev_eur_gbp_h1_long` | 3 | 0.0% | −9.8 | 0.0 / 0.0 |

🔴 **carry-dip remains the book's only materially positive cell, and its declared 150p SL vs the
measured 18.8p replacement is still the highest-value open item** (`_1H_PRESERVE_SLTP` unregistered,
R3 adjudication pending).

---

## 9. Lint

1. **Numeric consistency ✅** — `wiki/index.md`, `2026-08-31.md` and this file all carry
   N=579 / WR 43.5% / PnL −611.1 / DD 1198.8. No cross-page disagreement.
2. **`tier_integrity_check --check` → PASS** (exit 0). 2 warnings, both known:
   `hull_donchian_fade` QUICK_HARVEST_EXEMPT outside ELITE/PAIR_PROMOTED; no strategy file for
   `ob_retest`.
   ⚠️ **The "legacy dead inline" INFO contradiction has returned, on a different name.** All 14
   INFO entries claim "no production firing in 30+ days"; cross-checking each against this pull's
   live counters, **13 have zero counters (consistent) but `ob_retest` has 6 audit rows** — all
   GBP_JPY BUY, `bridge_status=skipped`, **2026-08-20 03:16→07:01Z = 12.2 days ago**, not 30+.
   Same root cause as the `ihs_neckbreak` contradiction reported 08-26→08-28: **the checker reads
   local state rather than the Render API.** (08-31 reported this contradiction as resolved; it was
   resolved *for `ihs_neckbreak`*, and the underlying checker defect was not.)
3. **`sync_kb_index --check` → PASS, "index.md is in sync"** (exit 0) — **2nd consecutive clean
   pass, `--write` not required.**
4. **Broken wikilinks** (path-aware resolver):
   - `wiki/index.md` **7** — `2026-04-27`, `2026-04-29`, `ma-regime-switch`, `ma-trend-perfect`,
     `mqe-gbpusd-fix`, `ob-retest`, `vsg-jpy-reversal`. **Identical set to 08-23 / 08-26 / 08-28 /
     08-31.**
   - `wiki/strategies/` non-MEMORY **1** — `xs-momentum-rsi-tv-phase2-2026-05-13`. Unchanged.
   - KB-wide **77** distinct, of which 29 are MEMORY-style (`project_*` / `feedback_*` /
     `reference_*`, expected to be unresolvable inside the KB) ⇒ **in-KB 48** (08-31: 44).
   - ⚠️ **One genuinely new in-KB broken link, and it is not from this run**:
     ``[[kalman-d7-minlot-carveout-prereg-2026-09-01]]`(未作成)`, referenced from
     `wiki/sessions/2026-09-01-session.md` (written by today's interactive session). The pre-reg
     document it points at has not been created. **This run introduced 0 broken links.**
5. **Staleness** — 🟢 audit current (`last_candidate_row` 11:52:30Z = 7.8s);
   🔴 `/api/oanda/equity` **22 days**; 🔴 learner **13.1 days**.
6. ⚠️ **`limit=30` insufficient again — 8th consecutive confirmation.** `limit=30` covers
   **5.31 hours** (09-01 06:17→11:35Z) with **0 live rows**; `limit=800` covers **14.95 days** with
   **14 live rows**. Today's "no new fill" conclusion happens to survive at limit=30, but *when the
   last fill was* and *that 7 fills sit in the recent window* are only determinable at the larger
   limit. Per `feedback_audit_limit30_hides_live_fills`.

---

## 10. Process note (continuing)

`raw/trade-logs/2026-08-29-pre_tokyo.md` exists but **no 08-29 entry appears in `wiki/log.md` or in
`index.md` Session History** — the 2nd instance of "wiki-daily Phase 3/6 completes partially",
first detected 08-26. **No retroactive entry has been fabricated**; recorded and left as-is.

---

## 11. Open items (unchanged unless noted)

- 🔴 `usdjpy_carry_dip_accumulator` declared 150p SL vs measured 18.8p — **highest-value open item**
- 🔴 `rnb_usdjpy` code read — **5 runs outstanding, top follow-up**
- 🔴 `hedge_block` shadow→live adjudication (rule:R2/H2) — user decision
- 🔴 `/api/oanda/equity` 22-day ingestion gap
- 🔴 08-21→08-26 105.5h outage — cause never established (no recurrence this window)
- 🔴 Price-Shock family demote decision
- ⚠️ JPY ledger ¥46,105.02 drift; `attribution` friction not subtracted; learner blacklist no-op;
  zero-unit emission; `API_AUTH_TOKEN` watchdog gap; `sr_anti_hunt_bounce` metadata corruption
  (see §4 for a partial, non-conclusive positive signal)
- ⚠️ `tier_integrity_check` reads local state, not the Render API (recurring INFO contradiction)
