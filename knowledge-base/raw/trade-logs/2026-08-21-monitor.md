# Trade Monitor Alerts: 2026-08-21

## Alert: 04:25 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=260

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=260`** は他のblock_countsと比較して突出して高い（次点の`daytrade_eurjpy:order_bar_dedup=118`の約2倍）
- `rnb_usdjpy`モードがシグナルを高頻度で生成しているが、**方向フィルター（direction_filter）に大量に弾かれている**状態
- ログの`[MTF_MONITOR]`に`d1=0 h4=-1`とあり、**上位足（D1フラット、H4下向き）とrnbのエントリー方向が整合していない**ため、direction_filterが継続的にブロックしている可能性が高い
- `mtf=range_wide`かつH4が逆向きのため、相場環境自体がrnbモードの方向条件を満たしにくい状態と推測

### 推測（中確度）

- `rnb_usdjpy`のシグナル生成ロジックが**相場環境の変化（レンジ拡大・上位足の方向転換）に対してフィルター条件を更新できていない**
- main_loop_restarts=1が発生しており、**再起動前後でdirection_filterの内部カウンターがリセットされずに蓄積**されている可能性

### 推測（低確度）

- direction_filterの閾値パラメータ自体が現在の相場ボラティリティに対して不適切になっている

---

## 確認事項

- [ ] `rnb_usdjpy`のdirection_filterが参照しているD1・H4・MTFの現在値を確認（どの足が阻害しているか特定）
- [ ] direction_filterのブロック開始時刻を確認（いつから260件が積み上がったか、main_loop_restart前後かを判断）
- [ ] `rnb_usdjpy`のシグナル生成頻度が異常に高くなっていないか（ポジション変化なしで連続発火していないか）
- [ ] block_countsの集計期間がリセットされているかどうか（起動からの累積か直近Nバーかの定義確認）
- [ ] 他通貨ペアのrnbモードに同様のdirection_filterブロックが発生していないか

---

## 対処方針

- **即時**
  - `rnb_usdjpy`のdirection_filter条件（参照する上位足・判定ロジック）を手動確認し、現在の相場環境（range_wide + H4逆向き）での意図的ブロックであれば**正常動作として扱う**
  - 異常な連続シグナル発火が起きている場合は`rnb_

## Alert: 06:22 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=471, daytrade_1h_nzdusd:hedge_block=241

### 診断
## 原因分析

### 推測（要確認）

**① rnb_usdjpy:direction_filter=471（突出して高い）**
- RNBストラテジーのUSDJPY方向フィルターが長期間・大量にシグナルを棄却し続けている
- 推測原因：USDJPYが一方向トレンド継続中のため、逆張り方向のシグナルが全棄却されている。または、direction_filterの基準値（閾値・参照指標）がズレており、フィルター自体が機能不全に陥っている
- 本来のトレード機会を大量に逃している可能性が高い

**② daytrade_1h_nzdusd:hedge_block=241（同一カテゴリ内で突出）**
- 1H NZDUSDのヘッジブロックが他通貨ペア比で2〜3倍以上の頻度
- 推測原因：NZDUSDポジションが長期間オープンのまま残存しており、ヘッジ判定が継続的に発動している。またはヘッジ判定ロジックがNZDUSD固有の価格帯・スプレッド条件で誤作動している

**③ R2_SHADOW_DEMOTE の連続発生（EUR_USD / GBP_USD）**
- ema_trend_scalpセルが約6時前後にわたり断続的にdemoteされ続けている
- 推測原因：EUR/GBP系でボラティリティが高い、またはシャドウセルのスコア閾値が現在の相場環境に適合していない

---

## 確認事項

| 優先 | 確認内容 |
|------|----------|
| 🔴高 | rnb_usdjpy の direction_filter 判定ロジック・閾値パラメータ、および参照指標（MA/ADX等）の現在値 |
| 🔴高 | NZDUSDの現在オープンポジション有無・保有時間・含み損益 |
| 🟡中 | daytrade_1h_nzdusd のヘッジ判定条件（price距離・ATR比較基準）が現在の値動きと整合しているか |
| 🟡中 | 08/21 06:00頃のEUR/GBP系の価格変動幅・ニュースイベント有無 |
| 🟡中 | main_loop_restarts=1 の再起動タイミングと上記異常の発生開始時刻の相関 |
| 🟢低 | R2_SHADOW_DEMOTEは既知の防御機構として正常動作の範囲内かを閾値と照合 |

---

## 対処方針

**即時対応**

## Alert: 08:23 UTC

### 検出された問題
- 💰 **含み益+29.4pip** (6ポジション, LIVE=0 Shadow=6) | Top: wick_imbalance_reversion×AUD_JPY +15.7pip | donchian_momentum_breakout×AUD_USD +7.0pip | donchian_momentum_breakout×AUD_JPY +6.0pip
- ⚠️ **Shadow含み益+31.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】問題の根本原因

**①　Shadow→Live昇格が起きていない（最重要）**
- 6ポジションすべてがShadowモードのまま。PAIR_PROMOTEDリストにAUD_JPY・AUD_USDが含まれていないため、含み益+31.6pipがOANDAに一切送信されていない
- `r2_shadow_demoted_cell`がscalpおよびscalp_5m系で計161件に達しており、Shadow→Live昇格の判定ロジックが抑制または未トリガーの可能性が高い

**②　ORDER_BAR_DEDUPが大量発生し、追加エントリーを全ブロック**
- 同一バー（08:00・08:15）に対して同一シグナルが繰り返し発火している
- シグナル生成ループの間隔（30秒/60秒）に対してバーの更新タイミングが合っておらず、同一bar_tsで何十回も発火する構造的な重複が発生中
- AUD_JPY単独でdaytrade_audjpy:order_bar_dedup=60件、daytrade_1h_audjpy=9件と突出

**③　HEDGE_BLOCKが特定ペアで多発**
- NZD_USDで78件、AUD_JPYで80件、EURで87件。ヘッジポジションが残存したまま新規エントリーが抑制されている可能性

**④　main_loop_restartが1回発生**
- ループ再起動後にShadow/Live状態の引き継ぎが正常にできているか不明

---

### 【確認事項】（実機で要確認）

- [ ] PAIR_PROMOTEDの現在のリストにAUD_JPY・AUD_USDが含まれているか
- [ ] r2_shadow_demoted_cellのdemote判定条件（閾値・ロジック）が意図通りに動いているか
- [ ] ORDER_BAR_DEDUPのbar_tsキャッシュがループ再起動時にリセットされていないか（再起動が重複発火の原因である可能性）
- [ ] HEDGE_BLOCKの元となるヘッジポジションがOANDA上に実際に残っているか、または誤検知か
- [ ] main_loop_restart後にShadowポジションの状態（profit等）が正しく引き継がれているか

---

## 対処方針

### 即時対応

- **PAIR_PROMOTEDにAUD_JPY・AUD_USDを追加**し、Shadow→Live昇格を許可する。含み益が乗っている今が昇格タイミングとして適切
- ただし昇格前に

## Alert: 10:17 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=338

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=338 の異常値について

- **direction_filterブロックが338回**に達しており、他のブロック種別（最大161）と比較して突出している
- `rnb`（おそらくRange-N-Breakまたは類似のレンジ系戦略）がUSDJPYに対して、方向フィルターを通過できない状態が長時間継続していると推測
- ログに`HOLD: DT含み益保護: BUY 953562cf`が繰り返し出現しており、**既存BUYポジションが保有継続中**のため、逆張り・反対方向シグナルがフィルターで大量遮断されている可能性が高い
- USDJPYが一方向（上昇）に偏ったトレンドまたはレンジ上限張り付き状態にあり、rnb戦略のシグナル方向と市場の実勢方向が継続的に不一致を起こしていると推測

### 【推測】副次的な異常

- `ORDER_BAR_DEDUP`でstreak_reversal USD_JPY SELLが同一バー（10:00:00）で繰り返しブロックされており、シグナル生成ループが過剰に回っている可能性あり
- `main_loop_restarts=1`があり、再起動後にカウンターがリセットされずに累積している可能性

---

## 確認事項（実機・ログで要検証）

- [ ] `direction_filter`の判定ロジック：フィルター解除条件（閾値・タイムアウト・ポジション決済連動）が正常に機能しているか
- [ ] `rnb_usdjpy`のシグナル生成頻度：単位時間あたりの発火回数が仕様範囲内か（ポーリング間隔とシグナル頻度の整合性）
- [ ] 既存BUYポジション`953562cf`の状態：決済済みか保有継続中か。保有継続中であればdirection_filterブロックは意図的動作の可能性あり
- [ ] `direction_filter`カウンターのリセットタイミング：日次リセット・再起動リセットの仕様を確認し、338が累積値か直近値かを判別
- [ ] `main_loop_restarts=1`の発生時刻とカウンター初期化の有無

---

## 対処方針

- **即時確認**
  - ポジション`953562cf`の現在状態を確認し、保有継続中であれば含み益保護の動作として正常か判断する
  - `direction_filter`が**ハードブロック**（フィルター解除ト

## Alert: 12:20 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=553, daytrade_gbpusd:hedge_block=288, scalp_5m_gbp:r2_shadow_demoted_cell=286, scalp:r2_shadow_demoted_cell=283, daytrade_audjpy:hedge_block=259

### 診断
## 原因分析

### 推測（ログ・カウントから高確率で推定）

**① `rnb_usdjpy:direction_filter=553`（最多・最深刻）**
- RNB戦略がUSDJPYに対して一方向フィルタを長期間かけ続けている
- 相場が単一トレンドに固着しており、フィルタ条件が解除されない状態が継続中
- またはフィルタの閾値・リセット条件にバグがあり、カウンタが解放されずに積み上がっている可能性

**② `daytrade_gbpusd:hedge_block=288` / `daytrade_audjpy:hedge_block=259`**
- 既存ポジションとの方向衝突によるヘッジブロックが大量発生
- GBPUSDおよびAUDJPYで逆張りシグナルが連続発生しているが、既存ポジションが長時間残留しているため解消されない
- HOLDログ（含み益保護）が頻出しており、ポジションが利確・反転できずにブロック源となっていることと整合する

**③ `scalp_5m_gbp:r2_shadow_demoted_cell=286` / `scalp:r2_shadow_demoted_cell=283`**
- R2シャドウセルがデモート（格下げ）され続けており、スキャルpモードのGBP系シグナルがほぼ全滅状態
- シャドウスロットが埋まらない（`shadow=0/2`のままbypassが続く）ことと連動している可能性
- R2モデルの評価スコアが閾値を下回り続けているか、シャドウ昇格条件が現在の相場環境に適合していない

**④ `ORDER_BAR_DEDUP`の大量発生（全体）**
- 同一バータイムスタンプのシグナルが複数ループにわたり再発火している
- インターバル設定（30秒・60秒）に対してバーの更新頻度が追いついておらず、同一バーを繰り返し処理している

---

### 確認事項（断定不可・要調査）

| 確認項目 | 確認方法 |
|---|---|
| `direction_filter`のリセット条件が正常に発火しているか | RNB戦略のフィルタロジックのリセットトリガーを直接確認 |
| HOLDで保護中のポジション（5fe37a4c, e1cc153d）のSL/TP設定が適切か | ポジション詳細・建値・現在価格を照合 |
| シャドウスロット（`shadow=0/2`）が昇格できない原因が

## Alert: 14:21 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=752, daytrade_gbpusd:hedge_block=354, scalp:r2_shadow_demoted_cell=352, daytrade:order_bar_dedup=319, scalp_5m_gbp:r2_shadow_demoted_cell=319

### 診断
## 原因分析

### 【推測】各ブロック異常の原因

**1. `rnb_usdjpy:direction_filter=752`（最多・最重要）**
- 方向フィルターが長時間トレンドと逆方向シグナルを排除し続けている
- USDJPY が強いトレンド相場（一方向継続）に入っており、フィルター閾値がその相場に対して過剰に反応している可能性
- あるいは direction_filter の参照するトレンド判定ロジック自体が固着（フリップしない）している可能性

**2. `scalp:r2_shadow_demoted_cell=352` / `scalp_5m_gbp:r2_shadow_demoted_cell=319`**
- ログより `ema_trend_scalp x EUR_USD` が繰り返しデモート対象になっている
- R2評価が継続的に低スコアとなっており、当該セルが実質的に永続デモート状態に陥っている可能性
- EUR/GBP系でShadow slot bypassも連発しており、EUR系通貨ペア全般で何らかの相場環境不適合が起きている

**3. `daytrade_gbpusd:hedge_block=354`**
- GBPUSDでヘッジポジションが既に張られた状態が長時間続いており、解除条件を満たせていない
- またはヘッジブロック判定ロジックが誤判定で固着している

**4. `daytrade:order_bar_dedup=319`**
- ログより `bar_ts=2026-08-21T14:15:00+00:00` が長時間同一のまま
- **バーの更新が止まっている（データフィード停滞）** か、dedup側のbar_ts参照キーがリセットされていない可能性が高い
- AUD_JPY `wick_imbalance_reversion`、GBP_USD `trendline_sweep` の両方で同一bar_tsが繰り返されている点が根拠

---

### 【確認が必要な事項】

| 確認項目 | 目的 |
|---|---|
| データフィードのbar_tsが現在時刻に追従しているか | order_bar_dedup固着の切り分け |
| direction_filterが参照するトレンド状態変数の現在値と最終更新時刻 | 固着か相場起因かの判別 |
| daytrade_gbpusd のヘッジポジションの実際の保有状態（ブローカー側照合） | ヘッジブロックが正当か誤判定かの確認 |
| R2スコアの時系列推移（ema_trend_scalp / EUR_USD） | 永続デモートか一時的デモートか

## Alert: 16:17 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=938, daytrade_gbpusd:hedge_block=458, scalp:r2_shadow_demoted_cell=434, scalp_5m_gbp:r2_shadow_demoted_cell=343, daytrade:order_bar_dedup=332

### 診断
## 原因分析

### 【推測】各ブロック要因の根本原因

**① `rnb_usdjpy:direction_filter=938`（最多・最重要）**
- RNBストラテジーのdirection_filterが938回発火 → USDJPYで方向フィルターが長期間・連続的にシグナルを棄却し続けている
- 推測：上位足（D1/H4）トレンドとRNBシグナル方向が恒常的に逆行している。または`direction_filter`の閾値設定が現在の相場ボラティリティ・レンジに対して過度に厳格化している

**② `scalp/scalp_5m:r2_shadow_demoted_cell=434/343`**
- shadow tracking cellが大量にデモート → R2シャドウ追跡セルが昇格条件を満たせずに滞留・蓄積している
- ログで`fib_reversal x EUR_USD`と`xs_momentum x USD_JPY`が繰り返し棄却されており、特定セル種別が固着していることが確認される
- 推測：シャドウ昇格条件（スコア・方向・時間）が現在の相場環境（EUR/USDのトレンド反転、USDJPYのレンジ）に対して機能不全を起こしている

**③ `daytrade_gbpusd:hedge_block=458`**
- GBPUSDのヘッジブロックが突出して多い（同系統の他通貨の2〜2.5倍）
- 推測：GBPUSDで既存ポジションの含み益保護（HOLDログで確認済み）またはヘッジ判定閾値との競合が常態化している

**④ `daytrade:order_bar_dedup=332`**
- 同一バー内での重複注文ブロック → シグナル生成ループが1バー内で複数回実行されている可能性
- ログで`bar_ts=2026-08-21T16:00:00+00:00`が複数回棄却されているのと一致
- 推測：interval=30秒のポーリング周期に対してバー確定粒度（15m足）が合わず、同一バーで繰り返しシグナルが生成されている

---

### 【確認事項】（推測と区別すべき事実確認）

- `rnb_usdjpy:direction_filter`の直近履歴：フィルター反転（緩和）がいつ最後に発生したか、またカウントが累積リセットされているか
- R2シャドウセルの現在の滞留数・昇格待ちキューの内容（何件が何時間停滞しているか）
- GBPUSDの現在オープ
