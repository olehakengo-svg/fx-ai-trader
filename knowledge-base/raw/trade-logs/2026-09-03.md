# 2026-09-03 Daily Trade Log (wiki-daily-update)

**Pull times**: demo/stats・learning・risk・audit = 2026-09-03 **11:47 UTC** / status・block-counts・equity・oanda-status = **11:51:46 UTC**
**Clock check**: ✅ local UTC 11:51:46 vs server `row_freshness_now` 11:51:46.884 — **skew 0s** (**5th consecutive non-reproduction** of the 08-26 ~5h40m skew).
**Audit window**: `limit=800` = 2026-08-19T17:20:43Z → **2026-09-03T11:31:05Z** (14.76 days, `total` 16,344; `execution_audit_count` **16,344** — agrees exactly).
**Orphan check**: `pgrep -f app.py` → **nothing running**; every figure below is Render API (`_db_path: /var/data/demo_trades.db`), no local-DB phantom. Per `feedback_check_orphan_local_app`.
**Audit limit**: pulled at **800**, not the task's default 30 — per `feedback_audit_limit30_hides_live_fills`. Today that mattered: the single live fill sits 35 rows deep and a 30-row slice would have caught it, but the **45-row `agg_kelly` history that overturns section 4 below is only visible across the full window**.

---

## 0. 🟢 HEADLINE — the fill drought broke, and it broke the explanation with it

Two independent things happened, and they must not be conflated:

1. **A live fill executed** at **2026-09-03T05:58:04Z** — `price_shock_rev_aud_jpy_h1_long` / `daytrade_1h_audjpy`, **AUD_JPY BUY 1,000u**, OANDA trade **#709570**. First fill since #709537 (08-26 14:59:26Z) = **7.63-day gap**. The position is **still open**; nothing has closed.
2. **The reason this log gave for the drought was wrong.** Sections 08-31 / 09-01 / 09-02 attributed the zero-fill run to the agg-Kelly gate. Measured against the full audit window, **the gate never touched the strategies that fill** (section 4). The drought was signal scarcity, not Kelly suppression.

⇒ Realized book is **still bit-flat** (section 1). One open live position ≠ a closed trade ≠ edge recovery.

---

## 1. Realized book — bit-flat for the **6th** consecutive snapshot

| Field | Value | vs 09-02 |
|---|---:|---|
| N (live_count / total) | **579** | flat |
| W / L / BE | 252 / 292 / 35 | flat |
| WR | **43.5%** | flat |
| decided WR | **46.3%** | flat |
| EV | **−1.06** | flat |
| total_pnl | **−611.1 pip** | flat |
| Wilson lower | 42.2 | flat |
| Wilson BF lower | 39.4 | flat |
| avg R | 0.12 | flat |

Every realized field is bit-for-bit identical to 09-02, 09-01, 08-31 and both 08-28 runs.
**Zero closed fills across ~6 days** (08-28 11:52Z → 09-03 11:52Z). Today's fill is **open**, so it
cannot yet move any of these.

⚪ **Engine provably alive, and now demonstrably able to reach the broker:**

- `shadow_count` 12,843 → **12,877 (+34)**
- `oanda_audit.total` 16,303 → **16,344 (+41)**; `execution_audit_count` **16,344** (exact agreement this pull)
- `engine_tick_age_sec` **0.6s**, `engine_tick_status` **ok**, stalest mode 57.5s (`daytrade_1h_usdchf`)
- **24** running modes, `main_loop_restarts` **1** (flat since 08-28), `watchdog_alive` true, `emergency_killed` false
- OANDA heartbeat **ok @87.5ms**; `last_candidate_row_age_sec` **8.9s** (fresh)
- `last_live_fill_row_age_sec` **21,223.9s = 5.90h**, status **ok** — first time in six runs this field points at *today*

---

## 2. Risk dashboard — the frozen positive window survives a **third** roll

**7th consecutive window-arithmetic reading. Still not edge recovery.**

`effective_date_from` rolled 08-03T19:42:06 → **08-04T11:50:14 (+16.1h)**. Across **three** rolls now
totalling **+70.2h** of window movement, **no trade has entered and none has aged out**:

| Field | 08-31 | 09-01 | 09-02 | **09-03** |
|---|---:|---:|---:|---:|
| n_trades | 13 | 13 | 13 | **13** |
| Kelly edge | +0.6709 | +0.6709 | +0.6709 | **+0.6709** |
| recommended | half_kelly 0.128 | 0.128 | 0.128 | **0.128** |
| Sharpe | +0.3067 | +0.3067 | +0.3067 | **+0.3067** |
| DSR (haircut) | 0.5624 (15.1%) | same | same | **0.5624 (15.1%)** |
| `is_significant` | false | false | false | **false** (z=0.157) |
| 30d net | +94.3 | +94.3 | +94.3 | **+94.3** |

**The same 13 trades re-reported four days running.** Per-strategy Kelly **3/3 `insufficient`**
(n=9/3/1); `small_sample_lowconf` true; ruin probability 0.0%; `worst_case_dd_99` 38.66;
`median_max_dd` 19.34.

⚠️ **`attribution` still returns `gross_pnl = net_pnl = alpha = 94.3` with `friction = 42.5`
unsubtracted** (`avg_friction_per_trade` 3.27, n=13). Correct 30d net is **+51.8**.
Open since 08-31, **not diagnosed this run**.

### DD / defensive posture — unchanged

`dd_pct` **0.097**, `dd_pips` **1198.8** (119.9% on the legacy $1000 pip basis), `eq_current` **−1181.9**
vs `eq_peak` **+16.9** (peak flat), `defensive_mode` **true**, `lot_multiplier` **0.2** — flat for the
6th consecutive snapshot.

---

## 3. Live path — the fill, in full

| Field | Value |
|---|---|
| Audit rows | `price_shock_rev_aud_jpy_h1_long` **sent** + `daytrade_1h_audjpy` **filled** (the `entry_type` twin-meaning, per `reference_oanda_audit_twin_meaning`) |
| Time | **2026-09-03T05:58:04Z** |
| Instrument / side / size | **AUD_JPY BUY 1,000u** |
| OANDA trade | **#709570**, state OPEN, `realizedPL` 0.0 |
| Entry price | **112.776** |
| Take-profit | order **#709571** @ **123.662** = **+1,088.6 pip** (PENDING, GTC) |
| Stop-loss | order **#709572** @ **111.474** = **−130.2 pip** (PENDING, GTC, `triggerMode` TOP_OF_BOOK) |
| Unrealized | **−¥472** (heartbeat −¥468) |
| Margin used | **¥4,492.48** (`initialMarginRequired` ¥4,510.72, `marginRate` 0.04) |

**Broker reconciliation** — `openTradeCount` **1** / `openPositionCount` **1** / `marginUsed` **¥4,492.48**,
matching the one `is_shadow: 0` row in `/api/demo/status`. **First run in this log where a live
DB row and the broker agree on a non-zero position.**

Live rate: new 41-row slice = 39 skipped / **1 sent / 1 filled** / **0 `agg_kelly` blocked** ⇒ live
rate **4.9%**, ending three consecutive 0.0% slices. `/api/oanda/status.audit_summary` concurs:
live 2 / recent_total 50 = **4%**.

### 🔴🆕 The bracket does not match the declared design — needs verification

[[price-shock-rev-aud-jpy-h1-long]] declares exit as **"12 bars 経過後の close で必ず close (horizon
exit) または −2×ATR 近似 SL"** — i.e. a horizon exit and a catastrophic stop, and **no take-profit at
all**. The live trade carries a **GTC TP at +1,088.6 pip**, which is not a parameter of the strategy,
and an SL of **−130.2 pip** (implying ATR ≈ 65 pip on AUD_JPY H1 if it really is −2×ATR).

⚠️ **Stated as measured, not diagnosed**: I have the broker-side bracket but have **not** read
`strategies/hourly/price_shock_rev_aud_jpy_h1_long.py` or the order-construction path this run, so
whether the TP is an inert placeholder, a bridge default, or a genuine contract break is
**unresolved**. This is the same family as the carry-dip finding (declared 150p SL replaced by 18.8p,
`_1H_PRESERVE_SLTP` unregistered) — **a declared SL/TP contract not surviving into the live order**.
Registered as the top open item; a horizon-exit strategy whose live position can be taken out by a
−130.2 pip stop it never asked for is an execution defect, not a strategy result.

### Zero-unit rows — unchanged, still zero exposure

New slice **2/41 (4.9%)**; window-wide **112/800 (14.0%)**; both labelled
`shadow_tracking(shadow_emit_no_lot)` and both `skipped`. Per PR #220 (`c41a4bdc`) `units: 0` on these
rows is a **lot-unassigned tracking marker, never a size** — closed 09-02, no reopening.
⚠️ The audit endpoint still does not expose `dedup_violation`, so PR #220's **write-time dedup flag
remains unverifiable from the API** for a second run.

---

## 4. 🔴🔴 CORRECTION — the agg-Kelly gate was never suppressing the fills

**This overturns a claim carried in three consecutive entries (08-31, 09-01, 09-02) and in
`wiki/index.md` System State.** Those entries read the co-occurrence of `agg_kelly=-0.315<0` blocks
with a zero-fill book as causation. Measured over the whole 800-row window, it is not.

**All 45 `agg_kelly` rows in the window land on just 8 `entry_type`s:**

| entry_type | rows |
|---|---:|
| `xs_momentum_rsi` | 20 |
| `donchian_momentum_breakout` | 6 |
| `kalman_d7_po_dn_flip` | 6 |
| `vsg_jpy_reversal` | 4 |
| `doji_breakout` | 4 |
| `dt_bb_rsi_mr` | 3 |
| `mqe_gbpusd_fix` | 1 |
| `ema200_trend_reversal` | 1 |

**`price_shock_rev_aud_jpy_h1_long` and `usdjpy_carry_dip_accumulator` — the only two strategies that
have actually filled live — appear zero times.** The decisive evidence is the 08-26 timeline, where
a fill is sandwiched between two Kelly blocks carrying the *identical* gate value:

```
2026-08-26T13:16:28  xs_momentum_rsi                  blocked   agg_kelly=-0.336<0
2026-08-26T14:46:21  xs_momentum_rsi                  blocked   agg_kelly=-0.336<0
2026-08-26T14:59:26  price_shock_rev_aud_jpy_h1_long  sent      ←  filled #709537
2026-08-26T17:06:40  xs_momentum_rsi                  blocked   agg_kelly=-0.336<0
```

And today's fill required **no change in the gate**: its last observed value is still
**`-0.315` at 2026-09-02T17:02:00Z**, the same figure reported on 08-31/09-01/09-02. Gate-value
history over the window (12 distinct-value transitions collapsed):
−0.371 (08-19) → −0.372 → −0.374 (08-20) → −0.331 (08-20) → −0.336 (08-21) → **−0.315 (08-27 onward)**.

**Corrected reading**: the agg-Kelly gate is a **per-candidate** filter hitting a specific set of
strategies; it is not a book-wide halt. The 7.63-day drought was **an absence of qualifying signals
in the live-eligible strategies** — consistent with `price_shock_rev` being a 1%-tile rare-event
design that fires on ~0.33% of bars. The two estimators (dashboard n=13 rolling slice vs gate
aggregate) do still disagree, and that remains worth resolving — but **the gate is not what kept
capital out of the market**, and the "two gates already filed for decision" framing from 08-31 is
half wrong.

⚠️ **Caveat, stated honestly**: absence of `agg_kelly` rows in today's slice is consistent with both
"gate now non-negative" and "no gated strategy produced a candidate". The gate's current value is
**not directly observable** unless a row is blocked, so I am not claiming it flipped.

---

## 5. `hedge_block` shadow→live suppression — the tell-tale condition **broke this run**

`open_trades` = **2 rows**, and for the first time they are **not all shadow**:

| entry_type | instrument | dir | `is_shadow` | entry_time | `edge_cell_id` | oanda id |
|---|---|---|---:|---|---|---|
| `session_time_bias` | GBP_USD | SELL | **1** | 2026-09-03T11:31:05Z | `""` | — |
| `price_shock_rev_aud_jpy_h1_long` | AUD_JPY | BUY | **0** | 2026-09-03T05:58:03Z | `""` | **709570** |

Prior runs recorded "all rows `is_shadow: 1` vs broker `open_trade_count: 0`" as the signature of
shadow positions occupying live hedge slots (5 confirmations, 08-28→09-02). **That exact condition is
not reproduced today**: broker `open_trade_count` is **1** and it maps to the one live row.
The mechanism is untouched — one shadow row is still in the book and `hedge_block` is still the
**#2 block family at 21.1%** — but this run supplies **no new confirmation**, and given section 4 the
whole "gates suppress fills" thesis needs re-derivation from counters rather than co-occurrence.
Still **a decision (rule:R2/H2) awaiting user adjudication — no code touched.**
Both rows carry `edge_cell_id: ""` (unchanged from 09-02; still unrelated to the closed-row
`sr_anti_hunt_bounce` metadata corruption).

### Block family shares — `/api/demo/block-counts`, full basis

**3,618 blocks over 15,025 ticks, 71 keys** (⚠️ counter window rolled again: 28,778 → 15,025 ticks
with `main_loop_restarts` flat at **1**, and key count 103 → 71 ⇒ **single-pull shares only, no
cross-pull deltas are valid**):

| family | blocks | share |
|---|---:|---:|
| `r2_shadow_demoted_cell` | 1,390 | **38.4%** |
| `hedge_block` | 762 | **21.1%** |
| `direction_filter` | 646 | **17.9%** |
| `order_bar_dedup` | 567 | **15.7%** |
| `score_gate` | 121 | 3.3% |
| `conf<30` | 56 | 1.5% |
| 65 further families | ≤16 each | ≤0.4% each |

---

## 6. 🔴 `rnb_usdjpy:direction_filter` — **7th consecutive confirmation**, again at exactly 100%

| counter | value |
|---|---:|
| `rnb_usdjpy` ticks | **664** |
| `rnb_usdjpy:direction_filter` | **646** (ratio 0.9729) |
| `rnb_usdjpy:conf<30` | **18** |
| **blocked total** | **664 / 664 = 100.0%** |

The residual 18 ticks are the `conf<30` family, so **every one of the mode's 664 ticks was blocked
and not a single candidate survived** — the same closed form as 09-02 (1,258/1,258). Seven
independently-windowed counters (08-26 1.0008 / 08-28 ×2 / 08-31 0.9788 / 09-01 1.0000 / 09-02 1.0000
/ 09-03 1.0000) cannot all do this by selection. Consistent with the `compute_rnb_signal` WAIT-path
`entry: 0` bug.
**Code read still not done — 7 runs as an open follow-up. Section 4 is the argument for finally
doing it: co-occurrence has now demonstrably misled this log once.**

---

## 7. 🔴 `/api/oanda/equity` — **24.2 days stale**, and a **second** round-number NAV step

Last curve point still **2026-08-10T07:20:34Z**, `count` **928**, `total_pl_jpy` **−80,686**,
`total_pl_pips` −942.2. Progression: **15d → 18d → 21d → 22d → 23.5d → 24.2d**.

### The NAV step repeated — this is now the diagnosable pattern 09-02 asked for

| basis | 09-02 | **09-03** | Δ |
|---|---:|---:|---:|
| broker balance (realized) | ¥278,105.48 | **¥277,875.48** | **−¥230.00** |
| broker NAV (mark-to-market) | ¥278,105.48 | **¥277,403.48** | −¥702.00 |
| ledger `eq_current_jpy` | ¥324,450.50 | **¥324,450.50** | flat |
| drift vs balance | ¥46,345.02 | **¥46,575.02** | **+¥230.00** |
| drift vs NAV | ¥46,345.02 | ¥47,047.02 | +¥702.02 |

The NAV column is confounded by today's open position (unrealized −¥472), so **the realized figure is
the one to read: balance fell exactly −¥230.00 with `realizedPL: 0.0` on the only open trade and
zero closed fills in the interval.** That follows 09-02's **−¥240.00**.

🔴 **Two consecutive exact-round-number realized steps with no closed trade is no longer plausibly a
one-off** — 09-02 pre-registered that a second instance warrants diagnosis, and this is it. Account
`financing` reads **¥17.8160** (cumulative, *positive*), so financing on the account as a whole does
not by itself explain −¥470 across two days; per-position `financing` shows non-zero legacy entries
(EUR_USD −¥14.08, EUR_JPY +¥11.19). Cumulative account `pl` **−¥81,251.34** vs the stale endpoint's
−¥80,686 = **−¥565.34 unaccounted for by the curve**.
**`/api/oanda/transactions` was not pulled this run** — that is the next concrete step, and it is now
the highest-value *diagnosable* item after the SL/TP contract question.

---

## 8. Learning API — frozen for a **9th** snapshot

Latest adjustment **id=93** (`2026-08-19 14:25:37`, scalp, `entry_type_blacklist_add`,
`sr_channel_reversal: WR 25.0%, EV −0.98 → 除外`, sample 190) — **14.89 days stale**.
`entry_type_blacklist` is still **`[]`** ⇒ the blacklist remains a **no-op, unresolved since 06-30**;
ids 90–93 are byte-identical re-affirmations of the same exclusion.

`current_params`: `confidence_threshold` 30 / `max_open_trades` 8 / `max_consecutive_losses` 3 /
`learn_every_n` 10 / `daily_loss_limit_pips` −99999 / `max_drawdown_pips` −99999 (both limits
effectively disabled).

| mode | n | WR | overall EV | note |
|---|---:|---:|---:|---|
| daytrade | 94 | 41.5% | **−2.60** | high −1.12 / mid −4.07 / low −2.18 — **all conf bands negative** |
| scalp | 388 | 40.7% | **−0.15** | only non-negative band is **low +0.48**; high −0.71 / mid −0.39 |
| swing | 0 | — | — | `ready: false` (min_required 10) |

**Bit-identical to 09-02, 09-01, 08-31 and 08-28.**

---

## 9. Strategy pages / tier — **one page updated, no tier change**

**No tier change**: zero closed fills ⇒ no cell gained or lost a trade ⇒ no promote/demote threshold
was touched. Today's fill is open and contributes nothing to any gate until it closes.

Headline cells (`by_type`, live-only, since 2026-04-08) — unchanged from 08-28:

| strategy | N | WR | PnL | Wilson BF lo |
|---|---:|---:|---:|---:|
| `usdjpy_carry_dip_accumulator` | 9 | 55.6% | **+84.0** | 15.7 |
| `price_shock_rev_aud_jpy_h1_long` | 3 | 66.7% | **−102.5** | 9.9 |
| `price_shock_rev_eur_gbp_h1_long` | 3 | 0.0% | −9.8 | 0.0 |

🔴 `usdjpy_carry_dip_accumulator` remains **the book's only materially positive cell**, and the
declared-150p-vs-actual-18.8p SL substitution is still the highest-value open item on it.

✅ **Page updated this run**: [[price-shock-rev-aud-jpy-h1-long]] carried a 現況 table stating
**N=2 / WR 50.0% / EV −61.3 / PnL −122.6** as of 08-20. The API has since recorded a third closed
trade: **N=3 / WR 66.7% / EV −34.2 / PnL −102.5**. That stale table was also the source of a
cross-page numeric inconsistency (section 10). Page now reflects N=3 plus the open 4th trade and the
bracket-mismatch flag from section 3.

---

## 10. Lint

| Check | Result |
|---|---|
| **WR/PnL consistency** | 🔴 **1 inconsistency found and fixed** — [[price-shock-rev-aud-jpy-h1-long]] stated N=2 / −122.6 pip against the API's N=3 / −102.5 pip (page frozen at its 08-20 snapshot). All other strategy-page figures spot-checked against `by_type` agree. `wiki/index.md` System State was one day stale (09-02) — updated this run. |
| **Broken `[[internal links]]`** | ⚠️ **118 refs across 76 targets**. Of these, **57 refs / 31 targets are `feedback_*` / `project_*` MEMORY slugs** — a deliberate cross-namespace convention, not KB breakage. **Genuine KB-internal breakage ≈ 61 refs / 45 targets**, worst offenders `feedback_tv_edge_discovery_loop` ×7, `lesson-bt-live-divergence` ×6, `feedback_ma_filter_breaks_mr` ×4, `feedback_size_lever_beats_skip_filter` ×4, `../strategies/{vsg_jpy_reversal,rsk_gbpjpy_reversion,mqe_gbpusd_fix}` ×2 each (underscore-vs-hyphen filename mismatch). Chronic, unchanged in magnitude, **not fixed this run** (bulk rename touches 100+ pages and belongs in its own task). |
| **Stale data (>3 days)** | 🔴 `/api/oanda/equity` **24.2 days** (section 7) · learning adjustments **14.9 days** (section 8) · `wiki/tier-master.json` `generated_at` **2026-08-03T06:17:40Z = 31.2 days** while `wiki/index.md` portfolio is stamped "auto-synced 2026-09-03" — **the two disagree about their own freshness**; `wiki/tier-master.md` and `wiki/edge-pipeline.md` likewise last touched 08-03. Realized book itself is 6 days without a closed fill but the API is fresh. |
| **False positive cleared** | `2026-09-30` references in `changelog.md` and 4 decision pages are **legitimate future backstop dates**, not typos or future-stamped data. |

---

## 11. Process notes

- **Concurrent-repo hazard**: HEAD at write time **`de21f8ca`** (09-02 recorded `874fb3c8`, so 
  other jobs have pulled since). Working tree already carried unrelated modifications from other
  automated jobs (`data/cache/massive/EUR_GBP_15m.parquet`, `data/sentiment/oanda_labs_h4_history.parquet`,
  `raw/hunt_events/2026-09-03.jsonl`, `wiki/sessions/2026-09-03-session.md`) and 5 stash entries are
  outstanding. Per `feedback_concurrent_agent_repo_hazard` every write below was **verified on disk
  after writing**. **No commit/push** — `project_fxai_stranded_staged_work_2026_09_01` still blocks
  that pending manual reconcile.
- **Endpoint scope**: the task lists 4 endpoints; 4 more (`demo/status`, `demo/block-counts`,
  `oanda/equity`, `oanda/status`) were pulled to keep continuity on the items this log tracks. All 8
  returned HTTP 200.
- **Method note**: section 4 exists because this run cross-checked a *carried narrative* against the
  primary data instead of re-asserting it. Per `feedback_label_empirical_audit` — the claim
  "the gate suppressed the fills" was deduced from co-occurrence and never measured; measuring it
  refuted it in one query.

---

## 12. Open items

| # | Item | State | Age |
|---|---|---|---|
| 1 | 🔴 **`price_shock_rev` live bracket vs declared design** — GTC TP +1,088.6p on a strategy that declares no TP; SL −130.2p vs "−2×ATR". Read the strategy + order-construction path. Same family as the carry-dip SL substitution | **NEW** | 0d |
| 2 | 🔴 **carry-dip declared 150p SL → actual 18.8p** (`_1H_PRESERVE_SLTP` unregistered) — book's only positive cell | open, R3 decision | — |
| 3 | 🔴 **−¥230.00 / −¥240.00 realized NAV steps with no closed trade** — pull `/api/oanda/transactions` | escalated (2nd instance) | 2d |
| 4 | 🔴 **`/api/oanda/equity` 24.2 days stale** | open | 24d |
| 5 | 🔴 **`rnb_usdjpy` 100% tick block** — read `compute_rnb_signal` WAIT path | open, 7 confirmations | 8d |
| 6 | 🔴 **`hedge_block` counting shadow positions** — rule:R2/H2 user adjudication; **re-derive the fill-suppression claim from counters** after section 4 | open, thesis weakened | 6d |
| 7 | ⚠️ **`attribution` friction not subtracted** (gross=net=94.3, friction=42.5, true net +51.8) | open, undiagnosed | 3d |
| 8 | ⚠️ **`entry_type_blacklist` no-op** (`[]` since 06-30) + learning frozen 14.9d | open | 65d |
| 9 | ⚠️ **PR #220 write-time dedup flag unverifiable** — `dedup_violation` not exposed by the audit API | open | 1d |
| 10 | ⚠️ **`tier-master.json` 31.2 days stale** vs index stamped 09-03 | open | 31d |
| 11 | ⚠️ **~61 genuine broken KB wikilinks** (underscore/hyphen mismatch) — own task | chronic | — |

