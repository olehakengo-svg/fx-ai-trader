# Trade Monitor Alerts: 2026-07-02

## Alert: 01:59 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=269

### 診断
## 原因分析

### 【推測】`rnb_usdjpy:direction_filter=269` の異常値について

**推測される原因**

- `rnb_usdjpy` モードのシグナルが、**方向フィルター（direction_filter）に大量に弾かれ続けている**
  - 現在のUSD/JPY相場のトレンド方向と、`rnb`（レンジブレイク系と推定）のシグナル方向が**恒常的に不一致**になっている可能性が高い
  - 直近の相場が一方向に強くバイアスされており、逆張りシグナルが連続生成されて全てフィルタされている状態と推測
- カウンターが**リセットされずに累積し続けている**設計上の問題の可能性あり
  - 他のblock_countが最大178程度に留まる中、269は突出しており、セッションをまたいでリセットが機能していない疑いがある
- `rnb_usdjpy` のシグナル生成ループが**高頻度で空振りし続けている**（インターバル設定が短すぎる等）

---

### 【推測】その他の状況から読み取れる副次的懸念

- `R2_SHADOW_DEMOTE` が数秒おきに連発 → USD/JPY・USD/CHF の特定セルが**継続的にシャドウ降格状態**に陥っており、実取引の機会を逃し続けている
- `squeeze_release_momentum` が `same_price_0pip` で繰り返しブロック → **同一価格帯での短時間連続シグナル**が発生しており、フィルターが正常に機能している一方、シグナル生成ロジック側に過剰生成の問題がある可能性
- `main_loop_restarts=1` → 直近で一度再起動が発生しており、**累積カウンタがリセットされなかった**可能性を補強する

---

## 確認事項

### 優先度：高
- [ ] `direction_filter` カウンタの**リセットタイミングの実装**を確認（日次・セッション単位でリセットされているか）
- [ ] `rnb_usdjpy` のシグナル生成間隔（interval）を確認し、**他モードと比較して異常に短くないか**を確認
- [ ] `direction_filter` の判定ロジックを確認し、**現在のUSD/JPYの相場方向（強い上昇or下落トレンド）と`rnb`の想定方向が根本的に乖離していないか**を確認

### 優先度：中
- [ ] `main_loop_restarts=1` の発生時刻とblock_count累積

## Alert: 05:57 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=264

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=264`の異常値**は、RNB（レンジブレイク系）戦略のUSDJPYモジュールで、方向性フィルターが264回連続または短期間に大量発火していることを示す
- 発火の背景として、**USDJPY（162円台）が明確なトレンド方向を持てずにいる**ため、シグナルが生成されるたびに方向性フィルターで全量棄却されている可能性が高い
- `daytrade`の直近シグナルが`WAIT`かつ`entry_type=unknown`、かつ同時刻帯にSL_HITが発生しており、**相場が方向感のないレンジ～ノイズ相場**に入っていると推定される
- `R2_SHADOW_DEMOTE`がUSDJPY（`sr_channel_reversal`）とEURUSD（`ema_trend_scalp`）で秒単位で連続発生しており、**価格がS/Rレベル周辺で細かく往来**している状態と推測される

### 推測（中確度）

- direction_filterのカウンターが**セッションリセットされずに累積**しているロジックバグの可能性（カウンター上限チェックの欠如）
- 162円台という**過去の強いレジスタンス帯**でrnbセルが繰り返しブレイクシグナルを出し、都度フィルターに弾かれているサイクルに陥っている可能性

---

## 確認事項

- [ ] `rnb_usdjpy`のdirection_filterカウンターが**セッション起動からの累積値か、時間窓内の件数か**を仕様で確認する
- [ ] カウンターのリセットタイミング（日次・セッション・手動）が**正常に機能しているか**をログで確認する
- [ ] 264という数値が**アラート閾値を設定した意図的な上限**に対してどの程度超過しているかを確認する
- [ ] 直近の上位足（1H/4H）でUSDJPYの方向性バイアスが明確に定義されているかを確認する
- [ ] `daytrade`の`entry_type=unknown`が正常なWAIT時の出力仕様か、**判定ロジックの抜け（バグ）**かを確認する

---

## 対処方針

- **即時対応**
  - rnb_usdjpyモジュールのdirection_filterカウンターを手動リセットし、累積起因かどうかを切り分ける
  - カウンターがリセット後すぐに再増加するよう

## Alert: 08:45 UTC

### 検出された問題
- 💰 **含み益+27.2pip** (3ポジション, LIVE=0 Shadow=3) | Top: dt_sr_channel_reversal×GBP_JPY +16.0pip | trendline_sweep×EUR_GBP +13.1pip | london_breakout×USD_CHF -1.9pip
- ⚠️ **Shadow含み益+29.1pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=291, daytrade_eurgbp:hedge_block=260

### 診断
## 原因分析と対処方針

---

### 【推測】現在起きていること

**① Shadow含み益+29.1pipがOANDAに未送信**
- `london_breakout×USD_CHF`、`dt_sr_channel_reversal×GBP_JPY`、`trendline_sweep×EUR_GBP` の3ポジションがいずれもShadow扱い
- Phase0 tier gateログより、これらが `ELITE_LIVE` / `PAIR_PROMOTED` / `PRIME` のいずれにも登録されていないため自動的にshadow降格されている
- `SHADOW_FIX`ログで `is_shadow=True` への上書きが発生しており、open_trade時点でのshadow判定ミスが修正されていることも確認できる

**② `rnb_usdjpy:direction_filter=291` の異常値**
- 他のブロックカウントが10〜100台の中で291は突出している
- direction_filterが長時間一方向を拒否し続けている＝USD/JPYトレンドに対してフィルタ条件が固着している可能性が高い
- ロジック上のhysteresis（転換閾値）が厳しすぎるか、参照するトレンド判定データが更新されていない可能性

**③ `daytrade_eurgbp:hedge_block=260` の異常値**
- EUR_GBPに対してhedge_blockが260回発火しており、ポジション解消されていないまま対当シグナルを出し続けている状態と推測
- 既存ポジション（eaf2871e、SELLのEUR_GBP系か）のHOLD保護と連動してhedge_blockが累積している可能性

**④ `london_breakout×USD_CHF` のMTF逆行**
- `mtf=trend_up_strong`（D1=+2, H4=+1）に対してSELLエントリーしており、上位足と逆張り
- 現在-1.9pipと軽微だが、MTF判定と乖離したまま継続保有はリスク

---

### 【確認事項】（断定前に要確認）

- [ ] `PAIR_PROMOTED` / `ELITE_LIVE` / `PRIME` の現在の登録ペア一覧 → USD_CHF・GBP_JPY・EUR_GBPが本当に含まれていないか
- [ ] `rnb_usdjpy` のdirection_filter参照元データ（トレンド判定ソース）の最終更新時刻 → データ停滞の可能性
- [ ] eaf2871eポジションの通貨ペアとhedge_block対象ペアの一致確認 → eurgbp:hedge_blockとの因果関係
- [ ] `main_loop_restarts

## Alert: 12:00 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=272

### 診断
## 原因分析

### 【推測】原因

- **`rnb_usdjpy:direction_filter=272`** は他のブロックカウント（最大191）と比較して**突出して高い**
- `rnb_usdjpy`（おそらくRange&Breakout系 USDJPYモード）が、**方向フィルターで大量にシグナルを棄却し続けている**
- ログに `SELL b07cce0c` の含み益保護HOLDが高頻度で発生しており、**既存SELLポジションが長時間保持中**
- direction_filterの判定基準（トレンド方向・上位足バイアス等）が現在の相場と**継続的に不一致**になっている可能性が高い
- `main_loop_restarts=1` があり、再起動後から状態が積み上がっている可能性あり

---

### 【確認事項】

1. **rnbモードの方向フィルター条件**
   - 上位足（1H/4H）のトレンド判定ロジックを確認
   - フィルターが「固着」していないか（一度BUY方向に固定された後、相場転換に追従できていないか）

2. **`b07cce0c` ポジションの状態**
   - SELLポジションが何時から保持されているか
   - 含み益が0.00057〜0.00080で推移しており未決済のまま → 手動確認が必要

3. **rnbモードのシグナル生成頻度**
   - 272回ブロックされているということは、そもそもシグナル自体は多数発生している
   - シグナル生成ロジックとフィルター条件の乖離幅を確認

4. **再起動（restarts=1）のタイミングと理由**
   - 再起動前後でフィルター状態がリセットされたか否かを確認

---

## 対処方針

- **即時対応**
  - `b07cce0c` SELLポジションのTP/SL水準と現在価格を確認し、手動決済の要否を判断
  - rnbモードのdirection_filterが参照する上位足バイアス値を直接ダンプして、現在値と乖離がないか確認

- **短期対応**
  - direction_filterの判定ロジックに「フィルター固着検知」（例：N回連続ブロックでリセットまたはアラート）を検討
  - ブロックカウントの異常検知閾値を設け、一定値超過でモード別にアラートを出す仕組みを整備

- **中期対応**
  - rnbモードのフィルター感度（上位足参照TFや

## Alert: 14:04 UTC

### 検出された問題
- 🔴 **含み損-34.3pip** (5ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 推測（ログ・ステータスから）

**① 含み損-34.3pip / 5ポジションの主因**
- 直近のSELLポジション（USD/JPY系）がSL_HITで決済されており、新規エントリー（`8573cc57` xs_momentum SELL @ 160.870）がその後も継続中とみられる
- `c0755fd7` EUR/USD BUY はBE_LOCK済みで保護されているが、MTF_MONITORが `trend_down_strong`（d1=-2, h4=-1）を示しており、上位足と逆張りエントリーの可能性が高い
- GBP/USD・EUR/USD・USD/CHF方面で複数の`hedge_block`が積み上がっており（計15ブロック）、ヘッジ解消機能が連続的に抑制されている

**② hedge_blockの多発**
- `daytrade`系4通貨ペアで合計15件のhedge_blockが蓄積。ヘッジシグナルが出ているにもかかわらず、ブロックが解除されていない状態が継続中と推測
- `same_price_0pip`（GBP/USD）が1件：スプレッド拡大またはティック停滞によりエントリー価格が変化せず弾かれている可能性

**③ `rnb_usdjpy:direction_filter` 3件**
- RNBロジックのUSD/JPYが方向フィルターで3回連続ブロック。相場の方向性が定まらず、シグナルが出ても実エントリーできていない可能性

**④ main_loop_restarts: 1**
- 一度プロセス再起動が発生。その際にポジション状態の再同期が不完全だった場合、`SHADOW_FIX`ログ（is_shadow=False→Trueへ補正）が複数出ているのと合致する可能性

---

### 確認事項（推測ではなく要確認）

- **OANDAの実際のオープンポジション一覧と含み損内訳**（どの通貨ペア・エントリー価格・方向かを特定）
- **`8573cc57` (USDJPY SELL)・`c0755fd7` (EURUSD BUY) の現在損益**（-34.3pipの主体かどうか）
- **hedge_blockのTTL/解除条件**（意図的に長期ブロック継続しているか、バグによる未解除かの切り分け）
- **main_loop_restart時刻とSHADOW_FIX発生時刻の照合**（再起動によるポジション状態不整合かどうか）
- **GBP/USD `
