# Trade Monitor Alerts: 2026-07-31

## Alert: 01:25 UTC

### 検出された問題
- 💰 **含み益+49.0pip** (1ポジション, LIVE=0 Shadow=1) | Top: price_shock_rev_aud_jpy_h1_long×AUD_JPY +49.0pip
- ⚠️ **Shadow含み益+49.0pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=205

### 診断
## 原因分析

### 推測（確認前）

**① Shadow含み益がLIVEに昇格しない問題**
- `price_shock_rev_aud_jpy_h1_long` は Shadow専用セルとして動作しており、AUD_JPYが `PAIR_PROMOTED` リストに含まれていないため、OANDAへの実注文送信が抑止されている
- +49.0pipという大きな含み益が機会損失になっている可能性が高い

**② `rnb_usdjpy:direction_filter=205` 異常**
- 他のblock_countsが一桁〜百前後の中、205は突出して多い
- USDJPYの方向性フィルタが長時間・連続的に拒否し続けている＝ `rnb_usdjpy` モードのシグナルが現在の相場方向と継続的に不一致か、フィルタ条件が過剰に厳格になっている
- main_loop_restarts=1 のタイミング付近でフィルタ状態がリセットされず蓄積されている可能性あり

**③ R2_SHADOW_DEMOTE の多発**
- GBP_USD / USD_JPY / EUR_USD において `ema_trend_scalp`, `bb_rsi_reversion`, `fib_reversal` が短時間に大量demote
- 市場ボラティリティ上昇（月末フロー、7/31）により過去セルのパフォーマンスが急悪化 → r2評価が閾値を下回りdemoteが連鎖している可能性
- `bb_rsi_reversion x USD_JPY` は SENTINEL_BLOCK_DIAG も同時発火しており、監視対象として浮上済み

---

### 確認事項（要調査）

| # | 確認項目 | 目的 |
|---|----------|------|
| 1 | `PAIR_PROMOTED` の現在リストにAUD_JPYが含まれているか | Shadow→LIVE昇格の阻害要因を確定 |
| 2 | `price_shock_rev_aud_jpy_h1_long` セルのr2スコア・昇格条件を確認 | 昇格が意図的に抑止されているのか設定漏れかを判断 |
| 3 | `rnb_usdjpy:direction_filter` の判定ロジックと現在のUSDJPYトレンド方向を照合 | フィルタ誤作動か相場環境の問題かを切り分け |
| 4 | main_loop_restart=1 発生時刻と `rnb_usdjpy` のカウント開始時刻の照合 | 再起動後にフィルタ状態が正常にリセットされたか確認 |
| 5 

## Alert: 05:21 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=287

### 診断
## 原因分析

### 【推測】根本原因

- `rnb_usdjpy:direction_filter` が **287** という突出した値を示しており、`rnb_usdjpy` モードのシグナルがほぼ全件 direction_filter によって遮断され続けている
- `bb_width_pct=0.0` が継続していることから、**USD/JPYのボラティリティが極端に低下（BB収縮＝スクイーズ状態）** に陥っており、複数モードに連鎖的なブロックが発生している
- スクイーズ状態でMR系シグナル（`dt_sr_channel_reversal`）が連続発火 → `regime_squeeze_mr` が正常にブロックしているが、**発火頻度自体が異常に高い**（ループ間隔に対してシグナル生成が過剰）

### 【推測】副次的要因

- `direction_filter=287` は累積カウントであり、セッション開始から現時点まで **方向フィルタが一切解除されていない** 可能性がある（トレンド判定がずっと逆方向 or 中立）
- `r2_shadow_demoted_cell` がscalp系で100件超（scalp: 145、scalp_eur: 92）蓄積しており、**R2評価の低下による広範なデモート状態**が複数通貨ペアに波及している

---

## 確認事項（要ファクト確認）

- [ ] `rnb_usdjpy` の direction_filter ロジックで参照しているトレンド判定指標（EMA方向・ADX等）の現在値
- [ ] カウンターのリセットタイミング（セッション跨ぎでリセットされているか、累積かどうか）
- [ ] `bb_width_pct=0.0` が実際に計算上ゼロなのか、**データ取得失敗によるデフォルト値落ち**なのか
- [ ] `main_loop_restarts: 1` のリスタート発生時刻と、カウント急増のタイミングが一致するか
- [ ] `rnb_usdjpy` モードの last_signal 内容（status出力が途切れており未確認）

---

## 対処方針

### 即時対応

- `rnb_usdjpy:direction_filter` の現在の判定根拠ログを直近50件以上抽出し、**何の条件でフィルタされているか**を特定する
- `bb_width_pct=0.0` が価格データの異常（feed断・固定値貼り付き）に起因しないか、ブローカーAPIのOHLCデータを直接確認する
- 異常が価格データ起因であれば、**当

## Alert: 13:24 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=201

### 診断
## 原因分析

### 推測（高確率）

- **`rnb_usdjpy:direction_filter` カウント201は突出して高い**。他のブロック項目が14〜185程度の中、同一セッション内で断然トップの値。
- RNB（レンジブレイクアウト系と推定）ストラテジーのUSD/JPYが、**方向フィルター（トレンド方向・上位足バイアス等）に継続的に弾かれ続けている**状態。
- 現在のUSD/JPYが**レンジ内でのみ価格推移**しており、ブレイク方向のシグナルが頻発するが上位足フィルターと矛盾するため全拒否されているパターンと推測。
- または**フィルター判定ロジックが硬直化**（キャッシュ・固定値・上位足データ未更新等）しており、本来なら通過すべき方向を恒常的に棄却している可能性あり。

### 推測（中確率）

- `main_loop_restarts: 1` が記録されており、再起動後に**上位足バイアスの状態変数がリセット・初期化不全**のまま固定されている可能性。
- USD/JPYが短期的に方向性の出やすい相場局面（日銀系イベント等）にあり、シグナル生成頻度そのものが高騰している可能性。

---

## 確認事項（要調査）

- **direction_filterの判定根拠**となっている上位足（1H/4H/日足）のトレンド方向・バイアス値の現在値を直接確認する
- フィルター判定に使うデータ（EMA、ADX、バイアスフラグ等）が**最終更新時刻として古くなっていないか**確認する
- `main_loop_restarts: 1` の再起動タイミングと、direction_filterカウント急増のタイミングが**一致しているかログで突き合わせる**
- direction_filterブロック時のログに**具体的な棄却理由（BUY/SELL方向とフィルター判定値）**が出ているか確認する
- 他の通貨ペアのRNBモード（存在する場合）で同様のブロックが出ていないか確認する

---

## 対処方針

- **即時確認**：上位足バイアス変数の現在値と最終更新タイムスタンプをダンプし、値の妥当性を目視検証する
- **再起動起因の疑い**：再起動後に上位足状態が正常に再初期化されているか、初期化処理のログを精査する。問題があれば強制

## Alert: 15:51 UTC

### 検出された問題
- 🔴 **含み損-87.1pip** (17ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 【推測】含み損-87.1pip / 17ポジション蓄積の主因

- **max_open上限(8)を大幅超過した17ポジション**が存在している
  - 複数モードの`max_open`ブロックが多発しているにもかかわらず、既存ポジションが累積している状態
  - `daytrade_eurgbp:hedge_block`が144回という突出した数値 → EURGBP方向でのヘッジ失敗またはトレンド逆行が継続中である可能性
- **`ema_trend_scalp x USD_JPY` のR2_SHADOW_DEMOTEが異常多発（数十秒間隔で連続）**
  - スキャルpモードでUSDJPY方向のシグナルが繰り返し降格させられており、実勢トレンドとモデルが乖離している可能性
  - これによりスキャル系の利確・反転エントリーが機能せず、含み損が放置されている可能性
- **`rnb_usdjpy:direction_filter`が146回**という高頻度ブロック
  - RNBモードがUSDJPYの方向フィルターで全シグナルを止めており、損失方向への修正エントリーが封じられている可能性
- **`HOLD: DT含み益保護`が連続発火**しているポジション(d516101f)は含み益があり正常だが、他の16ポジションの状態が不明

---

### 【確認が必要な事項】

| 優先度 | 確認項目 |
|--------|----------|
| 🔴高 | 17ポジションの内訳（通貨ペア・方向・個別損益）を取得し、どのモード・通貨が含み損の主体か特定する |
| 🔴高 | EURGBPの現在レートとポジション方向の乖離幅を確認（hedge_block 144回の原因）|
| 🔴高 | `main_loop_restarts: 1` の再起動タイミングとその前後のポジション状態を確認 |
| 🟡中 | R2_SHADOW_DEMOTEが連続する`ema_trend_scalp x USD_JPY`のセルが恒久的に降格状態になっていないか確認 |
| 🟡中 | `scalp:r2_shadow_demoted_cell: 98` および `scalp_eur:r2_shadow_demoted_cell: 38` の累積値が異常水準でないか閾値と照合 |
| 🟢低 | `daytrade_1h_usdchf:r2_shadow_demoted_cell: 15` がUSDCHF方向モデ

## Alert: 17:15 UTC

### 検出された問題
- 🔴 **含み損-130.3pip** (14ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 🔍 推測（ログ・ステータスから読み取れること）

**1. 含み損-130.3pip / 14ポジションの主因**
- `scalp:max_open=174`、`scalp_eur:max_open=178`、`daytrade_eur:hedge_block=122`、`daytrade_gbpusd:hedge_block=114` など、**ブロックカウントが異常に高い**ストラテジーが複数存在
- これらは「新規エントリーを止めながらも既存ポジションが逆行し続けている」状態を示唆
- `rnb_usdjpy:direction_filter=129` が極めて高く、**USDJPY方向フィルターが長時間一方向を弾き続けている** → トレンドに逆らったポジションが放置されている可能性

**2. SELLシグナルが連続ブロックされている**
- `ORDER_BAR_DEDUP` が同一バー（`17:00:00`）に対して繰り返し発火 → **streak_reversal SELLが同一足内で何度も生成されているが全てDEDUP排除**されている
- `HOLD / DT含み益保護` で BUYポジション3本（5b1a9106, d916a3c2, a5979e81）の**リバース信号が無効化**され続けている
- `R2_SHADOW_DEMOTE` が `fib_reversal`・`bb_rsi_reversion` を大量ブロック → **シャドウセルが降格状態のまま復帰できていない**

**3. main_loop_restarts=1**
- 直近で1回メインループが再起動している → **一時的な異常状態またはハング**があった可能性

---

### ✅ 確認事項（実機で要確認）

- [ ] 14ポジションの内訳（通貨ペア・方向・建値・ロット）をブローカー画面で直接確認
- [ ] `scalp` / `scalp_eur` の max_open が異常高値である理由（上限設定値との比較）
- [ ] `daytrade_eur:hedge_block=122` / `daytrade_gbpusd:hedge_block=114` → ヘッジブロックが何をトリガーしているか
- [ ] BUYポジション3本（5b1a9106等）が現在も含み益か、転落して含み損に寄与していないか
- [ ] `main_loop_restarts=1` の再起動タイミングと原因（ログの前半を遡る）
- [ ] `r2_shadow_demoted_cell` が解除されない理由（降格条件のリセットが機能しているか）

---

## 対処方針

**即

## Alert: 20:49 UTC

### 検出された問題
- 🔴 **含み損-190.4pip** (2ポジション) → ポジション確認推奨

### 診断
# 原因分析と対処方針

## 🔴 含み損 -190.4pip（2ポジション）の原因推測

### 推測（ログ・ステータスから）

- **USD/JPY系ポジションの逆行が主因の可能性が高い**
  - `daytrade_1h` がSELLシグナル（confidence=77）を出しているが、`mtf_reversal_confluence` がBUYを示しつつ `mtf=uncertain, d1=1, h4=-1` と上位足が矛盾→トレンド判定の齟齬の中でエントリーした可能性
  - `macdh_reversal×USD_JPY` がSHADOW状態で含み益+10.4pipという記録があり、**ライブポジションとSHADOWポジションの混在**が生じている可能性

- **AUD/JPY または NZD/JPY の逆行も関与している可能性**
  - `price_shock_rev_aud_jpy_h1_long`（BUY）が繰り返しブロックされているが、既存のライブポジション(live=1/1)が存在し、それが円高方向に逆行中の可能性
  - `daytrade_1h_audjpy:hedge_block` カウント45は異常に高く、**ヘッジが長時間発動し続けている**＝既存ポジションが長期逆行中を示唆

- **`main_loop_restarts: 1`**
  - ループ再起動により、一部のポジション状態管理が一瞬欠落し、想定外の方向でポジションが残留した可能性

---

## 確認事項（推測ではなく要確認）

| 優先度 | 確認項目 | 理由 |
|--------|----------|------|
| 🔴最高 | 2ポジションの通貨ペア・方向・エントリー価格・現在価格 | 190pip超の損失の実態把握 |
| 🔴最高 | SLが設定されているか・SL水準 | ロスカットラインの確認 |
| 🟠高 | SHADOW判定されたポジションがライブとして残っていないか | SHADOW/ライブ混在バグの確認 |
| 🟠高 | `main_loop_restarts`発生時刻とエントリー時刻の照合 | 再起動起因のポジション残留確認 |
| 🟡中 | `rnb_usdjpy:direction_filter` ブロック数138の状態 | USD/JPY方向フィルタが長時間反転している＝トレンド逆行の傍証 |
| 🟡中 | `hedge_block`（audjpy:45
