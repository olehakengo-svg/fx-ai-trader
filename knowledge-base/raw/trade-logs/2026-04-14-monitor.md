# Trade Monitor Alerts: 2026-04-14

## Alert: 11:41 UTC

### 検出された問題
- 💰 **含み益+55.5pip** (9ポジション, LIVE=1 Shadow=8) | Top: vix_carry_unwind×USD_JPY +37.0pip | post_news_vol×USD_JPY +23.8pip | ema_trend_scalp×EUR_USD +4.0pip
- ⚠️ **Shadow含み益+65.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】

**1. Shadow含み益がOANDAに未送信の主因：exposure上限ブロック**
- `USD net exposure 30,000u > 20,000u limit` が複数モードで大量発生（daytrade:18件、scalp:7件、scalp_5m:5件等）
- 現在LIVE=1/Shadow=8の非対称状態は、exposureブロックによりShadowシグナルがLIVEに昇格できていないことが主因と推測
- Shadow側は65.6pip含み益を積んでいるが、exposure上限がボトルネックになってPAIR_PROMOTEDに至っていない

**2. `daytrade_1h_eur/EUR_USD` が永続的にShadow待機**
- `Slot bypass: wait daytrade_1h_eur/EUR_USD (1/1 → shadow)` が約1分間に6回以上繰り返し発生
- Slot上限=1が埋まった状態でシグナルが連続発火しており、cascade_cdブロックと合わせてデッドロック的な待機ループになっている可能性あり

**3. `rnb_usdjpy:direction_filter` の異常多発（97件）**
- 他ブロックと比較して突出した件数。フィルタ条件が現在の相場レジームと恒常的に不一致になっている可能性

**4. DT含み益保護によるSIGNAL_REVERSE無効化の連鎖**
- 12925ac0・a103ba78が高頻度で同一ログを繰り返しており、反転シグナル抑制が長時間継続中

---

### 【確認事項】

| 確認項目 | 理由 |
|---|---|
| 現在のUSD net exposureの実値 | 20,000u上限に対して何u積まれているか。LIVEポジション精査 |
| Shadow→LIVE昇格ロジックの判定条件 | exposure上限がある限りPAIR_PROMOTEDが発火しない設計になっているか |
| `daytrade_1h_eur` のSlot=1上限は意図的か | シグナル連続発火に対してSlotが解放されるトリガー条件の確認 |
| `rnb_usdjpy:direction_filter` の97件の発火期間 | 今セッション内の累積か、特定時間帯に集中しているか |
| main_loop_restarts=1 の発生時刻とエラー内容 | 再起動前後でShadow/LIVE状態の継続性が担保されているか |
| DT含み益保護の解除条件 | 12925ac0・a103ba78のTP/SL水準と現在価

## Alert: 15:57 UTC

### 検出された問題
- 🔴 **含み損-30.5pip** (5ポジション) → ポジション確認推奨

### 診断
## 原因分析

### 【推測】含み損 -30.5pip の原因

- **GBP系通貨の逆行が主因の可能性が高い**
  - `daytrade_eurgbp:exposure:GBP net exposure` のブロック数が **110** と全戦略中最大
  - `daytrade_gbpjpy`, `daytrade_gbpusd` のGBPエクスポージャーブロックも多数（48, 40）
  - GBP絡みのポジションが上限超過状態で積み上がったまま、相場が逆行した可能性

- **エクスポージャー上限超過によるヘッジ・新規制御が機能しきれていない**
  - USD net / GBP net ともに 30,000u > 20,000u 超過ブロックが複数戦略で多発
  - 制御はされているが、**既存ポジションの損失は止まらない**状態

- **`daytrade_eurjpy` のシグナル状態が `unknown` で長時間継続**
  - ログ全体を通じて `unknown` 状態のSlot/Hedge bypassが大量発生
  - シグナル評価が正常に完了していない可能性（データ取得障害 or ロジックエラー）

- **`rnb_usdjpy:direction_filter` のブロックが 163 と突出**
  - USDJPYで方向フィルターに引っかかり続けており、相場のトレンド方向と戦略の方向が長期間不一致の疑い

---

### 【確認事項】（実施前に必ず確認すべき事項）

1. **5ポジションの内訳確認**
   - どの通貨ペア・戦略のポジションか
   - ロング/ショートの方向と現在レートとの乖離を確認
   - 損失が特定通貨（GBP/USD/JPY）に集中していないか

2. **`daytrade_eurjpy` の `unknown` 原因の特定**
   - シグナル生成部のデータソース（価格フィード・指標計算）が正常か
   - `unknown` が返る条件（例外ハンドリングで潰されていないか）をログで追跡

3. **`main_loop_restarts: 1` の再起動タイミング**
   - 再起動前後でポジション状態の整合性が取れているか
   - 再起動時にポジションの再認識が正しく行われたか

4. **ブロックカウントが異常に多い戦略の発火頻度**
   - `daytrade_eurgbp`（110）は正常な頻度でブロックされているのか、ループ異常で連打

## Alert: 19:55 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=427, scalp:exposure:USD net exposure 30,000u > 20,000u limit=246, daytrade_eur:exposure:USD net exposure 30,000u > 20,000u limit=233

### 診断
## 原因分析

### 【推測】

**1. rnb_usdjpy:direction_filter=427（最大の異常値）**
- USDJPYが強いトレンド相場（円安/円高の一方向）に入っており、rnb戦略のdirection_filterが長時間・高頻度でブロックし続けている
- フィルター閾値またはフィルター判定ロジックが現在の相場環境に対してチューニング不足の可能性
- rnb戦略固有のバグ（フィルター条件が永続的にTrueになるループ状態）の可能性

**2. scalp/daytrade_eur:exposure:USD net exposure 30,000u > 20,000u limit（246/233）**
- USD建てポジションが複数戦略にまたがって積み上がり、20,000uリミットを継続的に超過している
- EURUSDポジション（daytrade_eur）とUSDJPY系ポジション（scalp）が同時に同方向に偏っている可能性
- ポジション解消が遅い（損益的に含み損を抱えてホールドしている）か、エントリー頻度に対してexitが追いついていない

**3. ログのSHADOW集中**
- daytrade_1h_eurがSlot 1/1を占有し続けており、後続シグナルが全てshadow送りになっている
- scalp_eurはセッション時間外（outside active_hours）で正常にshadow化されているため、こちらは正常動作の範囲内

---

### 【確認事項】

| # | 確認内容 | 確認先 |
|---|----------|--------|
| 1 | 現在のUSD net exposureの実数値と通貨ペア別内訳 | ブローカーポジション一覧 |
| 2 | rnb_usdjpy が直近いつからdirection_filterをブロックし始めたか（開始時刻） | ログの時系列遡り |
| 3 | daytrade_1h_eurのスロット占有ポジションの損益・保有時間 | オープンポジション一覧 |
| 4 | main_loop_restarts=1 の発生時刻と原因 | 起動ログ・例外ログ |
| 5 | direction_filterの判定条件（閾値・参照TF・ロジック）が変更されていないか | 直近のコミット履歴 |

---

## 対処方針

### 緊急度：中（取引機会損失が継続中、リスク超過ではあるが増加トレンドか要確認）

**rnb_usdjpy:direction_filter 対応**
- direction_filterのブロック開始

## Alert: 22:32 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=277

### 診断
## 原因分析

### 推測（根拠あり）

- **`rnb_usdjpy:direction_filter=277` の異常値**
  - 他のブロック要因（最大199）と比較して突出して高い。通常運用では想定外の頻度でdirection_filterが発火し続けている
  - rnb（レンジブレイク系と推定）のUSDJPY戦略において、**相場の方向性判定ロジックが現在の値動きと継続的に不一致**を起こしている可能性が高い
  - 直近のUSDJPY相場が急落・急騰・乱高下しており、direction_filterの閾値条件を相場実態が超え続けている状態と推測

- **`daytrade_eurjpy` のShadow bypass多発**
  - `unknown daytrade_eurjpy/EUR_JPY` が繰り返しSlot bypass → Shadow遷移している
  - スロット管理上でdaytrade_eurjpyが「未知モード」として認識されており、**モード登録漏れまたは設定ファイルの不整合**が疑われる
  - main_loop_restarts=1が発生しており、再起動後にモード定義の一部が未ロードとなった可能性あり

---

### 確認事項（要実機確認）

1. **direction_filterの発火条件確認**
   - rnb_usdjpyのdirection_filterがどのシグナル値・指標値で判定しているか（MA乖離・ADX・トレンド方向など）
   - 現在のUSDJPY相場がその判定範囲を継続的に逸脱していないか時系列で確認

2. **277というカウントの計測期間を確認**
   - 他カウンターと同一リセット周期か、またはカウンターリセットが正常に動作しているか

3. **daytrade_eurjpyのモード登録状態を確認**
   - 設定ファイル・モード定義リストにdaytrade_eurjpyが正しく登録されているか
   - main_loop再起動（restarts=1）のタイミングとShadow bypass開始タイミングの一致を確認

4. **main_loop_restarts=1の原因確認**
   - 再起動直前のエラーログを確認（メモリ・例外・外部API障害など）

---

## 対処方針

- **即時対応（監視強化）**
  - rnb_usdjpyのdirection_filterによるブロックが実損を防いでいるのか、それとも本来エントリーすべき機会を過剰にブロックしているのかを判断し、過剰であれば閾値パラメータの見直しを検討
