# Trade Monitor Alerts: 2026-05-26

## Alert: 01:54 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=294

### 診断
## 原因分析

### 【推測】

**主因：`rnb_usdjpy:direction_filter` が異常カウント（294）**

- `rnb_usdjpy` モードが、何らかの条件により **ほぼすべてのシグナルを `direction_filter` でブロックし続けている**
- 他のブロック項目（最大でも119）と比較して突出しており、**フィルター判定ロジックが意図しない固着状態**に陥っている可能性が高い
- 考えられるパターン：
  - **トレンド方向判定（バイアス判定）の結果が長時間同一方向に固定**されており、逆張り・順張り問わずシグナルが通過できない状態
  - USDJPY が WIDE_RANGE レジーム（ログで繰り返し検出）にあるため、**レジーム判定がdirection_filterの判断基準に干渉**している
  - direction_filterのリセット条件・TTL（有効期限）が機能していないか、閾値設定が現在の相場環境に対して過剰に厳しい

---

### 【確認事項】

1. **`rnb_usdjpy` モードのステータス**
   - `running` フラグが `true` か
   - `last_signal` の内容・時刻（シグナル自体が止まっていないか）

2. **`direction_filter` の判定ソース**
   - どの時間足・指標（EMA/ADX/バイアス等）を参照しているか
   - 現在の判定値・方向が何で、いつから固定されているか

3. **フィルターのリセット条件**
   - TTL・有効期限が設定されているか、正常に機能しているか
   - `main_loop_restarts: 1` が発生したタイミングと、カウント急増のタイミングが一致するか

4. **WIDE_RANGE レジームとの関係**
   - `direction_filter` がWIDE_RANGEレジーム検出時に強化される設計になっているか
   - レジーム解除条件が満たされているか

---

## 対処方針

- **即時：** `rnb_usdjpy` の `direction_filter` カウント増加レートをモニタリングし、**現在進行形で積み上がっているか・頭打ちか**を確認
- **短期：** direction_filterの判定根拠（バイアス方向・数値）をログまたは内部状態から取得し、**固着の有無を確認**
- **暫定対処：** フィルター固着が確認された場合、`rnb_usdjpy` モードを**一時停止 → 再起動**してフィルター状態をリ

## Alert: 05:56 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=297, daytrade:hedge_block=281, daytrade_eurjpy:hedge_block=246

### 診断
## 原因分析

### 推測（ログ・数値から高確度で推定）

**① rnb_usdjpy:direction_filter=297（最多）**
- RNBストラテジーのdirection_filterが長時間・高頻度でブロックし続けている
- トレンド方向とシグナル方向が継続的に乖離している状態と推定
- 現在のUSDJPYが一方向トレンド中（BUY優勢）であり、RNBが逆張りシグナルを大量生成しているが全て遮断されている可能性が高い

**② daytrade:hedge_block=281 / daytrade_eurjpy:hedge_block=246**
- 複数ポジションがBUY/SELL混在でヘッジ状態に入り込んでいる
- ログにBUY系（profit=0.008〜0.012）とSELL系（profit=0.00083〜0.00095）が**同時進行**しており、実際にヘッジポジションが複数存在していることを裏付けている
- hedge_blockが累積し続けているのは、含み益保護（SIGNAL_REVERSE無効化）によりヘッジ解消のトリガーが機能せず、ポジションが張り付いている状態と推定

**③ DT含み益保護ログの異常頻度（数秒おきに同一ポジションIDが繰り返し出現）**
- SIGNAL_REVERSEが毎回生成→毎回無効化のループに入っている
- 本来は一度無効化されたら次サイクルまで再生成されないべきシグナルが、インターバル（30秒）未満の間隔で再生成され続けている構造的問題と推定

**④ SHADOW Slot bypass（unknown daytrade/USD_JPY）が頻発**
- `entry_type: "unknown"` がシグナルとして出力されており、entry_type分類ロジックが正常に機能していない可能性
- shadowスロットへの迂回が繰り返されているのは、liveスロットが埋まったまま解放されていない副作用と推定

---

### 確認事項（断定前に要確認）

- **ヘッジポジションの実在確認** — OANDA/証券会社側で現在のUSDJPY・EURJPYのオープンポジション一覧を確認し、BUY/SELLが混在しているか
- **RNBのdirection_filter判定基準** — 現在のdirectionフィルターに使用しているMAや基準足の向きがどちらを向いているか（一方向に固定されていないか）
- **SIGNAL_REVERSE生成元モジュール** — 30秒インターバルより短い周

## Alert: 09:35 UTC

### 検出された問題
- 💰 **含み益+285.7pip** (12ポジション, LIVE=0 Shadow=12) | Top: sr_anti_hunt_bounce×EUR_JPY +42.6pip | sr_anti_hunt_bounce×EUR_JPY +42.6pip | sr_anti_hunt_bounce×EUR_JPY +42.2pip
- ⚠️ **Shadow含み益+297.7pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】主因

**EUR/JPYのsr_anti_hunt_bounceシグナルがShadowモードに留まっており、OANDAへ発注されていない**

- `LIVE=0 / Shadow=12` という構成から、12ポジション全てがShadow（疑似）ポジションであり、実注文はゼロ
- EUR/JPYが `PAIR_PROMOTED` リストに含まれていないため、Shadowから昇格できずに含み益+297pipが空利益になっている
- `sr_anti_hunt_bounce×EUR_JPY` が上位3件を独占しており、戦略・通貨ペアとも集中している

---

### 【推測】副因・背景

- `daytrade_eurjpy:hedge_block` のブロック数が **63** と突出して高く、EUR/JPY方向への新規エントリーがヘッジ判定で大量に抑制されている
- `daytrade_eur:hedge_block` も36件あり、EUR系全般でヘッジブロックが支配的
- `ema_trend_scalp × USD_JPY` の `R2_SHADOW_DEMOTE` が連続発火しており、スキャルプ系セルも昇格できない状態
- `main_loop_restarts: 1` があり、直近でループ再起動が1回発生している（状態リセットの可能性）

---

### 【確認事項】（実施前に必ず確認すべきこと）

| # | 確認内容 | 理由 |
|---|----------|------|
| 1 | EUR/JPYが `PAIR_PROMOTED` に未登録であることを設定ファイルで確認 | 昇格ブロックの直接原因の特定 |
| 2 | `hedge_block` の判定根拠（どのポジションがヘッジ対象と見なされているか）を確認 | 誤検知かどうかの判断 |
| 3 | `main_loop_restarts: 1` の発生時刻とその前後ログを確認 | 状態喪失・Shadow/LIVE整合性崩壊の有無 |
| 4 | Shadowポジションの建値・ロット・方向がLIVE発注想定と一致しているか確認 | 昇格後に想定外ポジションが発生しないか |
| 5 | 現在のEUR/JPYスプレッド・市場流動性を確認（祝日・薄商い等） | 昇格タイミングの妥当性 |

---

## 対処方針

### 即時対応

- **EUR/JPYを `PAIR_PROMOTED` に追加するか否かを判断する**
  - 追加する場合：Shadowポジション12件が一斉にL

## Alert: 12:33 UTC

### 検出された問題
- 💰 **含み益+45.1pip** (8ポジション, LIVE=0 Shadow=8) | Top: sr_anti_hunt_bounce×EUR_JPY +28.3pip | sr_anti_hunt_bounce×EUR_JPY +28.3pip | bb_rsi_reversion×USD_CHF +1.5pip
- ⚠️ **Shadow含み益+59.4pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 推測（ログ・ステータスから判断）

**① Shadow→LIVE昇格が機能していない（主因）**
- 8ポジション全てがShadow状態のまま。LIVE=0は異常
- `sr_anti_hunt_bounce×EUR_JPY`が+28.3pipで滞留しており、昇格トリガーが発動していない
- `PAIR_PROMOTED`への追加漏れ、またはプロモーション判定条件（閾値・通貨ペア許可リスト）を通過できていない可能性

**② `daytrade_eurjpy:hedge_block`カウントが異常に高い（48回）**
- EUR_JPYへのエントリーがヘッジブロックで大量に抑制されている
- 既存ポジション（BUY c2438a2e / bd512444）が+0.28〜+0.327pips含み益を持ったままHOLD保護されており、逆方向シグナルを全て無効化 → ヘッジ判定を誘発し続けている可能性

**③ `rnb_usdjpy:direction_filter`が49回と突出**
- USD_JPYのRnBモードが方向フィルターで完全に封鎖されている
- MTFバイアスとの不整合、またはフィルター閾値が現在の相場環境に対して過剰に厳しい

**④ main_loop_restartが1回発生**
- 昇格ロジックや状態管理オブジェクトがリスタート後にリセットされ、Shadowポジションの追跡状態が引き継がれていない可能性

---

### 確認事項（推測と分離）

- [ ] `PAIR_PROMOTED`リストにEUR_JPY・USD_CHFが含まれているか
- [ ] Shadow昇格条件（pip閾値・保有時間・信頼度）の現在設定値
- [ ] main_loop_restart前後でShadowポジションのIDが正しく引き継がれているか（永続化ストアの確認）
- [ ] c2438a2e / bd512444 の2ポジションがHOLD保護されたまま残留している理由（利確・損切りが実行されていないか）
- [ ] `hedge_block`の判定ロジックにおいて、HOLDポジションが「オープンポジション」としてカウントされているか
- [ ] `direction_filter`の判定に使用しているMTFスコアの現在値

---

## 対処方針

- **即時対応**
  - EUR_JPYおよびUSD_CHFを`PAIR_PROMOTED`に追加し、Shadow→LIVE昇格を解放する
  - main_loop_restartに伴うShadowポジション追跡
