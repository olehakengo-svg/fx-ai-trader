# Trade Monitor Alerts: 2026-06-10

## Alert: 02:02 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=274

### 診断
## 原因分析

### 【推測】原因

- `rnb_usdjpy:direction_filter` のカウント **274** は他のブロック項目（最大68前後）と比較して**突出して異常に高い**
- `rnb_usdjpy` モジュールが方向フィルター（トレンド方向と逆向きシグナルを弾くロジック）に**繰り返しヒットし続けている**状態
- 現在のUSDJPYが **単一方向への強いトレンド中または方向判定が固着** しており、逆方向シグナルを大量発生させているか、もしくはフィルター判定の基準値（EMA・ADXなど）が**現在の相場レンジに対して過敏または誤方向に固定**されている可能性が高い
- `R2_SHADOW_DEMOTE` ログがUSD_JPY・GBP_USDで高頻度発生しており、**シグナル生成自体が過剰に活発**な状態であることとも整合する
- USD/CHFのTIME_DECAY_EXITでのロスもあり、アジア時間早朝帯（02:00前後）に**流動性低下＋スプレッド拡大（FR=178%⚠️）**が重なっている環境も影響している可能性

---

### 【確認事項】先に確認すべきこと

- [ ] `rnb_usdjpy` モジュールの**方向フィルターの判定基準**（使用インジケーター・閾値・参照足）を確認する
- [ ] 現在のUSDJPY価格帯・トレンド方向と、フィルターが認識している方向が**一致しているか**確認する
- [ ] direction_filterのカウントが**いつ頃から急増したか**を時系列で確認する（単発急増か、じわじわ増加か）
- [ ] `rnb_usdjpy` モードのステータスオブジェクトが**ログ出力に含まれているか**（statusのmodesに`rnb_usdjpy`が見当たらない→モード定義またはステータス取得に問題がある可能性）
- [ ] `direction_filter` のリセット条件が正しく機能しているか（カウンターが**リセットされずに累積**し続けていないか）

---

## 対処方針

- **即時確認**
  - `rnb_usdjpy` モードがstatusの`modes`に存在しない理由を確認する（モード停止・登録漏れの疑い）
  - direction_filterブロックのログを抽出し、ブロック開始タイミングと頻度推移を確認する

- **短期対処**
  - USD

## Alert: 14:46 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=390, daytrade_eurgbp:hedge_block=313, daytrade_eur:hedge_block=205

### 診断
## 原因分析

### 推測（ログ・カウントから読み取れる可能性が高い事象）

**① `rnb_usdjpy:direction_filter=390`（最多・最深刻）**
- RNBモードがUSDJPYに対して方向フィルターを連続発動している
- 相場の方向性が定まらない（レンジ内での往来）か、RNBの方向判定ロジックが現在の価格構造と乖離している可能性
- 他モードでUSDJPYのHOLD/含み益保護が作動しており、方向が固定されていることで対向シグナルを全て弾いている可能性

**② `daytrade_eurgbp:hedge_block=313`**
- EURGBPで既存ポジションのヘッジとなる方向に対してシグナルが持続的に発生している
- ログに `wick_imbalance_reversion score=-0.16 misaligned with BUY | EUR_GBP` が **約20秒おきに反復出現**しており、SCORE_GATEとhedge_blockが二重にブロックしている
- スコアが毎回同値(-0.16)であることから、特定セルのキャッシュ固着または評価ロジックの停滞が疑われる

**③ `daytrade_eur:hedge_block=205`**
- EURUSDで同方向の問題（EURGBPと相関）
- `r2_shadow_demoted_cell ema_trend_scalp x EUR_USD` が秒単位で大量発生しており、EURUSDのシャドーセルが機能不全に陥っている

---

### 確認事項（実機・ログで検証が必要なもの）

- [ ] **現在のオープンポジション一覧**：USDJPY/EURGBP/EURUSDに何のポジションが何方向で何本立っているか
- [ ] **hedge_blockのトリガー条件**：ヘッジブロックのリセット条件（ポジションクローズ、時間経過等）が正常に機能しているか
- [ ] **direction_filterの状態変数**：RNBのdirection判定フラグが前回の確定値から更新されているか（スタック・固着の可能性）
- [ ] **`ema_trend_scalp x EUR_USD` セルのR2スコア推移**：同一セルが繰り返しdemoteされているなら、promote条件を満たせていない理由の確認
- [ ] **`main_loop_restarts=1`の発生時刻**：再起動のタイミングとblock_count急増のタイミングが一致するか

---

## 対処方針

### 即時対応
- **rnb_usdjpy：direction_filterの強

## Alert: 18:22 UTC

### 検出された問題
- ⚠️ **block_counts異常** — daytrade:hedge_block=205

### 診断
## 原因分析

### 【推測】hedge_block=205の直接原因

- `daytrade:hedge_block` がシステム全体で最大値（205）を記録しており、**USD/JPYデイトレードポジションが長時間保有され、同方向・逆方向シグナルが大量にブロックされ続けている**可能性が高い
- `rnb_usdjpy:direction_filter=192` が連動して高値を示しており、**USD/JPY方向フィルタとhedge_blockが相互に機能して、USDJPYの新規エントリーがほぼ全件遮断されている**状態と推測
- `daytrade_gbpusd:hedge_block=95`、`daytrade_eur:hedge_block=113` も同様に高水準であり、**複数通貨ペアで既存ポジションのヘッジ抑制ロジックが広範に発動**していると見られる

### 【推測】副次的な懸念

- `main_loop_restarts=1` が記録されており、カウント蓄積のリセットが行われず **hedge_blockが起動以降の累積値として膨張**している可能性がある
- `log_count=9365` は比較的高く、シグナル生成頻度自体は正常だが、そのほとんどがブロックされて消費されている状態と推測される
- `daytrade_eurgbp:mtf_strong_bias=64` / `recent_emit=61` が高く、**EUR/GBPはシグナル生成は活発だが多重フィルタで実質停止**に近い

---

## 【確認事項】（実施前に必ず確認）

- **現在のオープンポジション一覧**：USD/JPYおよびGBP/USD、EUR/USDにアクティブなポジションが残存しているか
- **hedge_blockカウンタのリセット仕様**：累積カウントか、直近N件のローリングカウントか
- **`main_loop_restarts=1` の発生時刻とログ**：再起動前後でポジション状態の引き継ぎが正常に行われたか
- **hedge_blockの発動閾値設定値**：現在の閾値が相場ボラティリティに対して適切か
- **`rnb_usdjpy:direction_filter=192` の方向判定内容**：現在BULLISHかBEARISHか、および根拠となるレジーム判定

---

## 対処方針

- **即時対応（監視）**
  - オープンポジションの含み損益・ストップ位置を確認し、異常な塩漬けポジションが存在しないか確認する
  - `daytrade:hedge_block` が今後も増
