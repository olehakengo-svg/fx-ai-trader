# Trade Monitor Alerts: 2026-07-10

## Alert: 01:26 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=204

### 診断
## 原因分析

### 推測（仮説）

- **`rnb_usdjpy:direction_filter=204`が突出している主因**
  - 他のblock_countsが一桁〜数十台であるのに対し、204は約3〜30倍の異常値
  - `direction_filter`はトレンド方向判定フィルタと推定されるため、**USDJPYに対して長時間・高頻度でシグナルが発生し続けているが、全件フィルタで弾かれている**状態
  - rnbモードが短いインターバルで繰り返しシグナル生成を試み、そのたびにdirection_filterに引っかかっている可能性が高い

- **direction_filterが永続的にブロックし続けている背景として考えられるもの**
  - トレンド判定ロジックが現在の相場（USDJPY高値圏・161円台）に対して常に逆方向と判定している
  - フィルタ閾値やパラメータが相場環境の変化に追従できていない
  - rnbモードのシグナル生成ループのインターバルが極端に短い、またはシグナル発生条件が緩すぎる

- **`ORDER_BAR_DEDUP`の大量発生（別問題）**
  - `bar_ts=2026-07-10T01:15:00`が長時間継続しており、**バーの時刻が更新されていない**（バーデータ取得が止まっている、またはキャッシュが古いままになっている）可能性
  - main_loop_restarts=1が記録されており、再起動後にbar_tsリセットが不完全な可能性

---

### 確認事項（実機で要検証）

1. **rnbモードのシグナル生成頻度**
   - rnb_usdjpyのインターバル設定値と、実際のシグナル生成ログの時系列を確認
   - 直近1時間でdirection_filterにブロックされたタイムスタンプ分布を確認

2. **direction_filterの判定内容**
   - 現在の判定値（トレンド方向スコア等）と閾値を確認
   - 同時刻帯のUSDJPYトレンド指標（MA傾き・ADX等）を確認し、フィルタが意図通り動作しているか検証

3. **bar_tsが更新されていない件**
   - データフィードの死活状態（OANDAまたはデータソースAPIの応答確認）
   - main_loop_restarts=1の発生時刻とbar_ts停止時刻の一致確認

4. **rnbモードのパラメータ変更履歴**
   - 直

## Alert: 12:16 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=247, daytrade_eur:hedge_block=246

### 診断
## 原因分析

### 【推測】根本原因候補

**① rnb_usdjpy:direction_filter=247（最有力）**
- RNBストラテジーがUSDJPYの方向フィルターで247回連続ブロックされている
- ログに `[MTF_MONITOR] USD_JPY vol_momentum_scalp signal=SELL mtf=trend_up_weak d1=1 h4=1` が繰り返し出現しており、**上位足がトレンドアップ判定**のためSELLシグナルが全滅している状態
- フィルター条件（MTF方向一致要件）が現在の相場環境（上昇トレンド継続中）と永続的に噛み合っていない可能性

**② daytrade_eur:hedge_block=246（最有力）**
- EURペアのデイトレードがヘッジブロックで246回遮断されている
- 既存ポジションとの方向相反シグナルが大量発生していると推測
- ログの `DT含み益保護: BUY → SIGNAL_REVERSE無効化` パターンと構造が類似しており、**逆張りシグナルの連続発火**が根本にある可能性

---

### 【確認事項】（推測と分離）

| 確認項目 | 確認方法 |
|---|---|
| direction_filterの判定条件と閾値 | rnb_usdjpyのフィルターロジック設定値を確認 |
| USDJPYの現在のMTF方向判定値 | d1/h4のトレンド判定出力ログを確認 |
| daytrade_eurの既存ポジション方向と数量 | ブローカーAPI or ポジション管理テーブルを確認 |
| hedge_blockのトリガー条件（一方向ポジション上限か、損失閾値か） | hedge_blockロジックの設定を確認 |
| 両カウンターがリセットされているか（累積 vs 現セッション） | block_countsの集計起点（起動時刻 vs 固定期間）を確認 |
| main_loop_restarts=1の発生タイミングと原因 | 再起動前後のログを確認 |

---

## 対処方針

### 即時対応
- **現時点では緊急停止不要**（emergency_killed=false、main_loop_alive=true）
- ただし両カウンターが**実質的に発注機会ゼロ**を示している可能性があるため、意図した通りに動作しているか確認を優先

### rnb_usdjpy:direction_filter 対応
- direction_filterがUSDJPY上昇トレンド継続中に恒常的に機能しているな
