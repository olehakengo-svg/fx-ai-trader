# Trade Monitor Alerts: 2026-06-04

## Alert: 02:28 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=278

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=278 の異常カウント原因

**主要推測：**
- `rnb_usdjpy` の方向フィルター（direction_filter）が、現在のUSDJPY相場の方向性と**長時間・高頻度で競合**している
- RNBロジックが出すシグナル方向（例：SELL）と、上位足や内部バイアス判定が示す方向（例：BUY）が**持続的に不一致**となっており、フィルターが連続ブロックし続けている
- 278回というカウントは他のブロック項目（次点で `daytrade_eurjpy:hedge_block` の189）と比較しても**突出しており異常値**

**補足推測：**
- main_loop_restartsが1回発生しており、再起動後にカウンタがリセットされていない可能性、またはリセット後でも急増している可能性がある
- 相場がレンジ〜反転局面にあり、RNBが頻繁にシグナルを出す一方でフィルターが追いついていない構造的ミスマッチの可能性

---

### 【確認事項】（推測と分離）

- [ ] `rnb_usdjpy` の直近シグナル方向と、direction_filterが参照している上位足バイアスの現在値を確認
- [ ] direction_filterのブロック判定ロジック（閾値・参照timeframe・更新頻度）を確認
- [ ] block_countsのリセットタイミング（起動時リセット？定期リセット？）を確認し、278が**累積なのか短時間集中なのか**を切り分ける
- [ ] rnb_usdjpyのシグナル発火間隔ログを確認し、**異常に高頻度でシグナルを生成していないか**チェック
- [ ] main_loop_restart（1回）のタイミングと、カウント急増タイミングの相関を確認

---

## 対処方針

- **即時確認（監視継続）**
  - rnb_usdjpyのシグナル方向とdirection_filterのバイアス値を比較し、乖離が継続中かを確認
  - 乖離が継続中であれば**実害（誤発注・機会損失）は限定的**だが、RNBロジック自体がノイズシグナルを大量生成していないか確認

- **短期対処**
  - rnb_usddjpy戦略の一時的なシグナル発火頻度を制限（cooldownまたはrecent_emit制約の追加検討）
  - direction_filterが参照するバイアスの更新ロ

## Alert: 08:10 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=465, daytrade_eurjpy:hedge_block=317, daytrade_eurgbp:hedge_block=248, daytrade_gbpusd:hedge_block=235

### 診断
## 原因分析

### 【推測】高確度

- **`rnb_usdjpy:direction_filter=465`（最大異常）**
  - RNB戦略のdirection_filterが長時間・大量にブロックし続けている
  - USDJPYが現在レンジ相場（`daytrade` last_signal=WAIT, confidence=20）にもかかわらず、RNBが単一方向へのエントリーを繰り返し試みている可能性
  - またはdirection_filterの判定閾値・参照TFが現在の相場構造（WIDE_RANGE検出ログあり）と噛み合っていない可能性

- **`daytrade_eurjpy/eurgbp/gbpusd:hedge_block` 異常蓄積（317/248/235）**
  - EUR/GBP/JPY系の既存ポジションがヘッジ状態で長期間滞留しており、同一通貨軸の新規エントリーが全面的にブロックされ続けている
  - ログの `[HOLD] DT含み益保護` や `SIGNAL_REVERSE無効化` から、逆張りシグナルが出ても既存ポジション保護で決済・転換ができていない状態と整合する
  - EURJPYは `score_gate` も頻発しており、シグナル品質自体も低い状態でブロック数が膨らんでいる

- **WIDE_RANGE環境が根本的なトリガー**
  - ログで複数通貨ペアに `WIDE_RANGE detected` が記録されており、レンジ相場でのトレンドフォロー系シグナルが大量発生→全段階でブロックされる構造的な詰まりが発生している

---

### 【推測】中確度

- `same_price_0pip` の多発（EURGBP/GBPUSD等）は価格更新の停滞またはティック間隔の粗さを示唆しており、流動性低下が相場環境の悪化を補強している
- `main_loop_restarts=1` があり、再起動前後でヘッジブロックカウンターがリセットされずに引き継がれた可能性

---

### 【確認事項】（推測ではなく事実確認が必要なもの）

1. **ヘッジブロック対象ポジションの実態確認**
   - EURJPY/EURGBP/GBPUSDの現在オープンポジションの方向・建値・含み損益・保有時間を確認
   - ヘッジ判定の対象となっている通貨軸が実際に干渉し合っているか確認

2. **RNB direction_filterの判定ロジック確認**
   - どの

## Alert: 12:35 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=257

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=257 の異常値について

- **方向フィルターが連続ブロックし続けている**可能性が高い
  - 257回という値は他のブロックカウント（最大159: daytrade_eurgbp:hedge_block）と比較して突出しており、**フィルター条件が長時間・高頻度で発火し続けている**状態を示している
  - rnb_usdjpy（RNB戦略）がUSD/JPY相場の現在のトレンド方向と逆方向のシグナルを大量に生成しており、それをdirection_filterが全て弾いている構図と推測される
  - または、direction_filterの**判定ロジック自体が誤作動**（例：トレンド判定フラグが固着・反転しない状態に陥っている）している可能性がある

- **USD/JPY相場の膠着・レンジ相場**が背景にある可能性
  - daytrade側でも`confidence=20`（WAIT）、`daytrade_1h`でも`confidence=0`（WAIT）と全体的にシグナル品質が低く、相場の方向感がない状態であることと符合する

- ログにrnb_usdjpyの直接的なエントリー記録・ブロック診断が**見当たらない**
  - SENTINEL_BLOCK_DIAGがrnb_usdjpyについて出力されていないのは、rnbが診断ログ対象外か、ログが別ストリームに流れている可能性がある

---

### 【推測】副次的な懸念事項

- `ema_trend_scalp x USD_JPY` および `bb_rsi_reversion x EUR_USD` のR2_SHADOW_DEMOTEが**毎5秒前後で繰り返し**発生しており、シャドウトラッキングセルの復帰条件が満たされないまま高頻度でリトライされている
- `main_loop_restarts=1` — 過去に1回ループ再起動が発生しており、その際にdirection_filterのカウンターや内部状態が**リセットされず引き継がれた**可能性がある
- DT含み益保護（82c9a405/SELL）が機能しており、これ自体は正常動作だが、ポジション保有中にRNBがSIGNAL_REVERSEを連射している構図も考えられる

---

## 確認事項

### 優先度：高
- [ ] `rnb_usdjpy`のdirection_filter判定に使用している**トレンド方向フラグ・インジケーター値の現在値**を確認する（固着していないか）
- [ ] direction_filterのカウ

## Alert: 19:51 UTC

### 検出された問題
- 💰 **含み益+121.8pip** (23ポジション, LIVE=0 Shadow=23) | Top: xs_momentum×EUR_USD +12.7pip | xs_momentum×EUR_USD +10.8pip | sr_fib_confluence×EUR_USD +10.2pip
- ⚠️ **Shadow含み益+121.8pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — scalp_eur:max_open=338, scalp:max_open=314

### 診断
## 原因分析

### 推測（確認前）

**① Shadow23件がLIVEに昇格しない根本原因**
- `PAIR_PROMOTED`リストにEUR_USDが含まれていないため、Shadow→LIVE昇格の資格判定で弾かれている可能性が高い
- `scalp` / `scalp_eur`の`max_open`が300超という異常値は、**カウンタのリセット漏れ**または**LIVE送信失敗時にShadowポジションのカウントが累積し続けている**ことが疑われる

**② block_counts異常（scalp_eur:338, scalp:314）**
- 正常系では数十程度のはずが300超 → 「ブロックされたがカウントだけ増え続ける」ループ状態
- max_openの上限チェックは通過しているが、クリア条件（決済・昇格・タイムアウト）が機能していない可能性
- `main_loop_restarts=1`があり、再起動時にblock_countsが引き継がれたまま初期化されなかった可能性も併存

**③ SENTINEL_BLOCK_DIAG `max_open(23/8)`**
- 上限8に対して現在23が詰まっており、これが新規エントリを完全に封鎖している
- ShadowがLIVEに出ないためカウントが減らず、上限超過状態が固着している悪循環

---

### 確認事項（実機で要検証）

| # | 確認内容 | 確認方法 |
|---|----------|----------|
| 1 | `PAIR_PROMOTED`にEUR_USDが含まれているか | 設定ファイル・環境変数を直接確認 |
| 2 | block_countsのリセットタイミングが実装されているか | カウンタ管理コードのリセット条件を確認 |
| 3 | main_loop_restart時にblock_countsが永続化ストアから復元されているか | 再起動ログと永続化ストアの整合確認 |
| 4 | Shadowポジション23件の生成時刻と滞留時間 | ポジション一覧のタイムスタンプ確認 |
| 5 | OANDA APIへの送信試行自体が発生しているか | API送信ログ（エラー含む）を確認 |

---

## 対処方針

### 即時対応

- **`PAIR_PROMOTED`にEUR_USDを追加**し、Shadowポジションの昇格資格を付与する
- Shadow23件の含み益+121.8pipは時間経過で縮小リスクがあるため、**手動でLIVE昇格を強制実行**する手段がない
