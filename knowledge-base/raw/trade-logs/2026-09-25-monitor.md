# Trade Monitor Alerts: 2026-09-25

## Alert: 09:08 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=560

### 診断
## 原因分析

### 【推測】rnb_usdjpy:no_signal=560 の主因候補

1. **USD/JPY相場のレンジ膠着**
レンジブレイク（RnB）戦略はトレンド発生を前提とするが、相場がレンジ内に留まり続けているため、エントリー条件を満たすシグナルが長時間生成されていない可能性が高い

2. **SCORE_GATEによるUSD/JPYブロックとの連鎖**
ログにUSD_JPY daytrade向け `streak_reversal score=-1.10` のSCORE_GATEが連続しており、同一通貨ペアのシグナル評価環境が悪化、rnbモジュールのシグナル生成判断にも影響している可能性

3. **RnBモデル内部のブレイク判定閾値に対して値幅が不足**
160円台前後でレンジが圧縮されており、ブレイクアウト検知に必要なpip幅・ボラティリティ条件を相場が満たしていない可能性

4. **フィード・tick供給の問題ではない（ほぼ否定）**
`engine_tick_age_sec=2.9`、`engine_tick_status=ok` であり、tickは正常供給されている。データ欠落ではなくロジック的no_signalと判断

---

### 【確認事項】まず確認すべきこと

- **RnBモジュールのレンジ判定ログ**を抽出し、レンジ幅・上限/下限の更新頻度を確認する
- **no_signalカウンターがいつから急増したか**をタイムスタンプで特定し、相場イベント（指標発表・時間帯移行）との相関を確認する
- **rnb_usdjpy固有のパラメータ**（ブレイク閾値pip数、ATR乗数、最小レンジ幅等）が現在の相場ボラティリティと乖離していないか確認する
- `rnb_usdjpy:order_bar_dedup=4` は極めて少数であり、シグナル自体がほぼ出ていないことを裏付けているか確認する

---

## 対処方針

### 緊急度：低〜中（エンジン正常稼働中、損失リスクは現時点で低い）

- **監視継続**：他通貨・他モードは正常稼働しており、即時介入は不要。ただし560という数値は異常水準のため放置しない
- **RnBパラメータの一時的緩和を検討**：ブレイク判定閾値を現在のATR

## Alert: 14:49 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=1076, scalp:r2_shadow_demoted_cell=437, scalp_eur:r2_shadow_demoted_cell=406, daytrade:order_bar_dedup=383, scalp_5m_gbp:r2_shadow_demoted_cell=308

### 診断
## 原因分析

### 推測（ログ・カウントから高確度で判断）

**① r2_shadow_demoted_cell 多発（scalp/scalp_eur/scalp_5m_gbp/scalp_5m_eur 合計1,455件）**
- `fib_reversal` セルが USD_JPY・EUR_USD で連続的にR2シャドウトラッキングを外れている
- 発生間隔が数秒単位で極めて高頻度 → 相場がfibレベルを細かく往復するレンジ相場に入り、セルが昇格・降格を繰り返すデグレードループに陥っている可能性が高い
- scalp系複数モードで横断的に発生しているため、モード固有のバグではなく **相場環境起因の構造的な問題**

**② daytrade:order_bar_dedup 多発（383件）**
- `xs_momentum_rsi USD_JPY SELL` が同一bar_ts（14:45:00）で繰り返しブロック
- 初回ブロック理由が `same_price_5pip` → 5pip以内に価格が張り付いており、同バーのシグナルが発火し続けている
- 価格膠着により同一バーへの再エントリー試行が止まらない状態

**③ rnb_usdjpy:no_signal 異常多発（1,076件）**
- USDJPYがレンジ相場でRnBシステムの方向性判断が出せない状態
- ①②と合わせ、**USDJPYが方向感のない膠着相場**にあることを強く示唆

---

### 確認事項（断定前に要検証）

- [ ] 現在のUSDJPY価格帯とATR・ボラティリティ指標の値（レンジ相場仮説の裏付け）
- [ ] `fib_reversal` セルのR2スコア推移履歴（閾値付近での振動か、スコア計算バグか）
- [ ] `r2_shadow_demoted_cell` の異常検知閾値設定値（現在の閾値が相場環境に対して過敏すぎないか）
- [ ] `engine_tick_stalest_mode: daytrade_1h_usdcad` が56秒遅延している理由（処理負荷過多が波及していないか）
- [ ] `engine_start_origin: statusheal` → 直近に自動再起動が発生した経緯と原因ログ

---

## 対処方針

### 即時対応

- **scalp/scalp_eur/scalp_5m_gbp のfib_reversalセル一時停止を検討**
  - デグレードループが継続する限りブロックカ

## Alert: 19:37 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=1558, scalp:r2_shadow_demoted_cell=680, daytrade:order_bar_dedup=569, scalp_eur:r2_shadow_demoted_cell=540, scalp_5m_gbp:r2_shadow_demoted_cell=417

### 診断
## 原因分析

### 推測（ログ・数値から高確度で推定）

**① `r2_shadow_demoted_cell` 多発（scalp系/daytrade系 合計2,000件超）**
- `bb_rsi_reversion × USD_JPY` がシャドウセルとして降格され続けている
- スキャルプ系（scalp, scalp_5m, scalp_eur, scalp_5m_gbp）で同一パターンが全通貨ペアに波及しており、**bb_rsi_reversionストラテジー自体のR2スコアが閾値を下回る状態が継続**していると推測
- 相場がレンジブレイク気味またはボラティリティ急変により、BBリバーション系の予測精度が構造的に劣化している可能性

**② `rnb_usdjpy:no_signal=1558`（突出して多い）**
- RnBストラテジーがUSD/JPYでシグナルを一切出せていない
- レンジバウンド判定の条件（価格帯・BB幅・ATRなど）を現在の相場が満たしていない、またはフィルタが過剰に厳しくなっていると推測
- DT含み益保護ログで**SELLポジション11efdc75が長時間保有継続**しており、USD/JPYが一方向にトレンドしている可能性＝RnBがノーシグナルになる背景と整合

**③ `daytrade:order_bar_dedup=569`（daytrade系全体で合計1,400件超）**
- 同一バーでの重複発注ブロックが頻発
- エンジンが `statusheal` 起動（`engine_start_origin: statusheal`）しており、**再起動直後に内部状態の不整合**が生じ、同一シグナルを複数回発火させていると推測
- `daytrade_1h_usdchf` の stalest_age=65秒 も関連する可能性あり

**④ DT含み益保護のSIGNAL_REVERSE無効化ループ**
- 同一ポジション（11efdc75）に対してほぼ毎ティック発火しており、**逆張りシグナルが繰り返し生成されている**
- 含み益が0.025〜0.036で推移＝ポジションは生きているが、対向シグナルの抑制が連続発動するほど逆方向の圧力がある状態

---

### 確認事項（要実地検証）

- [ ] `bb_rsi_reversion` のR2スコア現在値と降格閾値を確認。閾値付近で連続降格→復帰を繰り返していないか（チャ

## Alert: 22:53 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=1806, scalp:r2_shadow_demoted_cell=1037, scalp_eur:r2_shadow_demoted_cell=604, daytrade:order_bar_dedup=569, scalp_5m_gbp:r2_shadow_demoted_cell=532

### 診断
## 原因分析

### 【推測】各異常の原因

**① `rnb_usdjpy:no_signal=1806`（最大異常）**
- RNB（レンジ判定系）ストラテジーがUSD/JPYでシグナル生成条件を長期間満たせていない
- 市場がRNBの想定レンジ幅・方向性と乖離した一方向トレンドまたは過大ボラティリティ状態に入っている可能性が高い
- パラメータ（レンジ判定閾値・バンド幅）が現在の相場環境と著しくミスマッチしている可能性

**② `r2_shadow_demoted_cell` の複数モード横断的急増（scalp系が特に顕著）**
- ログから `ema_trend_scalp x USD_JPY` と `fib_reversal x GBP_USD` が直近10分程度で連続発火しており、特定セルが繰り返し降格され続けている
- シャドートラッキングセルの昇格→降格サイクルが高速で繰り返されている＝相場が方向感なく細かく反転しているか、シグナルのノイズ耐性が不足している状態
- scalp・scalp_eur・scalp_5m_gbpで合計2000超は正常運用レベルを大幅に超えている

**③ `daytrade:order_bar_dedup=569`（デイトレード系全般）**
- 同一バー内での重複注文発生が多発 → ティック到達頻度とバー確定タイミングの競合が疑われる
- `daytrade_1h_nzdusd` のティック遅延（stalest=57秒）と関連している可能性あり

---

### 【確認事項】（推測と分離）

| # | 確認項目 | 確認方法 |
|---|----------|----------|
| 1 | USD/JPYの現在ボラティリティ・トレンド状態 | チャート・ATR確認 |
| 2 | `ema_trend_scalp` と `fib_reversal` の昇格条件・降格条件の閾値設定 | 設定ファイル確認 |
| 3 | `r2_shadow_demoted_cell` のカウントが今セッション開始から急増しているか、以前から継続しているか | 過去ステータスログとの比較 |
| 4 | `daytrade_1h_nzdusd` の57秒遅延がティックソース側の問題かエンジン側の問題か | ブローカーフィード・tick受信ログ確認 |
| 5 | `engine_start_origin: statusheal` → 直近にstatusheal
