# Trade Monitor Alerts: 2026-07-20

## Alert: 01:22 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=370

### 診断
## 原因分析

### 【推測】主要原因

- **`rnb_usdjpy:direction_filter=370` の異常値**は、他のブロックカウント（最大でも `daytrade_gbpjpy:gbp_asia_flash_crash=109`）と比較して約3倍以上突出しており、**direction_filterが恒常的にトリガーされ続けている**状態を示す
- RnB（レンジ＆ブレイク）戦略の USD/JPY において、**相場の方向性がフィルター条件と長時間整合していない**（トレンド方向判定が固定方向を向いており、シグナル側と逆向きになり続けている可能性）
- USD/JPY は `daytrade` モードで `WAIT` シグナル・confidence=30 と低調であり、**相場がレンジ縮小または方向感喪失の状態**にある可能性が高い
- `main_loop_restarts=1` が記録されており、**ループ再起動時にdirection_filterの内部状態がリセットされず**、古いバイアス方向が保持され続けている可能性がある

### 【推測】副次的懸念

- `ORDER_BAR_DEDUP` が `dt_sr_channel_reversal EUR_JPY BUY` で繰り返し発火しており、**シグナル生成ループが同一バーで多重評価されている**（約2秒おきに同一 `bar_ts` でブロック）
- `R2_SHADOW_DEMOTE` の多発（GBP/USD, USD/CHF, USD/JPY）は市場ボラティリティ上昇下でのセカンダリセル抑制として正常範囲の可能性があるが、数が多い

---

## 確認事項

1. **direction_filterのロジック確認**
   - フィルターが参照しているトレンド判定指標（MA方向・ADX・レジーム判定など）が現在どの値を返しているか
   - フィルター閾値が相場実態（現在のUSD/JPYのレンジ相場）に対して適切か

2. **main_loop_restarts=1 の影響確認**
   - 再起動タイミングを特定し、direction_filterの内部状態（方向バイアス変数）がリセット処理されているか
   - 再起動前後でカウントが急増していないかログで検証

3. **シグナル生成ループの多重評価確認**
   - `ORDER_BAR_DEDUP` が2秒おきに同一バーで発火しているのは、**評価トリガーが複数箇所から呼ばれている**可能性。呼び出しスタックを確認

4. **370カウントの発

## Alert: 08:33 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=341, daytrade:hedge_block=271

### 診断
## 原因分析

### 【推測】高カウントの根本原因

**`rnb_usdjpy:direction_filter=341`（最大値・最重要）**
- RNBロジックがUSDJPYに対してシステム起動以来、ほぼ全シグナルを方向フィルターで遮断し続けている
- 現在のUSDJPY価格帯（162円台）がRNBの想定レンジ外、またはトレンドフィルターの閾値設定が現在の相場環境（強い一方向トレンド）に適合していない可能性が高い
- RNBモード自体がシグナルを出せない状態で空回りしているため、カウントが積み上がっている

**`daytrade:hedge_block=271`**
- USDJPY daytradeで含み益ポジション（f39220e4 BUY）が継続保有中であり、逆張りシグナルが出るたびにhedge_blockで遮断されている
- ログの「DT含み益保護」が頻繁に発火していることと整合する
- ポジションが決済されない限りカウントは増加し続ける構造

**両者に共通する背景**
- main_loop_restarts=1があり、カウンターがリセットされずに引き継がれている可能性
- 現在08:30台でロンドン時間序盤、方向性が固まりきらない時間帯に多数のブロックが集中

---

### 【確認が必要な事項】

1. **f39220e4ポジションの現状**
   - 保有開始時刻、現在のpnl、TP/SL設定値を確認
   - 異常に長期間保有されていないか（ゾンビポジション疑い）

2. **RNBのdirection_filterロジック**
   - フィルター判定に使用しているトレンド指標・閾値の現在値
   - 341回すべてが同一セッションで発生しているか、それとも複数日分の累積か

3. **block_countsのリセットタイミング**
   - main_loop_restart時にカウンターが意図通りリセットされているか
   - 341という値が「本日分のみ」なのか「起動以来の累積」なのかを区別する

4. **USDCHFのlondon_breakoutシャドウ降格**
   - r2_shadow_demoted_cellが127と高く、USDCHFセルが恒常的に降格されている理由（学習データの問題か、現相場との乖離か）

---

## 対処方針

- **即時確認**
  - f39220e4ポジションのTP/SL・保有時間を手動確認

## Alert: 12:07 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=280, daytrade:hedge_block=224

### 診断
## 原因分析

### 🔴 推測（高確度）

**`rnb_usdjpy:direction_filter=280`**
- RNB（レンジ/ノイズブレイク）戦略のdirection_filterが異常に多発している
- USD/JPY相場が方向性フィルターの閾値付近で長時間滞留しており、シグナルが繰り返し生成→即ブロックされるループが発生している可能性が高い
- ログに `WIDE_RANGE detected` が頻出しており、相場がWIDE_RANGEレジームにあるにもかかわらず、RNBがシグナルを出し続けている構造的衝突が疑われる

**`daytrade:hedge_block=224`**
- USD/JPY daytrade側でBUYシグナルが継続生成されているが（last_signal: BUY 12:06:37）、何らかのヘッジポジションが存在してブロックし続けている
- `ORDER_BAR_DEDUP` で `wick_imbalance_reversion USD_JPY BUY` が同一バー内で繰り返しブロックされており、ポーリング間隔内での多重発火が起きている

**共通背景**
- `main_loop_restarts=1` が記録されており、再起動後に内部状態（ヘッジフラグ・デュープキャッシュ）がリセットされずに引き継がれた、またはリセットされすぎた可能性がある

---

### 🟡 推測（中確度）

- `scalp_eur:r2_shadow_demoted_cell=182` など複数戦略でR2シャドウデモートが高水準 → R2モデルの評価精度が現在の相場環境（WIDE_RANGE + squeeze）と乖離しており、セル全体が長期降格状態に陥っている可能性
- `daytrade_eur:order_bar_dedup=122` の異常値 → EUR系で同一バーに対するシグナル再評価が過剰に走っているポーリング設定の問題

---

## 確認事項（要調査）

| 優先 | 確認内容 |
|------|----------|
| 🔴 高 | RNBストラテジーのdirection_filter判定ロジックで、WIDE_RANGEレジーム中に除外条件が機能しているか |
| 🔴 高 | `daytrade` モードでヘッジポジションが現在も保有中か、またはフラグが残存しているか（再起動後の状態引き継ぎの整合性） |
| 🔴 高 | `main_loop_restarts=1` の再起動原因ログの確認（OOM・例外・タイ
