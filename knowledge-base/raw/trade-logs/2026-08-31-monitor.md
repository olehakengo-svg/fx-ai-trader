# Trade Monitor Alerts: 2026-08-31

## Alert: 01:53 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=206

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=206`** が突出して大きい。他の全ブロックカウントの合計と比較しても異常な水準であり、**rnb_usdjpyストラテジーがシグナルを出し続けているが、direction_filterに全数ブロックされている**状態を示す
- direction_filterの性質上、**上位足トレンドまたはレンジバイアスがシグナル方向と長時間逆向きに固定されている**可能性が高い（例：USDJPYが上昇トレンド中にSELL信号を連発、またはその逆）
- ログにUSGJPY BUYのORDER_BAR_DEDUPやSCORE_GATEブロックが散見されており、**USDJPY自体に方向性の混乱またはビジーな状態**が起きていることと整合する

### 推測（中確度）

- direction_filterの判定に使用している上位足インジケーター（EMA等）またはフィルター入力データが**フリーズ・古い値・計算誤りの状態**に陥り、フィルターが誤固定されている可能性がある
- `engine_tick_stalest_mode: daytrade_1h_euraud / 61.3秒`はこのモードとは直接無関係だが、**一部の時間足データ更新が遅延しており、上位足フィルターの誤判定を誘発**している可能性がある

---

## 確認事項

- **rnb_usdjpyのdirection_filterが参照している上位足トレンド判定値（バイアス値・EMA向き等）の現在値**を確認する
- direction_filterの**入力データの最終更新タイムスタンプ**を確認し、データ陳腐化・フリーズが起きていないか検証する
- 206件ブロックが**いつ頃から急増したか**をカウンターの時系列で確認する（設定変更・データ異常のタイミングと照合）
- **market実勢**（USDJPYの実際の方向）とフィルター判定方向が一致しているか確認する
- rnb_usdjpyのdirection_filterパラメーター・閾値に**最近の設定変更がないか**をデプロイ履歴で確認する

---

## 対処方針

- まずdirection_filterへの**入力値とタイムスタンプをダンプ**し、データ異常（固定値・NaN・古すぎるタイムスタンプ）がないか確認する
- データ陳腐化が確認された場合は**該当フィード・モジュ

## Alert: 07:46 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=463, scalp:r2_shadow_demoted_cell=240, scalp_eur:r2_shadow_demoted_cell=215

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=463`（最大異常値）**
  - USD/JPYが強いレンジ相場（ADX=10.9確認）に入っており、方向性フィルターが連続的に全シグナルを棄却し続けている
  - ログのSR抑制（ADX=10.9, L1=neutral）と完全に一致。レンジ環境でrnbモードのシグナルが大量生成→全量フィルタ落ち、という悪循環構造

- **`scalp:r2_shadow_demoted_cell=240` / `scalp_eur:r2_shadow_demoted_cell=215`**
  - ログ上でengulfing_bb × USD_JPYのR2_SHADOW_DEMOTEが短時間に連発されており、シャドウトラッキングセルが長期間デモート状態に固着している
  - スキャルpモードが高頻度でシグナルを生成しているにもかかわらず、同一セルが繰り返しブロックされていることを示す

- **共通背景**
  - 08/31 07:30〜07:50頃（ロンドン序盤）に相場がレンジ推移しており、複数戦略が同タイミングで同一バーのシグナルを量産している構造

---

### 推測（中確度）

- ORDER_BAR_DEDUPのカウント急増（london_breakout, doji_breakout, wick_imbalance_reversion）は、バータイムスタンプが更新されないまま再評価が繰り返されていることを示唆。エンジンの再評価サイクルが短すぎるか、バー確定判定のロジックに問題がある可能性
- `daytrade_1h_nzdjpy` が stalest_age=62秒 と突出して遅延。このモードだけデータフィード遅延またはスレッド処理詰まりが発生している可能性

---

### 確認が必要な事項

- **rnb_usdjpy**
  - direction_filterのカウントリセットタイミングと、ADXがいつから10台に低下しているか（直近のADX推移ログ）
  - rnbモードのシグナル生成頻度そのものが異常に高くなっていないか（生成数 vs ブロック数の比率）

- **r2_shadow_demoted_cell**
  - engulfing_bb × USD_JPYのセルがデモートされた起点（初回発生時刻）と、デモート解除条件が満たされているかどうか
  - scalp / scalp_eurで同一セルが何時間連続してデモート中か

- **ORDER

## Alert: 15:34 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=1297, scalp:r2_shadow_demoted_cell=711, scalp_eur:r2_shadow_demoted_cell=623, daytrade_eur:hedge_block=488, daytrade_eurjpy:order_bar_dedup=391

### 診断
## 原因分析

### 推測（状況証拠から）

**① `rnb_usdjpy:direction_filter=1297`（最大異常値）**
- USDJPYで方向フィルターが大量発火 → レンジ相場またはトレンド転換期に戦略のシグナル方向が市場の実方向と恒常的に不一致
- direction_filterのしきい値設定が現在のレジームに対してミスマッチしている可能性が高い

**② `scalp/scalp_eur:r2_shadow_demoted_cell=711/623`**
- ema_trend_scalp / sr_channel_reversalのシャドウセルが大量降格
- 複数通貨ペアに跨っており、scalp系全体でR2判定条件が現在の市場ボラティリティに追いついていない可能性
- ログで `ema_trend_scalp x GBP_USD/USD_JPY` が数秒おきに繰り返されており、セル復帰ロジックが機能していない

**③ `daytrade_eur:hedge_block=488` / `daytrade_eurjpy:order_bar_dedup=391`**
- EUR系daytradeでヘッジブロックが集中 → EUR関連で一方向ポジションが積み上がったまま解消されず、新規注文をヘッジブロックが塞ぎ続けている
- order_bar_dedupがbar_ts=15:30固定で連続ブロック → 同一バーのシグナルが新バー生成後もクリアされず残留している（バーリセット失敗の可能性）

**④ `SCORE_GATE` + `SENTINEL_BLOCK_DIAG` の連続ログ**
- dt_sr_channel_reversal GBP_JPY SellがSCORE_GATE(score=0.13)で弾かれつつ、同一シグナルが2秒おきに再評価されている
- ループ抑制が効いていない、またはシグナル生成側が冪等でない

---

### 確認が必要な事項（確定前に要検証）

| 確認項目 | 確認先 |
|---|---|
| direction_filterの判定基準（トレンド方向ソース）が最新の市場レジームを反映しているか | rnb_usdjpy戦略設定・レジーム判定ロジック |
| bar_ts=15:30のdedupエントリがバー確定後にクリアされているか | order_bar_dedup TTL/リセット処理 |
| R2シャドウセルの復帰条件（昇格閾値）が現在のボラ環境で到達可能な水準か | R2_SHADOW_DEMOTE昇格ロジック設

## Alert: 21:09 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=1902, scalp:r2_shadow_demoted_cell=1019, scalp_eur:r2_shadow_demoted_cell=855, daytrade_eurjpy:hedge_block=684, daytrade:hedge_block=564

### 診断
## 原因分析

### 推測（確認前）

**1. `rnb_usdjpy:direction_filter=1902`（最大異常）**
- RNBストラテジーのUSD/JPYで方向フィルターが長時間・大量に発動している
- トレンドフィルター用の参照データ（上位足・方向判定指標）が固着または極端な値を返し続けている可能性
- あるいはフィルターロジックのバグにより、一方向シグナルをすべて棄却するループに入っている可能性

**2. `scalp:r2_shadow_demoted_cell=1019` / `scalp_eur:r2_shadow_demoted_cell=855`**
- ログから `sr_channel_reversal x USD_JPY`・`bb_rsi_reversion x USD_JPY` が連続ブロックされている
- R2シャドウセルの降格状態が解除されず、USD/JPY絡みのセルがdemoted状態に固着している疑い
- 降格解除条件（価格条件・時間経過・ポジション解消など）が満たされていないか、解除ロジックが機能していない可能性

**3. `daytrade_eurjpy:hedge_block=684` / `daytrade:hedge_block=564`**
- ヘッジポジションが長期間オープンのまま、または既存ポジション検出ロジックが誤検知し続けている
- ヘッジ解除トリガーが発火していない可能性

**4. `ORDER_BAR_DEDUP` の連続ブロック**
- `ny_close_reversal EUR_USD BUY: bar_ts=2026-08-31T21:00:00` が同一bar_tsで繰り返しブロック
- 21:00バーのシグナルが次のバー確定後もリセットされず残存している（bar_ts更新不良）

---

### 確認事項（要調査）

| # | 確認内容 | 対象 |
|---|----------|------|
| 1 | `rnb_usdjpy` の方向フィルター参照データ（上位足トレンド指標）の現在値と更新タイムスタンプ | データフィード・指標キャッシュ |
| 2 | R2シャドウセルの降格フラグ保持期間・解除条件の最終評価時刻 | セル状態DB/メモリ |
| 3 | USD/JPY関連ポジションの実際のオープン状況（ブローカーAPI照合） | ポジション管理 |
| 4 | ヘッジブロックの解除トリガーログが出ているか | ヘッジロジックログ |
| 5 | `bar_ts=21:00:
