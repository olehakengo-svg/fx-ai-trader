# Trade Monitor Alerts: 2026-09-11

## Alert: 06:43 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=407

### 診断
## 原因特定

### 推測（高確度）

- **`rnb_usdjpy:no_signal=407`の本質的原因は、`rnb_usdjpy`モードがシグナル生成条件を満たせない状態に長時間陥っていること**
- `no_signal`ブロックが407回というのは他のブロック種別と比較して突出しており、単発フィルタではなく**シグナル源泉そのものが枯渇**している状態を示す
- ログに`rnb_usdjpy`固有のエラーは一切出ていない → シグナル計算は正常に動いているが**結果が常にno_signal判定**になっている
- USD_JPYペアに対しては`R2_SHADOW_DEMOTE`（`ema_trend_scalp`、`bb_rsi_reversion`）が継続的に発生しており、**USD_JPY関連の相場状態が`rnb`モードのシグナル要件と不整合**を起こしている可能性が高い

### 推測（中確度）

- `trade_row`の鮮度が2.7時間と古い（ログに記載あり）ことと連動して、`rnb_usdjpy`が実質的にトレード停止状態になっている可能性
- `rnb`モードのシグナルパラメータ（閾値・レンジ幅・ボラティリティ条件など）が現在の相場環境（低ボラ・方向感なし等）と合っていない

---

## 確認事項（要調査）

- [ ] `rnb_usdjpy`モードのシグナルロジックにおいて、`no_signal`を返す判定条件（例：レンジ幅、RSI帯、スプレッド閾値）と現在のUSD_JPYの市況を照合する
- [ ] 407カウントがいつ頃から急増したか時系列を確認（デプロイ・パラメータ変更・相場急変のタイミングと一致するか）
- [ ] `rnb_usdjpy`が参照するインジケータ計算（入力データ）に欠損・固着がないか確認
- [ ] `trade_row`の鮮度2.7時間が`rnb_usdjpy`の非発動と因果関係にあるか確認（依存関係の有無）
- [ ] 他の`rnb_*`モードが存在する場合、同様の`no_signal`急増が起きているか比較する

---

## 対処方針

- **即時対応（監視継続）**
  - エンジン自体は正常稼働（`emergency_killed: false`、tick age 0.4秒）のため、緊急停止は不要
  - ただし

## Alert: 12:23 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=1001, scalp:r2_shadow_demoted_cell=554, daytrade_eur:hedge_block=390, daytrade_gbpusd:order_bar_dedup=304, scalp_eur:r2_shadow_demoted_cell=244

### 診断
## 原因分析

### 【推測】各異常の推定原因

**① rnb_usdjpy:no_signal=1001（最重大）**
- RNB戦略のシグナル生成ロジック自体が止まっている可能性が高い
- レンジ判定パラメータが現在の相場環境（トレンド相場？）に合致せず、恒常的にレンジ外判定されている
- フィルタ条件（ATR、ボラティリティ閾値等）が厳しすぎて全ブロック状態に陥っている

**② scalp/scalp_eur:r2_shadow_demoted_cell（554/244）**
- ログで `ema_trend_scalp x USD_JPY` が繰り返しブロックされており、特定セルが恒常的にshadow降格されている
- shadow→liveへの昇格条件を満たせない状態が続いている（R2スコアの閾値未達）
- USDJPYのスキャルp系で`daytrade_1h/USD_JPY`もslot bypassが頻発しており、同通貨ペアで構造的な問題が集中

**③ daytrade_eur:hedge_block=390**
- EURペアのヘッジ条件が過剰に発動している
- EUR絡みの複数ポジションが同時保有されており、ヘッジ判定ロジックが連鎖的に発火している可能性
- 相場の方向性が不安定でヘッジフラグが解除されない状態

**④ daytrade_gbpusd:order_bar_dedup=304**
- ログで `bar_ts=2026-09-11T12:15:00` が複数回ブロックされており、同一バーへの注文が繰り返し生成されている
- GBPUSDでシグナルは出るが同バー内で重複判定が続いている（バー更新が遅延、または戦略側でバーtsが更新されていない）
- `engine_tick_stalest_mode: daytrade_1h_nzdjpy`（44秒）とは別件だが、バー管理の問題が疑われる

**⑤ HOLDログの大量出力**
- ポジション `a2cf4908`（SELL）および `1a0f8fe6`（BUY profit=0.07000）が長時間保持されており、含み益保護が反転シグナルを継続的に無効化
- BUY側のprofit=0.07000は異常に大きく、ポジションが意図せず長期化している可能性

---

### 【確認事項】（推測ではなく実データ要確認）

- [ ] `rnb_usdjpy` の最終シグナル生成時刻

## Alert: 17:22 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=1516, scalp:r2_shadow_demoted_cell=775, daytrade_eur:hedge_block=592, daytrade:order_bar_dedup=459, daytrade_gbpusd:order_bar_dedup=413

### 診断
## 原因分析

### 推測（ログ・カウントから高確率で判断できること）

**① rnb_usdjpy:no_signal=1516（最大異常）**
- RnBストラテジーがUSD/JPYでシグナル生成条件を満たせない状態が長期継続している
- 同時刻のR2_SHADOW_DEMOTEログ（ema_trend_scalp, vol_momentum_scalp, bb_rsi_reversionがすべてUSD/JPY）と相関しており、**USD/JPYのレンジ相場またはボラティリティ状態がRnBの想定外になっている**可能性が高い

**② scalp/scalp_eur/scalp_5m系 r2_shadow_demoted_cell 多発（775, 388, 300, 210, 142）**
- shadow-tracking cellが複数のスキャルpセルで連続demoteされている
- ログで`ema_trend_scalp`, `vol_momentum_scalp`, `bb_rsi_reversion`がすべてUSD/JPY軸でブロックされており、**R2モデルのシャドウセルがUSD/JPY方向のトレンドを誤学習または現在の相場構造と乖離している**と推測

**③ daytrade_eur:hedge_block=592（eur系で突出）**
- EURペア全般でヘッジポジションが解消されずブロックが蓄積している
- `daytrade_gbpusd:hedge_block=363`と合わせると、**現在オープン中のポジション（SELL側）が含み益を保持したまま継続**しており、含み益保護ロジックがSIGNAL_REVERSEを全ブロックしていることと整合する

**④ daytrade/daytrade_gbpusd:order_bar_dedup=459, 413**
- `bar_ts=2026-09-11T17:15:00`が繰り返し出現しており、**同一バー内でstreak_reversalが何度も発火している**
- バー更新が正常に進んでいない、またはシグナルが同バー内でループしている

---

### 確認事項（断定できないため要検証）

| 確認項目 | 理由 |
|---|---|
| USD/JPYの現在の価格レンジ・ボラティリティ指標 | no_signalとR2 demoteの根本が相場起因かモデル起因か判別するため |
| R2シャドウセルの最終学習タイムスタンプ | 再学習が停止・遅延していないか |
| `daytrade_1h_audjpy`のティック遅延54.9秒の原因 | `engine_tick_stalest`が同モードに集中しており、データフ
