# Trade Monitor Alerts: 2026-08-05

## Alert: 01:18 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=222

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=222` の異常値**は、RNB（レンジブレイク）戦略のUSDJPYにおいて、`direction_filter`ブロックが突出して多い（次点の`daytrade_eurjpy:hedge_block=114`の約2倍）
- 現在のUSDJPYが**明確な方向性を持てないレンジ相場**に入っており、RNB戦略の方向判定フィルターが両方向のシグナルを連続で棄却し続けている可能性が高い
- ログの`R2_SHADOW_DEMOTE`が直近1分間で高頻度発生しており、**相場全体がボラティリティ低下または方向感喪失の状態**にある状況と一致する
- `main_loop_restarts=1`があるが、現時点で`main_loop_alive=true`のため重大障害には至っていない

### 推測（中確度）

- direction_filterのカウントが**リセットされていない**（起動時からの累積値が単純に大きい）可能性
- RNBのシグナル生成ロジックが**過剰に頻繁に呼ばれている**（ループ間隔や重複呼び出しのバグ）

---

### 確認事項（要調査）

1. **`rnb_usdjpy`のblock_countリセットタイミング**を確認 — 他のカウンターと同一起点でリセットされているか、またはリセット漏れがないか
2. **direction_filterの判定ロジック**を確認 — 閾値（方向スコア条件）が現相場環境に対して厳しすぎる設定になっていないか
3. **USDJPYの直近レンジ幅・ATR**を確認 — 相場起因で正常にブロックされているのか、ロジック起因の誤棄却なのかを切り分ける
4. **RNBのシグナル生成頻度**をログで確認 — 短時間に何回direction_filterが評価されているか（異常な呼び出し頻度がないか）
5. **`main_loop_restarts=1`の発生時刻と原因**を確認 — 再起動後にカウンターが引き継がれていないか

---

## 対処方針

- **即時対応（監視強化）**
  - `rnb_usdjpy`の実注文・shadow注文が出ていないことを確認し、意図せぬ機会損失が生じていないか評価する
  - `main_loop_restarts`が増加していないかを継続監視する

- **短期対応（調査・判断

## Alert: 11:49 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=293, daytrade_eur:hedge_block=254, daytrade_eurgbp:hedge_block=218

### 診断
## 原因分析

### 推測（ログ・数値から高確度で推定）

**① `rnb_usdjpy:direction_filter=293`（最大異常値）**
- RNBモードのUSDJPYにおいて、方向フィルター（トレンド方向と逆のシグナルを弾くロジック）が異常に多く発火している
- 現在のUSDJPYが強いトレンド相場にあり、RNBロジック（レンジ系と推定）がトレンドと相性の悪い方向でシグナルを連発している可能性が高い
- または、方向フィルターの基準となるMAやトレンド判定パラメータが現在の相場レジームにミスマッチしている

**② `daytrade_eur:hedge_block=254` / `daytrade_eurgbp:hedge_block=218`**
- EURおよびEURGBPで既存ポジションのヘッジ判定が大量発動している
- 同一通貨ペアで逆方向シグナルが繰り返し生成されており、ヘッジブロックで抑制され続けている状態
- EUR絡みの複数モード（eurjpy=191, scalp_5m_eur=72, scalp_5m_gbp=95）でも同傾向 → EUR相場全体がトレードロジックの想定外の動きをしている（高ボラ・急反転等）

**③ `R2_SHADOW_DEMOTE` の連発（sr_channel_reversal x EUR_USD, ema_trend_scalp x USD_CHF）**
- 特定セルが継続的にシャドウ降格されており、これらのストラテジーが現在の相場で繰り返し負け判定を受けている
- `donchian_momentum_breakout EUR_USD BUY` がbar_tsを変えずに何度もDEDUPされているのは、同一足内でシグナルが再生成され続けるバグまたは設計上の問題の可能性あり

---

### 確認事項（断定前に要検証）

- [ ] **USDJPYの現在レジーム**：RNBが想定するレンジ幅に対して実際の変動幅が逸脱していないか（ATR比較）
- [ ] **EURの未決済ポジション有無**：hedge_blockが多い原因がポジション滞留なのかシグナル連発なのかを切り分ける
- [ ] **direction_filterの閾値・参照TF**：293回という数値がシステム稼働時間と比較して異常頻度かどうか（起動からの経過時間確認）
- [ ] **`main_loop_restarts=1`の原因**：再起動前後でblock_
