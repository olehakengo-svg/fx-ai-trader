# Trade Monitor Alerts: 2026-05-11

## Alert: 01:51 UTC

### 検出された問題
- 💰 **含み益+92.8pip** (9ポジション, LIVE=1 Shadow=8) | Top: rsk_gbpjpy_reversion×GBP_JPY +21.3pip | sr_break_retest×GBP_JPY +20.5pip | rsk_gbpjpy_reversion×GBP_JPY +20.1pip
- ⚠️ **Shadow含み益+96.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=1729, daytrade_gbpusd:hedge_block=1376, daytrade_eurjpy:hedge_block=710, daytrade_eur:hedge_block=612, daytrade:hedge_block=588

### 診断
## 原因分析と対処方針

---

### 【推測】原因の特定

**1. Shadow含み益+96.9pipがOANDAに未送信**
- Shadow=8に対しLive=1という偏りから、プロモーション条件（PAIR_PROMOTED閾値）を多くのシグナルが満たせていないと推測
- GBP_JPYの含み益上位3件がすべてShadowに滞留しており、プロモーションロジックが機能していないかパラメータが保守的すぎる可能性

**2. `rnb_usdjpy:direction_filter=1729`の異常**
- 全block_counts中で突出して高い値（次点の`daytrade_gbpusd:hedge_block=1376`の約1.25倍）
- MTFログで `USD_JPY signal=SELL mtf=uncertain d1=1 h4=-1` が繰り返されており、D1とH4が逆向きのまま固着 → direction_filterが延々とブロックし続けている状態と推測
- main_loop_restarts=1があり、再起動後もフィルタカウンタがリセットされていない可能性

**3. `daytrade_gbpusd:hedge_block=1376` / `daytrade_eurjpy:hedge_block=710`**
- GBP_JPYでBUYポジションを大量保持中のため、GBP建てペアにヘッジブロックが連鎖している構造と推測
- 「DT含み益保護 → SIGNAL_REVERSE無効化」が多数ログ出力されており、ポジション保有中はSELLシグナルが全滅する状態

**4. `daytrade_eurjpy` Slot bypass 連続出力**
- `live=1/1 shadow=0/2` のまま繰り返し出力 → Slotが埋まらずShadowループが空回りしている

**5. `scalp:r2_shadow_demoted_cell=503` / `bb_rsi_reversion×GBP_USD`の連続demote**
- GBP_USD方向性不確実な状況で同セルが繰り返し降格 → Shadowリソースを消費しながら実益ゼロ

---

### 【確認事項】（推測ではなく事実確認が必要なもの）

- [ ] PAIR_PROMOTEDの昇格条件（pip閾値・保持時間・連続確認回数）の現在設定値
- [ ] `rnb_usdjpy` のdirection_filterカウンタがシステム再起動後にリセットされているか
- [ ] `main_loop_restarts=1` の再起動タイミングとその直前のエラーログ
- [ ] `daytrade_eurjpy` Slot bypassが同一

## Alert: 08:53 UTC

### 検出された問題
- 💰 **含み益+31.7pip** (10ポジション, LIVE=0 Shadow=10) | Top: sr_anti_hunt_bounce×EUR_USD +5.2pip | sr_anti_hunt_bounce×EUR_USD +5.2pip | sr_anti_hunt_bounce×EUR_USD +4.5pip
- ⚠️ **Shadow含み益+35.6pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】Shadow→LIVE昇格がブロックされている主因

**1. hedge_blockによる大量ブロック（最有力）**
- `daytrade_eur:hedge_block=9`、`daytrade_eurjpy:hedge_block=9`、`scalp_5m_eur:hedge_block=9` など複数モードで上限値に張り付いている
- EUR_USDのBUYポジションが多数HOLDされている状態でSELLシグナルが出ているか、逆方向のオープンポジションがhedge_block判定を誘発していると推測
- shadow=10/live=0 という完全な昇格ゼロがこれで説明できる

**2. r2_shadow_demoted_cellによるshadow降格**
- `scalp_5m:r2_shadow_demoted_cell=7`、`scalp:r2_shadow_demoted_cell=1`
- shadow昇格候補セルが降格させられており、昇格候補のプールが枯渇している可能性

**3. spread_guard / spread_gate による昇格阻止**
- `scalp_eur:spread_guard=1`、`scalp_eur:spread_gate=3`、`scalp:spread_guard=2`
- EUR_USD系で現在スプレッドが閾値を超過している可能性

**4. DT含み益保護ロジックの過剰作動**
- 複数ポジション（6件以上）でSIGNAL_REVERSE無効化が毎サイクル発火している
- HOLDポジションが実質ロック状態になり、新規昇格の余地を圧迫していると推測

---

### 【確認事項】（推測と分離）

- [ ] **hedge_block=9の上限値は設計値か？** → 最大値が9なら飽和状態、設定変更が必要か確認
- [ ] **現在のLIVEポジション方向とshadowシグナルの方向が逆転していないか** → hedge_blockの発動条件を確認
- [ ] **ecaca99d（profit=0.026）のポジションはいつ建てられたか** → 異常に大きな含み益で他ロジックの基準値がずれている可能性
- [ ] **PAIR_PROMOTEDリストにEUR_USDが含まれているか** → 警告文の通り未追加なら昇格ルートが存在しない
- [ ] **R2_SHADOW_DEMOTEのdemote条件（engulfing_bb x USD_JPY）が正常トリガーか誤発火か**
- [ ] **spread_guardの閾値と現在のEUR_USDスプレッド実測値**

---

## 対処方針

**即時対応**
- `PAIR
