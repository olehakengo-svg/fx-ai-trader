# Trade Monitor Alerts: 2026-06-11

## Alert: 02:13 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=300, daytrade:hedge_block=209

### 診断
## 原因分析

### 【推測】主因

**1. `rnb_usdjpy:direction_filter=300` — 異常高値**
- rnb_usdjpy戦略のdirection_filterが300回ブロックは、**フィルタ判定ロジックが毎ループ連続ヒットしている**状態を示唆
- ログの`fib_reversal SELL`が短周期（12s〜63s間隔）で繰り返し発火しており、**シグナル生成ループが止まらない**可能性が高い
- WIDE_RANGE + range_tight + vol=squeezeという矛盾した相場認識が同時成立しており、**レジーム判定が不安定**になっている疑い

**2. `daytrade:hedge_block=209` — 異常高値**
- ヘッジブロックがこれだけ積み上がるのは、**既存ポジションと逆方向シグナルが継続的に発生**している、またはヘッジブロック解除条件が機能していない可能性
- `fib_reversal SELL`が連続発火しており、既存ロングポジション保有中であれば毎サイクルhegde_blockに引っかかる構造と整合

**3. `spike bypass: 16049.8pip/60s`**
- スパイク判定値が16000pip超は**明らかに異常値**。価格データ取得エラーまたは前回価格キャッシュが不正な値を保持している可能性が高い

---

### 【確認事項】（推測と分離）

- [ ] rnb_usdjpyのdirection_filterカウンタは**累積値か単位時間あたりの値か**（リセットタイミングの仕様確認）
- [ ] USD/JPY現在価格フィード（160.475）が正常かどうか（ブローカーAPI疎通・レート取得タイムスタンプ確認）
- [ ] spike判定に使う「前回価格」のキャッシュ値が何になっているか（16049.8pipの起点価格を確認）
- [ ] ヘッジポジションが実際に建っているか、オープンポジション一覧を確認
- [ ] `main_loop_restarts=1` のリスタート発生時刻と、カウント急増のタイミングが一致するか
- [ ] WIDE_RANGEとrange_tightが同時成立するレジーム判定の条件定義（矛盾の意図的設計か、バグか）

---

## 対処方針

### 即時対応
- **spike bypassの前回価格キャッシュを強制リセット**し、異常値（16049pip）の源泉を断つ
- **rnb_us

## Alert: 08:25 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=445, daytrade_eurjpy:hedge_block=279, daytrade_gbpusd:hedge_block=265, daytrade_eurgbp:hedge_block=240

### 診断
## 原因分析

### 【推測】各異常の発生原因

**1. `rnb_usdjpy:direction_filter=445`（最重大）**
- USDJPYが一方向に強くトレンドしており、direction_filterが長時間同一方向シグナルを拒否し続けている
- フィルター閾値に対してレート乖離が継続的に拡大しているか、フィルター条件のリセットロジックが機能していない可能性
- カウンターがリセットされずに累積している（バグ由来）可能性もある

**2. `daytrade_eurjpy:hedge_block=279` / `daytrade_gbpusd:hedge_block=265` / `daytrade_eurgbp:hedge_block=240`**
- 既存ポジション（be8a3f80、e8b0f443）が長時間HOLDされており、ヘッジブロックが解除されないまま新規シグナルを大量に抑制している
- ログより両ポジションとも含み益保護でSIGNAL_REVERSEが無効化され続けており、ポジションクローズのトリガーが発動していない
- GBP絡み通貨ペア（EURGBP・GBPUSD）で`gbp_asia_flash_crash`ブロックも同時に高水準（109、16）→ GBP系全体がブロック過多状態

**3. `same_price_0pip`によるsqueeze_release_momentumの繰り返しブロック**
- 価格が膠着しており、エントリー価格と現在価格が同値またはゼロpip圏に滞留
- `recent_emit bypass`でシャドーに格下げされているが、same_price_0pipで毎回ブロックされており、事実上シグナルが出続けているだけで何も実行されていない

---

### 【確認事項】（推測ではなく要確認）

- [ ] `be8a3f80` / `e8b0f443` の**ポジション開設時刻・現在の含み益額・SLライン**を確認 → 正常にクローズ条件が設定されているか
- [ ] `rnb_usdjpy:direction_filter` のカウンターが**前回リセットされた時刻**をログで確認 → 累積バグかトレンド継続かを切り分け
- [ ] `daytrade_eurgbp:gbp_asia_flash_crash=109` の**発動トリガー時刻**を確認 → フラッシュクラッシュ判定が古いイベントを参照したままになっていないか
- [ ] `main_loop_restarts=1` が**いつ発生したか** → 再起動時にブロックカウンター
