# Trade Monitor Alerts: 2026-04-28

## Alert: 01:30 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=276

### 診断
## 原因分析

### 【推測】rnb_usdjpy:direction_filter=276 の異常値について

**主因：direction_filterが恒常的にブロックし続けている**

- `rnb_usdjpy` のdirection_filterカウントが276と、他のブロックカウント（最大154）の**約1.8倍以上**に突出しており、明らかに異常
- direction_filterは「トレンド方向の一致確認」フィルタであるため、**USDJPYのレンジ/方向感なしの相場が継続**していることで、シグナル発生のたびに全棄却されている可能性が高い
- ログ上で `USD_JPY entry=ema_trend_scalp signal=BUY mtf=range_tight d1=0 h4=1 vol=squeeze` と**MTF整合性の欠如（d1=0）**が確認でき、上位足トレンドが不一致→direction_filterが毎回発動していると推測
- main_loop_restartsが1回あり、カウンタがリセットされずに積算されている可能性もある

---

### 【確認事項】（実際に調べるべきこと）

1. **direction_filterのカウンタリセットタイミング**：起動からの累積か、直近N分の積算か。再起動後にリセットされているかどうか
2. **USDJPYの現在のD1足トレンド状態**：d1=0が継続しているか。相場自体がレンジ化しているなら正常動作の可能性あり
3. **direction_filterのロジック閾値設定**：フィルタ条件が過剰に厳格になっていないか（例：パラメータ変更履歴の確認）
4. **rnb_usdjpyのシグナル発生頻度**：他モードと比較して異常に高頻度でシグナルが出ていないか（ループ間隔の設定ミスなど）
5. **main_loop_restarts=1との因果関係**：再起動のタイミングとカウント急増のタイミングが一致するか

---

## 対処方針

- **緊急度：中**（emergency_killed=false、main_loop_alive=true のためシステム自体は稼働中）

### 短期対応
- `rnb_usdjpy:direction_filter` のカウント増加速度をモニタリングし、**単位時間あたりのブロック率**を算出して正常範囲かどうか判定する
- ログにdirection_filterのブロック理由を明示的に出力させ、**どの条件で弾かれているかを特定**する
- main_loop_restartsが発生した時刻とdirection_filterカウントの推移

## Alert: 11:29 UTC

### 検出された問題
- 💰 **含み益+58.0pip** (11ポジション, LIVE=0 Shadow=11) | Top: sr_fib_confluence×EUR_JPY +24.6pip | sr_fib_confluence×EUR_JPY +19.5pip | session_time_bias×GBP_USD +7.9pip
- ⚠️ **Shadow含み益+63.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因特定

### 【推測】

**主因：Shadow含み益+63.9pipがLIVEに昇格されていない**
- EUR_JPYの`sr_fib_confluence`が+24.6pip/+19.5pipと高パフォーマンスを出しているが、当該ペア/ストラテジーがPAIR_PROMOTEDリストに未登録のためShadow止まり
- `fib_reversal EUR_USD`がEV=-4.489でShadowブロックされている → EV計算の履歴データが実態と乖離している可能性
- LIVE=0の状態が継続しており、全11ポジションがShadowのみ → PAIR_PROMOTEDの設定漏れか、昇格条件の閾値未達

**副因：ブロック数の偏り**
- `rnb_usdjpy:direction_filter`が174件と突出 → USD_JPYの方向フィルタが過剰に機能し、実質エントリー停止状態
- `daytrade_eurjpy:recent_emit`が63件、`sl_cluster`が39件 → EUR_JPYは頻繁にシグナルを出しているがSL密集でほぼ全ブロック
- `scalp_eur:conf<30`が48件 → EUR系スキャルプの信頼度が慢性的に低い

**`main_loop_restarts=1`**
- 軽微だが、再起動タイミングでShadow→LIVE昇格処理がリセットされた可能性を排除できない

---

### 【確認事項】

| 確認項目 | 確認方法 |
|---|---|
| EUR_JPY `sr_fib_confluence` がPAIR_PROMOTEDに含まれているか | 設定ファイル/DB直接確認 |
| PAIR_PROMOTED昇格条件（pip数・勝率・期間）の現在閾値 | 設定値と今回の実績の比較 |
| `fib_reversal EUR_USD` のEV=-4.489の算出元期間・サンプル数 | EVログ/バックテスト結果 |
| `rnb_usdjpy:direction_filter`の判定ロジックと現在の方向設定値 | フィルタ定義確認 |
| `main_loop_restarts=1`の再起動時刻とShadow状態の継続性 | ログの再起動前後を照合 |

---

## 対処方針

**即時対応（今日中）**
- EUR_JPY `sr_fib_confluence` をPAIR_PROMOTEDに手動追加することを検討（ただし本番追加前にEV・DDを再確認）
- `fib_reversal EUR_USD` のEV=-4

## Alert: 13:23 UTC

### 検出された問題
- 💰 **含み益+62.3pip** (16ポジション, LIVE=0 Shadow=16) | Top: sr_fib_confluence×GBP_JPY +16.3pip | sr_break_retest×GBP_JPY +11.1pip | trend_rebound×GBP_USD +8.2pip
- ⚠️ **Shadow含み益+69.9pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 推測（ログ・ステータスから高確率で推定）

1. **Shadow専有問題（最重要）**
   - 全16ポジションがShadow、LIVE=0という状態は「対象通貨ペアがPAIR_PROMOTEDリストに未登録」が主因と推測
   - GBP/JPY・GBP/USD・USD/JPYで優良シグナルが出ているが、OANDA送信経路に乗っていない

2. **max_openブロックの多発**
   - `daytrade:max_open=17`、`scalp:max_open=42` 等、ほぼ全モードでmax_openブロックが大量累積
   - Shadowポジションもmax_openカウントに加算されている可能性があり、LIVEエントリーの枠を圧迫していると推測

3. **DT含み益保護によるロック**
   - `SIGNAL_REVERSE無効化`が毎秒連続発火しており、既存ポジションが利確・損切りトリガーに到達せず長時間ホールドされ続けている
   - threshold値が一部ポジションで異常に低い（0.00022〜0.00030）か、profit値が閾値をわずかに超え続けている

4. **recent_emitブロックの連鎖**
   - `daytrade_eur:recent_emit=58`、`daytrade_gbpusd:recent_emit=41` など、短時間に同一シグナルが連続発火しemitクールダウンに引っかかり続けていると推測

5. **SCORE_GATE Sentinel bypass**
   - EUR_GBPで方向不一致のまま`is_shadow will be enforced`されており、Shadow汚染の一因となっている可能性

---

### 確認事項（要実機確認）

- [ ] `PAIR_PROMOTED`設定ファイルにGBP/JPY・GBP/USD・USD/JPYが含まれているか
- [ ] Shadowポジションがmax_openカウンタに含まれているかどうか（LIVEとShadowのカウント分離有無）
- [ ] `main_loop_restarts=1` の再起動タイミングとShadow開始タイミングの一致確認（再起動後にLIVEフラグが初期化された可能性）
- [ ] DT含み益保護thresholdの設定値が意図通りか（特に`a6b90544`の0.00022は他より低い）
- [ ] `rnb_usdjpy:direction_filter=177`（突出して多い）— フィルタ条件が恒常的に反対方向を弾いていないか

---

## 対処方針

### 即時対応

- **PAIR_PROM

## Alert: 16:08 UTC

### 検出された問題
- 💰 **含み益+99.4pip** (13ポジション, LIVE=0 Shadow=13) | Top: dual_sr_bounce×EUR_JPY +30.2pip | sr_fib_confluence×GBP_USD +20.9pip | sr_fib_confluence×GBP_JPY +16.7pip
- ⚠️ **Shadow含み益+111.3pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=242

### 診断
## 原因分析

### 推測（ログ・数値から高確率で推定）

**① Shadow→LIVE昇格が機能していない（最重要）**
- 13ポジション全てがShadow止まり、LIVE=0
- +111.3pipの機会損失が発生中
- PAIR_PROMOTEDリストにEUR_JPY・GBP_USD・GBP_JPYが未登録、もしくは昇格条件の閾値未到達の可能性が高い

**② rnb_usdjpy:direction_filter=242 の異常値**
- 他のblock_countsが10〜120台の中、242は明らかな外れ値
- `direction_filter`が恒常的にBLOCKし続けており、rnb_usdjpyストラテジーが実質的に機能停止している可能性が高い
- USDJPYの相場方向（BUY偏重）とフィルター設定が長期間ミスマッチしている疑い

**③ SIGNAL_REVERSE無効化の多発は正常動作の範囲内**
- 含み益保護ロジックが意図通り動作している
- ただしポジションが多数・長期滞留しているため頻度が高くなっている（副作用として問題化する前に要注視）

---

### 確認事項（断定不可・要調査）

| # | 確認項目 | 確認方法 |
|---|----------|----------|
| 1 | PAIR_PROMOTEDにEUR_JPY・GBP_USD・GBP_JPYが含まれているか | 設定ファイル・環境変数を直接確認 |
| 2 | Shadow昇格条件（pip閾値・勝率条件等）の現在値 | 昇格ロジックのパラメータ確認 |
| 3 | rnb_usdjpyのdirection_filterが参照しているトレンド判定ソース | MTFバイアスの現在値・更新タイミング確認 |
| 4 | direction_filterのカウントがリセットされているか | システム再起動（main_loop_restarts=1）後も継続増加しているか確認 |
| 5 | 13ポジションの滞留時間・TP/SL設定 | 長期放置によるロールオーバーコスト発生有無 |

---

## 対処方針

**即時対応（優先度：高）**

- EUR_JPY・GBP_USD・GBP_JPYを `PAIR_PROMOTED` に追加し、Shadow→LIVE昇格を有効化
- 昇格前に現在のShadow損益・勝率を手動確認し、統計的に有意であることを検証してから実施

**短期対応
