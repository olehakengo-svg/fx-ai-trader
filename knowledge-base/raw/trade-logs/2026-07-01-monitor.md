# Trade Monitor Alerts: 2026-07-01

## Alert: 02:05 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=281

### 診断
## 原因分析

### 【推測】原因

- **`rnb_usdjpy:direction_filter=281`** は他のブロックカウント（最大でも`scalp:r2_shadow_demoted_cell=98`）と比べて**約3〜10倍の異常値**であり、単純な市場環境によるブロックではなくロジック上の問題が疑われる
- `direction_filter`は「方向性フィルタが反シグナルを検出して弾く」条件と推定されるが、**281回という頻度はフィルタ条件が恒常的にトリガーされ続けている状態**を示す
- `rnb_usdjpy`モードが方向性判定に使用しているインジケータ（トレンド方向・バイアス参照元）が、**一方向に固着またはフリップを繰り返している**可能性が高い
- 現時点（アジア時間早朝）のUSDJPY（162円台）は過去の参照レンジや閾値設定と**乖離が大きく、閾値ハードコードが原因**の可能性もある
- `main_loop_restarts=1`が記録されており、再起動後に状態がリセットされず**フィルタ判定の内部状態が不正なまま継続**している恐れがある

---

### 【確認事項】（実施前に要検証）

- **`direction_filter`の判定ロジックを確認**
  - 何を参照して方向性を判定しているか（MTF・D1・H4バイアス、固定閾値、外部データ等）
  - 閾値が現在の価格帯・ボラティリティ水準に対して適切か
- **フィルタが281回ブロックした時間帯の分布確認**
  - 特定時刻に集中しているか、それとも均等に発生しているか（集中ならイベント起因、均等なら恒常的なロジック不整合）
- **`main_loop_restarts=1`の前後でカウントがリセットされているか**
  - 再起動前後のカウント推移ログを確認
- **`rnb_usdjpy`モードの内部状態（bias/direction変数）の現在値を確認**
  - 固着しているか、頻繁にフリップしているかを判定
- **他のUSDJPY関連モード（`daytrade`）が正常にシグナルを出せているかの比較確認**
  - 同一通貨ペアで方向判定が乖離していないか

---

## 対処方針

- **即時対応（監視・暫定）**
  - `rnb_usdjpy`モードの`direction_filter`カウント増

## Alert: 08:00 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=459, daytrade_eurjpy:hedge_block=299, scalp_eur:r2_shadow_demoted_cell=241, scalp:r2_shadow_demoted_cell=212

### 診断
## 原因分析

### 推測（ログ・数値から高確度で示唆されるもの）

**① rnb_usdjpy:direction_filter=459（最大異常）**
- RNBストラテジーのdirection_filterが継続的にブロックし続けている
- USDJPYが長期間一方向トレンド（円安継続）にあり、RNBのレンジ前提フィルターが根本的に市場環境と乖離している可能性が高い
- 単なる一時的な相場判定ではなく、フィルター閾値またはトレンド検出ロジックそのものが現環境に適応できていないと推測

**② daytrade_eurjpy:hedge_block=299**
- EURJPYでヘッジポジションが長時間解消されず、新規エントリーを継続ブロックしている
- ヘッジが意図せずロックされている（決済トリガーが機能していない、またはポジション状態の同期ズレ）の可能性

**③ scalp_eur / scalp:r2_shadow_demoted_cell=241/212**
- `sr_channel_reversal`および`sr_fib_confluence`セルがシャドウ降格され続けており、EUR系でR2評価が構造的に低下している
- 直近のSL_HIT（WR 40.9%）と合わせると、EURUSDのS/R精度が現在の相場構造と合っていないことを示唆
- シャドウスロットが常にlive=1/1でフルのため、バイパスが多発→demoteカウントがさらに積み上がるループ状態

**④ SHADOW Slot bypass の連続（daytrade_gbpusd/GBP_USD）**
- liveスロットが1/1で飽和した状態がずっと続いており、trendline_sweepシグナルがシャドウへ逃げ続けている
- GBPUSDのliveポジションが長期間クローズされていない（含み損保有中、またはBE_LOCKで拘束中）の可能性

---

### 確認事項（実機で確認が必要なもの）

1. **USDJPYの現在レート・トレンド方向** — RNBのdirection_filterが何の方向をブロックしているか、Buy側・Sell側どちらか確認
2. **EURJPYのオープンポジション一覧** — ヘッジポジションが実際にブローカー側で残っているか、それともシステム内部の状態フラグだけが残っているか確認（ゴーストポジション疑い）
3. **GBPUSDのliveポジション状態** — BE_LOCK_Bが発動したtrendline_

## Alert: 14:00 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=396, scalp:r2_shadow_demoted_cell=205

### 診断
## 原因分析

### 推測（ログ・カウント根拠あり）

**1. `rnb_usdjpy:direction_filter=396` — 異常高カウント**
- RNB(レンジ&ブレイク)戦略がUSDJPYに対して方向フィルターで大量ブロックされている
- 上位足バイアス（D1またはH4）が強いトレンド方向を示しており、逆張りシグナルが連続発生してことごとく弾かれている状態
- シグナル発生ロジック自体がフィルター条件と恒常的に相反する設定になっている可能性が高い

**2. `scalp:r2_shadow_demoted_cell=205` / `scalp_eur:r2_shadow_demoted_cell=153`**
- R2セル（シャドウトラッキングセル）の降格が異常に多発
- `engulfing_bb x USD_JPY` が繰り返しブロックされており、BBエントリー候補がR2条件を満たせない相場環境（低ボラ・レンジ）が続いている
- ログの`vol=squeeze`が示す通り、ボラティリティ収縮中にscalpシグナルが過剰発火している

**3. EUR_JPY `dt_sr_channel_reversal` の高頻度ブロック（ログ直接確認）**
- `mtf_strong_bias(SELL_vs_BUY)` によって約20〜40秒間隔で繰り返しブロック
- `recent_emit bypass`でシャドウに落ちているが900秒クールダウンが機能していない（毎回700〜800秒で再発火）
- シャドウパス後に再評価→即ブロックのループが発生しており、無駄な処理負荷を生んでいる

---

### 確認事項（要調査）

- [ ] RNB USDJPYの `direction_filter` がリセットされるトリガー条件を確認 → カウンターがリセットされずに累積している可能性
- [ ] `recent_emit bypass` のシャドウ経路がクールダウンタイマーを正しく更新しているか（タイマーがシャドウ経路では更新されないバグの有無）
- [ ] `r2_shadow_demoted_cell` のリセット条件 → 相場環境変化（vol=squeeze解消）でクリアされるか確認
- [ ] `main_loop_restarts=1` の再起動タイミングとカウンター初期化の有無
- [ ] EUR_JPYのMTFバイアス（H4=SELL）が実際のレート方向と整合しているか、データフィード異常がないか

---

## 対処方針

**即

## Alert: 17:44 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=299, daytrade_gbpusd:hedge_block=270, daytrade_eur:hedge_block=234

### 診断
## 原因分析

### 【推測】異常block_countsの原因

**① rnb_usdjpy:direction_filter=299（最多）**
- RnB(Range & Breakout)戦略がUSDJPYで一方向にシグナルを出し続けているが、direction_filterが全件拒否している
- 現在USDJPYでBUYポジションが複数保有中（ログ：74ef8295, 115131c6, 9d206168）であり、逆張り・順張り問わずフィルタが過剰に作動している可能性
- direction_filterの閾値または参照するトレンド判定（MTF/D1/H4バイアス）がこの相場環境と乖離している

**② daytrade_gbpusd:hedge_block=270**
- GBPUSDでSELLシグナルが頻発しているが、既存ポジションとヘッジ関係と判定されブロックされ続けている
- ログより`dt_sr_channel_reversal score=0.98`でも`hedge_block`で弾かれており、高スコアシグナルが無駄に消費されている
- GBPUSDの既存ポジション方向（おそらくBUY保有中）との衝突が継続中と推測

**③ daytrade_eur:hedge_block=234**
- EUR系通貨ペア全体（EUR/USD or EUR系クロス）で同様のヘッジブロックが集中発火
- EUR_JPYでSELLエントリーが入っている（ログ確認）が、他のEUR系ポジションとの関係でブロックが連鎖している可能性

---

### 【推測と確認事項の分離】

#### 推測（確認前の仮説）
- USDJPYのBUYポジション3本保有が、RnB・daytrade系のdirection_filter／hedge_blockを連鎖的に誘発している
- `main_loop_restarts=1`による内部状態の一部リセットがblock_countをリセットせず、カウントが累積されている可能性
- hedge_block判定のペア範囲が広すぎる（例：GBPUSD買いが他のGBP系をまとめてブロック）

#### 要確認事項
- [ ] 現在の全オープンポジション一覧（ペア・方向・ポジションID）→ block理由の根拠ポジション特定
- [ ] direction_filterが参照しているMTFバイアスの現在値（D1・H4のUSDJPY方向）
- [ ] hedge_block判定ロジックの「ヘッジ対象ペア」定義範囲（GBPUSD単体か、GBP系全体か）
- [ ] block_countsのリ
