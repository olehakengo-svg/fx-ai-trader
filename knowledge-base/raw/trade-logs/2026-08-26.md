# Trade Log: 2026-08-26

**Source**: Render API (primary) — https://fx-ai-trader.onrender.com
**Generated**: wiki-daily-update scheduled task
**Note**: Deltas are vs the last wiki-daily snapshot, **2026-08-23** (N=578 / −631.2pip / DD 1218.9pip / edge −26.46% / net −78.5 / n=15). Cadence gap = 3 days — but unlike 08-23, **08-24 (Mon) and 08-25 (Tue) are ordinary trading days**. OANDA audit window pulled at `limit=500` = **2026-08-14 10:31 → 2026-08-21 18:46 UTC**, `total` = **15,756 — byte-identical to the 08-23 pull**. There is **no new-since-last-log slice**: the engine has written **zero audit rows of any kind for ~4 days**.

---

## 🔴 Headline: the realized book and the audit table are bit-for-bit frozen, but the engine is running

Every portfolio-level number in this pull is **identical to 08-23, to the last decimal** — including `shadow_count`, which normally advances +100–200/day even in a dead window. Simultaneously `/api/demo/status` reports `running: true`, `main_loop_alive: true`, `watchdog_alive: true`, **62,472 accumulated ticks**, and OANDA `status: ok` at 130 ms latency.

**The engine is alive and generating candidates. Nothing is reaching the bridge — and nothing is reaching the shadow book either.** Under a shadow-first architecture shadow *is* the estimator, so for two trading days the estimator has been blind.

| Signal of life | Value | Reading |
|---|---|---|
| `running` / `main_loop_alive` / `watchdog_alive` | true / true / true | ✅ process healthy |
| `main_loop_restarts` | 1 | no restart storm |
| Total ticks across 24 modes | **62,472** | ✅ strategies are evaluating |
| OANDA heartbeat | `ok`, NAV ¥278,123.64, latency 130 ms, 0 open trades | ✅ broker reachable |
| `emergency_killed` | false | not an emergency stop |
| Total upstream blocks | **11,172** | ✅ candidates exist, all rejected |
| New `oanda_audit` rows since 08-21 18:46 UTC | **0** | 🔴 bridge/shadow layer receiving nothing |
| `shadow_count` delta | **+0** | 🔴 estimator frozen |

This is **not** the 08-23 pattern ("weekend, correctly quiet"). 08-22/08-23 were market-closed; **08-24 and 08-25 were not.**

---

## Portfolio Summary (post-cutoff 2026-04-08〜, is_shadow=false)
| Metric | Value | vs 2026-08-23 |
|--------|-------|---------------|
| Live N | **578** | **flat — 0 closed fills** |
| W / L / BE | 251W / 292L / 35BE | flat |
| Win Rate | 43.4% | flat |
| Decided WR | 46.2% | flat |
| EV | −1.09 pip/trade | flat |
| Total PnL | **−631.2 pip** | flat |
| Wilson Lower Bound | 42.1% | flat |
| Wilson BF Lower Bound | 39.3% | flat |
| Avg R | 0.12 | flat |
| Shadow Count | **12,386** | **+0** 🔴 |
| `oanda_audit` total | **15,756** | **+0** 🔴 |

**Read**: a flat realized book is ordinary and has happened repeatedly in this log (07-16=07-21=07-23=07-24=07-27). A flat **shadow_count** has not. In every previous flat window shadow firing continued (+111, +138, +145, +1,785). This is the first snapshot where the shadow series itself stopped.

## Risk State
| Metric | Value | vs 2026-08-23 |
|--------|-------|---------------|
| Drawdown (pip basis) | 🔴🔴🔴 **1218.9 pip** (eq=−$1202.0 vs peak +$16.9) | **flat — no new high** (121.9% legacy $1000 basis) |
| Drawdown (`dd_pct` field) | **0.0976 = 9.76%** (JPY NAV ledger basis) | flat |
| JPY NAV ledger | base ¥359,109 / peak ¥359,288.47 / current ¥324,249.50 / DD ¥35,038.97 | flat |
| Defensive Mode | Active (lot = 0.2×) | unchanged |
| Ruin Probability (MC) | 0.0% ✅ | unchanged |
| Overall Edge (Kelly, risk dash) | **−26.46%** | flat |
| Kelly Fraction | 0.0 (negative edge) | unchanged |
| Risk-window WR / odds | 40.0% / 0.8385 | flat |
| DSR (overall) | 0.0 (haircut 100%, Sharpe −0.1301, n_trials 4) | flat |
| 30d attribution | gross = net **−78.5** (n=15), friction 66.4 (4.43/trade) | flat |
| 30d by instrument | AUD_JPY −122.6 (n=2) / EUR_GBP −9.8 (n=3) / USD_JPY **+53.9** (n=10) | flat |
| MC worst-case DD99 / median max DD | 654.84 (11.29% of init) / 353.21 (6.09%) | flat |

⚠️ The 30d risk window's `effective_date_from` **did** roll (2026-07-24 → **2026-07-26T20:48:03**) while `n` stayed at **15** and every derived metric stayed bit-identical. That is only possible if no trade sits in the 07-24→07-26 slice — consistent with the freeze. **The window-roll easing that flattered 08-23 has stopped, because there is nothing left to roll.**

---

## 🔴 Where the signals are dying — upstream block census

`/api/demo/block-counts`, aggregated (11,172 blocks over 62,472 ticks):

| Block reason | Count | Share | Reading |
|---|---:|---:|---|
| `r2_shadow_demoted_cell` | **4,301** | 38.5% | 🔴 **this is what freezes `shadow_count`** — the R2 demoted-cell gate rejects *before* a shadow row is written |
| `order_bar_dedup` | 2,970 | 26.6% | per-bar dedup, designed behaviour |
| `direction_filter` | **2,841** | 25.4% | 🔴 see below — `rnb_usdjpy` only |
| `gbp_asia_flash_crash` | 443 | 4.0% | GBP guard, 254 GBP_JPY + 187 GBP_USD |
| `score_gate` | 394 | 3.5% | |
| `conf<30` | 48 | 0.4% | |
| all others (17 reasons) | 175 | 1.6% | incl. 26 `unknown_type:*` = registry gaps |

### 🔴 `rnb_usdjpy` blocks exactly once per tick — ratio 1.00

| Mode | Blocks | Ticks | Ratio |
|---|---:|---:|---:|
| **`rnb_usdjpy`** | **2,841** | **2,841** | **1.00** |
| `scalp` | 1,401 | 7,036 | 0.20 |
| `daytrade` | 1,099 | 2,780 | 0.40 |
| `scalp_eur` | 916 | 6,756 | 0.14 |
| `daytrade_1h_usdchf` | 457 | 1,454 | 0.31 |

`rnb_usdjpy:direction_filter` is not *selective* — it is **unconditional**. Blocks equal ticks to the unit across the whole counter window; the mode has never once passed its direction filter. The 08-25 monitor log escalated this as "271 → 517 and climbing"; it is now **2,841**, and the ratio identity says the filter is not evaluating market state, it is rejecting every evaluation. This is a **structural** candidate, not a regime story — and it is consistent with the known `compute_rnb_signal` WAIT-path bug (`entry: 0`, recorded in index.md 2026-07-06) still poisoning that mode's inputs.

> ⚠️ **Not diagnosed in this run.** The ratio identity is an observation from the counters; per `feedback_label_empirical_audit` the root cause needs a code-level read of `direction_filter`, not a deduction from aggregates. Filed as the top follow-up.

### `r2_shadow_demoted_cell` is the shadow-book killer
4,301 blocks, concentrated in the scalp family and `ema_trend_scalp` (1,845), `fib_reversal` (855), `bb_rsi_reversion` (232), `engulfing_bb` (172). These are demoted R2 cells being correctly suppressed for *live* — but the same gate is discarding them **before shadow tracking**, which is why the estimator has no new observations. Under shadow-first this is backwards: a demoted cell is exactly the cell you want to keep observing in shadow.

---

## OANDA Audit
Pulled at **`limit=500`**, window **2026-08-14 10:31 → 2026-08-21 18:46 UTC**, `total` **15,756**.

| Bucket | Count |
|---|---:|
| `skipped` (all `shadow_tracking`) | 449 |
| `blocked` (all `agg_kelly<0`, −0.331…−0.374) | 35 |
| `sent` | 8 |
| `filled` | 8 |
| **New rows since the 08-23 log** | **0** |

The 8 `sent`/`filled` pairs are the **same 8 live fills already recorded** on 08-20 and 08-23 (`entry_type` twin-meaning: `sent` carries the strategy name, `filled` the MODE name — `reference_oanda_audit_twin_meaning`):

| # | Time (UTC) | Instrument | Strategy | OANDA id | Logged in |
|---|---|---|---|---|---|
| 1 | 08-16 23:02 | USD_JPY | `usdjpy_carry_dip_accumulator` | 677910 | 08-20 |
| 2 | 08-17 06:52 | USD_JPY | `usdjpy_carry_dip_accumulator` | 677917 | 08-20 |
| 3 | 08-19 01:05 | USD_JPY | `usdjpy_carry_dip_accumulator` | 677924 | 08-20 |
| 4 | 08-20 07:03 | USD_JPY | `usdjpy_carry_dip_accumulator` | 677931 | 08-23 |
| 5 | 08-20 07:41 | EUR_GBP | `price_shock_rev_eur_gbp_h1_long` | 681143 | 08-23 |
| 6 | 08-20 10:19 | USD_JPY | `usdjpy_carry_dip_accumulator` | 681149 | 08-23 |
| 7 | 08-20 12:59 | EUR_GBP | `price_shock_rev_eur_gbp_h1_long` | 700421 | 08-23 |
| 8 | 08-21 11:39 | EUR_GBP | `price_shock_rev_eur_gbp_h1_long` | 709529 | 08-23 |

**0 new live fills. 0 false-sent** (every `sent` has a paired `filled` with a real trade id — the 07-02 accept/reject contract still holds).

⚠️ `limit=30` would have been *doubly* misleading this run: not only does it cover ~4 hours, the entire table is 4 days stale, so it would have shown 30 rows from 08-21 and given no signal that the series had stopped. `feedback_audit_limit30_hides_live_fills` — 4th consecutive confirmation.

---

## 🔴 New finding: the JPY NAV ledger has drifted ¥46,126 from the broker

| Source | Figure |
|---|---|
| `/api/demo/status` → `oanda.heartbeat.nav` (**live broker**) | **¥278,123.64** |
| `/api/risk/dashboard` → `dd_status.jpy_ledger.eq_current_jpy` | **¥324,249.50** |
| **Gap** | **¥46,125.86** |

Cross-check against `/api/oanda/equity`: cumulative realized = **−¥80,686**, and ¥359,109 (ledger base) − ¥80,686 = ¥278,423 ≈ the broker NAV (residual ¥299). **The broker's own curve reconciles with its NAV; the risk-dashboard ledger does not.**

Reading the implementation (`modules/demo_trader.py` L1058-1078): the ledger is not read from the broker. It is a synthetic incremental ledger, anchored once on **2026-07-28** at `OANDA_EQ_JPY_CURRENT_INIT = 326,472.58` ("D-a 実測", `oanda_trades.realized_pl` N=339) and thereafter accumulating `pnl_pips × pip_value_jpy` per close for broker-filled rows. The equity curve implies the account was at ≈¥279,017 on 07-29, so **the anchor itself was ~¥47k high**, and the ledger has faithfully carried that offset forward for a month.

**Consequence — sizing: none right now.** `DD_LOT_TIERS` (`modules/risk_analytics.py` L382) tops out at `DD ≥ 8% → 0.20×`. The ledger reads 9.76% and the broker-measured drawdown from the same peak (¥359,288.47 → ¥278,123.64 = **¥81,164.83 = 22.6% of base**) also exceeds 8%, so both map to the **same 0.20× multiplier**. The tier is saturated; position sizing is not being mis-set.

**Consequence — reporting: material.** The headline drawdown on the risk dashboard understates the broker-measured drawdown by **~12.8pp** (9.76% vs 22.6%), and the code comment declares this ledger the **SSOT for the lot multiplier**. It is correct today only by saturation.

> ⚠️ Populations may legitimately differ (the D-a anchor used N=339 `oanda_trades` rows; the equity curve has 928 closes; XAU is excluded from the ledger). This run establishes **that** the two disagree and that the gap predates 07-28; it does not establish which population is the intended one. Filed for decision, no code touched.

## ⚠️ `/api/oanda/equity` is 15 days stale
The realized-equity curve's last point is **2026-08-10 07:20 UTC** (928 points), yet **8 live fills closed between 08-14 and 08-21** and `open_trade_count` is 0. Those closes are visible in `demo/stats` and `risk/dashboard` but never entered the OANDA equity curve. Separate pipeline from the audit freeze (that one stops at 08-21, this one at 08-10), so this is a **second, independent ingestion gap**.

## ⚠️ Clock skew between runner and server
The Render heartbeat reports `last_check: 2026-08-26T00:59:54+00:00`; the runner's clock reads **2026-08-25 19:20 UTC**. Server is **~5h40m ahead**. Staleness conclusions are unaffected (~4 days either way), but timestamps sourced from the two should not be compared directly.

---

## Learning API
- **No new adjustment.** Latest is still **id=93 (2026-08-19 14:25)**, unchanged for a 3rd consecutive snapshot.
- `current_params.entry_type_blacklist` is still **`[]`** ⇒ the **blacklist write remains a no-op**; the same `sr_channel_reversal` adjustment will keep re-firing forever. Unresolved since 06-30.
- `current_params`: `confidence_threshold` 30, `max_open_trades` 8, `max_consecutive_losses` 3, `learn_every_n` 10, `daily_loss_limit_pips` / `max_drawdown_pips` = −99999 (disabled).
- By mode: **daytrade EV −2.60** (n=94; every confidence band and every regime negative), **scalp EV −0.15** (n=388; only low-conf +0.48 positive), **swing `ready=false`**.

## Strategy pages
**No update required.** 0 closed fills and 0 new audit rows ⇒ `by_type` is byte-identical to 08-23 for all 44 strategies. Best cumulative: `usdjpy_carry_dip_accumulator` +84.0 (N=9) / `orb_trap` +23.2 (N=4) / `post_news_vol` +19.0 (N=2). Worst: `price_shock_rev_aud_jpy_h1_long` −122.6 (N=2) / `session_time_bias` −67.8 (N=30) / `vwap_mean_reversion` −63.1 (N=11) / `wick_imbalance_reversion` −63.0 (N=14).

**No tier change executed** — no new evidence arrived on which to promote or demote anything.

---

## Lint
1. **Numeric consistency** — index.md current-state lines (Defensive mode / Ruin / Aggregate Kelly / Portfolio warnings / last-updated / Session History top) reconciled to this pull (N=578 / −631.2 / DD 1218.9p / edge −26.46% / **0 fills / 0 blocks / 0 new audit rows**) ✅. Historical rows left at their as-of values.
2. `tier_integrity_check --check` → **PASS** (exit 0). 2 warnings, both pre-existing: `hull_donchian_fade` QUICK_HARVEST_EXEMPT not in ELITE/PAIR_PROMOTED; no strategy file for `ob_retest`. ⚠️ Its 14 "legacy dead inline, no production firing in 30+ days" INFO lines include `ob_retest` and `ihs_neckbreak`, both of which have live block counters in *today's* pull (`unknown_type:ob_retest` 8, `unknown_type:ihs_neckbreak` 4) — the checker reads local state, not the Render API. **Third consecutive run reporting this contradiction.**
3. `sync_kb_index --check` → exit 1, **diff is the date stamp only** (`2026-08-23` → `2026-08-26`); cleared with `--write` ✅.
4. **Broken wikilinks** — `wiki/index.md` **7** (identical set to 08-23: the 5 auto-generated portfolio-block refs `mqe-gbpusd-fix` / `vsg-jpy-reversal` / `ma-regime-switch` / `ma-trend-perfect` / `ob-retest`, plus trade-log refs `2026-04-29` / `2026-04-27`); `wiki/strategies/` **1** (`macd-rsi-pullback.md`). KB-wide **63** excluding 55 MEMORY-style `project_*`/`feedback_*`/`reference_*` refs that resolve outside the KB. **0 new from this run**; `[[2026-08-26]]` resolves to this file ✅.
5. **Staleness** — 🔴 **this is the finding, not a footnote.** API pull is current, but the underlying series are not: `oanda_audit` last row **2026-08-21 18:46 UTC (~4 days)** across **2 trading days**; `shadow_count` +0; `/api/oanda/equity` last point **2026-08-10 (15 days)**; learner last adjustment **2026-08-19**.

---

## Still-open (carried)
- 🔴 **NEW — signal supply to bridge+shadow is zero across 2 trading days** while the engine ticks and blocks normally. Top follow-up: code-level read of `rnb_usdjpy:direction_filter` (blocks == ticks == 2,841) and of why `r2_shadow_demoted_cell` (4,301) suppresses shadow tracking rather than only live.
- 🔴 **NEW — JPY NAV ledger ¥46,126 above broker NAV**, anchor-inherited; headline DD understates broker-measured DD by ~12.8pp. Sizing unaffected (tier saturated).
- 🔴 **NEW — `/api/oanda/equity` 15 days stale** despite 8 closed live fills.
- 🔴 **`usdjpy_carry_dip_accumulator` declared 150p SL vs observed 18.8p substitution** — still the highest-value open item; 9 live fills have closed on the only positive cell in the book.
- 🔴 **Price-Shock family demote decision** (AUD_JPY n=2 −122.6 / EUR_GBP n=3 −9.8, both inverting a strongly positive BT) — no new evidence this window, decision still pending.
- **Learner blacklist no-op** (`entry_type_blacklist` never written).
- **Zero-unit emissions**, system-wide (184/800 rows on 08-23) — no new data this window.
- `API_AUTH_TOKEN` watchdog gap / `sr_anti_hunt_bounce` shadow metadata corruption.
