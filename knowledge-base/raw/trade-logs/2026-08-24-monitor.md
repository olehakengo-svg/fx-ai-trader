# Trade Monitor Alerts: 2026-08-24

## Alert: 00:30 UTC

### 検出された問題
- ⚠️ **直近4時間トレード0件** （一部モードはシグナルあり — 遮断フィルター確認推奨）
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=273

### 診断
## 原因分析

### 【推測】根本原因

**`rnb_usdjpy:direction_filter=273` が主犯である可能性が高い**

- 273回という異常なブロック数は、他フィルターの数倍〜数十倍のオーダー
- RNB USD/JPY は方向性フィルター（トレンド方向の一致判定）が長時間・大量に否決し続けている状態
- 「シグナルあり・トレード0件」の構造と完全に一致する

**フィルター過剰抑制の複合発生**

- `daytrade_eur:order_bar_dedup=50`、`daytrade_eurgbp:score_gate=27`、`scalp:r2_shadow_demoted_cell=23` など、複数モードで中〜高頻度ブロックが同時発生
- 全モード横断でトレード0件なのは、単一障害ではなく**市場環境起因のフィルター連鎖**の可能性

---

### 【推測】direction_filterの具体的原因候補

- 上位足トレンド判定ロジックが「現在のレンジ相場」に対して過敏に反応している
- 方向性判定に使うMA・ADX等のパラメータが現在の値動き幅にミスマッチ
- 参照データ（上位足）の取得エラーにより固定値が返り続けている（データ障害）
- direction_filterの閾値が直近の相場環境に対して厳しすぎる設定になっている

---

### 【確認事項】まず確認すべきこと

**ログ・データ系**
- `direction_filter` のブロック詳細ログを確認し、何の条件で弾かれているか特定する
- 上位足データ（1H/4H）の取得が正常か、NaN・固定値が返っていないか確認
- `main_loop_restarts=1` の再起動タイミングとdirection_filter急増の時刻が一致していないか確認

**設定・パラメータ系**
- direction_filterの判定ロジック・閾値が最近変更されていないか確認
- RNB USD/JPYの期待シグナル方向と、現在のUSD/JPY実勢トレンドの方向が本当に乖離しているか相場を目視確認

**市場環境系**
- 現在がレンジ・膠着相場でないか確認（全モードトレード0件の背景要因として妥当性を評価）
- 週末・祝日・流動性枯渇時間帯に該当していないか確認

---

### 【対処方針】

- **即時対応**
  - `rnb_

## Alert: 02:53 UTC

### 検出された問題
- ⚠️ **直近4時間トレード0件** （一部モードはシグナルあり — 遮断フィルター確認推奨）

### 診断
## 原因分析

### 推測（根拠あり）

- **`r2_shadow_demoted_cell` ブロックが支配的**
scalp系・scalp_5m系・scalp_eur系で合計63件ブロック。シグナル自体は出ているが、R2シャドウセルの降格判定フィルターが過剰に作動し、エントリーを全遮断している可能性が高い

- **`direction_filter` による RNB 遮断**
rnb_usdjpy で11件ブロック。方向性フィルターが現在の相場レンジ・トレンド条件に合致せず、全シグナルを弾いている可能性

- **`order_bar_dedup` による daytrade 遮断**
daytrade で9件ブロック。重複排除ロジックが同一バー内の再エントリーを連続して棄却しており、実質的にエントリー機会がゼロになっている

- **全モードで2日連続トレード0件**
ログ上8/22・8/23ともに全モード0件。単一フィルターの問題ではなく、**上位共通フィルター or 環境判定の閾値問題**が複合している疑いが強い

- **`main_loop_restarts: 1`**
1回再起動あり。再起動時にフィルター状態・カウンターがリセットされずに引き継がれた、または再起動後に初期化が不完全な状態でフィルターが誤作動した可能性

---

### 確認事項（未確定・要調査）

| # | 確認項目 | 確認先 |
|---|----------|--------|
| 1 | `r2_shadow_demoted_cell` の降格判定閾値・降格解除条件が正常か | フィルターロジック設定値・過去の降格履歴 |
| 2 | 降格されたセルが **解除されないままリセットされていない** か | セル状態の永続化ストレージ（DB/ファイル） |
| 3 | `direction_filter` の判定に使用している相場状態データが **古いまたは取得失敗** していないか | データフィード・キャッシュのタイムスタンプ |
| 4 | `order_bar_dedup` のバー管理タイマーが再起動後にズレていないか | 再起動タイミングとバー境界の一致確認 |
| 5 | 上位の市場時間・休日フィルターが誤作動していないか（8/23は土曜日） | カレンダーフィルター設定（週末判定） |
| 6 | daytrade の `last_signal` は `02:52` に SELL が出ているが、

## Alert: 04:33 UTC

### 検出された問題
- ⚠️ **直近4時間トレード0件** （一部モードはシグナルあり — 遮断フィルター確認推奨）

### 診断
## 原因分析

### 【推測】主因：r2_shadow_demoted_cell フィルターの大量ブロック

- `r2_shadow_demoted_cell` が複数モード・通貨ペアで合計 **500件超** のブロックを記録
- これはR2モデルのシャドウセルが「降格状態」にあることを示しており、**モデルの予測信頼度が閾値を下回り続けている**可能性が高い
- scalp / scalp_eur / scalp_5m / daytrade_1h_usdchf / daytrade_audjpy 等、広範囲に波及しており **単一通貨・単一モードの問題ではなく、共通のモデル基盤またはフィーチャー入力系の問題**と推測

### 【推測】副因：rnb_usdjpy の direction_filter 過剰遮断

- `rnb_usdjpy:direction_filter` が単独で **194件** とブロック数最多
- 相場のトレンド方向が方向性フィルターの許容レンジ外に長時間滞在している可能性
- または direction_filter のパラメーター（閾値・参照期間）がレンジ相場・夏枯れ相場に対してミスマッチを起こしている可能性

### 【推測】補足：daytrade の confidence 低下

- `daytrade` のlast_signal.confidence が **23**、他モードは **0**
- 全モードで WAIT シグナルが継続しており、シグナル生成自体は動作しているが**エントリー条件を満たしていない**状態
- main_loop_restart が **1回** あり、その際の状態リセットが影響している可能性もあり

---

## 確認事項（推測ではなく実態確認が必要なもの）

1. **r2_shadow_demoted_cell の降格トリガー条件**
   - いつ降格が発生したか（タイムスタンプ）
   - 降格の解除条件（昇格閾値）と現在のスコアの乖離幅
   - main_loop_restart (1回) のタイミングと降格発生タイミングの一致確認

2. **R2モデルへのフィーチャー入力の正常性**
   - 価格データ・テクニカル指標の取得が正常か（NaN混入・遅延・欠損がないか）
   - 複数モードで同時発生しているため、**共通フィーチャーパイプラインの上流を優先確認**

3. **direction_filter のパラメーター設定と現在の相場環境の適合性**
   - 194件ブロックは異常値。フィルター閾値の設

## Alert: 06:31 UTC

### 検出された問題
- ⚠️ **直近4時間トレード0件** （一部モードはシグナルあり — 遮断フィルター確認推奨）
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=407, scalp:r2_shadow_demoted_cell=295, scalp_eur:r2_shadow_demoted_cell=256

### 診断
## 原因分析

### 【推測】主因：複数フィルターの同時発動による全モード遮断

---

### 推測（確認前の仮説）

**① rnb_usdjpy:direction_filter=407 — 方向性フィルターが長期固着**
- カウント407は異常に高く、単なる一時的な相場方向の不一致ではなく、**フィルター判定ロジックのパラメータが現在の相場レンジ・トレンドと乖離している**可能性が高い
- USDJPYが一方向に固定されているか、あるいはフィルター基準値（閾値・MA等）がリセットされずに古い状態を参照し続けている可能性

**② scalp/scalp_eur:r2_shadow_demoted_cell=295/256 — シャドウセル降格が大量発生**
- `r2_shadow_demoted_cell`の多発は、**R2グリッド上のセルが軒並み信頼度低下判定**を受けていることを示す
- 相場のボラティリティパターンが学習済みセル分布と大きく乖離している、またはセル更新ロジックに障害がある可能性
- `daytrade_1h_usdchf:159`、`scalp_5m_eur:114`など複数通貨ペア・複数モードで横断的に発生しており、**共通コンポーネント（R2セル管理モジュール）側の問題**である可能性が高い

**③ 全モード0件トレード（2日連続）**
- 上記2系統のフィルターが実質的に全エントリーを遮断している
- `main_loop_alive=true`、`emergency_killed=false`のためシステム自体は稼働中
- `main_loop_restarts=1`が記録されており、再起動タイミングでフィルター状態がリセットされなかった可能性

---

### 確認事項（調査すべき項目）

| 優先度 | 確認項目 | 目的 |
|--------|----------|------|
| 🔴 高 | `direction_filter`の判定ロジックと現在の参照値（MA・トレンド判定パラメータ） | フィルター固着の原因特定 |
| 🔴 高 | R2セル管理モジュールのログ（降格判定の条件・頻度） | セル大量降格の根本原因特定 |
| 🔴 高 | `main_loop_restarts=1`の発生時刻とその前後のログ | 再起動との因果関係確認 |
| 🟡 中 | 各フィルターのカウントリセット仕様（起動時

## Alert: 08:33 UTC

### 検出された問題
- ⚠️ **直近4時間トレード0件** （一部モードはシグナルあり — 遮断フィルター確認推奨）
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=629, scalp:r2_shadow_demoted_cell=413, scalp_eur:r2_shadow_demoted_cell=348, daytrade_1h_usdchf:r2_shadow_demoted_cell=221

### 診断
## 原因分析

### 【推測】主因

**1. `r2_shadow_demoted_cell` フィルターの過剰発動によるトレード全面遮断**
- scalp(413), scalp_eur(348), daytrade_1h_usdchf(221), scalp_5m系(各100件台) と複数モードで同フィルターが異常カウント
- 相場のボラティリティ構造（レンジ・ヒゲ主体）が現在の閾値設定と長期間ミスマッチしている可能性
- または r2セル判定ロジックのバグ・パラメータ劣化（閾値が実勢から乖離）

**2. `rnb_usdjpy:direction_filter` の突出ブロック（629件）**
- RNBモードがほぼ全シグナルを方向フィルターで弾いている
- USDJPYが一方向トレンドでなくレンジ推移 → 方向判定が常に反転し続けている可能性
- またはdirection_filterの参照する上位足トレンド判定ロジックのバグ・データ不整合

**3. `daytrade:score_gate`(101件) による daytrade全般の抑制**
- スコアが閾値を恒常的に下回っている → 市場環境の変化 or スコアリング係数の劣化

---

### 【確認事項】（推測と分離）

| 優先 | 確認項目 | 目的 |
|---|---|---|
| 🔴高 | `r2_shadow_demoted_cell` の判定ロジックと閾値パラメータ | バグか閾値劣化かを切り分け |
| 🔴高 | 現在の実勢チャート（USDJPY等）のヒゲ・レンジ状況 | 相場起因か実装起因かを判断 |
| 🔴高 | `direction_filter` が参照している上位足データの鮮度・整合性 | データ欠損・スタック疑い |
| 🟡中 | `score_gate` のスコア分布ログ（実際に何点が弾かれているか） | 閾値の適切性確認 |
| 🟡中 | `main_loop_restarts: 1` のタイミングとその直前ログ | 再起動後にフィルター状態がリセットされているか |
| 🟢低 | `scalp_5m_eur:unknown_type:engulfing_bb_lvn_london_ny` エラー | シグナル型未定義 → コードデプロイ不整合の疑い |

---

## 対処方針

### 即時対応
- **`r2_shadow_demoted_

## Alert: 10:25 UTC

### 検出された問題
- ⚠️ **直近4時間トレード0件** （一部モードはシグナルあり — 遮断フィルター確認推奨）
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=832, scalp:r2_shadow_demoted_cell=507, scalp_eur:r2_shadow_demoted_cell=423, daytrade_1h_usdchf:r2_shadow_demoted_cell=327, scalp_5m_gbp:r2_shadow_demoted_cell=238

### 診断
## 原因分析

### 【推測】主因

**1. `r2_shadow_demoted_cell` フィルターによる広域ブロック**
- scalp/scalp_eur/scalp_5m_gbp/daytrade_1h_usdchf で合計1,500件超のブロック
- R2セルがdemoted状態（低信頼ゾーン）に大量該当しており、現在の相場レンジが過去の負けゾーンと重複していると判定されている可能性が高い
- 直近数日〜数週のR2学習データが特定価格帯で集中的にdemoteされた可能性

**2. `rnb_usdjpy:direction_filter=832` の異常**
- 全フィルター中最多。RNBモードがシグナルを出すたびに方向フィルターで全弾着
- USDJPYのトレンド判定ロジックと実際の方向が長期間乖離している可能性
- または direction_filter の参照する上位足トレンド判定がスタックしている可能性

**3. `daytrade:score_gate=143` の累積**
- confidence 閾値は超えていない（last_signal confidence=54）ためスコア不足が慢性的に発生している

---

### 【確認事項】切り分けのため要確認

| 優先 | 確認項目 | 目的 |
|------|----------|------|
| 🔴高 | R2セルのdemoted件数・更新日時・対象価格帯 | demoteが特定セルに集中していないか、TTL切れを起こしていないか |
| 🔴高 | `direction_filter` が参照するトレンド判定の現在値（1h/4h足） | 判定ロジックがフリーズしていないか |
| 🟡中 | `r2_shadow_demoted_cell` のブロック対象シグナルの方向・価格帯 | 相場全体が問題ゾーンに入っているのか、ロジックバグか |
| 🟡中 | `main_loop_restarts=1` のタイミングと原因ログ | 再起動時にトレンド状態がリセット・引き継ぎされているか |
| 🟢低 | `scalp_eur:unknown_type:engulfing_bb_lvn_london_ny` | シグナルタイプ未定義の警告。ブロック数は少ないが定義漏れ疑い |

---

## 対処方針

### 即時対応

- **direction_filterの状態をダンプし、参照トレンド値を手動確認**
  - 実際のUSDJPY方向と乖離していればトレンドキャッシュの強制リフレッシ

## Alert: 12:20 UTC

### 検出された問題
- ⚠️ **直近4時間トレード0件** （一部モードはシグナルあり — 遮断フィルター確認推奨）
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=1037, scalp:r2_shadow_demoted_cell=580, scalp_eur:r2_shadow_demoted_cell=525, daytrade_1h_usdchf:r2_shadow_demoted_cell=388, scalp_5m_gbp:r2_shadow_demoted_cell=260

### 診断
## 原因分析

### 【推測】主因

**1. `r2_shadow_demoted_cell` フィルターの過剰発火（最優先疑義）**
- scalp系・daytrade_1h_usdchf で合計 **約2,000件超** のブロックが集中
- R2セルがshadow（低品質セル）に大量降格されており、エントリー候補をほぼ全滅させている可能性
- 相場のボラティリティパターン変化、または降格ロジックの閾値がタイトすぎる状態に陥っている疑い

**2. `rnb_usdjpy:direction_filter=1037` の異常値**
- 1,000件超は明らかに正常範囲外
- 方向フィルターが一方向（BUYまたはSELL）に固着し、逆方向シグナルを全件排除している可能性
- トレンド判定ロジックまたはその参照データ（例：上位TFのバイアス値）が固定値・staleになっている疑い

**3. `daytrade:order_bar_dedup=209` / `score_gate=147`**
- こちらは副次的。同一バーへの重複発注防止と低スコア排除が積み重なっており、流動性または相場停滞が背景にある可能性

---

### 【確認事項】（推測と分離）

| 優先度 | 確認項目 | 確認先 |
|--------|----------|--------|
| 🔴高 | `r2_shadow_demoted_cell` の降格判定ロジックの閾値・降格解除条件 | フィルター設定ファイル / shadow判定モジュール |
| 🔴高 | `direction_filter` が参照する上位TFバイアス値の現在値・更新タイムスタンプ | rnb_usdjpy設定 / バイアスキャッシュ |
| 🔴高 | バイアスキャッシュがstale（古い値で固着）していないか | キャッシュTTL設定・最終更新時刻ログ |
| 🟡中 | `main_loop_restarts=1` の再起動原因 | エラーログ（再起動前後の時刻周辺） |
| 🟡中 | `scalp_eur:unknown_type:engulfing_bb_lvn_london_ny` — シグナルタイプ未定義エラー | シグナル種別登録テーブル |
| 🟡中 | shadow降格されたセルの具体的なR2値・対象時間足 | shadow_cell管理DB or ログ |
| 🟢低 | `score_gate` ブロックの平均スコア値（
