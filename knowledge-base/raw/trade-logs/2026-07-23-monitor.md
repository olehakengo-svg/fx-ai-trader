# Trade Monitor Alerts: 2026-07-23

## Alert: 01:25 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=235

### 診断
## 原因分析

### 【推測】

- `rnb_usdjpy:direction_filter=235` は他のブロックカウント（最大199）と比較して突出しており、**rnb_usdjpyモードのdirection_filterが継続的にシグナルを棄却し続けている**状態
- USDJPYが現在**一方向に強いトレンド**を形成しており（163円台で推移）、rnbモードの想定する逆張り・レンジ方向と**エントリー方向が恒常的に不一致**になっていると推測
- direction_filterはMTFまたは上位足トレンド判定に基づくフィルターと思われ、**上位足バイアスがBUY方向に固定されたまま**rnbモードがSELL方向シグナルを連発している可能性が高い
- `main_loop_restarts=1` があり、再起動後にdirection_filterのカウントが**リセットされずに累積**している、あるいは再起動後から集中的に発生している可能性もある

### 【確認が必要な事項】

- rnb_usdjpyモードの現在の方向設定（固定値か動的判定か）および直近のシグナル方向の実績
- direction_filterの判定根拠となっている上位足（D1/H4等）のトレンド方向の現在値
- 235件がいつ頃から急増したか（時系列分布）。再起動前後で分布が偏っていないか
- rnb_usdjpyモードのlast_signalが今回のステータスに含まれていない理由（モードが実質的にdead stateになっていないか）
- direction_filterの閾値・判定ロジックが直近のパラメータ変更・相場レジーム変化の影響を受けていないか

---

## 対処方針

- **即時対応**
  - rnb_usdjpyの直近シグナルログを抽出し、どの方向でどれだけの頻度でdirection_filterに弾かれているか件数と時刻分布を確認する
  - 上位足（D1/H4）のUSDJPYトレンド判定値をダンプし、現在のフィルター判定根拠を確認する

- **原因に応じた対処**
  - **上位足が強BUYトレンドで固着している場合** → rnbモードのエントリー方向ロジックがトレンドに逆らい続けているため、方向フィルターの条件を現状相場に合わせて見直すか、条件が満たされるまでrnbモードを一時停止する
  - **フィルター閾値が不適切に厳しくなっ

## Alert: 08:06 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=353, daytrade:hedge_block=276, daytrade_eurgbp:hedge_block=216

### 診断
## 原因分析

### 推測（確認前）

**① `rnb_usdjpy:direction_filter=353`（最多・最重要）**
- USDJPYが現在163円台と歴史的高値圏にあり、rnbストラテジーの方向性フィルタ（トレンド・レンジ判定）が長期間BUYをブロックし続けている可能性が高い
- D1/H4のMTFがrange_tight+vol=squeezeを示しており（ログ確認）、方向フィルタが「どちらにも張れない」状態で詰まっている可能性
- または、direction_filterの閾値設定が現在の相場ボラティリティ水準に対してタイトすぎる

**② `daytrade:hedge_block=276` / `daytrade_eurgbp:hedge_block=216`（次点）**
- ヘッジポジションが解消されずに残存し、新規エントリーがブロックされ続けている
- EURGBP・USDJPYで既存ポジションが滞留しており、ヘッジペアのネットが中立に戻っていない
- `daytrade_eurgbp:order_bar_dedup=116`が同時に高いことから、シグナル自体は頻繁に出ているがヘッジブロックで全て落ちている構造

**③ `ORDER_BAR_DEDUP`の大量発生（全体的）**
- ログを見るとbar_ts=08:00:00と08:03:00/08:04:00への同一バーへのブロックが数十秒間隔で繰り返されている
- メインループがインターバル内に複数回シグナル評価を実行している、またはサブモード間でシグナルが重複起動している疑い
- `main_loop_restarts=1`があり、再起動後にdedup状態がリセットされず古いbar_tsへの重複判定が増加した可能性

---

### 確認事項（実機・ログで要確認）

| 優先 | 確認項目 | 目的 |
|---|---|---|
| 🔴高 | rnb_usdjpy のdirection_filter判定ロジックと現在の閾値パラメータ | フィルタが永続的にロックされていないか |
| 🔴高 | 現在のヘッジポジション一覧（USDJPY・EURGBP） | 未決済ヘッジが残存しているか |
| 🔴高 | main_loop_restartsが1になった時刻とその直前ログ | 再起動起因でdedup/hedge状態が壊れていないか |
| 🟡中 | 各モードのevaluate呼び出し

## Alert: 11:43 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=296, daytrade_eurjpy:hedge_block=247

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=296`**
  - RnBストラテジーがUSDJPY相場の方向性と逆行するシグナルを大量生成し続けている
  - direction_filterが長時間・高頻度でブロックし続けている＝相場トレンドとストラテジーの方向判定が恒常的に不一致の状態
  - カウンターがリセットされず累積していることから、セッションをまたいで継続中の可能性が高い

- **`daytrade_eurjpy:hedge_block=247`**
  - EUR/JPY方向に対してヘッジポジションが既に成立しており、同方向エントリーを大量ブロック
  - `daytrade_eurgbp:hedge_block=176`・`daytrade_gbpjpy:hedge_block=109`も高値であり、**JPY関連・GBP関連のヘッジが広範囲に張られたまま解消されていない**可能性
  - ヘッジポジションのクローズ条件が満たされず、ブロックが連鎖継続している状態と推測

- **`ORDER_BAR_DEDUP`の多発**
  - 同一バー（11:30、11:00）に対するシグナルが複数ストラテジーから繰り返し発火している
  - インターバル設定に対してポーリング頻度が過剰、またはシグナル生成ロジックがバーの確定を待たずに再評価している

- **`main_loop_restarts=1`**
  - 直近で1回再起動が発生。リスタート前後でblock_countersがリセットされずに引き継がれているなら、カウント値が実態より膨らんでいる可能性あり

---

### 確認事項（要調査）

| 確認項目 | 目的 |
|---|---|
| `rnb_usdjpy`のdirection_filterのリセットタイミング定義 | セッション単位 or 累積カウントかを判別 |
| EUR/JPY・GBP/JPYのヘッジポジションの現在残高 | ヘッジが実際にオープンのままか確認 |
| ヘッジ解消条件（TP/SL/時間）のトリガー状況 | なぜ解消されないかを特定 |
| `main_loop_restarts`前後のblock_countリセット有無 | カウント値の信頼性確認 |
| RnBストラテジーの方向フィルター参照ソース（MA/トレンド判定）の現在値 | 相場との乖離幅を定量確認 |
| ORDER_BA

## Alert: 17:07 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=299, daytrade_eur:hedge_block=286

### 診断
## 原因分析

### 推測（高確度）

**`rnb_usdjpy:direction_filter=299`**
- RNBストラテジーがUSD/JPYの方向フィルターで大量ブロックされている
- 現在の相場方向（163.7円台）とRNBのトレンド判定が継続的に不一致を起こしている
- フィルター閾値が現在の相場レジームに対して過剰に厳格、またはトレンド判定ロジックが現状の値動き（レンジ推移）を誤認している可能性が高い

**`daytrade_eur:hedge_block=286`**
- EUR系通貨ペアでヘッジポジションが長時間解消されず、新規シグナルを大量ブロックし続けている
- `daytrade_eurgbp:hedge_block=137`、`daytrade_eurjpy:hedge_block=91`も同様に高値であり、EUR系全体でヘッジロック状態が継続中であることを裏付ける
- ログの `[HOLD] DT含み益保護` が頻発していることと整合性あり（既存ポジション保護優先でブロック継続）

### 推測（中確度）

- `order_bar_dedup` の多発は同一バー内で複数ストラテジーが同シグナルを重複発生させている正常動作の可能性があるが、件数が多すぎる場合はポーリング間隔とバー確定タイミングのズレが疑われる
- `main_loop_restarts=1` が発生しており、再起動前後でカウンタがリセットされず累積している可能性がある（数値の信頼性への影響要確認）

---

## 確認事項

- [ ] RNB_USDJPYのdirection_filterが参照しているトレンド判定指標（MA/ADX等）の現在値と閾値設定
- [ ] EUR系のヘッジポジションが実際にOANDA上でオープンしているか、またはヘッジ解除条件を満たせていない理由
- [ ] `daytrade_eur:hedge_block` カウントの累積開始時刻（main_loop_restart前後か）
- [ ] block_countsがセッションリセットされているか、または起動後累積値か

---

## 対処方針

**即時対応**

- EUR系ヘッジポジションの状態をOANDA管理画面で直接確認し、意図しないポジションが残留していれば手動クローズを検討する
- ヘッジ解除条件（損益閾値・時間条件）が現在の相場状況で達成不可能な設定になっていないか確認し、必要であれ
