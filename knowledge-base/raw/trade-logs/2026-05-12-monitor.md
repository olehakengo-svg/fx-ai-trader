# Trade Monitor Alerts: 2026-05-12

## Alert: 01:46 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=301

### 診断
## 原因分析

### 推測（確認前）

**主因：`rnb_usdjpy:direction_filter=301` の異常蓄積**

- `rnb_usdjpy` ストラテジーの方向フィルターが、長時間にわたって**一方向（おそらくBUY方向）のシグナルを全拒否し続けている**
- 他のblock_countsが最大でも99（`scalp_5m_gbp:hedge_block`）であるのに対し、301は**3倍超の突出値**であり、カウンターがリセットされずに蓄積し続けている可能性が高い
- USDJPYで `daytrade:score_gate=68` や `SCORE_GATE` ログが連続していることから、**USDJPYペア全体でトレンド方向の判定が拮抗または固着**している状態が推測される
- `bb_width_pct=0.0`（EURGBP）はスクイーズ状態の長期継続を示しており、**相場全体が方向感を失っている**セッション環境と整合する

**副次的背景**
- `daytrade:score_gate=68` と `SCORE_GATE` ログの `misaligned with signal=SELL` が継続 → directional biasとシグナル方向が噛み合っていない状態が長期化
- `main_loop_restarts=1` があり、再起動後にカウンターがリセットされていない可能性

---

### 確認事項（要調査）

1. **`direction_filter` のリセットタイミング**
   - 日次・セッション切替・再起動時にカウンターがリセットされる設計になっているか
   - 再起動（`main_loop_restarts=1`）後にカウンターが引き継がれていないか

2. **`rnb_usdjpy` の方向判定ロジックの状態**
   - 現在の判定方向（BUY固定 or SELL固定）と、その根拠となる指標の値を確認
   - 判定がフリーズ（更新されていない）していないか

3. **カウンター計測期間**
   - `block_counts` は「起動来累積」か「直近N分」か → 301が正常な累積値なのか異常な蓄積なのかの判断に直結

4. **`rnb_usdjpy` からの最終シグナル発火時刻**
   - 長時間シグナルゼロであればフィルターの実質的な機能停止とみなせる

---

## 対処方針

- **即時確認**
  - `rnb_usdjpy` の方向判定値・最終更新時刻をログ・内部状態から取得し、固着していない

## Alert: 05:28 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=288, daytrade_eurjpy:hedge_block=286

### 診断
## 原因分析

### 推測（ログ・数値から高確率で推定）

**① `rnb_usdjpy:direction_filter=288`**
- USDJPYの方向性フィルターが長時間・高頻度でブロックし続けている
- RNBストラテジーが現在のレンジ相場または方向性不一致の相場環境に対してシグナルを出し続けているが、フィルターが全て遮断している状態
- 相場の方向感がRNBの判定基準と乖離している可能性が高い

**② `daytrade_eurjpy:hedge_block=286`**
- EURJPY側でヘッジポジションが長期間ブロック状態を維持している
- 既存ポジションとの方向競合（ヘッジ検知）が解消されずにカウントが積み上がっている
- EURJPYに未決済の相反ポジションか、ヘッジ判定ロジックが膠着している可能性

**③ USDJPYデイトレードの`HOLD/SCORE_GATE`連発（ログより）**
- BUYポジション`3d230dd8`が含み益保護により逆シグナル（SELL）を無効化し続けている
- `streak_reversal score=0.53`がSELLシグナルと方向不一致でSCORE_GATEにも遮断されている
- ポジション自体は利益出ているが、クローズ条件を満たせず滞留中と推測

---

### 確認事項（要実査）

| 確認項目 | 目的 |
|---|---|
| RNBポジションの現在の保有状況・方向 | direction_filterのブロック対象が新規エントリーか既存ポジション関連かの切り分け |
| EURJPYの全オープンポジション（方向・枚数） | hedge_blockの原因となっているポジションの特定 |
| `3d230dd8`ポジションのTP/SL設定とエントリー時刻 | 含み益保護での滞留期間と意図した決済条件の確認 |
| direction_filterの判定基準（閾値・参照TF） | 288回ブロックが正常な保護動作か、ロジック膠着かの判断 |
| hedge_blockの解除条件と最終更新時刻 | 手動介入が必要な膠着状態かどうかの確認 |

---

## 対処方針

- **即時対応不要（緊急性低）**：`emergency_killed=false`、`main_loop_alive=true`であり、システム自体は稼働中。異常終了リスクは現時点でなし

- **RNB_

## Alert: 08:18 UTC

### 検出された問題
- 💰 **含み益+25.5pip** (5ポジション, LIVE=0 Shadow=5) | Top: london_session_breakout×GBP_USD +25.1pip | session_time_bias×EUR_USD +2.9pip | vdr_jpy×USD_JPY +0.0pip
- ⚠️ **Shadow含み益+28.0pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=308

### 診断
## 原因分析

### 推測（ログ・数値から高確率で推定）

**① Shadow益がLIVEに反映されていない（PAIR_PROMOTED未設定）**
- LIVE=0, Shadow=5 という構成から、対象ペア（GBP_USD, EUR_USD, USD_JPY）がPAIR_PROMOTEDリストに含まれていないため、Shadowシグナルが実注文に昇格していない
- london_session_breakout×GBP_USDの+25.1pipが特に大きく、機会損失が発生中

**② rnb_usdjpy:direction_filterが308と突出して高い**
- 他のblock_countsが概ね一桁〜三桁前半であるのに対し、308は明らかに異常値
- direction_filterロジックが「常にブロック判定を返す条件」にハマっている可能性が高い
- USD/JPYの相場方向が長時間一方向に固定（またはフィルター閾値と乖離）していることで、rnb_usdjpy戦略のシグナルが全量棄却されている

**③ DT含み益保護によるSIGNAL_REVERSE連続無効化**
- fdc1d11d / bd2f6768 のprofit=0.79800が異常に大きい（他ポジションの100倍以上）
- この2ポジションが長時間HOLDされ続け、逆張りシグナルを継続ブロックしている
- R2_SHADOW_DEMOTEがbb_rsi_reversion / sr_channel_reversal で頻発しており、USD/JPY方向感と逆行するセルが連続デモート中

---

### 確認事項（実機で要検証）

- PAIR_PROMOTEDの現在の設定値（GBP_USD, EUR_USD が含まれているか）
- rnb_usdjpyのdirection_filter判定条件と、現在のUSD/JPY相場の方向・閾値の突合
- profit=0.79800 の2ポジション（fdc1d11d, bd2f6768）の建値・ロットサイズ・開始時刻（計算ミスかスケール異常の可能性）
- main_loop_restarts=1 の再起動タイミングと、その前後でblock_countがリセットされているか（308が累積かどうか）

---

## 対処方針

**即時対応**

- GBP_USD, EUR_USD をPAIR_PROMOTEDに追加するか、Shadow→LIVE昇格条件を満たしているか確認し、問題なければ手動で承認
- profit=0.79800 の2ポジションの正当性を確認。スケール異常であればポジション強制クローズ

## Alert: 11:45 UTC

### 検出された問題
- 💰 **含み益+110.5pip** (9ポジション, LIVE=0 Shadow=9) | Top: vdr_jpy×USD_JPY +71.7pip | vdr_jpy×USD_JPY +71.7pip | sr_break_retest×EUR_JPY +6.9pip
- ⚠️ **Shadow含み益+153.1pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】主因

**Shadow→Live昇格ロジックが機能していない（または意図的に抑制されている）**

- 9ポジションすべてがShadowのまま留まり、LIVE=0が継続している
- 含み益+153.1pipというパフォーマンスが出ているにもかかわらず、OANDAへの発注が一切行われていない
- `PAIR_PROMOTED`への追加が未実施のため、昇格条件を満たせていない可能性が高い

**`bb_rsi_reversion × USD_JPY`のR2_SHADOW_DEMOTEが頻発**

- 約2分間で4回DEMOTEされており、このセルが昇格→降格を繰り返している、またはそもそも昇格できない状態でシグナルを出し続けている
- `scalp`系の`r2_shadow_demoted_cell`ブロックカウントが合計116件と突出しており、同一原因の可能性

**DT含み益保護のSIGNAL_REVERSE無効化が過剰発動**

- `fdc1d11d`・`bd2f6768`（BUY, ~+71pip）と`276a1408`（SELL, ~+6-7pip）に対し、逆シグナルが頻繁に発生しているが全て抑制されている
- 正常動作ではあるが、逆シグナルの頻度が高すぎる場合はシグナル生成ロジック側の問題も疑われる

---

### 【確認事項】（推測と分離）

**昇格ロジック関連**
- `PAIR_PROMOTED`設定に`vdr_jpy×USD_JPY`および`sr_break_retest×EUR_JPY`が含まれているか
- Shadow→Live昇格の判定条件（pip閾値・継続時間・信頼度など）を現在のShadowポジションが満たしているか
- `main_loop_restarts=1`の再起動タイミングで昇格状態がリセットされていないか

**R2_SHADOW_DEMOTE関連**
- `bb_rsi_reversion × USD_JPY`のR2スコアが昇格閾値を下回っている具体的な値
- DEMOTEトリガー条件（スプレッド・レジーム・SLクラスター等）のどれに該当しているか
- `scalp:r2_shadow_demoted_cell=59`が同一セルの累積か、複数セルに分散しているか

**逆シグナル頻発関連**
- `1b36807b`（SELL, profit=0.00031）が閾値ギリギリでHOLDされており、この水準のポジションが反転シグナル
