# Trade Monitor Alerts: 2026-08-28

## Alert: 01:49 UTC

### 検出された問題
- 💰 **含み益+40.8pip** (12ポジション, LIVE=0 Shadow=12) | Top: wick_imbalance_reversion×AUD_JPY +5.8pip | sr_break_retest×AUD_JPY +5.1pip | sr_break_retest×AUD_JPY +5.1pip
- ⚠️ **Shadow含み益+41.4pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=2277, scalp:r2_shadow_demoted_cell=1146, daytrade_eurgbp:hedge_block=929, daytrade:hedge_block=918, scalp_eur:r2_shadow_demoted_cell=813

### 診断
## 原因分析

### 推測（ログ・数値から高確率で推定）

**① Shadow→LIVE昇格が停止している（主因）**
- 12ポジション全てがShadow（LIVE=0）のまま+41.4pip含み益を出しているが、OANDAに1件も送信されていない
- `PAIR_PROMOTED`設定の欠落またはAUD_JPYが昇格対象ペアリストから外れている可能性が高い
- Shadow側のシグナルは正常に機能しており、戦略ロジック自体の問題ではない

**② `rnb_usdjpy:direction_filter=2277` が突出して異常**
- 他のブロック要因（hedge_block, order_bar_dedup）が数百件規模なのに対し、2277件は約2〜4倍
- direction_filterが一方向に固着している（トレンドフィルターのパラメータまたは参照データが固定値になっている疑い）
- main_loop_restarts=1があり、再起動後に内部状態がリセットされず偏りが継続している可能性

**③ `r2_shadow_demoted_cell` 系のカウントが複数モードで高水準**
- scalp: 1146, scalp_5m_gbp: 722, scalp_eur: 813 と広範囲に発生
- Shadowポジションがr2（第2ランク）セルに降格され続けており、昇格条件を満たせない状態が常態化している
- r2降格ロジックの閾値が厳しすぎる、またはShadow昇格の前段階で詰まっている

**④ `hedge_block` が複数通貨ペアで高水準**
- daytrade系の複数ペア（eurgbp:929, gbpusd:670, eur:657 等）で高いが、これは現在AUD_JPYのBUYポジションを多数保有しているためのヘッジ抑制として**正常動作の範囲内**とも見られる

**⑤ ORDER_BAR_DEDUP は正常動作**
- 同一バー(01:45:00)に対する重複注文を正しくブロックしており、これ自体は問題なし

---

### 確認事項（実際にログ・設定を見るまで断定できない）

- [ ] `PAIR_PROMOTED` または昇格許可リストにAUD_JPYが含まれているか
- [ ] Shadow→LIVE昇格の条件（pip閾値・時間経過・勝率条件等）が現在の相場環境で達成可能な水準か
- [ ] `rnb_usdjpy` のdirection_filterが参照しているデータソース（MA, ADX等）の現在値、および再起
