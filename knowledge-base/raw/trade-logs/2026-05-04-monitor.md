# Trade Monitor Alerts: 2026-05-04

## Alert: 01:27 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=386

### 診断
## 原因分析

### 【推測】原因

- `rnb_usdjpy:direction_filter` のblock_count=**386**は、他のフィルタ（最大129）と比較して**突出して高い**
- `direction_filter`は相場の方向性（トレンド）と逆方向のシグナルを弾くフィルタであり、カウントが異常に積み上がっているということは、**rnb_usdjpyが継続的にシグナルを生成しているにもかかわらず、方向判定で全てブロックされ続けている**状態と推測される
- 現在USDJPYはWIDE_RANGE（レンジ相場）が検出されており（ログ参照）、`direction_filter`の判定基準（例：EMA傾き・トレンドスコア閾値）と**実際の相場環境が著しく乖離している**可能性が高い
- または`direction_filter`の内部状態（基準価格・EMA等）が**何らかの理由でスタックまたは固定化**しており、常に同一方向のみを拒否し続けている可能性がある

---

### 【確認事項】

1. **direction_filterの判定ロジック確認**
   - どの指標（EMA方向・ADX・モメンタム等）を使って方向を判定しているか
   - フィルタ判定に使う参照データ（TF・期間）が正常に更新されているか

2. **直近のrnb_usdjpyシグナル内容の確認**
   - ブロックされているシグナルの方向（BUY/SELL）が一方向に偏っていないか
   - シグナル生成頻度が正常範囲か（過剰生成していないか）

3. **WIDE_RANGE検出との関係確認**
   - direction_filterがWIDE_RANGE環境に対応した設計になっているか
   - レンジ相場でのトレンドフィルタ誤判定が連鎖していないか

4. **main_loop_restarts=1の影響確認**
   - 再起動タイミングでdirection_filterの内部状態がリセット/破損していないか

5. **カウンタ自体のリセット確認**
   - block_countsはプロセス起動からの累積か、直近N分のウィンドウ集計か
   - 起動からの経過時間に対してlog_count=8734と照合し、異常蓄積速度でないか

---

## 対処方針

- **短期（即時）**
  - `rnb_usdjpy`の直近ブロックログを手動で抽出し、ブロックされているシグナルの方向・頻度・理由文字列を確認

## Alert: 08:12 UTC

### 検出された問題
- 💰 **含み益+96.9pip** (13ポジション, LIVE=0 Shadow=13) | Top: sr_break_retest×GBP_JPY +24.3pip | sr_break_retest×GBP_JPY +18.5pip | dual_sr_bounce×GBP_USD +16.0pip
- ⚠️ **Shadow含み益+106.8pip — OANDAに送信されていない** → PAIR_PROMOTED追加を検討

### 診断
## 原因分析

### 【推測】

**1. Shadow専有・LIVE昇格ゼロの根本原因**
- 全13ポジションがShadowに留まっており、PAIR_PROMOTEDリストにGBP_JPY・GBP_USDが含まれていないため、好成績ペアがLIVE昇格できていない
- sr_break_retestおよびdual_sr_bounceシグナルはEV正値で機能しているが、ペア未登録によりOANDA送信がブロックされている

**2. EUR_USD SELLシグナルの全Shadow化**
- MTFモニターで`mtf=trend_up_weak / d1=1 / h4=0`（日足上昇・4H中立）を検出し、逆張りSELLをEV=-2.714でブロックしている
- H8レイヤーでも同シグナルを二重ブロック → 正常動作の可能性が高い

**3. SIGNAL_REVERSE無効化の頻発**
- 複数ポジション（特にadfe6bef=+0.18pip、e276cb69=+0.24pip等）で含み益保護が発動し、反転シグナルを抑制している
- threshold=0.00030に対して利益が十分大きいため保護ロジック自体は正常動作とみられる

**4. hedge_blockの高カウント（daytrade:77、daytrade_gbpusd:76）**
- 既存ポジションとヘッジ関係にある方向のエントリーが大量ブロックされている
- Shadow13ポジションがヘッジ判定の母数になっている可能性

---

### 【要確認事項】

- [ ] PAIR_PROMOTEDの現在の設定値（GBP_JPY・GBP_USDが含まれているか）
- [ ] Shadow昇格条件（連続勝率・EV閾値等）の定義と、現在のsr_break_retestの実績値
- [ ] hedge_blockのカウント77/76がShadowポジションを母数に含んで計算されているか
- [ ] main_loop_restarts=1 の再起動タイミングとその直前ログ（異常終了か正常再起動か）
- [ ] daytrade_1h・daytrade_1h_eurがrunning=falseである意図的な停止か異常停止か

---

## 対処方針

**即時対応**
- GBP_JPYおよびGBP_USDをPAIR_PROMOTEDに追加し、sr_break_retestシグナルのLIVE昇格を許可する
- 追加前にsr_break_retestの直近バックテストEVおよびShadow実績（勝率・平均
