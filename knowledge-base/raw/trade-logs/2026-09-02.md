# 2026-09-02 Daily Trade Log (wiki-daily-update)

**Pull times**: demo/stats = 2026-09-02 **19:11 UTC** / learning・risk・audit = **19:42 UTC** / status・block-counts・equity・heartbeat = **20:24:58 UTC**
**Clock check**: ✅ local UTC 20:24:58 vs server `row_freshness_now` 20:24:58.984 — **skew 0s** (**4th consecutive non-reproduction** of the 08-26 ~5h40m skew).
**Audit window**: `limit=800` = 2026-08-19T13:02:10Z → **2026-09-02T18:31:22Z** (14.23 days, `total` 16,303; `execution_audit_count` 16,307 at 20:24Z).
**Orphan check**: `pgrep -f app.py` → **nothing running**; every figure below is Render API (`_db_path: /var/data/demo_trades.db`), no local-DB phantom. Per `feedback_check_orphan_local_app`.

---

## 1. Realized book — bit-flat for the **5th** consecutive snapshot

| Field | Value | vs 09-01 |
|---|---:|---|
| N (live_count / total) | **579** | flat |
| W / L / BE | 252 / 292 / 35 | flat |
| WR | **43.5%** | flat |
| decided WR | **46.3%** | flat |
| EV | **−1.06** | flat |
| total_pnl | **−611.1 pip** | flat |
| Wilson lower | 42.2 | flat |
| Wilson BF lower | 39.4 | flat |
| avg R | 0.12 | flat |

Every realized field is bit-for-bit identical to 09-01, 08-31 and both 08-28 runs. **Zero closed
fills across ~5 days** (08-28 11:52Z → 09-02 20:25Z).

⚪ **Still suppression, not outage — the engine is provably alive and the shadow book advances:**

- `shadow_count` 12,719 → **12,843 (+124)**
- `oanda_audit.total` 16,153 → **16,303 (+150)**; `execution_audit_count` **16,307** (4 rows added
  between the 19:42Z audit pull and the 20:25Z status pull — consistent, not a discrepancy)
- `engine_tick_age_sec` **0.1s**, **24** running modes, stalest mode `daytrade_1h_eurgbp` 58.6s
- `main_loop_alive` / `watchdog_alive` / `sltp_checker_active` = true, `main_loop_restarts` still **1**
- OANDA heartbeat `ok`, latency **78ms**, `last_check` 20:24:29Z
- `emergency_killed` **false**

⚠️ **Freshness slowed again but stays inside threshold**: `last_candidate_row_age_sec` **3,165s
(52.8 min)** vs 7.8s on 09-01 — the endpoint's own market-open conversion puts it at **0.9h against
a 6h threshold**, `status: ok`. `last_trade_row_age_sec` **525s (8.75 min)**, `status: ok`. The
20:16:14Z trade row is a **shadow** open (§4), so it is not evidence of live activity.

---

## 2. Risk dashboard — the frozen positive window survives a **second** roll

The window rolled again (`effective_date_from` 08-02T11:51:00 → **08-03T19:42:06**, **+31.9h**) and
**n stayed at 13 with every headline bit-identical for the third consecutive pull**:

| Field | 08-28 | 08-31 | 09-01 | **09-02** |
|---|---:|---:|---:|---:|
| Kelly edge | 0.0 | +0.6709 | +0.6709 | **+0.6709** |
| recommended | — | half_kelly 0.128 | half_kelly 0.128 | **half_kelly 0.128** |
| Sharpe (observed) | −0.0927 | +0.3067 | +0.3067 | **+0.3067** |
| DSR / haircut | 0.0 / 100% | 0.5624 / 15.1% | 0.5624 / 15.1% | **0.5624 / 15.1%** |
| 30d net | −58.4 | +94.3 | +94.3 | **+94.3** |
| n_trades | 16 | 13 | 13 | **13** |

⇒ Over **two** rolls totalling **+54.1h** of window movement, **no trade has entered and none has
aged out**. The same 13 trades have now been re-reported three days running. The 08-31 verdict
stands verbatim: **do not propagate as edge recovery** — this is the **6th consecutive
window-arithmetic reading**.

🔴 **The production sizing gate still disagrees, at the same value for the third pull.** All **13**
blocked rows in the new slice read `agg_kelly=-0.315<0` (09-01: 3 rows, 08-31: 6 rows — same
figure). The dashboard (n=13 rolling-30d live slice) and the gate (aggregate book) remain openly
divergent, and **the gate's number is the one that deploys capital**.

Nothing in the window is statistically significant: `dsr_overall.is_significant` **false**
(z=0.157, threshold 0.2602 vs observed 0.3067), `small_sample_lowconf` **true**, all three
per-strategy Kellys **`insufficient`** (n=9/3/1). Per-strategy DSR is computed only for carry-dip
(0.5193 / haircut 5.2% / **not significant**, z=0.048).

Window composition (n=13, unchanged): USD_JPY 9 **+84.0** / AUD_JPY 1 +20.1 / EUR_GBP 3 −9.8.
All cross-pair correlations **null** (insufficient overlap), `flagged_pairs` empty.
Monte Carlo: ruin **0.0%**, median max DD 19.34%, 99th-pct DD 38.66%, `small_sample_lowconf` true.

**Defensive posture unchanged**: `defensive_mode` **true**, `lot_multiplier` **0.2**,
DD **1198.8 pip** (`dd_pct` 0.097 on the JPY-NAV basis = **119.9%** on the legacy $1000 pip basis),
`eq_current` −$1181.9 vs peak +$16.9 — **all flat**.

⚠️ **`attribution` friction still not subtracted** — `gross_pnl = net_pnl = alpha = 94.3` with
`friction = 42.5` (`avg_friction_per_trade` 3.27). Correctly applied the 30d net is **+51.8**.
Existing, unresolved, **not diagnosed this run**.

---

## 3. Live path — 7th day with no fill

**Last live fill is still `price_shock_rev_aud_jpy_h1_long` AUD_JPY oanda #709537 (2026-08-26
14:59:26Z) — 7.23 days ago.**

New slice **09-01T12:33:14Z → 09-02T18:31:22Z (150 rows, 29.97h)**:

| bridge_status | count |
|---|---:|
| `skipped` — `shadow_tracking` | **128** |
| `skipped` — `shadow_tracking(shadow_emit_no_lot)` | **9** 🆕 |
| `blocked` — all `agg_kelly=-0.315<0` | **13** |
| `sent` | **0** |
| `filled` | **0** |

**Live rate 0.0% — 3rd consecutive zero-fill slice.**

- Instruments: USD_JPY 60 / GBP_USD 33 / EUR_USD 20 / AUD_JPY 12 / EUR_JPY 12 / GBP_JPY 11 / EUR_GBP 2
- Top entry types: `xs_momentum_rsi` 20 / `dt_sr_channel_reversal` 17 / `dual_sr_bounce` 10 /
  `vix_carry_unwind` 10 / `session_time_bias` 9 / `sr_break_retest` 9 / `trendline_sweep` 9 /
  `streak_reversal` 9

### ✅ RESOLVED — the zero-unit emission was never a defect

Carried as "undiagnosed" in every log since 08-23, this closes today. The audit shows a new
`block_reason`, `shadow_tracking(shadow_emit_no_lot)`, first occurring **2026-09-02 08:52:19Z**.
It is **a deliberate self-description shipped by PR #220** (`c41a4bdc`, *"fix(dedup): shadow_emit の
プロセス境界 dedup 突破を write-time DB flag で塞ぐ (rule:R3)"*, committed 09-02 16:21 JST = 07:21Z,
first audit row ~1.5h later):

> `modules/demo_trader.py:1492` — `block_reason=f"{SHADOW_TRACKING_BLOCK_REASON}(shadow_emit_no_lot)"`

**`units: 0` on these rows is a lot-unassigned tracking marker, not a zero-size order.** The commit
states this explicitly, and it is the answer to the question this log has been carrying: readers
must **never** treat that `units` field as a size. The `shadow_tracking` prefix was retained so
`startswith`-dependent guards and tooling stay compatible.

The split at the deploy boundary is exact, which is what confirms the attribution rather than
merely suggesting it:

| zero-unit rows in new slice | count | labelled |
|---|---:|---:|
| before 09-02 08:52:19Z | 6 | **0** |
| at/after 09-02 08:52:19Z | 9 | **9** |

**100% of post-deploy zero-unit rows carry the label and 0% of pre-deploy rows do** — so the 6
unlabelled rows (`sr_break_retest` AUD_JPY ×2 09-01 13:31, `vdr_jpy` EUR_JPY ×2 09-02 06:11,
`inducement_ob` GBP_USD ×2 09-02 07:19) are simply pre-deploy, **not a second unexplained code
path**. Rates are unchanged in character: **15/150 = 10.0%** in the new slice, **120/800 = 15.0%**
window-wide, all `bridge_status=skipped` ⇒ **zero live exposure** throughout.

⚠️ **What is NOT closed by this.** The same PR's substantive change is a *write-time DB dedup flag*
for process-boundary duplicate shadow rows (see
`wiki/lessons/lesson-shadow-emit-dedup-writetime-2026-09-02.md`, dedup family bug #5). Every
labelled pair above is a near-duplicate 13–32s apart — exactly the pattern that PR addresses — and
the fix **flags rather than prevents** (`dedup_violation=1`, row still inserted, by design). The
audit endpoint does not expose `dedup_violation`, so **this run cannot verify the flag is actually
being set**; a `demo_trades` query is required. Recorded as a new, narrower follow-up.

Within the 14.2-day pull the accept/reject contract holds: **6 sent / 6 filled / 6 real OANDA ids /
false-sent 0** (window truncated at 08-19; the full cumulative is 10/10 per the 08-31 log). The
07-02 contract is intact. Confirming the `entry_type` twin meaning
(`reference_oanda_audit_twin_meaning`): the 6 `sent` rows carry strategy names, the 6 `filled` rows
carry MODE names (`daytrade_1h` / `daytrade_1h_eurgbp` / `daytrade_1h_audjpy`) — **6 trades, not 12**.

---

## 4. 🔴 `hedge_block` shadow→live suppression — **5th independent confirmation**

`/api/demo/status` `open_trades` = **2 rows, every one `is_shadow: 1`**, against broker
`open_trade_count: 0` / `margin_used: ¥0`:

| entry_type | mode | instrument | dir | entry_time (UTC) |
|---|---|---|---|---|
| `ny_close_reversal` | daytrade | USD_JPY | SELL | 09-02 20:16:14 |
| `dual_sr_bounce` | daytrade_gbpusd | GBP_USD | SELL | 09-02 19:57:21 |

**The open book has now turned over completely four times (6 → 5 → 3 → 4 → 2 rows, with no strategy
carried over from 09-01) and the property has been preserved every time.** This is independent
corroboration of the 08-28 code read (`modules/demo_trader.py:4915-4928`,
`_mode_inst_live + _mode_inst_shadow`).
**Not a bug — a decision (rule:R2/H2). Still awaiting user adjudication. No code touched.**

⚠️ **Correction of direction vs 09-01's metadata note.** Both open rows carry a non-empty
`alpha_snapshot` (KSFT/KSFT2/RSV10/ROC10/QTLD5, `ts: latest`) — matching 09-01 — but **both have
`edge_cell_id: ""` (empty)**, where 09-01 had 2 of 4 populated with `"E8"`. 09-01's line should not
be read as a trend toward populated metadata. Neither observation touches
`project_sr_anti_hunt_demo_trades_meta_loss_2026-06-03`, which concerns **closed
`sr_anti_hunt_bounce` rows** — a targeted re-query of that strategy is still required.

### Block family shares

🆕 **Methodology correction — prior runs under-counted.** `/api/demo/status` truncates
`block_counts` to the **top 30 keys** (`modules/demo_trader.py:2061-2063`, `[:30]  # 上位30件のみ`).
The dedicated **`/api/demo/block-counts`** endpoint returns all **103** keys. This run uses the full
endpoint; the status-derived totals reported on 08-28 → 09-01 were therefore **lower bounds**, and
long-tail families (`same_price_5pip`, `recent_emit`, `sl_cluster`, `consec_loss`, …) were invisible
whenever they fell outside the top 30.

| family | count | share |
|---|---:|---:|
| `r2_shadow_demoted_cell` | 2,783 | **32.0%** |
| `order_bar_dedup` | 2,092 | **24.0%** |
| `hedge_block` | 1,853 | **21.3%** |
| `direction_filter` | 1,218 | **14.0%** |
| `score_gate` | 501 | 5.8% |
| `conf<30` | 85 | 1.0% |
| `same_price_0pip` | 42 | 0.5% |
| `same_price_5pip` | 27 | 0.3% |
| `recent_emit` | 23 | 0.3% |
| 17 further families | ≤15 each | ≤0.2% each |

Total **8,705** blocks over **28,778** ticks across 24 modes.

⚠️ **Counter window rolled again** (ticks 5,228 → 28,778 with `main_loop_restarts` still 1) — per
the 08-28 rolling-counter rule, **no cross-pull deltas are computed**. And because the measurement
basis changed this run, **the share column is not strictly comparable to 09-01's either**:
`hedge_block` moving 34.2% → 21.3% and `r2_shadow_demoted_cell` 32.9% → 32.0% is confounded by the
truncation fix and **must not be read as a gate change**. Ranking under the *same* full-endpoint
basis is the only comparison available from the next run onward.

---

## 5. 🔴 `rnb_usdjpy:direction_filter` — **6th consecutive confirmation**

**1,218 `direction_filter` blocks over 1,258 ticks = ratio 0.9682.** The residual 40 ticks are not
passes — they are `rnb_usdjpy:conf<30`, so **1,258 / 1,258 ticks = 100% of the mode's ticks were
blocked by one gate or the other, and not one candidate survived.**

| pull | dir_filter blocks | ticks | ratio |
|---|---:|---:|---|
| 08-26 | 2,624 | 2,622 | 1.0008 |
| 08-28 05:00Z | 2,624 | 2,622 | 1.0008 |
| 08-28 11:52Z | 630 | 630 | 1.0000 |
| 08-31 17:14Z | 1,477 | 1,509 | 0.9788 |
| 09-01 11:52Z | 235 | 235 | 1.0000 |
| **09-02 20:25Z** | **1,218** | **1,258** | **0.9682** |

A selective filter cannot produce this across six independently-windowed counters. Consistent with
the known `compute_rnb_signal` WAIT-path `entry: 0` bug. **The code read is still not done — 6 runs
running as the top open follow-up.**

---

## 6. 🔴 `/api/oanda/equity` — now **23.5 days stale**

Last curve point still **2026-08-10T07:20:34Z**, `count` still **928**, `total_pl_jpy` −80,686,
`total_pl_pips` −942.2. Progression **15d → 18d → 21d → 22d → 23.5d**, monotonically worsening
across five snapshots.

🆕 **JPY ledger drift widened for the first time in this series, by exactly ¥240.**

| | 09-01 | **09-02** | Δ |
|---|---:|---:|---:|
| `jpy_ledger.eq_current_jpy` | ¥324,450.50 | **¥324,450.50** | flat |
| broker NAV | ¥278,345.48 | **¥278,105.48** | **−¥240.00** |
| drift | ¥46,105.02 | **¥46,345.02** | **+¥240.00** |

The ledger side did not move; **the broker NAV fell ¥240.00 exactly, with `open_trade_count: 0`,
`margin_used: ¥0`, `unrealized_pl: 0` and zero fills in the interval.** A NAV change with no
position and no fill is most consistent with a financing/fee posting, but **this is a hypothesis —
not verified this run** (`/api/oanda/transactions` not pulled). Recorded for re-check next run;
a second unexplained round-number step would make it worth diagnosing.

Sizing impact still none (the `DD_LOT_TIERS` chain is saturated at 0.20×); reporting impact still
material (headline DD understates broker-measured DD).

---

## 7. Learning API — frozen for an **8th** snapshot

- Latest adjustment **id=93** (2026-08-19 14:25:37) — **14.25 days stale**, unchanged for 8
  consecutive snapshots
- `entry_type_blacklist` still **`[]`** ⇒ the exclusion is a **no-op, unresolved since 06-30**.
  Of the 30 journaled adjustments, **12 are byte-identical re-affirmations** of the same
  `sr_channel_reversal` exclusion — the writer keeps journaling a decision that never lands in
  `current_params`
- `current_params`: conf threshold 30, `learn_every_n` 10, `max_consecutive_losses` 3,
  `max_open_trades` 8, daily-loss and max-DD limits both **−99999** (disabled)
- daytrade EV **−2.60** (n=94, WR 41.5% — high −1.12/n32, mid −4.07/n39, low −2.18/n23; **every**
  confidence band negative). By regime: RANGE −1.59/n62, TREND_BEAR −5.66/n22, TREND_BULL −2.17/n10
- scalp EV **−0.15** (n=388, WR 40.7% — high −0.71/n54, mid −0.39/n207, **low +0.48/n127** is the
  only non-negative band)
- swing `ready: false` (sample 0, min_required 10)

**Bit-identical to 09-01, 08-31, 08-28 (both runs) and 08-26.**

---

## 8. Strategy pages / tier — **no update**

Zero closed fills ⇒ no cell gained or lost a trade, and no promote/demote threshold is touched.
`sync_kb_index --write` changed **only the date stamp** (2026-09-01 → 2026-09-03, the known JST/UTC
offset); the portfolio block content is byte-identical, confirming no tier movement.

The three live-path strategies are unchanged from 08-28 / 08-31 / 09-01:

| strategy | N | WR | PnL | Wilson / BF |
|---|---:|---:|---:|---|
| `usdjpy_carry_dip_accumulator` | 9 | 55.6% | **+84.0** | 26.7 / 15.7 |
| `price_shock_rev_aud_jpy_h1_long` | 3 | 66.7% | **−102.5** | 20.8 / 9.9 |
| `price_shock_rev_eur_gbp_h1_long` | 3 | 0.0% | −9.8 | 0.0 / 0.0 |

🔴 **carry-dip remains the book's only materially positive cell, and its declared 150p SL vs the
measured 18.8p replacement is still the highest-value open item** (`_1H_PRESERVE_SLTP` unregistered,
R3 adjudication pending).

Book-wide context (44 strategies, `include_shadow: false`, `exclude_xau: true`, from 2026-04-08):
only **11 of 44** carry positive PnL and 8 of those have N≤7. The five deepest holes are
`price_shock_rev_aud_jpy_h1_long` −102.5 (N=3), `session_time_bias` −67.8 (N=30),
`vwap_mean_reversion` −63.1 (N=11), `wick_imbalance_reversion` −63.0 (N=14),
`trendline_sweep` −49.7 (N=32 at **WR 62.5%** — a payoff-asymmetry signature, not a hit-rate one).

---

## 9. Lint

1. **Numeric consistency ✅** — `wiki/index.md`, `wiki/log.md` and this file all carry
   N=579 / WR 43.5% / PnL −611.1 / DD 1198.8. No cross-page disagreement.
2. **`tier_integrity_check --check` → PASS** (exit 0). 2 warnings, both known:
   `hull_donchian_fade` QUICK_HARVEST_EXEMPT outside ELITE/PAIR_PROMOTED; no strategy file for
   `ob_retest`.
   🔴 **The "legacy dead inline" INFO contradiction is now sharper than ever.** All 14 INFO entries
   claim "no production firing in 30+ days"; `ob_retest` fired **twice today** — GBP_USD BUY at
   **09-02 08:52:19Z and 08:52:32Z**, i.e. **~11.5 hours before this pull**, and they are the first
   two `shadow_emit_no_lot` rows in §3. Reported as 12.2 days out on 09-01, it is now **half a day**
   out. Third name in the series after `ihs_neckbreak` (08-26→08-28) and `ob_retest` (09-01); same
   root cause: **the checker reads local state, not the Render API.**
3. **`sync_kb_index --check` → FAIL (exit 1)**; `--write` applied, re-check **PASS** (exit 0).
   ⚠️ The only diff was the date stamp (2026-09-01 → **2026-09-03**) — the tool stamps JST while
   this run's data date is UTC 2026-09-02, so the portfolio block reads one day ahead. Known tz
   quirk, content byte-identical. This breaks the two-run `--write`-free streak (08-31, 09-01)
   **for a cosmetic reason only**. ⚠️ Had to be applied **twice** this run — see §11.
4. **Broken wikilinks** (path-aware resolver, 1,988 files):
   - `wiki/index.md` **7** — `2026-04-27`, `2026-04-29`, `ma-regime-switch`, `ma-trend-perfect`,
     `mqe-gbpusd-fix`, `ob-retest`, `vsg-jpy-reversal`. **Identical set to 08-23 / 08-26 / 08-28 /
     08-31 / 09-01** (all 7 re-verified absent from disk this run).
   - `wiki/strategies/` non-MEMORY **1** — `xs-momentum-rsi-tv-phase2-2026-05-13`. Unchanged.
   - KB-wide **76** distinct, of which 29 are MEMORY-style (`project_*` / `feedback_*` /
     `reference_*`, expected to be unresolvable inside the KB) ⇒ **in-KB 47** (09-01: 48).
   - 🟢 **The one genuinely new link flagged on 09-01 has been resolved**:
     `[[kalman-d7-minlot-carveout-prereg-2026-09-01]]` now exists at
     `wiki/decisions/kalman-d7-minlot-carveout-prereg-2026-09-01.md`. That accounts for the 48→47.
   - **This run introduced 0 broken links**; `[[2026-09-02]]` resolves to this file.
   - ⚠️ Self-correction: the first pass of this scan reported `wiki/index.md` = **0** broken, which
     was a defect in the scan — a stem-keyed dict silently dropped every duplicate `index.md` path,
     so `wiki/index.md` was never read. Re-run over an explicit file list; the 7 above are the
     verified figure. **No prior run's number was wrong — this run's first attempt was.**
5. **Staleness** — 🟡 audit current-ish (`last_candidate_row` 19:32:14Z = 52.8 min, market-open
   0.9h vs 6h threshold, `status: ok`); 🔴 `/api/oanda/equity` **23.5 days**; 🔴 learner **14.25 days**.
6. ⚠️ **`limit=30` insufficient again — 9th consecutive confirmation.** `limit=30` covers
   **4.64 hours** (09-02 13:53→18:31Z) with **0 live rows**; `limit=800` covers **14.23 days** with
   **12 live rows** (6 trades × sent+filled). Today's "no new fill" conclusion happens to survive at
   limit=30, but *when the last fill was* (08-26, 7.23 days back) is only determinable at the larger
   limit. Per `feedback_audit_limit30_hides_live_fills`.

---

## 10. Process notes

**(a) Continuing gap.** `raw/trade-logs/2026-08-29-pre_tokyo.md` exists but **no 08-29 entry appears
in `wiki/log.md` or in `index.md` Session History** — the 2nd instance of "wiki-daily Phase 3/6
completes partially", first detected 08-26. **No retroactive entry has been fabricated**; recorded
and left as-is. (08-30 has no files at all, which is a Sunday and therefore expected — not an
instance.)

**(b) 🔴 NEW — a concurrent repo process destroyed this run's work mid-flight, and it was silent.**
While this run was writing, another automated process executed `git reset` followed by
`git pull --ff-only origin main` at **2026-09-03 11:18:15–16 JST** (`git reflog`:
`f18c3b0b → 874fb3c8`, 17 commits). The effect on this run:

- `knowledge-base/raw/trade-logs/2026-09-02.md` (this file) — **deleted**, had to be rewritten
- `wiki/index.md` — **fully reverted** to the 09-01 state, all edits lost, re-applied
- `wiki/log.md` — **survived**, because its append happened after 11:18

The Write tool reported success and the file was genuinely gone minutes later, so **nothing in the
run signalled the loss**; it was caught only by a post-write verification pass. This is a live
instance of `feedback_concurrent_agent_repo_hazard`. **Mitigation adopted for this run**: re-apply,
then verify every artefact on disk at the end (§11 close-out). **Recommendation**: wiki-daily should
verify its own outputs exist after writing, and no automated job should `reset`/`pull` this
worktree while another agent holds uncommitted work.

**(c) 🟢 The clobbering pull was not all cost — it carried the answer to §3.** Commit `c41a4bdc`
(PR #220) and `wiki/lessons/lesson-shadow-emit-dedup-writetime-2026-09-02.md` arrived in that pull
and are what let the zero-unit emission be closed today rather than carried a 6th time.

---

## 11. Open items

**Closed this run**
- ✅ **zero-unit emission (open since 08-23)** — a deliberate lot-unassigned tracking marker,
  self-described by PR #220; `units: 0` must never be read as an order size

**Open**
- 🔴 `usdjpy_carry_dip_accumulator` declared 150p SL vs measured 18.8p — **highest-value open item**
- 🔴 `rnb_usdjpy` code read — **6 runs outstanding, top follow-up**
- 🔴 `hedge_block` shadow→live adjudication (rule:R2/H2) — user decision
- 🔴 `/api/oanda/equity` 23.5-day ingestion gap
- 🔴 08-21→08-26 105.5h outage — cause never established (no recurrence this window)
- 🔴 Price-Shock family demote decision
- 🆕 ⚠️ **Verify PR #220's write-time dedup flag is actually setting `dedup_violation=1`** on the
  13–32s near-duplicate shadow pairs seen in §3 — the audit endpoint does not expose the column, so
  a `demo_trades` query is needed (narrower successor to the closed zero-unit item)
- 🆕 ⚠️ broker NAV −¥240.00 with no position and no fill — recorded, re-check next run
- 🆕 🔴 concurrent `reset`/`pull` clobbering in-flight wiki-daily work (§10b)
- 🆕 ⚠️ `/api/demo/status` `block_counts` is top-30 truncated — use `/api/demo/block-counts` for
  every future share calculation
- ⚠️ JPY ledger ¥46,345.02 drift; `attribution` friction not subtracted; learner blacklist no-op;
  `API_AUTH_TOKEN` watchdog gap; `sr_anti_hunt_bounce` metadata corruption
- ⚠️ `tier_integrity_check` reads local state, not the Render API (now contradicted by a same-day
  firing)
