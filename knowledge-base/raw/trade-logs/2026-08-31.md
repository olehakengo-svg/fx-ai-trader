# 2026-08-31 Daily Trade Log (wiki-daily-update)

**Pull times**: demo/stats・learning・risk・audit = 2026-08-31 ~15:40 UTC / status・block-counts = **17:14:38 UTC**
**Clock check**: ✅ local UTC 17:14:38 vs server `row_freshness_now` 17:14:38.342 — **skew 0s**. The 08-26 ~5h40m skew does not reproduce (2nd consecutive non-reproduction).
**Audit window**: `limit=1000` = 2026-08-12T21:16:11Z → **2026-08-31T13:32:50Z** (18.68 days, `total` 16,080; `execution_audit_count` 16,095 at 17:14Z).

---

## 1. Realized book — flat for the 3rd consecutive snapshot

| Field | Value | vs 08-28 (evening) |
|---|---:|---|
| N (live_count / total) | **579** | flat |
| W / L / BE | 252 / 292 / 35 | flat |
| WR | **43.5%** | flat |
| decided WR | **46.3%** | flat |
| EV | **−1.06** | flat |
| total_pnl | **−611.1 pip** | flat |
| Wilson lower | 42.2 | flat |
| Wilson BF lower | 39.4 | flat |
| avg R | 0.12 | flat |

**Every realized field is bit-for-bit identical to both 08-28 runs.** Zero closed fills in ~3 days
(08-28 11:52Z → 08-31 17:14Z).

⚪ **This is NOT a repeat of the 08-21→08-26 freeze.** The engine is demonstrably alive and the
*shadow* book is advancing:

- `shadow_count` 12,557/12,558 → **12,655 (+97/98)**
- `oanda_audit.total` 15,966 → **16,080** (+114); `execution_audit_count` **16,095** at 17:14Z
- `engine_tick_age_sec` **0.1s**, 24 running modes, stalest mode `daytrade_1h_audusd` 60.6s
- `main_loop_alive` / `watchdog_alive` / `sltp_checker_active` = true, `main_loop_restarts` **1**
- OANDA heartbeat `ok`, latency 109ms, `last_check` 17:13:57Z

✅ **The 08-28 `shadow_count` decrement anomaly (12,558 → 12,557) did not recur** — the counter
advanced +97/98 this window. Per the 08-28 note ("次回実行で2度目の減少があれば本物"), one
observation stands alone; **closing as a non-event, no diagnosis performed.**

⚠️ Staleness caveat on the freshness rows: `last_candidate_row_age_sec` **775.3s (12.9 min)** and
`last_trade_row_age_sec` **1616.6s (26.9 min)**, both `status: ok`. Late-Sunday/Monday-open UTC —
not flagged as a fault, but noticeably slower than the 5.8–6.1s seen on 08-28.

---

## 2. 🔴🔴 Headline: the risk dashboard flipped positive, and it is **pure window arithmetic**

This is the single most misreadable number in today's pull. Read the caveat before the values.

| Field | 08-28 | **08-31** | Δ |
|---|---:|---:|---|
| `effective_date_from` | 07-29T11:50:31 | **08-01T13:41:34** | window **rolled** |
| `n_trades_post_filter` | 16 | **13** | **−3** |
| net_pnl (30d) | −58.4 | **+94.3** | **+152.7** |
| Kelly edge | ~0.0 (gate: −0.2006) | **+0.6709** | flipped |
| Kelly recommendation | — | **half_kelly 0.128** | first non-zero in this log |
| DSR overall | 0.0 (haircut 100%) | **0.5624** (haircut **15.1%**) | flipped |
| Sharpe observed | −0.0927 | **+0.3067** | flipped |
| MC median max DD | 4.66% | 19.34% | — |
| MC worst-case DD99 | 9.91% | 38.66% | — |
| ruin probability | 0.0% | **0.0%** | flat |

🔴 **There were ZERO new live fills in this window.** The last fill is still
`price_shock_rev_aud_jpy_h1_long` AUD_JPY oanda **#709537** (2026-08-26 14:59:26Z). Not one new
trade entered the sample. The entire +152.7p swing is **three losing trades ageing out of a
30-day rolling window** — the window start moved 07-29 → 08-01 and n fell 16 → 13.

⇒ **This is not edge recovery. It is the same window-arithmetic illusion flagged on 08-23 and
twice on 08-28 — now the 4th consecutive instance, and by far the largest.** Prior instances moved
edge by +6–7pp on one trade; this one flips every headline risk statistic's *sign* on **zero**
trades.

🟢 **Corroboration that the system itself does not believe it**: three independent internal
signals remain in their defensive state, unchanged:

1. `dd_status.defensive_mode` = **true**, `lot_multiplier` = **0.2** (unchanged)
2. `dd_pips` = **1198.8** (= 119.9% legacy $1000 basis) — **flat**, `dd_pct` 0.097
3. 🔴 **The live agg-Kelly gate still reports a NEGATIVE edge**: all 6 `blocked` rows in the new
   slice read **`agg_kelly=-0.315<0`**.

⚠️ **Two estimators now openly disagree**: `/api/risk/dashboard` reports Kelly edge **+0.6709**
while the production sizing gate reports **−0.315** at the same moment. They read different
populations (the dashboard's 3-strategy / n=13 rolling-30d live slice vs the gate's aggregate
book). **Recorded as an observation only — not diagnosed this run.** The operative number for
capital deployment is the gate's, and it is negative.

⚠️ **`attribution` friction bug persists and is now materially misleading**: returns
`gross_pnl = net_pnl = alpha = 94.3` while `friction = 42.5` (`avg_friction_per_trade` 3.27).
Friction is computed but never subtracted. Previously this inflated a *negative* window
(gross = net = −58.4, friction 71.3); it now inflates a *positive* one. If friction were applied,
the 30d net would be **+51.8, not +94.3** — a 45% overstatement of the only positive window in
this log. Existing, unresolved, **not diagnosed this run**.

### 30d window composition (n=13)

| Instrument | n | PnL | mean |
|---|---:|---:|---:|
| USD_JPY | 9 | **+84.0** | +9.33 |
| AUD_JPY | 1 | +20.1 | +20.1 |
| EUR_GBP | 3 | −9.8 | −3.27 |

`var_cvar` (n=13): VaR95 12.4 / VaR99 13.12 / CVaR95 13.3, mean +7.25, std 23.65,
`insufficient: true`.
`correlation`: all cross-pairs **null** (insufficient overlap), `flagged_pairs` empty.
Per-strategy Kelly: **all three `insufficient`** (n = 9 / 3 / 1) — including
`usdjpy_carry_dip_accumulator` at n=9. Per-strategy DSR: only carry-dip computes
(**0.5193, haircut 5.2%, `is_significant: false`**, z=0.048).

⇒ **Nothing in this window is statistically significant.** `dsr_overall.is_significant` = false,
`small_sample_lowconf` = true, every per-strategy Kelly insufficient.

---

## 3. OANDA audit — 0 new live fills

**New slice** (08-28T11:46Z → 08-31T13:32Z, **116 rows**):

| bridge_status | n |
|---|---:|
| skipped (`shadow_tracking`) | **110** |
| blocked (`agg_kelly=-0.315<0`) | **6** |
| sent | **0** |
| filled | **0** |

- **Live fills this window: 0.** Cumulative in the 18.68-day pull: **10 sent / 10 filled / 10 real
  oanda ids / false-sent 0** — the 2026-07-02 accept/reject contract holds.
- Instruments: USD_JPY 37 / EUR_USD 27 / GBP_USD 19 / EUR_JPY 14 / AUD_JPY 12 / GBP_JPY 7
- Top entry_types: `wick_imbalance_reversion` 18 / `dt_sr_channel_reversal` 12 /
  `dual_sr_bounce` 11 / `london_breakout` 9 / `xs_momentum_rsi` 8 / `sr_break_retest` 6
- **live rate 0.0%** — lowest in this log (prior floor was 0.5% on 08-26)
- **zero-unit emission: 23/116 = 19.8%** of the new slice (all `bridge_status=skipped` ⇒ live
  exposure zero). Property unchanged since 08-23, **still undiagnosed**.

⚠️ **`limit=30` would again have produced a materially incomplete picture** — measured, not
assumed: `limit=30` spans **5.76 hours** (08-31 07:47→13:32Z) and contains **0 live rows**;
`limit=1000` spans **18.68 days** and contains **20**. The headline "0 new fills" happens to be
correct today, but only `limit=1000` establishes *when* the last fill was and that 10 fills sit in
the recent window. **`feedback_audit_limit30_hides_live_fills` — 7th consecutive confirmation.**

---

## 4. Block counters (single-pull shares only)

Per the 08-28 standing rule (`/api/demo/block-counts` is a **rolling/windowed** counter, never
cumulative-since-boot): **absolute values are not differenced across pulls; only within-pull
shares are reported.** Total blocks **8,821** over **33,473** ticks / 24 modes.

| family | blocks | share | 08-28 evening share |
|---|---:|---:|---:|
| `r2_shadow_demoted_cell` | 2,540 | **28.8%** | 25.5% |
| `hedge_block` | 2,501 | **28.4%** | 33.8% |
| `order_bar_dedup` | 2,035 | **23.1%** | 22.4% |
| `direction_filter` | 1,477 | **16.7%** | 16.3% |
| `score_gate` | 236 | 2.7% | — |
| `conf<30` | 32 | 0.4% | — |

Shares are again **broadly preserved** across an independently re-windowed sample — consistent
with the 08-28 finding. `hedge_block` has ceded the #1 slot to `r2_shadow_demoted_cell`, but the
two are within 0.4pp and this is **not** evidence of a gate change.

### 🔴 `rnb_usdjpy:direction_filter` — 4th consecutive confirmation

| pull | blocks | ticks | ratio |
|---|---:|---:|---|
| 08-26 | 2,624 | 2,622 | 1.0008 |
| 08-28 05:00Z | 2,624 | 2,622 | 1.0008 |
| 08-28 11:52Z | 630 | 630 | 1.0000 |
| **08-31 17:14Z** | **1,477** | **1,509** | **0.9788** |

Rejecting **97.9% of all ticks** in a freshly re-windowed sample. A selective filter does not do
this. Consistent with the known `compute_rnb_signal` WAIT-path `entry: 0` bug.
🔴 **The code read is STILL not done — 4 consecutive runs at the top of the open-follow-up list.**

### 🔴 `hedge_block` counts shadow against live — 3rd independent confirmation

`/api/demo/status` `open_trades` = **3 rows, every one `is_shadow: 1`**
(`london_fix_reversal` GBP_USD / `gbp_deep_pullback` GBP_USD / `vsg_jpy_reversal` EUR_JPY),
while the OANDA heartbeat reports `open_trade_count: 0` and `margin_used: ¥0`.

**The book has turned over completely again** (6 rows → 5 rows → **3 rows**, all different
strategies) and the property is preserved. This re-confirms the 08-28 code-level finding
(`modules/demo_trader.py:4915-4928` iterates `_mode_inst_live + _mode_inst_shadow`) by pure
measurement. **Not a bug — a decision (rule:R2/H2), awaiting user adjudication. No code touched.**

---

## 5. Learning API — frozen, 6th consecutive snapshot

- Latest adjustment **id=93** (2026-08-19 14:25:37) — **unchanged for 6 snapshots**
- `entry_type_blacklist` = **`[]`** ⇒ **no-op unresolved since 06-30**
- `current_params`: confidence_threshold 30 / max_open_trades 8 / max_consecutive_losses 3 /
  learn_every_n 10 / daily_loss_limit_pips −99999 / max_drawdown_pips −99999
- 30 journaled adjustments; ids 90–93 are **byte-identical re-affirms** of the same
  `sr_channel_reversal` exclusion (WR 25.0%, EV −0.98)

| mode | ready | n | WR | EV | by_conf (ev / n / wr) |
|---|---|---:|---:|---:|---|
| daytrade | true | 94 | 41.5% | **−2.60** | high −1.12/32/46.9 · mid −4.07/39/38.5 · low −2.18/23/39.1 |
| scalp | true | 388 | 40.7% | **−0.15** | high −0.71/54/35.2 · **low +0.48/127/46.5** · mid −0.39/207/38.6 |
| swing | **false** | 0 | — | — | min_required 10 |

**Bit-identical to both 08-28 runs and to 08-26.** Every daytrade confidence tier is negative;
scalp's only non-negative tier is `low` (+0.48) — the inversion noted in prior runs persists.

---

## 6. Strategy / cell state — no changes

Zero closed fills ⇒ **no cell gained or lost a trade**; no promote/demote threshold is touched.
**No strategy page updated, no tier change.** The two live-relevant cells are unchanged from both
08-28 runs:

| cell | N | WR | PnL | wf_h1 | wf_h2 |
|---|---:|---:|---:|---:|---:|
| `usdjpy_carry_dip_accumulator` | 9 | 55.6% | **+84.0** | +20.05 | +0.76 |
| `price_shock_rev_aud_jpy_h1_long` | 3 | 66.7% | **−102.5** | +0.60 | −51.55 |
| `price_shock_rev_eur_gbp_h1_long` | 3 | 0.0% | −9.8 | −4.90 | −2.45 |

Best cumulative (44 types): `usdjpy_carry_dip_accumulator` +84.0 / `orb_trap` +23.2 /
`post_news_vol` +19.0 / `ema_pullback` +17.8 / `vol_momentum_scalp` +11.1.
Worst: `price_shock_rev_aud_jpy_h1_long` −102.5 / `session_time_bias` −67.8 /
`vwap_mean_reversion` −63.1 / `wick_imbalance_reversion` −63.0 / `trendline_sweep` −49.7.

🔴 **`usdjpy_carry_dip_accumulator` remains the book's only materially positive cell (+84.0, all 9
live fills closed), and its declared 150p SL vs measured 18.8p replacement is still the highest-
value open item** (`project_carrydip_sl_contract_and_slhit_label_2026_08_05`). Unchanged.

---

## 7. Carried / unresolved

| item | state |
|---|---|
| 🔴 `/api/oanda/equity` stale | **21 days and worsening** — last point still 2026-08-10T07:20:34Z, `count` still **928**, `total_pl_jpy` −80,686. Progression 15d → 18d → **21d** |
| 🔴 JPY NAV ledger drift | **¥46,105.02** — ledger ¥324,450.50 vs broker NAV ¥278,345.48. Unchanged. Sizing unaffected (0.20× tier saturated); reporting understates broker DD |
| 🔴 `rnb_usdjpy:direction_filter` ≈ unconditional | 4th confirmation, code read not done |
| 🔴 `hedge_block` shadow→live suppression | 3rd confirmation, **user decision open**, no code touched |
| 🔴 08-21→08-26 outage cause | **never established** (`main_loop_restarts` still 1); no recurrence in this window |
| 🔴 Price-Shock family demote decision | open — AUD_JPY cell still −102.5 at N=3 |
| 🔴 carry-dip 150p SL vs 18.8p measured | open — highest-value item |
| ⚠️ `attribution` friction not subtracted | persists; now inflating a positive window by 45% |
| ⚠️ learner `entry_type_blacklist` no-op | unresolved since 06-30 |
| ⚠️ zero-unit emission | 19.8% of new slice, undiagnosed |
| ⚠️ `sr_anti_hunt_bounce` meta corruption | carried |
| ⚠️ `API_AUTH_TOKEN` watchdog gap | carried |
| ✅ `shadow_count` decrement | **closed as non-event** — did not recur |
| ✅ clock skew | **not reproducible** (0s), 2nd consecutive |

---

## 8. Verdict

**A zero-information window on the realized book, carrying one high-risk reporting artifact.**

Three days produced **no closed trades and no live fills**, so nothing was learned about any
strategy's edge. The shadow book advanced normally (+97 rows) and the engine is healthy, so the
absence of fills is **suppression, not outage** — and the suppressors are the two gates already
filed for decision (`hedge_block` counting shadow positions, and an agg-Kelly gate reading
−0.315).

The one thing that *changed* is `/api/risk/dashboard`, which now reports positive Kelly, positive
Sharpe and an 85%-of-DSR-surviving haircut. **This must not be propagated as edge recovery.** It
is three losing trades ageing out of a rolling window on zero new evidence, it is inflated a
further 45% by the unfixed friction-attribution bug, and the production sizing gate — which is
what actually deploys capital — continues to read a negative aggregate edge and hold the book at
0.2× in defensive mode.
