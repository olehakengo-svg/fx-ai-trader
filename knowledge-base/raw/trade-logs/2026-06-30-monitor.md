# Trade Monitor Alerts: 2026-06-30

## Alert: 02:00 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=298, daytrade_eurgbp:hedge_block=268

### 診断
## 原因分析

### 推測（ログ・カウントから）

**① `rnb_usdjpy:direction_filter=298`（最大異常）**
- RNBストラテジーがUSDJPYで片方向（おそらくBUY or SELL一方）のシグナルを大量生成しているが、direction_filterで全てブロックされている
- 上位時間軸（MTF）との方向不一致が持続しており、フィルターが解除されないまま累積している可能性が高い
- ログに大量のR2_SHADOW_DEMOTEがUSDJPYで発生しており、**USDJPYそのものの相場環境がシステムの想定レンジ外**（強トレンドまたは急変動）になっていると推測

**② `daytrade_eurgbp:hedge_block=268`**
- EURGBPで既存のヘッジポジションが長時間解消されていない、またはヘッジ条件が長期間トリガーされ続けている
- `daytrade_eur:hedge_block=184`、`daytrade_gbpjpy:hedge_block=77`も同系統で高く、**GBP・EURクロス全体でヘッジブロックが連鎖**している構造的問題の可能性

**③ R2_SHADOW_DEMOTE多発**
- `ema_trend_scalp`、`fib_reversal`等がUSDJPYで短時間に連続demoteされており、相場のレジーム判定が安定していない（MTF_GATEのconflictログと一致）

---

### 確認事項（要実機確認）

- [ ] USDJPYの現在のD1・H4レジーム判定値（`d1`/`h4`の値がflipしていないか）
- [ ] RNBストラテジーのdirection_filterのリセット条件・TTLが正常に機能しているか（カウンターがリセットされずに積み上がっていないか）
- [ ] EURGBPのブローカー側オープンポジション実態（ヘッジポジションが実際に残存しているか、それともフラグの不整合か）
- [ ] `main_loop_restarts=1` のタイミングとblock_count急増のタイミングが一致しているか（再起動後にカウンターがリセットされていない疑い）
- [ ] `daytrade_gbpusd:gbp_asia_flash_crash=123` など複数モードで閾値超えのブロックが同時多発しており、**特定の時刻帯に外部イベント（経済指標・フラッシュクラッシュ）が発生していないか**をチャートで確認

---

## 対処方針

- **即時対応**
  -

## Alert: 07:40 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=417, daytrade:hedge_block=243

### 診断
## 原因分析

### 【推測】

**① `rnb_usdjpy:direction_filter=417`（最重要）**
- USDJPYが強いトレンド相場（上昇継続）にあり、`rnb`戦略の方向フィルターが逆張りシグナルを大量に弾いている
- MTFログで `mtf=trend_up_weak, d1=1, h4=1` が繰り返されており、上位足が買いトレンドを示しているにもかかわらず `vol_momentum_scalp` がSELLシグナルを出し続けている構造的な逆張り連発状態
- カウントが417と突出して高く、**他のブロック値と比較して明らかに異常蓄積**。リセット機構が機能していないか、フィルター判定がループ内で多重カウントされている可能性

**② `daytrade:hedge_block=243`**
- daytradeモード（USDJPY 15m）で既存ポジションに対するヘッジ判定が頻発
- トレンド方向と逆のシグナルが継続発生し、ヘッジブロックが積み上がっている
- `last_signal.confidence=20, signal=WAIT` からも、エントリー判断が迷走していることが示唆される

**③ `R2_SHADOW_DEMOTE` の連続発生**
- `bb_rsi_reversion x USD_JPY` および `ema_trend_scalp x GBP_USD` が数分おきに降格を繰り返している
- セルが降格→復帰→再降格のループに入っており、R2昇格条件を安定して満たせていない可能性

---

### 【確認事項】（推測でなく要検証）

- [ ] `rnb_usdjpy:direction_filter` のカウンターがセッションリセット・日次リセットされているか（累積カウントかどうか）
- [ ] `direction_filter` の判定ロジックがループ1回あたり複数回呼ばれていないか（多重計上バグの有無）
- [ ] `daytrade:hedge_block` の発生タイミングが `rnb_usdjpy:direction_filter` と時間的に連動しているか
- [ ] `bb_rsi_reversion` の R2昇格→降格サイクルの閾値設定が現在の相場ボラティリティ帯に適合しているか
- [ ] `main_loop_restarts=1` の再起動タイミングとブロック急増の時刻が一致しているか（再起動によるカウンター未リセットの可能性）
- [ ] MTFモニターで `vol=squeeze` が継続しているか、現在も解消されていないかを確認

---

## 対

## Alert: 13:49 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=405, scalp_eur:r2_shadow_demoted_cell=250

### 診断
## 原因分析

### 【推測】異常の根本原因

**rnb_usdjpy:direction_filter=405（最大の異常値）**
- `rnb_usdjpy`モードがUSD/JPYの一方向シグナルを大量発生させているが、`direction_filter`で全件遮断されている状態
- 相場が強トレンド（一方向継続）の中でRnBロジックが逆張りシグナルを連続出力し続けている可能性が高い
- または`direction_filter`の参照するバイアス判定ロジック（MTF/MA等）がフリーズ・固着しており、常に同方向をNGと判定し続けている可能性

**scalp_eur:r2_shadow_demoted_cell=250（次点の異常値）**
- EUR系シャドウセルが`r2_shadow_demoted_cell`によって大量にデモート＝ブロックされ続けている
- `fib_reversal×EUR_USD`や`ema_trend_scalp×EUR_USD`のデモートがログに頻出しており、EUR/USDのシャドウトラッキング全体が機能不全に陥っている
- scalp_5m_eur(97)、scalp(91)、scalp_5m(27)でも同系統のカウントが高く、EUR系セル全体に共通した抑制要因が存在する

---

### 【確認事項】

#### rnb_usdjpy:direction_filter について
- [ ] `direction_filter`の判定ソース（MTFバイアス・MA方向・上位足トレンド判定）が現在どの値を返しているか確認
- [ ] カウントが単調増加しているか、それとも特定時間帯に集中しているか（ログ時系列を確認）
- [ ] `rnb_usdjpy`モードのシグナル発火頻度（interval設定）が誤って短く設定されていないか
- [ ] `direction_filter`の判定ロジックに前回状態キャッシュの残留・更新漏れが起きていないか

#### scalp_eur:r2_shadow_demoted_cell について
- [ ] `R2_SHADOW_DEMOTE`の昇格条件（r2スコア閾値、セル復帰ロジック）が現在のEUR/USD相場環境で達成不可能な水準になっていないか
- [ ] EUR/USDのボラティリティ・レンジが縮小し、シャドウセルがr2昇格基準を満たせない状態が継続していないか
- [ ] デモート後の復帰クールダウン時間が正常に機能しているか（永続ブロック状態になっていないか）
- [ ] `main_loop_restarts=1`の再起動タ
