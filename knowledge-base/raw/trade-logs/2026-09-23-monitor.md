# Trade Monitor Alerts: 2026-09-23

## Alert: 08:52 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=363, scalp:r2_shadow_demoted_cell=217

### 診断
## 原因分析

### 推測（状況証拠から高確率）

**① `scalp:r2_shadow_demoted_cell=217` — 異常多発**
- `ema_trend_scalp` と `sr_channel_reversal` の2セルがほぼ連続してdemoteされ続けている
- EUR_USD・USD_JPYで同一セルが繰り返しブロックされており、一度demoteされたセルが復帰→再demoteのループに入っている可能性が高い
- 相場がレンジ/チョッピー状態でR2スコアが閾値付近を反復しているか、セルのプロモーション条件が緩すぎて即demoteされるサイクルになっている

**② `rnb_usdjpy:no_signal=363` — 異常多発**
- rnb_usdjpyモードがシグナルを全く生成できていない
- フィルタ（スコアゲート・セッション・スプレッド等）以前の段階で候補が0になっている可能性が高い
- USD/JPY自体はR2_SHADOW_DEMOTEのログに出ているため、データ欠損よりもモデル/ロジック起因の可能性が高い

---

### 確認事項（推測と区別）

| 確認項目 | 対象 | 目的 |
|---|---|---|
| R2スコアの時系列推移 | `ema_trend_scalp`, `sr_channel_reversal` × EUR_USD | 閾値付近での振動か、完全低下かを切り分け |
| demoteのプロモーション条件・ヒステリシス設定値 | R2_SHADOW_DEMOTEロジック | 復帰閾値がdemote閾値と近すぎないか |
| `rnb_usdjpy`の内部スコア・特徴量ログ | rnb_usdjpyモード | no_signalがどのステップで発生しているか特定 |
| USD/JPYの市場ボラティリティ・スプレッド状況 | 外部データ | 相場環境起因かシステム起因かの判別 |
| `engine_tick_stalest_mode: daytrade_1h_eurgbp=66秒` | エンジンヘルス | 関連モードのスレッド遅延・詰まりがないか |
| `scalp_eur:unknown_type:engulfing_bb_lvn_london_ny` | シグナル定義 | 未定義シグナルタイプが混入している経緯の確認 |

---

## 対処方針

### 即時対応
- **R2_SHADOW_DEMOTEのループ監視強化**：同一セルが一定時間内に閾値以上demoteされた

## Alert: 14:29 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=335

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:no_signal=335`** が突出して大きい（他のblock_countsと1桁以上の差）
  - `rnb`ストラテジーがシグナル生成ロジックの入口で335回連続して「シグナルなし」と判定されている
  - 単なる市場条件不一致ではなく、**フィルタ・前提条件の手前で弾かれている**可能性が高い（score_gateやorder_bar_dedupではなく`no_signal`という上流ブロック）
  - `rnb`（レンジ＆バウンス系と推定）はUSDJPYの**現在の相場構造（トレンド相場・ボラティリティ異常・レンジ幅消滅など）と根本的に合致していない**状態が継続している

- **USDJPYでの`order_bar_dedup` + `first_block=consec_loss`の組み合わせ**
  - `xs_momentum_rsi USD_JPY BUY`が同一bar_ts（14:15）で繰り返しブロックされており、**直近の連続損失フラグが立ったまま**当該バーをまたいで再エントリを試み続けている

- **`daytrade_1h`の最遅tick age 65.3秒**
  - 他モードが6秒前後で更新されている中で1hだけが遅延しており、**1hバー生成・受信の処理ボトルネック**が疑われる

### 推測（中確度）

- `rnb_usdjpy:no_signal`の急増タイミングと`daytrade_1h`のtick遅延が連動している場合、**1hレートフィードの遅延がrnbの判定材料（レンジ境界値など）を無効化**させている可能性がある
- EUR_USDの`wick_imbalance_reversion score`が-0.38〜-0.39で固定的（変動が微小）であり、**スコア計算のキャッシュ固着または特徴量更新停止**の可能性がある

---

## 確認事項

### 最優先

1. **`rnb_usdjpy`の`no_signal`増加開始タイムスタンプ**を取得し、直前に設定変更・フィード断・相場イベントがなかったか確認
2. **`rnb`ストラテジーの`no_signal`判定条件**を確認
   - レンジ幅の最小閾値、ATR条件、ボラティリティフィルタなどが現在のUSDJPYの値動きに対してOut of Rangeになっていないか
3. **`

## Alert: 19:16 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=834, scalp:r2_shadow_demoted_cell=485, daytrade:order_bar_dedup=362, scalp_eur:r2_shadow_demoted_cell=352, scalp_5m_eur:r2_shadow_demoted_cell=266

### 診断
## 原因分析

### 推測（高確度）

- **`r2_shadow_demoted_cell` 多発（scalp系・daytrade系横断）**
  - `bb_rsi_reversion × USD_JPY` および `fib_reversal × EUR_USD` のシャドウセルが繰り返しデモートされている
  - R2層のシャドウ追跡セルが昇格条件を満たせない状態が長時間続いており、ブロックが累積している
  - scalp・scalp_5m・scalp_eur・scalp_5m_eur・scalp_5m_gbpで計1500件超と、スキャルプ系全体に波及しており、特定戦略の問題ではなくR2評価ロジックかフィード品質の問題と推測

- **`rnb_usdjpy:no_signal=834`**
  - RnBモード（レンジ＆ブレイク）がUSD/JPYで信号を全く生成できていない
  - `r2_shadow_demoted_cell` の多発と同一通貨ペアであり、USD/JPYのR2セル評価失敗が上流でno_signal化している可能性が高い

- **`daytrade:order_bar_dedup=362`**
  - 同一バーでの重複注文シグナルが大量に発生している
  - `engine_tick_stalest_age_sec=60.5`（`daytrade_1h_audjpy`）との関連で、1hモードのティック遅延が同一バー判定を誤らせている可能性

- **DT含み益保護の頻発ログ**
  - BUY `b022be31` / SELL `2f606cba` の2ポジションが長時間ホールドされており、SIGNAL_REVERSEが繰り返し無効化されている
  - これ自体は設計通りの動作だが、逆張りシグナルが頻発していること自体は相場のボラティリティ上昇を示唆

---

### 推測（中確度）

- `candidate_row` の鮮度が38分（`age_sec=2290`）と古い
  - 候補行の更新が止まっており、これがno_signal・block累積の根本原因である可能性
  - 市場オープン換算で0.6hとの記載があるため、市場クローズ近辺またはデータフィード側の遅延が疑われる

- `daytrade_1h_audjpy` のティック最遅60秒はスレッド競合またはデータ取得タイムアウトによるもの

---

## 確認事項

| 優先度 | 確認項目 |
|--------|----------|
| 🔴高 | `candidate_row` が38分更新され

## Alert: 22:30 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=310

### 診断
## 原因分析

### 【推測】rnb_usdjpy:no_signal=310 の主因

- `rnb_usdjpy` モードにおいて、シグナル生成条件が長時間満たされていない
- ログを見ると USD_JPY 向けセル（`bb_rsi_reversion`, `engulfing_bb`, `sr_channel_reversal`）が **R2_SHADOW_DEMOTE により連続ブロック**されており、rnb モードへ渡るシグナル候補が枯渇している可能性が高い
- `no_signal` カウントが 310 と他のブロック種別に比べ突出しており、単発のフィルタ弾きではなく **構造的にシグナルが発生していない状態**と見られる

### 【推測】副次的背景

- `scalp:r2_shadow_demoted_cell=116`, `scalp_eur:r2_shadow_demoted_cell=58` など **r2_shadow_demote が全体的に多発**しており、現在の相場環境がシャドウ追跡セルの昇格条件を満たしにくい局面（レンジ・方向感なし等）にある可能性
- `daytrade_eur:score_gate=105`, `SCORE_GATE ny_close_reversal score=-3.00` のログから、**スコアが逆方向に傾いており戦略全体が抑制モード**に入っている
- `freshness_ui` の `candidate_row` age が **44分**と古く、候補行そのものの更新が止まっている可能性

---

## 確認事項（事実確認が必要なもの）

1. **rnb_usdjpy モードのシグナル生成ロジック**が、r2_shadow_demote されたセルに依存していないか（依存していれば直接の因果関係が確定）
2. **candidate_row の最終更新が止まっている原因**（データフィード断・上流パイプライン停止・マーケットクローズ扱いになっていないか）
3. `daytrade_1h_euraud` の stalest_age=49秒が閾値内か、**1h系モードの tick 受信が遅延していないか**
4. **R2_SHADOW_DEMOTE の昇格リセット条件**が直近に発動していないか（大きな値動き後のリセット等）
5. `no_signal` カウントのリセットタイミング（セッション単位か累積か）——310 がどの時間幅で積み上がったか

---

## 対処方針

- **即時確認（優先度：高）**
  - candidate_row の age=44分 を起点に、上流データパイプライン・フィード受信ログを確認し、データ断の有無を
