# 2026-09-07 Daily Trade Log (wiki-daily-update)

**Pull times**: demo/stats・learning・risk・audit = 2026-09-07 **11:50 UTC** / oanda-status・block-counts・equity・demo-status・transactions・regime = **11:52–11:58 UTC**
**Clock check**: ✅ local UTC 11:55:40 vs server `/api/market/regime.ts` 11:55:40.994 — **skew ~0s** (**7th consecutive non-reproduction** of the 08-26 ~5h40m skew). Cross-check `row_freshness_now` 11:58:05 vs local 11:58 — agrees.
**Audit window**: `limit=600` = 2026-08-28T10:46:21Z → **2026-09-07T10:35:00Z** (10.0 days, `total` **16,562**; `execution_audit_count` **16,562** — agrees exactly).
**Orphan check**: `pgrep -f app.py` → **nothing running**; every figure below is Render API (`_db_path: /var/data/demo_trades.db`). Per `feedback_check_orphan_local_app`.
**Audit limit**: pulled at **600**, not the task's default 30 — per `feedback_audit_limit30_hides_live_fills`. 🔁 **The rule earned its keep again, in its purest form yet**: `limit=30` covers **07:28:31Z → 10:35:00Z = 3.1 hours** and reports **0 live / 0 filled**; the 600-row pull reveals **4 filled live rows** and the entire incident below. A run that trusted the default would have logged "no live activity" on the day of the largest execution anomaly in this log's history.
**🆕 Endpoint constraint**: `/api/oanda/transactions` accepts the `?from=<txID>&to=<txID>` integer form (per `reference_oanda_transactions_endpoint`) but **rejects any span >100 IDs with `{"error":"range too wide (max 100)"}`**. Record the cap — the storm below had to be mapped by sampling, not by one pull.
**Gap note**: last wiki-daily was **2026-09-04 11:52Z**; this run closes a **3-day gap** (09-05 / 09-06 had no daily run). Deltas below are vs the 09-04 snapshot.

---

## 0. HEADLINE — a 47-hour, 128,340-transaction runaway retry loop ran against the live broker over the weekend, and the demo book recorded its outcome with the wrong sign

Five things, which must not be conflated:

1. 🔴🔴 **A close-retry loop fired ~64,170 market orders at OANDA over 47h20m** (2026-09-04T21:45:00Z → 2026-09-06T21:04:58Z), every one cancelled `MARKET_HALTED`, at a steady **1 attempt / 2.66s**, with **no backoff and no circuit breaker**. Account transaction IDs advanced **709,602 → 837,942 = 128,340 transactions** in three days, against a normal rate of ~76 per two days (§1).
2. 🔴🔴 **The demo book recorded that trade as a `+11.6 pip` WIN. The broker realized `−¥141 = −14.1 pip`.** A **sign inversion of 25.7 pip** on the system's only materially positive live cell (§2).
3. 🔴 **`usdjpy_carry_dip_accumulator`'s SL/TP contract break is now measured exactly, on two consecutive fills** — and it does not merely shrink the stop, it **inverts the payoff geometry** from the declared R:R 0.53 to a live R:R ~2.3 (§3).
4. 🔴 **The 2nd of those fills stopped out for −24.6 pip at a level the declared stop would never have reached.** USD_JPY is, at write time, **2 pip from that trade's entry** (§3).
5. 🟢 **The 8-run `rnb_usdjpy:direction_filter` 100%-rejection signature has broken** — the key is **absent from all 96 block keys**, replaced by `no_signal` at 57.0% (§7).

Book: N **581→583**, PnL **−685.6→−698.8 pip**. Engine healthy throughout (`engine_tick_age_sec` **0.1s**, `main_loop_restarts` **1**). **Live fills today (09-07 UTC): 0** — 57 audit rows, all `skipped`.

---

## 1. 🔴🔴 NEW TOP ITEM — the weekend `MARKET_HALTED` retry storm

`usdjpy_carry_dip_accumulator` position **#709598** (USD_JPY BUY 1,000u, entered 2026-09-04T17:01:45Z @ **156.122**) reached an exit decision at **2026-09-04T21:45:00Z** — Friday, after the FX week closed. The close could not execute, and the engine never stopped trying.

**The measured shape**, identical at every sampled point in the ID space:

```
MARKET_ORDER  USD_JPY −1000  reason=TRADE_CLOSE
ORDER_CANCEL                 reason=MARKET_HALTED
```

| Sample `from=` | Wall-clock at that ID | 31 tx span | Composition |
|---|---|---|---|
| 709,602 | 2026-09-04T21:45:00Z | — | storm begins |
| 750,000 | 2026-09-05T13:16:20Z | 39 s | 16 `MARKET_ORDER` / 15 `ORDER_CANCEL`, 100% `TRADE_CLOSE`/`MARKET_HALTED` |
| 800,000 | 2026-09-06T07:17:28Z | 37 s | 15 / 16, 100% |
| 830,000 | 2026-09-06T18:12:36Z | 38 s | 15 / 16, 100% |
| 837,700 | 2026-09-06T20:59:13Z | 37 s | 15 / 16, 100% |
| 837,942 | 2026-09-06T21:04:58Z | — | **first successful fill** → storm ends |

**Extent**: 837,942 − 709,602 = **128,340 transactions ≈ 64,170 close attempts** over **47h 20m (170,400 s)**.
**Internal consistency check**: 170,400 s / 64,170 attempts = **1 per 2.66 s**, which reproduces the sampled cadence (31 tx / ~38 s ≈ 1 attempt per 2.45 s) — the extrapolation and the direct measurement agree, so the storm was **uniform for its whole duration**, not a burst.

**Consequences, in order of severity:**

- 🔴🔴 **API-access risk.** Per [[status-volume-keeper-2026-09-01]] this account's REST access is contingent on OANDA Japan **Gold status**, and losing it means the API stops and the token must be reissued. Firing 64,170 rejected orders in one weekend is precisely the behaviour that attracts a broker-side rate-limit or manual review. **This is the reason this item is the run's top finding, ahead of the PnL effects.**
- 🔴 **A −25.7 pip adverse execution.** The exit was wanted at 21:45Z on 09-04 (USD_JPY ≈ **156.238**, implied by the demo book's own recorded exit) and actually filled 47h later at the Sunday reopen, **155.981** — the position was carried, unhedged and unintended, across the entire weekend gap.
- 🔴 **It corrupted the estimator** — see §2.
- ⚪ Not currently active: `lastTransactionID` **837,964** (02:25:20Z) with `openTradeCount` **0**, so nothing has been sent for **9.5 h**. The loop is dormant **because the position closed**, not because anything stopped it.

⚠️ **This will recur.** The trigger is "position open when an exit is wanted while the market is halted". `usdjpy_carry_dip_accumulator` is the most active live cell, fires on USD_JPY H1, and holds ≤24 H1 bars — a hold window that spans Friday close routinely. **A precursor is already on record**: the same pattern appeared at 709,602–709,620 on **09-04T21:45**, and the 09-06T21:01:12Z `weekend_gap_fade` order was also cancelled `MARKET_HALTED`. Order-construction and retry code **still unread** — this section is measurement only, per `feedback_label_empirical_audit`.

🔴 **Related defect — a misclassified error.** `/api/oanda/status.recent_errors[0]` reports for tx **837792**: `"OPEN buy ok but no tradeID: {...}"`. The ledger shows 837792 was a `weekend_gap_fade` USD_JPY +1,000u `CLIENT_ORDER` **cancelled `MARKET_HALTED` at 837793, in the same second**. The order was **rejected, not filled** — there is no orphan position (broker `openTradeCount` 0 confirms). The handler reads a halt-cancel as **"ok"**, so a hard rejection is logged as a partial success. Benign in effect here; misleading in any triage that trusts it.

---

## 2. 🔴🔴 NEW TOP ITEM — the demo book and the broker disagree on the SIGN of trade #709598

Isolated by date-windowing `/api/demo/stats`, which keys on **entry** date:

| Window | N | W | L | PnL |
|---|---|---|---|---|
| `date_from=2026-09-04` | 2 | **1** | 1 | **−13.2** |
| `date_from=2026-09-05` | 1 | 0 | 1 | −24.8 |
| `date_from=2026-09-06` | 1 | 0 | 1 | −24.8 |
| `date_from=2026-09-07` | **0** | 0 | 0 | 0 |

Both trades are `usdjpy_carry_dip_accumulator`. Subtracting: the 09-06 fill is **−24.8**, so the 09-04 fill (#709598) is recorded as **+11.6 = a WIN**. The broker ledger for the same trade:

| | Demo book | Broker (`/api/oanda/transactions`) | Δ |
|---|---|---|---|
| **#709598** (entry 09-04T17:01:45 @156.122) | **+11.6 pip (WIN)** | tx 837943 @ **155.981**, `pl` **−¥141.00 = −14.1 pip (LOSS)** | **−25.7 pip, sign flipped** |
| **#837947** (entry 09-06T22:03:39 @156.274) | −24.8 pip | tx 837950 @ 156.028, `pl` **−¥246.00 = −24.6 pip** | −0.2 pip ✅ (spread/rounding) |

**Mechanism (consistent with §1, not yet code-verified)**: the demo book recorded the exit at the price it *intended* to trade at 21:45Z on 09-04 (156.238 implied), while the broker's fill happened 47h later at the reopen (155.981). The second fill, which closed normally on its stop, reconciles to **0.2 pip** — so the discrepancy is **specific to the halted-exit path**, not a general pip-conversion error.

**Why this outranks the pip count.** Per `feedback_shadow_first_quant_architecture` the demo/shadow book **is** the estimator that promotion decisions run on, and per `reference_oanda_transactions_endpoint` the broker ledger is the **only** primary source for realized PnL. Here the estimator reports a win on a trade the account lost money on, in the one cell that carries the book's positive narrative.

**Broker-basis correction** (⚠️ derived, not an API reading):

| | As reported | Broker-corrected |
|---|---|---|
| `usdjpy_carry_dip_accumulator` | N=11, **6W/5L**, WR **54.5%**, PnL **+70.8**, EV +6.44 | N=11, **5W/6L**, WR **45.5%**, PnL **+45.1**, EV +4.10 |
| Book total | N=583, 254W/294L/35BE, WR 43.6%, PnL **−698.8** | 253W/295L/35BE, WR **43.4%** (decided **46.2%**), PnL **−724.5** |

⚠️ **Scope**: this checks **one** trade against the ledger. Whether earlier `carry_dip` fills carry the same defect is **not established** — every fill that closed during a halt is suspect, and a full ledger reconciliation of all 11 is the follow-up. Do not treat +45.1 as audited.

---

## 3. 🔴 `usdjpy_carry_dip_accumulator` SL/TP contract break — measured exactly, twice, and it INVERTS the payoff geometry

[[usdjpy_carry_dip_accumulator]] declares (page line 51): **`TP = entry + 0.8円` (80 pip) / `SL = entry − 1.5円` (150 pip)**, the SL being an explicit *intervention-gap tail cap*, and the BT that justified the cell (WR 90.9% / PF 4.35) assumed a **−2.0円 hard tail cap**. Both live fills carried brackets attached `ON_FILL` by the order itself (`stopLossOnFill`/`takeProfitOnFill` are present on the `MARKET_ORDER`, so **the substitution happens upstream of OANDA, in order construction**):

| Trade | Entry | SL sent | SL dist | vs 150p | TP sent | TP dist | vs 80p | live R:R |
|---|---|---|---|---|---|---|---|---|
| **#709598** (09-04) | 156.122 | 155.837 | **28.5 p** | **0.19×** | 156.771 | **64.9 p** | 0.81× | **2.28** |
| **#837947** (09-06) | 156.274 | 156.028 | **24.6 p** | **0.16×** | 156.862 | **58.8 p** | 0.74× | **2.39** |
| *declared* | — | — | *150 p* | 1.00× | — | *80 p* | 1.00× | **0.53** |

🔑 **This is the sharpest characterisation this log has had of the defect.** It is not "the stop is too tight". The declared cell is a **high-WR, tail-capped accumulator** (R:R 0.53 — takes 80p, risks 150p, wins ~91% of the time). What is actually deployed is a **R:R ~2.3 breakout-style bracket** — the payoff geometry is *inverted*, which changes the break-even WR from ~65% to ~30% and makes the BT's 90.9% WR structurally unreachable. **Live WR is 54.5% as-booked / 45.5% broker-corrected vs BT 90.9%** — exactly the direction and rough magnitude a 150p→25p stop truncation predicts on a pair whose ATR(14) is **1.126 = 112.6 pip**: the deployed stop is **0.22 × daily ATR**, i.e. inside routine intraday noise.

**#837947 is the first instance where the substituted bracket demonstrably caused a realized loss.** It was closed by `STOP_LOSS_ORDER` (tx 837950) at 156.028, 2h19m after entry, for **−¥246**. The declared 150p stop would have sat at **154.774** — never approached. USD_JPY at write time is **156.254** (`/api/market/regime`), i.e. **2.0 pip below the entry**, and the trade is still inside its declared ≤24 H1 hold window (which runs to ~09-07T22:03Z). ⇒ **the truncated stop converted a roughly flat position into a booked −24.6 pip loss.**

📌 **Correction to the standing hypothesis.** `project_carrydip_sl_contract_and_slhit_label_2026_08_05` and the 09-04 entry frame this as a risk shared across the `daytrade_1h_*` order path. **That framing is now too broad**: the `weekend_gap_fade` order (tx 837792, same account, ~1h before #837947) carried `stopLossOnFill` **154.452** against a market of ~156.0 = **~155 pip**, i.e. the wide stop its design asks for. So the truncation is **not universal to the shared bridge** — it is specific to this strategy's (or its mode's) bracket computation. Narrows where to read code. Still **R3 決裁待ち**; unchanged since 08-05, now with two exactly-measured instances.

---

## 4. Realized book — +2 closed fills, −13.2 pip as booked (−38.7 pip at the broker)

| Metric | 09-04 | **09-07** | Δ |
|---|---|---|---|
| N | 581 | **583** | **+2** |
| W / L / BE | 253 / 293 / 35 | **254 / 294 / 35** | +1 / +1 / 0 |
| WR | 43.5% | **43.6%** | +0.1 |
| decided WR | 46.3% | **46.4%** | +0.1 |
| EV | −1.18 | **−1.20** | −0.02 |
| PnL | −685.6 | **−698.8 pip** | **−13.2** |
| Wilson lo / BF lo | 42.2 / 39.5 | **42.2 / 39.5** | flat |
| avg R | 0.12 | **0.11** | −0.01 |

⚠️ **The −13.2 is a netting artifact**: it is (+11.6 phantom win) + (−24.8 loss). The broker booked **−141 + −246 = −¥387 = −38.7 pip** on the same two trades. **True book delta is −38.7 pip, and true PnL is −724.5** (§2).

**Engine alive**: `shadow_count` 12,966→**13,046 (+80)**, `oanda_audit.total` 16,467→**16,562 (+95)**, `engine_tick_age_sec` **0.1s** (stalest mode `daytrade_1h_eur` 43.1s), **27 modes** (was 24), `main_loop_restarts` **1**, OANDA `ok` @ **86.8ms**.
**Today's live rate 0.0%** — 09-07 UTC audit slice **N=57, all `skipped`** (47 `shadow_tracking` / 10 `shadow_tracking(shadow_emit_no_lot)`), **0 sent / 0 filled / 0 blocked**. Firing: `london_breakout`(16) / `vix_carry_unwind`(8) / `dual_sr_bounce`(7) / `sr_anti_hunt_bounce`(6). Instruments: USD_JPY(23) / EUR_USD(18) / AUD_JPY(6) / GBP_USD(6). Units 1000×46 / 0×10 / 5000×1.
⚪ **Zero `agg_kelly` blocks today** (09-04 had 6). Over the full 600-row window the gate fired 40 times (`agg_kelly=-0.315<0` ×34, `-0.363<0` ×6) — consistent with the 09-03 correction that the gate is a **per-candidate filter, not a book-wide halt**.

---

## 5. Risk dashboard — still negative, n=16, and `attribution` friction is still unsubtracted

`effective_date_from` **2026-08-08T11:50:43**, n **16** (09-04: 14). Kelly edge **−0.1323** (was −0.1053), `recommended_fraction` **0.0**, WR 43.75%, odds 0.9834. Sharpe **−0.0470**, DSR **0.0 @ 100% haircut**, `is_significant: false`, `n_trials` 3.

⚠️ **`attribution` friction bug persists and is now maximally misleading**: reports `gross_pnl = net_pnl = alpha = **−22.4**` with `friction` **60.0** (`avg_friction_per_trade` 3.75) **never deducted** ⇒ **true 30d net is −82.4**, a **3.7× understatement**. Unchanged behaviour since 08-31; the absolute error has grown (49.1 → 60.0).

30d by instrument: **USD_JPY n=10 +41.8** (was n=8 +55.0 — the two new fills dragged it) / EUR_GBP n=4 −6.8 / AUD_JPY n=2 **−57.4**.
Per-strategy Kelly **3/3 or 2/3 `insufficient`**; only `usdjpy_carry_dip_accumulator` computes (DSR **0.3305**, Sharpe 0.1534 vs threshold 0.3005, z −0.439, **`is_significant: false`**, edge +0.2855, half_kelly 0.0909). ⚠️ **That DSR is computed on the uncorrected series containing the phantom win** (§2) — it is optimistic by construction.
**Correlation**: 3 strategies, matrix **all `null`** except the self-term; `flagged_pairs` empty ⇒ no usable correlation estimate at these N. Do not read "no flagged pairs" as diversification.
**DD**: `dd_pips` **1209.0** (09-04: 1195.8), `dd_pct` **0.0972** = **9.72% on the JPY-ledger basis** (`dd_jpy` ¥34,906.84 / base ¥359,109), eq_current **−1192.1** vs peak **+16.9 (flat)**, `defensive_mode` **true**, `lot_multiplier` **0.2**. 📌 Basis note retained: the legacy **$1000-pip** headline reads **120.9%** — a *different denominator*, not a contradiction; the JPY ledger is the one tied to the real account.
**Monte Carlo**: `ruin_probability` **0.0**, median final equity 5,717.07, `median_max_dd` **149.57**, `worst_case_dd_99` **346.41**, `small_sample_lowconf` **true**.

---

## 6. 🔴 `/api/oanda/equity` is lossy — and this run pins the leak to the yen

Last curve point still **2026-08-10T07:20:34Z**, `count` **928** flat ⇒ **28.19 days** stale (progression 15→18→21→22→23.5→24.2→25.2→**28.2 d**).

| | 09-04 | **09-07** | Δ |
|---|---|---|---|
| account `pl` | −¥82,204.23 | **−¥82,831.23** | −¥627.00 |
| curve `total_pl_jpy` | −80,686 | **−80,686** (frozen) | 0 |
| **unaccounted** | **−¥1,518.23** | **−¥2,145.23** | **−¥627.00** |

🟢 **Exact reconciliation.** The gap grew by **−¥627.00**, and the transaction ledger for the same interval sums to **exactly −¥627.00**: −141 (#709598 close) + −246 (#837947 stop) + −240 (3× SVK @ −80). **Zero residual.** The curve recorded **none** of it. This upgrades "lossy" from an inference to a closed measurement: **any analysis using this endpoint as the realized-PnL source is short ¥2,145 and rising.**

Broker NAV **¥276,922.59 → ¥276,304.30 = −¥618.28**, which is −627.00 realized **+¥8.72 financing** (account `financing` +26.53 cumulative) ✅.
⚠️ **JPY-ledger drift is a separate, pre-existing item**: ledger `eq_current_jpy` **¥324,381.63** vs NAV **¥276,304.30** = **¥48,077.33** (09-04: ¥47,591.05, **+¥486.28**). SVK explains ~¥960 of it (**~2.0%**); the remainder predates 09-01 and is **undiagnosed**.

---

## 7. 🟢 `rnb_usdjpy:direction_filter` — the 8-run 100%-rejection signature has BROKEN

Across 08-26 → 09-04 this log recorded eight consecutive readings of `rnb_usdjpy:direction_filter` blocking at ratio **0.9788–1.0008**, i.e. ~100% of the mode's ticks, and carried "read `compute_rnb_signal`" as the top open follow-up. This run, over **96 block keys / 7,853 blocks**:

- **`rnb_usdjpy:direction_filter` does not appear at all.**
- `rnb_usdjpy:no_signal` **1,516** and `rnb_usdjpy:conf<30` **2**, against `tick_counts.rnb_usdjpy` **2,661** ⇒ block ratio **0.570**, so **~43% of the mode's ticks now survive** (previously ~0%).

⚠️ **Not declared fixed.** Per `feedback_label_empirical_audit` a counter change is not a code reading: `direction_filter` may have been fixed, renamed, or removed, and `no_signal` is a *different* rejection. What is established is that **the pathological 100% form is gone** and the follow-up must be re-scoped from "why does it reject everything" to "what changed, and is `no_signal` at 57% the intended behaviour". 🔴 Note the mode still contributes **0 live fills**: whatever now survives `rnb_usdjpy` is absorbed downstream.

**Block families** (full basis, `/api/demo/block-counts`, 7,853 blocks / 96 keys — the `/api/demo/status` copy is still truncated to the top 30 per `modules/demo_trader.py:2061-2063`):
`r2_shadow_demoted_cell` **28.3%** (2,222) / `hedge_block` **25.9%** (2,032) / `no_signal` **19.3%** (1,516) / `order_bar_dedup` **18.5%** (1,451) / `gbp_asia_flash_crash` **4.3%** (337) / `score_gate` 1.6%.
Per-strategy: `ema_trend_scalp:r2_shadow_demoted_cell` **954**, `sr_channel_reversal:r2_shadow_demoted_cell` **370**, `dual_sr_bounce:order_bar_dedup` 327, `london_breakout:order_bar_dedup` 315.

---

## 8. ⚠️ `hedge_block` shadow→live suppression — 7th confirmation

`/api/demo/status.open_trades` = **2 rows, BOTH `is_shadow: 1`** (`dt_sr_channel_reversal` GBP_USD SELL @1.35374 / `trendline_sweep` EUR_GBP SELL @0.85842) against broker `open_trade_count` **0** / `marginUsed` **¥0** / `openPositionCount` **0**. The 08-28→09-02 + 09-04 signature reproduces; 09-03's break remains explained as the one run that held a real live position. Mechanism already code-verified at `modules/demo_trader.py:4915-4928` (iterates `_mode_inst_live + _mode_inst_shadow`, blocks regardless of layer). `hedge_block` is the **#2** blocker at 25.9%.

---

## 9. ✅ `status_volume_keeper` — behaving exactly as designed (no anomaly this run)

`/api/demo/status.status_volume_keeper`: `enabled`/`running` **true**, `rt_count` **12**, `volume_usd` **240,000 / target 520,000**, `behind_pace` **false**, `last_error` empty, `last_rt_at` **2026-09-07T02:25:20Z**, `last_skip_at` **11:55:20Z**, `last_skip_reason` **`outside_window(h=11)`**.

⚠️ **A false alarm was avoided here and is worth recording as method.** SVK ran three hourly round-trips (00:25:19, 01:25:19, 02:25:20 — tx 837953-837964, each USD_JPY +10,000u → immediate close at **−¥80.00** = 0.8 pip round-trip spread) and then **went silent for 9.5 h**. Read from the ledger alone that looks like a stalled module; `last_skip_reason` shows it is the **configured operating window** declining h=11. Per [[status-volume-keeper-2026-09-01]] (**user 決裁 09-01, 案 A**) this is an approved API-access cost, not an edge claim, and the "demo DB には一切書かない" design is why these trades are correctly absent from `oanda_audit`/`by_type`.
**Cost as measured**: 12 rt MTD = **¥960**; at target ≈ **¥2,080/month ≈ 0.75%/month** of the ¥276,304 balance, against an M2 target of **+0.5%/month** — a standing headwind that **exceeds the target**; verify M1/M2 basis is net of it. Guards unbreached (NAV floor <¥262,000; current ¥276,304.30 = **¥14,304.30 headroom**, down ¥618 from 09-04).

---

## 10. ⚠️ Learning API frozen for an 11th snapshot

`adjustments[0]` id=**93** (2026-08-19 14:25:37) unchanged ⇒ **18.90 days** stale. `current_params.entry_type_blacklist` still **`[]`** = **no-op unresolved since 06-30**, despite **12 of 30** logged adjustments being `entry_type_blacklist_add` for `sr_channel_reversal` ("WR 25.0%, EV −0.98 → 除外", repeated 06-11 → 08-19).

🔍 **Why the no-op has been harmless — now measured rather than assumed.** `sr_channel_reversal` fires **0 times** in the 10-day, 600-row audit window, and `block-counts` shows **`sr_channel_reversal:r2_shadow_demoted_cell` = 370**. ⇒ the strategy is suppressed by the **R2 demote path**, which masks the blacklist's failure entirely. The blacklist bug is real but currently **inert**; it would surface the moment R2 stopped demoting that cell. Recorded so the next run does not re-derive it.

Analysis unchanged bit-for-bit: daytrade EV **−2.60** (n=94, WR 41.5%, **all three conf bands negative**: high −1.12 / mid −4.07 / low −2.18) / scalp EV **−0.15** (n=388, WR 40.7%, only `low` **+0.48** non-negative) / swing `ready: false` (sample 0). `by_regime` daytrade: RANGE −1.59 (n=62) / TREND_BEAR −5.66 (n=22) / TREND_BULL −2.17 (n=10).

---

## 11. Regime snapshot

| Pair | Regime | ATR%ile 20d / 5d | ATR(14) | SMA20 slope | last |
|---|---|---|---|---|---|
| USD_JPY | **VOLATILE** | **75.9 / 86.2** | 1.126 | +0.00055 | **156.254** |
| EUR_JPY | VOLATILE | 65.5 / 87.9 | 1.027 | +0.00209 | 181.474 |
| GBP_JPY | VOLATILE | 65.5 / 86.2 | 1.306 | +0.00162 | 211.218 |
| EUR_USD | RANGING | 46.6 / 46.6 | 0.00462 | +0.00155 | 1.16138 |
| GBP_USD | RANGING | 36.2 / 34.5 | 0.00552 | +0.00107 | 1.35162 |

All three JPY crosses are VOLATILE with 5d ATR percentiles **86–88** — directly relevant to §3: a 24.6 pip stop on USD_JPY at ATR(14)=112.6 pip is **0.22× ATR**, and the 5d percentile says the current tape is at the top of its recent volatility range.

---

## 12. Lint results

1. **数値整合 ✅ (with one caveat)** — index.md / log.md / this file agree on N=583 / WR 43.6% / PnL −698.8 / DD 1209.0 after this run's updates. ⚠️ **The as-reported PnL is itself wrong by +25.7 pip** against the broker (§2); both figures are carried explicitly rather than silently reconciled.
2. **`tier_integrity_check --check` PASS** (exit 0). 2 known warnings (`hull_donchian_fade` QUICK_HARVEST_EXEMPT not in ELITE/PAIR_PROMOTED; no strategy file for `ob_retest`). 🔴 **The "legacy dead inline / no firing in 30+ days" INFO contradiction recurs — 4th instance.** `ob_retest` is listed as dead, but the audit window holds **2 rows on 2026-09-02T08:52:19Z/08:52:32Z (GBP_USD)** = **5.1 days ago**, and `block-counts` carries `unknown_type:ob_retest` **15**. Same root cause as `ihs_neckbreak` (08-26→08-28) and `ob_retest` (09-01, 09-02): **the checker reads local state, not the Render API.**
3. **`sync_kb_index --check` FAIL (exit 1)** → `--write` → re-check **PASS "index.md is in sync"**. ⚠️ Diff was **the date stamp only** (`auto-synced, 2026-09-06` → `2026-09-07`); portfolio composition **unchanged**. Known JST/UTC cosmetic, no content movement.
4. **Broken wikilinks** (path-aware resolver — resolves `a/b` forms against the file's own directory, the `wiki/` root, and bare stem; 2,042 files): **`wiki/index.md` = 7**, the **identical set** to 08-23/08-26/08-28/08-31/09-01/09-02 (`2026-04-27`, `2026-04-29`, `ma-regime-switch`, `ma-trend-perfect`, `mqe-gbpusd-fix`, `ob-retest`, `vsg-jpy-reversal`; all 7 re-confirmed absent on disk). **`wiki/strategies/` non-MEMORY = 1** (`xs-momentum-rsi-tv-phase2-2026-05-13`, in `macd-rsi-pullback.md`) — also unchanged. KB-wide **121**, of which **56 MEMORY-format** ⇒ **in-KB 65**. ⚠️ **The KB-wide figure is not comparable to 09-02's "76 / in-KB 47"**: resolver details dominate that number (a stem-only first pass in this run returned 379 / index.md 21 before path resolution was added). The two anchors that *are* methodologically stable — index.md=7 and strategies=1, both identical sets — reproduce exactly. **This run introduced 0 new broken links.**
5. **Staleness** — 🟢 audit current (`last_trade_row_at` 2026-09-07 10:35:00Z = **1.38 h**, `last_candidate_row_at` 11:45:21Z = **12.7 min**, both `status: ok`); 🔴 **equity 28.19 d** *and lossy* (§6); 🔴 **learner 18.90 d** (§10); 🟢 `tier-master.json` in sync with the index portfolio stamp.
6. **Repo state** — HEAD **`8ddbe749`**, `main...origin/main` **[ahead 1, behind 1]** 🟢 (09-04: ahead 1 / **behind 12** — improved; `project_fxai_main_stranded_staged_work_2026_09_01` is partly relieved but **not resolved**). No rebase/merge in progress. ⚠️ `git stash list` = **180** (09-04: 178, +2). No competing Claude/Codex process on the repo at write time (only the Discord responder and chroma-mcp). Per `feedback_concurrent_agent_repo_hazard` **no history surgery attempted**; this run only writes files and does **not** commit.

---

## 13. Open items (ranked)

| # | Item | State |
|---|---|---|
| 1 | 🔴🔴 **Weekend `MARKET_HALTED` retry storm** — 64,170 attempts / 47h, no backoff, no breaker; **broker API-access risk**; recurs on any position open at Friday close | **NEW**, code unread |
| 2 | 🔴🔴 **Demo↔broker sign inversion (#709598, 25.7 pip)** — estimator reports a win on a losing trade; full ledger reconciliation of all 11 `carry_dip` fills outstanding | **NEW** |
| 3 | 🔴 **`carry_dip` SL/TP substitution** — R:R inverted 0.53→~2.3, SL 0.16–0.19× declared, now 4 instances; scope narrowed **off** the shared `daytrade_1h_*` bridge (§3) | **R3 決裁待ち** since 08-05 |
| 4 | 🔴 **`/api/oanda/equity` lossy** — ¥2,145.23 unaccounted, leak pinned to the yen; unusable as PnL source | measured, unfixed |
| 5 | 🔴 **`attribution` friction unsubtracted** — 30d net reads −22.4, true −82.4 (3.7×) | unfixed since 08-31 |
| 6 | ⚠️ **JPY-ledger drift ¥48,077** — SVK explains ~2%; remainder predates 09-01 | undiagnosed |
| 7 | ⚠️ **`price_shock_rev_aud_jpy_h1_long` rejection trigger fired** (N=4, EV −45.0, both WF halves negative) — no demote executed (needs N=15/N≥10) | **user adjudication** |
| 8 | ⚠️ **`hedge_block` counts shadow against live** — 7th confirmation, #2 blocker at 25.9% | code-verified, unfixed |
| 9 | ⚠️ **Learner frozen 18.9 d / blacklist no-op** — currently inert, masked by R2 demote (§10) | re-scoped |
| 10 | ⚠️ **`tier_integrity_check` reads local state** — 4th "dead strategy that fired 5 days ago" | unfixed |
| 11 | 🟢 **`rnb_usdjpy` 100% signature broken** — re-scope from "why reject all" to "what changed" | **re-scoped** |
| 12 | ⚠️ `git stash list` 180 | not investigated |
