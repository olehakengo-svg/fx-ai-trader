# Trade Monitor Alerts: 2026-07-24

## Alert: 01:21 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=224

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=224` が突出して高い**のは、`rnb_usdjpy` モードが現在の相場方向（おそらく上昇トレンド）に対して**逆方向シグナルを大量生成し続けているが、direction_filterが全てブロックしている**状態
- カウント224は他のblock_counts（最大85）と比較して**2〜6倍以上**であり、フィルターが正常に機能しているというより、**シグナルロジック自体が方向判定と恒常的に乖離している**可能性が高い
- 時刻が01:17〜01:21（アジア時間早朝）であり、**流動性が低い時間帯特有のノイズシグナルを大量生成している**可能性もある

### 推測（中確度）

- `rnb_usdjpy` の方向判定ロジック（トレンドフィルター参照元：EMA/MA系か）が**固定値または古いキャッシュを参照しており、現在価格と乖離した方向を出し続けている**
- `daytrade`の`last_signal.confidence=24`・`scalp:conf<30`が散見されることから、**全体的にシグナル品質が低下しており、USDJPYの相場環境がシステムの想定レンジ外にある**可能性

---

## 確認事項（要調査）

1. **`rnb_usdjpy` モードの現在の内部direction判定値**（BUY/SELL固定になっていないか）
2. **direction_filterの参照先インジケーター（EMA等）の最終更新時刻**（キャッシュ固着・データフィード断がないか）
3. **224カウントの発生開始タイミング**（特定のバー起点か、徐々に積み上がっているか）
4. **`main_loop_restarts=1` の再起動タイミング**とdirection_filter急増のタイミングが一致しているか（再起動後に状態が不整合になった可能性）
5. `rnb_usdjpy` モードが他のモードと**方向フィルターを共有しているか、独立しているか**

---

## 対処方針

- **即時対応**
  - `rnb_usdjpy` モードを一時的に停止し、他モードへの波及・リソース圧迫がないか確認する
  - direction_filterのキャッシュ・状態変数を強制リセットして再評価させる

- **調査対応**
  - `main_loop_restarts=1` 後の状態復元処理を精査し、directionフラグが再起動前の

## Alert: 04:56 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=239

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:direction_filter=239` の異常値**は、RnB（レンジ＆ブレイクアウト）戦略のUSDJPYにおいて、方向性フィルターが繰り返しシグナルを棄却し続けていることを示す
- 現在のUSDJPYが**強いトレンド方向に固定**されており（163円台の高値圏）、RnBロジックが想定する「逆方向エントリー」や「レンジ内反転」が全て方向フィルターに弾かれている状態と推測
- セッション継続中に同一バー・同一方向の判定が蓄積するループ構造になっている可能性がある（インターバルごとに毎回評価→毎回ブロック）

### 推測（中確度）

- `direction_filter`のリセットタイミングが**日次・セッション単位でなくプロセス起動時のみ**になっており、カウンターが累積し続けている設計上の問題の可能性
- USDJPYが一方向に動き続けているため、RnBの内部バイアス判定が**長時間同一方向に固着**している

---

## 確認事項

| # | 確認内容 | 目的 |
|---|---------|------|
| 1 | `rnb_usdjpy`の`direction_filter`のリセット条件・タイミング | カウンターが意図通りクリアされているか |
| 2 | `direction_filter`が参照している上位足（D1/H4）のバイアス値 | フィルターがどの方向に固着しているか |
| 3 | `block_counts`のリセットがプロセス再起動のみかどうか | `main_loop_restarts=1`と照合し、239回がどの時間幅で蓄積したか特定 |
| 4 | RnBがシグナルを出している方向（BUY/SELL）と、フィルターが許可している方向の不一致ログ | 逆張りシグナルが連続生成されているのか確認 |
| 5 | 他のUSDJPY系戦略（daytrade, daytrade_1hとも`signal=WAIT`）と比較し、RnBだけが逆シグナルを出し続けていないか | 戦略間の方向判定の乖離確認 |

---

## 対処方針

- **即時対応不要**（`emergency_killed=false`、他戦略は正常稼働中）だが放置はリスク
- `direction_filter`の**カウンターリセット条件をセッション/バー単位に見直す**こと

## Alert: 17:18 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:direction_filter=314, daytrade_eurgbp:hedge_block=206

### 診断
## 原因分析

### 【推測】

**① rnb_usdjpy:direction_filter=314（突出して高い）**
- RNB戦略のdirection_filterが他の通常ブロック系カウントと比較して桁違いに多い
- USDJPYが長時間にわたり方向性フィルタの条件を満たし続けている可能性が高い
- 具体的には「D1またはH4のトレンド方向とシグナル方向が不一致」な状態が固着していると推測
- ログの`d1=0 h4=1 vol=squeeze`（MTF_MONITOR）がヒント：D1が中立/逆向きでH4は上方向という乖離が続いている

**② daytrade_eurgbp:hedge_block=206（他のhedge_blockと比べ突出）**
- daytrade_eur:hedge_block=189、daytrade:hedge_block=166と比較してもEURGBPは高水準
- EURGBPで既存ポジションとの方向相殺（ヘッジ関係）が長期間解消されていないと推測
- `eurgbp_daily_mr blocked at: conf<30(was:25)` がログに繰り返し出現しており、そもそもシグナル自体もconf=25と低い状態でヘッジブロック以前に弱い

---

### 【確認が必要な事項】

**rnb_usdjpy:direction_filter について**
- [ ] direction_filterの判定ロジックで参照しているD1足の方向判定値（現在の実値）を確認
- [ ] カウントがリセットされる条件（セッション区切り・ポジションクローズ等）が正常に機能しているか確認
- [ ] カウントの集計方法が「累積」なのか「直近N件」なのかを確認（設計上の意図と一致しているか）
- [ ] vol=squeezeが長時間継続しているか、ボラティリティ環境の変化を確認

**daytrade_eurgbp:hedge_block について**
- [ ] 現在EURGBPまたは相関通貨ペアでオープンポジションが存在するか確認
- [ ] ヘッジ判定に使われている通貨エクスポージャーの計算が正確か確認（EUR建てポジションとの干渉を疑う）
- [ ] `main_loop_restarts=1` が発生したタイミングとhedge_blockカウント急増のタイミングが一致していないか確認（再起動後にヘッジ状態の引継ぎが正常に行われたか）

---

## 対処方針

**即時対応（確認ベース）**

- rnb_usdjpyのdirection
