# Trade Monitor Alerts: 2026-09-22

## Alert: 02:05 UTC

### 検出された問題
- 🚨 STATUS API取得失敗: The read operation timed out
- ⚠️ **直近4時間トレード0件** かつ全モードWAIT — 発火条件を満たしていないか、フィルターで全遮断の可能性

### 診断
## 原因分析

### 推測（確認前の仮説）

**APIタイムアウト起因の情報欠損**
- STATUS APIがタイムアウトしているため、ステータス・ログともに実態が不明
- ログが空配列`[]`なのは「正常に空」ではなく「取得自体が失敗している」可能性が高い
- トレード0件・全モードWAITは、システム停止または凍結状態を示唆

**考えられる根本原因（優先順）**
1. トレードエンジンプロセスのハング／クラッシュ → APIサーバーが応答不能
2. ネットワーク経路の障害（監視サーバー↔トレードサーバー間）
3. APIサーバーのスレッド枯渇・デッドロック
4. ブローカーAPIへの接続断がトレードエンジン全体を道連れにしている

---

### 確認事項（実態把握のために必須）

- [ ] トレードサーバー自体のプロセス生存確認（`ps`/サービス管理ツール）
- [ ] APIサーバーのポート疎通確認（`telnet`/`nc`）
- [ ] サーバーリソース確認（CPU・メモリ・FD枯渇）
- [ ] OSレベルのシステムログ（`/var/log/syslog`等）にOOM・パニックの痕跡
- [ ] ブローカーAPIの接続状態・認証トークンの有効期限
- [ ] 監視サーバー側のタイムアウト設定値（過短設定の誤検知可能性）

---

## 対処方針

**即時対応（影響局所化）**
- トレードエンジンプロセスの再起動を検討（ただし未約定ポジション確認を先行すること）
- 手動でブローカー画面を確認し、未決済ポジションの有無を把握してリスクを把握

**調査優先**
- APIサーバーに対してヘルスチェックエンドポイントへ直接アクセスし、タイムアウト箇所を特定
- ネットワーク障害か、プロセス障害かを切り分けることが最優先

**再発防止**
- APIタイムアウト発生時のアラートと自動再起動フローの整備
- ログをAPIとは独立したストレージ（ファイル・外部ログ基盤）に出力する構成に変更
- ヘルスチェックの多重化（APIポーリング以外の死活監視を追加）

---

**⚠️ 最重要:** ポジションを保有している可能性がゼロ

## Alert: 08:51 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=246

### 診断
## 原因分析

### 推測（高確度）

- **`rnb_usdjpy:no_signal=246` が異常値の本質**
  - `rnb_usdjpy` モードがシグナル生成ロジックに到達しているにもかかわらず、エントリー条件を満たすシグナルが全く出ない状態が大量に継続している
  - 246回という数値は他の `no_signal` 系カウントが存在しないことと比較して突出しており、モード固有の問題である可能性が高い
  - RnB（レンジアンドブレイク）系戦略の性質上、**現在の市場がレンジ・ブレイク条件を満たせない相場環境**（トレンド相場、またはボラティリティ過多/過少）に陥っている可能性が最も高い

- **`rnb_usdjpy:order_bar_dedup=7` が併存**
  - シグナルがゼロではなく、まれに発火しているがほぼ全てが `no_signal` で終わっている → ロジック自体は動いているが判定で弾かれ続けている

- **他モードの `ORDER_BAR_DEDUP` 多発**
  - 同一バーからの重複発火が全モードで多い → エンジン側の問題ではなく、**各ストラテジーのシグナル発生頻度が高い相場**（ロンドン時間開始直後の08:45〜08:50バー集中）に起因する正常動作の範囲内と推測

---

### 確認事項（要調査）

1. **`rnb_usdjpy` のシグナル判定ロジック側パラメータ確認**
   - レンジ幅の閾値、ブレイク判定の閾値が現在のUSDJPY値動き幅と乖離していないか
   - フィルター条件（ATR・スプレッド・時間帯フィルター等）が過剰に厳しくなっていないか

2. **`rnb_usdjpy` の設定変更・デプロイ履歴**
   - 直近でパラメータ変更やモデル差し替えが行われていないか
   - `no_signal` カウントの急増がいつから始まったか（今日初めてか、数日前からか）

3. **現在のUSDJPY市場環境**
   - 強いトレンド相場またはフラッシュクラッシュ後の膠着状態になっていないか
   - ボラティリティ指標（ATR等）がRnBの想定レンジ外に出ていないか

4. **`daytrade_1h_nzdusd` のスタール（

## Alert: 14:13 UTC

### 検出された問題
- ⚠️ **block_counts異常** — rnb_usdjpy:no_signal=284

### 診断
## 原因分析

### 【推測】rnb_usdjpy:no_signal=284 の主因

- `rnb_usdjpy` モードが信号生成ロジックの入口で継続的に `no_signal` を返しており、**シグナル条件を一切満たせない状態が長時間続いている**
- 284カウントは他の `block_counts` と性質が異なる。他は「シグナルは出たが後段フィルタで遮断」であるのに対し、`no_signal` は**シグナル生成そのものが0件**であることを示す
- USD/JPY 自体はログ上で `R2_SHADOW_DEMOTE`（engulfing_bb、fib_reversal）が頻発しており、同通貨ペアへのシグナルが後段でも軒並み抑制されている状況と整合する

### 【推測】副次的要因

- `daytrade_1h_usdchf:r2_shadow_demoted_cell=107`、`scalp:r2_shadow_demoted_cell=160` など、USD絡み・JPY絡みのR2シャドウデモートが全体的に高水準 → **USD/JPYの相場レンジ状態またはボラティリティ構造の変化**がシグナル条件を潰している可能性
- `engine_tick_stalest_age_sec=49.7`（最遅: daytrade_1h_euraud）は軽微だが、1hモードのティック遅延が連鎖して評価タイミングにズレを生じさせている可能性

---

## 確認事項

### 必須確認（緊急度：高）

- [ ] `rnb_usdjpy` モードの**シグナル生成ロジック入口のフィルタ条件**（ボラティリティ閾値・トレンドフィルタ・時間帯フィルタ等）を確認し、現在の相場環境で構造的に通過不能になっていないか
- [ ] USD/JPY の**現在のATR・スプレッド・レンジ幅**が `rnb`（レンジブレイク系と推定）の発火条件を下回っていないか
- [ ] 284カウントの**発生開始時刻**を特定し、特定イベント（指標発表・流動性枯渇・週明けギャップ等）と一致するか照合

### 追加確認（緊急度：中）

- [ ] `rnb_usdjpy` モードに固有の**パラメータ変更・デプロイ履歴**が直近にないか
- [ ] `daytrade_1h_euraud` のティック遅延50秒が、USD/JPY評価サイクルに影響を与えるアーキテクチ
